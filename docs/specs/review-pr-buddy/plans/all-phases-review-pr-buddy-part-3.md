---
kind: implementation-plan
feature_id: review-pr-buddy
status: Complete
created: 2026-06-08
phases: [3, 4]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-08.
> To execute: `/execute-plan <this file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 3 of 3. Sibling parts:
> - part 1: `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy\plans\all-phases-review-pr-buddy-part-1.md`
> - part 2: `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy\plans\all-phases-review-pr-buddy-part-2.md`
> - part 3: `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy\plans\all-phases-review-pr-buddy-part-3.md` (this file)
>
> Execute parts strictly in order. Parts 1 (shared protocol) and 2 (reuse stage wired into `review-pr.md`) MUST be complete first — the buddy delegates to the `review-pr` pipeline and surfaces the reuse findings produced by Part 2. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — review-pr-buddy (Phase 3: Buddy Command + Phase 4: Integration & Docs)  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy\PHASES.md` (review-pr-buddy — this part covers Phases 3 and 4)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy\SPEC.md`

**Total phases:** 2 (Phases 3 and 4 of 4)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates + smoke tests, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL command/agent markdown + plugin config (`plugin.json`, `README.md`, `CLAUDE.md`) | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit`/`Write` on deliverable files. Compose `Agent({ model: "sonnet" })` calls. The ONLY files you may modify directly: `PHASES.md`, `work-log.jsonl`. (Note: the plugin's `CLAUDE.md` is a *deliverable* here — Phase 4 WU-4 edits it via a subagent. The "you may edit CLAUDE.md directly" carve-out refers only to tracking-doc CLAUDE.md edits in the claude-md-review post-step; the Phase 4 content edit goes through a subagent.)

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL
Before each step, `Read` the listed component files from disk. After compaction, re-read this plan first.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |
| Final | Work Log | `~/.claude/skills/_components/work-log.md` |

## References — Upstream Artifacts
(none — no completed hard upstream deps. Intra-feature prerequisites: Parts 1 and 2.)

## MANDATORY RULES — DO NOT SKIP ANY STEP
1. **ALL authoring delegated to Sonnet subagents** (exception: trivial PASS-WITH-FIXES).
2. No worktrees for subagents.
3. TDD gate honored (this part has no TDD work units — buddy command + docs are markdown; verified by structure + smoke test).
4. PHASES.md updated after each batch.
5. Output reviewed before continuing.
6. Mistakes fixed immediately.
7. Integration verification after all batches in each phase.
8. CLAUDE.md updated if warranted.
9. Each phase committed before the next.
10. Self-contained.
11. Read listed components before each step.

### CRITICAL CONTEXT — buddy design constraints (EMBED IN EVERY SUBAGENT PROMPT)
- Plugin root: `C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review\` (symlinked into the marketplace; edit the real path).
- **Phase 0 = delegation, not duplication** (resolves SPEC Open Question #1): the buddy's non-interactive prep MUST reference/invoke the existing `review-pr.md` pipeline step bodies — do NOT copy them. `review-pr.md` stays the single source of pipeline truth. Phase 0 runs prep → journey → triage → planner-validation → reuse stage + investigation + sweep → aggregate → post-process; when it finishes, the journey file + `processed-findings.json` (including reuse findings from Part 2) are on disk.
- **Phase 1 = interactive walk** along the journey's **Manual Review Guide** (template lives at `agents/journey-planner.md`, the `## Manual Review Guide` section with `### Step N: {Group}` / `Files:` / `What to look for:` / `Key questions:`). Per chunk: (1) Teach what/why/how; (2) Surface pre-computed findings scoped to the chunk's files (investigation + sweep + reuse flags); (3) Socratic prompt (the journey's Key questions + the buddy's own); (4) Capture verdict via `AskUserQuestion` — keep / dismiss(+note) / will-comment / add-own; (5) checkpoint to `{cacheDir}/buddy-session.json`; (6) advance. Reviewer may interrupt to dig in, open a file, or revisit a prior chunk.
- **Cache boundary:** Phase 1 reads cached diffs/files for teaching (cache-bound like the journey). If the reviewer asks to open an UNCHANGED local file, that's an investigation-style carve-out (local-codebase-on-main read), NOT sweep's cache-only rule.
- **Phase 2 = human-curated synthesis:** produce `PR-{id}.md` in the synthesizer-v2 format/location containing ONLY kept findings + the reviewer's own observations + will-comment notes. Do NOT run the autonomous synthesizer as a competitor — the session IS the synthesis. The journey file + `REVIEWED.md` sentinel behavior are unchanged from `review-pr`.
- `.editorconfig`: tabs, CRLF.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | review-pr-buddy | P3 | `review-pr-buddy` Command | Parts 1 & 2 | Solo |
| 2 | review-pr-buddy | P4 | Integration + Docs | P3 (docs document the buddy) | Batch of 3 parallel doc WUs |

## Per-Phase Plans

### Phase: review-pr-buddy P3 — `review-pr-buddy` Command

**Goal:** Author the interactive senior-architect pair-review command: non-interactive Phase 0 prep delegation, interactive Phase 1 journey walk, human-curated Phase 2 synthesis, with session-state + compaction recovery.

**Entry criteria:** Parts 1 and 2 complete — the reuse stage is wired into `review-pr.md` and emits `processed-findings.json` reuse entries.

**SPEC.md references:** § User Experience "/cognito-pr-review:review-pr-buddy" (Phases 0/1/2); § Technical Design "review-pr-buddy command"; PHASES.md Phase 3; Validation Criteria rows for buddy pre-compute / walk / per-chunk verdicts / curated doc.

#### Work Units

**WU-1 (P3) — Author `commands/review-pr-buddy.md`**
- **Scope:** All Phase 3 deliverables (command shell + Phase 0 delegation + Phase 1 walk + cache-boundary + session-state + Phase 2 curated synthesis). This is one cohesive new command file; per the file-overlap rule it is a single work unit.
- **TDD:** no (orchestration markdown; verified by Phase 4 smoke test).
- **Files to create/modify:** `commands/review-pr-buddy.md` [VERIFY: test ! -f "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/commands/review-pr-buddy.md" && echo "net-new, good"] (net-new).
- **Implementation goal:** Author the command per the CRITICAL CONTEXT design. Specifics:
  - Argument parsing mirrors `review-pr` (PR id / `local` / aspects) — reference `commands/review-pr.md` [VERIFY: test -f "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/commands/review-pr.md"] for the exact arg shape.
  - **Phase 0:** delegate to `review-pr.md` steps (reference the step bodies; do NOT copy). On completion, announce prep done and the walk starting.
  - **Phase 1:** drive the journey's Manual Review Guide from `agents/journey-planner.md` [VERIFY: grep -n "## Manual Review Guide" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/agents/journey-planner.md"]; implement the 6-step per-chunk loop (Teach / Surface / Socratic / Capture via `AskUserQuestion` / Checkpoint / Advance); surface findings scoped to the chunk's files from `processed-findings.json` (investigation + sweep + the `source:"reuse"` flags from Part 2).
  - **Session state:** define and write `{cacheDir}/buddy-session.json` recording per-chunk progress + per-finding dispositions (keep / dismiss+note / will-comment / add-own); Task tools track Phase 0/1/2; a mid-walk compaction must resume at the correct chunk by reading this file.
  - **Cache boundary:** cache-bound teaching reads; reviewer-requested unchanged-file opens use the investigation carve-out — mirror the access wording from `agents/investigation.md` [VERIFY: grep -n "local codebase\|main" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/agents/investigation.md"].
  - **Phase 2:** synthesize `PR-{id}.md` in the synthesizer-v2 format — reference `agents/synthesizer-v2.md` output template [VERIFY: grep -n "# Cognito PR Review" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/agents/synthesizer-v2.md"] including the "Reuse & Duplication" section name added in Part 2 [VERIFY: grep -n "Reuse" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/agents/synthesizer-v2.md"]; include ONLY kept findings + reviewer observations + will-comment notes. Preserve `review-pr`'s journey-file + `REVIEWED.md` sentinel behavior [VERIFY: grep -n "REVIEWED.md" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/commands/review-pr.md"].
  - Add command frontmatter consistent with the other `commands/*.md` in the plugin (name, description, argument-hint).
- **Spec requirements:** SPEC § User Experience (all of the buddy section) + § Technical Design "review-pr-buddy command".
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | None (net-new file) |

### Phase: review-pr-buddy P4 — Integration + Docs

**Goal:** Document the buddy command + reuse stage in plugin metadata; end-to-end + plugin-reload smoke tests.

**Entry criteria:** P3 complete — the buddy command exists and the reuse stage (Part 2) is wired.

**SPEC.md references:** § Implementation Phases item 4; PHASES.md Phase 4.

#### Work Units

**WU-2 (P4) — Bump `plugin.json`**
- **Scope:** Version bump + description update. **TDD:** no.
- **Files to create/modify:** `.claude-plugin/plugin.json` [VERIFY: test -f "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/.claude-plugin/plugin.json"] (currently v2.5.0).
- **Implementation goal:** Bump the version (minor bump for the new command + stage) and update the description to mention `review-pr-buddy` and the reuse-candidacy stage.
- **Batch:** 1 (of Phase 4).

**WU-3 (P4) — Update `README.md`**
- **Scope:** Document the buddy command + reuse stage. **TDD:** no.
- **Files to create/modify:** `README.md` [VERIFY: grep -n "prep\|journey\|investigation" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/README.md"].
- **Implementation goal:** Document `/cognito-pr-review:review-pr-buddy` (arguments, Phase 0/1/2 behavior); update the pipeline diagram to show the reuse-candidacy stage dispatched in parallel with investigation+sweep.
- **Batch:** 1.

**WU-4 (P4) — Update plugin `CLAUDE.md`**
- **Scope:** Pipeline description + editing notes. **TDD:** no.
- **Files to create/modify:** `CLAUDE.md` (plugin root) [VERIFY: test -f "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/CLAUDE.md"].
- **Implementation goal:** Add the buddy command to the v2 pipeline description; document the reuse agent + the shared protocol path + the reuse verdict→severity mapping (as a `/learn-from-pr` tunable); update the pipeline step list to include the reuse-candidacy stage.
- **Batch:** 1.

**Manifest note (orchestrator, not a WU):** `manifest.psd1` already carries the `cognito-pr-review` plugin symlink (added in a prior session). Phase 4 only VERIFIES it (`pwsh -File claude-config/setup.ps1 check`); it does NOT recreate it.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-2, WU-3, WU-4 | Yes (3 distinct files) | None |

## Execution Protocol

### Phase Selection Loop
Execute P3 (Batch 1 → post-phase → commit), then P4 (Batch 1 → post-phase → commit + smoke tests), then Completion.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)
Read `~/.claude/skills/_components/task-tracking.md`; load task tools; create a task per WU; update through the run.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read PHASES.md (Phases 3 & 4 + Validated Assumptions), SPEC.md (§ User Experience, § Technical Design), this plan, and the CRITICAL CONTEXT block.

#### Step B.1: Launch Subagents (COMPOSE Agent CALLS — ZERO INLINE AUTHORING)
**PRE-FLIGHT:** all edits via `Agent({ model: "sonnet" })`.
Read: `~/.claude/skills/_components/tdd-protocol.md` (verdict: no TDD WUs here), `~/.claude/skills/_components/subagent-launch.md`, `~/.claude/skills/_components/tdd-test-agent.md`, `~/.claude/skills/_components/implementation-agent.md`. Every subagent prompt embeds: the CRITICAL CONTEXT block, the relevant SPEC excerpt, the WU scope + verified paths + `[VERIFY]` anchors, and "You do not run `git commit`/`git push`/`git add`."

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md` + `~/.claude/skills/_components/mount-site-verification.md`. For WU-1 (the buddy command — a new file), mount-site verification MUST confirm the command is discoverable (correct `commands/` location + frontmatter matching sibling commands). Verify the buddy delegates to (does not copy) `review-pr.md`, defines the `buddy-session.json` schema, and curates (does not auto-synthesize) the final doc.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `~/.claude/skills/_components/phases-update.md`. Check off Phase 3 / Phase 4 deliverables; Implementation Notes record the buddy-session.json schema and the final artifact path.

#### Step B.4: Run Quality Gates (MANDATORY)
Read `~/.claude/skills/_components/quality-gates.md`. Gates:
- `python ~/.claude/scripts/lint-skills.py` exit 0; `python ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities` 0 errors (the buddy command + doc edits must not introduce broken `!cat`/capability drift).
- `plugin.json` remains valid JSON.
- (P4) plugin-reload + end-to-end smoke tests — see Completion.

#### Step B.5: Commit Batch
Read the commit policy (`.claude/skill-config/commit-policy.md` if present; else `~/.claude/skills/_components/commit-and-push.md`). `claude-config` is personal; honor the policy on push.

#### Step B.6: Proceed
Checklist: review verdict; PHASES.md updated; gates green; commit done. Then next batch/phase.

### Post-Phase Steps (after each phase's batches)

#### Integration Verification (MANDATORY)
Read `~/.claude/skills/_components/integration-verification.md`.
- After P3: confirm Phase 0 produces the journey + `processed-findings.json` BEFORE any Phase 1 prompt; each Phase 1 chunk maps to a journey Manual Review Guide step; `buddy-session.json` records dispositions and a simulated resume returns to the right chunk; the curated `PR-{id}.md` contains only kept findings + observations.
- After P4: confirm docs accurately describe the shipped behavior; `setup.ps1 check` reports the plugin symlink OK.

#### Update CLAUDE.md Files (MANDATORY)
Read `~/.claude/skills/_components/claude-md-review.md`. (P4's WU-4 already edits the plugin CLAUDE.md; this step double-checks the claude-config-level docs too.)

#### Commit and Push Post-Phase Changes
Per the resolved commit policy.

## Blocking Issue Protocol
If blocked: stop, commit completed batches, print **Completed batches** (hashes) / **Blocked WU** / **Reason** / **Recovery suggestion** / **Remaining WUs**. Do not work around. Blocking issues include: subagent failure unfixable after 2 retries; a gate failure unfixable after 2 retries; the buddy's Phase 0 delegation not resolving the `review-pr.md` steps (would force a copy — escalate rather than duplicate the pipeline); plugin reload not surfacing the new command.

## Completion
When Phases 3 & 4 are complete:
1. **End-to-end smoke test (MANDATORY):** on a real PR that adds duplicative code — (a) run `/cognito-pr-review:review-pr` → review doc contains a "Reuse & Duplication" section with a cited `file:line` candidate; (b) run `/cognito-pr-review:review-pr-buddy` on the same PR → Phase 0 pre-computes (journey + processed-findings on disk), Phase 1 presents chunks with scoped findings, Phase 2 produces a curated `PR-{id}.md`.
2. **Plugin-reload smoke test:** after a Claude Code restart, confirm both `/cognito-pr-review:review-pr` and `/cognito-pr-review:review-pr-buddy` appear and are argument-complete.
3. Print a completion report:

   ## Implementation Batch — Complete
   **Feature implemented:** review-pr-buddy (all 4 phases across 3 parts)
   **Phases completed this part:** 3, 4
   **Commits:** (hashes)
   **Final gate status:** lint/projection green; plugin valid; smoke tests pass
   **Implementation Notes summary:** (buddy-session schema, reuse verdict→severity tunables, any deferred tuning)

## Append to Work Log (MANDATORY — DO NOT SKIP)
Read `~/.claude/skills/_components/work-log.md` and call `interview_work_log_append` (skill=`implement-phase`/`execute-plan`, project=`claude-config`, title, summary, files_modified, technical_context covering the interactive-walkthrough orchestration, compaction-recovery checkpointing via buddy-session.json, and the delegation-not-duplication design that keeps review-pr.md the single pipeline source of truth).
