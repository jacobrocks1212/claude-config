---
kind: implementation-plan
feature_id: review-pr-buddy
status: Complete
created: 2026-06-08
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-08.
> To execute: `/execute-plan <this file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 3. Sibling parts:
> - part 1: `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy\plans\all-phases-review-pr-buddy-part-1.md` (this file)
> - part 2: `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy\plans\all-phases-review-pr-buddy-part-2.md`
> - part 3: `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy\plans\all-phases-review-pr-buddy-part-3.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — review-pr-buddy (Phase 1: Shared Reuse-Discovery Protocol)  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy\PHASES.md` (review-pr-buddy, 4 phases — this part covers Phase 1)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy\SPEC.md`

**Total phases:** 1 (of 4 — this part)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL content/source/test files | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on the deliverable files below. The deliverables of this part are MARKDOWN component/config files — they are authored by Sonnet subagents, not by you. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`, `work-log.jsonl`.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |
| Final | Work Log | `~/.claude/skills/_components/work-log.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies; SPEC `**Depends on:** (none)`.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL authoring work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on the deliverable files. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines).
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline. (This part has no TDD work units — all deliverables are markdown components verified by lint/projection scripts. Honor the TDD gate in `tdd-protocol.md` anyway.)
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's output is reviewed for correctness and spec alignment before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in a phase finish, integration verification confirms all changes work together.
8. Relevant CLAUDE.md files are created/updated after the phase if changes warrant it.
9. The completed phase is committed before the next part begins.
10. This plan is self-contained — follow it exactly as written without relying on external context.
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.

### CRITICAL CONTEXT — claude-config symlink + editing rules (READ BEFORE ANY EDIT)

This plan edits files in the **`claude-config`** repo (`C:\Users\JacobMadsen\source\repos\claude-config`), NOT the Cognito Forms repo. Key facts the executing subagents MUST know:

- The live skill/component locations (`~/.claude/skills/...`) are **symlinks** into `claude-config/user/...`. The Edit/Write tools **refuse to write through symlinks** — subagents MUST edit the **real targets** under `claude-config/` (e.g. `claude-config/user/skills/_components/...`, `claude-config/repos/cognito-forms/.claude/skill-config/...`), never the `~/.claude/...` symlink path.
- `project-skills.py` **recursively expands** all `!cat` component references, so a `!cat` of the new shared protocol nested inside `reuse-first-discovery.md` will be expanded transitively — this is the intended mechanism.
- Follow `.editorconfig`: tabs for indentation, CRLF line endings (matches the repo).
- Commit changes in the `claude-config` repo (these files are NOT tracked by the Cognito Forms repo).

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | review-pr-buddy | P1 | Shared Reuse-Discovery Protocol | — | Solo (2 sequential batches) |

## Per-Phase Plans

### Phase: review-pr-buddy P1 — Shared Reuse-Discovery Protocol

**Goal:** Extract the codebase-neutral reuse-discovery protocol core into one shared component file, and refactor the Cognito `/spec` reuse component to consume it — one source of truth, no drift.

**Entry criteria:** None (first phase).

**SPEC.md references:** § Technical Design "Shared reuse-discovery protocol"; § Reuse Ledger row "Reuse-discovery protocol" (verdict: refactor); PHASES.md Phase 1.

#### Work Units

**WU-1 — Create the shared protocol core (net-new)**
- **Scope:** PHASES.md Phase 1 deliverable: "Create `user/skills/_components/reuse-discovery-protocol.md` — codebase-neutral protocol core."
- **TDD:** no (markdown component; verified by lint + projection, not unit tests).
- **Files to create/modify:** `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\_components\reuse-discovery-protocol.md` (net-new).
- **Implementation goal:** Author the codebase-neutral core of the reuse-discovery protocol, lifted and generalized from the existing Cognito override `repos/cognito-forms/.claude/skill-config/reuse-first-discovery.md` [VERIFY: test -f "C:/Users/JacobMadsen/source/repos/claude-config/repos/cognito-forms/.claude/skill-config/reuse-first-discovery.md"] (R1 extract capabilities, R2 parallel discovery, R3 ledger, R4 100%-confidence gate, R5 surface/confirm, R6 persist). The shared file must contain ONLY the codebase-neutral substance: (a) capability extraction (decompose the change into load-bearing capabilities); (b) a *grounding-resource catalog placeholder* (the protocol says "ground each capability in the consuming context's domain skills / docs / structural tools" — without naming Cognito-specific skills, which stay in the consumer's wrapper); (c) the verdict taxonomy `reuse` / `extend` / `refactor` / `wrap` / `acceptable-new` (a.k.a. build-new); (d) the Reuse Ledger table shape (Capability | candidate file:symbol | does-today | verdict | evidence | confidence); (e) the 100%-confidence gate (build-new/acceptable-new requires a recorded negative-search trail); (f) the negative-search-trail requirement. Do NOT include `/spec`-specific framing, `--batch`/`NEEDS_INPUT.md` hooks, or the Cognito domain-skill catalog — those remain in the consumer wrapper (WU-2). Write a one-line header comment stating this file is the shared, codebase-neutral protocol core consumed by both `/spec`'s `reuse-first-discovery.md` and the cognito-pr-review plugin's reuse agents.
- **Spec requirements:** SPEC § "Shared reuse-discovery protocol" — "Extract the codebase-neutral core ... into a single file at a stable live path ... One source of truth, no drift."
- **Batch:** 1.

**WU-2 — Refactor the Cognito reuse component to consume the shared core**
- **Scope:** PHASES.md Phase 1 deliverable: "Refactor `repos/cognito-forms/.claude/skill-config/reuse-first-discovery.md` to reference the shared protocol core … preserving the Cognito-specific wrapper verbatim."
- **TDD:** no.
- **Files to create/modify:** `C:\Users\JacobMadsen\source\repos\claude-config\repos\cognito-forms\.claude\skill-config\reuse-first-discovery.md` (refactor in place).
- **Implementation goal:** Refactor this file so its codebase-neutral protocol body is replaced by a nested `!cat` of the shared core created in WU-1 [VERIFY: test -f "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/_components/reuse-discovery-protocol.md" || echo "WU-1 must run first"], while **preserving verbatim** the Cognito-specific wrapper content the touchpoint audit identified: title (line 1), the domain-skill catalog (cognito-storage / cognito-auth / cognito-expressions etc., ~lines 22-28) [VERIFY: grep -n "cognito-" "C:/Users/JacobMadsen/source/repos/claude-config/repos/cognito-forms/.claude/skill-config/reuse-first-discovery.md"], the agent-docs references (~lines 29-35), the `/spec`+`/spec-bug` framing (~lines 75-76), and the `--batch NEEDS_INPUT` hook (~lines 81-86). The nested include must use the same fallback-injection pattern the repo already uses, referencing the shared core at `~/.claude/skills/_components/reuse-discovery-protocol.md`. Confirm `project-skills.py` recursively expands the nested include (it does — it recursively expands all `!cat` references). Net effect: the Cognito wrapper supplies the framing + grounding catalog; the shared core supplies the protocol mechanics; zero behavioral change to what `/spec` injects beyond the de-duplication.
- **Spec requirements:** SPEC § "Shared reuse-discovery protocol" — "`/spec`'s `reuse-first-discovery.md` references it (wrapping it with spec-design framing)."
- **Batch:** 2 (depends on WU-1's file existing).

**WU note — no-op default unchanged:** `user/skills/_components/reuse-first-discovery.md` (the 6-line HTML-comment opt-in default) is NOT modified by this phase. Do not touch it.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | None |
| 2 | WU-2 | Solo | None (depends on WU-1 output) |

## Execution Protocol

This protocol governs autonomous execution. Follow it exactly.

### Phase Selection Loop

This part contains a single phase. Execute Phase 1's batches in order, then run Post-Phase Steps, commit, and finish (the next part is a separate `/execute-plan` invocation).

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (load task tools via ToolSearch, create a task per work unit, update statuses through the run).

### Per-Batch Steps

For each batch (Batch 1, then Batch 2):

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: PHASES.md (Phase 1 + any Implementation Notes), SPEC.md (§ Technical Design, § Reuse Ledger), and this plan file. The PHASES.md `## Validated Assumptions` block is authoritative.

#### Step B.1: Launch Subagents (COMPOSE Agent CALLS — ZERO INLINE AUTHORING)
**PRE-FLIGHT CHECK:** All deliverable edits go through `Agent({ model: "sonnet" })`. You will NOT Edit/Write the deliverable files yourself.
Read before proceeding: `~/.claude/skills/_components/tdd-protocol.md` (gate — expected verdict: TDD not applicable, markdown deliverables), `~/.claude/skills/_components/subagent-launch.md`, `~/.claude/skills/_components/tdd-test-agent.md`, `~/.claude/skills/_components/implementation-agent.md`. Every subagent prompt MUST embed: the CRITICAL CONTEXT block above (symlink/editconfig rules), the relevant SPEC.md excerpt, the exact WU scope + file path, and the clause "You do not run `git commit`/`git push`/`git add` — the orchestrator handles commits."

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md` and `~/.claude/skills/_components/mount-site-verification.md`; run the full protocol (scope measurement, ground-truth verification, review, propagation check, verdict). For this phase, the review MUST additionally confirm: (a) WU-1's shared file contains NO Cognito-specific names; (b) WU-2 preserved every Cognito wrapper section the audit listed; (c) the nested `!cat` resolves.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `~/.claude/skills/_components/phases-update.md`. Check off Phase 1 deliverables; add an Implementation Notes block (date, what was authored, the exact shared-file path, and the include mechanism used) — Part 2's reuse agent depends on the shared path being stable.

#### Step B.4: Run Quality Gates (MANDATORY)
Read `~/.claude/skills/_components/quality-gates.md`. For THIS feature the quality gates are the skill-tooling scripts (run from any dir):
- `python ~/.claude/scripts/lint-skills.py` — MUST exit 0 (no broken/dangerous `!cat`).
- `python ~/.claude/scripts/project-skills.py` — MUST complete with no errors; spot-check that `skills-projected/cognito-forms/` `/spec` output now contains the shared protocol body inline (expanded via the nested include) and still contains the Cognito wrapper sections.
- `python ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities` — MUST report 0 errors (no unexpanded directives, no capability drift).
100% pass required before proceeding.

#### Step B.5: Commit Batch
Read the commit policy (`.claude/skill-config/commit-policy.md` in the claude-config root if present; else `~/.claude/skills/_components/commit-and-push.md`). NOTE: `claude-config` is a **personal** repo (push is allowed) — but per the user's stated intent these changes may be reviewed before push; honor whatever the commit policy says. If no policy file exists, commit locally with a conventional message scoped to claude-config and do NOT push unless the policy explicitly allows it.

#### Step B.6: Proceed to Next Batch
Checklist (all true before next batch): review verdict produced; PHASES.md updated; all 3 lint/projection gates green; commit done. Then run Batch 2.

### Post-Phase Steps (after Batch 2)

#### Integration Verification (MANDATORY)
Read `~/.claude/skills/_components/integration-verification.md`. Confirm: a simulated `/spec` projection for cognito-forms resolves the full reuse protocol (wrapper + shared core) with no content lost; `_default` and other repos are unaffected (the shared core is generic and the no-op default still applies to non-Cognito repos).

#### Update CLAUDE.md Files (MANDATORY)
Read `~/.claude/skills/_components/claude-md-review.md`. Consider whether `claude-config/CLAUDE.md` (or the components README) should note the new shared `reuse-discovery-protocol.md` and that `/spec`'s Cognito component now consumes it.

#### Commit and Push Post-Phase Changes
Per the commit policy resolved in Step B.5.

## Blocking Issue Protocol

If a blocking issue is encountered:
1. Stop all in-progress work; do not launch new subagents.
2. Commit any completed batch not yet committed.
3. Print a blocking-issue report: **Completed batches** (with commit hashes), **Blocked work unit**, **Reason**, **Recovery suggestion**, **Remaining work units (not attempted)**.
4. Do not work around the blocker — the user resolves and re-triggers.
Blocking issues include: a subagent failure unfixable after 2 retries; a lint/projection gate failure unfixable after 2 retries; the nested-include mechanism not expanding (would require rethinking the shared-file approach — escalate, do not hack around it).

## Completion

When Phase 1 is complete:
1. Run the full lint/projection suite one final time (all three commands green).
2. Print a completion report: **Phase completed:** review-pr-buddy P1; **Commits:** (hashes); **Gate status:** all green; **Next:** execute part 2.

## Append to Work Log (MANDATORY — DO NOT SKIP)
Read `~/.claude/skills/_components/work-log.md` and call `interview_work_log_append` (skill=`execute-plan` or `implement-phase`, project=`claude-config`, title, summary, files_modified, technical_context covering the shared-component-extraction pattern and the recursive `!cat` projection mechanism).
