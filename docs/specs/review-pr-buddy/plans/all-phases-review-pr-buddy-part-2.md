---
kind: implementation-plan
feature_id: review-pr-buddy
status: Complete
created: 2026-06-08
phases: [2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-08.
> To execute: `/execute-plan <this file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 3. Sibling parts:
> - part 1: `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy\plans\all-phases-review-pr-buddy-part-1.md`
> - part 2: `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy\plans\all-phases-review-pr-buddy-part-2.md` (this file)
> - part 3: `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy\plans\all-phases-review-pr-buddy-part-3.md`
>
> Execute parts strictly in order. Part 1 (shared protocol) MUST be complete before this part — WU-2 below reads the shared protocol file Part 1 creates. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — review-pr-buddy (Phase 2: Reuse-Candidacy Stage)  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy\PHASES.md` (review-pr-buddy — this part covers Phase 2)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy\SPEC.md`

**Total phases:** 1 (Phase 2 of 4)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, run `/rebuild-agents`, update tracking docs | `Agent`, `Read`, `Bash` (gates/scripts only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source (`.ts`), agent/command markdown, and YAML files | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on `.ts` / `.md` / `.yaml` deliverable files. Compose an `Agent({ model: "sonnet" })` call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`, `work-log.jsonl`.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining them. **Before each step**, `Read` the listed component files from disk. After compaction, re-read this plan first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |
| Final | Work Log | `~/.claude/skills/_components/work-log.md` |

## References — Upstream Artifacts

(none — no completed hard upstream deps. Intra-feature: Part 1's `user/skills/_components/reuse-discovery-protocol.md` must exist before WU-2.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL source/markdown/YAML edits MUST be delegated to Sonnet subagents** — orchestrator never Edits/Writes deliverable files (exception: trivial PASS-WITH-FIXES of a few lines).
2. Subagent edits happen in the current worktree — NEVER create worktrees.
3. Every TDD work unit goes through the test-first pipeline (see Step B.1 + the per-WU TDD flag + the TDD note below).
4. PHASES.md updated AFTER EACH batch.
5. Every subagent's output is reviewed before continuing.
6. Mistakes fixed immediately before the next batch.
7. Integration verification after all batches.
8. CLAUDE.md updated after the phase if warranted.
9. The phase is committed before Part 3.
10. Self-contained — follow exactly.
11. **Before each step, `Read` the listed component files from disk.**

### TDD note (read before Step B.1)
The TS deliverables (WU-1 `prep-pr.ts`, WU-4 `aggregate-findings.ts`, WU-5 `post-process.ts`) carry **TDD: yes**, BUT the cognito-pr-review plugin's `scripts/` may not have an established test runner. At Step B.1, run the `tdd-protocol.md` gate AND first confirm whether a test harness exists for `scripts/` (look for `*.test.ts`/`*.spec.ts`, a `vitest`/`jest`/`node:test` config, or a `test` script in the plugin's `package.json`). If a harness exists, use the test-first pipeline. **If none exists, do NOT fabricate test infrastructure** — downgrade those WUs to direct implementation with a **runnable fixture verification**: the impl agent writes a tiny fixture (a sample manifest / sample agent-output JSON) and runs the script against it, asserting the observable output (populated `baselines[]`, a recognized `reuse-*.json`, a verdict→severity-mapped finding). Record which path was taken in the PHASES.md Implementation Notes.

### CRITICAL CONTEXT — plugin layout, cache boundary, editing rules (EMBED IN EVERY SUBAGENT PROMPT)
- Plugin root: `C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review\`. This is symlinked into the local-tools marketplace; edit the **real** path under `claude-config/`, never the symlink.
- **Cache boundary (critical for the reuse agent design):** the pipeline's `sweep` agent is **cache-only** (PreToolUse hook blocks reads outside `.claude/pr-cache/`); the `investigation` agent has a **carve-out** allowing local-codebase reads (the local repo is on `main`, NOT the PR branch) + tree-sitter MCP tools. The new reuse agent inherits **investigation's** access model, NOT sweep's. Sweep may only FLAG a reuse heuristic and ESCALATE — it must never assert a local-codebase fact.
- Verdict taxonomy: `reuse` / `extend` / `refactor` / `wrap` / `acceptable-new`. Verdict→severity (post-process): `refactor`/`reuse` → important; `extend`/`wrap` → minor (nit); `acceptable-new` → dropped.
- Reuse findings follow the **investigation lane** in `post-process.ts` (fixed `effective_weight: 1.0`, NOT EMA-weighted, NOT dropped by `MIN_EFFECTIVE_WEIGHT`).
- `.editorconfig`: tabs, CRLF.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | review-pr-buddy | P2 | Reuse-Candidacy Stage (Wired, Parallel) | Part 1 (shared protocol) | Solo phase, 2 internal batches |

## Per-Phase Plans

### Phase: review-pr-buddy P2 — Reuse-Candidacy Stage (Wired, Parallel)

**Goal:** Implement and wire the reuse-candidacy stage end-to-end: real baseline detection → per-cluster reuse agent → parallel pipeline wiring → aggregation/post-process for the `reuse` source → sweep escalation rules → synthesizer rendering.

**Entry criteria:** Part 1 complete — `user/skills/_components/reuse-discovery-protocol.md` exists.

**SPEC.md references:** § Technical Design "Reuse-candidacy stage (wired, parallel)" + "Pipeline + script wiring"; § Reuse Ledger rows "Reuse / duplicate detection" (refactor), "Baseline pre-identification" (implement), "Findings aggregation + weighting" (extend), "Rule corpus + weights + calibration" (extend); PHASES.md Phase 2 + Validated Assumptions 1-4.

#### Work Units

**WU-1 (2a) — Implement `findBaselines()` in `prep-pr.ts`**
- **Scope:** Implement real baseline detection so `manifest.baselines[]` is populated (PHASES.md deliverable 2a; Validated Assumption 1).
- **TDD:** yes (see TDD note).
- **Files to create/modify:** `scripts/prep-pr.ts` — implement the body of `findBaselines()` [VERIFY: grep -n "async function findBaselines" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/scripts/prep-pr.ts"] (currently a stub returning `[]` at the line containing "This would need actual implementation to search for baselines"). Populate `ManifestFile["baselines"]` whose schema is `Array<{path; similarityScore; cachedFile}>` [VERIFY: grep -n "baselines: Array<{" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/scripts/prep-pr.ts"].
- **Implementation goal:** For each changed file, find candidate baseline files among existing files on the local `main` branch (same extension and/or same directory; e.g. a new `*Service.cs` → other `*Service.cs` in the same area), compute a `similarityScore` (0-100) from name/path proximity + lightweight structural similarity, copy/record the top matches into the `{cacheDir}/baselines/` dir as `cachedFile`, and return the populated array. Respect the existing consumer contract: `cognito-consistency-checker.md` reads baselines with `similarityScore >= 50` [VERIFY: grep -n "similarityScore" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/agents/cognito-consistency-checker.md"], so scores must be meaningful on that scale. Do not break the existing empty-array initializers elsewhere in the file. Fixture verification: run prep against a small sample changed-file set and assert `baselines[]` is non-empty with plausible scores.
- **Spec requirements:** SPEC § Reuse Ledger "Baseline pre-identification" (now: implement the stub); PHASES.md Validated Assumption 1.
- **Batch:** 1.

**WU-2 (2b) — Grow `cognito-consistency-checker.md` into the per-cluster reuse agent**
- **Scope:** Author the reuse-candidacy agent by growing the orphaned checker in place (PHASES.md 2b; Validated Assumption 4; resolves SPEC Open Question #2 with grow-in-place).
- **TDD:** no (agent-prompt markdown).
- **Files to create/modify:** `agents/cognito-consistency-checker.md` [VERIFY: test -f "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/agents/cognito-consistency-checker.md"] — grow in place.
- **Implementation goal:** Evolve this agent into the per-cluster reuse-candidacy agent: (1) it answers the capability-level question "does an existing Cognito system already do this, such that the PR should have reused/extended/refactored it?"; (2) it READS the shared protocol at `~/.claude/skills/_components/reuse-discovery-protocol.md` [VERIFY: test -f "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/_components/reuse-discovery-protocol.md" || echo "Part 1 must complete first"] — instruct the agent to Read that path directly at runtime (plugin agent prompts are NOT projected like skills, so use a direct Read instruction, not a `!cat`); (3) it inherits investigation's access model (cache + local-codebase-on-main reads + tree-sitter) — copy the access wording from `agents/investigation.md` [VERIFY: grep -n "local codebase\|tree-sitter\|main" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/agents/investigation.md"]; (4) it consumes `manifest.baselines[]` (existing `similarityScore>=50` reading strategy, now live thanks to WU-1); (5) it emits findings with a `verdict` ∈ the taxonomy, an existing-system candidate (`file:line`/symbol/skill), blast radius via tree-sitter `get_callers` for `refactor`, and a `negative_search_trail` for `acceptable-new`; (6) it writes one JSON file per cluster to `{cacheDir}/agent-output/reuse-{cluster}.json` in a schema compatible with `aggregate-findings.ts`. Preserve the existing duplicate-logic detection content as a sub-capability. Because we grow-in-place, the references at `commands/rebuild-agents.md` and `commands/learn-from-pr.md` remain valid — do NOT delete the file.
- **Spec requirements:** SPEC § "Reuse-candidacy stage" steps 1-3; § Reuse Ledger "Reuse / duplicate detection" (refactor).
- **Batch:** 1.

**WU-3 (2c) — Wire the parallel stage into `review-pr.md`**
- **Scope:** Insert the reuse-candidacy stage into the pipeline, parallel with investigation+sweep (PHASES.md 2c).
- **TDD:** no (orchestration markdown).
- **Files to create/modify:** `commands/review-pr.md` [VERIFY: grep -n "Investigation + Sweep\|PARALLEL\|Planner Validates Triage" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/commands/review-pr.md"].
- **Implementation goal:** Add a stage that (a) clusters net-new / substantially-modified substantive files from the manifest (services, types, components, helpers), seeding from `manifest.baselines[]`; (b) fans out one reuse agent (WU-2) per cluster, cap ~6 clusters; (c) is positioned **after Step 4 (planner-validates-triage)** and **dispatched in parallel with Step 5 (investigation+sweep)** so it adds no serial latency; (d) writes `{cacheDir}/agent-output/reuse-{cluster}.json`. Reuse the existing parallel-dispatch shape used for investigation/sweep. Keep `review-pr.md` the single source of pipeline truth (Part 3's buddy will delegate to these steps). Renumber/annotate steps consistently; do not break the existing Step 1.5 cache marker, Step 1.6 cogDocsItemDir, Step 7/8 aggregate/post-process, or Step 12.6 REVIEWED.md.
- **Spec requirements:** SPEC § "Reuse-candidacy stage (wired, parallel)"; Validation Criteria rows "Reuse stage runs in parallel" + "Reuse stage emits a verdict finding".
- **Batch:** 2 (depends on WU-2 existing — the stage dispatches that agent).

**WU-4 (2d) — Extend `aggregate-findings.ts` to recognize `reuse-*.json`**
- **Scope:** Add the `reuse` source to aggregation (PHASES.md 2d).
- **TDD:** yes (see TDD note).
- **Files to create/modify:** `scripts/aggregate-findings.ts` — extend the agent-output discovery at the line filtering `f.startsWith("investigation-")` [VERIFY: grep -n 'startsWith("investigation-")' "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/scripts/aggregate-findings.ts"].
- **Implementation goal:** Recognize `reuse-*.json` agent outputs alongside `investigation-*.json` and the hardcoded `sweep.json`; tag aggregated reuse findings with `source: "reuse"`. Keep the per-finding JSON schema compatible with the existing investigation/sweep finding shapes (so post-process can route them). Fixture verification: drop a sample `reuse-cluster1.json` in the agent-output dir and confirm it appears in the aggregated `findings.json` with `source:"reuse"`.
- **Spec requirements:** SPEC § "Pipeline + script wiring" — "accept `reuse-*.json` agent outputs; add `reuse` to the recognized source set."
- **Batch:** 1.

**WU-5 (2e) — Extend `post-process.ts`: investigation lane + verdict→severity**
- **Scope:** Route reuse findings through the investigation lane and map verdicts to severity (PHASES.md 2e; Validated Assumption 2).
- **TDD:** yes (see TDD note).
- **Files to create/modify:** `scripts/post-process.ts` — add `reuse`-source handling beside the investigation path that assigns `effective_weight: 1.0` [VERIFY: grep -n "effective_weight: 1.0" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/scripts/post-process.ts"] and beside the sweep weight-floor filter at `MIN_EFFECTIVE_WEIGHT` [VERIFY: grep -n "MIN_EFFECTIVE_WEIGHT" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/scripts/post-process.ts"].
- **Implementation goal:** Treat `source:"reuse"` findings like investigation findings: fixed `effective_weight: 1.0`, NOT EMA-weighted, NOT dropped by the `MIN_EFFECTIVE_WEIGHT` sweep filter. Add a verdict→severity map: `refactor`/`reuse` → `important`; `extend`/`wrap` → `nit` (minor); `acceptable-new` → dropped entirely (not surfaced). Then let reuse findings flow through the existing dedup/rank/filter path. `CATEGORY_MAP` already bridges `code-consistency` → `consistency` [VERIFY: grep -n "CATEGORY_MAP" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/scripts/post-process.ts"] — no change needed there. Fixture verification: feed sample reuse findings (one `refactor`, one `extend`, one `acceptable-new`) and assert the first maps to `important`, the second to `nit`, the third is absent, and none were weight-filtered.
- **Spec requirements:** SPEC § "Pipeline + script wiring"; PHASES.md Validated Assumption 2.
- **Batch:** 1.

**WU-6 (2f) — Add reuse rules + weights for sweep escalation**
- **Scope:** Add `code-consistency` reuse rules so cache-only sweep can flag + escalate (PHASES.md 2f; Validated Assumption 3).
- **TDD:** no (rule/data YAML).
- **Files to create/modify:** `knowledge/rules/code-consistency.yaml` (append rules; file-level `category: code-consistency` at line 4 [VERIFY: grep -n "^category: code-consistency" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/knowledge/rules/code-consistency.yaml"]) and `knowledge/weights.yaml` (add `rule_weights` entries [VERIFY: grep -n "^rule_weights:" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/knowledge/weights.yaml"]).
- **Implementation goal:** Append a small set of reuse-detection rules using the real per-rule schema (`id`/`severity`/`description`/optional `trigger_patterns`/`anti_pattern`/`correct_pattern`) — e.g. "new `*Service` mirrors an existing service", "new helper duplicates an existing utility". Each rule's `description` must instruct the cache-only sweep agent to FLAG the heuristic and ESCALATE to the reuse stage rather than assert a local-codebase fact (sweep cannot read the local codebase). Add matching `rule_weights` entries in `weights.yaml`. Use plain language in descriptions (no jargon-only phrasing).
- **Spec requirements:** SPEC § "Pipeline + script wiring" — reuse rules under `code-consistency` so sweep flags + escalates; Validation Criteria "Sweep escalates reuse heuristics rather than asserting".
- **Batch:** 1.

**WU-7 (2g) — Render reuse findings in `synthesizer-v2.md`**
- **Scope:** Add a "Reuse & Duplication" subsection to the synthesizer output template (PHASES.md 2g).
- **TDD:** no (agent-prompt markdown).
- **Files to create/modify:** `agents/synthesizer-v2.md` — insert after the `## Rule-Based Findings` section [VERIFY: grep -n "## Rule-Based Findings" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/agents/synthesizer-v2.md"], before `## Strengths` [VERIFY: grep -n "## Strengths" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/agents/synthesizer-v2.md"].
- **Implementation goal:** Add a "Reuse & Duplication" output subsection that renders `source:"reuse"` findings — each with its verdict, the existing-system candidate (`file:line`/symbol), and the suggested action (reuse/extend/refactor/wrap). Make the section omittable when there are no reuse findings, consistent with the existing section-omission rules in the same file. Keep the section name stable — Part 3's buddy curated-doc references it.
- **Spec requirements:** SPEC § "Pipeline + script wiring" — "add handling so reuse findings render."
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1, WU-2, WU-4, WU-5, WU-6, WU-7 | Yes (6 distinct files; no shared compiled backend) | None |
| 2 | WU-3 | Solo | None (depends on WU-2's agent existing) |

**Orchestrator action between Batch 1 and Batch 2:** after Batch 1 review passes (the grown reuse agent + the new rules both exist), run `/rebuild-agents` (or follow `commands/rebuild-agents.md`) to re-embed the new `code-consistency` rules into the agent prompts. Then proceed to Batch 2.

## Execution Protocol

### Phase Selection Loop
Single phase. Execute Batch 1, run `/rebuild-agents`, execute Batch 2, run Post-Phase Steps, commit, finish (Part 3 is a separate invocation).

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)
Read `~/.claude/skills/_components/task-tracking.md`; load task tools; create a task per WU (and per Test/Impl split for TDD WUs); update statuses through the run.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read PHASES.md (Phase 2 + Validated Assumptions), SPEC.md (§ Technical Design, § Reuse Ledger), this plan, and the CRITICAL CONTEXT block above.

#### Step B.1: Launch Subagents (COMPOSE Agent CALLS — ZERO INLINE CODE)
**PRE-FLIGHT:** all deliverable edits via `Agent({ model: "sonnet" })`; you will not Edit/Write yourself.
Read: `~/.claude/skills/_components/tdd-protocol.md`, `~/.claude/skills/_components/subagent-launch.md`, `~/.claude/skills/_components/tdd-test-agent.md`, `~/.claude/skills/_components/implementation-agent.md`. Apply the **TDD note** above for the TS WUs (confirm/establish a harness or fall back to fixture verification — do not fabricate test infra). Every subagent prompt MUST embed: the CRITICAL CONTEXT block, the relevant SPEC excerpt, the WU scope + verified file paths + `[VERIFY]` anchors, the contract details (verdict taxonomy, severity map, reuse-{cluster}.json schema, cache-boundary access model), and "You do not run `git commit`/`git push`/`git add`."
**Build-concurrency note:** these are TS/markdown/YAML files with no shared slow compiled backend — Batch 1's 6 agents may run in parallel.

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md` + `~/.claude/skills/_components/mount-site-verification.md`; run the full protocol. For TS WUs, the review MUST run the fixture verification (or the test suite) fresh and diff against the agent's GROUND-TRUTH block. Specifically verify: WU-1 populates `baselines[]`; WU-4 surfaces `source:"reuse"`; WU-5's three-way verdict→severity mapping + no weight-filtering of reuse findings; WU-2's agent reads the shared protocol path and emits the negative-search trail for `acceptable-new`; WU-6 rules instruct ESCALATION not assertion.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `~/.claude/skills/_components/phases-update.md`. Check off Phase 2 deliverables (2a-2g); Implementation Notes must record: the TDD path taken (harness vs fixture), the consistency-checker disposition decided (grow-in-place expected), the exact reuse-{cluster}.json schema, and the reuse-{cluster}.json output path (Part 3 surfaces these).

#### Step B.4: Run Quality Gates (MANDATORY)
Read `~/.claude/skills/_components/quality-gates.md`. Gates for this phase:
- TS typecheck / build for the plugin `scripts/` (run the plugin's typecheck or `tsc --noEmit` if available); the three edited scripts must compile.
- The fixture/test verification for WU-1/4/5 (green).
- End-to-end: run `review-pr` on a PR that adds code duplicating an existing system → confirm a `source:"reuse"` finding lands in `processed-findings.json` with a `file:line` candidate + verdict; confirm reuse agents ran in parallel with investigation/sweep (agent-output timestamps); confirm an `acceptable-new` finding carries a `negative_search_trail`; confirm sweep emitted an escalation (not a fabricated local fact) for a reuse-heuristic hit.
- `/rebuild-agents` completed without error and the new rules are embedded.

#### Step B.5: Commit Batch
Read the commit policy (`.claude/skill-config/commit-policy.md` in claude-config root if present; else `~/.claude/skills/_components/commit-and-push.md`). `claude-config` is personal; honor the policy on push vs local-only.

#### Step B.6: Proceed
Checklist: review verdict; PHASES.md updated; typecheck + fixtures green; (after Batch 1) `/rebuild-agents` run; commit done. Then Batch 2 / Post-Phase.

### Post-Phase Steps

#### Integration Verification (MANDATORY)
Read `~/.claude/skills/_components/integration-verification.md`. Confirm the full chain works: prep populates baselines → reuse stage dispatches per cluster in parallel → reuse-{cluster}.json → aggregate (`source:"reuse"`) → post-process (investigation lane + verdict→severity) → synthesizer "Reuse & Duplication" section. Confirm a plain `review-pr` run is unbroken and gains the new finding class with no regression to existing findings.

#### Update CLAUDE.md Files (MANDATORY)
Read `~/.claude/skills/_components/claude-md-review.md`. Update the plugin's `CLAUDE.md` pipeline description to include the reuse-candidacy stage and the grown agent; note the reuse verdict→severity mapping as a tunable for future `/learn-from-pr` calibration.

#### Commit and Push Post-Phase Changes
Per the resolved commit policy.

## Blocking Issue Protocol
If blocked: stop, commit completed batches, print **Completed batches** (hashes) / **Blocked WU** / **Reason** / **Recovery suggestion** / **Remaining WUs**. Do not work around. Blocking issues include: subagent failure unfixable after 2 retries; typecheck/fixture/end-to-end gate failure unfixable after 2 retries; the parallel-dispatch wiring causing a pipeline regression that can't be isolated; `/rebuild-agents` failing. If re-reading `cognito-consistency-checker.md` against the shared protocol reveals grow-in-place is genuinely wrong (a true design fork, not mechanical), STOP and surface it rather than guessing supersede-vs-grow.

## Completion
When Phase 2 is complete: run the full gate suite once more (typecheck + fixtures + one end-to-end `review-pr` surfacing a reuse finding); print a completion report (phase, commits, gate status, next: execute part 3).

## Append to Work Log (MANDATORY — DO NOT SKIP)
Read `~/.claude/skills/_components/work-log.md` and call `interview_work_log_append` (skill=`implement-phase`/`execute-plan`, project=`claude-config`, title, summary, files_modified, technical_context covering the pipeline-stage-insertion + parallel-fan-out + two-lane post-processing design and the cache-boundary access model).
