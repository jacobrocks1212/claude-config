---
kind: implementation-plan
feature_id: review-pr-buddy-issue-format
status: Complete
created: 2026-06-30
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-30.
> To execute: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy-issue-format\plans\all-phases-standardized-issue-format.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — review-pr-buddy-issue-format (Standardized Post-Disposition Issue Format)  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy-issue-format\PHASES.md` (review-pr-buddy-issue-format, 2 phases)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\review-pr-buddy-issue-format\SPEC.md`

**Total phases:** 2
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (e.g. a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the **Plan-specific execution notes** below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (OVERRIDE the contract's defaults)

This plan edits **markdown agent/command prose** in the `claude-config` repo. The contract's default code-oriented steps are overridden as follows:

- **No build, no test harness, no TDD.** Both WUs are pure prose edits to plugin markdown. There is **no** `/msbuild`, `/mstest`, `/nxbuild`, or unit-test step — none exists for this repo, and the PHASES "Testing Strategy" states "No automated test harness exists for markdown skill prose." Both WUs are `TDD: no`.
- **Quality gate = targeted self-review + lint where applicable.** After each WU, the gate is: (a) re-read the edited file against the deliverables/Minimum Verifiable Behavior, (b) confirm the four invariants below are intact, and (c) optionally run `python ~/.claude/scripts/lint-skills.py` (note: it targets `user/skills/`, so it may not cover these `user/plugins/...` files — a clean/no-error exit is informational only, not a pass criterion). The real pass criterion is the inspection checklist in each WU.
- **Edit, don't dispatch-for-code.** Because each phase is a single-file prose edit, the orchestrator MAY perform the edit inline rather than dispatching a code subagent; if dispatched, brief the Sonnet agent with the exact verified section line-ranges below. Either way, **one writer per file**.
- **Not in the lazy pipeline.** `docs/specs/` has no `queue.json` and is not walked by `lazy-state.py` (see `docs/specs/CLAUDE.md`). There is **no Step-9 `/mcp-test` gate**. The PHASES "Runtime Verification" rows are `<!-- verification-only -->` and are checked by **Jacob's manual inspection / optional dry-run**, NOT by `/execute-plan` and NOT by an automated runtime gate. `/execute-plan` closes the `- [ ] WU-N` rows only; it MUST NOT attempt to tick the verification-only PHASES rows.
- **MCP runtime:** not-required (PHASES line 5). No MCP-reachable surface exists in this repo.
- **Commit + push per phase** per the contract's Post-Phase steps, subject to the contract's push-eligibility check (`git config user.email`: if `jacob@cognitoforms.com`, do NOT push — work-repo hook blocks it; otherwise `git push origin main`). Commit the edited file **plus** this plan and the PHASES.md checkbox updates.

### Four invariants that MUST survive both edits (regression guards)

1. **Calibration IDs unaltered.** The canonical `<file>:<line>` / `<file>#<slug>` finding IDs (review-pr-buddy.md `### Finding ID Convention` L74–81) that `scripts/disposition-calibration.ts` joins on MUST NOT change. All new sections are **additive only**. (SPEC "Affected Area" calibration-join row; PHASES P2 deliverable + RV row.)
2. **Ordering preserved.** Findings are pre-sorted by `tier → severity → effective_weight` (`step4_rank`); the renderer MUST NOT re-sort. (synthesizer-v2.md `## Ordering` L175–177, repeated L192.)
3. **Section omission preserved.** Sections with zero kept findings stay omitted. (synthesizer-v2.md `## Section Omission Rules` L166–173.)
4. **Cache boundary preserved.** synthesizer-v2 MUST NOT read the local codebase; its snippet comes only from cached `evidence.snippet`. (synthesizer-v2.md `## Cache Boundary` L186.)

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this plan has no completed hard upstream dependencies) | — | — | — |

The SPEC's `**Related:**` line names `docs/specs/review-pr-buddy/SPEC.md` (the originating buddy feature) but it is not a machine-parseable `**Depends on:**` hard dep; no look-back reality check applies.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | review-pr-buddy-issue-format | P1 | Define + apply the standardized issue block in `synthesizer-v2.md` (format SSOT) | — | Solo (Batch 1) |
| 2 | review-pr-buddy-issue-format | P2 | Apply the format in `review-pr-buddy.md` — artifact + in-chat digest | P1 (references the block format defined in P1) | Solo (Batch 2) |

The two phases are **file-disjoint** (`synthesizer-v2.md` vs. `review-pr-buddy.md`) but MUST run **sequentially**: Phase 2 references "the exact synthesizer-v2 output format" defined in Phase 1. Do not start WU-2 until WU-1 is complete and its block shape is verified present in `synthesizer-v2.md`.

## Work Units

- [x] WU-1 — Standardized issue block in synthesizer-v2.md (format SSOT)
- [x] WU-2 — Apply format in review-pr-buddy.md (artifact + in-chat digest)

---

### Phase: review-pr-buddy-issue-format P1 — Define + apply the standardized issue block in `synthesizer-v2.md` (format SSOT)

**Goal:** Rewrite the per-finding rendering across all four source sections of `synthesizer-v2.md` into one canonical standardized block; this file becomes the single source of format truth that Phase 2 consumes by reference.

**Entry criteria:** None (first phase). Working tree clean per the contract's dirty-tree check.

**SPEC.md references:** "Proposed Standardized Issue Block (design target)" (block shape, L78–84); Resolved Decisions D1 (all kept findings), D2 (snippet-where-cheap-else-prose), D4 (apply to synthesizer-v2 too); "Affected Area" → Shared synthesis format row; Open Questions (cache-only vs local-main asymmetry — synthesizer-v2 is cache-only).

#### WU-1 — Standardized issue block in synthesizer-v2.md (format SSOT)

- **Scope:** All Phase 1 deliverables — define the canonical block; re-express all four per-finding render sections to it; add per-source field-sourcing guidance; state the fix-form rule; state the cache-only snippet constraint; state the comment-style rule; explicitly preserve section grouping, omission rules, and the ordering note.
- **TDD:** no (markdown agent prose; no testable code behavior).
- **Files to create/modify:** `user/plugins/local-tools/plugins/cognito-pr-review/agents/synthesizer-v2.md` [VERIFY: `C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review\agents\synthesizer-v2.md` — confirmed exists, 193 lines].
- **Test files:** none.
- **Implementation goal:**
  - Define the canonical standardized block (new `## Standardized Issue Block` section, or inline in the `## Output Format` preamble at L69 [VERIFY: `## Output Format` heading at synthesizer-v2.md:69]):
    ```
    ### {Issue title}
    **Severity:** {Blocking|Important|Suggestion}   **Source:** {investigation|sweep|reuse|intrafile|reviewer}   **Location:** {file}:{line}   **Confidence:** {CONFIRMED|UNVERIFIED|—}
    **What:** {1–2 line issue statement + why it matters — from hypothesis/description/evidence}
    **Proposed fix:** {concrete before→after snippet when cheap & snippet available; precise prose steps otherwise (always prose for sweep under cache-only)}
    **Proposed PR comment:** {ready-to-paste draft, reviewer-voiced, references file:line — net-new generated text; never auto-posted}
    ```
  - Re-express **all four** per-finding render shapes into the block above (DRIFT NOTE — use the verified real headings, not PHASES' approximate labels):
    - **Investigation** — currently the `### {Finding title}` template under `## Output Format` (synthesizer-v2.md:95–102, headers `**File:** / **Severity:** / **Evidence:** / **Suggestion:**`). Replace with the standardized block.
    - **sweep** — `## Rule-Based Findings` (synthesizer-v2.md:104), currently `### Important`/`### Minor` one-line bullets `- {title} [{file}:{line}] (weight: …)`. Replace per-finding bullets with the standardized block (keep the section heading + Important/Minor grouping if that grouping is part of "section grouping" — see preservation note).
    - **reuse** — `## Reuse & Duplication` (synthesizer-v2.md:116), currently `- **{verdict}** — [{file}:{line}] → candidate: … — {action}` bullets. Replace with the standardized block.
    - **intrafile** — `## Intra-File Consistency` (synthesizer-v2.md:128), currently `- **{verdict}** — [{file}:{line}] → in-file candidate: …` bullets. Replace with the standardized block.
  - Add per-source **field-sourcing** guidance: **What** ← investigation `hypothesis` (L34), sweep `description` (L49), reuse/intrafile `hypothesis`|`description`; **Proposed fix** ← investigation/reuse/intrafile `suggestion` (L36,50) + `evidence.snippet` (L35) when available, sweep `description`+`suggestion` (prose only — sweep has no snippet); **Proposed PR comment** ← net-new generated text seeded from the fix + `evidence.reference` (L35). [VERIFY: JSON shape `## Processed findings JSON shape` at synthesizer-v2.md:21]
  - State the **fix-form rule** explicitly: concrete before→after snippet/diff when small/local AND `evidence.snippet` available; precise prose steps otherwise; **sweep is always prose** under cache-only.
  - Reaffirm the **cache-only snippet constraint** in `## Cache Boundary` (synthesizer-v2.md:179–186; the "Do NOT read from the local codebase" line at L186 [VERIFY: `## Cache Boundary` at synthesizer-v2.md:179]) — snippets come exclusively from cached `evidence.snippet`. Do NOT add any instruction that contradicts L186.
  - State the **comment-style rule**: terse, reviewer-voiced, references `file:line`, paste-ready, never auto-posted (per `user/CLAUDE.local.md`).
  - **Preserve** (do not remove or weaken): the four `##` section groupings + their omission rules (`## Section Omission Rules` synthesizer-v2.md:166–173) and the ordering note (`## Ordering` synthesizer-v2.md:175–177, also restated at L192). Call the ordering note out explicitly: findings are pre-sorted by `tier → severity → effective_weight`; **do not re-sort** in the renderer.
- **Spec requirements:** SPEC "Proposed Standardized Issue Block" L78–89 (block shape + notes: supersedes heterogeneous shapes, fold reviewer `note` into Proposed PR comment, comment terse/reviewer-voiced); D1/D2/D4; cache-only asymmetry (Evidence Collected L50, Open Questions L107).
- **Inspection checklist (Minimum Verifiable Behavior — the pass criterion):**
  - [ ] `## Output Format` (and the four source sections) show ONE canonical block shape applied uniformly across investigation/sweep/reuse/intrafile — no source retains its old heterogeneous shape.
  - [ ] Per-source field-sourcing notes present for What / Proposed fix / Proposed PR comment.
  - [ ] Fix-form rule present (snippet-when-cheap-else-prose); sweep section explicitly says **prose only** and does not reference `evidence.snippet`.
  - [ ] `## Cache Boundary` still prohibits local-codebase reads (L186 intact); no new instruction contradicts it.
  - [ ] Ordering note present and unaltered ("do not re-sort; pre-sorted by tier→severity→weight").
  - [ ] Section omission rules present and unaltered (omit zero-finding sections).
- **Batch:** 1 (solo).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | N/A             |

**Post-phase:** tick `- [ ] WU-1` above; tick Phase 1 `### Deliverables` checkboxes in PHASES.md (NOT the `<!-- verification-only -->` rows — those are Jacob's manual check); commit (`feat(review-pr-buddy-issue-format): Phase 1 — standardized issue block in synthesizer-v2.md`) + push per eligibility. Verify the block shape is present in `synthesizer-v2.md` before starting WU-2.

---

### Phase: review-pr-buddy-issue-format P2 — Apply the format in `review-pr-buddy.md` (artifact + in-chat digest)

**Goal:** Update `review-pr-buddy.md` to generate a proposed fix + draft PR comment per kept finding, emit the Phase-1 standardized block in `PR-{id}.md`, and render a new in-chat standardized digest at session close — without altering finding IDs.

**Entry criteria:** Phase 1 complete and verified — the standardized block is defined in `synthesizer-v2.md`.

**SPEC.md references:** "Proposed Standardized Issue Block" (same format, two surfaces — D3); D4 (buddy consumes the format by reference); "Affected Area" → Buddy Phase 2 synthesis + Buddy in-chat close + Calibration-join rows; Open Questions (buddy local-`main` snippet asymmetry — allowed richer snippet, identical format).

#### WU-2 — Apply format in review-pr-buddy.md (artifact + in-chat digest)

- **Scope:** All Phase 2 deliverables — update "Collect Curated Content" (per-finding fix + draft-comment generation, reviewer `note` folding, local-`main` snippet allowance); update "Review Document Format" (emit standardized block by reference); update "Cleanup and Report" (new in-chat standardized digest); leave "Finding ID Convention" unaltered. The three edited sections all live in one file and share the standardized-block reference, so they form one cohesive WU.
- **TDD:** no (markdown command prose).
- **Files to create/modify:** `user/plugins/local-tools/plugins/cognito-pr-review/commands/review-pr-buddy.md` [VERIFY: `C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review\commands\review-pr-buddy.md` — confirmed exists, 425 lines].
- **Test files:** none.
- **Implementation goal:**
  - **`### Collect Curated Content` (review-pr-buddy.md:296–309 [VERIFY: `### Collect Curated Content` at review-pr-buddy.md:296]):** add a buddy-authored step — for each kept finding, generate a **proposed fix** (concrete snippet when cheap, using the buddy's local-`main` read access; prose otherwise) and a **draft PR comment** (terse, reviewer-voiced, references `file:line`). If the finding carries a reviewer `note`, fold it into / seed the draft PR comment. **DRIFT NOTE:** L309 forbids invoking the `synthesizer-v2` *agent* — the buddy "writes the review directly." This new generation is therefore **buddy-authored inline synthesis, NOT an agent invocation**; do not contradict L309.
  - Document the buddy's **local-`main` snippet-grounding allowance** alongside the fix-generation step (it MAY use a fresher/richer snippet from local `main`, including for sweep which has no `evidence.snippet`); the block **FORMAT** stays identical to synthesizer-v2's — only snippet richness may differ. Call out the asymmetry vs. synthesizer-v2's cache-only constraint. [VERIFY: local-`main` carve-out at review-pr-buddy.md:226]
  - **`### Review Document Format` (review-pr-buddy.md:311–328 [VERIFY: `### Review Document Format` at review-pr-buddy.md:311]):** update so `PR-{id}.md` emits the Phase-1 standardized block for every kept finding. The clause referencing "the exact synthesizer-v2 output format defined in `agents/synthesizer-v2.md`" (review-pr-buddy.md:313 — the coupling seam) is updated to reflect the new block shape. **DRIFT NOTE:** preserve buddy's existing artifact section grouping/names (`## Critical Findings`, `## Rule-Based Findings`, `## Reuse & Duplication`, `## Intra-File Consistency`, omission rules) — only the per-finding block shape changes.
  - **`### Cleanup and Report` (review-pr-buddy.md:384–401 [VERIFY: `### Cleanup and Report` at review-pr-buddy.md:384]):** at session close, render a NEW in-chat standardized digest of all kept findings (most-important-first, same block format as `PR-{id}.md`) in addition to — not instead of — the existing artifact path, journey path, finding counts, and REVIEWED.md status.
  - **`### Finding ID Convention` (review-pr-buddy.md:74–81 [VERIFY: `### Finding ID Convention` at review-pr-buddy.md:74]):** leave **byte-identical / functionally unchanged** — canonical `<file>:<line>` / `<file>#<slug>` IDs that the calibration join depends on are not touched. All new sections are additive only.
- **Spec requirements:** SPEC D3 (both surfaces, one format), D4 (consume synthesizer-v2 format by reference), "Proposed Standardized Issue Block" notes (fold reviewer `note`, terse reviewer-voiced comment); "Affected Area" buddy rows + calibration-join regression guard; Open Questions (buddy richer snippet, identical format).
- **Inspection checklist (Minimum Verifiable Behavior — the pass criterion):**
  - [ ] "Collect Curated Content" instructs per-finding fix + draft-comment generation, with reviewer `note` folded into the draft comment when present.
  - [ ] buddy local-`main` snippet-grounding allowance documented with the cache-only-vs-local asymmetry called out; no agent-invocation contradiction with L309.
  - [ ] "Review Document Format" references the standardized block (Phase-1 format) and no longer describes the old heterogeneous per-source shapes; buddy's section grouping/names preserved.
  - [ ] "Cleanup and Report" renders an in-chat digest in the standardized block format, most-important-first, alongside (not replacing) paths + counts + REVIEWED.md status.
  - [ ] "Finding ID Convention" (L74–81) is unchanged (diff confirms no edit to canonical-ID definitions).
- **Batch:** 2 (solo; depends on WU-1).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 2     | WU-2      | Solo      | N/A (different file from WU-1; sequential on format dependency) |

**Post-phase:** tick `- [ ] WU-2` above; tick Phase 2 `### Deliverables` checkboxes in PHASES.md (NOT the `<!-- verification-only -->` rows); commit (`feat(review-pr-buddy-issue-format): Phase 2 — standardized block in review-pr-buddy.md + in-chat digest`) + push per eligibility.

---

## Completion

Per `~/.claude/skills/_components/execution-contract.md` Completion section, with these repo-specific notes:

- After both WUs land and both inspection checklists pass, the standardized block is live on both surfaces (autonomous `/review-pr` via `synthesizer-v2.md`; buddy Phase 2 + in-chat close via `review-pr-buddy.md`).
- The PHASES.md `<!-- verification-only -->` Runtime Verification rows are **Jacob's** to check (manual inspection / optional dry-run with a one-finding-per-source `processed-findings.json` fixture). `/execute-plan` does NOT tick them and does NOT block on them — this spec is not in the lazy pipeline (no `queue.json`, no Step-9 gate).
- Report to the user: both files edited, the four invariants confirmed intact (calibration IDs, ordering, omission, cache boundary), the two commits (+ push status), and a pointer that the optional dry-run verification remains for manual confirmation.

**Blocking Issue Protocol:** per the contract. If a genuine design fork surfaces mid-edit (none expected — the format is locked in SPEC), write `BLOCKED.md` / surface to the user rather than guessing.
