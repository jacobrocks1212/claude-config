---
kind: implementation-plan
feature_id: lazy-hardening
status: Ready
created: 2026-06-10
phases: [7]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-10.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-hardening\plans\all-phases-lazy-hardening-part-7.md`
> Fully self-contained.

> **Plan series:** part 7 of 7 (FINAL). Siblings part-1 … part-7 under
> `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-hardening\plans\` (this file = part-7).
> Execute parts strictly in order; each is self-contained.
> **WSL equivalent:** swap repo root → `/home/jacob/repos/claude-config`.

# Implementation Plan — Lazy System Hardening (Phase 7)  (v2)

**PHASES.md:** `...\docs\specs\lazy-hardening\PHASES.md` (Phase 7 of 7)
**Design contract:** `user/scripts/plans/lazy-hardening.md` → "Phase 7 — Environment & compaction hardening"
**Plan version:** v2 · **Entry criteria:** Phase 6 complete. *(The design plan lists Phase 7 as "independent after Phase 1," but it is scheduled LAST here on purpose: WU-1 wires Step-0 preflight and WU-4 purges stale prompt refs INTO `lazy-bug-batch`, which Phase 6 rebuilds by-reference. Running Phase 7 before Phase 6 would have its `lazy-bug-batch` edits clobbered by the rebuild. Phase 7 therefore depends on Phase 6 in practice.)*

---

## Touchpoint Summary
AlgoBooth touchpoint audit N/A. Touchpoints: the 3 batch SKILLs, 3 single-dispatch wrappers (`lazy`, `lazy-bug`, `lazy-cloud`), `skill-config`, plus a new preflight `_component` and a new compaction-protocol `_component`. Adds preconditions/templates + grep-replaces a stale tool reference — no production logic surface growth in the scripts. No Phase-0 decomposition warranted.

## EXECUTION MODEL — READ THIS FIRST
| Role | Does | Allowed tools |
|------|------|---------------|
| **Orchestrator** | plan, dispatch Agents, review, run `--test` gates + grep/git, update tracking | `Agent`,`Read`,`Bash`(tests/grep/git), task tracking, `Edit`/`Write` on **PHASES.md only** |
| **Sonnet subagent** | write ALL `SKILL.md`/`_components/*.md`/skill-config | `Edit`,`Write`,`Read`,`Bash`,`Grep`,`Glob` |

**HARD CONSTRAINT:** orchestrator edits PHASES.md only. Subagent prompts name the absolute repo root + carry verbatim: *"You do not `git commit`/`push`; staging is the orchestrator's; end with a `GROUND-TRUTH OUTPUT` block."*
**Dispatch:** `Agent({ description, model: "sonnet", prompt: "<FULL self-contained>" })`

## COMPONENT LOADING PROTOCOL
Before each step `Read` listed components from disk. After compaction re-read this plan first.

## Component Reference Card
| Step | Component | Path |
|------|-----------|------|
| 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| B.1 | TDD / Launch / Test Agent / Impl Agent | `~/.claude/skills/_components/{tdd-protocol,subagent-launch,tdd-test-agent,implementation-agent}.md` |
| B.2 | Review / Mount-Site | `~/.claude/skills/_components/{subagent-review,mount-site-verification}.md` |
| B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` (Repo-Specific override) |
| Post | Integration / CLAUDE.md | `~/.claude/skills/_components/{integration-verification,claude-md-review}.md` |
| Final | Work Log | `~/.claude/skills/_components/work-log.md` |

**MCP note:** runtime MCP test AlgoBooth-specific → N/A. Verification = three `--test` gates + simulated DOA conditions + grep.

## Repo-Specific Quality Gates & Commit Policy
```bash
python3 ~/.claude/scripts/lazy-state.py --test
python3 ~/.claude/scripts/bug-state.py --test
python3 ~/.claude/scripts/test_lazy_core.py
```
Plus (after WU-4):
```bash
grep -rn "interview_work_log_append" user/skills/   # only legitimate work-log step usages may remain; ZERO in dispatch prompt templates
```
**Commit policy:** orchestrator stages/commits/pushes; personal email → push allowed; `feat(lazy-hardening): <summary>`; no AI attribution.

## References — Upstream Artifacts
| Upstream | Kind | Path | Why |
|----------|------|------|-----|
| design plan | source-of-truth | `user/scripts/plans/lazy-hardening.md` (Phase 7) | preflight/compaction/long-build/hygiene specs + DOA evidence |
| Phase-6 rebuilt lazy-bug-batch | hard | this feature's PHASES.md Phase 6 Notes | WU-1/WU-4 edits land in the rebuilt file (must follow Phase 6) |
| project CLAUDE.md | reference | `CLAUDE.md` (AlgoBooth) → "Claude Code Config" + "WSL PATH note" | symlink recreation recipe + node-path mapping for the preflight |

## MANDATORY RULES — DO NOT SKIP
1. All skill/config writing → Sonnet subagents. 2. No worktrees. 3. Prose/config WUs verified by grep + simulated DOA (no unit tests). 4. PHASES.md after each batch. 5. Structured review verdict. 6. Fix immediately. 7. Integration verification. 8. CLAUDE.md if warranted. 9. Phase committed+pushed. 10. **Preflight consumes ZERO cycles on failure** — it runs BEFORE the start banner and any remote sync, prints the setup recipe, and stops. 11. **WU-4 purges `interview_work_log_append` ONLY from dispatch PROMPT templates** (the base cycle prompt subagents receive) — the orchestrator's own final Work-Log step legitimately uses it and MUST remain. 12. Self-contained; read components from disk before each step.

## Execution Schedule
| Step | Phase | Title | Blocked by | Parallel? |
|------|-------|-------|------------|-----------|
| 1 | P7 | Environment & compaction hardening | Phase 6 (lazy-bug-batch rebuild) | Solo phase; batches sequential (shared batch SKILLs) |

---

## Phase: lazy-hardening P7 — Environment & compaction hardening

**Goal:** Stop runs dying or degrading on preconditions and context churn (2 of 7 Windows sessions DOA on missing symlinks / python3; 7 failed node-PATH probes; post-compact 41% of spawns lost `model:` + 13 Edit-without-Read errors).
**Entry criteria:** Phase 6 complete; tree clean.

### Work Units

#### WU-1 — Step 0 preflight (symlink / python3 / scripts / node) + Windows node path  *(TDD: no — prose/config; verified by simulated DOA)*
- **Coupling reason (2 deliverables → 1 WU):** the Windows node-path bake IS one of the preflight checks; splitting would leave the preflight referencing a node location the config doesn't yet define.
- **Scope:** "Step 0 preflight … before banner in batch skills + wrappers; failure prints setup recipe, zero cycles consumed" · "Windows node path (`/c/nvm4w/nodejs`) baked into preflight/skill-config"
- **Files:** new `~/.claude/skills/_components/lazy-preflight.md` (or skill-config equivalent) + wire into `user/skills/lazy-batch/SKILL.md`, `user/skills/lazy-bug-batch/SKILL.md`, `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md`, and the 3 wrappers `user/skills/lazy/SKILL.md`, `user/skills/lazy-bug/SKILL.md`, `repos/algobooth/.claude/skills/lazy-cloud/SKILL.md` + `.claude/skill-config` [VERIFY: `ls user/skills/lazy-batch/SKILL.md`; node location per AlgoBooth CLAUDE.md WSL PATH note]
- **Implementation goal:** BEFORE the start banner and any remote sync, verify: `.claude/skills` symlink resolves, `python3` runs, `~/.claude/scripts/lazy-state.py` (or bug-state) exists, node resolvable. On failure: print the setup recipe (symlink recreation per CLAUDE.md, path mapping) and STOP — zero cycles consumed. Bake the known Windows Git-Bash node location (`/c/nvm4w/nodejs`) into the preflight/skill-config so per-call `export PATH` boilerplate disappears.
- **Verify:** simulate DOA — rename the symlink in a scratch clone / shadow `python3` → preflight catches both with the recipe printed.
- **Batch:** 1

#### WU-2 — Compaction protocol (on-disk dispatch template + Read-before-Edit)  *(TDD: no — prose)*
- **Scope:** "Compaction protocol: on-disk canonical dispatch template re-read after compact; Read-before-Edit rule"
- **Files:** new `~/.claude/skills/_components/lazy-dispatch-template.md` (small canonical dispatch skeleton: `subagent_type`, `model` param, prompt skeleton) + the 3 batch SKILLs (re-read it after any compact boundary; apply a "Read before Edit" rule since read-state is wiped by compaction)
- **Implementation goal:** after any compaction boundary the orchestrator re-reads the on-disk dispatch template before the next dispatch; note the operator's manual-compact-during-dispatch pattern as the sanctioned cadence.
- **Batch:** 2 (overlaps WU-1 on batch SKILLs)

#### WU-3 — Long-build ownership rule + `cargo check --release` pre-flight  *(TDD: no — prose)*
- **Scope:** "Long-build ownership rule (orchestrator-owned harness-tracked) + `cargo check --release` pre-flight"
- **Files:** `.claude/skill-config` + the 3 batch SKILLs (codify: any build/test expected to exceed a subagent turn is orchestrator-owned as a harness-tracked background task — subagent-backgrounded processes die at turn end, a `tauri build` silently vanished this way; require `cargo check --release` before committing to a 20–40min packaged build)
- **Batch:** 3 (overlaps WU-1/WU-2 on batch SKILLs)

#### WU-4 — Prompt hygiene (purge `interview_work_log_append` from dispatch templates + add clauses)  *(TDD: no — prose; grep-verified)*
- **Scope:** "`interview_work_log_append` purged from all dispatch templates; canonical-sentinel-filename + work-branch clauses added to base dispatch prompt"
- **Files:** dispatch prompt templates carrying the stale ref [VERIFY: `grep -rln "interview_work_log_append" user/skills/` → lazy, lazy-bug, plan-bug, plan-feature, implement-phase, implement-phase-batch, etc.] — **purge ONLY from the dispatch PROMPT templates (the prompt subagents receive); the orchestrator's own final Work-Log step legitimately uses the tool and MUST remain** (e.g. the `log` skill, the "Append to Work Log" step). Add to the base dispatch prompt: canonical-sentinel-filenames clause + work-branch-only-commits clause.
- **Implementation goal:** subagents reliably burn closing turns discovering `interview_work_log_append` doesn't exist for them — remove it from what they're handed; the canonical-sentinel + work-branch clauses prevent the 3 subagent git/sentinel deviations seen in one Jun-9 run.
- **Verify:** `grep -rn "interview_work_log_append" user/skills/` shows ZERO matches inside dispatch prompt blocks (legitimate orchestrator-step usages may remain).
- **Batch:** 4 (overlaps WU-1/WU-2/WU-3 on the skills)

### Batch Overview
| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | new preflight component + 3 batch SKILLs + 3 wrappers + skill-config |
| 2 | WU-2 | Solo | new dispatch-template component + batch SKILLs (overlaps WU-1) |
| 3 | WU-3 | Solo | skill-config + batch SKILLs (overlaps WU-1/2) |
| 4 | WU-4 | Solo | dispatch templates across skills (overlaps WU-1/2/3) |

**Runtime Verification (gates/DOA-sim/grep, not WUs):**
- [ ] Simulated DOA conditions (missing symlink / shadowed python3) caught by preflight with recipe
- [ ] `grep -r interview_work_log_append user/skills/` returns nothing in dispatch prompt templates

---

## Execution Protocol
**Step 0:** Read `task-tracking.md`; one task per WU (WU-1..WU-4).
**Phase Selection Loop:** one phase (P7). Announce "Implementing lazy-hardening Phase 7: Environment & compaction hardening". Re-read design plan P7 + AlgoBooth CLAUDE.md symlink/node recipe + Phase-6 rebuilt lazy-bug-batch Notes. Execute batches 1→2→3→4. Post-Phase Steps. Report hash.
**Per-Batch Steps:** B.0 source-reread → B.1 launch (read tdd-protocol/subagent-launch/tdd-test-agent/implementation-agent; self-contained Sonnet Agents w/ repo root + no-commit clause + GROUND-TRUTH OUTPUT) → B.2 review (structured verdict, blocking) → B.3 PHASES.md update → B.4 `--test` ×3 + DOA-sim/grep → B.4.5 MCP N/A → B.5 commit → B.6 proceed only when verdict+PHASES+gates+commit done.
**Post-Phase:** Integration Verification (`integration-verification.md`) — preflight fires before banner in all 6 entry points; compaction template + clauses present in the (Phase-6-rebuilt) lazy-bug-batch; CLAUDE.md Review (`claude-md-review.md`) — update AlgoBooth CLAUDE.md / skill-config if the node-path/preflight recipe moved; Commit and Push.

## Blocking Issue Protocol
If blocked: stop, commit+push completed batches, print `## Implementation — Blocked` (completed WUs+hashes, blocked WU, reason, recovery), do not work around. Blocking: subagent/gate failure unfixable after 2 retries; push conflict unresolvable by rebase; a preflight check that can't be made portable across Windows+WSL without an operator decision.

## Completion — SERIES FINAL
When P7 is complete, the **entire 7-part lazy-hardening series is done.**
1. Run all three `--test` gates + the grep/DOA checks one final time.
2. Print:
   > ## lazy-hardening — Implementation Series Complete (parts 1–7)
   > **Phases completed:** P1–P7
   > **Commits:** [full log across all parts, if known]
   > **Final gate status:** all three `--test` gates green; default-mode zero-drift byte-identical; grep checks clean; rebuilt lazy-bug-batch compliance read passed
   > **Cross-phase notes:** integrity side-doors closed (P2); routing cycle-burn fixed (P3); opt-in park/auto-accept live behind `--park` with no-flag invariant preserved (P4); orchestrator loop scripted to ≤3 messages (P5); lazy-bug-batch rebuilt by-reference (P6); preflight + compaction hardening (P7).
3. Note for the operator: this feature's runtime validation is the `--test` regression suite + the dry-run compliance reads — there is no Tauri/MCP gate for claude-config. The lazy-family skills are now ready for a live `/lazy-batch --park` shakedown run.

## Append to Work Log (MANDATORY)
Read `~/.claude/skills/_components/work-log.md`; call `interview_work_log_append` (skill, project=claude-config, title, summary, files_modified, technical_context). This is the orchestrator's own step — it legitimately uses the tool (WU-4 only purged it from *subagent dispatch* prompts).
