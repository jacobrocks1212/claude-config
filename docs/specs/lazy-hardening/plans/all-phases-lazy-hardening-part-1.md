---
kind: implementation-plan
feature_id: lazy-hardening
status: Complete
created: 2026-06-10
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-10.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-hardening\plans\all-phases-lazy-hardening-part-1.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 7. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-hardening\plans\all-phases-lazy-hardening-part-1.md` (this file)
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-hardening\plans\all-phases-lazy-hardening-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-hardening\plans\all-phases-lazy-hardening-part-3.md`
> - part 4: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-hardening\plans\all-phases-lazy-hardening-part-4.md`
> - part 5: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-hardening\plans\all-phases-lazy-hardening-part-5.md`
> - part 6: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-hardening\plans\all-phases-lazy-hardening-part-6.md`
> - part 7: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-hardening\plans\all-phases-lazy-hardening-part-7.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.
> **WSL path equivalent:** replace `C:\Users\Jacob\source\repos\claude-config` with `/home/jacob/repos/claude-config` and use forward slashes.

# Implementation Plan — Lazy System Hardening (Phase 1)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-hardening\PHASES.md` (lazy-hardening, 7 phases — this part covers Phase 1)

**SPEC.md files:**
- (no SPEC.md) — the design contract is the authored plan `user/scripts/plans/lazy-hardening.md` (this repo). Treat it as the source of truth for correctness.

**Total phases (this part):** 1 (Phase 1 of 7)
**Plan version:** v2 (reference-based — components loaded from disk per step)

---

## Touchpoint Summary

The AlgoBooth `npm run audit:touchpoints` gate does NOT exist in the claude-config repo, so it was not run. Touchpoints for this part are existing Python files (`user/scripts/bug-state.py` ≈ 1850+ LOC, `user/scripts/lazy-state.py` ≈ 3270+ LOC). These are large pre-existing files; their decomposition is explicitly scoped as a later phase (Phase 6 componentization extracts the lazy-batch prompt blocks). No Phase-0 decomposition is warranted for Phase 1: this phase only **adds** test fixtures + a baseline mechanism and cleans docstrings — zero behavior change, no growth of logic surface. Skip reason recorded per the Touchpoint Audit Gate "heuristic note": the audit tool is AlgoBooth-specific and unavailable here; the only files touched grow test data / comments, not production logic.

---

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture (claude-config variant):

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run `--test` regression harnesses + git, update tracking docs | `Agent`, `Read`, `Bash` (tests/git only), task tracking, `Edit`/`Write` on **PHASES.md only** |
| **Sonnet subagent** | Write ALL source/test/skill changes (`.py`, `SKILL.md`, `_components/*.md`, baseline/fixture files) | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** the orchestrator MUST NOT `Edit`/`Write` any `.py`, `SKILL.md`, `_components/*.md`, or baseline/fixture file. If you are about to modify one — STOP and compose an `Agent` call instead. The ONLY files you may modify directly: `PHASES.md`, `work-log.jsonl`. Every subagent prompt must name the absolute repo root and include verbatim:

> **You do not run `git commit` or `git push`.** Staging (`git add`) is also reserved for the orchestrator. Your job ends after producing the `GROUND-TRUTH OUTPUT` block defined in your briefing.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

---

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` (see **Repo-Specific Quality Gates** below — overrides the generic commands) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |
| Final | Work Log | `~/.claude/skills/_components/work-log.md` |

## Repo-Specific Quality Gates & Commit Policy (this repo — overrides generic component)

**Regression gates (run after every batch and every phase; all three MUST exit 0):**
```bash
python3 ~/.claude/scripts/lazy-state.py --test
python3 ~/.claude/scripts/bug-state.py --test
python3 ~/.claude/scripts/test_lazy_core.py
```
**Zero-drift sanity probe (run whenever a phase claims zero behavior change — Phase 1 DOES):**
```bash
python3 ~/.claude/scripts/lazy-state.py --repo-root "C:\Users\Jacob\repos\AlgoBooth"   # diff against captured baseline; must be byte-identical
```

**Commit policy (claude-config; there is NO `.claude/skill-config/commit-policy.md` for this repo):** the orchestrator stages, commits, and pushes after each phase. `git config user.email` here is a personal `@users.noreply.github.com` address → **push is allowed** (`git push origin <current-branch>`). Commit message convention: `feat(lazy-hardening): <phase summary>` or `test(lazy-hardening): <summary>`. No AI attribution lines.

## References — Upstream Artifacts

| Upstream | Kind | Path | Why this plan references it |
|----------|------|------|------------------------------|
| lazy-hardening design plan | source-of-truth | `user/scripts/plans/lazy-hardening.md` | Full deliverable specs, line anchors, Operator Decision Record (D1–D6), risk/sequencing notes. Re-read its **Phase 1** section before starting. |
| audit memory | reference | session-memory `lazy-system-audit-2026-06-10.md` + "Windows AlgoBooth Fable" 2026-06-10 session report | Full finding inventory with quotes/anchors if a deliverable's anchor text has drifted. |

---

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation/test/fixture/skill writing MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrator MUST NOT `Edit`/`Write` source, test, fixture, baseline, or skill files. Only exception: trivial PASS-WITH-FIXES items (a few lines).
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline — failing test first, then implementation.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in a phase finish, integration verification confirms all changes work together.
8. Relevant CLAUDE.md files (`user/scripts/CLAUDE.md`, etc.) are created/updated after the phase if changes warrant it.
9. The completed phase is committed and pushed before declaring the part done.
10. This part covers ONE phase; there is no cross-feature parallelism here.
11. This plan is self-contained — follow it exactly without relying on external context or sibling parts.
12. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.

---

## Execution Schedule

| Step | Phase | Title | Blocked by | Parallel? |
|------|-------|-------|------------|-----------|
| 1 | P1 | Test safety net (zero behavior change) | — (first phase) | Solo phase; internal batches parallel where no file conflict |

---

## Phase: lazy-hardening P1 — Test safety net (zero behavior change)

**Goal:** Lock in the current behavior of `bug-state.py` with a byte-pinned `--test` baseline + missing fixtures BEFORE any later phase touches the scripts. `lazy-state.py` already has a byte-pinned `--test` baseline; `bug-state.py` has none.

**Entry criteria:** None — first phase. Working tree clean.

**Design-plan references:** `user/scripts/plans/lazy-hardening.md` → "Phase 1 — Test safety net". This phase is **zero behavior change**: only test data, a baseline mechanism, and docstring cleanup.

### Work Units

#### WU-1 — bug-state `--test` baseline mechanism + durable matching test  *(TDD: yes)*

- **Scope (PHASES deliverable):** "`user/scripts/tests/baselines/bug-state-test-baseline.txt` + durable matching test"
- **Files to modify/create:**
  - `user/scripts/tests/baselines/bug-state-test-baseline.txt` (new — generated golden output)
  - `user/scripts/bug-state.py` (add/confirm a `--test` mode that prints normalized fixture output, mirroring lazy-state's `--test` [VERIFY: `grep -n "add_argument(\"--test\"" user/scripts/lazy-state.py` → line 3245] and its temp-path normalization placeholder)
  - test wiring: extend `user/scripts/test_lazy_core.py` OR add a sibling test that asserts `bug-state.py --test` output matches the baseline (mirror how the lazy-state baseline is consumed [VERIFY: `grep -rn "lazy-state-test-baseline" user/scripts/test_lazy_core.py`])
- **Test expectations:** A test that runs `bug-state.py --test`, normalizes temp paths the same way lazy-state does, and asserts byte-equality with `bug-state-test-baseline.txt`. RED first (baseline absent / mismatch), GREEN after baseline captured.
- **Implementation goal:** A reproducible `bug-state.py --test` golden-output mechanism identical in shape to lazy-state's, so every later phase has a zero-behavior-change contract for the bug pipeline.
- **Batch:** 1

#### WU-2 — AlgoBooth `--repo-root` JSON baseline for bug-state  *(TDD: yes — capture + stability assert)*

- **Scope (PHASES deliverable):** "AlgoBooth `--repo-root` JSON baseline for bug-state.py"
- **Files to create:** `user/scripts/tests/baselines/bug-state-algobooth.json` (new — parallel to the existing `lazy-state-algobooth.json` [VERIFY: path `user/scripts/tests/baselines/lazy-state-algobooth.json`])
- **Implementation goal:** Capture `python3 bug-state.py --repo-root <AlgoBooth>` output as a committed JSON baseline + a test asserting current output matches it (the zero-drift probe for the bug pipeline). Independent of WU-1's code change → runs parallel in Batch 1.
- **Note:** AlgoBooth root = `C:\Users\Jacob\repos\AlgoBooth` (Windows) / `/home/jacob/repos/AlgoBooth` (WSL). If AlgoBooth is unavailable on the executing host, the subagent must report this in its GROUND-TRUTH OUTPUT, not fabricate a baseline → orchestrator routes to Blocking Issue Protocol.
- **Batch:** 1

#### WU-3 — Missing bug-state fixtures (cloud / device-reopen / step-9 / backfill / severity)  *(TDD: yes)*

- **Scope (PHASES deliverables):** "Cloud-mode fixtures (currently zero `cloud=True` coverage)" · "Device re-open fixture (`STEP_DEVICE_REOPEN`)" · "Step-9 `SKIP_MCP_TEST.md` / `MCP_TEST_RESULTS.md` fixtures" · "`backfill_receipts` fixture; severity-ordering fixture (multiple unlisted bugs)"
- **Coupling reason (4 PHASES checkboxes → 1 WU):** all four add entries to the **same embedded fixtures structure** in `bug-state.py`. This is single-file additive co-creation where parallel splitting is impossible without merge conflicts — exactly the file-overlap case the sizing rules say to keep in one batch rather than fan out. One subagent owns all fixture additions, then regenerates the baseline.
- **Files to modify:** `user/scripts/bug-state.py` (embedded fixtures), and regenerate `user/scripts/tests/baselines/bug-state-test-baseline.txt`
- **Anchors:**
  - `cloud=True` path exists in `compute_state` [VERIFY: `grep -n "def compute_state" user/scripts/bug-state.py` → 395; `grep -n "cloud" user/scripts/bug-state.py | head`]
  - `STEP_DEVICE_REOPEN` [VERIFY: `grep -n "STEP_DEVICE_REOPEN" user/scripts/bug-state.py` → 134, 677]
  - `backfill_receipts` [VERIFY: `grep -n "def backfill_receipts" user/scripts/bug-state.py` → 759]
  - `DEFERRED_NON_CLOUD.md` [VERIFY: `grep -n "DEFERRED_NON_CLOUD" user/scripts/bug-state.py` → 701]
- **Test expectations:** Demonstrate fail-then-pass for **at least** the cloud and device-reopen cases (RED on a deliberately broken assertion, GREEN after). All three regression gates green after baseline regeneration.
- **Batch:** 2 (modifies `bug-state.py`, overlaps WU-1)

#### WU-4 — Stale harness docstring cleanup (both scripts)  *(TDD: no — comments only)*

- **Scope (PHASES deliverable):** "Stale harness docstrings cleaned (both scripts)"
- **Files to modify:** `user/scripts/bug-state.py` (stale "All failures stem from NotImplementedError because compute_state() is a stub" near the test harness [VERIFY: `grep -n "compute_state() is a stub\|NotImplementedError" user/scripts/bug-state.py`]) · `user/scripts/lazy-state.py` (the "RED" comments around scope kwargs [VERIFY: `grep -n "RED" user/scripts/lazy-state.py | head`])
- **Implementation goal:** Remove docstrings/comments that no longer describe reality. Pure comment edits — must not change any executable line. Confirm zero-drift probe stays byte-identical.
- **Batch:** 3 (modifies `bug-state.py`, overlaps WU-3; also `lazy-state.py`)

### Batch Overview

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1, WU-2 | Yes | None (bug-state.py+baseline.txt+test vs. new algobooth.json) |
| 2 | WU-3 | Solo | bug-state.py (overlaps WU-1) |
| 3 | WU-4 | Solo | bug-state.py (overlaps WU-3) + lazy-state.py |

**Runtime Verification (ticked by the regression gates in Step B.4 / Post-Phase — NOT separate WUs):**
- [ ] `python3 ~/.claude/scripts/lazy-state.py --test` exits 0
- [ ] `python3 ~/.claude/scripts/bug-state.py --test` exits 0 and matches baseline
- [ ] `python3 ~/.claude/scripts/test_lazy_core.py` exits 0

---

## Execution Protocol

This protocol governs autonomous execution. Follow it exactly.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — DO THIS FIRST)

Read `~/.claude/skills/_components/task-tracking.md` and follow it exactly: load task tools via ToolSearch, create one task per work unit (WU-1..WU-4), update status (in_progress on start, completed on done) through the test and implementation phases.

### Phase Selection Loop

This part has one phase (P1). Execute it, run Post-Phase Steps, commit+push, then this part is complete (proceed to part 2 in a new `/execute-plan`).

1. **Announce:** Print "Implementing lazy-hardening Phase 1: Test safety net".
2. **Review prior context:** none (first phase). Re-read `user/scripts/plans/lazy-hardening.md` → Phase 1.
3. **Execute all batches** (1 → 2 → 3) per Per-Batch Steps.
4. **Run Post-Phase Steps.**
5. **Report:** "lazy-hardening Phase 1: Test safety net — committed as [hash]".

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: PHASES.md (Phase 1 + any Implementation Notes), `user/scripts/plans/lazy-hardening.md` (Phase 1), and this plan file. Do NOT rely on cached content.

#### Step B.1: Launch Subagents (COMPOSE Agent CALLS — ZERO INLINE IMPLEMENTATION)
**PRE-FLIGHT:** confirm (1) `Agent` tool with `model: "sonnet"` for ALL changes, (2) you will NOT `Edit`/`Write` source/test/fixture/skill files.
Read ALL before proceeding: `~/.claude/skills/_components/tdd-protocol.md`, `~/.claude/skills/_components/subagent-launch.md`, `~/.claude/skills/_components/tdd-test-agent.md`, `~/.claude/skills/_components/implementation-agent.md`. Each subagent prompt is fully self-contained, names the absolute claude-config repo root, includes the "you do not commit/stage/push" clause from the EXECUTION MODEL, and ends by emitting a `GROUND-TRUTH OUTPUT` block (files changed, gate results, RED→GREEN evidence).
**POST-DISPATCH GATE:** verify you composed `Agent` calls and did NOT edit files directly.

#### Step B.2: Review Batch Output (MANDATORY BLOCKING GATE)
Read `~/.claude/skills/_components/subagent-review.md` (and `~/.claude/skills/_components/mount-site-verification.md` for new files). Produce a structured review report with a PASS / PASS-WITH-FIXES / NEEDS-REWORK verdict. "Looks correct" is NOT a review.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `~/.claude/skills/_components/phases-update.md`. Check off completed deliverables and add an Implementation Notes block (date, work completed, integration notes, pitfalls, files modified).

#### Step B.4: Run Quality Gates (MANDATORY)
Run the three Repo-Specific regression gates above (all exit 0) + the zero-drift probe (byte-identical — this phase is zero behavior change). If WU touched parsing/imports, run the full set. 100% pass required.

#### Step B.5: Commit Batch
Per the Repo-Specific Commit Policy above (orchestrator stages/commits; push deferred to end of phase or commit per-batch — your choice, but the phase MUST end pushed).

#### Step B.6: Proceed to Next Batch
Checklist (all true): review report w/ verdict · PHASES.md updated · gates pass · batch committed. Else go back.

### Post-Phase Steps

- **Integration Verification (MANDATORY):** Read `~/.claude/skills/_components/integration-verification.md`. Confirm the baseline mechanism + fixtures + docstring cleanup cohere and the zero-drift probe is byte-identical.
- **Update CLAUDE.md (MANDATORY review):** Read `~/.claude/skills/_components/claude-md-review.md`. Likely update `user/scripts/CLAUDE.md` / `user/scripts/tests/baselines/README.md` to document the new bug-state baseline + fixtures.
- **Commit and Push:** push the phase per the Commit Policy.

---

## Blocking Issue Protocol

If a blocking issue occurs:
1. Stop all in-progress work; launch no new subagents.
2. Commit and push any completed batches.
3. Print:
   > ## Implementation — Blocked
   > **Completed:** [batches/WUs with hashes]
   > **Blocked at:** lazy-hardening Phase 1 — [WU]
   > **Reason:** [specific]
   > **Recovery suggestion:** [what the user should do]
4. Do not work around the blocker.

Blocking issues include: AlgoBooth unavailable for WU-2 baseline; a subagent failure unfixable after 2 retries; a regression-gate failure unfixable after 2 retries; a git push conflict unresolvable by rebase; zero-drift probe diverges and the cause is a real (unintended) behavior change.

---

## Completion

When Phase 1 is complete:
1. Run the full regression suite + zero-drift probe one final time.
2. Print:
   > ## lazy-hardening Part 1 — Complete
   > **Phase completed:** P1 Test safety net
   > **Commits:** [list]
   > **Final gate status:** all green; zero-drift byte-identical
   > **Next:** `/execute-plan ...part-2.md`

---

## Append to Work Log (MANDATORY)

Read `~/.claude/skills/_components/work-log.md` and follow it. Call `interview_work_log_append` with skill, project (claude-config), title, summary, files_modified, technical_context.
