---
kind: implementation-plan
feature_id: lazy-hardening
status: Complete
created: 2026-06-10
phases: [3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-10.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-hardening\plans\all-phases-lazy-hardening-part-3.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 3 of 7. Siblings: part-1 … part-7 under
> `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-hardening\plans\` (this file = part-3).
> Execute parts strictly in order; each is self-contained.
> **WSL equivalent:** swap `C:\Users\Jacob\source\repos\claude-config` → `/home/jacob/repos/claude-config`.

# Implementation Plan — Lazy System Hardening (Phase 3)  (v2)

**PHASES.md:** `...\docs\specs\lazy-hardening\PHASES.md` (Phase 3 of 7)
**Design contract (source of truth):** `user/scripts/plans/lazy-hardening.md` → "Phase 3 — Routing & parser fixes (cycle-burn class)"
**Plan version:** v2 (reference-based) · **Entry criteria:** Phase 1 baselines green (Phase 2 independent — Phase 3 does NOT require Phase 2).

---

## Touchpoint Summary
AlgoBooth `npm run audit:touchpoints` does not exist in claude-config — not run. Touchpoints: `lazy_core.py`, `lazy-state.py`, `bug-state.py`, the write-plan/PHASES-template + spec-bug-SPEC-template components, `halt-resolution.md`, and the 3 batch SKILLs. These are parser/anchor hardening + a new pseudo-action — guards on existing logic, not net-new surface. No Phase-0 decomposition warranted (skill-file decomposition is Phase 6).

## EXECUTION MODEL — READ THIS FIRST
| Role | Does | Allowed tools |
|------|------|---------------|
| **Orchestrator (you)** | plan, dispatch Agents, review, run `--test` gates + git, update tracking | `Agent`, `Read`, `Bash` (tests/git), task tracking, `Edit`/`Write` on **PHASES.md only** |
| **Sonnet subagent** | write ALL `.py`/`SKILL.md`/`_components/*.md` | `Edit`,`Write`,`Read`,`Bash`,`Grep`,`Glob` |

**HARD CONSTRAINT:** orchestrator never Edits/Writes `.py`/`SKILL.md`/`_components/*.md` (only PHASES.md). Each subagent prompt names the absolute repo root + includes verbatim: *"You do not run `git commit`/`git push`; staging is the orchestrator's; end with a `GROUND-TRUTH OUTPUT` block."*
**Dispatch:** `Agent({ description, model: "sonnet", prompt: "<FULL self-contained>" })`

## COMPONENT LOADING PROTOCOL
Before each step `Read` the listed components from disk (never memory). After compaction re-read this plan first.

## Component Reference Card
| Step | Component | Path |
|------|-----------|------|
| 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| B.1 | TDD Protocol / Subagent Launch / Test Agent / Impl Agent | `~/.claude/skills/_components/tdd-protocol.md` · `subagent-launch.md` · `tdd-test-agent.md` · `implementation-agent.md` |
| B.2 | Subagent Review / Mount-Site | `~/.claude/skills/_components/subagent-review.md` · `mount-site-verification.md` |
| B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` (Repo-Specific override below) |
| Post | Integration Verification / CLAUDE.md Review | `~/.claude/skills/_components/integration-verification.md` · `claude-md-review.md` |
| Final | Work Log | `~/.claude/skills/_components/work-log.md` |

**MCP note:** runtime MCP test is AlgoBooth-specific → N/A; runtime verification = three `--test` gates + fixture RED→GREEN.

## Repo-Specific Quality Gates & Commit Policy
```bash
python3 ~/.claude/scripts/lazy-state.py --test
python3 ~/.claude/scripts/bug-state.py --test
python3 ~/.claude/scripts/test_lazy_core.py
```
Phase 3 changes routing behavior intentionally — new fixtures assert the new routing. **Commit policy:** orchestrator stages/commits/pushes; personal email → push allowed; message `feat(lazy-hardening): <summary>`; no AI attribution.

## References — Upstream Artifacts
| Upstream | Kind | Path | Why |
|----------|------|------|-----|
| design plan | source-of-truth | `user/scripts/plans/lazy-hardening.md` (Phase 3 + D3) | deliverable specs, anchors, locked D3 counters |
| halt-resolution | reference | `~/.claude/skills/_components/halt-resolution.md` | WU-6 fixes its "(max_cycles bounds it regardless)" claim |

## MANDATORY RULES — DO NOT SKIP
1. All code/skill writing → Sonnet subagents (trivial PASS-WITH-FIXES excepted). 2. No worktrees. 3. TDD WUs: failing test first. 4. PHASES.md after each batch. 5. Structured review verdict before continuing. 6. Fix mistakes immediately. 7. Integration verification after batches. 8. CLAUDE.md if warranted. 9. Phase committed+pushed before part done. 10. **D3 split counters are locked** — implement exactly: forward-work cycles consume `max_cycles`; resolution/meta cycles get a separate counter ceiling 2× `max_cycles`; both reported each cycle + run end. 11. Self-contained. 12. Read listed components from disk before each step.

## Execution Schedule
| Step | Phase | Title | Blocked by | Parallel? |
|------|-------|-------|------------|-----------|
| 1 | P3 | Routing & parser fixes | P1 baselines green | Solo phase; internal batches parallel where no file conflict |

---

## Phase: lazy-hardening P3 — Routing & parser fixes (cycle-burn class)

**Goal:** Eliminate the state-script blind spots that burned ~5+ Opus cycles per long run.
**Entry criteria:** Phase 1 baselines green; tree clean. (Phase 2 NOT required.)

### Work Units

#### WU-1 — plan-bug wiring on concluded investigation  *(TDD: yes)*
- **Scope:** "bug-state emits `plan-bug` on concluded investigation (conclusion marker documented in SPEC template); lazy-bug-batch description updated"
- **Files:** `user/scripts/bug-state.py` Step 4 [VERIFY: `grep -n "spec-bug" user/scripts/bug-state.py | head`]; the spec-bug SPEC template doc [VERIFY: `grep -rln "Status:" user/skills/spec-bug/`]; `user/skills/lazy-bug-batch/SKILL.md` description (the "never plan-bug" text) [VERIFY: `grep -n "plan-bug" user/skills/lazy-bug-batch/SKILL.md`]
- **Implementation goal:** bug-state Step 4 currently always emits `spec-bug` when PHASES.md is absent, so a concluded investigation loops. Detect conclusion (SPEC `**Status:** In-progress` OR a `## Conclusion`/investigation-concluded marker — pick ONE, document it in the SPEC template) and emit `plan-bug`. Update lazy-bug-batch's description.
- **Test:** concluded-investigation fixture → `plan-bug` (RED→GREEN).
- **Batch:** 1

#### WU-2 — Fence-aware checkbox parsing + bold-marker heuristic anchoring  *(TDD: yes)*
- **Coupling reason (2 deliverables → 1 WU):** both edit the SAME parsing functions in `lazy_core.py`; fixing one without the other leaves the verification-only heuristic miscounting.
- **Scope:** "Fence-aware `- [ ]` parsing in `count_deliverables` / `remaining_unchecked_are_verification_only` / `_unchecked_wus_in_plan_scope` (+ fixture fix)" · "Verification-only heuristic anchored to `## Runtime Verification` heading (no bold-marker clash)"
- **Files:** `user/scripts/lazy_core.py` [VERIFY: `grep -n "def count_deliverables\|def remaining_unchecked_are_verification_only\|def _unchecked_wus_in_plan_scope" user/scripts/lazy_core.py` → 784,805,609]; fix the `workstation-verification-only-bold-marker` fixture; tests in `test_lazy_core.py`
- **Implementation goal:** skip `- [ ]` lines inside ``` fences (phantom deliverables → plan churn); anchor the verification-only heuristic to the `## Runtime Verification` section heading instead of `**bold:**` prose markers (clashed with `**Assessment:**` twice in production).
- **Test:** fenced checkboxes ignored; substring-/bold-collision row → no false verification-only classification.
- **Batch:** 1

#### WU-3 — Verification-row placement convention  *(TDD: no — docs)*
- **Scope:** "Verification-row placement convention pinned in write-plan component / PHASES template"
- **Files:** the write-plan component + PHASES template [VERIFY: `grep -rln "Runtime Verification" user/skills/_components/ user/skills/write-plan/ 2>/dev/null`]
- **Implementation goal:** pin that runtime-verification checkboxes live under `## Runtime Verification`, NEVER under a phase's `### Deliverables`. (This is the convention WU-2's heuristic now depends on.)
- **Batch:** 1

#### WU-4 — Substring anchoring (roadmap / upstream / is_stub)  *(TDD: yes)*
- **Scope:** "`roadmap_marks_complete` / `upstream_is_complete` / `is_stub_spec` anchored (no substring collisions)"
- **Files:** `user/scripts/lazy-state.py` [VERIFY: `grep -n "def roadmap_marks_complete\|def upstream_is_complete\|def is_stub_spec" user/scripts/lazy-state.py` → 499,703,602]
- **Implementation goal:** `roadmap_marks_complete`/`upstream_is_complete` use bare substring matches (a feature name that is a substring of a completed row spuriously hard-halts the queue) — anchor to whole-row/word-boundary. Anchor `is_stub_spec`'s `Draft (pre-Gemini)` match to the `**Status:**` line / blockquote trailer.
- **Test:** substring-collision ROADMAP row → no false halt.
- **Batch:** 1

#### WU-5 — Stale already-applied plan → inline flip pseudo-action  *(TDD: yes)*
- **Scope:** "Stale already-applied plan → inline flip pseudo-action (not execute-plan)"
- **Files:** `user/scripts/lazy-state.py` (the prober that selects `execute-plan`) [VERIFY: `grep -n "__flip_plan_complete_cloud_saturated__\|execute-plan" user/scripts/lazy-state.py | head`]; shared impl may land in `lazy_core.py`
- **Implementation goal:** when every deliverable a plan references is already `[x]`, emit an inline `__flip_plan_complete_stale__`-style pseudo-action instead of dispatching `execute-plan` (two Opus dispatches were burned re-verifying weeks-old plans).
- **Test:** stale plan (all refs `[x]`) → flip action, not execute-plan.
- **Batch:** 2 (overlaps WU-4 on `lazy-state.py`)

#### WU-6 — D3 split counters + cap reachability + halt-resolution fix  *(TDD: no — prose)*
- **Scope:** "D3: split forward/meta counters (meta ceiling 2× max_cycles) + cap check at top of every resolution mode; halt-resolution.md claim fixed"
- **Files:** the 3 batch SKILLs (`user/skills/lazy-batch`, `user/skills/lazy-bug-batch`, `repos/algobooth/.claude/skills/lazy-batch-cloud`) + `~/.claude/skills/_components/halt-resolution.md` [VERIFY: `grep -n "max_cycles bounds it regardless" user/skills/_components/halt-resolution.md`]
- **Implementation goal (D3 locked):** fix the unreachable-cap bug (the 1a→1b→1g/1h/1i→1a path never passes the Step 1c cap check) by checking the cap at the TOP of every resolution mode; implement forward vs meta counters (meta ceiling 2× `max_cycles`); report both in cycle header + final report. Fix halt-resolution.md's "(max_cycles bounds it regardless)" claim.
  > **⏩ Forward pointer (2026-06-15, commit `e54b296`):** the meta ceiling (2× `max_cycles`) and the resolution-mode cap-checks implemented here were later **removed** per operator decision — `meta_cycles` is now tracked + reported but uncapped/unenforced; only `forward_cycles` is capped. This describes the original D3 work as built; see `docs/specs/lazy-validation-readiness/PHASES.md`.
- **Note:** Phase-2/Phase-6 will further edit lazy-bug-batch; this WU adds D3 counters to it inline (Phase 6 inherits).
- **Batch:** 2 (overlaps WU-1 on lazy-bug-batch SKILL.md)

#### WU-7 — `scoped-id-not-found` terminal + queue diagnostics  *(TDD: yes)*
- **Scope:** "`scoped-id-not-found` terminal; diagnostics for malformed queue entries"
- **Files:** `user/scripts/lazy-state.py` + `user/scripts/bug-state.py` [VERIFY: `grep -n "feature-id\|all-features-complete" user/scripts/lazy-state.py | head`; `grep -n "bug-id\|all-bugs-fixed" user/scripts/bug-state.py | head`]
- **Implementation goal:** scoped `--feature-id`/`--bug-id` matching nothing must emit a distinct `scoped-id-not-found` terminal, not `all-features-complete`/`all-bugs-fixed`. Emit diagnostics for queue entries skipped for missing `name`/`id`/`spec_dir`.
- **Test:** typo'd scope id → `scoped-id-not-found`.
- **Batch:** 3 (overlaps WU-1 on bug-state.py + WU-4/WU-5 on lazy-state.py)

#### WU-8 — Realign hash gate + `check_stale_upstream` wiring + Step-10 NEEDS_INPUT writer  *(TDD: yes)*
- **Scope:** "Realign mtime gate → recorded upstream-PHASES hash; `check_stale_upstream` wired to CLI/probe; Step-10 unexpected-state writes its NEEDS_INPUT.md"
- **Files:** `user/scripts/lazy-state.py` [VERIFY: `grep -n "def check_stale_upstream" user/scripts/lazy-state.py` → 434; called at 3054]
- **Implementation goal:** replace the realign mtime freshness gate with a recorded upstream-PHASES hash in the realign plan frontmatter; wire `check_stale_upstream` to a CLI flag OR auto-run at probe start when `materialized.json` exists (currently no production writer triggers the stale-upstream halt); Step-10 "unexpected state" must WRITE the `NEEDS_INPUT.md` it tells the orchestrator to resolve.
- **Test:** stale-upstream condition → halt emitted; Step-10 unexpected-state → NEEDS_INPUT.md written.
- **Batch:** 4 (overlaps WU-7 on `lazy-state.py`)

### Batch Overview
| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1, WU-2, WU-3, WU-4 | Yes | None (bug-state+spec-bug-template+lazy-bug-batch-desc · lazy_core · write-plan/PHASES docs · lazy-state) |
| 2 | WU-5, WU-6 | Yes | None (lazy-state · batch-SKILLs+halt-resolution); each overlaps a Batch-1 WU |
| 3 | WU-7 | Solo | bug-state+lazy-state (overlaps WU-1, WU-4/5) |
| 4 | WU-8 | Solo | lazy-state (overlaps WU-7) |

**Runtime Verification (gates/fixtures, not WUs):**
- [ ] Regression gates green
- [ ] Fixtures: concluded investigation → plan-bug; fenced checkboxes ignored; substring-collision row → no false halt; stale plan → flip; typo'd scope id → scoped-id-not-found

---

## Execution Protocol
**Step 0:** Read `task-tracking.md`; one task per WU (WU-1..WU-8).
**Phase Selection Loop:** one phase (P3). Announce "Implementing lazy-hardening Phase 3: Routing & parser fixes". Re-read design plan P3 + D3. Execute batches 1→2→3→4. Post-Phase Steps. Report hash.
**Per-Batch Steps:** B.0 source-reread → B.1 launch (read tdd-protocol/subagent-launch/tdd-test-agent/implementation-agent; self-contained Sonnet Agents w/ repo root + no-commit clause + GROUND-TRUTH OUTPUT) → B.2 review (structured verdict, blocking) → B.3 PHASES.md update → B.4 three `--test` gates exit 0 → B.4.5 MCP N/A → B.5 commit → B.6 proceed only when verdict+PHASES+gates+commit all done.
**Post-Phase:** Integration Verification (`integration-verification.md`) — routing fixes cohere, no false halts; CLAUDE.md Review (`claude-md-review.md`) — update `user/scripts/CLAUDE.md` / template docs; Commit and Push the phase.

## Blocking Issue Protocol
If blocked: stop, commit+push completed batches, print `## Implementation — Blocked` (completed WUs+hashes, blocked WU, reason, recovery), do not work around. Blocking: subagent/gate failure unfixable after 2 retries; push conflict unresolvable by rebase; a decision beyond locked D1–D6.

## Completion
P3 done → run all gates final; print `## lazy-hardening Part 3 — Complete` (phase, commits, gates green, "Next: /execute-plan ...part-4.md").

## Append to Work Log (MANDATORY)
Read `~/.claude/skills/_components/work-log.md`; call `interview_work_log_append` (skill, project=claude-config, title, summary, files_modified, technical_context).
