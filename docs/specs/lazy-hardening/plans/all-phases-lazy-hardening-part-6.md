---
kind: implementation-plan
feature_id: lazy-hardening
status: Complete
created: 2026-06-10
phases: [6]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-10.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-hardening\plans\all-phases-lazy-hardening-part-6.md`
> Fully self-contained.

> **Plan series:** part 6 of 7. Siblings part-1 … part-7 under
> `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-hardening\plans\` (this file = part-6).
> Execute parts strictly in order; each is self-contained.
> **WSL equivalent:** swap repo root → `/home/jacob/repos/claude-config`.

# Implementation Plan — Lazy System Hardening (Phase 6)  (v2)

**PHASES.md:** `...\docs\specs\lazy-hardening\PHASES.md` (Phase 6 of 7)
**Design contract:** `user/scripts/plans/lazy-hardening.md` → "Phase 6 — Fork rebuild + contradiction sweep (D4)"
**Plan version:** v2 · **Entry criteria:** Phases 2, 4, 5 complete (the rebuild inherits their canonical lazy-batch text + counters + subcommands).

---

## Touchpoint Summary
AlgoBooth touchpoint audit N/A. **This IS the decomposition phase** the earlier parts' Touchpoint Summaries deferred to: WU-2 extracts ~45KB of inlined prompt templates out of the 108KB `lazy-batch/SKILL.md` into `_components/lazy-batch-prompts/*.md` (load-on-demand), and WU-1 rebuilds the hand-forked `lazy-bug-batch/SKILL.md` by-reference. Highest-regression-risk phase (design-plan risk note). Mitigation: Phase-1 baselines + a post-rebuild compliance read; the OLD lazy-bug-batch text stays retrievable via git, NOT via a parallel file. No separate Phase-0 needed — decomposition is the phase content.

## EXECUTION MODEL — READ THIS FIRST
| Role | Does | Allowed tools |
|------|------|---------------|
| **Orchestrator** | plan, dispatch Agents, review, run `--test` gates + grep/git, update tracking | `Agent`,`Read`,`Bash`(tests/grep/git), task tracking, `Edit`/`Write` on **PHASES.md only** |
| **Sonnet subagent** | write ALL `SKILL.md`/`_components/*.md`/`.py` | `Edit`,`Write`,`Read`,`Bash`,`Grep`,`Glob` |

**HARD CONSTRAINT:** orchestrator edits PHASES.md only. Subagent prompts name the absolute repo root + carry verbatim: *"You do not `git commit`/`push`; staging is the orchestrator's; end with a `GROUND-TRUTH OUTPUT` block."*
**Dispatch:** `Agent({ description, model: "sonnet", prompt: "<FULL self-contained>" })`

## COMPONENT LOADING PROTOCOL
Before each step `Read` listed components from disk. After compaction re-read this plan first.

## Component Reference Card
| Step | Component | Path |
|------|-----------|------|
| 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| B.1 | TDD / Launch / Test Agent / Impl Agent | `~/.claude/skills/_components/{tdd-protocol,subagent-launch,tdd-test-agent,implementation-agent}.md` |
| B.2 | Review / Mount-Site | `~/.claude/skills/_components/{subagent-review,mount-site-verification}.md` |
| B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` (Repo-Specific override) |
| Post | Integration / CLAUDE.md | `~/.claude/skills/_components/{integration-verification,claude-md-review}.md` |
| Final | Work Log | `~/.claude/skills/_components/work-log.md` |

**MCP note:** runtime MCP test AlgoBooth-specific → N/A. Skills are PROSE — primary verification is a compliance read of the rebuilt skill against a dry-run + grep checks (see Runtime Verification). The three `--test` gates still run (lazy-bug-batch description text feeds bug-state's clean-stop list).

## Repo-Specific Quality Gates & Commit Policy
```bash
python3 ~/.claude/scripts/lazy-state.py --test
python3 ~/.claude/scripts/bug-state.py --test
python3 ~/.claude/scripts/test_lazy_core.py
```
Plus grep verification (run after WU-1/WU-2/WU-3):
```bash
# every path reference resolves; zero inline `!`cat`` of sentinel-frontmatter in thin wrappers
grep -rn "cat ~/.claude/skills/_components/sentinel-frontmatter" user/skills/lazy/SKILL.md user/skills/lazy-bug/SKILL.md repos/algobooth/.claude/skills/lazy-cloud/SKILL.md  # must be empty after WU-3
```
**Commit policy:** orchestrator stages/commits/pushes; personal email → push allowed; `feat(lazy-hardening): <summary>`; no AI attribution. Keep WU-1 (rebuild) as its own commit so the old text is cleanly recoverable via git history.

## References — Upstream Artifacts
| Upstream | Kind | Path | Why |
|----------|------|------|-----|
| design plan | source-of-truth | `user/scripts/plans/lazy-hardening.md` (Phase 6 + D4) | full rebuild spec, contradiction inventory |
| canonical lazy-batch | hard | `user/skills/lazy-batch/SKILL.md` | rebuild references it; must reflect Phase 2/4/5 edits |
| lazy-batch-cloud pattern | reference | `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` | the by-reference template to mirror |

## MANDATORY RULES — DO NOT SKIP
1. All skill/code writing → Sonnet subagents. 2. No worktrees. 3. Prose WUs verified by grep + compliance read (no unit tests). 4. PHASES.md after each batch. 5. Structured review verdict. 6. Fix immediately. 7. Integration verification. 8. CLAUDE.md if warranted. 9. Phase committed+pushed; **WU-1 rebuild its own commit.** 10. **D4 is locked:** lazy-bug-batch is rebuilt BY-REFERENCE (path references into lazy-batch + a "Differences from /lazy-batch" table) — NOT a fresh hand-fork. The OLD text is recoverable only via git, never a parallel file. 11. Self-contained. 12. Read components from disk before each step.

## Execution Schedule
| Step | Phase | Title | Blocked by | Parallel? |
|------|-------|-------|------------|-----------|
| 1 | P6 | Fork rebuild + contradiction sweep | Phases 2, 4, 5 | Solo phase; internal batches parallel where no file conflict |

---

## Phase: lazy-hardening P6 — Fork rebuild + contradiction sweep (D4)

**Goal:** Rebuild lazy-bug-batch by-reference and fix every documented contradiction so the family has one source of truth per rule.
**Entry criteria:** Phases 2, 4, 5 complete (canonical lazy-batch text reflects their edits); tree clean.

### Work Units

#### WU-1 — D4: lazy-bug-batch by-reference rebuild  *(TDD: no — prose; own commit)*
- **Scope:** "D4: lazy-bug-batch rebuilt by-reference (cloud pattern) — ports Step 1d.0 pre-boot + NO-FIRE-AND-FORGET, `__mark_fixed__` gate parity (wrapper too), Step 1.5 exclusion parity, 1d.5 dual-trigger wording; six `!cat`s → path references"
- **Files:** `user/skills/lazy-bug-batch/SKILL.md` (full rebuild) [VERIFY: path exists; `grep -c "cat ~/.claude" user/skills/lazy-bug-batch/SKILL.md` → 7 inline `!`cat`` includes today]; reference target `user/skills/lazy-batch/SKILL.md`; mirror pattern `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md`
- **Implementation goal (D4 locked):** mirror the lazy-batch-cloud by-reference pattern ("See ~/.claude/skills/lazy-batch/SKILL.md Step X" + a "Differences from /lazy-batch" table). Must inherit/port: Step 1d.0 mcp-test runtime pre-boot + "RUNTIME IS ALREADY UP" + "NO FIRE-AND-FORGET / resultless return is a violation"; `__mark_fixed__` gate parity (batch runs BOTH gates; `/lazy-bug` runs one — make them identical, wrapper too); Step 1.5 exclusion-set parity; 1d.5 prompt wording covering BOTH spec-bug and spec-phases triggers. Replace its six `!cat`s with path references (~66KB saved per invocation).
- **Verify:** `grep -c "cat ~/.claude" user/skills/lazy-bug-batch/SKILL.md` drops to ~0 (the inline `!`cat`` includes become "See …/SKILL.md" path references); every referenced path resolves.
- **Batch:** 1

#### WU-2 — lazy-batch componentization (extract prompts)  *(TDD: no — prose)*
- **Scope:** "lazy-batch prompt templates + announcement templates extracted to `_components/lazy-batch-prompts/` (read on demand); Step 1f/Step 4 announcement deduped"
- **Files:** new `user/skills/_components/lazy-batch-prompts/*.md` [VERIFY: dir absent today — `ls user/skills/_components/lazy-batch-prompts 2>/dev/null` returns nothing → this WU CREATES it] + `user/skills/lazy-batch/SKILL.md` (replace inlined templates with on-demand Reads)
- **Implementation goal:** extract the three inlined prompt templates (cycle base prompt + per-skill overrides, LOOP block, input-audit prompt) and the Step 1f / Step 4 research-halt announcement templates (near-duplicates — dedupe) into `_components/lazy-batch-prompts/*.md`, Read on demand by cycle type (~45KB of the 108KB file loads on 100% of invocations but is used on ~10% of cycles).
- **Batch:** 1 (distinct files from WU-1/WU-5)

#### WU-3 — Stop sentinel `!cat` in thin wrappers + nested-`!cat` fix + frontmatter trims  *(TDD: no — prose)*
- **Scope:** "sentinel-frontmatter.md no longer `!cat` in thin wrappers; mark-fixed-archive nested-`!cat` fixed; batch-skill frontmatter descriptions trimmed"
- **Files:** `user/skills/lazy/SKILL.md`, `user/skills/lazy-bug/SKILL.md`, `repos/algobooth/.claude/skills/lazy-cloud/SKILL.md` (path-reference sentinel-frontmatter.md instead of `!cat`ing 22KB) [VERIFY: `grep -n "sentinel-frontmatter" user/skills/lazy/SKILL.md`]; `~/.claude/skills/_components/mark-fixed-archive.md` (fix nested-`!`cat`` hazard ~line 19) [VERIFY: `grep -n "cat ~/.claude" user/skills/_components/mark-fixed-archive.md` → 1 include today]; trim the 2–3KB frontmatter descriptions of all 3 batch skills to 2–3 sentences.
- **Batch:** 2 (frontmatter trims overlap WU-1 lazy-bug-batch + WU-2 lazy-batch)

#### WU-4 — Contradiction sweep  *(TDD: no — prose; do them ALL)*
- **Scope:** "Contradiction sweep: cloud constraint renumbering + Step 8/9 drift + lazy-status rows; lazy-batch stale Notes/refs + HARD CONSTRAINT 5 exceptions; sentinel lifecycle table rename-not-delete; component coupling notes include bug consumers; plan-feature artifact; lazy step-label collision"
- **Files (each item small):**
  - `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md`: HARD CONSTRAINT numbering off-by-one; "Step 8 deferral" stragglers [VERIFY: `grep -n "HARD CONSTRAINT" repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md | head`]
  - `user/skills/lazy-status/SKILL.md`: stale Step 8 + missing `__flip_plan_complete_cloud_saturated__` row [VERIFY: `grep -n "Step 8\|__flip_plan_complete" user/skills/lazy-status/SKILL.md`]
  - `user/skills/lazy-batch/SKILL.md`: stale Notes ("orchestrator does not commit anything itself except…"); dangling `__write_deferred_non_cloud__` reference; HARD CONSTRAINT 5 must enumerate the Step 5 resume + adhoc-enqueue AskUserQuestion exceptions (frontmatter desc too) [VERIFY: `grep -n "HARD CONSTRAINT 5\|__write_deferred_non_cloud__" user/skills/lazy-batch/SKILL.md`]
  - `~/.claude/skills/_components/sentinel-frontmatter.md`: lifecycle table — NEEDS_INPUT cleared by **rename**, not delete; identify NEEDS_RESEARCH owner [VERIFY: `grep -n "lifecycle\|rename\|delete" user/skills/_components/sentinel-frontmatter.md | head`]
  - `~/.claude/skills/_components/completion-integrity-gate.md` + `mcp-coverage-audit.md`: coupling notes must list the bug-pipeline consumers [VERIFY: `grep -n "consumer\|bug" user/skills/_components/completion-integrity-gate.md`]
  - `user/skills/plan-feature/SKILL.md`: remove the ~line 75 authoring artifact ("wait, that's the sentinel file…") [VERIFY: `grep -n "that's the sentinel file\|wait," user/skills/plan-feature/SKILL.md`]
  - `user/skills/lazy/SKILL.md`: fix `__mark_complete__` internal step-label collision [VERIFY: `grep -n "__mark_complete__" user/skills/lazy/SKILL.md | head`]
- **Batch:** 3 (overlaps WU-2/WU-3 on lazy-batch, lazy, lazy-cloud, sentinel-frontmatter)

#### WU-5 — lazy-batch-retro grading fixes  *(TDD: no — prose)*
- **Scope:** "lazy-batch-retro: workstation inline-override branch; R-O-3 exceptions; R-O-6 fix; Step 3 scan list; Notes path; Phase-4 park/auto-accept checks"
- **Files:** `repos/algobooth/.claude/skills/lazy-batch-retro/SKILL.md` [VERIFY: path exists]
- **Implementation goal:** add an explicit workstation **inline-override branch** (detect "INLINE OVERRIDE — LOAD-BEARING"; stop relying on the accidental "does NOT have the `Agent` tool" substring); add Sonnet LOOP-DETECTED + 1e.4a recovery dispatches to R-O-3's exception list; fix R-O-6's `-u` expectation; add `__flip_plan_complete_*` + `__mark_fixed__` to the Step 3 scan list; fix the Notes sentinel-frontmatter path (~608); add Phase-4 checks (parks fire notifications; auto-accepts carry two keys + digest entry).
- **Batch:** 1 (distinct file from WU-1/WU-2)

### Batch Overview
| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1, WU-2, WU-5 | Yes | None (lazy-bug-batch · lazy-batch + new components · lazy-batch-retro) |
| 2 | WU-3 | Solo | thin wrappers + mark-fixed-archive + frontmatter trims (overlaps WU-1/WU-2) |
| 3 | WU-4 | Solo | sweep across lazy-batch/lazy/cloud/sentinel/components (overlaps WU-2/WU-3) |

**Runtime Verification (gates/grep/compliance read, not WUs):**
- [ ] Regression gates green
- [ ] Grep: every path reference resolves; zero sentinel-frontmatter `!cat` in wrappers
- [ ] Compliance read of rebuilt lazy-bug-batch against a dry-run (retro-style checklist)

---

## Execution Protocol
**Step 0:** Read `task-tracking.md`; one task per WU (WU-1..WU-5).
**Phase Selection Loop:** one phase (P6). Announce "Implementing lazy-hardening Phase 6: Fork rebuild + contradiction sweep". Re-read design plan P6 + D4 + canonical lazy-batch SKILL (Phase 2/4/5 edits) + the cloud by-reference pattern. Execute batches 1→2→3. Post-Phase Steps. Report hash.
**Per-Batch Steps:** B.0 source-reread → B.1 launch (read tdd-protocol/subagent-launch/tdd-test-agent/implementation-agent; self-contained Sonnet Agents w/ repo root + no-commit clause + GROUND-TRUTH OUTPUT; **WU-1 prompt MUST give the absolute lazy-batch + lazy-batch-cloud paths and the "Differences from /lazy-batch" table requirement, and forbid re-hand-forking**) → B.2 review (structured verdict + compliance read of the rebuild, blocking) → B.3 PHASES.md update → B.4 `--test` ×3 + grep checks → B.4.5 MCP N/A → B.5 commit (WU-1 own commit) → B.6 proceed only when verdict+PHASES+gates+commit done.
**Post-Phase:** Integration Verification (`integration-verification.md`) — rebuilt lazy-bug-batch behaves identically to the canonical text it references; all `!cat`→path conversions resolve; CLAUDE.md Review (`claude-md-review.md`); Commit and Push.

## Blocking Issue Protocol
If blocked: stop, commit+push completed batches, print `## Implementation — Blocked` (completed WUs+hashes, blocked WU, reason, recovery), do not work around. Blocking: the by-reference rebuild can't preserve a behavior the old fork had (escalate — do NOT silently re-fork); subagent/gate failure unfixable after 2 retries; push conflict unresolvable by rebase.

## Completion
P6 done → run gates + grep checks + compliance read final; print `## lazy-hardening Part 6 — Complete` (phase, commits, gates green, grep clean, "Next: /execute-plan ...part-7.md").

## Append to Work Log (MANDATORY)
Read `~/.claude/skills/_components/work-log.md`; call `interview_work_log_append` (skill, project=claude-config, title, summary, files_modified, technical_context).
