---
kind: implementation-plan
feature_id: mid-build-code-review-checkpoints
status: Complete
complexity: mechanical
created: 2026-06-15
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-15.
> To execute: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\mid-build-code-review-checkpoints\plans\all-phases-mid-build-code-review-checkpoints.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — mid-build-code-review-checkpoints  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\mid-build-code-review-checkpoints\PHASES.md` (mid-build-code-review-checkpoints, 1 phase)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\mid-build-code-review-checkpoints\SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

> **REPO NOTE — this plan operates on the `claude-config` repo, NOT Cognito Forms.**
> Working directory for all edits: `C:\Users\JacobMadsen\source\repos\claude-config`.
> Git identity here is **personal** (`jacobmadsen12321@gmail.com`) and pushes are **not** blocked
> by the work-repo hook. The edited files are symlinked to their live locations
> (`~/.claude/skills/execute-plan/SKILL.md` and `<cognito-forms>/.claude/skill-config/`), so editing
> the repo source writes through to the live skill. Do NOT edit through the symlinked live paths —
> edit the real targets under `claude-config/`.

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run verification, update tracking docs | `Agent`, `Read`, `Bash` (verification only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL component/markdown content and skill edits | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on the deliverable files. The deliverables here are markdown/skill-config files — they are still authored by Sonnet subagents via the `Agent` tool. The ONLY files you may modify directly: `PHASES.md`.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies. SPEC `**Depends on:** (none)`.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL deliverable authoring MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on the deliverable files. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines).
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. This feature has NO testable code surface (markdown skill-config + prose injection). TDD does not apply — see Step B.1 TDD decision gate; both WUs are non-TDD. Verification is by projection + inspection, not a test runner.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's output is reviewed for correctness and spec alignment before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in the phase finish, integration verification runs `project-skills.py` and confirms the projection diff.
8. Relevant CLAUDE.md files are reviewed/updated after the phase if changes warrant it.
9. The completed phase is committed (and pushed — personal repo, push allowed) before completion.
10. This plan is self-contained — follow it exactly as written without relying on external context.
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
12. **Do NOT author or tick the SPEC.md `**Status:**` flip as a WU** — that transition is gate/manual-owned (Step 1c.5 gate-owned-row ban). The plan's job ends at the two file deliverables + projection verification.

---

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | mid-build-code-review-checkpoints | P1 | Cognito-Forms-scoped per-batch Why↔How checkpoint | — | Solo phase / Batch 1 = WU-1 + WU-2 parallel |

---

## Work Units

- [x] WU-1 — Create `post-phase-code-review-checkpoint.md` skill-config component
- [x] WU-2 — Add fallback-echo injection sub-step to `execute-plan/SKILL.md` Step 3 + renumber

---

## Phase: mid-build-code-review-checkpoints P1 — Cognito-Forms-scoped per-batch Why↔How checkpoint

**Goal:** Add a non-blocking, interactive-only, per-batch Why↔How code-review checkpoint to the Cognito Forms projection of `/execute-plan`, delivered via the `.claude/skill-config/` fallback-echo injection convention so every other repo resolves it to a no-op.

**Entry criteria:** None (single-phase feature; no upstream dependencies).

**SPEC.md references:**
- "Technical Design → Delivery mechanism — `.claude/skill-config/` injection" (the `_FALLBACK_ECHO` form).
- "Technical Design → Component contents (new file)" — the 5-point component contract.
- "Technical Design → Injection point (edit to global skill)" — insert between Step 3 items 8 and 9.
- "User Experience" — the rendered checkpoint format example (📋 header, Purpose, Why→How `file:symbol` anchors, "Continuing to Batch N").
- "Validation Criteria" table — the six rows the deliverables must satisfy.

### Batch Overview

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2 | Yes      | None (distinct files) |

> WU-2's injection line references the component **filename** (fixed by SPEC), not its **contents**, so there is no ordering dependency. Distinct files → both run in Batch 1 in parallel.

---

### WU-1 — Create `post-phase-code-review-checkpoint.md` skill-config component

**Scope (PHASES.md deliverable 1):** Net-new component `repos/cognito-forms/.claude/skill-config/post-phase-code-review-checkpoint.md` implementing the SPEC's component contract.

**TDD:** no — markdown skill-config prose; no compiled code, no test surface.

**Files to create/modify:**
- **CREATE (net-new):** `C:\Users\JacobMadsen\source\repos\claude-config\repos\cognito-forms\.claude\skill-config\post-phase-code-review-checkpoint.md`
  - Confirmed net-new: the directory `repos/cognito-forms/.claude/skill-config/` exists and contains 13 sibling components [VERIFY: ls "C:\Users\JacobMadsen\source\repos\claude-config\repos\cognito-forms\.claude\skill-config\"] but NO file of this name yet.

**Test files:** none.

**Implementation goal — the component file MUST encode ALL FIVE contract points (SPEC "Component contents"):**
1. **Trigger:** fires after the batch's atomic gate+commit lands (Step 3 item 8 of `execute-plan`) and before proceeding to the next batch (item 9). Exactly one checkpoint per committed batch.
2. **Interactive-only guard:** explicitly state *skip entirely under `--batch`* — under autonomous `/lazy-batch` there is no chat audience, so the orchestrator emits nothing.
3. **Content contract:** concise Why↔How prose, 2–5 sentences. State the batch's **purpose** (drawn from PHASES.md/SPEC.md intent + the WU scope), then map each part of the purpose to the concrete `file:symbol` locations that fulfill it. Instruct the orchestrator to **reuse the material from the Batch Review Gate it just completed** — do not re-analyze from scratch.
4. **Non-blocking mandate:** the orchestrator **MUST NOT call `AskUserQuestion`**, **MUST NOT pause**, and **MUST proceed immediately** to the next batch after printing. Steering is the user's responsibility via interrupt; the run never waits.
5. **No new artifacts:** chat-only — writes no files, does not alter PHASES.md, the plan, or commits.
   - Include a rendered format guide mirroring the SPEC "Format example": a `📋 Batch N checkpoint — Why → How` header, a `Purpose:` line, bulleted Why→How `file:symbol` anchors, and a closing `Continuing to Batch N+1.` line. (Header wording is a low-stakes detail per SPEC Open Questions — match the example.)

**Spec requirements:** SPEC "Technical Design → Component contents (new file)" (verbatim 5-point contract) and the "Validation Criteria" rows for *Non-blocking*, *Suppressed under `--batch`*, and *Why↔How content shape*.

**Reuse directive:** model the file's tone/structure on existing sibling skill-config instruction components — e.g. `repos/cognito-forms/.claude/skill-config/cog-doc-track-open.md` [VERIFY: ls "C:\Users\JacobMadsen\source\repos\claude-config\repos\cognito-forms\.claude\skill-config\cog-doc-track-open.md"]. Do NOT invent a new component format.

**Style constraints (repo conventions):**
- This is a config-repo markdown file. Follow the surrounding skill-config component prose style.
- Do NOT reference this plan, PHASES.md, SPEC.md, work-item ids, or the implementation process inside the component text — it is shipped instruction prose, not a planning artifact.

---

### WU-2 — Add fallback-echo injection sub-step to `execute-plan/SKILL.md` Step 3 + renumber

**Scope (PHASES.md deliverable 2):** Add the injection sub-step to `user/skills/execute-plan/SKILL.md` Step 3 "Per-Step Protocol", inserted between item 8 ("Commit the batch atomically") and item 9 ("Proceed"), using the `_FALLBACK_ECHO` form. Renumber the following item (9 "Proceed" → 10).

**TDD:** no — prose injection into a skill file; no test surface.

**Files to create/modify:**
- **EDIT:** `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\execute-plan\SKILL.md`
  - Verified: Step 3 "Per-Step Protocol" numbered list, item 8 = "**Commit the batch atomically**" (~line 259), item 9 = "**Proceed** — Move to the next step/batch" (~line 260). [VERIFY: grep -n "Commit the batch atomically" "C:\Users\JacobMadsen\source\repos\claude-config\user\skills\execute-plan\SKILL.md"]
  - This file is symlinked to `~/.claude/skills/execute-plan/SKILL.md`; editing the repo source writes through. Edit the repo source only.

**Test files:** none.

**Implementation goal:**
1. Insert a **new numbered sub-step between item 8 and item 9** of Step 3's Per-Step Protocol. The new step:
   - Carries a one-line framing sentence so the no-op case still reads coherently (e.g. "**Mid-build checkpoint (project-scoped)** — after the commit lands and before proceeding, emit the project's per-batch code-review checkpoint if one is configured:").
   - Then, on its own line, the EXACT fallback-echo injection (this is the literal line `project-skills.py` resolves — match the `_FALLBACK_ECHO` regex exactly):
     ```
     !`cat .claude/skill-config/post-phase-code-review-checkpoint.md 2>/dev/null || echo "<!-- no per-batch code-review checkpoint configured for this repo -->"`
     ```
2. **Renumber** the former item 9 ("Proceed") to item 10. If any later text in Step 3 cross-references "item 9" / "item 8" by number, update those references to stay consistent (verify with a grep for `item 9`/`item 8` within Step 3 after editing).
3. Do NOT alter item 8's text or the Batch Review Gate (item 4) — only insert + renumber.

**Spec requirements:** SPEC "Technical Design → Injection point (edit to global skill)" and "Technical Design → Delivery mechanism" (the exact fallback-echo line). The injection line MUST byte-match the `_FALLBACK_ECHO` regex in `user/scripts/project-skills.py`: `^!`cat\s+\.claude/skill-config/(.+?)\s+2>/dev/null\s*\|\|\s*echo\s+"(.+?)"`$`.

**Reuse directive:** mirror the existing fallback-echo precedents — `user/skills/investigate/SKILL.md:89` and `user/skills/_components/quality-gates.md:7` [VERIFY: grep -n "2>/dev/null || echo" "C:\Users\JacobMadsen\source\repos\claude-config\user\skills\_components\quality-gates.md"]. Same mechanism, no new convention.

**Blast-radius note:** `execute-plan/SKILL.md` is a high-traffic shared skill. The change is purely additive (one injected sub-step + a renumber) and resolves to a literal HTML-comment no-op in every repo except Cognito Forms, so behavior elsewhere is unchanged. The reviewer MUST confirm the no-op fallback renders correctly for `_default`.

---

## Execution Protocol

This protocol governs the autonomous execution of the phase. Follow it exactly.

### Phase Selection Loop

Single phase — execute it, then run Post-Phase Steps, then go to Completion.

1. **Select ready phase:** Phase 1 (entry criteria: none).
2. **Announce:** Print "Implementing mid-build-code-review-checkpoints Phase 1: Cognito-Forms-scoped per-batch Why↔How checkpoint".
3. **Review prior context:** none (terminal single phase). Re-read PHASES.md + SPEC.md per Step B.0.
4. **Execute Batch 1** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "mid-build-code-review-checkpoints Phase 1 — committed as [hash]".
7. Proceed to Completion.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly. Create tasks for WU-1 and WU-2.

### Per-Batch Steps

For Batch 1:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow it. Re-read from disk: this plan file, PHASES.md (Phase 1 + Runtime Verification rows), and SPEC.md ("Component contents", "Injection point", "User Experience" format example, "Validation Criteria").

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — ZERO INLINE AUTHORING)

**PRE-FLIGHT CHECK:** You will use the `Agent` tool with `model: "sonnet"` for BOTH deliverables, and will NOT call `Edit`/`Write` yourself.

Read before proceeding:
1. `~/.claude/skills/_components/tdd-protocol.md` — TDD decision gate. **Both WUs are non-TDD** (no testable code surface). The gate confirms direct-implementation path; skip the test-agent phase.
2. `~/.claude/skills/_components/subagent-launch.md` — launch orchestration + failed-agent recovery.
3. `~/.claude/skills/_components/implementation-agent.md` — impl agent prompt template; include verbatim in each impl agent's prompt.

Dispatch WU-1 and WU-2 as two parallel Sonnet impl agents in a single message (distinct files, no conflict). Each agent prompt MUST include: the full WU scope above, the SPEC excerpts it implements, the verified file paths, and the repo note (edit `claude-config` source, not symlinked live paths). For WU-2, hand the agent the EXACT fallback-echo line to insert and the renumber instruction.

(Ignore `tdd-test-agent.md` for this batch — no test agents are launched.)

**POST-DISPATCH GATE:** After both subagents complete, verify you composed `Agent` calls and did NOT edit files directly.

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` and follow its complete protocol. Also read `~/.claude/skills/_components/mount-site-verification.md` (WU-1 is a net-new file — confirm it lands at the exact path and is wired by the injection line in WU-2).

Review focus for this feature:
- **WU-1:** all 5 contract points present and unambiguous? Non-blocking mandate explicit ("MUST NOT call AskUserQuestion / MUST proceed immediately")? Interactive-only / skip-under-`--batch` guard explicit? Why↔How shape (purpose + `file:symbol` anchors, 2–5 sentences) specified? No planning-artifact leakage in the prose?
- **WU-2:** injection line byte-matches the `_FALLBACK_ECHO` regex? Inserted strictly between item 8 and item 9? Item 8 text untouched? Renumber complete and any in-Step-3 numeric cross-references updated? Framing sentence reads coherently in the no-op case?

Produce a structured review report with a PASS / PASS-WITH-FIXES / NEEDS-REWORK verdict. Fix mistakes before Step B.3.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md` and follow it. Check off the two implementation deliverables in Phase 1 (the component + the injection sub-step). Add an Implementation Notes block (date, work completed, files modified, any pitfalls). **Do NOT check the three "Runtime Verification" rows** — those are ticked by the manual projection inspection in Post-Phase / Completion, not by implementation. **Do NOT flip SPEC.md `**Status:**`** (gate/manual-owned).

#### Step B.4: Run Quality Gates (Projection Verification) (MANDATORY — DO NOT SKIP)

There is no compiler/test runner for this config-repo prose change. The quality gate IS the projection check. Run:

```bash
python ~/.claude/scripts/project-skills.py
```

Confirm it **runs clean (exit 0)** with no circular-include or unresolved-`!cat` errors. (Read `~/.claude/skills/_components/quality-gates.md` for the general gate philosophy, but the project-skills.py run is the concrete gate here.)

Then verify the projection diff at the injection point (this is the SPEC's Minimum Verifiable Behavior + Validation Criteria rows 1–2):
- `skills-projected/cognito-forms/execute-plan/SKILL.md` — the **rendered Why↔How checkpoint instruction block** appears, positioned between the commit step and the proceed step.
- `skills-projected/_default/execute-plan/SKILL.md` — **ONLY** the `<!-- no per-batch code-review checkpoint configured for this repo -->` no-op echo appears at the injection point; no checkpoint instructions.

If either projection is wrong, treat as a gate failure: fix (re-dispatch the relevant WU) and re-run before proceeding.

#### Step B.4.5: MCP Integration Test

**SKIP** — PHASES.md declares `MCP runtime: not-required` and `MCP Integration Test Assertions: N/A`. No runtime-observable behavior; validation is projection + inspection (done in Step B.4). Note the skip and continue.

#### Step B.5: Commit Batch

Read the commit policy: first try `.claude/skill-config/commit-policy.md` in the project root; if absent, read `~/.claude/skills/_components/commit-and-push.md`. **Note:** the working repo is `claude-config` (personal identity, push NOT blocked) — commit the component + SKILL.md edit + PHASES.md update together in the `claude-config` repo. These files are NOT tracked by the Cognito Forms host repo.

#### Step B.6: Proceed

**Checklist before proceeding (all must be true):**
- [ ] Review report produced with a verdict.
- [ ] PHASES.md implementation deliverables checked + Implementation Notes added (Runtime Verification rows left unchecked; SPEC Status not flipped).
- [ ] `project-skills.py` runs clean AND both projections verified (cognito-forms = rendered, _default = no-op).
- [ ] Batch committed per policy.

Single batch → proceed to Post-Phase Steps.

### Post-Phase Steps (after Batch 1)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md` and follow it. For this feature, integration verification = the projection diff from Step B.4 plus a contract spot-check of the component text against all six "Validation Criteria" rows in SPEC.md. Confirm the injected step reads coherently end-to-end in the projected `cognito-forms` SKILL.md (Per-Step item 8 → checkpoint → item 10 Proceed).

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md` and follow it. Likely no update needed (single skill-config component using an established convention) — but check whether `claude-config/CLAUDE.md` or the cognito-forms repo skill-config docs enumerate skill-config components and should list the new one.

#### Commit and Push Post-Phase Changes

Read the commit policy (as in Step B.5). Push to the `claude-config` remote (personal identity — push allowed). If everything was already committed in Step B.5 and no post-phase doc changes were made, just push.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed work** not yet committed.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed work:** [list with commit hashes]
   **Blocked work unit:** [WU-N: title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining work (not attempted):**
   - [list]

4. **Do not work around the blocker.** Surface it and wait for the user.

Blocking issues include:
- `project-skills.py` fails to run clean and the cause can't be fixed after 2 attempts (e.g. the injection line doesn't match the `_FALLBACK_ECHO` regex and the correct form can't be determined).
- A subagent failure that can't be fixed after 2 retry attempts.
- The projection renders incorrectly (checkpoint leaks into `_default`, or no-op appears in `cognito-forms`) and the root cause can't be resolved after 2 attempts.
- Any error requiring architectural decisions beyond the scope of the SPEC.

---

## Completion

When Batch 1 is complete and verified:

1. **Run the projection gate one final time:** `python ~/.claude/scripts/project-skills.py`, then re-confirm both projections (cognito-forms = rendered checkpoint block; _default = no-op echo only). This is the authoritative final verification — there is no separate test suite.
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Features implemented:** mid-build-code-review-checkpoints
   **Total phases completed:** 1
   **Total commits:** [N]
   **Final verification status:** project-skills.py clean; cognito-forms projection renders the Why↔How checkpoint block between commit and proceed; _default projection shows only the no-op echo.

   **Commit log:**
   | Commit | Phase | Title |
   |--------|-------|-------|
   | [hash] | P1 | Cognito-Forms-scoped per-batch Why↔How checkpoint |

   **Implementation Notes summary:**
   [Key notes: the exact injected fallback-echo line, the component's 5 contract points, and the renumber. Note that SPEC `**Status:**` flip to Complete is a separate gate/manual step — NOT done by this plan.]

3. **Reminder to the user:** the SPEC.md `**Status:**` flip from `Draft` → `Complete` is gate/manual-owned and was intentionally NOT performed by this plan. Flip it separately if/when the feature is accepted.
