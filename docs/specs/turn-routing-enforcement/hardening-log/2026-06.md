# Hardening Log — 2026-06

Rounds are APPENDED (never overwritten). One file per calendar month. Each round follows the
`/harden-harness` Step 4 template.

---

## Round 1 — 2026-06-12 — validate-deny

**Item in flight:** e2e-validation

**Divergence point:** E2E probe assertion 2 — a deliberately hand-composed `say hello` dispatch
was submitted as test input (not a `--emit-prompt` / `--emit-dispatch <class>` script-emitted
prompt). The `lazy-dispatch-guard.sh` → `lazy_guard.py` PreToolUse guard denied it. The "divergence"
from the scripted path is synthetic: the E2E harness composed the prompt by hand on purpose to
exercise the deny path. Per locked decision 4 the deny then routed to this hardening stage
(depth-0, inline, unbounded).

**Root cause:** **none — working-as-designed (deny-path assertion validated).** This is the
intended outcome of E2E assertion 2, not a harness defect. Evidence:
- The denial reason in the dispatch is byte-for-byte the canonical corrective recipe at
  `~/.claude/scripts/lazy_guard.py:124-133` (`_CORRECTIVE_RECIPE`) — the guard emitted exactly
  the contract-specified deny, not an error or a malformed reason.
- Every legitimate hand-composed dispatch class already has a `--emit-dispatch <class>` path
  (SPEC.md §Validate-deny, line 79 — the D7-complete option: no "trusted hand prompt"
  allowlist, zero unregistered legitimate dispatches). The `say hello` probe corresponds to no
  legitimate dispatch class — it is synthetic test input designed to be denied.
- No orchestrator procedure step improvised a hand prompt: the five formerly-hand-composed
  classes (apply-resolution 1g/1h, input-audit 1d.5, `/investigate`, recovery/coherence-recovery,
  NEEDS_RUNTIME re-dispatch) are all `--emit-dispatch`-bound per SPEC line 79.
- Depth guard is intact: `_HARDENING_DEPTH_CAP_REASON` (`lazy_guard.py:135-140`) halts at depth 1
  with halt + PushNotification and no recursive `--emit-dispatch hardening`. This dispatch is
  depth-0 and passed normally.

Because the harness behaved exactly as designed, there is **no mechanical fix** (inventing one
would risk weakening a correct, load-bearing gate — prohibition 2) and **no contract fork**
(an E2E-synthetic exemption would be dedup-by-category, which locked decision 4 explicitly
overrode — "every occurrence is signal, not noise"; re-litigating a locked decision is out of
scope for this stage). The integrity-preserving terminal state is this no-change round.

**Action:**
  - No harness change. Deny-path E2E assertion 2 validated: the guard denied a deliberately
    hand-composed probe with the canonical corrective recipe and routed the deny to the
    hardening stage inline (locked decision 4). Full gate suite run as health evidence (all
    green). No commit, no NEEDS_INPUT.md — recording a fix or a fork here would be dishonest
    (no defect exists) and/or gate-weakening.

**Gates run:**
  test_lazy_core.py: 277/277
  test_hooks.py: 21/21
  lint-skills.py: OK
  lazy-state.py --test: OK
  bug-state.py --test: OK


## Round 2 — 2026-06-12 — manual

**Item in flight:** d8-stem-management (AlgoBooth session `2f6f27dc`, `/lazy-batch 70 --park`)
**Divergence point:** three validate-denies (execute-plan cycle dispatch, the hardening dispatch itself, a recovery dispatch) with ZERO executed hardening rounds; §1d.1 depth-cap declined as too blunt for a transcription-slip denial; self-elected early stop (fwd 14/70) with no contract shape; 0/8 meta dispatches carried canonical headings.

**Root cause:** ambiguous-prose + missing-contract — (a) §1d.1's depth-cap paragraph conflated the guard's two deny shapes (corrective recipe vs halt reason), prescribing a full halt for a mere transcription slip, so the orchestrator deviated and abandoned the stage; (b) "EVERY deny dispatches hardening" existed only as prose with no mechanical state, so denial #3 silently skipped it; (c) the unattended early-stop case had no sanctioned shape (budget-and-queue guard assumed an attended operator); (d) `--emit-dispatch` shipped no `cycle_header`, so meta headings had nothing mechanical to echo; (e) multi-slot template interpolation maximized the transcription surface (the hardening dispatch's own denial). Evidence: AlgoBooth `docs/features/_index/LAZY_BATCH_REVIEW_2026-06-12_overview.md` + per-feature artifacts; jsonl L201/L237/L247/L503/L639.

**Action:**
  Mechanical fix applied: Phase 7 of turn-routing-enforcement (deny ledger written by the guard + probe-surfaced `pending_hardening` debt + run-end refusal; §1d.1 retry branch keyed on the guard's deny shape; single-slot templates + widened hash normalization + re-emit-fresh rule; sanctioned unattended checkpoint contract; meta `cycle_header` + T7 D7 digest skeleton + spin-off legs). Gates run: test_lazy_core.py 291/291, test_hooks.py 22/22, lint-skills.py OK, --test suites OK (baselines byte-identical). Commit: 511b0b8 (plan: 31f0686).

**Gates run:**
  test_lazy_core.py: 291/291
  test_hooks.py: 22/22
  lint-skills.py: OK
  lazy-state.py --test: OK
  bug-state.py --test: OK


## Round 3 — 2026-06-12 — manual

**Item in flight:** d8-stem-management (AlgoBooth session `e076ed30`, `/lazy-batch 50`) + the concurrent interactive session
**Divergence point:** (1) a concurrent interactive session's inject hook DELETED the live run's marker (~14:53Z) via `read_run_marker` staleness path B (delete-on-read), silently disarming enforcement mid-run; (2) the orchestrator field-extracted probe JSON and dispatched a forward route over `pending_hardening: 1`; (3) Phase 7's emission-time ack could drain debt without any dispatch.

**Root cause:** script-defect + ambiguous-prose — path B assumed sessions are sequential ("a leftover marker from a crashed run") but concurrent sessions are normal operation; Phase 7 surfaced debt as data instead of routing it; ack-at-emission measured the wrong event. Evidence: marker absent at 14:55Z with the run mid-cycle; session `e076ed30` L158 extractor pipe; live registry/ledger inspection.

**Action:**
  Mechanical fix applied: Phase 8 of turn-routing-enforcement — `read_run_marker` path B now non-destructive (non-owner sees None, file survives; age/corrupt deletion unchanged); probe WITHHOLDS the forward route while debt pending (`route_overridden_by` + pre-composed `hardening_emit_command`, ⚠ stderr line); ack moved to guard-allow time (hardening-class allow acks oldest, fresh-consumption path only); full-probe-JSON consumption rule (extractor piping banned) mirrored ×3 skills. Gates: test_lazy_core.py 297/297, test_hooks.py 23/23, lint-skills.py OK, --test suites OK. Commit: ee2289e (plan: b3bde50).

**Gates run:**
  test_lazy_core.py: 297/297
  test_hooks.py: 23/23
  lint-skills.py: OK
  lazy-state.py --test: OK
  bug-state.py --test: OK


## Round 4 — 2026-06-12 — validate-deny

**Item in flight:** d8-stem-management (Step 9 mcp-test, `pending_hardening=1`)

**Divergence point:** Step 1d.0 → Step 1d (mcp-test cycle dispatch, Step 9). The orchestrator hand-edited an emitted Step-9 `cycle_prompt` (spliced a fresh-restart paragraph into the RUNTIME-IS-ALREADY-UP block) before the `Agent` call; `lazy-dispatch-guard.sh` → `lazy_guard.py` denied — the dispatched prompt's hash (`denied_sha12: 852659ca3a8f`) matched no registered emission (guard case 3, "no matching entry at all"). **This is the SAME deny event already hardened in the L444-prose round (commit `62077a0`).** Evidence: the single deny-ledger entry (`~/.claude/state/lazy-deny-ledger.jsonl`) carries `denied_sha12 852659ca3a8f`, deny `ts 1781275027` (14:37:07Z); `852659ca3a8f` is absent from the registry's cycle shas (`c584a1`, `0f2ec2`, `c1727c`, `01a3ac`, `d63fc3`, `fa3c86`) — confirming a hand-composed, never-registered prompt.

**Root cause:** none for THIS round — **working-as-designed (orphaned-debt re-route + ack-on-execution).** The triggering misroute's root cause was `ambiguous-prose` (the stale L444 "(amend the prompt only)" parenthetical in the runtime-up branch) and was already mechanically fixed by commit `62077a0` ("harden(skill-prose): drop stale 'amend the prompt only' from lazy-batch runtime-up branch", 17:57:16Z). Re-applying that fix here would violate the "do not re-investigate the same root cause without new evidence" input rule. Why a SECOND hardening dispatch fired for an already-hardened deny: the `62077a0` fix retired the *prose defect* but NOT the *deny-ledger debt* — under Phase 8 WU-8.2 the only mechanism that acks a ledger entry is `lazy_guard.py::_ack_if_hardening` at guard-allow time of a registry-emitted `hardening`-class dispatch (`lazy_guard.py:241-261, 353`; deliberate Phase-8 move so a bare `--emit-dispatch hardening` cannot drain the ledger without a dispatch reaching execution — `test_emit_dispatch_hardening_no_longer_acks`). The `62077a0` fix was performed out-of-band (no guard-consumed hardening dispatch), so the entry stayed `acked: false`; the `--emit-prompt` probe correctly WITHHELD the forward route (`lazy-state.py:5194-5212`, `route_overridden_by: pending-hardening-debt`) and re-routed a hardening dispatch — exactly the inline-unbounded, no-dedup cadence (locked decision 4: "every occurrence is signal, not noise"). This dispatch's guard-allow has now FIFO-acked the entry (ledger shows `acked: true`, `acked_ts 1781292217` / 19:23:37Z) — the mechanical debt is retired. The machinery (guard deny, ledger append, probe withhold, guard-allow ack) all behaved exactly as designed; there is no new defect to fix, and inventing one would risk weakening a correct gate (prohibition 2). A dedup-by-signature suppression ("this deny was already hardened, skip the re-route") would re-litigate locked decision 4 and is explicitly out of scope for this stage.

**Process observation (no action — out of this agent's scope):** the `62077a0` round was logged ONLY to the AlgoBooth working-tree copy `docs/specs/turn-routing-enforcement/hardening-log/2026-06.md` (git-tracked in AlgoBooth, numbered "Round 1" there), NOT appended to this canonical claude-config log — so the canonical log skipped from Round 3 to this Round 4 and the L444 round is orphaned in the wrong repo. The harness-hardening agent MAY NOT edit the target repo (prohibition 1), so the AlgoBooth copy is left untouched; surfacing the divergence here is the integrity-preserving action. The two logs should be reconciled by the operator (canonical artifact = claude-config).

**Action:**
  - No harness change. The triggering misroute was already mechanically fixed by `62077a0`; the duplicate hardening dispatch is the intended orphaned-debt re-route under locked decision 4 + Phase-8 ack-on-execution, and this dispatch's guard-allow has retired the ledger debt. Recording a fix or a contract fork here would be dishonest (no new defect) and/or gate-weakening. Full gate suite run as health evidence (all green).

**Gates run:**
  test_lazy_core.py: 297/297
  test_hooks.py: 23/23
  lint-skills.py: OK
  lazy-state.py --test: OK
  bug-state.py --test: OK


## Round 5 — 2026-06-12 — manual

**Item in flight:** d8-stem-management (AlgoBooth session `e076ed30`, resumed `/lazy-batch 50`) + concurrent interactive session
**Divergence point:** bind-on-first-hook-firing race — the run's bind-pending marker was stamped by a CONCURRENT interactive session's inject (19:33Z), disarming the run's guard (non-owner fast-path) and inflating its persisted repeat counters (3→4, 10→11) via bystander banner probes.

**Root cause:** script-defect (design) — inject fires before a turn's work, so the orchestrator's invocation turn can never bind its own marker; "first hook firing anywhere wins" hands the bind to whichever session the operator touches next. Evidence: marker `started_at 19:22:43Z` bound to `2899da98…` (interactive) at 19:33Z; hook-context captures turn 3/4.

**Action:**
  Mechanical fix applied: Phase 9 of turn-routing-enforcement — inject NEVER binds (unbound marker → silent exit, zero side effects); guard binds on its first ALLOW of a registered prompt (an anchor bystanders cannot forge), fail-open, both allow paths; deny never binds. Immediate remediation: operator-authorized hand-rebind of the live marker at 19:38Z. Gates: test_lazy_core.py 304/304, test_hooks.py 23/23, lint-skills.py OK, --test suites OK. Commit: ff4a130 (plan: e69b384).

**Gates run:**
  test_lazy_core.py: 304/304
  test_hooks.py: 23/23
  lint-skills.py: OK
  lazy-state.py --test: OK
  bug-state.py --test: OK


## Round 6 — 2026-06-12 — validate-deny

**Item in flight:** clap-plugin-hosting (AlgoBooth session `e076ed30`, `/lazy-batch 50`, Step 7a write-plan)

**Divergence point:** Step 1d → the Step-7a (`write-plan`) forward cycle dispatch. The orchestrator hand-modified its copy of an emitted+registered `write-plan` cycle prompt before the `Agent` call; `lazy-dispatch-guard.sh` → `lazy_guard.py` denied — the dispatched prompt's hash (`denied_sha12: 7cf05c071951`) matched no registry entry (guard case 3, "no matching entry at all"). The deny then routed THIS hardening dispatch inline (locked decision 4).

**Root cause:** none for THIS round — **working-as-designed re-route (the triggering misroute's root cause is the hand-edit/transcription class already mechanically hardened in Rounds 2 & 4).** Evidence that the deny was a hand-modified copy of a legitimately-emitted prompt, not a novel gap:
- The registry holds a clap `cycle` emission `071a072e4c75` emitted at `1781295737` (`consumed: false` — never consumed); the deny fired 86s later at `1781295823` against a DIFFERENT sha `7cf05c071951` that is absent from every registry sha (`071a072e4c75`, `b15740d775ab`, `52ce2e4f0a36`). A registered prompt was available; a hand-edited one was dispatched → hash mismatch (`lazy_guard.py:130-139` `_CORRECTIVE_RECIPE`, byte-for-byte the deny reason in this dispatch).
- No pending debt existed when `071a072e4c75` was emitted: the only prior unacked denial (`852659ca3a8f`, d8) was acked at `1781292217` < `1781295737`, so `pending_hardening()` was 0 and the probe CORRECTLY emitted the forward route (`lazy-state.py:5196` withhold condition false) — no script defect in the emit decision.
- The Phase-8 withhold machinery then fired exactly as designed: the deny appended ledger entry `7cf05c071951`; the next `--emit-prompt` probe WITHHELD the forward route (`route_overridden_by: pending-hardening-debt`, `lazy-state.py:5196-5212`) and produced the `_probe_summary` string `step=Step 7a: write plan sub_skill=write-plan feature_id=clap-plugin-hosting pending_hardening=1` — byte-identical to this dispatch's injected probe JSON; the hardening dispatch was registered (`52ce2e4f0a36`, class `hardening`); and the guard-allow of THIS dispatch FIFO-acked the entry (`acked: true`, `acked_ts 1781295875`, `lazy_guard.py::_ack_if_hardening:244-264`). Ledger debt is retired.
- The hand-edit failure class is already exhaustively named in `lazy-batch/SKILL.md` (line 475 "Hand-editing emitted text is the failure class", line 502 the Phase-8 withhold contract, lines 506-507 verbatim-dispatch + every-deny-routes-hardening) and was the subject of the Round-4 `62077a0` prose fix. Re-applying or inventing a fix here would re-investigate an already-hardened root cause (input rule) and risk weakening a correct, load-bearing gate (prohibition 2). A dedup-by-signature suppression ("this hand-edit class was already hardened, skip the re-route") would re-litigate locked decision 4 ("every occurrence is signal, not noise") and is out of scope for this stage.

**Action:**
  - No harness change. The triggering misroute is the hand-edit/transcription class already hardened (Rounds 2 & 4); the deny → ledger-append → probe-withhold → hardening-reroute → guard-allow-ack machinery all behaved exactly as designed, and this dispatch's guard-allow retired the ledger debt. Recording a mechanical fix or a contract fork here would be dishonest (no new defect) and/or gate-weakening. Full gate suite run as health evidence (all green).

**Gates run:**
  test_lazy_core.py: 304/304
  test_hooks.py: 23/23
  lint-skills.py: OK
  lazy-state.py --test: OK
  bug-state.py --test: OK


## Round 7 — 2026-06-12 — validate-deny

**Item in flight:** clap-target-export (AlgoBooth session `e076ed30`, `/lazy-batch 50`, Step 9 mcp-test, `pending_hardening=1`)

**Divergence point:** Step 1d → the Step-9 (`mcp-test`) forward cycle dispatch. The orchestrator hand-composed a `Step 9: run MCP tests` cycle prompt and dispatched it via `Agent` while the probe was WITHHOLDING the forward route over pending hardening debt; `lazy-dispatch-guard.sh` → `lazy_guard.py` denied — the dispatched prompt's hash (`denied_sha12: 10aec8ef8afb`) matched no registry entry (guard case 3, "no matching entry at all"). The deny then routed THIS hardening dispatch inline (locked decision 4). **Structurally identical to Round 4 (same d8 Step-9 mcp-test shape) and Round 6 (same hand-composed-over-registered-route class).**

**Root cause:** none for THIS round — **working-as-designed re-route (the triggering misroute's root cause is the hand-composed/transcription class already mechanically hardened in Rounds 2 & 4).** Evidence the deny was a hand-composed forward prompt over a live withhold, not a novel gap:
- The live deny ledger (`~/.claude/state/lazy-deny-ledger.jsonl`) newest entry is THIS deny: `denied_sha12 10aec8ef8afb`, `ts 1781320506` (CLAP Target Export), `reason_head` byte-for-byte the canonical `_CORRECTIVE_RECIPE` (`lazy_guard.py:130-139`), `prompt_head` "…Feature: CLAP Target Export (clap-target-export)…State script said: Step 9: run MCP t" — byte-identical to this dispatch's injected denied-prompt summary and denial reason.
- `10aec8ef8afb` is absent from EVERY registry sha — confirming a hand-composed, never-registered prompt (guard case 3). Meanwhile the registry held multiple legitimate `clap-target-export` `cycle` emissions, several UNCONSUMED (`7742493e…`, `c56b7d95…` ×2 at `1781320304`–`1781320399`) — a registered forward route was available; a hand-composed one was dispatched instead.
- The Phase-8 withhold machinery fired exactly as designed: with `pending_hardening=1` and the marker present, the `--emit-prompt` probe WITHHELD the forward route (`lazy-state.py:5196-5212`, `route_overridden_by: pending-hardening-debt`) and produced the `_probe_summary` string `step=Step 9: run MCP tests sub_skill=mcp-test feature_id=clap-target-export pending_hardening=1` — byte-identical to this dispatch's injected probe JSON; the hardening dispatch was registered (`77d4366e…`, class `hardening`, `emitted_at 1781320539`, registry entry [21]); and the guard-allow of THIS dispatch FIFO-acked the ledger entry (`acked: true`, `acked_ts 1781320569`, `lazy_guard.py::_ack_if_hardening:244-264`). Ledger debt is retired.
- The hand-composed-forward-over-withhold failure class is already exhaustively named in `lazy-batch/SKILL.md` (§1d.1 line 502 the Phase-8 withhold contract — "run that command verbatim and dispatch its `dispatch_prompt`"; lines 505-507 verbatim-dispatch + every-deny-routes-hardening; the full-probe-JSON / extractor-ban rule) and was the subject of the Round-2 mechanical hardening (Phase 7) and Round-3 Phase-8 withhold machinery. Re-applying or inventing a fix here would re-investigate an already-hardened root cause (input rule) and risk weakening a correct, load-bearing gate (prohibition 2). A dedup-by-signature suppression ("this Step-9 hand-compose was already hardened in Round 4, skip the re-route") would re-litigate locked decision 4 ("every occurrence is signal, not noise") and is out of scope for this stage.

**Action:**
  - No harness change. The triggering misroute is the hand-composed-forward/transcription class already hardened (Rounds 2, 3 & 4); the deny → ledger-append → probe-withhold → hardening-reroute → guard-allow-ack machinery all behaved exactly as designed, and this dispatch's guard-allow retired the ledger debt (`10aec8ef8afb` acked). Recording a mechanical fix or a contract fork here would be dishonest (no new defect) and/or gate-weakening. Full gate suite run as health evidence (all green).

**Gates run:**
  test_lazy_core.py: 304/304
  test_hooks.py: 23/23
  lint-skills.py: OK
  lazy-state.py --test: OK
  bug-state.py --test: OK


## Round 8 — 2026-06-13 — validate-deny

**Item in flight:** polyphonic-parameter-modulation (AlgoBooth session `e076ed30`, `/lazy-batch 50`, Step 7a write-plan, `pending_hardening=1`)

**Divergence point:** Step 1e.4a → the `recovery` cycle dispatch (post-execute-plan ledger-consistency guard). The orchestrator hand-edited its copy of a script-emitted+registered `recovery` prompt before the `Agent` call; `lazy-dispatch-guard.sh` → `lazy_guard.py` denied — the dispatched prompt's hash (`denied_sha12: cf272d7c2c15`) matched no registry entry (guard case 3). The deny then routed THIS hardening dispatch inline (locked decision 4). The triggering misroute is the hand-edit/transcription class already mechanically hardened in Rounds 2 & 4 — but running the gate suite as health evidence surfaced a SEPARATE, genuine harness defect (below), so this round IS a mechanical fix.

**Root cause:** **script-defect** — `lazy_core.update_repeat_counts` (`lazy_core.py:2673-2675` pre-fix) read the SINGLE global run marker via `read_run_marker()` UNSCOPED, then gated the Phase-2 F2 step-debounce on the GLOBAL `consumed_emission_count()` whenever ANY marker was on disk. The function was therefore non-hermetic to its `repo_root` argument:
- **Test fragility (observed):** the 6 `step_repeat_count` unit tests probe a temp-dir repo with no marker of their own, but `read_run_marker()` returned THIS live run's AlgoBooth marker (`e076ed30`, `repo_root C:/Users/Jacob/repos/AlgoBooth`), set `current_consume_count=9`, the temp `sig.json` round-tripped it as `prior_consume_count`, the debounce engaged spuriously and HELD `step_repeat_count` at 1 instead of incrementing to 2. The suite was green only when NO marked run was live (confirmed: the 6 failures are present at the introducing commit `973339b`, and `read_run_marker()` returns the live marker from a unit-test-style call → `consumed_emission_count()` == 9).
- **Latent cross-repo contamination:** a probe for repo A would debounce off a concurrent marked run in repo B — the same cross-session hazard Rounds 3 & 5 closed for the marker itself.

The recovery template itself is sound (registry [10] `prompt_norm` rendered with ZERO unbound tokens; emit succeeded; probe correctly WITHHELD the forward route over `pending_hardening=1`), so the triggering misroute is NOT a recovery-class gap — it is the already-hardened hand-edit class. The NEW defect is the F2 step-debounce repo-scoping bug, which the hardening dispatch's own gate run exposed.

**Action:**
  Mechanical fix applied: gate the F2 debounce oracle on the marker's `repo_root` resolving to the probe's `repo_root` (`lazy_core.py` — a marker missing `repo_root` / bind-pending is non-matching → debounce stays inert). Added regression test `test_update_repeat_counts_debounce_inert_for_foreign_repo_marker` (writes a FOREIGN-repo marker with a non-zero consume-count and asserts THIS repo's step counter still increments 1→2; verified RED on the pre-fix impl, GREEN after). Gates run: test_lazy_core.py 317/317 (was 310/316 with the live marker present pre-fix), test_hooks.py 24/24, lint-skills.py OK, --test suites OK. Commit: `7ddac63`.

**Process note (no action — prohibition 1):** the canonical log's Round 7 (clap-target-export) is present in the claude-config working tree but was left UNCOMMITTED by a prior hardening dispatch. This round was appended after it; the operator should commit Round 7 + Round 8 of the log together.

**Gates run:**
  test_lazy_core.py: 317/317
  test_hooks.py: 24/24
  lint-skills.py: OK
  lazy-state.py --test: OK
  bug-state.py --test: OK


## Round 9 — 2026-06-13 — validate-deny

**Item in flight:** polyphonic-parameter-modulation (AlgoBooth session `e076ed30`, `/lazy-batch 50`, Step 8 retro phase stale, `pending_hardening=1`)

**Divergence point:** Step 1d.1 (pending-hardening-debt reroute) → the HARDENING dispatch itself. A prior `recovery` deny (`cf272d7c2c15`, ts `1781388388` — the Round-8 triggering misroute) created 1 unit of hardening debt; the probe correctly WITHHELD the forward route (`route_overridden_by: pending-hardening-debt`) and emitted+registered hardening prompt `9b7238…` (nonce `d2ea73`, `emitted_at 1781388444`). The orchestrator's `Agent` hardening dispatch then carried prompt `c506a8c15503` — which did NOT byte-match the registered `9b7238…`; `lazy-dispatch-guard.sh` → `lazy_guard.py` denied with the ORDINARY corrective recipe (guard case 3, "no matching entry at all"), NOT the depth-1 halt reason. This is the SKILL §548(a) shape: a transcription slip on the orchestrator's copy of the emitted `dispatch_prompt`, applied to the hardening dispatch itself — explicitly NOT depth-1 recursion (the depth cap fires only when the denied prompt MATCHES a registered hardening-class entry; `lazy_guard.py:518,540`).

**Root cause:** none for THIS round — **working-as-designed re-route (the triggering misroute's root cause is the hand-edit/transcription class already mechanically hardened in Rounds 2 & 4, and the §548(a) "transcription slip on the hardening dispatch" recovery branch is already prose-specified).** Evidence the deny was a hand-edited copy of a legitimately-emitted hardening prompt, not a novel gap:
- The live deny ledger (`~/.claude/state/lazy-deny-ledger.jsonl`) entry for this deny: `denied_sha12 c506a8c15503`, `ts 1781388475`, `reason_head` byte-for-byte the canonical `_CORRECTIVE_RECIPE` (`lazy_guard.py:131-134`) — the ordinary recipe, NOT `_hardening_cap_deny_reason` — and `prompt_head` "You are the harness-hardening agent for the AlgoBooth autonomous pipeline…follow it ex", byte-identical to this dispatch's injected denied-prompt summary and denial reason.
- `c506a8c15503` is ABSENT from every registered hardening-class sha. The registry holds four `hardening` entries for this item: `9b7238…` (nonce `d2ea73`, consumed `015shL…`; plus two re-emits `f63c7a`/`74c42f`, both UNCONSUMED) and `a6fb95…` (nonce `f54b80`, consumed `01DPXw…`). A registered hardening prompt was available; a hand-edited one was dispatched → hash mismatch. Because the slip sha matched no hardening-class entry, the guard correctly returned the ordinary recipe (§548(a)) rather than the depth-1 cap — so this is NOT recursive hardening.
- The Phase-8 withhold + ack machinery fired exactly as designed: the prior recovery deny appended `cf272d`; the `--emit-prompt` probe WITHHELD (`lazy-state.py:5196-5212`, `route_overridden_by`) and produced `_probe_summary` `step=Step 8: retro phase (stale — 1 phases added since retro) sub_skill=retro-feature feature_id=polyphonic-parameter-modulation pending_hardening=1` — byte-identical to this dispatch's injected probe JSON; the eventually-consumed hardening dispatch's guard-allow FIFO-acked the `c506a8` slip-deny (`acked: true`, `acked_ts 1781390032`, `lazy_guard.py::_ack_if_hardening`). Ledger debt retired.
- The §548(a) transcription-slip-on-the-hardening-dispatch recovery branch is already exhaustively specified in `lazy-batch/SKILL.md:546-553` (deny-shape discrimination: ordinary recipe → ONE verbatim re-emit+re-dispatch; second recipe denial OR a halt reason → 4-step halt). The re-emit/re-dispatch behavior is exactly what the live state shows (`f63c7a`/`74c42f` re-emits, then `a6fb95` consumed). Re-applying or inventing a fix here would re-investigate an already-hardened root cause (input rule) and risk weakening a correct, load-bearing gate (prohibition 2). A dedup-by-signature suppression ("this transcription class was already hardened, skip the re-route") would re-litigate locked decision 4 ("every occurrence is signal, not noise") and is out of scope for this stage.

Gate suite run as health evidence (mandatory, and to surface any latent defect à la Round 8): all green, no new defect surfaced. Schemas untouched → coupled-pair mirroring + sentinel-schema lockstep N/A this round.

**Process note (no action — prohibition 1):** consistent with Round 8's note, the canonical log's Rounds 7 & 8 (and now 9) accrue in the claude-config working tree; the operator owns the push for claude-config and should commit/push these rounds together.

**Action:**
  - No harness change. The triggering misroute is the §548(a) transcription-slip-on-the-hardening-dispatch class, already mechanically hardened (Rounds 2 & 4) and prose-specified (`lazy-batch/SKILL.md:546-553`); the deny → ledger-append → probe-withhold → hardening-reroute → guard-allow-ack machinery all behaved exactly as designed, and this slip-deny's debt is already retired (`c506a8` acked). Recording a mechanical fix or a contract fork here would be dishonest (no new defect) and/or gate-weakening. Full gate suite run as health evidence (all green).

**Gates run:**
  test_lazy_core.py: 317/317
  test_hooks.py: 24/24
  lint-skills.py: OK
  lazy-state.py --test: OK
  bug-state.py --test: OK


## Round 10 — 2026-06-13 — no-route

**Item in flight:** d8-session-format (AlgoBooth `docs/features/audio/audio-vision/domains/d8-session-format/`; orchestrator-initiated no-route hardening, no denial)

**Divergence point:** Step 8 (retro phase) staleness routing → the route never advances. The probe permanently emits `current_step='Step 8: retro phase (stale — 1 phases added since retro)'`, `sub_skill=retro-feature`, even though `RETRO_DONE.phase_count_at_retro=7` already equals the real phase count and re-running `/retro` re-writes 7 — a provably non-advancing loop with no productive forward route.

**Root cause:** **script-defect** in `lazy_core._PHASE_HEADING_RE` (`user/scripts/lazy_core.py:1140`). The regex was `^#{2,3}\s+Phase\b` — it matched ANY h2/h3 heading whose first word is "Phase", including the non-phase roll-up section `## Phase Summary` (line 811 of the feature's PHASES.md). So `parse_phases()` returned 8 records for a 7-real-phase file (7 `### Phase N:` + the summary). `/retro` recorded `phase_count_at_retro=7` via a *different* counter (`grep -c '^### Phase'`, which the h2 summary correctly escapes), so `retro_staleness()` (`lazy_core.py:1262`, `current=len(parse_phases(...))`) computed `(8, 7)` on EVERY probe → `current > recorded` → permanent "stale retro". Two defects compounded: (1) the over-broad regex (its own comment FALSELY claimed lockstep with the AlgoBooth checker's `PHASE_HEADER_RE = /^(#{2,4})\s+Phase\s+([A-Za-z0-9.+]+)\s*[:—-]\s*(.*)$/`, whose author comment is explicit that the identifier+delimiter exists "to prevent matching headers like '### Phase Dependency Graph' where 'Phase' is just an English word"); (2) the writer (`/retro` prose) and the comparator (`parse_phases`) used two non-identical counters, so they could disagree silently. Evidence: `grep -nE '^#{2,3}[[:space:]]+Phase\b'` on the live file → 8 matches (incl. `811:## Phase Summary`); `grep -nE '^### Phase [0-9]'` → 7.

**Action:**
  - Mechanical fix applied (three coordinated changes so the writer and comparator can NEVER disagree):
    1. `harden(script)` `lazy_core._PHASE_HEADING_RE` tightened to a digit-OR-delimiter discriminator: `^#{2,3}\s+Phase\s+(?:[A-Za-z.+]*\d[A-Za-z0-9.+]*|[A-Za-z0-9.+]+\s*[:—-])`. Keeps every legitimate id form (bare numeric `### Phase 1`, alnum `### Phase 4A —`, delimited non-numeric `## Phase G+:`, `### Phase 10:`); rejects English-word sections (`## Phase Summary`, `### Phase Dependency Graph`, `## Phase Implementation Notes`). Strictly wider than the checker only for bare numeric ids, which real PHASES.md and the existing `parse_phases` fixtures rely on. Verified: `parse_phases` now counts **7** for the d8-session-format file.
    2. `harden(script)` added `lazy-state.py --count-phases <PHASES.md>` — prints `len(parse_phases(...))` (missing file → 0). This is now the SINGLE source of truth for the phase count behind both the writer and `retro_staleness()`'s comparator. Verified end-to-end: `--count-phases` on the live file prints `7`.
    3. `harden(skill-prose)` updated `/retro` SKILL.md Step 2b + the RETRO_DONE template to compute `phase_count_at_retro` via `--count-phases` (NOT `grep -c '^### Phase'`), and aligned the `sentinel-frontmatter.md` component doc; re-projected skills (88 components, no errors) and re-linted.
  - Regression coverage: `test_parse_phases_phase_summary_section_not_a_phase` (reproduces the 7+summary shape → asserts 7), `test_parse_phases_english_word_after_phase_not_counted` (keep/drop matrix), `test_count_phases_cli_matches_parse_phases` (CLI ≡ `parse_phases`). All registered in `_TESTS`.
  - Net effect: with `parse_phases`=7 and `phase_count_at_retro`=7, `retro_staleness()` returns `None` → the stale-retro route clears and the state machine can advance. bug-state.py inherits the fix via the shared `lazy_core.retro_staleness()` predicate (no separate counter). Coupled-pair (lazy-batch ↔ lazy-bug-batch) N/A — retro is feature-pipeline-only. Sentinel-schema lockstep: `phase_count_at_retro` stays an optional int (AlgoBooth `SENTINEL_SCHEMAS` `optional`+`intFields` unchanged) — only the computation prose changed.
  - Commit: `99dc449` (`harden(script): exclude '## Phase Summary' from phase count; unify retro staleness counter`).

**Process note (no action — prohibition 1):** consistent with Rounds 7–9, this round and the still-uncommitted Round 9 accrue in the claude-config working tree; the operator owns the push for claude-config and should commit/push these rounds together. (Round 9's content is a different item/trigger and was left untouched.)

**Gates run:**
  test_lazy_core.py: 320/320
  test_hooks.py: 24/24
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: OK
  bug-state.py --test: OK


## Round 11 — 2026-06-14 — validate-deny

**Item in flight:** d7-spectral-processing (AlgoBooth `/lazy-batch 25` run, active marker; probe `current_step="Step 10: mark complete (just completed)"`)

**Divergence point:** a non-cycle, self-discovered "fix a harness gap mid-run" action → the orchestrator hand-composed a **Sonnet `Agent`** prompt to fix AlgoBooth's `check-docs-consistency.ts` (to add the `complexity` plan key to `PLAN_OPTIONAL`) instead of routing the discovered gap through `--emit-dispatch hardening`. `lazy-dispatch-guard.sh` → `lazy_guard.py` denied — a hand-composed prompt during a marked run matches no registry entry (guard case 3). The deny then routed THIS hardening dispatch inline (locked decision 4). The triggering misroute is the hand-composed-over-registered class already mechanically hardened (Rounds 2 & 4) and prose-specified (`lazy-batch/SKILL.md:585,590,1235` — the hardening dispatch is the ONLY sanctioned route for a self-discovered harness gap). But like Round 8, the gate-suite/evidence pass surfaced a SEPARATE genuine harness defect underneath (below), so this round IS a mechanical fix.

**Root cause:** **missing-contract** — the plan-frontmatter schema (`user/skills/_components/plan-frontmatter.md`) is the *producer* half of a producer/consumer contract whose *consumer* half is AlgoBooth's `scripts/check-docs-consistency.ts` (`PLAN_REQUIRED`/`PLAN_OPTIONAL`), but — unlike `sentinel-frontmatter.md` ↔ `SENTINEL_SCHEMAS`, which carries an explicit lockstep clause (`sentinel-frontmatter.md:174-175,206-207,242-243`) — the plan-frontmatter schema had **no lockstep contract at all** (verified: zero occurrences of `PLAN_OPTIONAL`/`PLAN_REQUIRED` anywhere under `user/`). So `lazy-validation-readiness` Phase 9 added `complexity` to the producer side (`plan-frontmatter.md:30`; emitted by `/write-plan` Step 2.5, `write-plan/SKILL.md:148-164,591`) with no contract requiring the consumer lint to learn the key — and `check-docs-consistency.ts:702` `PLAN_OPTIONAL = ['phases','deliverables','superseded_by','upstream_phases_hashes']` rejects `complexity` (and also lacks the canonical `source_branch`/`source_commit`; and carries `upstream_phases_hashes`, absent from the canonical doc — drift in BOTH directions). The gate exits 1 on every plan carrying `complexity`, blocking the pipeline gate recurrently. The defect is real and live (probe note confirmed against both files); the schema doc's own line 3 ("tomorrow: AlgoBooth's `check-docs-consistency.ts` lint") shows the consumer was always known but never clamped.

**Action:**
  Mechanical fix applied: added a **"Consumer lockstep" subsection** to `user/skills/_components/plan-frontmatter.md` mirroring the sentinel-schema lockstep convention — it names AlgoBooth's `check-docs-consistency.ts` `PLAN_REQUIRED`/`PLAN_OPTIONAL` as the consumer half, enumerates the canonical Required (`kind`/`feature_id`/`status`/`created`) and Optional (`complexity`/`phases`/`deliverables`/`superseded_by`/`source_branch`/`source_commit`) key sets, and mandates same-change consumer updates so any future producer-side schema change self-announces the consumer obligation (drift = a key present on exactly one side). Also hardened the adjacent producer rule from "Tools MAY reject unknown keys in the future" to "rejects TODAY". The AlgoBooth-side `PLAN_OPTIONAL` edit itself is the operator's/orchestrator's to land in the TARGET repo — prohibition #1 forbids this agent from editing target-repo source; the lockstep clause is the harness mechanism that makes the obligation explicit and self-announcing. Re-projected skills (78 skills / 88 components, no errors) and re-linted. Coupled-pair mirroring N/A (component doc, not a lazy-batch/lazy-bug-batch SKILL pair). Sentinel-schema lockstep N/A (no sentinel schema touched — this is the *plan-file* analogue). Commit: `3a7a4ae` (`harden(docs): add plan-frontmatter consumer-lockstep clause for AlgoBooth check-docs-consistency PLAN_OPTIONAL`).

**Process note (no action — prohibition 1):** consistent with Rounds 7–10, the canonical log's recent rounds accrue uncommitted in the claude-config working tree; the operator owns the push for claude-config and should commit/push the `harden(docs)` fix + this Round 11 log entry together (and the still-pending earlier rounds).

**Gates run:**
  test_lazy_core.py: 361/361
  test_hooks.py: 34/34
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: OK
  bug-state.py --test: OK

## Round 12 — 2026-06-15 — manual

**Item in flight:** AlgoBooth `/lazy-batch` run (parallel session, screenshot) — a feature concluded planning with no `NEEDS_INPUT.md` surfaced and no way to tell whether the skip was deliberate.

**Divergence point:** Step 1d.5 post-cycle input-audit, clean branch. After a `/spec` / `plan-feature` cycle the input-audit second-opinion runs; when it finds no product-behavior decision baked in, `lazy-batch/SKILL.md` Step 1d.5 item 2 instructed the orchestrator to "emit no audit line — the audit is silent on clean cycles to keep cycle output lean." So the no-`NEEDS_INPUT.md` outcome produced ZERO operator-facing output — indistinguishable from an audit that never ran or a step that silently skipped the question.

**Root cause:** **missing-contract** — the NEEDS_INPUT response-shape rules covered only the *positive* case (decision surfaced → sentinel + `audit … surfaced` line). The *negative* case (no sentinel needed) had no disclosure contract anywhere in the stack: the input-audit returned a bare `clean` with no justification (`input-audit-prompt.md`/`dispatch-input-audit.md` step 5: "Write nothing. STOP."), the cycle subagent's REPORT (`cycle-base-prompt.md` R12 §4) never stated its NEEDS_INPUT disposition, and the orchestrator was actively told to stay silent (`lazy-batch/SKILL.md:685`). Operator directive: every step that CAN write `NEEDS_INPUT.md` must explicitly disclose, with a short justification, when it determined none was needed.

**Action:**
  Mechanical fix applied (operator-directed prose/contract change — no design fork, so no NEEDS_INPUT.md; the operator already owns and dictated this rule). Added the canonical **"Disclose the skip"** rule as Producer responsibility #7 in `sentinel-frontmatter.md` (single source of truth: the no-sentinel outcome is never silent; disclosure carries decisions-reviewed count, auto-accept class, ≤12-word reason). Wired it through every response-shape surface that feeds the operator: (1) `lazy-batch/SKILL.md` Step 1d.5 item 2 — replaced "emit no audit line / silent on clean" with a REQUIRED skip-disclosure `audit` line; (2) `orchestrator-voice.md` T3 — documented the `audit` line carrying the NEEDS_INPUT disposition (surfaced AND skipped) as required on `/spec` & `plan-feature` cycles; (3) `input-audit-prompt.md` + `dispatch-input-audit.md` step 5 + return-format — the clean return now carries a `needs-input skipped — …` justification instead of bare `clean`; (4) `cycle-base-prompt.md` R12 §4 REPORT (workstation + cloud) — the cycle subagent must state its NEEDS_INPUT disposition explicitly; (5) `lazy-batch-cloud/SKILL.md` inline T3 description — mirrored the skip-disclosure (coupled-pair); (6) `lazy-batch-retro/SKILL.md` R-V-2 — made the missing-skip-disclosure on `/spec`/`plan-feature` cycles a concrete graded failure. `lazy-bug-batch/SKILL.md` inherits via its existing pointer to `/lazy-batch` Step 1d.5 (no duplicate text). Re-projected + re-linted clean. Sentinel-schema lockstep N/A (no sentinel *schema* field changed — this is a producer-responsibility/response-shape rule; `SENTINEL_SCHEMAS` in `check-docs-consistency.ts` unaffected). Commit: `96b1569` (`harden(skill-prose): make NEEDS_INPUT skip self-announcing with justification`).

**Process note (no action — prohibition 1):** consistent with Rounds 7–11, the canonical log + harden(skill-prose) commit accrue locally in the claude-config working tree; the operator owns the claude-config push.

**Gates run:**
  test_lazy_core.py: 361/361
  test_hooks.py: 33/34 (the 1 failure is `test_pipe_tests_wsl` — `FileNotFoundError: 'wsl'`; the Windows `wsl` binary is absent on this Linux/WSL host. Environment-only, unrelated to this markdown-only change; all 18 guard/inject/ref hook-logic tests pass. Round 11 recorded 34/34 on a host where `wsl` is present.)
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: OK
  bug-state.py --test: OK


## Round 13 — 2026-06-14 — validate-deny

**Item in flight:** d8-effect-chains (AlgoBooth session `e076ed30`, `/lazy-batch 25`, Step 7a execute-plan, `pending_hardening=1`)

**Divergence point:** Step 1d.1 (pending-hardening-debt reroute) → the HARDENING dispatch itself. The orchestrator hand-composed a hardening-class `Agent` prompt — opening "You are **a harness-hardening engineer** for the lazy-batch autonomous pipeline. The harness skills + scripts live in the **claude-config repo** at `C:\Users\Jacob\source\repos\claude-config`…" — instead of dispatching the script-emitted `--emit-dispatch hardening` text (which opens "You are **the harness-hardening agent for the AlgoBooth autonomous pipeline**…follow it exactly"). `lazy-dispatch-guard.sh` → `lazy_guard.py` denied with the ORDINARY corrective recipe (`denied_sha12: 45bf91d74b7a`, guard case 3, "no matching entry at all"), NOT the depth-1 cap reason and NOT the cheap transcription-slip reason. The deny then routed THIS (script-emitted, registered) hardening dispatch inline (locked decision 4).

**Root cause:** none for THIS round — **working-as-designed re-route (the triggering misroute is the hand-composed-prompt-reaching-the-guard class already mechanically hardened in Rounds 2 & 4 and prose-specified).** Evidence the deny was a hand-composed hardening prompt, not a transcription slip and not a novel gap:
- The live deny ledger (`~/.claude/state/lazy-deny-ledger.jsonl`) newest DENY entry: `denied_sha12 45bf91d74b7a`, `ts 1781493134`, `reason_head` byte-for-byte the canonical `_CORRECTIVE_RECIPE` (`lazy_guard.py:172-188`) — the ordinary recipe, NOT `_HARDENING_DEPTH_CAP_REASON` (`lazy_guard.py:190-195`) and NOT `_TRANSCRIPTION_SLIP_REASON` (`lazy_guard.py:198-208`) — and `prompt_head` "You are a harness-hardening engineer for the lazy-batch autonomous pipeline. The harness skills + scripts live…", byte-identical to this dispatch's injected denied-prompt summary and denial reason.
- The denied text is **structurally distinct** from the script-emitted hardening prompt (different opening clause, different persona phrasing — "engineer/lazy-batch" vs "agent/AlgoBooth", claude-config repo path inline), so F2b normalization correctly did NOT fold it into a transcription slip → it received the ordinary recipe (a true hand-compose), not the cheap no-ledger slip deny. It is therefore NOT depth-1 recursion either: the depth cap fires only when the denied prompt MATCHES a registered hardening-class entry; `45bf91d74b7a` matches no registry sha.
- The registry's sole live entry is THIS dispatch: nonce `08c08bead9f84125b71daab196926eec`, `class: hardening`, `item_id: d8-effect-chains`, `consumed: true`, `consumed_by: toolu_0188UHNZK4f63dYCXLkb8RqR` — the script-emitted+registered hardening prompt ("the harness-hardening agent for the AlgoBooth autonomous pipeline"). A registered route was produced and consumed normally (depth-0 allow).
- The Phase-8 withhold + ack machinery fired exactly as designed: the deny appended `45bf91d74b7a`; the `--emit-prompt` probe WITHHELD the forward route (`route_overridden_by: pending-hardening-debt`) and produced `_probe_summary` `step=Step 7a: execute plan sub_skill=execute-plan feature_id=d8-effect-chains pending_hardening=1` — byte-identical to this dispatch's injected probe JSON; THIS dispatch's guard-allow FIFO-acked the slip-deny (`acked: true`, `acked_ts 1781497268`, `lazy_guard.py::_ack_if_hardening`). Ledger debt retired. (The `dispatch_by_reference` allow at `ts 1781497269`, `resolved_sha12 4b29d1d54fc5`, nonce `08c08bead9f84…` is this dispatch's by-reference resolution via the current `@@lazy-ref` mechanism — working as designed.)
- The hand-composed-prompt-reaching-the-guard failure class is already exhaustively named in `lazy-batch/SKILL.md` (the Phase-8 withhold contract; verbatim-dispatch / by-reference-`@@lazy-ref` rule; every-deny-routes-hardening) and is the subject of the Round-2 (Phase 7) and Round-4 mechanical hardening. Re-applying or inventing a fix here would re-investigate an already-hardened root cause (input rule) and risk weakening a correct, load-bearing gate (prohibition 2). A dedup-by-signature suppression would re-litigate locked decision 4 ("every occurrence is signal, not noise") and is out of scope for this stage.

Gate suite run as health evidence (mandatory, and to surface any latent defect à la Rounds 8/10/11): all green WITH the live AlgoBooth marker present, no new defect surfaced. Schemas untouched → coupled-pair mirroring + sentinel-schema lockstep N/A this round.

**Action:**
  - No harness change. The triggering misroute is the hand-composed-prompt class already mechanically hardened (Rounds 2 & 4) and prose-specified; the deny → ledger-append → probe-withhold → hardening-reroute → guard-allow-ack machinery all behaved exactly as designed, and this slip-deny's debt is already retired (`45bf91d74b7a` acked). Recording a mechanical fix or a contract fork here would be dishonest (no new defect) and/or gate-weakening. Full gate suite run as health evidence (all green).

**Process note (no action — prohibition 1):** consistent with Rounds 7–12, this no-change round accrues uncommitted in the claude-config working tree; the operator owns the claude-config push and should commit the pending log rounds together.

**Gates run:**
  test_lazy_core.py: 368/368
  test_hooks.py: 34/34
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: OK
  bug-state.py --test: OK


## Round 14 — 2026-06-15 — manual

**Item in flight:** mcp-testing

**Divergence point:** lazy-state.py compute_state Step 7 (phase-completion routing) → the Step-7 verification bypass at `lazy-state.py:1548`. The orchestrator's route was `write-plan` (`sub_skill_args: docs/features/mcp-testing/PHASES.md`), re-emitted on an unprogressable route; the in-session loop guard fired on the identical 4-tuple two cycles running (`step_repeat_count: 2`, `repeat_count: 1` — the latter resetting because each PHASES checkbox edit moved HEAD). No registry denial; manual harness-friction trigger (productive-looking write-plan oscillation caught by `step_repeat_count` + the in-session loop guard).

**Root cause:** script-defect — `lazy-state.py:1548`. The Step-7 verification bypass required ALL of `not plans` AND `_has_any_complete_plan(spec_path)` AND `(cloud or remaining_unchecked_are_verification_only(phases_text))`. For mcp-testing, `_has_any_complete_plan` (`lazy_core.py:1099`) returned False: the feature's 10 phases were implemented batch-by-batch via PHASES checkboxes directly, so it has NO `plans/` dir and no plan with `status: Complete`. Control therefore fell to `elif not plans` (`lazy-state.py:1567`) → `write-plan`. `/write-plan` is correctly banned (Step 1c.5) from emitting a verification-only re-run-`/mcp-test` WU, so it wrote no plan and returned → identical state → infinite write-plan loop. Class of bug: ANY feature implemented without a parent `plans/` receipt whose only remaining unchecked rows are Runtime Verification could never reach the Step-9 MCP gate.

**Action:**
  Mechanical fix applied: split the Step-7 bypass into two mode-specific discriminators. **Cloud** still requires `_has_any_complete_plan` (cloud can't tick any workstation row, so the verification-only predicate is not a valid "impl done" proxy there). **Workstation** now bypasses when `remaining_unchecked_are_verification_only(phases_text)` is True WITHOUT requiring a Complete plan on disk — a verification-only remainder is itself proof no implementation work remains (write-plan is banned from emitting such a WU, so there is nothing to plan). The legacy combined path (Complete plan present + verification-only) is preserved as a subset of the new workstation branch. No gate weakened: a genuine pending implementation row leaves `verification_only` False and still routes to write-plan. Two regression tests added in `test_lazy_core.py` (`test_lazy_state_no_plans_verification_only_routes_to_mcp` — the mcp-testing shape routes to Step 9 mcp-test; `test_lazy_state_no_plans_real_impl_row_still_write_plan` — inverse guard). Schemas untouched → coupled-pair mirroring + sentinel-schema lockstep N/A. Commit: `18a287e` (`harden(script): break no-plans verification-only Step-7 write-plan deadlock`).

**Process note (no action — prohibition 1):** consistent with Rounds 7–13, the canonical log round + `harden(script)` commit accrue locally in the claude-config working tree; the operator owns the claude-config push.

**Gate note (pre-existing, not introduced):** `lazy-state.py --test` reports one failure — the `materialize_wi` smoke fixture shells out to `bug-state.py --enqueue-adhoc`, which is REFUSED (exit 3) because the live `lazy-cycle-active` marker for feature `mcp-testing` (this run's own orchestrator marker) correctly blocks the orchestrator-only `--enqueue-adhoc` op. Reproduced byte-identically with the fix stashed → environment condition of running the gate inside a marked run, NOT a regression from this change. All ROUTING smoke fixtures pass, including `workstation-verification-only-*` and `workstation-all-plans-complete-*`.

**Gates run:**
  test_lazy_core.py: 387/387
  test_hooks.py: 48/48
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: routing fixtures all pass; 1 pre-existing marker-induced materialize_wi subprocess refusal (reproduced with fix stashed — not introduced here)
  bug-state.py --test: OK


## Round 15 — 2026-06-15 — process-friction

**Item in flight:** hardening-blind-to-process-friction

**Divergence point:** TWO defects in the freshly-shipped Phase 2/3 process-friction path, both on the `--cycle-end → friction-ledger → hardening-reroute` chain. (D1) `lazy-state.py --cycle-end`'s `cycle_end_friction_check` (`lazy_core.py:cycle_end_friction_check`) FALSE-POSITIVED `unexpected-commits` on a normal multi-commit `execute-plan` cycle (begin_head_sha=0a0e928c6711, marker `sub_skill=None`, budget=1). (D2) the routed hardening dispatch the probe withheld over (`route_overridden_by: pending-hardening-debt`, `pending_hardening=1`) would have been REFUSED at `--emit-dispatch hardening`: `build_hardening_emit_command`'s process-friction branch bound the wrong `--context` keys.

**Root cause:** script-defect (both).
  - **D1 (`lazy_core.py:5351` / `cycle_end_friction_check`):** the cycle marker never carried the dispatched `sub_skill`, and the I/O wrapper hard-coded `sub_skill=None` into `detect_cycle_bracket_friction`. With `None`, `_CYCLE_COMMIT_BUDGET.get(None, DEFAULT)` returned the conservative default `1` — so the `execute-plan` budget of `3` was never consulted, and any cycle that legitimately commits twice (test commit + impl commit) tripped `unexpected-commits` every time. Reproduced: `detect_cycle_bracket_friction(..., sub_skill=None, commits_since=2)` → `unexpected-commits`; with `sub_skill='execute-plan'` → `None`.
  - **D2 (`lazy_core.py:6704` / `build_hardening_emit_command`):** the process-friction branch emitted `--context friction_reason=… friction_detail=…`, but `dispatch-hardening.md` line 1 `@requires denied_prompt_summary,denial_reason,…` (a single shared evidence section for every `trigger_kind`). Those required keys were therefore unbound and `emit_dispatch_prompt('hardening', …)` refused: "requires context key 'denied_prompt_summary' which is absent." The template header + `hardening-dispatch.md` + the feature PHASES.md ALL already documented the intended mapping (`friction_reason → denied_prompt_summary`, `friction_detail → denial_reason`) — the code, and a test (`test_build_hardening_emit_command_process_friction_binding`) asserting the buggy `denied_prompt_summary= NOT in cmd`, had locked in the divergence from the documented contract.

**Action:**
  Mechanical fix applied (both defects):
  - **D1:** `write_cycle_marker` gained an additive `sub_skill` param (default None → legacy markers/fixtures unbroken) persisted into the marker; `cycle_end_friction_check` now recovers `marker.get("sub_skill")` and passes it to the detector. Threaded a new `--sub-skill` arg into `--cycle-begin` in BOTH `lazy-state.py` and `bug-state.py`, and updated the three orchestrator call sites to pass `--sub-skill {sub_skill}` (`lazy-batch`, `lazy-bug-batch`, `lazy-batch-cloud` — coupled-pair + cloud lockstep). No gate weakened: a genuine runaway beyond budget 3 still trips (regression test added).
  - **D2:** the process-friction branch now binds `friction_reason → denied_prompt_summary` and `friction_detail → denial_reason` (matching the template `@requires` and all three doc surfaces). The contract-violating test was corrected to assert the documented binding, and a new coupling regression (`test_process_friction_context_resolves_hardening_template`) feeds the emit command's exact context dict through `emit_dispatch_prompt` and asserts `ok=True` — so the bindings and the template's `@requires` cannot silently drift again.
  - Tests added: `test_cycle_end_friction_check_no_false_positive_on_execute_plan_multi_commit` (real temp git repo, 2 commits, no trip), `test_cycle_end_friction_check_runaway_commits_still_trips` (5 commits, still trips), `test_process_friction_context_resolves_hardening_template`. Marker round-trip + emit-binding tests updated for the new field/mapping. No sentinel schema touched → sentinel-schema lockstep N/A; coupled-pair (`lazy-batch ↔ lazy-bug-batch`) + cloud mirrored and `lazy_parity_audit.py --repo-root .` clean (exit 0). Commit: `b3ca26a` (the `harden(script):` commit; an earlier draft of this round cited the pre-amend hash `8cb6e81`).

**Process note (no registry/marker edit — prohibition 3):** the false-positive ledger entry was NOT laundered. The fix is to the script that WROTE it (`cycle_end_friction_check` now selects the right budget), per the "if the registry is wrong, fix the script" rule. Existing pre-fix friction entries remain honest debt.

**Gate note (pre-existing, not introduced — identical to Round 14):** `lazy-state.py --test` aborts at the `materialize-bug` smoke fixture: it shells out to `bug-state.py --enqueue-adhoc`, REFUSED (exit 3) because THIS hardening run's own live `lazy-cycle-active` marker (`feature_id=hardening-blind-to-process-friction`, `kind=meta`) correctly blocks the orchestrator-only `--enqueue-adhoc` op (C3). Proven environmental: re-run under `LAZY_ORCHESTRATOR=1` (C3 immunity) → "All smoke tests passed." All ROUTING fixtures pass. Not a regression from this change. The friction/marker unit coverage runs under pytest (464/464), which the bare in-file `test_lazy_core.py` runner (387/387) does not collect — both run.

**Gates run:**
  test_lazy_core.py: pytest 464/464; in-file runner 387/387
  test_hooks.py: 59/59
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: routing fixtures all pass; 1 pre-existing marker-induced materialize_wi subprocess refusal (green under LAZY_ORCHESTRATOR=1 — not introduced here)
  bug-state.py --test: OK

## Round 16 — 2026-06-16 — process-friction

**Item in flight:** mcp-testing

**Divergence point:** Step 10 (`__mark_complete__`) — the cycle's `--cycle-begin` meta-dispatch bracket. `--cycle-end`'s `cycle_end_friction_check` (`lazy_core.py`) appended a `kind: process-friction` / `unexpected-commits` ledger entry for a `__mark_complete__` cycle (`detail: HEAD advanced 2 commits since --cycle-begin (begin_head_sha=0a0e928c6711, sub_skill=None, budget=1)`; a second identical entry at `730a4df88d17`). The probe withheld the forward route (`route_overridden_by: pending-hardening-debt`, `pending_hardening=2`) and `build_hardening_emit_command`'s process-friction branch (Round 15 D2, working) routed this dispatch. This is the **pseudo-skill / meta-dispatch sibling** of Round 15's `execute-plan` false-positive: Round 15 fixed `execute-plan` (threaded `--sub-skill`, budget 3) but left the gap open for the inline pseudo-skill completion cycles.

**Root cause:** `missing-contract` + `ambiguous-prose`.
  - **missing-contract (`lazy_core.py:_CYCLE_COMMIT_BUDGET`):** the map enumerated only `execute-plan: 3` / `retro-feature: 3`. The pipeline-advancing terminal pseudo-skills (`__mark_complete__` / `__mark_fixed__`) were absent → `_CYCLE_COMMIT_BUDGET.get("__mark_complete__", DEFAULT)` returned the conservative default `1`. But a `__mark_complete__` completion cycle legitimately commits >1: the `--apply-pseudo` receipt+flip is one atomic commit, and the Gate-1 corrective-coverage meta path (`lazy-batch/SKILL.md §428`) authors+commits mcp-tests scenario(s) and may commit a gate-halt cycle_log / SPEC test-exempt note. Reproduced: `detect_cycle_bracket_friction(..., sub_skill='__mark_complete__', commits_since=2)` → `unexpected-commits`; with the new budget row → `None`; `commits_since=7` → still trips (no gate weakened). The SPEC's D1 (`docs/bugs/_archive/hardening-blind-to-process-friction/SPEC.md:107`) explicitly establishes a "conservative per-`sub_skill` expected-commit budget" — adding pseudo-skill rows is squarely inside that contract (mechanical, not a policy fork).
  - **ambiguous-prose (`lazy-batch/SKILL.md:586-590` + coupled pair):** the bracket comment said "any meta-dispatch: --kind meta", but the canonical command line hard-coded `--kind real --sub-skill {sub_skill}` and the explanation only motivated `--sub-skill` for `execute-plan`. For a meta-dispatch whose probe `sub_skill` is a pseudo-skill (`__mark_complete__`), the orchestrator omitted `--sub-skill` (treating `--kind meta` as licence to drop it) → marker `sub_skill=None` → default budget. The live cycle marker at dispatch time (`kind: meta`, `sub_skill: null`) confirms the omission was still happening post-Round-15.

**Action:**
  - **Mechanical fix applied.** (1) `_CYCLE_COMMIT_BUDGET` gains `__mark_complete__: 3` and `__mark_fixed__: 3` (bug `__mark_fixed__` resolves the same key via the shared `lazy_core` module). (2) `lazy-batch` / `lazy-bug-batch` / `lazy-batch-cloud` SKILL.md: `--sub-skill` is now stated MANDATORY on EVERY bracket — real AND meta (`--kind meta` is NOT a licence to omit it) — bound to the probe's `sub_skill` verbatim incl. a pseudo-skill name, and the command examples show `--kind {real|meta}` (coupled-pair + cloud lockstep). (3) Regression test `test_detect_friction_mark_complete_meta_cycle_multi_commit_within_budget` asserts the 2-commit pseudo-skill cycle no longer trips while a 7-commit runaway still does. No gate weakened; no registry/marker edited (the two false-positive ledger entries remain honest debt — the fix is to the script that wrote them, per prohibition 3 / "fix the script not the registry"). Commit: `3892c9f`.

**Gates run:**
  test_lazy_core.py: pytest 465/465; in-file runner 387/387
  test_hooks.py: 59/59
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: OK (under LAZY_ORCHESTRATOR=1 — this run's own live cycle marker otherwise C3-refuses the materialize smoke fixture, identical to Rounds 14/15; not a regression)
  bug-state.py --test: OK (same env note)

## Round 17 — 2026-06-16 — process-friction

**Item in flight:** mcp-testing

**Divergence point:** Step 10 (`__mark_complete__`) — the SAME `--cycle-begin` meta-dispatch bracket Round 16 just hardened. The probe withheld the forward route (`route_overridden_by: pending-hardening-debt`, `pending_hardening=1`) over the SECOND of the two false-positive `unexpected-commits` deny-ledger entries Round 16 enumerated (`detail: HEAD advanced 2 commits since --cycle-begin (begin_head_sha=730a4df88d12, sub_skill=None, budget=1)`). Registry state at dispatch: entry `730a4df8` unacked; entry `0a0e928c` acked by Round 16. The two SHAs map to the Phase 2/3 feature commits of `hardening-blind-to-process-friction` itself (`0a0e928c` = `feat(lazy-state): process-friction detector → deny ledger`; `730a4df8` = `feat(hardening-trigger): add process-friction as 4th hardening trigger class`).

**Root cause:** `missing-contract` (DUPLICATE of Round 16, already mechanically fixed). This is NOT a new defect — it is the FIFO-sibling debt retirement of Round 16's exact root cause, surfacing by design.
  - **Why it re-fired (working-as-designed, not a gap):** the routed-hardening debt is a FIFO ledger drained ONE entry per dispatch (`lazy_core.py:6778 ack_oldest_deny`, called allow-time from `lazy_guard.py:354 _ack_if_hardening`), per locked decision 4 ("unbounded per-run dispatch count, no dedup-by-signature; each occurrence is signal, not noise"). The two false-positive entries were written before the fix landed; Round 16's dispatch acked the oldest (`0a0e928c`) AND landed the mechanical fix (`3892c9f`), leaving the second entry (`730a4df8`) as pending debt that correctly routes THIS dispatch. There is no dedup-by-root-cause cap, by operator decision — so a duplicate-root-cause sibling dispatching is the contract, not a defect.
  - **Fix already on disk (verified, no new change):** `_CYCLE_COMMIT_BUDGET` (`lazy_core.py:5188`) already carries `__mark_complete__: 3` and `__mark_fixed__: 3` (Round 16, commit `3892c9f`); the `lazy-batch`/`lazy-bug-batch`/`lazy-batch-cloud` mandatory-`--sub-skill`-on-every-bracket prose is in place. Reproduced live: `detect_cycle_bracket_friction(marker{begin_head_sha set}, sub_skill='__mark_complete__', commits_since=2|3)` → `None` (false positive gone); `commits_since=7|12` → `unexpected-commits` (runaway still trips — no gate weakened); and the original symptom path `sub_skill=None, commits_since=2` → `unexpected-commits` still trips, confirming the prose fix (marker must carry the pseudo-skill via `--sub-skill`) is the load-bearing half going forward.

**Action:**
  - **No mechanical fix and no NEEDS_INPUT.** The root cause was already fixed mechanically in Round 16 (`3892c9f`); re-applying nothing is correct. The pending second ledger entry (`730a4df8`) is retired by the guard's allow-time FIFO ack of THIS dispatch (`_ack_if_hardening` → `ack_oldest_deny`) — a script-owned mutation, NOT an operator/registry edit (prohibition 3 honored: the ledger is drained by the script, never hand-laundered). This round is the documentation deliverable retiring the sibling debt and recording the duplicate diagnosis so a future walker sees Round 16+17 as one root cause across two FIFO entries. No source touched (prohibition 1 honored — working tree clean before this log append). Commit: docs-only `harden(docs):` append.

**Gates run:**
  test_lazy_core.py: pytest 465/465; in-file runner 387/387
  test_hooks.py: 59/59
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: OK (under LAZY_ORCHESTRATOR=1 — this run's own live cycle marker otherwise C3-refuses the materialize smoke fixture, identical to Rounds 14/15/16; not a regression)
  bug-state.py --test: OK (same env note)

## Round 18 — 2026-06-16 — manual

**Item in flight:** mcp-testing

**Divergence point:** Step 10 (`__mark_complete__`, feature pipeline) — the deterministic completion write in `apply_pseudo` (`lazy_core.py:2590`, the shared `__mark_complete__`/`__mark_fixed__` branch). The branch wrote COMPLETED.md + flipped SPEC/PHASES Status but never trimmed the completed feature's entry from `docs/features/queue.json`. Observed live: completing `mcp-testing` left its queue entry → AlgoBooth's `check-docs-consistency.ts` raised 1 `queue.no-completed` error. Manual systemic-gap dispatch (registry empty; no denied prompt).

**Root cause:** `script-defect` (asymmetry between the two pipelines). The BUG pipeline trims its queue in `archive_fixed` step 6 (`lazy_core.py:3578`, match on `spec_dir`/`id`), invoked because bugs have an archive step that moves the dir to `_archive/`. The FEATURE pipeline has NO archive step — a completed feature stays in place and only its SPEC status flips — so there was no code path that ever removed the entry from `docs/features/queue.json`. AlgoBooth's `queue.no-completed` rule (`scripts/check-docs-consistency.ts:2137`) is the inverse of `queue.missing`: a Complete/Superseded entry lingering in the active-work queue is a HARD ERROR. The checker's own comment states "lazy-state.py skips terminal-status entries" — i.e. the harness was *expected* to trim on completion, but the feature pipeline never did. Every feature completion therefore minted a fresh docs-consistency error.

**Action:**
  - **Mechanical fix applied** (`lazy_core.py`, commit `089b347`): added step (d) to the shared `__mark_complete__`/`__mark_fixed__` branch, gated on `not is_fixed` so ONLY the feature (complete) path trims `docs/features/queue.json` (the fixed path's queue lives at `docs/bugs/queue.json` and is owned by `archive_fixed`). Match keys mirror the bug trim (`spec_dir == spec_path.name` OR `id == feature_id`); JSON preserved with `indent=2` + trailing newline; idempotent (the trim sits BEHIND the receipt-noop short-circuit at `lazy_core.py:2646`, so an already-receipted dir never re-trims). New result key `queue_trimmed` (always False on the fixed path). Malformed-queue policy: degrades to a non-fatal `warnings` entry rather than refusing post-receipt — the receipt + status flips are already on disk, and the un-trimmed entry re-surfaces as the same `queue.no-completed` error, preserving the signal without mis-reporting the completion as failed. 4 regression tests added: trim removes only the matching entry (unrelated survives), trim sits behind the receipt-noop, fixed path leaves the features queue untouched, malformed queue warns-not-refuses.
  - **Coupled-pair / sentinel-schema:** none touched. Change is confined to `lazy_core.py` (shared script) — no `lazy-batch`/`lazy-bug-batch`/`lazy-batch-cloud` SKILL prose and no sentinel schema were modified, so neither the pairs-table mirroring nor the `sentinel-frontmatter.md` ↔ `SENTINEL_SCHEMAS` lockstep applies. Prohibition 1 honored (claude-config only; the AlgoBooth checker was READ for evidence, never edited). No gate weakened (prohibition 2): the trim makes the existing AlgoBooth gate pass by fixing the harness, not by softening the checker. Registry/marker untouched (prohibition 3).

**Gates run:**
  test_lazy_core.py: pytest 469/469 (4 new); in-file runner 387/387
  test_hooks.py: 59/59
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: OK under LAZY_ORCHESTRATOR=1 — this run's own live `lazy-cycle-active` marker (feature `mcp-testing`) otherwise C3-refuses the `--enqueue-adhoc` materialize smoke fixture (exit 3); verified preexisting via `git stash` on a clean tree, identical to Rounds 14–17, not a regression and unrelated to this edit
  bug-state.py --test: 59/59 (OK; same env note)

## Round 19 — 2026-06-16 — process-friction

**Item in flight:** hardening-blind-to-process-friction

**Divergence point:** THREE coupled defects exposed during the `hardening-blind-to-process-friction` run, producing a run-state-corruption deadlock (operator-authorized recovery in flight). (D-A) the `--cycle-end` process-friction detector (`cycle_end_friction_check` → `detect_cycle_bracket_friction`) tripping `unexpected-commits` on a `kind: meta` cycle. (D-B) a nested `/lazy` (feature) `--run-start` silently CLOBBERING an ACTIVE bug run marker. (D-C) a bare `@@lazy-ref` dispatch token reaching a subagent UNRESOLVED in the Agent path, which improvised an off-task `/lazy` (the trigger for D-B). Probe at failure: `step=Step 9 sub_skill=__grant_skip_no_mcp_surface__ recovery=operator-authorized post-run-marker-clobber`.

**Root cause:** `script-defect` (D-A, D-B) + `hook-defect` (D-C mechanical half); the residual D-C dispatch-preference contract is a `missing-contract`/policy fork surfaced via NEEDS_INPUT.md.
  - **D-A (`script-defect`, `lazy_core.py` `detect_cycle_bracket_friction`):** a `kind: meta` cycle (hardening / input-audit / recovery / apply-resolution) is an orchestrator-driven remediation dispatch that legitimately commits an UNBOUNDED number of times (e.g. a hardening cycle commits a script fix AND a hardening-log append) and carries `sub_skill=None`, so the per-sub_skill budget defaulted to 1 and 2+ legit commits tripped signal (b) on EVERY meta cycle — a self-perpetuating loop where each hardening cycle re-tripped at its own `--cycle-end`. Rounds 16/17 chased this via pseudo-skill budget rows + mandatory `--sub-skill` prose, but a meta cycle has NO sub_skill to budget; the structural fix is to exempt `marker['kind'] == 'meta'` from signal (b). Confirmed live: this dispatch's cycle marker is `kind: meta, sub_skill: null, begin_head_sha=5bba87bc9ad7` — the exemption returns `None` for a meta multi-commit cycle, so this very hardening cycle cannot re-trip at its own `--cycle-end`.
  - **D-B (`script-defect`, `lazy_core.py` `write_run_marker` was unconditional):** `--run-start` → `write_run_marker` overwrote the run marker with no liveness check, so a nested feature `--run-start` re-pointed an active bug run's identity (`pipeline:bug session X` → `pipeline:feature session Y`), breaking the validate-deny/ack guard for the real orchestrator (its hardening debt could never ack — its marker no longer existed). At `--run-start` the incoming run has no `session_id` yet (bind-pending), so the robust mechanical discriminator is the PIPELINE field: a feature `--run-start` over a live `bug` marker (or vice versa) is always a cross-run accident; a same-pipeline re-`--run-start` is the legitimate checkpoint-resume case.
  - **D-C (`hook-defect`, `lazy_guard.py` marker-absent fast-path):** the guard resolves `@@lazy-ref` only inside the F2a branch, which requires a live owner run marker; the `marker is None` fast-path `return None` (silent allow) let a bare ref token pass UNRESOLVED to the subagent. After D-B clobbered/hid the owning marker, this fast-path allowed the literal token through — the subagent received the bare string and improvised. A literal `@@lazy-ref` is never a valid subagent prompt.

**Action:**
  - **Mechanical fix applied** (commit `32e840d`):
    - **D-A:** `detect_cycle_bracket_friction` returns `None` (signal b skipped) when `marker.get("kind") == "meta"`. Signal (a) bracket-break is NOT exempted (a meta cycle that tears the run bracket — exactly the D-B clobber — still self-announces). Exemption is read from the marker dict the caller already threads, so it is live for the meta cycle running this dispatch.
    - **D-B:** new `lazy_core.refuse_run_start_clobber(incoming_pipeline)` — reads the run marker RAW (so the session-id staleness path cannot mask the live owner), honors only 24h age-staleness, and refuses (exit 3, corrective stderr, ZERO side effects) when an age-fresh well-formed marker of a DIFFERENT pipeline exists. Same-pipeline resume + stale/corrupt markers fail-open (proceed). Wired into both `--run-start` handlers (`lazy-state.py` "feature" / `bug-state.py` "bug" — coupled-pair mirror).
    - **D-C (mechanical half):** `lazy_guard.py` now DENIES a bare `@@lazy-ref` token whenever it cannot resolve+consume it — early at the marker-absent fast-path (`_is_bare_ref` → `_deny_json(_UNRESOLVED_REF_REASON)`) and after a failed F2a resolution (`_deny_and_ledger`). A live marker + fresh nonce still resolves+allows normally (F2a unchanged). The corrective reason names the unresolved-by-reference defect and prescribes the verbatim `dispatch_prompt`.
    - **9 regression tests added** (7 in `test_lazy_core.py`, 2 in `test_hooks.py`): meta-cycle exemption (signal-b skipped, signal-a still fires, real-cycle control still trips); `refuse_run_start_clobber` (cross-pipeline refuses + names both + leaves marker untouched / same-pipeline resume allowed / no-marker allowed / age-stale allowed / corrupt fails-open); guard bare-ref no-marker denies-not-allows + non-ref no-marker still-allows (deny scope check).
  - **NEEDS_INPUT.md written** (`docs/specs/turn-routing-enforcement/NEEDS_INPUT.md`, `class: product`): D-C's dispatch-PREFERENCE contract fork — keep `dispatch_prompt_ref` PREFERRED (recommended; the mechanical deny already closes the silent-improvisation hole and by-ref retains its transcription-slip immunity) vs. flip to verbatim `dispatch_prompt` for Agent dispatches (reverses a deliberate Phase 7 design choice, re-opens the transcription-slip class). Surfaced rather than baked because it trades one real failure class for another and touches three coupled SKILL files.
  - **Coupled-pair / sentinel-schema:** the `--run-start` guard wiring is mirrored across `lazy-state.py` ↔ `bug-state.py` (the run-lifecycle pair). No SKILL.md prose and no sentinel schema were changed, so the `lazy-batch` ↔ `lazy-bug-batch` pairs-table mirroring and the `sentinel-frontmatter.md` ↔ `SENTINEL_SCHEMAS` lockstep do not apply to the mechanical commit. (The by-ref preference prose in `lazy-batch`/`lazy-bug-batch` is the subject of the NEEDS_INPUT fork, NOT edited.)
  - **Prohibitions honored:** claude-config only (the AlgoBooth tree was never touched — prohibition 1); no gate weakened — D-A still trips a real runaway and a meta bracket-break, D-B still allows legit resume, D-C's F2a allow path is unchanged (prohibition 2); no registry/marker edited — the fixes are to the SCRIPTS that write them (prohibition 3).

**Gates run:**
  test_lazy_core.py: pytest 476/476 (7 new); in-file runner 387/387
  test_hooks.py: 61/61 (2 new)
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: OK (under LAZY_ORCHESTRATOR=1 — this run's own live `kind: meta` cycle marker otherwise C3-refuses the materialize smoke fixture, identical to Rounds 14-18; not a regression)
  bug-state.py --test: OK (same env note)

## Round 20 — 2026-06-16 — process-friction

**Item in flight:** research-gate-ignores-existing-phases

**Divergence point:** the bug pipeline could not COMPLETE a run. A `--cycle-end` process-friction entry (`kind: process-friction`, session-less) plus an operator-authorized `--run-end --ack-unhardened` left `pending_hardening()` stuck at 1, so the next bug's advancing probe withheld the forward route (`route_overridden_by: pending-hardening-debt`, `pending_hardening=1`, `sub_skill=plan-bug`). Two residual `script-defect`s past the Rounds 15/19 fixes (run-marker clobber + meta-cycle exemption + by-ref deny).

**Root cause:** `script-defect` (both).
  - **DEFECT-1 (PRIMARY) — `--ack-unhardened` never acked anything.** The `--run-end --ack-unhardened` override branch (`lazy-state.py` ~5637 / `bug-state.py` ~4143) only set an `override_note` and proceeded to delete the marker — it called NO ack mutator, so every unacked entry stayed `acked: false` on disk. The framing was "session-less entries are unclearable," but the real defect is broader: `pending_hardening()` (`lazy_core.py:6876`) never filtered by `session_id` at all — it counts any falsy-`acked` entry — so the override left ALL debt (session-less process-friction AND normal validate-deny) on disk. Phase 8 WU-8.2 had moved the per-dispatch ack to GUARD-ALLOW time; the operator OVERRIDE path was never given a corresponding blanket-ack, so authorizing the override discharged the gate but not the debt → the next run's probe re-withheld forever. The existing `test_run_end_refuses_on_unacked_deny` asserted `run_marker_deleted is True` + the override note but NEVER asserted the ledger was drained, so the bug was invisible to the suite.
  - **DEFECT-2 (SECONDARY) — fixed execute-plan budget false-positived multi-phase plans.** `_CYCLE_COMMIT_BUDGET["execute-plan"] = 3` (`lazy_core.py:5291`) is a flat constant, but `/execute-plan` commits once per phase (the standard per-phase gate+commit cadence). A legitimate 6-phase single-part plan makes ~6 commits and tripped `unexpected-commits` even with `sub_skill=execute-plan` correctly threaded (Round 19's meta-cycle exemption does not cover a real execute-plan cycle).

**Action:**
  - **Mechanical fix applied** (commit `b23ceea`):
    - **DEFECT-1:** new `lazy_core.ack_all_unacked_denies(now=None) -> int` — the operator-override counterpart to `ack_oldest_deny` (which retires exactly one). It flips EVERY unacked entry to `acked` REGARDLESS of `kind` (validate-deny + process-friction) and REGARDLESS of any `session_id` (the field `pending_hardening` never consumed), atomic-rewrites the ledger, and returns the count discharged; a rewrite failure leaves the ledger unchanged and returns 0 (no over-report). Wired into BOTH `--run-end --ack-unhardened` override branches (`lazy-state.py` ↔ `bug-state.py`, coupled-pair mirror) so the operator authorization actually clears the debt before the marker is deleted; the override note now reports `acked N`.
    - **DEFECT-2:** the execute-plan budget now SCALES with the plan part's declared phase count. `--cycle-begin` gained an additive `--sub-skill-args` flag (mirrored on both scripts) persisted into the cycle marker as `sub_skill_args` via `write_cycle_marker`; at `--cycle-end`, new `_execute_plan_commit_budget(sub_skill, sub_skill_args)` reads the plan's `phases:` frontmatter (existing `_plan_phase_set` helper) and returns `phase_count + 2` (slack for an intra-phase test+impl split), which `cycle_end_friction_check` passes as a new `budget_override` param to `detect_cycle_bracket_friction` (a positive override supersedes the fixed table; None/degraded/non-execute-plan falls back to the table — never a false negative, never a crash). A genuine runaway (commits beyond phase_count + slack) still trips.
    - **5 regression tests added** (all in `test_lazy_core.py`): strengthened `test_run_end_refuses_on_unacked_deny` to assert `pending_hardening()==0` after the override; `test_ack_all_unacked_denies_clears_sessionless_friction` (proves a session-less `kind: process-friction` entry — verified to carry NO `session_id` on disk — is cleared alongside a validate-deny entry; idempotent; empty→0); `test_run_end_ack_unhardened_clears_sessionless_friction` (subprocess end-to-end: refuse-without-override, then override deletes marker AND drains ledger); `test_execute_plan_commit_budget_scales_with_phase_count` (6-phase plan → budget 8, trailing-flag args tolerated, non-execute-plan/blank/unreadable/no-phases → None, detector honors override so 6 commits pass but 9 trip, and WITHOUT the override the same 6 commits false-positive against the table budget of 3 — pinning that the override is the fix).
  - **Coupled-pair / schema:** the `--ack-unhardened` blanket-ack and the `--sub-skill-args`/`--cycle-begin` wiring are mirrored across `lazy-state.py` ↔ `bug-state.py` (the run-lifecycle / cycle-marker pair). No SKILL.md prose changed, so the `lazy-batch` ↔ `lazy-bug-batch` pairs-table mirror does not apply. The cycle marker (`lazy-cycle-active.json`) is a runtime state-dir marker, not a doc sentinel, so `sentinel-frontmatter.md` ↔ `SENTINEL_SCHEMAS` lockstep does not apply (the added `sub_skill_args` field is additive, default None — legacy markers degrade to the fixed budget).
  - **Prohibitions honored:** claude-config only (prohibition 1 — no target-repo source touched); NO gate weakened — DEFECT-1 makes the OPERATOR OVERRIDE correctly discharge the debt it is authorized to discharge (the no-override gate still refuses exactly as before), DEFECT-2 still trips a genuine runaway and still defaults to the conservative table when the plan is unreadable (prohibition 2); the registry/marker were never hand-edited — the fixes are to the SCRIPTS that write them (prohibition 3).

**Gates run:**
  test_lazy_core.py: pytest 482/482 (5 new); the 2 `test_refuse_guard_*` C3 fixtures pass clean WITHOUT `LAZY_ORCHESTRATOR` (pytest uses hermetic per-test state dirs); under `LAZY_ORCHESTRATOR=1` those 2 invert (the env grants the structural immunity they assert against) — env-induced, not a regression
  test_hooks.py: 59/59
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: OK (under LAZY_ORCHESTRATOR=1 — this run's own live `kind: meta` cycle marker `research-gate-ignores-existing-phases` otherwise C3-refuses the materialize smoke fixture, identical to Rounds 14-19; not a regression)
  bug-state.py --test: OK (same env note)

### Round 20 follow-up — 2026-06-16 — WU-scaling (supersedes DEFECT-2's phase-only budget)

**Why:** Round 20's `_execute_plan_commit_budget` scaled by *phase* count (`phase_count + slack`), but `/execute-plan` commits once per **WORK UNIT** (the per-WU `tick + commit` cadence), not once per phase. A WU-dense plan part — more WUs than phases — still false-positived: the live recurrence was `cycle-subagent-runs-orchestrator-work` part-1 (5 WUs across 2 phases → 5 commits vs a phase-derived budget of `2 + 2 = 4`), which tripped `unexpected-commits` on a fully legitimate cycle and forced an operator-authorized `--ack-unhardened`.

**Fix (commit pending):** `_execute_plan_commit_budget` now scales by `max(phase_count, wu_count) + slack`, where `wu_count` is the parseable `- [ ] WU-N` / `- [x] WU-N` checkbox total from the plan part body (existing `_plan_wu_checkbox_counts` helper). Both signals are best-effort: whichever the plan actually declares (a legacy phase-only plan, or an ISSUE-6 per-WU plan with no `phases:` field) governs; only when NEITHER is parseable does it return `None` (fixed-table fallback preserved). A genuine runaway (commits beyond the declared work + slack) still trips. New regression `test_execute_plan_commit_budget_scales_with_wu_count` covers the 5-WU/2-phase recurrence, the phase-count-wins case, the no-`phases:`-but-has-WUs case, and the empty fallback.

**Provenance note:** found during the `/lazy-bug-batch` run that shipped Rounds 15/19/20 (the friction detector trips were observed on the pipeline's own cycles); fixed as a direct follow-up under full gates, not a marked hardening dispatch. Gates: pytest test_lazy_core.py + test_hooks.py 568 passed; `lazy-state.py --test` / `bug-state.py --test` PASS clean (no `LAZY_ORCHESTRATOR` override needed — bug-4's smoke-fixture `LAZY_STATE_DIR` isolation fix is live); lint-skills OK.

## Round 21 — 2026-06-16 — manual

**Item in flight:** mcp-audio-quality-observability (verification pass)

**Divergence point:** `/lazy-batch` Step 1a probe stuck at "Step 4.6: upstream realign needed" — the `realign-spec` route never cleared. `step_repeat_count` reached 3 (the oscillation tripwire) on the FIRST queue item; HEAD advanced each cycle (the realign subagent kept committing harmless stale-path doc fixes) which MASKED a no-forward-progress loop that blocked the entire 68-item queue behind item 1. Surfaced via manual `/harden-harness` after the orchestrator stopped the loop and the operator chose "stop & harden harness".

**Root cause:** `script-defect` (`lazy-state.py` `realign_is_fresh`, ~line 846). In the hash path the per-upstream loop checked `if dir_name not in recorded_hashes: return False` BEFORE the `current_sha = _phases_sha(ud); if current_sha is None: continue` guard. A hard-complete upstream with NO PHASES.md (`audio-pipeline-v2` — older SPEC-only feature, never decomposed) is unhashable, so it can NEVER appear in a realign plan's `upstream_phases_hashes`; the not-in-recorded_hashes check fired first and forced `realign_is_fresh -> False` permanently. `mcp-audio-quality-observability` depends hard on `mcp-testing` (PHASES.md present, hash recorded + matching `9fcc8d63...`) AND `audio-pipeline-v2` (no PHASES.md) -> the freshness gate could never report fresh -> Step 4.6 re-emitted `realign-spec` every cycle. The realign cycle subagent did its job correctly twice (recording every hashable upstream); the gap was structurally unfixable from the feature side.

**Action:**
  - **Mechanical fix applied** (commit `df8f741`): reordered `realign_is_fresh`'s hash-path loop so the `current_sha is None -> continue` skip-guard PRECEDES the `dir_name not in recorded_hashes -> return False` stale check. A hashless upstream is now skipped (it contributes no PHASES-hash drift); freshness is decided by the hashable upstreams alone. Genuine drift on a hashed upstream still reports stale.
  - **Regression test added** (`lazy-state.py --test` direct `realign_is_fresh` unit check `realign-fresh-hashless-upstream-not-stale`): positive control (hard upstream with no PHASES.md + a matching-hash decomposed upstream -> fresh=True) AND negative control (changed hashed-upstream PHASES.md -> stale=False), so the None-guard reorder cannot mask real drift. The `lazy-state-test-baseline.txt` snapshot was regenerated through the test's own `_normalize_smoke_output` and PROVEN to differ by exactly one additive PASS line (zero removals) before acceptance — not a masking regeneration.
  - **Live repro flip confirmed:** post-fix AlgoBooth probe routes `mcp-audio-quality-observability` to `Step 7a: execute plan` (`sub_skill=execute-plan`, no terminal) instead of Step 4.6.
  - **Coupled-pair / sentinel-schema:** none applicable — pure `lazy-state.py` script defect; `bug-state.py` has no realign step (its Step 4.5/4.6/5 exclusion), no SKILL.md prose changed, no sentinel schema touched.
  - **Prohibitions honored:** claude-config only (AlgoBooth source untouched); no gate weakened (the negative control proves genuine drift still trips); no registry/marker edited.

**Gates run:**
  test_lazy_core.py: 397/397 (1 new)
  test_hooks.py: 67/67
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: OK
  bug-state.py --test: OK

## Round 22 — 2026-06-16 — process-friction

**Item in flight:** adhoc-mcp-runner-payload-interpolation

**Divergence point:** Step 7a (`execute-plan` real-skill cycle) — the C1 `--cycle-begin` dispatch bracket. `--cycle-end`'s `cycle_end_friction_check` appended a `kind: process-friction` / `unexpected-commits` ledger entry (`detail: HEAD advanced 4 commits since --cycle-begin (begin_head_sha=6eb8c997c91f, sub_skill='execute-plan', budget=3)`). The probe withheld the forward route (`route_overridden_by: pending-hardening-debt`, `pending_hardening=1`, `step=Step 9: run MCP tests`, `sub_skill=mcp-test`). Registry: 10 entries, 0 unconsumed. The deny detail's `budget=3` is the smoking gun — the FIXED table budget, NOT the Round-20 WU-scaled override. This is the direct continuation of Rounds 15/16/17/19/20: Round 20 (+ its WU-scaling follow-up) built `_execute_plan_commit_budget` to scale the `execute-plan` budget by `max(phase_count, wu_count) + slack`, but the budget came back as the fixed `3` — proving the override returned `None` and the detector fell back to the table.

**Root cause:** `missing-contract` (with an ambiguous-prose component) — `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md:395`/`:403`, `user/skills/lazy-batch/SKILL.md:603`/`:619`, `user/skills/lazy-bug-batch/SKILL.md:541`/`:549`. The WU-scaling machinery is complete and correct on the SCRIPT side: `--sub-skill-args` is a registered CLI flag (`lazy-state.py:5699`, `bug-state.py:4057`), the `--cycle-begin` handler persists it into the marker (`lazy-state.py:5892` → `write_cycle_marker(... sub_skill_args=args.sub_skill_args)`), and `cycle_end_friction_check` reads `marker.get("sub_skill_args")` into `_execute_plan_commit_budget` (`lazy_core.py:5673`). The ONLY missing link was the **prose contract that feeds it**: the C1 bracket command templates in all three coupled orchestrators mandated `--sub-skill {sub_skill}` (Round 15/16 prose) but NEVER added `--sub-skill-args {sub_skill_args}`. So the orchestrator, following the prose verbatim, never supplied it → `args.sub_skill_args` was always `None` → the marker never carried `sub_skill_args` → `_execute_plan_commit_budget` returned `None` (its documented "missing/blank sub_skill_args" fallback) → the detector fell back to the fixed table `budget=3` → any `execute-plan` plan part with >3 work units false-positived `unexpected-commits` (here: 4 commits vs budget 3). Round 20 added the capability and the read/persist wiring but left the prose half un-authored — a classic build-the-engine-but-never-connect-the-throttle gap. Reproduced by inspection: `_execute_plan_commit_budget("execute-plan", None)` → `None` (line 5416-5417 `if not sub_skill_args: return None`); with a real plan path carrying ≥4 WUs → `wu_count + slack ≥ 6`, so 4 commits pass.

**Action:**
  - **Mechanical fix applied** (commit `ce10a52`): added the mandatory `--sub-skill-args {sub_skill_args}` binding to the C1 `--cycle-begin` dispatch bracket in all three coupled orchestrators — both the inline command template AND the prose contract, mirrored:
    - `user/skills/lazy-batch/SKILL.md` (§1d bracket template + Dispatch step-1 template + the MANDATORY-flag prose, which now documents the `budget=3` fixed-table fallback failure mode by name and the 2026-06-16 `adhoc-mcp-runner-payload-interpolation` 4-commits-vs-budget-3 recurrence).
    - `user/skills/lazy-bug-batch/SKILL.md` (§1d bracket one-liner + Dispatch step-1 comment + prose; `bug-state.py` variant).
    - `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` (§1d bracket one-liner + Dispatch step-1 comment + prose; `--cloud` variant, with the cloud-specific "each spurious friction-trip cycle's stray commit gets pushed" emphasis).
    The binding is `--sub-skill-args {sub_skill_args}` bound to the probe's `sub_skill_args` VERBATIM. Safe on non-execute-plan cycles: `_execute_plan_commit_budget` gates on `sub_skill != "execute-plan"` FIRST (returns `None` regardless), and a null/blank `sub_skill_args` argument degrades to the fixed table exactly as before — never a crash, never a false negative.
  - **No gate weakened:** a genuine runaway still trips — the WU-scaled budget is `max(phase_count, wu_count) + slack`, so commits beyond the plan's declared work still fire `unexpected-commits`. This fix REMOVES a false-positive by feeding the detector the input it was already designed to consume (the prose was starving a working code path); the detector's runaway-detection ceiling is unchanged. The fixed-table fallback (the pre-Round-20 conservative behavior) remains intact for any cycle that genuinely cannot supply a plan path.
  - **No script change:** the script side (flag, persistence, read, scaling) was already correct from Round 20 + follow-up; this is a pure prose/contract repair. No `lazy_core.py`/`lazy-state.py`/`bug-state.py` edit.
  - **Coupled-pair mirroring:** the bracket prose is part of the `lazy-batch` ↔ `lazy-bug-batch` (↔ `lazy-batch-cloud`) coupled trio per the CLAUDE.md pairs table — all three edited symmetrically (`bug-state.py`/`--cloud` deltas preserved). **Sentinel-schema lockstep:** N/A — the cycle marker (`lazy-cycle-active.json`) is a runtime state-dir marker, not a doc sentinel; no `sentinel-frontmatter.md` / `SENTINEL_SCHEMAS` schema was touched (the `sub_skill_args` marker field is additive and already shipped in Round 20).
  - **Prohibitions honored:** claude-config only (no AlgoBooth source touched — prohibition 1); no gate weakened (prohibition 2 — the fix unstarves a working detector input, the runaway ceiling is unchanged); the registry/marker were never hand-edited — the gap was in the SKILL prose that drives the script that writes the marker, and the prose is what was fixed (prohibition 3 — fix the writer, not the marker).

**Gates run:**
  test_lazy_core.py: 397/397
  test_hooks.py: 67/67
  lint-skills.py: OK (projected + capabilities; re-run clean after `project-skills.py` regenerated `skills-projected/`)
  lazy-state.py --test: OK
  bug-state.py --test: OK

## Round 23 — 2026-06-16 — process-friction

**Item in flight:** mcp-audio-quality-observability

**Divergence point:** Step 9 (`/mcp-test` validation cycle) — the C1 `--cycle-begin`/`--cycle-end` dispatch bracket. `--cycle-end`'s `cycle_end_friction_check` appended a `kind: process-friction` / `unexpected-commits` ledger entry (`detail: HEAD advanced 2 commits since --cycle-begin (begin_head_sha=a28085bb938e, sub_skill='mcp-test', budget=1)`). The probe then withheld the forward route (`route_overridden_by: pending-hardening-debt`, `pending_hardening=1`, `step=Step 10: mark complete`, `sub_skill=__mark_complete__`). Registry: 4 entries, 1 unconsumed.

**Root cause:** `missing-contract` — `user/scripts/lazy_core.py:5354` (`_CYCLE_COMMIT_BUDGET`). The budget table enumerated `execute-plan: 3`, `retro-feature: 3`, and the `__mark_complete__`/`__mark_fixed__: 3` pseudo-skills, but NEVER `mcp-test`. The Step-9 `/mcp-test` validation cycle legitimately commits MORE THAN ONCE: a clean pass is one commit (the engine's terminal sentinel + the PHASES reconcile, committed together — `repos/algobooth/.claude/skills/mcp-test/SKILL.md:268` Step 5.4), but the deterministic engine's audited mechanics-only self-heal (a `heals[]` field/JSONPath/threshold-typo edit to the scenario YAML or tool-methods map — SKILL.md Step 3.4) lands as a SEPARATE commit, and a `result: partial` `MCP_TEST_RESULTS.md` may land before the terminal sentinel on a follow-up pass. With `mcp-test` absent from the table, `_CYCLE_COMMIT_BUDGET.get("mcp-test", _CYCLE_COMMIT_BUDGET_DEFAULT)` returned the default `1` (`lazy_core.py:5561`), so a normal 2-commit validation cycle tripped `unexpected-commits` (`commits_since=2 > budget=1`, `lazy_core.py:5564`). This is the IDENTICAL missing-row defect class Round 15 fixed for `execute-plan` and Rounds 16/17 fixed for the `__mark_complete__`/`__mark_fixed__` pseudo-skills — `mcp-test` was simply the next un-enumerated multi-commit sub_skill in the same table.

**Action:**
  - **Mechanical fix applied** (commit `fbf00fb`): added a `"mcp-test": 3` row to `_CYCLE_COMMIT_BUDGET` in the shared `lazy_core.py`, with an inline comment naming the two-commit cadence (self-heal commit + sentinel/PHASES-reconcile commit), the `result: partial` follow-up case, and the `a28085bb938e` / budget=1 recurrence. Budget `3` matches the existing `execute-plan`/`retro-feature`/pseudo-skill convention. A genuine runaway (>3 commits) STILL trips — the runaway ceiling is unchanged.
  - **Regression test added** (`test_detect_friction_mcp_test_cycle_multi_commit_within_budget`): positive control (`sub_skill='mcp-test'`, `commits_since=2`, intact run identity → None) AND negative control (`commits_since=7` → `unexpected-commits` still trips), mirroring the Round 16/17 pseudo-skill test. Confirmed discovered + green via pytest (500 items collected, 2 selected pass).
  - **Coupled-pair / sentinel-schema:** none applicable. The budget table lives in the SHARED `lazy_core.py` module — `bug-state.py` (which routes its own Step-9 MCP gate, `SKILL_MCP_TEST = "mcp-test"`) inherits the row automatically through the same `detect_cycle_bracket_friction`, so no `bug-state.py` edit and no SKILL.md prose change. No sentinel schema touched (`sentinel-frontmatter.md` / `SENTINEL_SCHEMAS` untouched — this is a runtime cycle-budget table, not a doc sentinel).
  - **Prohibitions honored:** claude-config only (no AlgoBooth source touched — prohibition 1); no gate weakened (prohibition 2 — the runaway ceiling is unchanged; the fix gives a legitimate multi-commit sub_skill the same honest ceiling its siblings already had); the registry/marker were never hand-edited (prohibition 3 — the gap was in the script's budget table that drives the friction detector, and the table is what was fixed; the `pending_hardening=1` debt clears when this dispatch is acked, not by laundering the ledger).

**Gates run:**
  test_lazy_core.py: 397/397 (curated runner) + new test green via pytest (500 collected, 2 selected)
  test_hooks.py: 67/67
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: OK
  bug-state.py --test: OK

## Round 24 — 2026-06-16 — no-route

**Item in flight:** d8-session-format

**Divergence point:** Step 7a (`write plan`) — the orchestrator re-emitted `sub_skill=write-plan` in a no-progress loop. Probe: `current_step='Step 7a: write plan'`, `sub_skill=write-plan`, `repeat_count=1`, `step_repeat_count=1`; all 8 phases impl-complete, Phase 8 In-progress with only reachability-smoke + Runtime-Verification rows unchecked. Registry: empty (the write-plan dispatch is a guaranteed no-op — every implementation plan part (part-1..6) is Complete, so there is no plannable work to route to). The forward route never advances past Step 7a toward the retro→MCP gate.

**Root cause:** `script-defect` — `user/scripts/lazy_core.py:1245` (`_VERIFICATION_SECTION_RE`). The regex matched `runtime verification` and the three `mcp\s+(?:integration\s+test|test\s+assertion|assertion)` variants but NOT `reachability smoke`. d8-session-format Phase 8's only non-RuntimeVerification unchecked `- [ ]` row sat under a SIBLING bold subsection header `**Reachability smoke (new API surface introduced this phase):**`. In `remaining_unchecked_are_verification_only` (`lazy_core.py:1305-1322`) a bold-lead line that does NOT match the verification pattern leaves `in_verification` untouched; since the preceding row context was not in a matching verification scope, the reachability-smoke row was walked with `in_verification=False` and the function returned `False` (`lazy_core.py:1330-1331`). That `False` told the Step-7 workstation bypass that a genuine implementation row remained, so Step 7a kept emitting `write-plan`. The row is in fact a live-MCP-call assertion owned by `/mcp-test` (invoke `trigger_file_menu_action` against the live app + confirm via `get_session_events`). `Reachability smoke (new API surface ...)` is a `/spec-phases`-authored convention (`user/skills/_components/phases-runtime-verification.md:66-84`) so this recurs across every feature that introduces a new API surface — same un-enumerated-verification-subsection defect class the regex already covered for the runtime/mcp variants.

**Action:**
  - **Mechanical fix applied** (commit `d8d02ef`): extended `_VERIFICATION_SECTION_RE` to match `reachability\s+smoke` alongside the existing alternatives, with an inline comment naming the `/spec-phases` convention, the bold-sibling-header authoring shape, and the d8-session-format Phase 8 recurrence. This makes the `**Reachability smoke ...**` sibling header itself a verification boundary (`in_verification=True`), so its rows are correctly classified verification-only and the bypass falls through to the retro→MCP gate. A genuine implementation row outside any verification subsection STILL returns `False` — the conservative leave-scope semantics are unchanged.
  - **Regression tests added** (`test_ruvonly_reachability_smoke_bold_subsection`, `test_ruvonly_reachability_smoke_heading`): the bold-subsection form reproduces the live d8-session-format Phase 8 fixture (a `**Reachability smoke (new API surface introduced this phase):**` header + a `trigger_file_menu_action`/`get_session_events` row + a trailing `**Runtime Verification**` row) and asserts True; the heading form is the markdown-heading mirror. Both green in the 397/397 curated run.
  - **Coupled-pair / sentinel-schema:** none applicable. `_VERIFICATION_SECTION_RE` and `remaining_unchecked_are_verification_only` live in the SHARED `lazy_core.py`; `bug-state.py` and `lazy-state.py` both import them (`bug-state.py:93`, `lazy-state.py:91-92`), so the bug pipeline inherits the fix automatically — no `bug-state.py` edit and no SKILL.md prose change. No sentinel schema touched.
  - **Prohibitions honored:** claude-config only (no AlgoBooth source touched — prohibition 1); no gate weakened (prohibition 2 — the conservative leave-verification-scope semantics for genuine implementation rows are unchanged; the fix only adds a legitimate verification-subsection title the detector was blind to); the registry/marker were never hand-edited (prohibition 3 — the gap was in the script's section regex that drives the detector, and the regex is what was fixed).

**Gates run:**
  test_lazy_core.py: 397/397
  test_hooks.py: 67/67
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: OK
  bug-state.py --test: OK

## Round 25 — 2026-06-16 — no-route

**Item in flight:** d8-session-format

**Divergence point:** Step 7a (`write plan`) — the orchestrator re-emitted `sub_skill=write-plan` in a no-progress loop (probe: `current_step='Step 7a: write plan'`, `sub_skill=write-plan`, `repeat_count=1`, `step_repeat_count=1`; Phase 9 impl deliverables all `[x]`, plan part-7 Complete, registry empty). The write-plan dispatch is a guaranteed no-op — every implementation plan part is Complete — so the forward route never advances past Step 7a toward the retro→MCP gate. SECOND consecutive instance of this exact regex-gap class THIS RUN (the first was `reachability smoke`, Round 24 / `d8d02ef`).

**Root cause:** `script-defect` — `user/scripts/lazy_core.py` (`_VERIFICATION_SECTION_RE`). The regex matched `runtime verification`, the `mcp\s+(?:integration\s+test|test\s+assertion|assertion)` variants, and (post-Round-24) `reachability smoke`, but NOT `full-chain seam audit` / `seam audit`. d8-session-format Phase 9's only unchecked `- [ ]` rows (post-fix re-probe seam rows + the certifying `Workstation: /mcp-test session-format-end-to-end passes` row) sat under the bold subsection header `**Full-chain seam audit (HARD — retry_count >= 2 escalation; consumes INVESTIGATION.md ## Seam Table):**`. In `remaining_unchecked_are_verification_only` (`lazy_core.py:1333-1335`) a non-matching bold lead leaves `in_verification` untouched, so those rows were walked with `in_verification=False` and the function returned `False`, telling the Step-7 workstation bypass a genuine implementation row remained. Those rows are in fact live-MCP seam re-probes + the certifying `/mcp-test` row, all owned by `/mcp-test`. `Full-chain seam audit` is the `retry_count >= 2` escalation convention authored by `user/skills/_components/blocked-resolution.md` (step 1a/6) + `phases-runtime-verification.md`, so it recurs on EVERY escalated feature — the same un-enumerated-verification-subsection defect class the regex already covered for the runtime/mcp/reachability-smoke variants.

**Action:**
  - **Mechanical fix applied** (commit `f2b1552`): extended `_VERIFICATION_SECTION_RE` with the seam-audit escalation family `full[-\s]chain\s+seam|seam\s+(?:audit|re-?validation)` (covers "full-chain seam audit", "seam audit", "seam re-validation"/"revalidation"). Took the robust path the dispatch evidence asked for over per-phrase whack-a-mole: documented the FULL canonical verification-subsection header set (Runtime Verification / MCP Integration Test+assertions / Reachability smoke / seam-audit escalation family) in the comment above the regex as the single source of truth, and added a REGEX-LOCKSTEP note to `phases-runtime-verification.md` instructing harness maintainers to mirror any NEW verification/escalation subsection convention into the regex + a test fixture. Conservative leave-verification-scope semantics for genuine implementation rows are unchanged — a real implementation row outside any verification subsection still returns `False`.
  - **Regression tests added + orphans rescued** (`test_ruvonly_full_chain_seam_audit_bold_subsection`, `test_ruvonly_seam_audit_heading_variants`): the bold-subsection form reproduces the live d8-session-format Phase 9 fixture (the `**Full-chain seam audit (HARD — retry_count >= 2 escalation; ...):**` header + two seam re-probe rows + the `Workstation: /mcp-test ... passes` row) and asserts True; the heading-variant test covers `### Seam Audit` / `### Seam Re-validation` / `### Full-Chain Seam Audit`. **Latent test-gap fixed:** Round 24's two reachability-smoke tests were authored at `test_lazy_core.py:409/440` but NEVER registered in the `_TESTS` runner tuple — they had never actually executed (dead-code regression coverage for the prior round of THIS SAME bug class). All four (2 orphaned RD24 + 2 new) are now registered and green.
  - **Coupled-pair / sentinel-schema:** none applicable. `_VERIFICATION_SECTION_RE` and `remaining_unchecked_are_verification_only` live in the SHARED `lazy_core.py`; `bug-state.py` and `lazy-state.py` both import them, so the bug pipeline inherits the fix automatically — no `bug-state.py` edit, no SKILL.md prose change, no sentinel schema touched. The component edit (`phases-runtime-verification.md`) is documentation lockstep, re-projected + lint-clean.
  - **Prohibitions honored:** claude-config only (no AlgoBooth source touched — prohibition 1); no gate weakened (prohibition 2 — only added verification-subsection titles the detector was blind to; the conservative semantics for genuine implementation rows are unchanged); registry/marker never hand-edited (prohibition 3 — the gap was in the script's section regex that drives the detector, and the regex is what was fixed).

**Gates run:**
  test_lazy_core.py: 403/403
  test_hooks.py: 67/67
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: OK
  bug-state.py --test: OK

## Round 26 — 2026-06-16 — validate-deny (manual disposition)

**Item in flight:** (none — friction observed during a `/spec` authoring session in `claude-config`, no active run)

**Divergence point:** Two `Explore`-agent dispatches issued during a `/spec` session in `claude-config` (mapping the orchestrator cycle shape + the state-script/toolify surface for the specs being authored) were denied by `lazy-dispatch-guard.sh` ("dispatch prompt not script-emitted this turn"), and the two `validate-deny` entries then withheld `claude-config`'s forward route (`pending_hardening: 2`) on the next probe — blocking the queued `multi-repo-concurrent-runs` feature from advancing to `plan-feature`.

**Root cause:** `script-defect` / `missing-contract` (global-singleton run marker). The run marker is a single global file (`~/.claude/state/lazy-run-marker.json`) and the guard keys off its mere existence, not its `repo_root`. A concluded AlgoBooth run (`repo_root: C:/Users/Jacob/repos/AlgoBooth`, `forward_cycles: 19/20`) had left an orphaned marker (no clean `--run-end` — registry also orphaned), so the guard was armed machine-wide and denied legitimate, unrelated exploration dispatches in a different repo's session. This is cross-repo contamination: the denials were *correct given the singleton design* but spurious as harness debt — they reflect no `claude-config` misroute. The deny-ledger then propagated the contamination into `claude-config`'s pipeline state.

**Action:**
  - **Disposition (operator-authorized):** `python user/scripts/lazy-state.py --run-end --ack-unhardened` discharged the orphaned AlgoBooth marker + prompt registry and blanket-acked the 2 spurious `validate-deny` entries (the sanctioned Round-20 unclearable-debt path). NOT a laundering of denials — the denied dispatches were not re-run or legitimized; they are recorded as correctly-denied cross-repo contamination and the debt discharged with operator authorization. Re-probe confirmed the forward route reopened to `plan-feature` for `multi-repo-concurrent-runs`.
  - **Durable fix:** already specced as the `multi-repo-concurrent-runs` feature (queued P1, enqueued 1st) — per-`repo_root` marker registry, hooks match the current repo, `--run-end` clears its own marker, same-repo second run refused. This round is the live evidence that motivates that feature; the fix is not re-derived here.
  - **Prohibitions honored:** claude-config only (no AlgoBooth source touched); no gate weakened (the guard behaved correctly — the defect is the marker's global scope, not the guard's check); marker/registry cleared via the sanctioned `--run-end` writer, never hand-edited.

**Gates run:** (disposition-only — no code change this round; the durable fix lands under full gates inside the `multi-repo-concurrent-runs` feature)
  re-probe: forward route reopened (plan-feature) — OK


## Round 27 — 2026-06-19 — validate-deny

**Item in flight:** completion-coherence-gate-reconciliation (claude-config session `60fadd89`, `/lazy-batch`, Step 4.5 stub-spec → `/spec`, `pending_hardening=1`)

**Divergence point:** Step 1d (cycle dispatch). The orchestrator dispatched a `cycle`-class `Agent` prompt whose sha (`denied_sha12: 10c4045a4233`) matched NO registry entry, so `lazy-dispatch-guard.sh` → `lazy_guard.py` denied it with the ORDINARY corrective recipe (guard case 3, "miss / no matching entry"). The deny then withheld the forward route (`route_overridden_by: pending-hardening-debt`, probe `step=Step 4.5: stub-spec detected sub_skill=spec feature_id=completion-coherence-gate-reconciliation pending_hardening=1`) and routed THIS script-emitted, registered hardening dispatch inline (locked decision 4).

**Root cause:** none for THIS round — **working-as-designed re-route of the hand-composed / non-verbatim-prompt-reaching-the-guard class already mechanically hardened (Rounds 2 & 4) and exhaustively prose-specified** (`lazy-batch/SKILL.md`: dispatch `cycle_prompt` VERBATIM from `--emit-prompt`; "Never hand-append to `cycle_prompt`" line 534; the Phase-8 withhold + every-deny-routes-hardening contract). Forensic evidence that the deny was a non-verbatim dispatch, not a script-defect and not a novel gap:
- **Per-repo state dir** `~/.claude/state/853ac81e…/` (the `multi-repo-concurrent-runs` keyed dir for `repo_root: C:/Users/Jacob/source/repos/claude-config` — Round-26's durable fix, now live; the AlgoBooth run lives in a SEPARATE keyed dir `37850b6e…` and did NOT contaminate this deny). Marker `last_advance_state_key` = `[completion-coherence-gate-reconciliation, Step 4.5: stub-spec detected, spec]`, byte-matching the injected probe JSON.
- **Registry (`lazy-prompt-registry.json`, 17 entries) holds 6 entries for this item across 3 distinct sha256 / 3 distinct classes:** `3acf8f57ab34` (8966 chars, class `cycle`, emitted ×4 at ts …210/…223/…104/…128), `d29bffd7b15f` (7572, class `apply-resolution`, consumed), `d4b73ec9ed27` (5501, class `hardening` — this very dispatch, consumed). The denied sha `10c4045a4233` is **absent from every `prompt_sha256`** — the dispatched cycle prompt was NOT a verbatim copy of any of the 4 identical `3acf8f57ab34` cycle emits.
- **Emit determinism confirmed:** re-running `lazy-state.py --emit-prompt` for this exact state twice produced byte-identical output (`8ab6888f0c5f`, len 11628, both runs) — `--emit-prompt` is deterministic, so the script emitted a STABLE cycle prompt (`3acf8f57ab34`) at failure time; the divergent `10c4045a4233` could only have been introduced by hand-alteration/composition before the `Agent` call, not by the script. (The current-state sha differs from the failure-time `cycle` sha only because state has advanced and `pending_hardening` context shifted — expected, not a defect.)
- **The withhold + reroute + ack machinery fired exactly as designed:** the deny appended `10c4045a4233` (ledger `ts 1781899199.697`, `reason_head` = the canonical corrective recipe); the next probe withheld the forward route and emitted the hardening route; the recovery dispatch (`d4b73ec9ed27`, class `hardening`, ledger `ts 1781899242`, `acked: true`) is THIS stage, depth-0, allowed normally. A registered route was produced and consumed at every step.
- Re-applying or inventing a fix here would re-investigate an already-hardened root cause and risk weakening a correct, load-bearing gate (prohibition 2); a dedup-by-signature suppression would re-litigate locked decision 4 ("every occurrence is signal, not noise"). Mirrors the Round-13 disposition (same class, hardening-class hand-compose there vs `cycle`-class non-verbatim here).

Full gate suite run as health evidence (mandatory, to surface any latent defect) WITH the live claude-config marker present — all green, no new defect surfaced. Schemas untouched → coupled-pair mirroring + sentinel-schema lockstep N/A this round.

**Action:**
  - **No harness change.** The triggering misroute is the hand-composed / non-verbatim-`cycle_prompt` class already mechanically hardened (Rounds 2 & 4) and prose-specified; the deny → ledger-append → probe-withhold → hardening-reroute → guard-allow-ack machinery all behaved exactly as designed. Recording a mechanical fix or contract fork here would be dishonest (no new defect) and/or gate-weakening. Per-repo keying (Round-26 durable fix) is confirmed working — this deny is NOT cross-repo contamination. Full gate suite run as health evidence (all green).

**Over-fit spin-off:** none — no mechanical fix landed (working-as-designed disposition), so there is no fix to generalize; the durable structural fix for this class (verbatim-dispatch contract + `@@lazy-ref` by-reference mechanism + Round 2/4 hardening) already exists. No over-fit smell tripped.

**Gates run:**
  test_lazy_core.py: 574/574
  test_hooks.py: 76/76
  lint-skills.py: OK
  lazy-state.py --test: OK
  bug-state.py --test: OK

## Round 28 — 2026-06-19 — manual

**Item in flight:** f1-global-scale (AlgoBooth `/lazy-batch 25`, Step 7a, plan part-0 `phases: [0]`)
**Divergence point:** Step 7a plan-selection routed `__flip_plan_complete_stale__` on a decomposition part whose work was UNDONE — the orchestrator surfaced it via `AskUserQuestion`; operator chose "harden the harness".

**Root cause:** `script-defect` — vacuous-truth in the stale-plan gate (`lazy-state.py` Step 7a, `_unchecked_wus_in_plan_scope` consumer ~line 1844). `/write-plan` emits a `phases: [0]` decomposition part for touchpoint-audit `block` verdicts (here: `StrudelRuntime.ts` 2048 LOC + `useStrudelCompletions.ts` 1613 LOC) but adds NO matching `### Phase 0` section to PHASES.md — the decomposition WUs (`WU-0A`/`WU-0B`) live only in the plan body. `_unchecked_wus_in_plan_scope(phases_text, {0})` (`lazy_core.py:1030`) walks PHASES.md tracking `^### Phase (\d+)`; with no Phase 0 it returns `[]`. The gate read empty-unchecked as `finalize_stale = True` and flipped the plan Complete, SILENTLY DROPPING the decomposition (then Phases 1–7 would extend the un-split 2048/1613-LOC files, risking the composite-score arch ratchet). The plan's own `- [ ] WU-0A/0B` boxes were never consulted. Same failure shape the existing empty-`plan_phase_set` guard already defends ("scope unknown → fall through"), but it did not cover a `phases:[N]` referencing a non-existent PHASES phase. Evidence: live probe returned `sub_skill=__flip_plan_complete_stale__` for `plans/all-phases-global-scale-part-0.md`; PHASES.md headings are Phase 1–7 only (`grep '^### Phase'`); part-0 body has 2 unchecked `- [ ] WU` rows.

**Action:**
  - Mechanical fix applied (commit `328d146`): added `lazy_core._all_wus_in_plan_scope()` (TOTAL checked+unchecked in-scope rows). The Step 7a gate now disambiguates the empty `_unchecked_wus_in_plan_scope()` result via the TOTAL count: scope has rows → original gate unchanged; scope resolves to ZERO rows → fall back to the plan's OWN per-WU checkboxes (`_plan_wu_checkbox_counts` / `_plan_unchecked_wus_are_verification_only`, the ISSUE-6 machine source of truth). Stale ONLY when the plan has parseable WU boxes and every non-verification one is `[x]`; any unchecked real WU — or no parseable boxes — falls through to `/execute-plan` so the decomposition runs. Verified on the live repro: probe now returns `sub_skill=execute-plan` for part-0. Two regression fixtures added (`stale-empty-scope-decomposition-executes` → execute-plan; `stale-empty-scope-all-plan-wus-checked-flips` → flip) proving the fallback discriminates done-vs-undone. Baseline regenerated, proven purely additive (+2 lines, 0 removed).

**Over-fit spin-off:** none — fix is structural (TOTAL-row disambiguation + plan-WU fallback; generalizes to any `phases:[N]` referencing a non-existent PHASES phase, not a literal-phrase-to-matcher) and the class has not recurred (prior "stale" rounds were retro-staleness/realign-freshness, a different root-cause class).

**Gates run:**
  test_lazy_core.py: 582/583 (the 1 failure — `test_archive_fixed_real_drivers_pass_audit` — is PRE-EXISTING test-pollution: PASSES in isolation, FAILS identically on the clean tree with my edits stashed; not cwd-based; unrelated to this round — noted for a future test-isolation round)
  test_hooks.py: 76/76
  lint-skills.py: OK
  lazy-state.py --test: OK (incl. 2 new fixtures)
  bug-state.py --test: OK

## Round 29 — 2026-06-19 — validate-deny

**Item in flight:** feature-budget-guard-and-skip-ahead (claude-config session `24410412`, `/lazy-batch 25`, Step 4.5 stub-spec → `/spec`, `pending_hardening=1` at dispatch time)

**Divergence point:** Step 1d (cycle dispatch). The orchestrator took the script-emitted stub-finalize `cycle` prompt and hand-**APPENDED** a `NOTE FOR THIS CYCLE` paragraph to it before the `Agent` call. The append mutated the bytes (`denied_sha12: df06fe937dd9`) away from every registered `cycle` emission, so `lazy-dispatch-guard.sh` → `lazy_guard.py` denied it with the ORDINARY corrective recipe (guard case 3, "miss / no matching entry": *"dispatch prompt not script-emitted this turn — re-run the Step 1a probe (`--emit-prompt`) and dispatch its `cycle_prompt` verbatim…"*). The deny then withheld the forward route (`route_overridden_by: pending-hardening-debt`, probe `step=Step 4.5: stub-spec detected sub_skill=spec feature_id=feature-budget-guard-and-skip-ahead pending_hardening=1`) and routed THIS script-emitted, registered hardening dispatch inline (locked decision 4).

**Root cause:** none for THIS round — **working-as-designed re-route of the hand-append / non-verbatim-`cycle_prompt`-reaching-the-guard class already mechanically hardened (Rounds 2 & 4) and named VERBATIM in the SKILL prose.** The exact failure — "appending a note" to the emitted prompt — is the canonical example in the Freshness rule (`lazy-batch/SKILL.md:593`: *"Hand-editing emitted text is the failure class this rule names: appending a note, 'cleaning it up', re-typing it, or splicing in context all mutate the hash and trip the guard … The template's `--context key=value` slots are the ONLY sanctioned customization point"*) and the "Never hand-append to `cycle_prompt`" rule (`lazy-batch/SKILL.md:534`, mirrored in `_components/lazy-dispatch-template.md:43-48`). Forensic evidence that the deny was a hand-appended dispatch, not a script-defect and not a novel gap:
- **Per-repo state dir** `~/.claude/state/853ac81e…/` (the `multi-repo-concurrent-runs` keyed dir for `repo_root: C:/Users/Jacob/source/repos/claude-config` — Round-26's durable fix, live; the AlgoBooth run lives in a SEPARATE keyed dir and did NOT contaminate this deny). Marker `last_advance_state_key` = `[feature-budget-guard-and-skip-ahead, Step 4.5: stub-spec detected, spec]`, byte-matching the injected probe JSON.
- **The denied sha `df06fe937dd9` is ABSENT from every registry `prompt_sha256`.** The registry holds 10 entries for this item across 4 sha / 3 classes: `1b16972cbd90` (`cycle`, ×5), `5cc0c93a0954` (`cycle`, ×3), `acf232d14be0` (`apply-resolution`, ×1), `c18e3026ec57` (`hardening` — this very dispatch, consumed). The denied prompt was NOT a verbatim copy of any registered `cycle` emit — the appended NOTE paragraph mutated it away. The live ledger's `prompt_head` for the deny (`"Run exactly one skill, then stop. You are one worker in the feature pipeline.\nFeature: Feature Budget Guard + Skip-Ahead…"`) confirms it WAS the stub-finalize cycle prompt, hand-altered.
- **Emit determinism confirmed:** re-running `lazy-state.py --emit-prompt` for this state twice produced byte-identical output (`d6905c5ef4b6`, len 11576, both runs) — `--emit-prompt` is deterministic, so the script emitted a STABLE cycle prompt at failure time; the divergent `df06fe937dd9` could only have been introduced by the hand-append before the `Agent` call, never by the script. (The current-state sha differs from the failure-time `cycle` shas only because state advanced and `pending_hardening` context shifted — expected, not a defect.)
- **The withhold + reroute + ack machinery fired exactly as designed:** the deny appended ledger entry `df06fe937dd9` (`ts 1781927428.903`, `reason_head` = the canonical corrective recipe); the next `--emit-prompt` probe WITHHELD the forward route (`lazy-state.py:6965-6981`, `route_overridden_by: pending-hardening-debt`) and produced the hardening route; the guard-allow of THIS dispatch FIFO-acked the entry (`acked: true`, `acked_ts: 1781927526`). Post-ack the live probe returns `pending_hardening=0` and a normal forward `cycle_prompt` for the same `Step 4.5 / spec` route — debt retired, route restored.
- Re-applying or inventing a fix here would re-investigate an already-hardened root cause and risk weakening a correct, load-bearing gate (prohibition 2); a dedup-by-signature suppression would re-litigate locked decision 4 ("every occurrence is signal, not noise"). Mirrors the Round-27 disposition exactly (same item-state shape: Step 4.5 stub-spec → `/spec`; Round 27 was a fully hand-composed cycle prompt, this is the hand-APPEND variant — the same non-verbatim class).

Full gate suite run as health evidence (mandatory, to surface any latent defect) WITH the live claude-config marker present — all green, no new defect surfaced. Schemas untouched → coupled-pair mirroring + sentinel-schema lockstep N/A this round.

**Action:**
  - **No harness change.** The triggering misroute is the hand-append / non-verbatim-`cycle_prompt` class already mechanically hardened (Rounds 2 & 4) and named verbatim in `lazy-batch/SKILL.md:593` (Freshness — "appending a note") + `:534` (Never hand-append). The deny → ledger-append → probe-withhold → hardening-reroute → guard-allow-ack machinery all behaved exactly as designed. Recording a mechanical fix or contract fork here would be dishonest (no new defect) and/or gate-weakening. Per-repo keying (Round-26 durable fix) confirmed working — this deny is NOT cross-repo contamination. Full gate suite run as health evidence (all green).

**Over-fit spin-off:** none — no mechanical fix landed (working-as-designed disposition), so there is no fix to generalize; the durable structural fix for this class (verbatim-dispatch contract + `--context`-only customization channel + `@@lazy-ref` by-reference mechanism + Round 2/4 hardening) already exists. No over-fit smell tripped.

**Gates run:**
  test_lazy_core.py: 603/603
  test_hooks.py: 76/76
  lint-skills.py: OK
  lazy-state.py --test: OK
  bug-state.py --test: OK

## Round 30 — 2026-06-21 — validate-deny

**Item in flight:** budget-guard-defers-near-complete-feature (claude-config session `acdfd6d8`, `/lazy-batch 20`, `__mark_complete__`/`__mark_fixed__` → coherence-recovery cycle, `pending_hardening=1` at dispatch time)

**Divergence point:** Step 1d (cycle dispatch) for the `coherence-recovery` class. After `--apply-pseudo` refused the terminal flip on a phase-incoherent PHASES.md (unchecked deliverable boxes), the orchestrator was meant to dispatch the script-emitted+registered `coherence-recovery` `dispatch_prompt` VERBATIM (`lazy-batch/SKILL.md:500-508`). Instead it dispatched a **raw inline, non-verbatim** coherence-recovery prompt (`denied_sha12: 66bc1f2b65d9`) that matched no registry entry, so `lazy-dispatch-guard.sh` → `lazy_guard.py` denied it with the ORDINARY corrective recipe (guard case 3, "miss / no matching entry"). The deny withheld the forward route (`route_overridden_by: pending-hardening-debt`, probe `step=None sub_skill=None feature_id=None pending_hardening=1` — the gate-halt withhold shape) and routed THIS script-emitted, registered hardening dispatch inline (locked decision 4).

**Root cause:** none for THIS round — **working-as-designed re-route of the hand-composed / non-verbatim-prompt-reaching-the-guard class already mechanically hardened (Rounds 2 & 4) and exhaustively prose-specified** (`lazy-batch/SKILL.md:508` "Dispatch `dispatch_prompt` VERBATIM using `dispatch_model`"; the Freshness/never-hand-append rules `:534`/`:593`; the `@@lazy-ref` by-reference dispatch mechanism). Forensic evidence (per-repo keyed state dir `~/.claude/state/853ac81e…/` — the `multi-repo-concurrent-runs` dir for `repo_root: C:/Users/Jacob/source/repos/claude-config`; the AlgoBooth run lives in the separate keyed dir `37850b6e…` and did NOT contaminate this deny):
- **The registry holds THREE legitimate `coherence-recovery` emissions for this item** — `40bc883df335` (nonce `bfe22447`, unconsumed), `38fedd84c11b` (nonce `29b8db50`, unconsumed), and `38fedd84c11b` (nonce `7aa20ef5`, **consumed**). A registered coherence-recovery route was available; a non-verbatim one was dispatched.
- **The denied sha `66bc1f2b65d9` is ABSENT from every registry `prompt_sha256`** — confirming a hand-composed / non-verbatim prompt, never registered (guard case 3). The live ledger's `prompt_head` ("You are running a COHERENCE-RECOVERY cycle…PHASES.md is in an incoherent state…") is byte-identical to this dispatch's injected denied-prompt summary, confirming it WAS the coherence-recovery prompt, hand-altered.
- **The deny entry carries NO `dispatch_by_reference` flag and NO `nonce`** (raw inline dispatch), whereas the IMMEDIATELY-FOLLOWING ledger entry shows the orchestrator self-corrected: it re-dispatched the registered emission `38fedd84c11b` **by reference** (`dispatch_by_reference: true`, nonce `7aa20ef5…`, `item_id: budget-guard-defers-near-complete-feature`) — consumed. The `@@lazy-ref` channel is exactly the sanctioned verbatim path; the deny was the orchestrator bypassing it inline, then recovering onto it.
- **The withhold + reroute + ack machinery fired exactly as designed:** the deny appended `66bc1f2b65d9` (`ts 1782102536.56`, `reason_head` = the canonical `_CORRECTIVE_RECIPE`); the probe WITHHELD the forward route over `pending_hardening=1`; the hardening dispatch (`f83396fab200`, class `hardening`, by-reference) is THIS stage, depth-0, allowed normally; its guard-allow FIFO-acked the deny (`acked: true`, `acked_ts 1782103066.198`). Because the denied sha matched no hardening-class entry, the guard correctly returned the ordinary recipe (NOT the depth-1 cap) — this is NOT recursive hardening.
- Re-applying or inventing a fix here would re-investigate an already-hardened root cause (input rule) and risk weakening a correct, load-bearing gate (prohibition 2); a dedup-by-signature suppression would re-litigate locked decision 4 ("every occurrence is signal, not noise"). Mirrors the Round-27 & Round-29 dispositions (same non-verbatim-`cycle_prompt` class; this is the `coherence-recovery`-class instance of it).

Full gate suite run as health evidence (mandatory, to surface any latent defect à la Rounds 8/11) WITH the live claude-config cycle marker present — all green, no new defect surfaced. Schemas untouched → coupled-pair mirroring + sentinel-schema lockstep N/A this round.

**Action:**
  - **No harness change.** The triggering misroute is the hand-composed / non-verbatim coherence-recovery `dispatch_prompt` class already mechanically hardened (Rounds 2 & 4) and prose-specified (`lazy-batch/SKILL.md:508` VERBATIM-dispatch; `:534`/`:593` never-hand-append/Freshness). The deny → ledger-append → probe-withhold → hardening-reroute → guard-allow-ack machinery all behaved exactly as designed, and the orchestrator self-corrected by re-dispatching the registered emission by reference (`38fedd84c11b`). Recording a mechanical fix or contract fork here would be dishonest (no new defect) and/or gate-weakening. Per-repo keying (Round-26 durable fix) confirmed working — this deny is NOT cross-repo contamination.

**Over-fit spin-off:** none — no mechanical fix landed (working-as-designed disposition), so there is no fix to generalize; the durable structural fix for this class (verbatim-dispatch contract + `@@lazy-ref` by-reference mechanism + Round 2/4 hardening) already exists. No over-fit smell tripped.

**Gates run:**
  test_lazy_core.py: 764/764
  test_hooks.py: 93/93
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: OK
  bug-state.py --test: OK

## Round 31 — 2026-06-22 — process-friction

**Item in flight:** d2-sample-import-ui (AlgoBooth `/lazy-batch`, Step 7a planning bracket, `pending_hardening=1` at dispatch time)
**Divergence point:** Step 7a (the planning dispatch — direct `/write-plan` here; the consolidated Step-6 `/plan-feature` shares the same exposure). `--cycle-end`'s `cycle_end_friction_check` → `detect_cycle_bracket_friction` appended a `kind: process-friction` / `unexpected-commits` ledger entry (`detail: HEAD advanced 2 commits since --cycle-begin (begin_head_sha=08d33d580cfe, sub_skill='write-plan', budget=1)`). The probe withheld the forward route (`route_overridden_by: pending-hardening-debt`, `pending_hardening=1`, forward probe `step=Step 9: run MCP tests sub_skill=mcp-test`) and `build_hardening_emit_command`'s process-friction branch (Round 15 D2, working) routed THIS dispatch.

**Root cause:** `missing-contract` — `user/scripts/lazy_core.py` `_CYCLE_COMMIT_BUDGET` (def at `lazy_core.py:8940`). The budget table enumerated `execute-plan: 3`, `retro-feature: 3`, `mcp-test: 3`, and the `__mark_complete__`/`__mark_fixed__: 3` pseudo-skills, but NEVER the **planning** sub_skills. The planning dispatch legitimately commits MORE THAN ONCE: the consolidated `/plan-feature` runs `/spec-phases` (commits PHASES.md) THEN `/write-plan` back-to-back in one cycle (`lazy-state.py:2813`), and `/write-plan` may emit a **multi-part plan series** (`all-phases-<slug>-part-1.md`, `-part-2.md`, … per the 8-WU partition cap — `write-plan/SKILL.md` Step 2.5), committing once per part. The codebase already names this exposure ("the d8 write-plan loop, where each cycle COMMITS (HEAD advances)" — `lazy_core.py:4534`). With the planning skills absent, `_CYCLE_COMMIT_BUDGET.get('write-plan', _CYCLE_COMMIT_BUDGET_DEFAULT)` returned the default `1` (`lazy_core.py:9165`), so a normal 2-commit planning cycle tripped `unexpected-commits` (`commits_since=2 > budget=1`). Reproduced live (module-loaded `detect_cycle_bracket_friction`): `sub_skill='write-plan'|'plan-feature', commits_since=2` → `unexpected-commits` (budget 1); after the fix → `None`; `commits_since=7` → still trips (no gate weakened); `execute-plan, 2` → `None` (the already-fixed sibling). This is the IDENTICAL missing-budget-row defect class Round 15 fixed for `execute-plan`, Rounds 16/17 for the `__mark_complete__`/`__mark_fixed__` pseudo-skills, and the `mcp-test` row — the two planning skills (+ the bug-pipeline `/plan-bug` analog, `bug-state.py:1183` `sub_skill=SKILL_PLAN_BUG`, which also authors PHASES.md THEN dispatches `/write-plan`) were simply the next un-enumerated multi-commit sub_skills in the same table.

**Action:**
  - Mechanical fix applied (commit `8039dbc`): added three rows to `_CYCLE_COMMIT_BUDGET` in the shared `lazy_core.py` — `write-plan: 3`, `plan-feature: 3`, `plan-bug: 3` — with an inline comment naming the spec-phases-PHASES + write-plan-multi-part cadence, the `lazy_core.py:4534` "write-plan loop COMMITS" precedent, and the `08d33d580cfe` / budget=1 recurrence. Budget `3` matches the existing `execute-plan`/`mcp-test`/pseudo-skill convention; a genuine runaway (>3 commits) STILL trips — the runaway ceiling is unchanged. Regression test `test_detect_friction_planning_cycle_multi_commit_within_budget` added (mirrors the Round 16/17 `__mark_complete__` + `mcp-test` test shape: asserts `write-plan`/`plan-feature`/`plan-bug` 2-commit cycles → `None`, and a 7-commit runaway → `unexpected-commits`) and registered in `_TESTS`.
  - **Coupled-pair / sentinel-schema:** none applicable. The budget table lives in the SHARED `lazy_core.py` module — `bug-state.py` (which routes its own planning via `/plan-bug` and its Step-9 MCP gate) inherits the rows automatically through the same `detect_cycle_bracket_friction`, so no `bug-state.py` edit and no `lazy-batch`/`lazy-bug-batch`/`lazy-batch-cloud` SKILL.md prose change. No sentinel schema touched (`sentinel-frontmatter.md` / `SENTINEL_SCHEMAS` untouched — this is a runtime cycle-budget table, not a doc sentinel).
  - **Prohibitions honored:** claude-config only (the AlgoBooth tree was the cwd but never edited — prohibition 1); no gate weakened (prohibition 2 — the runaway ceiling is unchanged; the fix gives a legitimate multi-commit sub_skill the same honest ceiling its siblings already had); registry/marker never hand-edited (prohibition 3 — the gap was in the script's budget table that drives the friction detector; the false-positive ledger entry remains honest debt, retired by the guard's allow-time FIFO ack of THIS dispatch, not by laundering the ledger).

**Over-fit spin-off:** harden(spinoff): class-recurred-≥2 (signal 2) + literal-row-to-allowlist (signal 1) — the missing-budget-row class has now recurred FIVE times (Round 15 `execute-plan`, Rounds 16/17 `__mark_complete__`/`__mark_fixed__`, the `mcp-test` round, and this `write-plan`/`plan-feature`/`plan-bug` round); each round reactively appends another literal row to the hand-maintained `_CYCLE_COMMIT_BUDGET` allow-list AFTER a production false-positive (textbook whack-a-mole). → front-enqueued `/spec-bug` `adhoc-derive-cycle-commit-budget` (commit `1ec139c`, top of claude-config `docs/bugs/queue.json`) for the class «derive the per-sub_skill commit budget from a single source of truth — the orchestrator dispatch-skill registry / per-skill metadata — so a newly added multi-commit sub_skill cannot silently default to 1». Class boundary IN: any dispatched sub_skill keyed in `_CYCLE_COMMIT_BUDGET` whose cycle legitimately commits >1. OUT: the `budget_override` phase-scaling path (Round 20) and the `kind==meta` exemption (Round 19) — both left intact. Cited instances: Rounds 15/16/17 + the mcp-test round + this round; `lazy_core.py:8940` `_CYCLE_COMMIT_BUDGET`; literal rows `write-plan`/`plan-feature`/`plan-bug`. PushNotification sent.

**Gates run:**
  test_lazy_core.py: 767/767
  test_hooks.py: 93/93
  lint-skills.py: OK (projected + capabilities)
  lazy-state.py --test: OK (under LAZY_ORCHESTRATOR=1)
  bug-state.py --test: OK (under LAZY_ORCHESTRATOR=1)
