---
kind: implementation-plan
feature_id: spec-buddy
status: Ready
created: 2026-06-08
phases: [1, 2, 3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-08.
> To execute: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\spec-buddy\plans\all-phases-spec-buddy.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — spec-buddy  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\spec-buddy\PHASES.md` (spec-buddy, 3 phases)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\spec-buddy\SPEC.md`

**Total phases:** 3
**Plan version:** v2 (reference-based — components loaded from disk per step)

---

## FEATURE-SPECIFIC ADAPTATIONS — READ BEFORE ANYTHING ELSE

This is a **markdown skill-authoring** feature in the `claude-config` repo, NOT a compiled-code feature. The standard code-feature machinery is adapted as follows. These adaptations OVERRIDE the generic component instructions wherever they conflict:

1. **No TDD.** Every work unit is `TDD: no`. The deliverables are markdown skill/component files with no executable logic and no test suite. Skip Phase A (test agents) entirely. Do NOT read `tdd-protocol.md` / `tdd-test-agent.md`.
2. **Quality gates = skill lint + projection, not code build/test.** The only gates are:
   - `python ~/.claude/scripts/lint-skills.py` — exits 0, no broken/dangerous `!cat` references.
   - `python ~/.claude/scripts/project-skills.py` — resolves all `!cat` includes with no unresolved or circular includes; spot-check the projected output.
   Wherever a step says "run quality gates," run these two instead. Do NOT look for a code QG suite — there is none for this feature.
3. **No MCP integration test.** Step B.4.5 is N/A — skip with a note.
4. **Repo identity is personal** (`jacobmadsen12321@gmail.com`) — commits AND `git push` are allowed. No work-repo push block applies.
5. **DIRTY-TREE CAUTION (critical).** The `claude-config` tree contains substantial **unrelated** uncommitted WIP at plan-authoring time (e.g. `manifest.psd1`, `user/scripts/*.py`, other `docs/specs/*`, `user/plugins/`). **Do NOT run `git add -A`.** Stage ONLY the spec-buddy files this plan creates/edits:
   - `docs/specs/spec-buddy/**` (SPEC.md, PHASES.md, RESEARCH_SUMMARY.md, plans/, this plan)
   - `user/skills/_components/spec-buddy-checkin-format.md`
   - `user/skills/spec-buddy/SKILL.md`
   Use explicit `git add <path>` for each. Never checkpoint the unrelated WIP.
6. **Subagents author the markdown.** Skill/component `.md` files ARE the source artifacts here — they are authored by Sonnet subagents per the execution model. The orchestrator still does not Write them directly; it dispatches `Agent` calls. (The orchestrator MAY directly edit `PHASES.md` for status updates, as usual.)

---

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run lint/projection gates, update tracking docs | `Agent`, `Read`, `Bash` (gates/git only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL skill/component markdown files | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on the skill/component source files (`user/skills/spec-buddy/SKILL.md`, `user/skills/_components/spec-buddy-checkin-format.md`). If you are about to modify one — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, the work log.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

---

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step. Honor the FEATURE-SPECIFIC ADAPTATIONS above when a component's generic instructions conflict.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.5 | Commit & Push | `~/.claude/skills/_components/commit-and-push.md` |
| Final | Work Log | `~/.claude/skills/_components/work-log.md` |

(TDD, quality-gates, and MCP components are intentionally omitted — see FEATURE-SPECIFIC ADAPTATIONS.)

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies; the SPEC's `**Depends on:**` is `(none)`.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL skill/component authoring MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit`/`Write` on the skill/component files. Exception: trivial PASS-WITH-FIXES items (a few lines).
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. No TDD pipeline for this feature (markdown, no testable logic) — see ADAPTATIONS.
4. PHASES.md is updated AFTER EACH phase completes (not deferred).
5. Every subagent's output is reviewed for correctness and spec alignment before continuing.
6. Mistakes are fixed immediately before launching the next phase.
7. After each phase, run lint + projection gates (the QG equivalent for this feature).
8. Each completed phase is committed (and pushed) before the next begins — staging spec-buddy paths ONLY (never `-A`).
9. This plan is self-contained — follow it exactly without relying on external context.
10. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.

---

## Execution Schedule

| Step | Phase | Title | Blocked by | Parallel? |
|------|-------|-------|------------|-----------|
| 1 | P1 | Check-in format component | — | Solo |
| 2 | P2 | The spec-buddy skill (SKILL.md) | P1 | Solo |
| 3 | P3 | Integration, projection & smoke test | P2 | Solo |

All phases are sequential: P2 `!cat`s the component built in P1; P3 validates the skill built in P2. Each phase is a single work unit touching a single file, so there is no intra-phase parallelism.

---

### Phase: spec-buddy P1 — Check-in format component

**Goal:** Author the shared `_components/spec-buddy-checkin-format.md` defining the confidence-scored per-partition check-in format.
**Entry criteria:** None (first phase).
**SPEC.md references:** § "The check-in format (build-new — the heart of the skill)".

#### Work Units

**WU-1 — Author check-in-format component**
- **Scope:** Deliverables of PHASES.md Phase 1 — create `user/skills/_components/spec-buddy-checkin-format.md` with the six-field check-in shape (Partition, Recommendation [asserted only at high confidence], Evidence [bulleted, each cited `file:line`/symbol/`PHASES.md`], Confidence [high/med/low + reason; low ⇒ propose investigation], Pseudo-code [where code-shaped], Open question(s)) plus the minor-tier condensation rule.
- **TDD:** no.
- **Files to create/modify:** `user/skills/_components/spec-buddy-checkin-format.md` (net-new).
- **Implementation goal:** A self-contained component file that another SKILL.md can `!cat` — no forward references to `spec-buddy/SKILL.md`. Mirror the prose style of sibling components in `~/.claude/skills/_components/` [VERIFY: ls ~/.claude/skills/_components/atomic-thinking.md].
- **Spec requirements:** SPEC § "The check-in format"; tier rule from SPEC § User Experience ("minor-tier partitions condense…").
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | None |

---

### Phase: spec-buddy P2 — The spec-buddy skill (SKILL.md)

**Goal:** Author `user/skills/spec-buddy/SKILL.md` — the full partition-walk orchestration skill, consuming the P1 component.
**Entry criteria:** P1 complete (`spec-buddy-checkin-format.md` exists to `!cat`).
**SPEC.md references:** § User Experience (Phases 0–4), § Technical Design.

#### Work Units

**WU-2 — Author spec-buddy/SKILL.md**
- **Scope:** All deliverables of PHASES.md Phase 2 — create `user/skills/spec-buddy/SKILL.md` covering: frontmatter (`name: spec-buddy`, `description`, `allowed-tools: [Read, Glob, Grep, Write, Edit, Bash, AskUserQuestion, Agent, WebSearch]`); Phase 0 groundwork; Phase 1 tiered partition planning + approval gate; Phase 2 walk loop (recon → check-in → decide → persist → advance); Phase 3 user-only Gemini research; Phase 4 finalize; `buddy-session.json` schema note.
- **TDD:** no.
- **Files to create/modify:** `user/skills/spec-buddy/SKILL.md` (net-new; `spec-buddy/` dir is net-new). **No `manifest.psd1` edit** (directory-level symlink auto-discovers the skill).
- **Implementation goal — reuse via `!cat`, do NOT copy:**
  - Repo-overridable components use the fallback form `` !`cat .claude/skill-config/<name>.md 2>/dev/null || cat ~/.claude/skills/_components/<name>.md` ``: `team-architect-stance` [VERIFY: ls ~/.claude/skills/_components/team-architect-stance.md], `reuse-first-discovery` [VERIFY: ls ~/.claude/skills/_components/reuse-first-discovery.md], `spec-testing-guidance` [VERIFY: ls ~/.claude/skills/_components/spec-testing-guidance.md], `cog-doc-track-open` [VERIFY: ls ~/.claude/skills/_components/cog-doc-track-open.md].
  - Simple form `` !`cat ~/.claude/skills/_components/<name>.md` `` for: `dep-block-schema` [VERIFY: ls ~/.claude/skills/_components/dep-block-schema.md], `atomic-thinking` [VERIFY: ls ~/.claude/skills/_components/atomic-thinking.md], `subagent-launch` [VERIFY: ls ~/.claude/skills/_components/subagent-launch.md], `subagent-partitioning` [VERIFY: ls ~/.claude/skills/_components/subagent-partitioning.md], `decision-resume` [VERIFY: ls ~/.claude/skills/_components/decision-resume.md], `work-log` [VERIFY: ls ~/.claude/skills/_components/work-log.md], and the new `spec-buddy-checkin-format` [VERIFY: ls ~/.claude/skills/_components/spec-buddy-checkin-format.md] (built in P1).
  - **Minimal replication, NOT extraction (do NOT edit `/spec`):** `/spec` is monolithic — its final SPEC.md structure template and Gemini-prompt structure are inline in `user/skills/spec/SKILL.md` [VERIFY: ls ~/.claude/skills/spec/SKILL.md], not components. Per the SPEC's "/spec stays untouched" constraint, replicate those two small blocks inline in spec-buddy's Phase 3/Phase 4 sections rather than refactoring `/spec`. See PHASES.md `## Validated Assumptions` #2.
- **Spec requirements:** SPEC § User Experience (Phase 0–4 behavior), § Technical Design (partition-walk loop, recon subagents, downstream compatibility), and the six brainstorm decisions (separate skill reusing /spec; conversational co-design; reuse partition first; tiered depth; user-only Gemini; audit woven into SPEC.md).
- **Batch:** 1 (single file).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-2 | Solo | None |

---

### Phase: spec-buddy P3 — Integration, projection & smoke test

**Goal:** End-to-end validation; add a usage note; confirm downstream compatibility and that no manifest edit was needed.
**Entry criteria:** P2 complete.
**SPEC.md references:** § Validation Criteria.

#### Work Units

**WU-3 — Integration, smoke test, usage note**
- **Scope:** All deliverables of PHASES.md Phase 3 — repo-wide lint + projection green; smoke-test `/spec-buddy` on a small real feature (groundwork runs; tiered partition list with Reuse Ledger first; per-partition confidence-scored check-ins; incremental SPEC.md writes; `buddy-session.json` written); dry-run the produced SPEC.md through `/spec-phases` (valid `**Depends on:**` block + Validation Criteria table); add a **Usage** note to `SKILL.md`; confirm `git diff manifest.psd1` is empty.
- **TDD:** no.
- **Files to create/modify:** `user/skills/spec-buddy/SKILL.md` (additive Usage note only); a one-line index entry in `user/skills/README.md` ONLY if that file already exists [VERIFY: ls ~/.claude/skills/README.md] — do not create it if absent.
- **Implementation goal:** Prove the skill loads, resolves all includes, and emits a downstream-compatible SPEC.md. Document the smoke-test result inline in the phase's Implementation Notes. The smoke-test SPEC.md output is a throwaway — write it to a temp/scratch feature dir and note it; do not leave a stray spec in `docs/specs/`.
- **Spec requirements:** SPEC § Validation Criteria (all rows).
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-3 | Solo | None |

---

## Execution Protocol

This protocol governs autonomous execution of every phase. Follow it exactly, honoring the FEATURE-SPECIFIC ADAPTATIONS.

### Phase Selection Loop

Repeat until all 3 phases are complete or a blocking issue triggers early exit:

1. **Select ready phase:** P1 → P2 → P3 in order (each depends on the prior).
2. **Announce:** Print "Implementing spec-buddy Phase N: [title]".
3. **Review prior context:** Re-read prior phases' Implementation Notes in PHASES.md (paths/decisions may differ from this plan; they take priority where they diverge).
4. **Execute the batch** per Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "spec-buddy Phase N: [title] — committed as [hash]".
7. **Loop** to the next phase.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE FIRST)

Read `~/.claude/skills/_components/task-tracking.md` and follow it. Create tasks for WU-1, WU-2, WU-3 and the per-phase reviews. (No test-agent tasks — TDD is off for this feature.)

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and this plan file. Do NOT rely on memory.

#### Step B.1: Launch Subagent (COMPOSE Agent CALL — ZERO INLINE AUTHORING)
**PRE-FLIGHT:** Confirm you will use `Agent` with `model: "sonnet"` for the markdown authoring and will NOT call `Edit`/`Write` on the skill/component files yourself.

Read before proceeding:
1. `~/.claude/skills/_components/subagent-launch.md` — launch orchestration + failed-agent recovery. **Skip its Phase A (test agents)** — TDD is off here.
2. `~/.claude/skills/_components/implementation-agent.md` — impl-agent briefing; include it verbatim in the agent prompt.

Each agent prompt MUST include: the relevant SPEC.md sections (quoted — the subagent has zero context), the exact PHASES.md deliverables for the phase, the exact file path to create/modify, the reuse/`!cat` directives from the work unit above (with the exact include syntax), the "do not edit /spec / do not edit manifest.psd1" constraints, and the commit-ownership clause (subagent does NOT commit/stage). For WU-1, also have the agent skim 1–2 sibling `_components/*.md` for house style. For WU-2, have the agent read the new `spec-buddy-checkin-format.md`, the existing `user/skills/spec/SKILL.md` (for structure + the two blocks to minimally replicate), and `plan-feature/SKILL.md` (for the `Skill({...})` delegation idiom).

**POST-DISPATCH GATE:** Verify you composed `Agent` calls and did not edit the skill/component files directly.

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md` and `~/.claude/skills/_components/mount-site-verification.md`; follow the protocol (ground-truth verification, structured report, verdict). Adapt: there is no code test suite — "correctness" means the markdown is well-formed, frontmatter valid, every `!cat` path resolves, the two replicated /spec blocks match current /spec, and the behavior matches the SPEC's six decisions. For new-file mount-site verification, the "mount site" is skill discovery: confirm the new `spec-buddy/` dir sits under `user/skills/` so the directory symlink picks it up, and that no `manifest.psd1` entry was added.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `~/.claude/skills/_components/phases-update.md`. Check off completed deliverables; add an Implementation Notes block (date, what was built, integration notes, pitfalls, files modified).

#### Step B.4: Run Gates (lint + projection — the QG equivalent)
Run, from the claude-config repo root:
- `python ~/.claude/scripts/lint-skills.py` — must exit 0 (no broken/dangerous `!cat`).
- `python ~/.claude/scripts/project-skills.py` — must resolve all includes; spot-check the projected `spec-buddy` output has no unresolved or circular includes.
100% pass required before proceeding. (No code QG suite exists for this feature.)

#### Step B.4.5: MCP Integration Test — N/A
Skip with a note: this feature produces no runtime-observable code behavior.

#### Step B.5: Commit Batch
Read `~/.claude/skills/_components/commit-and-push.md` and follow it, with these overrides:
- **Stage spec-buddy paths ONLY** (explicit `git add` per path) — NEVER `git add -A`; the tree has unrelated WIP.
- Identity is personal → `git push` is allowed after commit.

#### Step B.6: Proceed to Next Phase
Checklist (all true): review report with verdict produced; PHASES.md updated; lint + projection green; commit (+push) done staging only spec-buddy paths. If any unchecked, go back.

### Post-Phase Steps (after the phase's batch)

#### Integration Verification (adapted)
For P2/P3: confirm the skill resolves end-to-end via `project-skills.py` and (P3) that a real `/spec-buddy` run produces a `/spec-phases`-acceptable SPEC.md. No code integration suite applies.

#### Update CLAUDE.md / index (if warranted)
Only if a skills index/README exists and warrants a one-line entry (P3). Do not create new index files.

#### Commit and Push Post-Phase Changes
Per Step B.5 policy (spec-buddy paths only; push allowed).

---

## Blocking Issue Protocol

If a blocking issue is encountered:
1. Stop all in-progress work; launch no new subagents.
2. Commit + push any completed phases (spec-buddy paths only).
3. Print a blocking-issue report:

   ## Implementation Batch — Blocked
   **Completed phases:** [list with commit hashes]
   **Blocked phase:** spec-buddy Phase N: [title]
   **Reason:** [specific]
   **Recovery suggestion:** [what the user should do]
   **Remaining phases (not attempted):** [list]

4. Do not work around the blocker.

Blocking issues include: a subagent failure unfixable after 2 retries; a lint/projection failure unfixable after 2 retries; a circular `!cat` include that can't be resolved without a design change; any error needing architectural decisions beyond the SPEC.

---

## Completion

When all 3 phases are complete:
1. **Run lint + projection one final time** across the repo — both green.
2. **Print a completion report:**

   ## Implementation Batch — Complete
   **Feature implemented:** spec-buddy
   **Total phases completed:** 3
   **Total commits:** [N]
   **Final gate status:** lint + projection green
   **Commit log:** [table: commit | phase | title]
   **Implementation Notes summary:** [key decisions/pitfalls — e.g. the minimal-replication seam vs /spec]

---

## Append to Work Log (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/work-log.md` and follow it. Call `interview_work_log_append` with skill (`write-plan`/`implement-phase-batch` as appropriate), project (`claude-config`), title, summary, files_modified, and technical_context (note the markdown-skill-authoring nature, the minimal-replication seam against monolithic /spec, and the auto-discovery-no-manifest-edit fact).
