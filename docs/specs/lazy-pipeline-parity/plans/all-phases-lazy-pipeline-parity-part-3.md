---
kind: implementation-plan
feature_id: lazy-pipeline-parity
status: Ready
created: 2026-06-15
complexity: complex
phases: [3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-15.
> To execute: `/execute-plan {this-file-path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 3 of 4. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\plans\all-phases-lazy-pipeline-parity-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\plans\all-phases-lazy-pipeline-parity-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\plans\all-phases-lazy-pipeline-parity-part-3.md` (this file)
> - part 4: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\plans\all-phases-lazy-pipeline-parity-part-4.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — lazy-pipeline-parity (Part 3 of 4)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\PHASES.md` (lazy-pipeline-parity, 4 phases — this part covers Phase 3)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\SPEC.md`

**Total phases (this part):** 1 (Phase 3)
**Plan version:** v2 (reference-based — components loaded from disk per step)
**Complexity:** complex — four full per-pair Tier-1/Tier-2 audits, each requiring gap-vs-divergence judgment, regex/heading authoring, and (for genuine leaks) prose fixes in derived skills. The two full-restatement pairs are the highest drift risk.

---

## ⚠ REPO CONTEXT — READ BEFORE ANYTHING ELSE (this is NOT AlgoBooth)

This feature lives in the **`claude-config`** repo, not AlgoBooth.

- **Repo root (cwd):** `C:\Users\Jacob\source\repos\claude-config`. All relative paths resolve here. Note the two subtrees: `user/skills/` (bug-axis twins + canonical roots) and `repos/algobooth/.claude/skills/` (cloud-axis twins) — the audit reads both.
- **Git:** branch `main`. Email `66210812+jacobrocks1212@users.noreply.github.com` → **push eligible**.

**Quality gates for THIS repo (override `quality-gates.md`):** no `npm run qg`. Gates:
1. `pytest user/scripts/test_lazy_parity.py -q` — all tests green (fixtures + ALL FIVE live pairs after this part).
2. `python3 user/scripts/lazy_parity_audit.py --repo-root .` — exits 0 (zero drift across all five pairs).
3. `python3 user/scripts/lint-skills.py --check-projected` — exits 0 (any in-place SKILL.md gap-fixes must not introduce projection regressions).

**MCP runtime: not-required.** **Skip Step B.4.5 entirely.**

**Commit policy:** no `.claude/skill-config/commit-policy.md`. Use `~/.claude/skills/_components/commit-and-push.md`. Push after the phase.

---

## EXECUTION MODEL — READ THIS FIRST

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL edits (`.json` manifest, `SKILL.md` gap-fixes, `test_lazy_parity.py`) | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** Orchestrator MUST NOT `Edit`/`Write` `.json`/`.py`/`SKILL.md`. ONLY direct edits: `PHASES.md`, `CLAUDE.md`. (Exception: trivial PASS-WITH-FIXES.)

**Dispatch pattern:** `Agent({ description, model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL
References component files by path. **Before each step**, `Read` the listed components from disk — not from memory. After compaction, re-read this plan first.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` (⚠ OVERRIDDEN — pytest + audit CLI + `--check-projected`, NOT npm) |
| Step B.4.5 | MCP Integration Test | SKIP — `MCP runtime: not-required` |
| Step B.5 | Commit Policy | `~/.claude/skills/_components/commit-and-push.md` |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts
(none — SPEC `**Depends on:** (none)`. The contract is SPEC §Technical Design §1–§2 + the family-wide spot-check findings table; re-read those.)

## MANDATORY RULES — DO NOT SKIP ANY STEP
1. ALL edits delegated to Sonnet subagents (except trivial PASS-WITH-FIXES).
2. Subagent edits in the current worktree — NEVER create worktrees.
3. TDD WUs go through test-first (this part extends an existing test file — the test agent adds the live assertions; for genuine SKILL.md gap-fixes, the assertion failing → fix → assertion green IS the test loop).
4. PHASES.md updated AFTER each batch.
5. Every subagent output reviewed before continuing.
6. Mistakes fixed immediately before the next batch.
7. Integration verification after all batches.
8. CLAUDE.md reviewed after the phase.
9. Phase committed and pushed before the part ends.
10. Self-contained — no reliance on sibling parts.
11. **Before each step, `Read` the listed components.**
12. **Gates = pytest + audit CLI + `--check-projected` — NOT npm.** Skip MCP.
13. **Gap-vs-divergence discipline (the core judgment of this phase):** a canonical mechanic/heading with NO coverage in the derived skill that is **unambiguously a leak** (should be there) → FIX the derived SKILL.md AND record `coverage: "restated"`/`"inherited"`. A canonical unit that **legitimately should not exist** for this twin's axis → record a `divergence` with a stated `reason` (do NOT add it to the derived skill). When ambiguous, record a divergence with an explicit reason — never silently drop. **Do NOT add `dev:kill` to `lazy-batch-cloud`** — its absence is the canonical legitimate divergence (cloud never boots the runtime).

---

## Touchpoint Summary
**Audit skipped — documented.** No `audit:touchpoints` gate in claude-config. Touchpoints: `user/scripts/lazy-parity-manifest.json` (grown with 4 pair sections — JSON data), `user/scripts/test_lazy_parity.py` (4 new assertions appended), and **possible** in-place gap-fixes to `user/skills/lazy-bug/SKILL.md` (335 ln), `user/skills/lazy-bug-status/SKILL.md` (159 ln), `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` (903 ln), `repos/algobooth/.claude/skills/lazy-cloud/SKILL.md` (274 ln) — all skill markdown, not AlgoBooth TS/Rust source. No `block` verdict possible; no Phase 0.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | lazy-pipeline-parity | P3 | Extend the Registry to the Remaining Four Pairs | Part 2 (engine must exist + be importable) | Solo (4 sequential batches — all WUs share `manifest.json` + `test_lazy_parity.py`) |

**Entry precondition (verify before starting):** `user/scripts/lazy_parity_audit.py` exists and is importable, and `pytest user/scripts/test_lazy_parity.py -q` is green for the `lazy-bug-batch` pair. If not, Part 2 was not completed — STOP (Blocking Issue Protocol).

> **Why 4 sequential batches (file-overlap rule):** every WU edits the SAME two files — `lazy-parity-manifest.json` (its pair section) and `test_lazy_parity.py` (its live assertion). No two WUs may touch the same file in one batch, so each pair is its own sequential batch (a review gate between each). Each pair's gap-fixes touch a DIFFERENT derived `SKILL.md`, so those do not conflict — but the shared manifest + test file force sequencing regardless. Order: cloud pair first (lowest risk, establishes the `mechanic_override` reference pattern), then the two restatement pairs (highest risk), then the cloud thin-wrapper.

---

## Work Units

- [ ] WU-1 — `lazy-batch`→`lazy-batch-cloud` registry section (inherit-by-reference; `dev:kill` mechanic_override divergence) + live assertion
- [ ] WU-2 — `lazy`→`lazy-bug` registry section (full restatement — highest drift risk) + gap-fixes + live assertion
- [ ] WU-3 — `lazy`→`lazy-cloud` registry section (thin wrapper, pointer-resolution audit) + live assertion
- [ ] WU-4 — `lazy-status`→`lazy-bug-status` registry section (full restatement) + live assertion

---

### Phase: lazy-pipeline-parity P3 — Extend the Registry to the Remaining Four Pairs

**Goal:** Run a full Tier-1/Tier-2 audit for each of the four remaining pairs, author its registry section, fix any genuine gaps in the derived skill, record intentional divergences (including the known `dev:kill`-in-cloud divergence), and extend `test_lazy_parity.py` with a live zero-drift assertion per pair so the suite covers all five.
**Entry criteria:** Part 2 (P2) complete — importable engine + populated manifest + green `lazy-bug-batch` live assertion.
**SPEC.md references:** §"Family-wide spot-checks" table; §Technical Design §1 (`mechanic_overrides`, per-pair coverage), §2 (C1–C6 — C2 pointer-resolution dominates inherit-by-reference pairs; C1 heading-parity + C3 mechanics dominate restatement pairs); §Validation Criteria rows 5 (per-pair override) & 8 (whole family in sync).

> **Common per-WU shape** (applies to all four): (1) read the canonical + derived SKILL.md; (2) enumerate canonical `## Step`/`### sub-step` headings; (3) for each, decide `restated`/`inherited`/`divergence` and record it in the pair's `headings[]`, with `reason` for divergences; (4) run the pair's Tier-2 `mechanic_set` predicates — for any genuine leak, FIX the derived SKILL.md and record `restated`/`inherited`; for a legitimate axis gap, add a `mechanic_override` with `coverage:"divergence"` + `reason`; (5) add `audit_pair(repo_root, pair_name="<name>")` zero-drift assertion to `test_lazy_parity.py`; (6) confirm `audit_pair` returns zero drift for that pair.

#### WU-1 — `lazy-batch`→`lazy-batch-cloud` registry section

- **Scope (PHASES Phase 3 deliverable 1):** full `headings[]` audit of the cloud batch twin (`repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` [VERIFY: file exists — confirmed 903 ln]). Record the **`run-end-dev-kill` mechanic as a legitimate divergence** via `mechanic_overrides` with `reason: "Cloud defers Tauri/MCP and never boots the orchestrator-owned runtime, so there is nothing to tear down at run-end."` — the canonical per-pair gap-vs-divergence example. Confirm all OTHER `lazy-batch` Tier-2 mechanics resolve (spot-check found `cycle_prompt_ref` synced 8/8; confirm `dispatch_prompt_ref`, `orchestrator-voice`, `completeness-policy`, `operator-authorized`, `completion-integrity`). Record cloud-axis steps (`DEFERRED_NON_CLOUD`, cloud terminals `cloud-queue-exhausted`/`device-queue-exhausted`) as derived-only divergences. **Do NOT add `dev:kill` to this file.**
- **TDD:** the live assertion is the test. The `mechanic_override` suppressing C3 for `run-end-dev-kill` is exactly SPEC Validation row 5 — confirm C3 passes for this pair despite `dev:kill` being absent, while still required for `lazy-bug-batch`.
- **Files to modify:** `user/scripts/lazy-parity-manifest.json` (fill the `lazy-batch-cloud` stub), `user/scripts/test_lazy_parity.py` (add the live assertion). Any genuine gap → `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md`.
- **Implementation goal:** `audit_pair(repo_root, "lazy-batch-cloud")` → zero drift; the `dev:kill` override demonstrably suppresses C3 for this pair only.
- **Spec requirements:** SPEC §Technical Design §1 (the `lazy-batch-cloud` pair illustration with the exact `dev:kill` override reason); §Validation row 5.
- **Batch:** 1.

#### WU-2 — `lazy`→`lazy-bug` registry section (highest drift risk)

- **Scope (PHASES Phase 3 deliverable 2):** full Tier-1 heading-parity + Tier-2 mechanic audit of `user/skills/lazy-bug/SKILL.md` [VERIFY: file exists — confirmed 335 ln] against canonical `user/skills/lazy/SKILL.md` [VERIFY: confirmed 281 ln]. **Full restatement — prose duplicated with no pointers** → highest drift risk. Audit every `## Step`/`### sub-step` heading in `lazy/SKILL.md` (spot-check found near-identical structure: Sentinel Format, Step 0.0/0/0.3/1/2a/2b/3/4/5, Refs, State-Machine Summary; two-gate `__mark_fixed__` logic). Apply `mechanic_set: "lazy"` predicates (`completion-integrity`, `exactly ONE sub-skill`, `lazy-preflight\.md`, `completeness-policy\.md`). Apply `token_substitutions` (feature→bug vocab, `lazy-state.py`→`bug-state.py`, `COMPLETED.md`→`FIXED.md`, `__mark_complete__`→`__mark_fixed__`). Record bug-axis divergences (skips Gemini/research). For each genuine gap found, FIX `lazy-bug/SKILL.md` and record `restated`.
- **TDD:** the `lazy-bug` live assertion is the test; any gap-fix is proven by the assertion going from red→green.
- **Files to modify:** `lazy-parity-manifest.json` (fill `lazy-bug` stub), `test_lazy_parity.py` (live assertion). Gaps → `user/skills/lazy-bug/SKILL.md`.
- **Implementation goal:** `audit_pair(repo_root, "lazy-bug")` → zero drift; every canonical `lazy` heading maps to a restated/inherited/divergence entry whose `heading:` text matches the **current** canonical text exactly (avoid C4 false-positives from minor rewords).
- **Spec requirements:** SPEC §"Family-wide spot-checks" (`lazy`→`lazy-bug` row); §Technical Design §2 (restatement → C1+C3 emphasis); PHASES Testing Strategy (restatement heading-match noise check).
- **Batch:** 2.

#### WU-3 — `lazy`→`lazy-cloud` registry section (thin wrapper)

- **Scope (PHASES Phase 3 deliverable 3):** full pointer-resolution audit (C2) + Tier-2 mechanic predicates for `repos/algobooth/.claude/skills/lazy-cloud/SKILL.md` [VERIFY: file exists — confirmed 274 ln] (~44 `/lazy` pointers). Record cloud-axis steps (`__write_deferred_non_cloud__`, deferral bookends) as derived-only divergences. `mechanic_set: "lazy"`. Close any genuine pointer-resolution gaps (an `inherited` "see `/lazy` Step N" whose target evidence no longer resolves).
- **TDD:** the `lazy-cloud` live assertion is the test (C2 is the primary signal for this inherit-by-reference pair).
- **Files to modify:** `lazy-parity-manifest.json` (fill `lazy-cloud` stub), `test_lazy_parity.py` (live assertion). Gaps → `repos/algobooth/.claude/skills/lazy-cloud/SKILL.md`.
- **Implementation goal:** `audit_pair(repo_root, "lazy-cloud")` → zero drift; every `inherited` pointer resolves (C2).
- **Spec requirements:** SPEC §"Family-wide spot-checks" (`lazy`→`lazy-cloud` row); §Technical Design §2 (inherit-by-reference → C2 emphasis).
- **Batch:** 3.

#### WU-4 — `lazy-status`→`lazy-bug-status` registry section

- **Scope (PHASES Phase 3 deliverable 4):** full Tier-1 heading-parity (spot-check: 7/7 headings) + Tier-2 mechanic audit of `user/skills/lazy-bug-status/SKILL.md` [VERIFY: confirmed 159 ln] against canonical `user/skills/lazy-status/SKILL.md` [VERIFY: confirmed 130 ln]. Full restatement; lower stakes (read-only dashboards) but in scope. `mechanic_set: "lazy-status"` (`NO mutations|read-only`, `state\.py`). Record bug-axis divergences (`bug-state.py` vs `lazy-state.py`, bug-facing terminology). Apply token substitutions. Fix any genuine gap in `lazy-bug-status/SKILL.md`.
- **TDD:** the `lazy-bug-status` live assertion is the test.
- **Files to modify:** `lazy-parity-manifest.json` (fill `lazy-bug-status` stub), `test_lazy_parity.py` (live assertion). Gaps → `user/skills/lazy-bug-status/SKILL.md`.
- **Implementation goal:** `audit_pair(repo_root, "lazy-bug-status")` → zero drift.
- **Spec requirements:** SPEC §"Family-wide spot-checks" (`lazy-status`→`lazy-bug-status` row).
- **Batch:** 4.

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-1      | Solo | shares manifest+test file with all others → sequential |
> | 2     | WU-2      | Solo | same |
> | 3     | WU-3      | Solo | same |
> | 4     | WU-4      | Solo | same |

**Phase-end Verification (orchestrator confirms in final Step B.4):**
- `pytest user/scripts/test_lazy_parity.py -q` — green across fixtures + all FIVE live pairs.
- `python3 user/scripts/lazy_parity_audit.py --repo-root .` — exits 0 (zero drift, all five pairs).
- `python3 user/scripts/lint-skills.py --check-projected` — exits 0.

---

## Execution Protocol

### Phase Selection Loop
One phase (P3), four sequential batches (WU-1 → WU-2 → WU-3 → WU-4). After each batch: review, update PHASES.md, run the per-pair gate (`audit_pair` for that pair exits clean + its assertion green), commit. After all four: post-phase steps, commit+push, proceed to part 4.

1. **Verify entry precondition** (engine importable; `lazy-bug-batch` assertion green). If unmet, STOP.
2. **Announce:** "Implementing lazy-pipeline-parity Phase 3: Extend the Registry to the Remaining Four Pairs".
3. **Review prior context:** Re-read PHASES.md Phase 2 Implementation Notes (the manifest structure + engine surface) + Phase 3; re-read SPEC §"Family-wide spot-checks" + §Technical Design.
4. **Execute Batches 1–4** per Per-Batch Steps.
5. **Run Post-Phase Steps.**
6. **Report:** "lazy-pipeline-parity Phase 3 — committed as [hashes]".

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE FIRST)
Read `~/.claude/skills/_components/task-tracking.md`. Create tasks for WU-1..WU-4; update status as you progress.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read PHASES.md (Phase 3 + prior notes), SPEC §"Family-wide spot-checks" + §Technical Design, and this plan. For the current WU, `Read` BOTH the canonical and derived SKILL.md in full so the subagent prompt can quote exact heading text and mechanic locations.

#### Step B.1: Launch Subagent (COMPOSE Agent TOOL CALL)
**PRE-FLIGHT CHECK:** `Agent` + `model: "sonnet"`; no direct orchestrator edits.
Read `~/.claude/skills/_components/tdd-protocol.md`, `subagent-launch.md`, `implementation-agent.md`. Each WU is one Sonnet impl agent (the test extension is appended by the same agent — it owns both `manifest.json` and `test_lazy_parity.py` for its pair, so no intra-batch file conflict). The agent prompt MUST include: repo-context block; the gap-vs-divergence discipline (rule 13); the pair's canonical+derived paths; the `mechanic_set` + token_substitutions for the pair; the explicit instruction to enumerate canonical headings and classify each; the requirement to FIX genuine leaks in the derived SKILL.md (and record restated/inherited) vs record divergences with reasons; and to add the `audit_pair(..., "<name>")` zero-drift assertion and confirm it green. Include `implementation-agent.md` verbatim. For WU-1 specifically, hand it the exact `dev:kill` override reason string.

**POST-DISPATCH GATE:** Confirm Agent dispatch; no direct edits.

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md` + `mount-site-verification.md`. Per pair, verify: the manifest section is complete and parseable; every canonical heading has an entry; divergences carry reasons (C5); genuine leaks were FIXED in the derived skill (not papered over as divergences); the live assertion is real (`assert len(...) == 0`, drives the actual repo). **Watch for the gap-vs-divergence failure mode**: a real leak recorded as a "divergence" to force green is a defect — the reason must be a genuine axis justification, not "this was missing." For WU-1, confirm `dev:kill` was NOT added to `lazy-batch-cloud`.

#### Step B.3: Update PHASES.md (MANDATORY — orchestrator edits directly)
Read `~/.claude/skills/_components/phases-update.md`. After each batch, check off that pair's Phase 3 deliverable; add Implementation Notes (date, the pair, heading count, which mechanics resolved, any genuine gaps fixed + where, divergences recorded + reasons).

#### Step B.4: Run Quality Gates (MANDATORY — repo-specific, NOT npm)
Per batch: `python3 user/scripts/lazy_parity_audit.py --repo-root . --pair <this-pair>` (exit 0) + `pytest user/scripts/test_lazy_parity.py -q` (green) + (if the derived SKILL.md was edited) `python3 user/scripts/lint-skills.py --check-projected` (exit 0). Final batch additionally: `python3 user/scripts/lazy_parity_audit.py --repo-root .` (all five pairs, exit 0) + `pytest user/scripts/ -q` (no regressions). (Ignore npm in `quality-gates.md`.)

#### Step B.4.5: MCP Integration Test — SKIP (`MCP runtime: not-required`).

#### Step B.5: Commit Batch
Read `~/.claude/skills/_components/commit-and-push.md`. Commit each pair's manifest section + test assertion + any SKILL.md gap-fix + PHASES.md update together.

#### Step B.6: Proceed to Next Batch
Checklist (all true): review verdict; PHASES.md updated; per-pair audit exit 0 + assertion green; committed. Then start the next pair.

### Post-Phase Steps

#### Integration Verification (MANDATORY)
Read `~/.claude/skills/_components/integration-verification.md`. Confirm the full five-pair manifest is internally consistent and the whole suite is green (SPEC Validation row 8 — "whole family in sync"). Confirm every `divergence` that has a corresponding human-facing "Differences" table entry carries a `doc_anchor` field — **Part 4's C6 cross-linking reads these**, so populate them now where the derived skill already has a Differences table.

#### Update CLAUDE.md Files (MANDATORY)
Read `~/.claude/skills/_components/claude-md-review.md`. Record decision (likely none beyond a possible scripts-dir note).

#### Commit and Push Post-Phase Changes
Read `~/.claude/skills/_components/commit-and-push.md`. Commit post-phase docs; `git push origin main`.

---

## Blocking Issue Protocol
If blocked:
1. Stop in-progress work; launch no new subagents.
2. Commit and push completed batches (per-pair progress is preserved).
3. Print a blocking-issue report (Completed pairs + hashes; Blocked WU/pair; Reason; Recovery; Remaining pairs).
4. Do not work around — the user resolves.

Blocking issues include: a genuine gap whose gap-vs-divergence classification is truly ambiguous and design-bearing (record it as a divergence with an explicit "AMBIGUOUS — provisional" reason and surface for confirmation rather than guessing); a restatement pair where C1/C4 noise is so high that the canonical and derived have genuinely diverged in structure (a real finding — surface it, do not force-match headings); a subagent or gate failure unfixable after 2 retries; a push conflict unresolvable by rebase.

## Completion
When Phase 3 is complete:
1. Run the full gate set once more (five-pair audit exit 0; `pytest user/scripts/ -q` green; `--check-projected` exit 0).
2. Print:

   ## Implementation Batch (Part 3 of 4) — Complete
   **Phase completed:** lazy-pipeline-parity P3 — Extend the Registry to the Remaining Four Pairs
   **Commits:** [hashes]
   **Gate status:** five-pair audit exit 0; suite green; `--check-projected` green.
   **Pairs:** all five now carry full `headings[]` + live zero-drift assertions; `dev:kill`-in-cloud recorded as `mechanic_override` divergence.

3. **Proceed to part 4:** `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\plans\all-phases-lazy-pipeline-parity-part-4.md`

   > Part 4 cross-links the `doc_anchor` fields to the derived skills' "Differences" prose (C6 → zero warnings), adds the optional `--check-parity` lint flag + authoring notes, and the `lazy-batch-retro` registry-awareness note.
