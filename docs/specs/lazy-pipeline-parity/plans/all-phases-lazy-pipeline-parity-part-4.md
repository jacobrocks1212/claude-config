---
kind: implementation-plan
feature_id: lazy-pipeline-parity
status: Complete
created: 2026-06-15
complexity: mechanical
phases: [4]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-15.
> To execute: `/execute-plan {this-file-path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 4 of 4. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\plans\all-phases-lazy-pipeline-parity-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\plans\all-phases-lazy-pipeline-parity-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\plans\all-phases-lazy-pipeline-parity-part-3.md`
> - part 4: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\plans\all-phases-lazy-pipeline-parity-part-4.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — lazy-pipeline-parity (Part 4 of 4)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\PHASES.md` (lazy-pipeline-parity, 4 phases — this part covers Phase 4, the terminal phase)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\SPEC.md`

**Total phases (this part):** 1 (Phase 4)
**Plan version:** v2 (reference-based — components loaded from disk per step)
**Complexity:** mechanical — prose reconciliation (C6 doc-anchors + authoring notes across the five derived skills' "Differences" sections), one CLI flag that mirrors the already-shipped `--check-projected`/`--check-capabilities` pattern (fully specified by SPEC as "analogous to"), and one consumer-note addition. No novel design, algorithm, or cross-boundary seam — see the per-WU "why mechanical" notes.

---

## ⚠ REPO CONTEXT — READ BEFORE ANYTHING ELSE (this is NOT AlgoBooth)

This feature lives in the **`claude-config`** repo, not AlgoBooth.

- **Repo root (cwd):** `C:\Users\Jacob\source\repos\claude-config`. All relative paths resolve here.
- **Git:** branch `main`. Email `66210812+jacobrocks1212@users.noreply.github.com` → **push eligible**.

**Quality gates for THIS repo (override `quality-gates.md`):** no `npm run qg`. Gates:
1. `pytest user/scripts/` — full suite green (all prior-phase tests still pass; no regressions).
2. `python3 user/scripts/lazy_parity_audit.py --repo-root .` — exits 0 with **zero C6 warnings** (the primary signal for this phase; C6 warns to stderr but the goal is zero).
3. `python3 user/scripts/lint-skills.py --check-projected --check-capabilities` — exits 0.
4. If the `--check-parity` flag is implemented (WU-2): `python3 user/scripts/lint-skills.py --check-parity` — exits 0.

**MCP runtime: not-required.** **Skip Step B.4.5 entirely.**

**Commit policy:** no `.claude/skill-config/commit-policy.md`. Use `~/.claude/skills/_components/commit-and-push.md`. Push after the phase.

**⚠ Corrected path — `lazy-batch-retro`:** PHASES.md Phase 4 lists `user/skills/lazy-batch-retro/SKILL.md`, but that file does NOT exist there. The actual location (verified) is **`repos/algobooth/.claude/skills/lazy-batch-retro/SKILL.md`**. Use the corrected path in WU-3.

---

## EXECUTION MODEL — READ THIS FIRST

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL edits (`SKILL.md` prose, `lint-skills.py`, `manifest.json` doc_anchors) | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** Orchestrator MUST NOT `Edit`/`Write` `.py`/`.json`/`SKILL.md`. ONLY direct edits: `PHASES.md`, `CLAUDE.md`. (Exception: trivial PASS-WITH-FIXES.)

**Dispatch pattern:** `Agent({ description, model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL
References component files by path. **Before each step**, `Read` the listed components from disk. After compaction, re-read this plan first.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` (⚠ OVERRIDDEN — pytest + audit CLI zero-C6 + `--check-projected --check-capabilities`, NOT npm) |
| Step B.4.5 | MCP Integration Test | SKIP — `MCP runtime: not-required` |
| Step B.5 | Commit Policy | `~/.claude/skills/_components/commit-and-push.md` |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts
(none — SPEC `**Depends on:** (none)`. The completed five-pair manifest from Part 3 is the input surface; re-read it + SPEC §Technical Design §2 C6/§3 for the `--check-parity` flag.)

## MANDATORY RULES — DO NOT SKIP ANY STEP
1. ALL edits delegated to Sonnet subagents (except trivial PASS-WITH-FIXES).
2. Subagent edits in the current worktree — NEVER create worktrees.
3. TDD WUs go through test-first (this part is doc/flag work; WU-2's flag, if a test is warranted, follows the existing `test_lint_skills.py` pattern — see WU-2).
4. PHASES.md updated AFTER the batch.
5. Every subagent output reviewed before continuing.
6. Mistakes fixed immediately.
7. Integration verification after all batches.
8. CLAUDE.md reviewed after the phase.
9. Phase committed and pushed before the part ends.
10. Self-contained — no reliance on sibling parts.
11. **Before each step, `Read` the listed components.**
12. **Gates = pytest + audit CLI (zero C6) + `--check-projected --check-capabilities` — NOT npm.** Skip MCP.

---

## Touchpoint Summary
**Audit skipped — documented.** No `audit:touchpoints` gate in claude-config. Touchpoints: the five derived `SKILL.md` files (authoring notes + any `doc_anchor` prose), `user/scripts/lazy-parity-manifest.json` (doc_anchor value tweaks), `user/scripts/lint-skills.py` (~260 ln — one flag, mirrors existing flags), `repos/algobooth/.claude/skills/lazy-batch-retro/SKILL.md` (one note). All skill markdown + one small CLI script, not AlgoBooth TS/Rust source. No `block` verdict possible; no Phase 0.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | lazy-pipeline-parity | P4 | Doc Reconciliation + Hardening | Part 3 (full five-pair manifest must exist) | Batch 1: WU-1, WU-2, WU-3 in parallel (no shared files) |

**Entry precondition (verify before starting):** `python3 user/scripts/lazy_parity_audit.py --repo-root .` runs and covers all five pairs (Part 3 complete). If the four-pair sections are absent/stub, Part 3 was not completed — STOP (Blocking Issue Protocol).

> **All three WUs are file-disjoint → one parallel batch.** WU-1 edits the five derived `SKILL.md` + manifest `doc_anchor` values; WU-2 edits `lint-skills.py`; WU-3 edits `lazy-batch-retro/SKILL.md`. No file is touched by two WUs, so they run concurrently in Batch 1.

---

## Work Units

- [x] WU-1 — C6 doc-anchor cross-linking + authoring notes across the five derived skills
- [x] WU-2 — `lint-skills.py --check-parity` flag (optional — mirror existing `--check-*` flags)
- [x] WU-3 — `lazy-batch-retro` registry-awareness note

---

### Phase: lazy-pipeline-parity P4 — Doc Reconciliation + Hardening

**Goal:** Reconcile each derived skill's prose "Differences" table with the manifest so `doc_anchor` fields resolve and C6 emits zero warnings; add an optional `--check-parity` lint flag for standalone human runs; add authoring notes pointing future editors at the parity gate; and add a `lazy-batch-retro` registry-awareness note. Confirm the gate passes clean family-wide.
**Entry criteria:** Part 3 (P3) complete — full five-pair manifest with `headings[]` + divergences + `doc_anchor` fields.
**SPEC.md references:** §Technical Design §2 (C6 soft check), §3 (optional `--check-parity` flag); §"User Experience" ("Reading the contract" — Differences table is the human view, audit warns soft if it disagrees); §Validation row "C6 doc-table consistency".

#### WU-1 — C6 doc-anchor cross-linking + authoring notes (five derived skills)

- **Scope (PHASES Phase 4 deliverables 1 & 3 — combined because both edit the SAME five derived `SKILL.md` files; splitting would only force two sequential single-file-group batches):**
  - **C6 doc-anchor cross-linking:** for every `divergence` entry in the manifest that has a `doc_anchor` field, verify the anchor text is present in that derived skill's "Differences" section. Where missing/stale, either add the referenced divergence description to the derived skill's prose OR update the `doc_anchor` value in the manifest to match the current prose. Goal: C6 emits **zero** warnings.
  - **Authoring notes:** add a short note to each derived skill's intro/header or "Differences" section: *"Before editing this skill, run `python3 user/scripts/lazy_parity_audit.py --repo-root . --pair <name>` to confirm parity is clean, and run the suite after to confirm your change doesn't introduce drift. The source of truth for intentional divergences is `user/scripts/lazy-parity-manifest.json`."*
  - The five derived skills: `user/skills/lazy-bug-batch/SKILL.md`, `user/skills/lazy-bug/SKILL.md`, `user/skills/lazy-bug-status/SKILL.md`, `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md`, `repos/algobooth/.claude/skills/lazy-cloud/SKILL.md`. [VERIFY: all five exist — confirmed]
- **Why mechanical:** pure prose reconciliation — making anchor text match existing prose (or vice-versa) and inserting a fixed boilerplate note. No logic, no design decision.
- **TDD:** no — doc edits. Verification is C6-zero-warnings from the audit CLI.
- **Files to modify:** the five derived `SKILL.md` files; `user/scripts/lazy-parity-manifest.json` (only `doc_anchor` value adjustments, if needed).
- **Implementation goal:** `python3 user/scripts/lazy_parity_audit.py --repo-root .` exits 0 with **zero C6 warnings on stderr**; each derived skill carries the authoring note.
- **Spec requirements:** SPEC §Technical Design §2 (C6); §"User Experience" (docs never silently rot); §Validation row "C6 doc-table consistency".
- **Batch:** 1.

#### WU-2 — `lint-skills.py --check-parity` flag (optional)

- **Scope (PHASES Phase 4 deliverable 2):** add a CLI flag to `user/scripts/lint-skills.py` **analogous to** the existing `--check-projected` (line ~240) and `--check-capabilities` (line ~249) [VERIFY: `grep -n -- "--check-projected\|--check-capabilities\|add_argument" user/scripts/lint-skills.py`] that invokes `lazy_parity_audit.audit_all_pairs(repo_root)` across all five pairs and reports findings; non-zero findings cause `lint-skills.py` to exit non-zero. **Decision at implementation:** if this is determined too noisy or duplicative given `test_lazy_parity.py` already enforces the hard gate, DOCUMENT the decision (in the PHASES Implementation Notes + a one-line code comment) and SKIP the flag — the pytest gate is sufficient. Tick the deliverable either way (implemented OR documented-skip).
- **Why mechanical:** the flag mirrors a fully-specified, already-shipped pattern (the other `--check-*` flags are the template), invokes an existing importable function, and sets an exit code. No novel logic or seam — it is the canonical "registry/mechanical extension following an existing pattern" shape. The only judgment ("skip if noisy") is a documented decision, not design.
- **TDD:** if implemented, a minimal test in the existing `user/scripts/test_lint_skills.py` (following that file's pattern) is warranted but optional per PHASES Testing Strategy; the hard gate is `test_lazy_parity.py`.
- **Files to modify:** `user/scripts/lint-skills.py` (flag) [VERIFY: exists — confirmed]; optionally `user/scripts/test_lint_skills.py` (a flag test).
- **Implementation goal:** if implemented, `python3 user/scripts/lint-skills.py --check-parity` exits 0 on the clean repo and non-zero when drift is injected; if skipped, the decision is documented.
- **Spec requirements:** SPEC §Technical Design §3 (optional `--check-parity`, "decided at implementation").
- **Batch:** 1.

#### WU-3 — `lazy-batch-retro` registry-awareness note

- **Scope (PHASES Phase 4 deliverable 4):** add a comment/note in **`repos/algobooth/.claude/skills/lazy-batch-retro/SKILL.md`** (corrected path — NOT `user/skills/`) — in its grading-rubric section or intro — indicating that the divergence registry at `user/scripts/lazy-parity-manifest.json` records the known intentional differences between `lazy-batch` and its derived twins, and that retro grading rubrics may reference these entries when assessing whether a deviation from the canonical pattern is expected or anomalous.
- **Why mechanical:** a single fixed informational note in a consumer skill. No logic.
- **TDD:** no.
- **Files to modify:** `repos/algobooth/.claude/skills/lazy-batch-retro/SKILL.md` [VERIFY: exists at this corrected path — confirmed].
- **Implementation goal:** the note is present and points at the manifest; `--check-projected` still clean.
- **Spec requirements:** SPEC §"The lazy skill family — parity DAG" (`lazy-batch-retro` is a consumer, gets a Phase-4 registry-awareness note); PHASES Phase 4 deliverable 4.
- **Batch:** 1.

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-1, WU-2, WU-3 | Yes | None (file-disjoint) |

**Phase-end Verification (orchestrator confirms in Step B.4):**
- `pytest user/scripts/` — full suite green (no regressions).
- `python3 user/scripts/lazy_parity_audit.py --repo-root .` — exits 0 with **zero C6 warnings**.
- `python3 user/scripts/lint-skills.py --check-projected --check-capabilities` — exits 0.
- If `--check-parity` implemented: `python3 user/scripts/lint-skills.py --check-parity` — exits 0.

---

## Execution Protocol

### Phase Selection Loop
One phase (P4), one parallel batch (WU-1, WU-2, WU-3). Execute the batch, review, update PHASES.md, run gates, commit; run post-phase steps; commit+push; print the final feature-complete report.

1. **Verify entry precondition** (five-pair manifest exists; audit covers all five). If unmet, STOP.
2. **Announce:** "Implementing lazy-pipeline-parity Phase 4: Doc Reconciliation + Hardening".
3. **Review prior context:** Re-read PHASES.md Phase 3 Implementation Notes (which divergences carry `doc_anchor` fields) + Phase 4; re-read SPEC §Technical Design §2 (C6)/§3 + §"User Experience".
4. **Execute Batch 1** (3 parallel agents) per Per-Batch Steps.
5. **Run Post-Phase Steps.**
6. **Report:** "lazy-pipeline-parity Phase 4 — committed as [hash]" + the feature-complete report.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE FIRST)
Read `~/.claude/skills/_components/task-tracking.md`. Create tasks for WU-1, WU-2, WU-3; update status as you progress.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read PHASES.md (Phase 4 + Phase 3 notes), SPEC §Technical Design §2/§3 + §"User Experience", this plan, and the completed manifest (to know which `divergence` entries carry `doc_anchor` fields). For WU-2, `Read` the `--check-projected`/`--check-capabilities` argparse block in `lint-skills.py` so the agent prompt quotes the exact pattern to mirror.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — 3 PARALLEL)
**PRE-FLIGHT CHECK:** `Agent` + `model: "sonnet"` for all three; no direct orchestrator edits.
Read `~/.claude/skills/_components/tdd-protocol.md`, `subagent-launch.md`, `implementation-agent.md`. Dispatch THREE Sonnet impl agents in a single message (file-disjoint → parallel):
- **WU-1 agent:** the five derived skill paths; the list of `doc_anchor`-bearing divergence entries from the manifest; the C6-zero-warnings goal; the exact authoring-note boilerplate. It runs `lazy_parity_audit.py --repo-root .` and confirms zero C6 warnings before returning.
- **WU-2 agent:** the `lint-skills.py` argparse pattern to mirror (quote the existing `--check-*` flags); the `audit_all_pairs` call; the decision rule (skip-if-noisy → document). It runs `--check-parity` (if implemented) and confirms exit 0.
- **WU-3 agent:** the corrected `lazy-batch-retro/SKILL.md` path; the note content pointing at the manifest.
Include `implementation-agent.md` verbatim in each prompt.
**POST-DISPATCH GATE:** Confirm Agent dispatch; no direct edits.

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md` + `mount-site-verification.md`. Verify: every `doc_anchor` now resolves (C6 zero warnings); each of the five derived skills carries the authoring note; WU-2 either implemented the flag correctly (mirrors the existing flags, exits non-zero on drift) or documented the skip; WU-3's retro note is present at the corrected path.

#### Step B.3: Update PHASES.md (MANDATORY — orchestrator edits directly)
Read `~/.claude/skills/_components/phases-update.md`. Check off all four Phase 4 deliverables + the Verification rows; add Implementation Notes (date, the `--check-parity` implement-or-skip decision + rationale, which doc_anchors were reconciled, files modified). Note: this is the **terminal phase** — its Integration Notes are "None".

#### Step B.4: Run Quality Gates (MANDATORY — repo-specific, NOT npm)
From repo root: `python3 user/scripts/lazy_parity_audit.py --repo-root .` (exit 0, **zero C6 warnings on stderr** — inspect stderr explicitly) → `pytest user/scripts/` (full suite green) → `python3 user/scripts/lint-skills.py --check-projected --check-capabilities` (exit 0) → if implemented, `--check-parity` (exit 0). 100% pass required. (Ignore npm in `quality-gates.md`.)

#### Step B.4.5: MCP Integration Test — SKIP (`MCP runtime: not-required`).

#### Step B.5: Commit Batch
Read `~/.claude/skills/_components/commit-and-push.md`. Commit all three WUs' edits + PHASES.md update together.

### Post-Phase Steps

#### Integration Verification (MANDATORY)
Read `~/.claude/skills/_components/integration-verification.md`. Confirm the hard gate (`test_lazy_parity.py`) is active family-wide and C6 is clean — the human "Differences" docs and the registry now agree across all five pairs (SPEC Validation "C6 doc-table consistency"). Confirm a future canonical edit adding a `## Step` heading would surface as a C1 failure per affected twin (the new-unit detector is live).

#### Update CLAUDE.md Files (MANDATORY)
Read `~/.claude/skills/_components/claude-md-review.md`. Consider noting the parity gate + manifest in any relevant `user/scripts/` CLAUDE.md or skill-maintenance doc. Record the decision.

#### Commit and Push Post-Phase Changes
Read `~/.claude/skills/_components/commit-and-push.md`. Commit post-phase docs; `git push origin main`.

---

## Blocking Issue Protocol
If blocked:
1. Stop in-progress work; launch no new subagents.
2. Commit and push completed work.
3. Print a blocking-issue report (Completed + hashes; Blocked WU; Reason; Recovery; Remaining WUs).
4. Do not work around — the user resolves.

Blocking issues include: a `doc_anchor` that cannot be reconciled because the derived skill genuinely lacks a "Differences" section (a real finding — surface whether to add one or drop the anchor); a `--check-parity` implementation that proves genuinely noisy/duplicative (that is the documented-skip path, NOT a blocker — take it); a subagent or gate failure unfixable after 2 retries; a push conflict unresolvable by rebase.

## Completion
When Phase 4 is complete, the **entire feature** (all four phases) is implemented:
1. Run the full gate suite one final time across the repo (`pytest user/scripts/` green; five-pair audit exit 0 zero-C6; `--check-projected --check-capabilities` exit 0).
2. Print the completion report:

   ## Implementation Batch (Part 4 of 4) — Complete — FEATURE COMPLETE
   **Feature implemented:** lazy-pipeline-parity (all 4 phases)
   **Total phases completed:** 4 (P1 leaks closed → P2 engine+manifest+tests → P3 five-pair registry → P4 doc reconciliation + hardening)
   **Commit log:** [table: Commit | Phase | Title]
   **Final gate status:** `pytest user/scripts/` green; `lazy_parity_audit.py --repo-root .` exit 0 with zero C6 warnings; `--check-projected --check-capabilities` exit 0; `--check-parity` [exit 0 / documented-skip].
   **Hard gate now active family-wide:** drift in any of the five pairs makes the pytest suite red; the only way green is mirror-the-unit or add a per-pair registry divergence with a reason.

   **Implementation Notes summary:** [the `--check-parity` decision; any genuine gaps fixed in P3 and where; the recorded divergences — F5, research/Gemini, `dev:kill`-in-cloud, cloud/bug-axis steps.]

> **Note:** the SPEC.md top-level `**Status:**` flip (Draft → Complete), `COMPLETED.md`/receipt writes, and ROADMAP marks are NOT part of this plan — they are owned by the lazy-pipeline completion gate (`__mark_complete__` / completion-integrity), not `/execute-plan`. Do NOT author them here.
