---
kind: implementation-plan
feature_id: lazy-pipeline-parity
status: Complete
created: 2026-06-15
complexity: mechanical
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-15.
> To execute: `/execute-plan {this-file-path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 4. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\plans\all-phases-lazy-pipeline-parity-part-1.md` (this file)
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\plans\all-phases-lazy-pipeline-parity-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\plans\all-phases-lazy-pipeline-parity-part-3.md`
> - part 4: `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\plans\all-phases-lazy-pipeline-parity-part-4.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — lazy-pipeline-parity (Part 1 of 4)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\PHASES.md` (lazy-pipeline-parity, 4 phases — this part covers Phase 1)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\SPEC.md`

**Total phases (this part):** 1 (Phase 1)
**Plan version:** v2 (reference-based — components loaded from disk per step)
**Complexity:** mechanical (two targeted prose edits to one SKILL.md, mirroring already-shipped canonical wording; grep-verified)

---

## ⚠ REPO CONTEXT — READ BEFORE ANYTHING ELSE (this is NOT AlgoBooth)

This feature lives in the **`claude-config`** repo, not AlgoBooth. The executing session MUST operate against:

- **Repo root (cwd for all commands):** `C:\Users\Jacob\source\repos\claude-config`
- All relative paths in this plan resolve against that root.
- **Git:** branch `main`. `git config user.email` → `66210812+jacobrocks1212@users.noreply.github.com` (personal account) → **push IS eligible** (`git push origin main`). This is NOT the cognito work repo.

**Quality gates for THIS repo (override the AlgoBooth-oriented `quality-gates.md` component):** there is **no `npm run qg`** here. The gates are:
1. `python3 user/scripts/lint-skills.py --check-projected` — must exit 0 (no skill-projection regressions).
2. The phase's own `**Verification**` greps (listed per work unit below).

**MCP runtime: not-required.** PHASES.md declares `MCP runtime: not-required` — this is harness tooling (skill-markdown + Python lint scripts), no Tauri desktop or MCP HTTP server. **Skip Step B.4.5 (MCP Integration Test) entirely** in every batch of this plan.

**Commit policy:** there is **no** `.claude/skill-config/commit-policy.md` in claude-config. Use the fallback component `~/.claude/skills/_components/commit-and-push.md`. Push after each phase (eligible per the email above).

---

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source/prose/test edits (`.py`, `.json`, `SKILL.md`) | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source, prose, or test files. If you are about to modify a `.py`, `.json`, or any `SKILL.md` file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`. (Exception: a trivial PASS-WITH-FIXES of a few lines.)

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` (⚠ OVERRIDDEN — see Repo Context: gates are `lint-skills.py --check-projected` + phase greps, NOT npm) |
| Step B.4.5 | MCP Integration Test | SKIP — `MCP runtime: not-required` |
| Step B.5 | Commit Policy | `~/.claude/skills/_components/commit-and-push.md` (no `.claude/skill-config/commit-policy.md` in this repo) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies; SPEC `**Depends on:** (none)`. The substantive prior-art relationships — `lazy-validation-readiness`, `lazy-hardening`, `turn-routing-enforcement` — are recorded in SPEC.md's dep-block prose and are read-only context, not contracts this plan re-implements.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation/prose/test edits MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit`/`Write` on source/prose/test files. ONLY exception: trivial PASS-WITH-FIXES (a few lines).
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline. (This part has no TDD WU — see Step B.1 note.)
4. PHASES.md is updated AFTER the batch completes (not deferred).
5. Every subagent's output is reviewed before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in the phase finish, integration verification confirms the change.
8. Relevant CLAUDE.md files are created/updated after the phase if changes warrant it.
9. The completed phase is committed and pushed before the part ends.
10. This plan is self-contained — follow it exactly without relying on external context (or sibling parts).
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
12. **Gates for this repo are `lint-skills.py --check-projected` + the phase greps — NOT `npm run qg`.** Skip the MCP step.

---

## Touchpoint Summary

**Audit skipped — documented.** claude-config has no `npm run audit:touchpoints` gate (that gate targets AlgoBooth TS/Rust source-file LOC growth). This part's only touchpoint is `user/skills/lazy-bug-batch/SKILL.md` (866 ln, markdown skill prose) — not a source file the LOC-decomposition gate governs, and well within reason for a skill doc. No new files. No `block` verdict possible; no Phase 0 decomposition needed.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | lazy-pipeline-parity | P1 | Close the Known Runtime-Affecting Leaks | — | Solo |

---

## Work Units

- [x] WU-1 — Close the two `lazy-bug-batch` runtime-affecting leaks (cycle-dispatch-by-ref + run-end `dev:kill`)

---

### Phase: lazy-pipeline-parity P1 — Close the Known Runtime-Affecting Leaks

**Goal:** Two targeted prose edits to `user/skills/lazy-bug-batch/SKILL.md` that restore behavioral parity with canonical `lazy-batch`, closing the two runtime-affecting gaps the full audit found.
**Entry criteria:** None (first phase). Working tree clean.
**SPEC.md references:** §"`lazy-batch` → `lazy-bug-batch` (fully audited)" findings table (F2a dispatch-by-reference GAP; mcp-test `dev:kill` run-end teardown GAP); §Validation Criteria rows 1–2.

#### WU-1 — Close the two `lazy-bug-batch` runtime-affecting leaks

- **Scope (both PHASES.md deliverables — merged into one WU because both are tiny, same-file prose edits with no logic; splitting would only force two sequential single-file batches for no gain):**
  - **F2a cycle-dispatch template fix:** in the **cycle-dispatch block** of `user/skills/lazy-bug-batch/SKILL.md`, change the dispatch instruction from "use the probe's `cycle_prompt` verbatim" wording to **prefer `cycle_prompt_ref` if present, otherwise `cycle_prompt` verbatim** — mirroring the canonical `cycle_prompt_ref`/`cycle_prompt` fallback already shipped in `user/skills/lazy-batch/SKILL.md`. ⚠ PHASES.md cites "~line 495"; the **actual** cycle-dispatch wording is around lines **417–420** (`use the probe's cycle_prompt verbatim`) — locate it by grep, NOT by the cited line number. [VERIFY: `grep -n "cycle_prompt verbatim\|cycle_prompt\` verbatim\|probe's \`cycle_prompt\`" user/skills/lazy-bug-batch/SKILL.md`]
  - **`dev:kill` run-end teardown in §1c.6:** add a mandatory `npm run dev:kill` on every `--run-end`/terminal path of `lazy-bug-batch`, placed **AFTER** `bug-state.py --run-end`, **BEFORE** the `PushNotification` call — matching the structure in canonical `lazy-batch` §1c.6 (canonical line ~410: `npm run dev:kill   # workstation only; no-op-safe if nothing is running`). Annotate it workstation-only / no-op-safe (copy the canonical note: it tears down Vite/MCP/sidecar/orphaned Tauri binaries; N/A for cloud). Rationale: bug mcp-test cycles boot the orchestrator-owned runtime in Step 1d.0 and currently leak it at run-end.
- **TDD:** no — prose/doc edits to a skill markdown file; no executable behavior. Verification is grep + lint (below).
- **Files to modify:** `user/skills/lazy-bug-batch/SKILL.md` — two prose edits (cycle-dispatch template; §1c.6 run-end teardown). [VERIFY: file exists — confirmed 866 ln]
- **Reference for the agent (canonical wording to mirror):**
  - Canonical `dev:kill` block — [VERIFY: `grep -n "dev:kill" user/skills/lazy-batch/SKILL.md`] (canonical line ~410 + the explanatory paragraph ~413).
  - Canonical meta-dispatch already mirrored in bug-batch line 84 (`dispatch_prompt_ref`) — use its wording as the pattern for the cycle-dispatch `cycle_prompt_ref` fallback. [VERIFY: `grep -n "dispatch_prompt_ref" user/skills/lazy-bug-batch/SKILL.md`]
- **Implementation goal:** After the edits, the two Minimum-Verifiable-Behavior greps both return matches at the correct structural locations, and `--check-projected` still passes (no projection side-effects).
- **Spec requirements:** SPEC §Validation Criteria rows 1–2 ("F2a cycle-dispatch mirrored — template references `cycle_prompt_ref`"; "`dev:kill` teardown mirrored — `npm run dev:kill` present on run-end path"). PHASES Phase 1 deliverables + Minimum Verifiable Behavior block.
- **Batch:** 1 (solo).

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-1      | Solo      | N/A             |

**WU-1 Verification (the agent runs these; orchestrator confirms in Step B.4):**
- `grep -n "cycle_prompt_ref" user/skills/lazy-bug-batch/SKILL.md` — must now match ≥1 line in the cycle-dispatch block (in addition to the pre-existing line-84 meta-dispatch match).
- `grep -n "dev:kill" user/skills/lazy-bug-batch/SKILL.md` — must match ≥1 line in the §1c.6 run-end block.
- `python3 user/scripts/lint-skills.py --check-projected` — exits 0.

---

## Execution Protocol

This protocol governs autonomous execution. Follow it exactly.

### Phase Selection Loop

This part has one phase (P1). Execute it, run post-phase steps, commit+push, then proceed to the Completion section (which hands off to part 2).

1. **Announce:** Print "Implementing lazy-pipeline-parity Phase 1: Close the Known Runtime-Affecting Leaks".
2. **Review prior context:** Re-read PHASES.md Phase 1 (no prior Implementation Notes — this is the first phase).
3. **Execute Batch 1** per Per-Batch Steps below.
4. **Run Post-Phase Steps.**
5. **Report:** Print "lazy-pipeline-parity Phase 1 — committed as [hash]".

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE FIRST)

Read `~/.claude/skills/_components/task-tracking.md` and follow it exactly: load task tools via ToolSearch, create a task for WU-1, update status as you progress.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: PHASES.md (Phase 1), SPEC.md (the two findings rows + Validation Criteria rows 1–2), and this plan file. Also `Read` the two canonical anchor locations in `lazy-batch/SKILL.md` so the subagent prompt can quote the exact wording to mirror.

#### Step B.1: Launch Subagent (COMPOSE Agent TOOL CALL — ZERO INLINE EDITING)

**PRE-FLIGHT CHECK:** You will use the `Agent` tool with `model: "sonnet"` for the edit, and will NOT call `Edit`/`Write` on the SKILL.md yourself.

Read `~/.claude/skills/_components/tdd-protocol.md` and `~/.claude/skills/_components/subagent-launch.md`. WU-1 is **non-TDD** (prose edits; no executable behavior) — per the TDD decision gate it goes through **direct implementation**, no test-agent phase. Dispatch ONE Sonnet impl agent. Include in its prompt (it has zero prior context):
- The full repo-context block above (cwd = claude-config root; this is skill-markdown).
- The exact canonical wording to mirror (quote the `dev:kill` block + the `dispatch_prompt_ref` fallback paragraph you Read in Step B.0).
- The two precise edits and their structural locations (grep anchors, not line numbers).
- The WU-1 Verification greps it must run and confirm green before returning.
- Read `~/.claude/skills/_components/implementation-agent.md` and include that briefing verbatim in the agent prompt.

**POST-DISPATCH GATE:** Confirm you dispatched via `Agent` and did not edit the SKILL.md directly.

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md` and `~/.claude/skills/_components/mount-site-verification.md`. Produce a structured review verdict (PASS / PASS-WITH-FIXES / NEEDS-REWORK). Confirm: (a) `cycle_prompt_ref` now appears in the cycle-dispatch block with the correct "prefer ref, fall back to verbatim" semantics; (b) the `dev:kill` line sits AFTER `--run-end` and BEFORE `PushNotification` with the workstation-only/no-op-safe annotation; (c) no unrelated prose was touched.

#### Step B.3: Update PHASES.md (MANDATORY — orchestrator edits this directly)
Read `~/.claude/skills/_components/phases-update.md`. Check off the two Phase 1 deliverables and the three Verification checkboxes. Add an Implementation Notes block (date 2026-06-15, work completed, the actual line locations edited, the canonical anchors mirrored, files modified).

#### Step B.4: Run Quality Gates (MANDATORY — repo-specific, NOT npm)
Run, from the claude-config repo root:
- `python3 user/scripts/lint-skills.py --check-projected` → exit 0.
- The two WU-1 greps → both match.
100% pass required before proceeding. (Ignore the AlgoBooth `npm run qg` instructions inside `quality-gates.md` — they do not apply to this repo.)

#### Step B.4.5: MCP Integration Test — SKIP
`MCP runtime: not-required`. Skip with a one-line note in the commit/PHASES notes.

#### Step B.5: Commit Batch
Read `~/.claude/skills/_components/commit-and-push.md`. Commit the SKILL.md edits + PHASES.md update together.

### Post-Phase Steps

#### Integration Verification (MANDATORY)
Read `~/.claude/skills/_components/integration-verification.md`. Confirm both mechanics are present and structurally correct, and that `--check-projected` is clean (no projection drift introduced into other skills by the edits).

#### Update CLAUDE.md Files (MANDATORY)
Read `~/.claude/skills/_components/claude-md-review.md`. Likely no update needed (prose parity fix); record the decision.

#### Commit and Push Post-Phase Changes
Read `~/.claude/skills/_components/commit-and-push.md`. Commit any post-phase doc changes, then `git push origin main` (eligible).

---

## Blocking Issue Protocol

If a blocking issue is encountered:
1. Stop all in-progress work. Do not launch new subagents.
2. Commit and push any completed work.
3. Print a blocking-issue report (Completed work + commit hashes; Blocked WU; Reason; Recovery suggestion; Remaining WUs).
4. Do not work around the blocker — the user provides resolution.

Blocking issues include: a subagent failure unfixable after 2 retries; a `--check-projected` failure unfixable after 2 retries; a git push conflict unresolvable by rebase; a grep anchor that cannot be located (the canonical wording moved) — surface it rather than guessing.

## Completion

When Phase 1 is complete:
1. Run `python3 user/scripts/lint-skills.py --check-projected` one final time (exit 0) and re-confirm both greps.
2. Print a completion report:

   ## Implementation Batch (Part 1 of 4) — Complete
   **Phase completed:** lazy-pipeline-parity P1 — Close the Known Runtime-Affecting Leaks
   **Commit(s):** [hash(es)]
   **Gate status:** `--check-projected` green; both parity greps match.

3. **Proceed to part 2:** `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\specs\lazy-pipeline-parity\plans\all-phases-lazy-pipeline-parity-part-2.md`

   > Phase 2's live `lazy-batch→lazy-bug-batch` zero-drift assertion depends on these two gaps being closed — this part is its prerequisite.
