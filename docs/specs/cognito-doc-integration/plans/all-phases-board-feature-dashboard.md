---
kind: implementation-plan
feature_id: cognito-doc-integration
status: Complete
created: 2026-06-02
phases: [5]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-02.
> To execute: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\cognito-doc-integration\plans\all-phases-board-feature-dashboard.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — cognito-doc-integration Phase 5 (Board-Aware & Feature-Grouped Dashboard)  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\cognito-doc-integration\PHASES.md` (cognito-doc-integration, Phase 5)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\cognito-doc-integration\SPEC.md`

**Total phases:** 1 (Phase 5)
**Plan version:** v2 (reference-based — components loaded from disk per step)

---

## REPO & PATH ORIENTATION (read before Step 0)

All source edited by this plan lives in the **`claude-config`** git repo, NOT the cwd repo:

- **Edit root:** `C:\Users\JacobMadsen\source\repos\claude-config\`
  - `user/scripts/ado-sync.py`, `user/scripts/work-status.py`
  - `user/skills/dashboard/SKILL.md`
  - `repos/cognito-forms/.claude/skill-config/ado-doc-integration.yml` (version-controlled config; also symlinked into the Cognito Forms tree)
- **Runtime repo:** `C:\Users\JacobMadsen\source\repos\cog-docs\` — holds the live mirror and the **runtime-authoritative** config copy `cog-docs/.claude/skill-config/ado-doc-integration.yml` (this is the file `--repo-root` actually reads at runtime; it is **untracked** in the `cog-docs` repo).
- **Git identity:** `claude-config` is a **personal** repo (`jacobmadsen12321@gmail.com`, remote `jacobrocks1212/claude-config`). **Push IS allowed.**
- **Always** `export PYTHONUTF8=1` before any Python command — Windows cp1252 crashes on Unicode in ADO titles.

---

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py`, `.ts`, `.cs`, `.vue`, `.yml` config, SKILL `.md`, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`, `work-log.jsonl`, and task tracking.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

---

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

---

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.4.5 | MCP Integration Test | `~/.claude/skills/_components/mcp/mcp-integration-test.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |
| Final | Work Log | `~/.claude/skills/_components/work-log.md` |

---

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this plan has no completed hard upstream dependencies; SPEC `**Depends on:** (none)`) | — | — | — |

This phase builds on the **same-feature** completed Phases 1 and 2. The executing session SHOULD re-read their Implementation Notes in `PHASES.md` before starting (Step 3 of the Phase Selection Loop). Key inherited contracts:
- **Phase 1 mirror schema** (`work_item_from_api` @ `ado-sync.py:213`) — 19 keys, additive-safe, `serialize_mirror` @152 uses `sort_keys=True`.
- **Phase 2 renderer** (`render_markdown` @ `work-status.py:382`, `filter_recent_team` @330, `_escape_md_pipe` @377, `match_self_pr` @306, `_is_mine` @113, `load_sources` @80) — the deterministic-clock rule: never `datetime.now()`.

---

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines).
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline — dedicated test agent writes failing tests, dedicated implementation agent makes them pass.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing — including the **Ground-Truth Verification Gate** (independently re-run the agent's `git status --short`, `wc -l`, `grep -n` anchors, and `--test` runner; diff against the pasted block; any mismatch = NEEDS-REWORK).
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in the phase finish, integration verification confirms all changes work together.
8. Relevant CLAUDE.md files are created/updated after the phase if changes warrant it.
9. The completed phase is committed and pushed before finishing.
10. This plan is self-contained — follow it exactly as written without relying on external context.
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
12. **Always `export PYTHONUTF8=1`** before Python. Tests are offline/stdlib-only — run with `python <script>.py --test`.
13. **Determinism:** every new helper must be clock-free (no `datetime.now()` / `Math.random()`); board/feature grouping derives entirely from config + mirror bytes. Fixtures must be non-tautological (would fail if the behavior were reverted).
14. **Surgical staging only** — never `git add -A`. Stage explicit per-file paths so the untracked `cog-docs` config copy and unrelated files are never swept in.

---

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | cognito-doc-integration | P5 | Board-Aware & Feature-Grouped Dashboard | — | per-batch (below) |

Single phase. Batch structure within the phase:

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 (`ado-sync.py`), WU-2 (config YAMLs) | Yes | None (disjoint files) |
| 2 | WU-3 (`work-status.py` pure helpers) | Solo | N/A |
| 3 | WU-4 (`work-status.py` renderer + `--feature`), WU-5 (`dashboard/SKILL.md`) | Yes | None (disjoint files) |

**Why this ordering:** WU-3 and WU-4 both modify `work-status.py`, so they MUST be in separate sequential batches (file-overlap rule). WU-4 consumes WU-3's helpers and WU-1's new `boardColumn` field, so WU-3 (Batch 2) and WU-1 (Batch 1) precede it. WU-2 (configs) and WU-5 (skill doc) touch disjoint files and ride along in Batches 1 and 3 respectively.

---

## Phase: cognito-doc-integration P5 — Board-Aware & Feature-Grouped Dashboard

**Goal:** Replace the flat WI list in `DASHBOARD.md` with a compact Poseidon-board summary plus a feature-grouped priority queue (active feature AB#54423 pinned first), and additively capture each WI's board column in the mirror.

**Entry criteria:** Phases 1 and 2 complete (verified: `work_item_from_api`, `serialize_mirror`, `render_markdown`, `filter_recent_team`, `_escape_md_pipe`, `match_self_pr`, `load_sources` all present at the line numbers in the audit table below).

**SPEC.md references:** §"Layer 1 — Deterministic ADO sync" (mirror schema), §"Layer 2 — Work dashboard" (`render_markdown` / `filter_recent_team` / `/dashboard`), §"Codebase grounding" (Poseidon area, custom WI types, `System.Parent` nesting).

### Verified touchpoint audit (ground truth — do NOT re-derive from the spec)

| Planned file | Exists? | Real anchors | Action |
|--------------|---------|--------------|--------|
| `user/scripts/ado-sync.py` | yes | `work_item_from_api(raw)` @213 (19 keys, `fields.get("System.*")` @242-258); `serialize_mirror` @152 (`json.dumps(..., sort_keys=True)`); `run_self_tests` @464, `total = 5` @629, fixtures `fixtureN_*` | refactor |
| `user/scripts/work-status.py` | yes | `render_markdown(sources, current_branch=None, *, all_team=False)` @382; `render_dashboard` @120 (terminal — DO NOT TOUCH); `filter_recent_team` @330; `_escape_md_pipe` @377; `_is_mine` @113; `match_self_pr` @306; `load_sources` @80 (keys: `repo_root, mirror, feat_queue, bug_queue, materialized, leases, stale_paths`; mirror WIs at `sources["mirror"]["workItems"]`; `syncedAt`/`query` available); `main` argparse @1091-1115 (`--test/--repo-root/--markdown/--all-team/--out/--current-branch`); `run_self_tests` @596, `total = 9` @599, fixtures A–I | refactor |
| `user/skills/dashboard/SKILL.md` | yes | `argument-hint: "[--refresh] [--all-team] [--out <path>]"`; Step 1 (arg parse), Step 4 (`python work-status.py --markdown --repo-root … [--all-team] [--out]`) | refactor |
| `repos/cognito-forms/.claude/skill-config/ado-doc-integration.yml` | yes | top keys `wiql_identity/type_pipeline_map/github_repo/pool`; NO `active_feature_id`, NO `board_columns` | refactor |
| `cog-docs/.claude/skill-config/ado-doc-integration.yml` | yes (untracked runtime copy, identical) | same | refactor |

**Notes carried from audit (read before dispatching):**
- `work-status.py` currently does **not** read `parentId` or any config file — WU-3/WU-4 introduce both. `boardColumn` does not exist in the mirror until WU-1 lands.
- The mirror exposes `parentId` on WIs already (6161/7503 populated; feature **54423 is itself in the mirror** with 79 direct children — so the active feature's title is resolvable from the mirror parent WI).
- `serialize_mirror`'s `sort_keys=True` means appended keys slot in alphabetically with no ordering churn — the additive change is determinism-safe.

---

### Work Units

#### WU-1 — Capture board column in the mirror (`ado-sync.py`)  **[TDD]**

- **Scope (PHASES.md deliverable):** "`ado-sync.py` `work_item_from_api`: capture `System.BoardColumn` → `boardColumn` (str, `""` when off-board) and `System.BoardColumnDone` → `boardColumnDone` (bool, `false` default) — append-only schema additions; the existing 19 keys are unchanged and un-renamed."
- **Files to create/modify:** `user/scripts/ado-sync.py`
- **Test files:** `user/scripts/ado-sync.py` (in-file `run_self_tests()` — add `fixture6_*`; bump `total = 5` → `total = 6`)
- **Test expectations:**
  - `fixture6_board_column_capture`: build a raw API dict with `fields` containing `"System.BoardColumn": "In Progress"` and `"System.BoardColumnDone": True`; assert `work_item_from_api(raw)["boardColumn"] == "In Progress"` and `["boardColumnDone"] is True`.
  - Off-board case: a raw dict whose `fields` lack both keys → `boardColumn == ""` and `boardColumnDone is False` (defaults, never `None`, never `KeyError`).
  - Regression: assert the 19 pre-existing keys are still all present in the output (e.g. `set(expected19).issubset(result.keys())`).
  - The fixture must FAIL against the current code (no `boardColumn` key) and pass after impl (RED→GREEN).
- **Implementation goal:** In `work_item_from_api` (@213), add two output keys using the existing `fields.get("System.*")` access pattern: `"boardColumn": fields.get("System.BoardColumn") or ""` and `"boardColumnDone": bool(fields.get("System.BoardColumnDone", False))`. Do not rename or remove any existing key. `serialize_mirror`'s `sort_keys=True` handles ordering.
- **Spec requirements:** SPEC §"Layer 1 … Mirror schema"; PHASES.md Phase 5 deliverable 1 + MCP assertion 1.
- **Batch:** 1

#### WU-2 — Add board/feature config keys (both YAML copies)  **[non-TDD: config]**

- **Scope (PHASES.md deliverable):** "Config: add `active_feature_id: 54423` and `board_columns: [...]` (canonical lane order) to `ado-doc-integration.yml` — in both the Cognito-forms copy and the `cog-docs` runtime copy."
- **Files to create/modify:**
  - `repos/cognito-forms/.claude/skill-config/ado-doc-integration.yml`
  - `C:\Users\JacobMadsen\source\repos\cog-docs\.claude\skill-config\ado-doc-integration.yml`
- **Test files:** none (config). Verification: `python -c "import yaml,io; ..."` parse check in the review gate.
- **Implementation goal:** Append two top-level keys to BOTH files (keep them byte-identical), preserving the existing `wiql_identity/type_pipeline_map/github_repo/pool` blocks and the `.editorconfig` rules (tabs/CRLF do not apply to YAML — match the existing file's 2-space style and LF):
  ```yaml
  active_feature_id: 54423
  board_columns:
    - New
    - Next
    - "In Progress"
    - "PR Review"
    - "Ready for Testing"
    - Reviewing
    - Merged
  ```
- **Spec requirements:** PHASES.md Phase 5 deliverable 2; SPEC §"Layer 3 … config-driven".
- **Batch:** 1

#### WU-3 — Pure grouping helpers (`order_board` + `group_by_feature`)  **[TDD]**

- **Scope (PHASES.md deliverables):** the `order_board(...)` and `group_by_feature(...)` deliverables. Two pure, clock-free helpers co-located in `work-status.py`; merged into one WU because both are consumed by WU-4's renderer and are co-created in the same file (≤3 deliverables, single-file co-creation).
- **Files to create/modify:** `user/scripts/work-status.py`
- **Test files:** `user/scripts/work-status.py` (in-file `run_self_tests()` — add new fixtures; bump `total = 9` accordingly)
- **Test expectations:**
  - `order_board(wis, board_columns)`: returns an ordered mapping `{column: [wis]}` in canonical `board_columns` order; a WI with `boardColumn == ""` or a value not in `board_columns` lands in a trailing `"(no column)"` bucket (never dropped). Fixture: WIs spanning several lanes + one off-board WI → assert bucket order matches config and the off-board WI is in `"(no column)"`.
  - `group_by_feature(team_wis, active_feature_id, mirror_index)`: returns ordered groups with the active feature's children FIRST, remaining features after, orphans (no `parentId`) LAST; the active group's title resolves from `mirror_index[active_feature_id]["title"]` when present, else `f"Feature {active_feature_id}"`. Multi-level nesting (e.g. a WI whose `parentId` points to a User Story whose `parentId` is the active feature) is attributed to the active feature by walking the `parentId` chain within `mirror_index`; if an ancestor is absent from `mirror_index`, attribute to the nearest known parent, else orphan.
  - Fixtures (non-tautological — would fail if grouping/order were reverted): (a) all-lanes + off-board bucketing; (b) active-pinned-first + title resolution + orphan floor; (c) 3-level `parentId` chain-walk → attributed to active feature.
- **Implementation goal:** Add the two helpers near the other pure helpers in `work-status.py`. They take plain data (lists/dicts) — NO file IO, NO clock. `mirror_index` is an `{id: wi}` dict the caller builds from `sources["mirror"]["workItems"]`. Use an `OrderedDict` (or insertion-ordered dict) for deterministic output.
- **Spec requirements:** PHASES.md Phase 5 deliverables 3–4 + MCP assertions 3–4.
- **Batch:** 2

#### WU-4 — Board + active-feature rendering & `--feature` flag (`work-status.py`)  **[TDD]**

- **Scope (PHASES.md deliverables):** the `render_markdown` extension deliverable + the `--feature <id>` CLI override deliverable + the renderer-side tests.
- **Files to create/modify:** `user/scripts/work-status.py`
- **Test files:** `user/scripts/work-status.py` (in-file `run_self_tests()` — add fixtures; bump `total`)
- **Test expectations:**
  - `render_markdown` output now contains a `## Poseidon Board` section (compact `| Column | # |` count table, lanes in canonical order) positioned BEFORE any feature section, and an `### 🎯 Active Feature: <title> (AB#<id>)` priority-queue table (columns: rank, WI, lane, title, PR) sorted by board-lane order, then remaining feature groups. Reuse `_escape_md_pipe` on every new cell.
  - Graceful pre-Phase-5 path: rendering a mirror whose WIs LACK `boardColumn` → exit 0, board section shows a "no board data; run `/dashboard --refresh`" notice, no traceback.
  - `--feature <id>` overrides the config `active_feature_id`; absent both config and flag → no active-feature section (board + plain feature groups only), still exit 0.
  - Do NOT alter `render_dashboard` (terminal) output — a fixture asserting the terminal render is unchanged is encouraged.
- **Implementation goal:**
  - Add a small config loader: read `board_columns` + `active_feature_id` from `<repo_root>/.claude/skill-config/ado-doc-integration.yml` (PyYAML, lazy import — keep `--test` stdlib-only); if the file or keys are absent, fall back to a hardcoded canonical `board_columns` list and `active_feature_id = None`. Read-only; must never crash the dashboard (Phase 2 graceful-degradation ethos).
  - Build `mirror_index` from `sources["mirror"]["workItems"]`; call `order_board` + `group_by_feature` (WU-3); render the board summary + active-feature + feature-group sections inside `render_markdown`.
  - Add `--feature <id>` in `main` argparse after line 1114; thread it into the `render_markdown` call (flag wins over config).
- **Spec requirements:** PHASES.md Phase 5 deliverables 5–6 + MCP assertions 2 & 5.
- **Batch:** 3

#### WU-5 — Document `--feature` in the dashboard skill (`dashboard/SKILL.md`)  **[non-TDD: doc]**

- **Scope (PHASES.md deliverable):** "`/dashboard` SKILL.md documents the `--feature` passthrough."
- **Files to create/modify:** `user/skills/dashboard/SKILL.md`
- **Test files:** none (doc). Verification: `python lint-skills.py` (if present in `user/skills/` or `user/scripts/`) must still pass; otherwise a manual frontmatter/structure check in review.
- **Implementation goal:** Update `argument-hint` to `"[--refresh] [--all-team] [--feature <id>] [--out <path>]"`; add a Step 1 bullet describing `--feature <id>` (overrides the config `active_feature_id`; pins that feature's children as the priority queue); add `[--feature <id>]` to the Step 4 `python work-status.py …` command line.
- **Spec requirements:** PHASES.md Phase 5 deliverable 6; SPEC §"Layer 2 … `/dashboard`".
- **Batch:** 3

---

## Execution Protocol

This protocol governs the autonomous execution of the phase. Follow it exactly.

### Phase Selection Loop

There is a single phase. Execute its three batches in order, then run Post-Phase Steps. Do not parallelize across batches (Batches 2 and 3 both touch `work-status.py` after Batch 1).

1. **Announce:** Print "Implementing cognito-doc-integration Phase 5: Board-Aware & Feature-Grouped Dashboard".
2. **Review prior context:** Re-read Phase 1 and Phase 2 Implementation Notes in `PHASES.md` — they hold the locked mirror schema, the `render_markdown`/`filter_recent_team` seams, the deterministic-clock rule, and the `PYTHONUTF8=1` pitfall. They take priority over this plan where they diverge.
3. **Execute Batch 1, then Batch 2, then Batch 3** per the Per-Batch Steps below.
4. **Run Post-Phase Steps.**
5. **Report:** Print "cognito-doc-integration Phase 5 — committed as [hash]".

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow it exactly. Create one task per work unit (WU-1 … WU-5). Verify with `TaskList`.

### Per-Batch Steps

For each batch:

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: `PHASES.md` (Phase 5 + prior Implementation Notes), `SPEC.md` (Layers 1–3), and this plan file. Do NOT rely on remembered content.

#### Step B.1: Launch Subagents (COMPOSE Agent CALLS — ZERO INLINE IMPLEMENTATION)
**PRE-FLIGHT:** Confirm you will use `Agent({ model: "sonnet" })` for ALL code/config/doc edits and will NOT call `Edit`/`Write` on source/test/config files yourself.

Read before dispatching: `~/.claude/skills/_components/tdd-protocol.md`, `subagent-launch.md`, `tdd-test-agent.md`, `implementation-agent.md`.

Per-batch dispatch:
- **Batch 1:** dispatch WU-1 (TDD: test-agent writes `fixture6` RED → impl-agent makes it GREEN) and WU-2 (single impl-agent for the two YAML files) — these touch disjoint files and may run in parallel. Give each subagent the relevant audit anchors (line numbers above) and the full WU scope; subagents have ZERO prior context.
- **Batch 2:** dispatch WU-3 (TDD: test-agent adds the three grouping fixtures RED → impl-agent adds `order_board`/`group_by_feature` GREEN). Solo on `work-status.py`.
- **Batch 3:** dispatch WU-4 (TDD: test-agent adds renderer fixtures RED → impl-agent extends `render_markdown` + config loader + `--feature` GREEN) and WU-5 (impl-agent edits `dashboard/SKILL.md`) — disjoint files, parallel.

Every Python subagent must run its `--test` with `export PYTHONUTF8=1` and report the `N/N fixtures passed` line plus a GROUND-TRUTH OUTPUT block (`git status --short`, `wc -l` of changed files, `grep -n` of the new symbol/fixture anchors, and the `--test` exit line).

**POST-DISPATCH GATE:** Verify you composed Agent calls and did not edit files directly.

#### Step B.2: Review Batch Output (MANDATORY BLOCKING GATE)
Read `~/.claude/skills/_components/subagent-review.md` and `~/.claude/skills/_components/mount-site-verification.md`. Run the full protocol, INCLUDING the Step 1.5 Ground-Truth Verification Gate: independently re-run each subagent's `git status --short`, `wc -l`, `grep -n` anchors, and `python <script>.py --test` from your own shell; diff against the pasted block. Any mismatch, or a tautological/vacuous fixture (would still pass if the behavior were reverted), forces NEEDS-REWORK. Produce a verdict (PASS / PASS-WITH-FIXES / NEEDS-REWORK) before proceeding.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `~/.claude/skills/_components/phases-update.md`. Check off the deliverables this batch completed and add an Implementation Notes block (date, work completed, integration notes, pitfalls, files modified). Re-read PHASES.md to confirm the write landed.

#### Step B.4: Run Quality Gates (MANDATORY)
Read `~/.claude/skills/_components/quality-gates.md`. The gates for this phase are the in-file `--test` suites (run with `export PYTHONUTF8=1`):
- `python user/scripts/ado-sync.py --test` (expect `6/6` after WU-1)
- `python user/scripts/work-status.py --test` (count grows past 9 as WU-3/WU-4 fixtures land)
- YAML parse check for WU-2 (both files).
- If `user/scripts/lint-skills.py` exists, run it for WU-5.
- Also re-run the sibling state-machine suites as a regression guard if any shared import changed (it should not): `python user/scripts/bug-state.py --test`, `python user/scripts/test_lazy_core.py` — note the known pre-existing `test_lazy_state_test_output_matches_baseline` Windows-temp-path failure is NOT a regression.
100% pass (modulo that one known baseline) required before proceeding.

#### Step B.4.5: MCP Integration Test (BLOCKING — if applicable)
Read `~/.claude/skills/_components/mcp/mcp-integration-test.md`. Phase 5 HAS an `MCP Integration Test Assertions` block (5 assertions). These are runtime/live assertions (require a `--refresh` poll against ADO and a rendered `DASHBOARD.md`). Treat them as the phase's Runtime Verification: after Batch 3, optionally run `/dashboard --refresh` and spot-check that on-board WIs carry `boardColumn`, the doc opens with `## Poseidon Board`, and AB#54423's children lead the active-feature section. If a live poll is not available in the execution session, record the assertions as deferred-to-manual in the Implementation Notes (consistent with how Phase 1/2 runtime rows were handled) — do NOT block the phase on live ADO availability.

#### Step B.5: Commit Batch
Read the commit policy: try `.claude/skill-config/commit-policy.md` first; fallback `~/.claude/skills/_components/commit-and-push.md`. **Repo-specific notes for this plan:**
- All commits target the **`claude-config`** repo (`git -C "C:/Users/JacobMadsen/source/repos/claude-config" ...`). It is personal → commit AND push are allowed.
- **Stage explicit paths only** (never `git add -A`): e.g. `user/scripts/ado-sync.py`, `user/scripts/work-status.py`, `user/skills/dashboard/SKILL.md`, `repos/cognito-forms/.claude/skill-config/ado-doc-integration.yml`, and the Phase 5 `PHASES.md`/`SPEC.md`/this plan.
- The **`cog-docs` runtime config copy is in a different repo and is untracked** — do NOT attempt to commit it. It must still receive the new keys (WU-2) for runtime correctness; just leave it uncommitted in `cog-docs`.

#### Step B.6: Proceed to Next Batch
Checklist (all true before next batch): review verdict produced; PHASES.md updated + verified; quality gates pass; commit done. If any unchecked, complete it first.

### Propagation Awareness Note
WU-1 adds fields to the mirror schema that WU-4 consumes — they are in different batches (1 then 3), so the producer lands first. No import indirection or mock-alias changes are introduced. Run the full `--test` suite of each touched script (not a subset) at its batch gate.

### Post-Phase Steps (after all three batches)

#### Integration Verification (MANDATORY)
Read `~/.claude/skills/_components/integration-verification.md`. Confirm the end-to-end seam: `ado-sync.py` emits `boardColumn` → `work-status.py` config loader reads `board_columns`/`active_feature_id` → `order_board`/`group_by_feature` consume them → `render_markdown` emits `## Poseidon Board` + `### 🎯 Active Feature`. If a live mirror is available, render once and eyeball the doc; otherwise verify via the fixtures that the data path is wired.

#### Update CLAUDE.md Files (MANDATORY)
Read `~/.claude/skills/_components/claude-md-review.md`. Check whether `user/scripts/CLAUDE.md` (if present) needs a note about the new mirror fields / grouping helpers.

#### Commit and Push Post-Phase Changes
Per the commit policy above. Final push to `claude-config` `main`.

---

## Blocking Issue Protocol

If a blocking issue is encountered:
1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit any completed batches** that haven't been committed (to `claude-config`).
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked
   **Completed batches:** [list with commit hashes]
   **Blocked work unit:** WU-N — [title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]
   **Remaining work units (not attempted):** [list]

4. **Do not attempt to work around the blocker.** The user provides a resolution and re-triggers execution.

Blocking issues include: a subagent failure unfixable after 2 retries; a quality-gate failure unfixable after 2 retries; a push conflict unresolvable by rebase; any error requiring an architectural decision beyond this spec. **NOT blocking:** live ADO unavailability for the runtime/MCP assertions (record as deferred-to-manual); the known `test_lazy_state_test_output_matches_baseline` Windows-temp-path failure.

---

## Completion

When all three batches are complete:
1. **Run the full `--test` suite one final time** (`PYTHONUTF8=1`): `ado-sync.py --test`, `work-status.py --test`, plus the regression guards. All green (modulo the known baseline).
2. **Print a completion report:**

   ## Implementation Batch — Complete
   **Feature implemented:** cognito-doc-integration Phase 5
   **Work units completed:** 5 (WU-1 … WU-5)
   **Batches completed:** 3
   **Total commits:** M
   **Final quality-gate status:** all green (modulo known baseline)

   **Commit log:**
   | Commit | Batch | Work Units | Summary |
   |--------|-------|-----------|---------|
   | … | 1 | WU-1, WU-2 | mirror boardColumn + config keys |
   | … | 2 | WU-3 | order_board + group_by_feature |
   | … | 3 | WU-4, WU-5 | render board/active-feature + --feature + skill doc |

   **Implementation Notes summary:** [board-column field name confirmation, any live-render observations, deferred-to-manual runtime assertions].

---

## Append to Work Log (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/work-log.md` and follow it. Call `interview_work_log_append` with `skill=implement-phase-batch` (or `write-plan`-executed), `project=cognito-doc-integration`, a title, summary, `files_modified`, `phases_md`/`spec_md` paths, `phase_number=5`, `work_units_completed=5`, `batches_completed=3`, and a `technical_context` covering the additive-schema evolution, the clock-free pure-helper grouping, and the parentId chain-walk.
