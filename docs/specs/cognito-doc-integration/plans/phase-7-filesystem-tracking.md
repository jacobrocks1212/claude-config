---
kind: implementation-plan
feature_id: cognito-doc-integration
status: Complete
created: 2026-06-03
phases: [7]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-03.
> To execute: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\cognito-doc-integration\plans\phase-7-filesystem-tracking.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — cognito-doc-integration, Phase 7 (Filesystem-Derived Tracking + Review Co-location)  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\cognito-doc-integration\PHASES.md` (cognito-doc-integration, Phase 7)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\specs\cognito-doc-integration\SPEC.md`

**Total phases:** 1 (Phase 7)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## CROSS-REPO NOTE — READ BEFORE STARTING

This plan modifies files in **three** locations. They commit independently:

| Location | Files | Commit where | Push? |
|----------|-------|--------------|-------|
| `claude-config` repo | `user/scripts/lazy_core.py`, `user/scripts/work-status.py`, `user/scripts/track-work.py` (net-new), `user/scripts/test_lazy_core.py`, the 6 `user/skills/*/SKILL.md`, `user/skills/_components/cog-doc-track-*.md` (net-new), `repos/cognito-forms/.claude/skill-config/cog-doc-track-*.md` (net-new) | in `claude-config` | personal repo — local commit only; do NOT push (claude-config push 401/403s in this environment; leave to the user) |
| `cognito-pr-review` plugin | `~/.claude/plugins/local-tools/plugins/cognito-pr-review/commands/review-pr.md`, `scripts/prep-pr.ts` | in the plugin dir (standalone working dir, **not** symlinked into claude-config) | not a tracked remote — local only |
| `cog-docs` runtime | none authored here — only read at runtime (`docs/work/materialized.json`, item dirs) | — | — |

The skills/scripts at `~/.claude/...` are symlinks into `claude-config/user/...`; editing either path writes through to `claude-config`. The Edit tool refuses to write *through* a symlink, so subagents MUST target the real path under `claude-config/user/...`, never `~/.claude/...`.

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.ts`, `.js`, `.cs`, `.vue`, `.py`, `.rs`, `.tsx`, `.jsx`, `.md` skill file, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`, `work-log.jsonl`.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.4.5 | MCP Integration Test | `~/.claude/skills/_components/mcp/mcp-integration-test.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |
| Final | Work Log | `~/.claude/skills/_components/work-log.md` |

## Project-Specific Quality Gates (use these — the generic component does not map cleanly to claude-config)

Run from `C:\Users\JacobMadsen\source\repos\claude-config` with `PYTHONUTF8=1` set (Windows cp1252 crashes on Unicode in diagnostics):

- **lazy_core changes:** `python user/scripts/test_lazy_core.py` — known pre-existing failure `test_lazy_state_test_output_matches_baseline` (Windows temp-path golden) is NOT a regression; all OTHER tests must pass.
- **track-work.py:** `python user/scripts/track-work.py --test` (the new script ships its own self-test, mirroring `ado-sync.py`/`work-status.py`).
- **work-status.py:** `python user/scripts/work-status.py --test` — must report `N/N fixtures passed`, exit 0.
- **Skill/config changes:** `python ~/.claude/scripts/lint-skills.py` (exit 0) then `python ~/.claude/scripts/project-skills.py` — confirm `cog-doc-track-open`/`-close` content appears in `skills-projected/cognito-forms/` and resolves to the no-op in `skills-projected/_default/`. Then `python ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities` (0 errors).
- **Plugin TS (`prep-pr.ts`):** from `~/.claude/plugins/local-tools/plugins/cognito-pr-review/scripts`, run `npx tsc --noEmit` (no build step exists; this typechecks). The plugin has no automated test harness — `commands/review-pr.md` changes are validated by manual `/cognito-pr-review:review-pr` invocation, noted as deferred-to-manual.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies; SPEC's `**Depends on:**` is `(none)`.)

## Verified Touchpoint Audit (consumed by every work unit — use these exact paths/symbols)

| Planned file | Exists? | Verified anchor | Reuse / refactor directive |
|---|---|---|---|
| `user/scripts/lazy_core.py` | yes | `_atomic_write`@88, `parse_sentinel`@121 (returns dict / `{}` / None), `has_completion_receipt(spec_path, filename="COMPLETED.md") -> bool`@192, `read_stale_upstream`@267, materialized helpers@291-340, consts `_STALE_UPSTREAM_FILENAME`@264 `_MATERIALIZED_FILENAME`@291 | Add `derive_stage` + `track_open/track_touch/track_close` + `_WIP_FILENAME` after the materialized helpers (~@342). **REUSE `has_completion_receipt` for COMPLETED.md/FIXED.md detection.** Stage ladder is presence-based (stdlib only). |
| `user/scripts/test_lazy_core.py` | yes | test-registry list + symbol-presence asserts; run `python user/scripts/test_lazy_core.py` | Add behavioral tests for `derive_stage` + `track_*` following the existing registry pattern |
| `user/scripts/work-status.py` | yes | IN FLIGHT in `render_dashboard`@247-273 AND `render_markdown`@737-760 (both read `sources["leases"]` only); `load_sources`@84 returns `{repo_root,mirror,feat_queue,bug_queue,materialized,leases,stale_paths}`; `_scan_stale_upstream`@68; self-test `_TESTS` list + `total` counter (currently 18) | Clone `_scan_stale_upstream` → `_scan_wip_markers`; add WIP union to BOTH renderers; guard `import lazy_core` (try/except → None) to keep `--test` stdlib-only; degrade to lease-only if `lazy_core` unimportable |
| `user/scripts/track-work.py` | **net-new** | — | New `open|touch|close` CLI + `--test`; imports `lazy_core` |
| `user/skills/spec/SKILL.md` | yes | Task Tracking section ~155 | open-hook injection |
| `user/skills/spec-bug/SKILL.md` | yes | Step 1 "Context Gathering" ~22 (no task-tracking section — inject one line at Step 1 start) | open-hook injection |
| `user/skills/spec-phases/SKILL.md` | yes | Task Tracking Step 0 ~74-88 | open-hook injection |
| `user/skills/write-plan/SKILL.md` | yes | Step 1 "Load All Context" ~64 | open-hook injection |
| `user/skills/execute-plan/SKILL.md` | yes | Step 2 "Initialize Task Tracking" ~125 | open-hook injection (place beside existing task-init, do not duplicate) |
| `user/skills/retro/SKILL.md` | yes | TERMINAL — Step 7 "Append to Work Log" ~717 | **close-hook** injection |
| `repos/cognito-forms/.claude/skill-config/cog-doc-track-open.md`, `cog-doc-track-close.md` | **net-new** | sibling example: `spec-bug-work-item-context.md` | Cognito-only injected instruction (open vs close verbs) |
| `user/skills/_components/cog-doc-track-open.md`, `cog-doc-track-close.md` | **net-new** | — | No-op fallback (single HTML comment) so non-cognito projections inject nothing |
| `cognito-pr-review` plugin `commands/review-pr.md`@332-341, `scripts/prep-pr.ts`@998-1024 | yes | orchestrator writes review at Step 10; `workItems[].id` in `pr-context.json`; `--cache-root` arg, `process.cwd()` default; `sourceRefName` available; zero cog-docs awareness | Resolve `<COG_DOCS>` via sibling `../cog-docs` (or `COG_DOCS_ROOT` env); map `wi_id→feature_id` via `materialized.json`; probe `docs/features/<slug>` then `docs/bugs/<slug>`; fallback to `.claude.local/reviews/` |

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines).
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline — dedicated test agent writes failing tests, dedicated implementation agent makes them pass.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in a phase finish, integration verification confirms all changes work together.
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it.
9. Each completed phase is committed before the next phase begins (push per the cross-repo note — do NOT push claude-config).
10. Cross-feature phases may run in parallel when dependencies are satisfied and no file conflicts exist.
11. This plan is self-contained — follow it exactly as written without relying on external context.
12. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
13. **Subagents edit the real `claude-config/user/...` paths, never the `~/.claude/...` symlinks.** Stage is DERIVED by `derive_stage`, never asserted by any skill — do not add a stage field to `WIP.md` that the dashboard trusts over the artifact ladder.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | cognito-doc-integration | P7 | Filesystem-Derived Tracking + Review Co-location (3 batches) | — | Within-phase batches (see batch table) |

---

### Phase: cognito-doc-integration P7 — Filesystem-Derived Tracking for Manual Work + Review Artifact Co-location

**Goal:** Make manually-invoked workflow skills surface on the dashboard as in-flight with a stage derived purely from filesystem state (no lease), and co-locate `review-pr` output into the cog-docs item dir with a local `reviewed` signal.
**Entry criteria:** Phase 2 complete (dashboard renderers + In Flight panel), Phase 3 complete (`materialized.json` `wi_id→feature_id` join, sentinel helpers, receipts). Both are complete in PHASES.md. Independent of deferred Phase 6.
**SPEC.md references:** "The determinism contract (load-bearing)"; Layer 2 (work dashboard, `render_dashboard`/`render_markdown`); Layer 3 (materialization, `materialized.json`); Validation Criteria rows for branch-link parse and stale-upstream gate.

#### Work Units

**WU-1 — `derive_stage(item_dir)` in `lazy_core.py`** *(TDD: yes)*
- **Scope (PHASES.md deliverable):** "`derive_stage(item_dir) -> str` pure helper in `user/scripts/lazy_core.py`, placed beside the existing sentinel readers — maps the on-disk ladder to exactly one of `spec | research | phases | plan | implement | review | reviewed | blocked | needs-input | stale-upstream | done` … Read-only, clock-free, side-effect-free. Stage is derived, never asserted by a skill."
- **Files to create/modify:** `user/scripts/lazy_core.py`
- **Test files:** `user/scripts/test_lazy_core.py`
- **Test expectations:** A canned temp item-dir built rung-by-rung asserts the returned label: `SPEC.md` only → `spec`; `+RESEARCH.md`/`RESEARCH_SUMMARY.md` (no PHASES) → `research`; `+PHASES.md` (no plans/) → `phases`; `+plans/*.md` → `plan`; an unchecked-WU PHASES with `plans/` present but pre-implementation → `implement`; `+REVIEWED.md` → `reviewed`; `+COMPLETED.md` or `+FIXED.md` receipt → `done`. **Precedence:** `STALE_UPSTREAM.md` → `stale-upstream`, `BLOCKED.md` → `blocked`, `NEEDS_INPUT.md` → `needs-input` must win over the artifact ladder (assert each halts derivation). A `review` stage appears when implementation is complete + an open-PR signal exists but no `REVIEWED.md` yet (derive from receipt-absent + a `PR`/`wait_on_pr` marker if present; otherwise omit and let `implement` stand — document the exact rule chosen).
- **Implementation goal:** Presence-based ladder using `os`/`pathlib` only (NO yaml needed for stage determination — keep it stdlib so callers can run in `--test`). **REUSE `has_completion_receipt(item_dir, filename=...)`@192 for `COMPLETED.md`/`FIXED.md` detection** and `read_stale_upstream`@267 for the stale check — do NOT re-implement file probes. Place the function and a `_WIP_FILENAME = "WIP.md"` / `_REVIEWED_FILENAME = "REVIEWED.md"` constant immediately after the materialized-list helpers (~line 342), preserving every existing export. Return a plain `str`; never raise on a missing dir (return `spec` or a documented default).
- **Spec requirements:** Determinism contract — `derive_stage` is a pure transform of on-disk bytes; the sentinel ladder + receipts already exist (Phase 3 Implementation Notes).
- **Batch:** 1

**WU-2 — `track_open` / `track_touch` / `track_close` in `lazy_core.py`** *(TDD: yes)*
- **Scope (PHASES.md deliverable):** "`track_open` / `track_touch` / `track_close` helpers in `lazy_core.py` managing a per-item `WIP.md` liveness sentinel (frontmatter per `_components/sentinel-frontmatter.md`: `kind: wip`, `wi_id`, `slug`, `branch`, `host`, `started_at`, `last_touched`), all writes atomic via `_atomic_write`. `track_open` is idempotent — creates the item-dir registration if absent and refreshes `last_touched` on every call; `track_close` removes the sentinel."
- **Files to create/modify:** `user/scripts/lazy_core.py`
- **Test files:** `user/scripts/test_lazy_core.py`
- **Test expectations:** `track_open(item_dir, wi_id, slug, branch, host, now=<injected>)` creates `WIP.md` with `kind: wip` frontmatter and all fields; a SECOND `track_open` does not duplicate the sentinel or the dir and advances `last_touched` to the new injected clock while preserving `started_at`; `track_close` removes `WIP.md` (no-op if absent). Time injected via a `now` parameter (epoch/ISO) for determinism — mirror the `lazy_coord.py` `now=` convention; no `datetime.now()` in the helper path used by tests.
- **Implementation goal:** Write via `_atomic_write`@88. Frontmatter must round-trip through `parse_sentinel`@121 (so write YAML-compatible frontmatter between `---` fences). `track_open` reads any existing `WIP.md` (via `parse_sentinel`) to preserve `started_at`, then rewrites with refreshed `last_touched`. Add beside `derive_stage` (after ~line 342). Do not remove/rename existing exports.
- **Spec requirements:** SPEC determinism contract; the WIP sentinel is the manual-work liveness signal distinct from a Phase 4 lease.
- **Batch:** 2 (overlaps WU-1 on `lazy_core.py`)

**WU-3 — `track-work.py` CLI (net-new)** *(TDD: yes)*
- **Scope (PHASES.md deliverable):** "`user/scripts/track-work.py` (net-new): a thin `open | touch | close` CLI over those helpers. Resolves the current item from the git branch (`^p/(\d+)-`) joined through `materialized.json` (`wi_id → feature_id`), or from an explicit `--slug`. Safe no-op (exit 0) when no cog-docs root/config is resolvable."
- **Files to create/modify:** `user/scripts/track-work.py` (net-new)
- **Test files:** self-contained `--test` mode inside `track-work.py` (mirror `ado-sync.py`/`work-status.py` self-test convention — no separate test file).
- **Test expectations:** `--test` fixtures (offline, stdlib + injected inputs): (1) branch `p/56618-foo` + a `materialized.json` mapping `56618→<slug>` resolves the item dir and `open` writes `WIP.md` there; (2) `--slug <x>` bypasses branch resolution; (3) no cog-docs root resolvable → exit 0, nothing written, a diagnostic line emitted; (4) branch that doesn't match `^p/(\d+)-` and no `--slug` → exit 0 no-op. Item-dir resolution probes `docs/features/<slug>` then `docs/bugs/<slug>`.
- **Implementation goal:** `import lazy_core`; call `track_open`/`track_touch`/`track_close`. Resolve `<COG_DOCS>` from `--repo-root`, else `COG_DOCS_ROOT` env, else sibling `../cog-docs` of the current git root, else no-op. Resolve `wi_id` from `--wi-id` or branch regex (`subprocess git rev-parse --abbrev-ref HEAD`), map to `feature_id` via `lazy_core.read_materialized`. Host from `socket.gethostname()`. Run with `PYTHONUTF8=1`-safe output. Depends on WU-2's helpers.
- **Spec requirements:** branch convention `p/<wi_id>-<slug>` / regex `^p/(\d+)-` (Phases 2/4); `materialized.json` join (Phase 3).
- **Batch:** 3 (depends on WU-2)

**WU-4 — Cognito-forms-scoped skill hook (skill-config + no-op fallbacks + 6 skill injections)** *(TDD: no — config/docs)*
- **Scope (PHASES.md deliverable):** "Add a single fallback-cat injection line … to each of `spec`, `spec-bug`, `spec-phases`, `write-plan`, `execute-plan`, `retro`. The net-new `repos/cognito-forms/.claude/skill-config/cog-doc-track.md` carries the actual instruction (front-half → `track-work.py open`; terminal → `track-work.py close`), present only in the cognito-forms projection."
- **Files to create/modify:**
  - `repos/cognito-forms/.claude/skill-config/cog-doc-track-open.md` (net-new) — instruction: at this point, register/refresh dashboard tracking by running `python ~/.claude/scripts/track-work.py open` (resolves the item from the current `p/<id>-` branch); non-fatal if it no-ops.
  - `repos/cognito-forms/.claude/skill-config/cog-doc-track-close.md` (net-new) — instruction: `python ~/.claude/scripts/track-work.py close`.
  - `user/skills/_components/cog-doc-track-open.md` (net-new) — single HTML comment no-op fallback.
  - `user/skills/_components/cog-doc-track-close.md` (net-new) — single HTML comment no-op fallback.
  - `user/skills/spec/SKILL.md`, `spec-bug/SKILL.md`, `spec-phases/SKILL.md`, `write-plan/SKILL.md`, `execute-plan/SKILL.md` — add the open-hook fallback-cat line at the verified early-step anchor (do NOT displace any existing `plan-mode-gate` or batch-mode preamble; spec-bug has no task-tracking section → place at Step 1 start).
  - `user/skills/retro/SKILL.md` — add the close-hook fallback-cat line at the Step 7 anchor.
- **Test files:** none. **Validation:** `python ~/.claude/scripts/lint-skills.py` (0); `python ~/.claude/scripts/project-skills.py` then confirm `skills-projected/cognito-forms/{spec,...}` contains the `track-work.py` instruction and `skills-projected/_default/...` resolves to the no-op comment; `python ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities` (0 errors).
- **Implementation goal:** Mirror the EXACT idiom found in `spec-bug` (`!`cat .claude/skill-config/<file> 2>/dev/null || cat ~/.claude/skills/_components/<fallback>``). The injection directive must be a line on its own. Front-half skills inject `cog-doc-track-open`; retro injects `cog-doc-track-close`. **Do not embed the literal injection pattern anywhere except as a live directive** (lint flags documentation copies).
- **Spec requirements:** scoping to the cognito-forms projection via the established fallback-cat idiom (Locked Decision: Cognito wiring lives in `repos/cognito-forms/.claude/`).
- **Batch:** 1 (independent files)

**WU-5 — `work-status.py` in-flight union (leases ∪ WIP markers)** *(TDD: yes)*
- **Scope (PHASES.md deliverable):** "the *In flight* panel becomes leases ∪ `WIP.md` markers, deduped by `wi_id`/branch …; stage column sourced from `derive_stage`; staleness flagged from `last_touched` age against a config threshold; items carrying a `COMPLETED.md`/`FIXED.md` receipt drop out of in-flight. Absent any WIP marker, behavior is byte-identical to the Phase 2/5 dashboard."
- **Files to create/modify:** `user/scripts/work-status.py`
- **Test files:** in-file self-test fixtures (`_TESTS` list + bump `total`), run via `python user/scripts/work-status.py --test`.
- **Test expectations:** (1) a lease + a WIP marker for the same `wi_id` → In Flight lists it exactly once (dedup); (2) a WIP-only item (no lease) appears In Flight with a stage from `derive_stage`; (3) an item whose dir has `COMPLETED.md`/`FIXED.md` is dropped from In Flight; (4) no WIP markers present → In Flight output byte-identical to pre-change (lease-only) — guard fixture; (5) staleness: a WIP `last_touched` older than the threshold (computed against the mirror `syncedAt`, NOT `datetime.now()`) renders a STALE flag. Both `render_dashboard` (terminal) and `render_markdown` (GFM) covered.
- **Implementation goal:** Add `_scan_wip_markers(base)` (clone `_scan_stale_upstream`@68, filename `WIP.md`); extend `load_sources`@84 to return `wip_paths` (scan `docs/features/` + `docs/bugs/`). In both renderers (`render_dashboard`@247-273, `render_markdown`@737-760), build the union: parse each `WIP.md` (via `lazy_core.parse_sentinel` if available; else a minimal stdlib frontmatter read so `--test` stays stdlib-only), compute stage via `lazy_core.derive_stage`, dedup against lease `wi_id`/`branch`, drop receipt-bearing items (`lazy_core.has_completion_receipt`), flag staleness from `last_touched` vs `syncedAt` + threshold. **Guard `import lazy_core` in try/except → None; if unavailable, degrade to lease-only (current behavior).** Threshold default in code; optional config override read lazily.
- **Spec requirements:** Layer 2 — `render_dashboard`/`render_markdown` share data selection; deterministic clock = `syncedAt`, never `datetime.now()` (Phase 5 Context note).
- **Batch:** 3 (depends on WU-1 + WU-2)

**WU-6 — `review-pr` cog-docs output path resolution** *(TDD: no — plugin; tsc + manual)*
- **Scope (PHASES.md deliverable):** "`cognito-pr-review:review-pr` resolves the WI id (`AB#\d+` in the PR description, fallback branch regex `^p/(\d+)-`) → cog-docs item dir via `materialized.json` (`wi_id → feature_id` slug), and writes its review artifact (`PR-<id>.md` + the persistent journey) into `<COG_DOCS>/docs/{features,bugs}/<slug>/`. Falls back to the current `.claude.local/reviews/` location when the WI is not materialized."
- **Files to create/modify:**
  - `~/.claude/plugins/local-tools/plugins/cognito-pr-review/scripts/prep-pr.ts` — add `sourceRefName`→branch and a branch `^p/(\d+)-` fallback to the `workItems` extraction (@998-1003); resolve `<COG_DOCS>` (sibling `../cog-docs` of cwd, or `COG_DOCS_ROOT` env), read `docs/work/materialized.json`, map the primary `wi_id`→`feature_id`, probe `docs/features/<slug>` then `docs/bugs/<slug>`, and write the resolved `cogDocsItemDir` (or null) into `pr-context.json`.
  - `~/.claude/plugins/local-tools/plugins/cognito-pr-review/commands/review-pr.md` — Step 10 (@332-341): if `pr-context.json.cogDocsItemDir` is set, write `PR-{pr_id}.md` (and journey) there; else fall back to `.claude.local/reviews/` exactly as today. Make the orchestrator instruction explicit and deterministic.
- **Test files:** none (no harness). **Validation:** `npx tsc --noEmit` in `scripts/`; manual `/cognito-pr-review:review-pr` deferred-to-manual note in PHASES.md.
- **Implementation goal:** Keep zero behavior change when the WI is unmaterialized or cog-docs is absent (pure fallback). Do not break local-mode (`LOCAL-{branch}-{timestamp}.md`). `feature` vs `bug` is decided by which of `docs/features/<slug>` / `docs/bugs/<slug>` exists (reuse the Phase-3 probe order).
- **Spec requirements:** Layer 3 materialization join; SPEC Round-5 self-link regex `^p/(\d+)-`.
- **Batch:** 1 (independent of the Python WUs; touches plugin files only)

**WU-7 — `review-pr` emits `REVIEWED.md`** *(TDD: no — plugin; manual)*
- **Scope (PHASES.md deliverable):** "`review-pr` emits a local `REVIEWED.md` sentinel in the item dir on completion, so `derive_stage` reports `reviewed` … No ADO board write (the poller PAT is `vso.work` read-only)."
- **Files to create/modify:** `~/.claude/plugins/local-tools/plugins/cognito-pr-review/commands/review-pr.md` — add a finalization step (after Step 12 completion output) that, when `cogDocsItemDir` resolved (from WU-6), writes `REVIEWED.md` there with frontmatter (`kind: reviewed`, `pr`, `date`, finding counts). No-op when unresolved.
- **Test files:** none. **Validation:** manual; confirm `REVIEWED.md` frontmatter parses and `derive_stage` (WU-1) returns `reviewed` against a fixture dir containing it (add that assertion into WU-1's test set if not already covered).
- **Implementation goal:** Pure additive finalization; never block the review on a write failure (warn, continue). No ADO write.
- **Spec requirements:** "reviewing → reviewed" tracked locally; determinism contract (judgment stays in the review; the sentinel is a pure marker).
- **Batch:** 2 (overlaps WU-6 on `review-pr.md`)

#### Batch overview

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 (`lazy_core.py`), WU-4 (skills + skill-config + `_components`), WU-6 (`prep-pr.ts` + `review-pr.md`) | Yes | None (distinct files) |
| 2 | WU-2 (`lazy_core.py`), WU-7 (`review-pr.md`) | Yes | None across the pair; each overlaps a Batch-1 WU's file so it waits for Batch 1 |
| 3 | WU-3 (`track-work.py`), WU-5 (`work-status.py`) | Yes | None (distinct files); both depend on WU-1+WU-2 |

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** Identify phase(s) whose entry criteria are satisfied. There is one phase (P7); proceed to it.
2. **Announce:** Print "Implementing cognito-doc-integration Phase 7: Filesystem-Derived Tracking + Review Co-location".
3. **Review prior context:** Re-read all previously completed phases' Implementation Notes in PHASES.md (Phases 0–5 establish the mirror schema, dashboard renderers, sentinel helpers, and `materialized.json` join this phase builds on). They take priority over the plan where they diverge.
4. **Execute all batches** per the Per-Batch Steps below, in order 1 → 2 → 3.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "cognito-doc-integration Phase 7 — committed as [hash(es)]".
7. **Loop:** No further phases; proceed to Completion.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

**This is the first thing you do when executing this plan. Do NOT skip ahead to any phase or batch.**

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly. Create tasks for all 7 work units (WU-1 … WU-7), tracking test and implementation phases separately for the TDD units (WU-1, WU-2, WU-3, WU-5).

### Per-Batch Steps

For each batch within the phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow its instructions. Re-read from disk: PHASES.md (Phase 7 + prior Implementation Notes), SPEC.md (determinism contract, Layers 2–3), and this plan file. Do NOT rely on cached content.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — ZERO INLINE IMPLEMENTATION)

**PRE-FLIGHT CHECK:** Confirm (1) you will use the `Agent` tool with `model: "sonnet"` for ALL code changes, (2) you will NOT call `Edit`/`Write` on any source/test/skill file. If either is false, re-read EXECUTION MODEL.

Read ALL of these before proceeding:
1. `~/.claude/skills/_components/tdd-protocol.md` — TDD decision gate (WU-1/2/3/5 are TDD; WU-4/6/7 are non-TDD config/plugin work — for those, dispatch a single implementation agent with the validation commands as its gate).
2. `~/.claude/skills/_components/subagent-launch.md`
3. `~/.claude/skills/_components/tdd-test-agent.md`
4. `~/.claude/skills/_components/implementation-agent.md`

**Every subagent prompt MUST include, verbatim:** (a) the cross-repo note (real `claude-config/user/...` paths, never `~/.claude/...` symlinks; plugin files are standalone); (b) the relevant row(s) of the Verified Touchpoint Audit; (c) `set PYTHONUTF8=1` for any Python run; (d) the WU's exact files/tests/goal from above. Subagents have zero prior context.

Note: `subagent-launch.md` references the other components above via internal directives — since you've already read them, ignore those directives in the file.

**POST-DISPATCH GATE:** After subagents complete, verify you composed `Agent` calls and did NOT edit files directly. If violated, revert and re-dispatch.

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

**Blocking gate.** Read `~/.claude/skills/_components/subagent-review.md` and `~/.claude/skills/_components/mount-site-verification.md`; follow the complete protocol to a structured verdict (PASS / PASS-WITH-FIXES / NEEDS-REWORK). For WU-4 specifically, verify the injection lines match the live idiom exactly and the no-op fallback files exist. For WU-1, verify stage is presence-derived (no yaml dependency) and `has_completion_receipt` is reused (not re-rolled).

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md`. Check off the completed Phase 7 deliverables for this batch's WUs and add an Implementation Notes block (date, work completed, integration notes, pitfalls, files modified). For deferred-to-manual plugin validation (WU-6/WU-7), record it under Runtime Verification as unchecked-with-note, consistent with prior phases.

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/quality-gates.md`, then run the **Project-Specific Quality Gates** listed near the top of this plan for the files this batch touched (with `PYTHONUTF8=1`). 100% pass (excluding the one known pre-existing `test_lazy_core.py` baseline failure) before proceeding. WU-4 must additionally pass the projection + capability lint.

#### Step B.4.5: MCP Integration Test (BLOCKING — if applicable)

Read `~/.claude/skills/_components/mcp/mcp-integration-test.md` to determine applicability. This phase's changes are local-filesystem + skill-projection behavior, not HTTP/runtime MCP surfaces; for most WUs this is **N/A — skip with a note**. The PHASES.md MCP Integration Test Assertions for Phase 7 are fixture/projection-verifiable (covered by Step B.4) and live two-surface checks are deferred-to-manual, consistent with Phases 1/2/5.

#### Step B.5: Commit Batch

Read the commit policy: first try `.claude/skill-config/commit-policy.md` in the project root; else `~/.claude/skills/_components/commit-and-push.md`. Commit `claude-config` changes in the `claude-config` repo and plugin changes in the plugin dir **separately** (see the cross-repo note). Do NOT push `claude-config` (personal repo, push blocked/401 in this environment) — leave pushing to the user.

#### Step B.6: Proceed to Next Batch

**Checklist before proceeding (all must be true):**
- [ ] Review report produced with a verdict
- [ ] PHASES.md updated with completed deliverables and implementation notes
- [ ] All applicable quality gates pass
- [ ] Step B.5 completed (committed per policy)

If any item is unchecked, complete it. Do NOT launch the next batch.

### Propagation Awareness Note

WU-5 introduces a new dependency from `work-status.py` onto `lazy_core` (`derive_stage`, `parse_sentinel`, `has_completion_receipt`). The guarded `try: import lazy_core except ImportError: lazy_core = None` MUST be in place so `--test` stays stdlib-only and the dashboard degrades to lease-only if the import fails. WU-4 adds skill-injection indirection — after it lands, run the FULL projection + capability lint (not just `lint-skills.py`) to confirm no cross-repo pollution.

### Post-Phase Steps (after all batches in the phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md` and follow it. Specifically verify the end-to-end seam: `track-work.py open` writes `WIP.md` → `work-status.py` In Flight lists the item with `derive_stage`'s label → adding `REVIEWED.md`/`COMPLETED.md` transitions the stage / drops it. Confirm the cognito-forms projection injects the track hook while `_default` resolves to the no-op.

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md`. Consider whether `claude-config/user/scripts/` guidance (if any) or the plugin's `CLAUDE.md` should note the new tracking surface and the cog-docs review output path.

#### Commit and Push Post-Phase Changes

Read the commit policy as in Step B.5. Commit per repo; do NOT push claude-config.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point during execution:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit any completed batches** that haven't been committed (per repo).
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed work units:** [list with commit hashes]
   **Blocked work unit:** WU-N: [title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining work units (not attempted):**
   - [list]

4. **Do not attempt to work around the blocker.** The user provides a resolution and re-triggers execution.

Blocking issues include:
- A subagent failure that can't be fixed after 2 retry attempts
- A quality-gate failure that can't be fixed after 2 retry attempts (excluding the known `test_lazy_core.py` baseline)
- `project-skills.py` / capability lint reporting cross-repo pollution that can't be resolved by adjusting the injection
- The cognito-pr-review plugin path being absent or `npx tsc` unavailable (plugin WUs only — report and continue Python WUs if isolable)
- Any error requiring architectural decisions beyond the spec/phase scope

---

## Completion

When all batches are complete:

1. **Run the full quality-gate suite one final time:** `python user/scripts/test_lazy_core.py`, `python user/scripts/track-work.py --test`, `python user/scripts/work-status.py --test`, `python ~/.claude/scripts/lint-skills.py`, `python ~/.claude/scripts/project-skills.py`, `python ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities`, and `npx tsc --noEmit` in the plugin `scripts/` (all with `PYTHONUTF8=1` for Python).
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Feature implemented:** cognito-doc-integration Phase 7
   **Total work units completed:** 7
   **Total commits:** M (claude-config + plugin)
   **Final quality-gate status:** all green (excluding known `test_lazy_core.py` baseline)

   **Commit log:**
   | Commit | Repo | Batch | Work Units |
   |--------|------|-------|------------|

   **Implementation Notes summary:**
   [key integration notes: derive_stage as the single stage authority; WIP vs lease distinction; cognito-forms projection scoping; review-pr cog-docs path + REVIEWED.md; deferred-to-manual plugin verification]

---

## Append to Work Log (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/work-log.md` and follow its instructions. Call `interview_work_log_append` with `skill="execute-plan"`, `project="cognito-doc-integration"`, a title/summary covering the filesystem-derived tracking design (derived-not-asserted stage, WIP liveness, projection-scoped skill hooks, review co-location), `files_modified` (all touched paths across claude-config + plugin), and `technical_context` (determinism contract, lease-vs-WIP distinction, runtime-vs-projection scoping, read-only-PAT → local REVIEWED.md).
