---
kind: implementation-plan
feature_id: unified-pipeline-orchestrator
status: Complete
created: 2026-06-17
phases: [3]
complexity: complex
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-17.
> To execute: `/execute-plan {this-path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 3 of 5. Sibling parts:
> - part 1: `docs/features/unified-pipeline-orchestrator/plans/all-phases-unified-pipeline-orchestrator-part-1.md`
> - part 2: `docs/features/unified-pipeline-orchestrator/plans/all-phases-unified-pipeline-orchestrator-part-2.md`
> - part 3: `docs/features/unified-pipeline-orchestrator/plans/all-phases-unified-pipeline-orchestrator-part-3.md` (this file)
> - part 4: `docs/features/unified-pipeline-orchestrator/plans/all-phases-unified-pipeline-orchestrator-part-4.md`
> - part 5: `docs/features/unified-pipeline-orchestrator/plans/all-phases-unified-pipeline-orchestrator-part-5.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — unified-pipeline-orchestrator (Phase 3) (v2)

**PHASES.md files:**
- `docs/features/unified-pipeline-orchestrator/PHASES.md` (unified-pipeline-orchestrator, 5 phases — this part covers Phase 3)

**SPEC.md files:**
- `docs/features/unified-pipeline-orchestrator/SPEC.md`

**Total phases:** 5 (this part: 3)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Dispatch subagents, review, run gates, update tracking | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** Orchestrator MUST NOT `Edit`/`Write` source/test files. Direct-edit only: `PHASES.md`, `CLAUDE.md`.

> **SHARED-COMPONENT WARNING.** `adhoc-enqueue.md` is injected into FOUR `/lazy*` skills. After editing it, run `project-skills.py` to confirm all four re-resolve. The default (feature) path MUST stay byte-equivalent in behavior — `--type bug` is purely additive.

> **DISPATCH-LIMITED CONTEXT NOTE.** Inside a dispatch-limited cycle subagent (no `Agent` tool), implement INLINE with `Edit`/`Write`.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

Before executing each step, `Read` the listed component files from disk. Do NOT proceed from memory.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — SPEC `**Depends on:** (none)`.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. ALL impl/test work via Sonnet subagents (or inline under the dispatch-limited override). Trivial PASS-WITH-FIXES excepted.
2. No worktrees.
3. TDD work units go test-first.
4. PHASES.md updated after each batch.
5. Every output reviewed.
6. Mistakes fixed before next batch.
7. Integration verification after all batches.
8. CLAUDE.md updated if warranted.
9. Phase committed + pushed before next part.
10. Self-contained.
11. Read step components from disk first.
12. **After editing `adhoc-enqueue.md`, run `project-skills.py`** to re-resolve all four injecting skills.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | unified-pipeline-orchestrator | P3 | Ad-hoc enqueue `--type bug` | part-1 (merged view sees bugs), part-2 (unified driver dispatches bugs) | Solo |

## Work Units

- [x] WU-1 — `--type {feature|bug}` CLI routing on `--enqueue-adhoc`
- [x] WU-2 — `adhoc-enqueue.md` shared protocol `--type bug` path + projection

## Phase: unified-pipeline-orchestrator P3 — Ad-hoc enqueue `--type bug`

**Goal:** Extend the ad-hoc enqueue surface so `--type bug` routes the item into `docs/bugs/queue.json` (via the existing `bug-state.py` enqueue) and the unified run picks it up; default (`--type feature` / omitted) is byte-identical to today.

**Entry criteria:** part-1 + part-2 complete (the merged view sees `docs/bugs/queue.json` and the unified driver dispatches bug items).

**SPEC.md references:** "Ad-hoc enqueue extends to bugs"; Implementation Phase 3; Validation Criteria "Ad-hoc bug enqueue"; RESEARCH_SUMMARY Locked Decision 5 (harden-harness spin-off path).

### WU-1 — `--type {feature|bug}` CLI routing on `--enqueue-adhoc`

- **Scope:** Deliverable "`--enqueue-adhoc` CLI: feature enqueue lands in `docs/features/queue.json`; bug enqueue lands in `docs/bugs/queue.json` via existing `bug-state.py:enqueue_adhoc` — do NOT duplicate it" + the default-path no-regression deliverable.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy-state.py` — add a `--type {feature,bug}` argparse arg (default `feature`); when `bug`, the enqueue dispatches to `bug-state.py`'s enqueue path [VERIFY: grep -n "def enqueue_adhoc" user/scripts/bug-state.py] (subprocess call, mirroring the existing bug-type adhoc routing already present at `lazy-state.py:431-444` [VERIFY: grep -n "bug-state.py" user/scripts/lazy-state.py]) — reuse, do NOT reimplement bug enqueue.
- **Test files:** `user/scripts/test_lazy_core.py` and/or the `bug-state.py --test` in-file harness (fixtures 12/13 are the template) [VERIFY: grep -n "enqueue-adhoc-writes-entry\|enqueue-adhoc-idempotent" user/scripts/bug-state.py].
- **Test expectations:** `--enqueue-adhoc --type bug --id … --name … --brief …` writes a `spec_dir`-keyed entry to `docs/bugs/queue.json` and seeds `docs/bugs/<slug>/`; default (no `--type`) writes a `docs/features/queue.json` entry byte-identical to today; second call with the same id is idempotent (no-op, no raise).
- **Implementation goal:** Type-aware routing with the feature path unchanged. The bug path reuses the existing bug enqueue, which already writes the keyed entry.
- **Spec requirements:** "the shared `adhoc-enqueue.md` protocol gains a `--type bug` path so … the item lands in the correct queue".
- **Batch:** 1.

### WU-2 — `adhoc-enqueue.md` shared protocol `--type bug` path + projection

- **Scope:** Deliverable "`adhoc-enqueue.md`: add a `--type {feature|bug}` selection (default feature — byte-identical when omitted); bug path invokes `bug-state.py --enqueue-adhoc` and seeds the bug-doc shape (`docs/bugs/<slug>/`)" + "the four `/lazy*` skills that inject it continue to work unchanged for the default path".
- **TDD:** no (shared component prose; behavior verified by WU-1's CLI tests + the projection lint).
- **Files to create/modify:**
  - `user/skills/_components/adhoc-enqueue.md` — document the `--type` selection: Step 1-4 gain a type branch; the bug path uses `bug-state.py --enqueue-adhoc --type bug` and `docs/bugs/<feature_id>/` (the component currently hard-codes `docs/features/` in Step 5 / the Notes [VERIFY: grep -n "docs/features" user/skills/_components/adhoc-enqueue.md]).
- **Implementation goal:** The shared protocol is type-aware while the default feature path's prose stays behavior-equivalent. After the edit, `python ~/.claude/scripts/project-skills.py` re-resolves all four injecting skills cleanly and `lint-skills.py --check-projected` passes.
- **Spec requirements:** "The shared `adhoc-enqueue.md` protocol gains a `--type bug` path".
- **Batch:** 2 (after WU-1 — the doc references the CLI flag WU-1 adds).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | `lazy-state.py` + tests |
| 2 | WU-2 | Solo | `adhoc-enqueue.md` |

## Execution Protocol

### Phase Selection Loop
Single phase. Batches in order, then Post-Phase Steps.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)
Read `~/.claude/skills/_components/task-tracking.md`. If task tools unavailable, track via `- [ ] WU-N`.

### Per-Batch Steps
- **Step B.0 — Re-read:** Read `source-reread.md`; re-read PHASES.md (Phase 3 + prior notes), SPEC.md, this plan.
- **Step B.1 — Launch:** Read `tdd-protocol.md`, `subagent-launch.md`, `tdd-test-agent.md`, `implementation-agent.md`. Dispatch (or inline). Tick `- [ ] WU-N`.
- **Step B.2 — Review (GATE):** Read `subagent-review.md` + `mount-site-verification.md`. Verdict required.
- **Step B.3 — Update PHASES.md:** Read `phases-update.md`. Check off Phase 3 deliverables; Implementation Notes.
- **Step B.4 — Quality Gates:** Read `quality-gates.md` + `.claude/skill-config/quality-gates.md`. Gates: `python3 user/scripts/bug-state.py --test`, `python3 user/scripts/lazy-state.py --test`, `python3 user/scripts/test_lazy_core.py`, `python ~/.claude/scripts/project-skills.py`, `python ~/.claude/scripts/lint-skills.py --check-projected`. 100% pass.
- **Step B.4.5 — MCP:** N/A (`not-required`).
- **Step B.5 — Commit:** Per `.claude/skill-config/commit-policy.md` (main only).
- **Step B.6 — Proceed:** Checklist.

### Post-Phase Steps
- **Integration Verification:** Read `integration-verification.md`. Confirm an ad-hoc bug enqueued via `--type bug` is picked up by a unified `/lazy-batch` run (end-to-end: enqueue → merged head → bug pipeline dispatch).
- **Update CLAUDE.md:** Read `claude-md-review.md`. Update `user/scripts/CLAUDE.md` CLI surface + the project CLAUDE.md adhoc-enqueue component note.
- **Commit and Push:** Per commit policy.

## Blocking Issue Protocol

If blocked: stop, commit completed work, print blocking report, do not work around. Blocking issues: 2-retry-exhausted subagent/gate failure; unresolvable push conflict; a genuine fork in bug-doc seeding semantics (write `NEEDS_INPUT.md`).

## Completion

When Phase 3 is complete: run the full gate suite; print a completion report; proceed to part 4.
