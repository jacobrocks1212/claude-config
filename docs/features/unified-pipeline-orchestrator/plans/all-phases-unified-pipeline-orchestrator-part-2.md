---
kind: implementation-plan
feature_id: unified-pipeline-orchestrator
status: Ready
created: 2026-06-17
phases: [2]
complexity: complex
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-17.
> To execute: `/execute-plan {this-path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 5. Sibling parts:
> - part 1: `docs/features/unified-pipeline-orchestrator/plans/all-phases-unified-pipeline-orchestrator-part-1.md`
> - part 2: `docs/features/unified-pipeline-orchestrator/plans/all-phases-unified-pipeline-orchestrator-part-2.md` (this file)
> - part 3: `docs/features/unified-pipeline-orchestrator/plans/all-phases-unified-pipeline-orchestrator-part-3.md`
> - part 4: `docs/features/unified-pipeline-orchestrator/plans/all-phases-unified-pipeline-orchestrator-part-4.md`
> - part 5: `docs/features/unified-pipeline-orchestrator/plans/all-phases-unified-pipeline-orchestrator-part-5.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — unified-pipeline-orchestrator (Phase 2) (v2)

**PHASES.md files:**
- `docs/features/unified-pipeline-orchestrator/PHASES.md` (unified-pipeline-orchestrator, 5 phases — this part covers Phase 2)

**SPEC.md files:**
- `docs/features/unified-pipeline-orchestrator/SPEC.md`

**Total phases:** 5 (this part: 2)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **COUPLED-PAIR WARNING (CLAUDE.md).** This phase edits BOTH `lazy-batch/SKILL.md` and `lazy-batch-cloud/SKILL.md`. They are a coupled pair: any orchestration-shape change MUST be mirrored. Whichever subagent edits one MUST also diff the other and confirm the change matches. State-machine logic goes in the SCRIPTS, never the wrapper prose.

> **DISPATCH-LIMITED CONTEXT NOTE.** If executing inside a `/lazy-batch` cycle subagent with NO `Agent` tool, perform all implementation INLINE with `Edit`/`Write`. Otherwise dispatch to Sonnet subagents as written.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — SPEC `**Depends on:** (none)`.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. ALL implementation/test work delegated to Sonnet subagents (EXCEPT the dispatch-limited inline override). Trivial PASS-WITH-FIXES excepted.
2. No worktrees for subagents.
3. TDD work units go test-first.
4. PHASES.md updated after each batch.
5. Every output reviewed before continuing.
6. Mistakes fixed before next batch.
7. Integration verification after all batches.
8. CLAUDE.md updated if warranted (`user/scripts/CLAUDE.md` lazy-batch wrapper docs).
9. Phase committed + pushed before next part.
10. Self-contained — follow exactly.
11. Read step components from disk before each step.
12. **Coupled-pair mirror is mandatory** — `/lazy-batch` ↔ `/lazy-batch-cloud`.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | unified-pipeline-orchestrator | P2 | Unified batch skill (single driver, two state scripts) | part-1 Phase 1 (`--next-merged` exists) | Solo |

## Work Units

- [ ] WU-1 — Rewire `/lazy-batch` cycle loop to the merged view + State Machine Summary
- [ ] WU-2 — Mirror into `/lazy-batch-cloud` + converge `/lazy-bug-batch` cross-reference
- [ ] WU-3 — Extend `lazy_parity_audit.py` with merged-view dispatch-consistency assertion + no-regression guard

## Phase: unified-pipeline-orchestrator P2 — Unified batch skill (single driver, two state scripts)

**Goal:** Make `/lazy-batch` the shared driver looping over the Phase-1 merged view, type-dispatching each cycle to `lazy-state.py` (feature, `__mark_complete__`) or `bug-state.py` (bug, `__mark_fixed__`); mirror into the cloud variant; extend the parity audit; preserve single-type behavior.

**Entry criteria:** part-1 Phase 1 complete — `lazy-state.py --next-merged` and the ordering comparator exist.

**SPEC.md references:** "Unified orchestrator" UX bullets; Technical Design "The unified batch skill loops…"; "Coupled-pair impact"; Validation Criteria "Merged run drains both types" + "Single-type unchanged"; RESEARCH_SUMMARY Locked Decision 1.

### WU-1 — Rewire `/lazy-batch` cycle loop to the merged view + State Machine Summary

- **Scope:** Deliverables "cycle loop rewired (probe merged head → dispatch via matching state script → commit → push)" + "type-dispatch tabulated in the State Machine Summary".
- **TDD:** no (skill-doc prose; behavior is verified by the parity audit in WU-3, not unit tests).
- **Files to create/modify:**
  - `user/skills/lazy-batch/SKILL.md` — rewire the cycle loop to call `lazy-state.py --next-merged` [VERIFY: grep -rn "next-merged" user/scripts/lazy-state.py — note: this anchor is BUILT by part-1; if part-1 is not yet landed, treat as a build dependency, not a phantom] for the head, then dispatch via the matching script's existing `--emit-prompt`/`--probe` contract [VERIFY: grep -n "emit-prompt\|--probe" user/scripts/lazy-state.py]; update the State Machine Summary table at the bottom to show feature→`lazy-state.py`+`__mark_complete__` and bug→`bug-state.py`+`__mark_fixed__`.
- **Implementation goal:** The driver probes the merged head each cycle and dispatches the correct per-item pipeline. Both state machines + gates run unchanged — the skill carries NO new state-machine logic (that lives in the scripts). Terminal actions stay type-correct.
- **Spec requirements:** "The orchestrator dispatches via the matching state script. Terminal actions stay type-correct."
- **Batch:** 1.

### WU-2 — Mirror into `/lazy-batch-cloud` + converge `/lazy-bug-batch` cross-reference

- **Scope:** Deliverables "Cloud mirror: lazy-batch-cloud updated with same merged-view shape; Differences-from-/lazy-batch block updated" + "lazy-bug-batch converged/cross-referenced".
- **TDD:** no (skill-doc prose).
- **Files to create/modify:**
  - `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` — mirror the merged-view orchestration shape; update the "Differences from /lazy-batch" block to list only intended divergences (`--cloud`, cloud-queue-exhausted handling, `__write_deferred_non_cloud__`, cloud-limitations block, the merged-view branch's cloud push).
  - `user/skills/lazy-bug-batch/SKILL.md` — add a cross-reference documenting that the unified `/lazy-batch` driver now drains both queues and supersedes a standalone bug loop for unified runs; do NOT delete `/lazy-bug-batch` (single-type bug runs still use it) — clarify the relationship.
- **Implementation goal:** Coupled-pair parity preserved. The cloud variant's merged-view branch matches the workstation driver except for the tabulated cloud divergences.
- **Spec requirements:** "mirror into the cloud variant"; CLAUDE.md coupled-pair rule.
- **Batch:** 2 (separate batch from WU-1 — different files, but WU-2 mirrors WU-1's shape so it must follow WU-1).

### WU-3 — Extend `lazy_parity_audit.py` with merged-view dispatch-consistency assertion + no-regression guard

- **Scope:** Deliverables "lazy_parity_audit.py extended to assert the merged-view dispatch branch stays consistent across feature/bug handling" + "No-regression guard (single-type run identical to per-type batch)".
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy_parity_audit.py` — add the merged-view dispatch-consistency check to `audit_state_script_parity` [VERIFY: grep -n "def audit_state_script_parity" user/scripts/lazy_parity_audit.py] or `audit_all_pairs` [VERIFY: grep -n "def audit_all_pairs" user/scripts/lazy_parity_audit.py].
- **Test files:** `user/scripts/test_lazy_parity.py` [VERIFY: ls user/scripts/test_lazy_parity.py].
- **Test expectations:** the audit FAILS (returns a finding) when the cloud variant's merged-view dispatch branch diverges from the workstation variant in an unintended way; PASSES when they match. A single-type fixture (only-features queue) produces the same cycle sequence the pre-change per-type batch would.
- **Implementation goal:** The parity audit grows to cover the new shared-driver dispatch branch (feature/bug type-dispatch consistency), per SPEC "the parity audit grows to assert the merged-view dispatch branch stays consistent across feature/bug handling".
- **Spec requirements:** "Coupled-pair impact … the parity audit grows…"; Validation Criteria "Single-type unchanged → Parity audit; regression run".
- **Batch:** 2 (parallel with WU-2 — different files: `lazy_parity_audit.py`/tests vs the cloud/bug SKILL.md).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | `lazy-batch/SKILL.md` |
| 2 | WU-2, WU-3 | Yes | None (cloud/bug SKILLs vs parity audit + tests) |

## Execution Protocol

### Phase Selection Loop
Single phase. Execute batches in order, then Post-Phase Steps.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)
Read `~/.claude/skills/_components/task-tracking.md`. If task tools unavailable (dispatch-limited), track via the `- [ ] WU-N` checkboxes.

### Per-Batch Steps
- **Step B.0 — Re-read:** Read `source-reread.md`; re-read PHASES.md (Phase 2 + prior notes), SPEC.md, this plan.
- **Step B.1 — Launch:** Read `tdd-protocol.md`, `subagent-launch.md`, `tdd-test-agent.md`, `implementation-agent.md`. Dispatch (or inline). Tick `- [ ] WU-N`.
- **Step B.2 — Review (GATE):** Read `subagent-review.md` + `mount-site-verification.md`. Produce a verdict. Run `project-skills.py` after the SKILL.md edits to confirm the projection still resolves.
- **Step B.3 — Update PHASES.md:** Read `phases-update.md`. Check off Phase 2 deliverables; add Implementation Notes incl. the coupled-pair mirror confirmation and the parity-audit assertion added.
- **Step B.4 — Quality Gates:** Read `quality-gates.md` + `.claude/skill-config/quality-gates.md`. Gates: `python3 user/scripts/lazy_parity_audit.py`, `python3 user/scripts/test_lazy_parity.py`, `python3 user/scripts/lazy-state.py --test`, `python3 user/scripts/bug-state.py --test`, `python ~/.claude/scripts/lint-skills.py --check-projected`, `python ~/.claude/scripts/project-skills.py`. 100% pass.
- **Step B.4.5 — MCP:** N/A (`not-required`).
- **Step B.5 — Commit:** Per `.claude/skill-config/commit-policy.md` (main only).
- **Step B.6 — Proceed:** Checklist (verdict, PHASES updated, gates green, committed).

### Post-Phase Steps
- **Integration Verification:** Read `integration-verification.md`. Confirm a merged two-type fixture run dispatches correctly per type and a single-type run is unchanged.
- **Update CLAUDE.md:** Read `claude-md-review.md`. Update `user/scripts/CLAUDE.md` (the lazy-batch wrapper row / coupled-pair table) + the project CLAUDE.md coupled-pair table if the driver shape changed.
- **Commit and Push:** Per commit policy.

## Blocking Issue Protocol

If blocked: stop, commit completed work, print a blocking report (completed/blocked/reason/recovery/remaining), do not work around. Blocking issues: 2-retry-exhausted subagent or gate failure; unresolvable push conflict; a parity divergence that is a genuine design fork (e.g. the cloud variant genuinely cannot mirror a merged-view behavior — write `NEEDS_INPUT.md`).

## Completion

When Phase 2 is complete: run the full gate suite; print a completion report; proceed to part 3.
