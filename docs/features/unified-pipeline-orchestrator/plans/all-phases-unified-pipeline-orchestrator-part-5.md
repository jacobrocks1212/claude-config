---
kind: implementation-plan
feature_id: unified-pipeline-orchestrator
status: In-progress
created: 2026-06-17
phases: [5]
complexity: complex
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-17.
> To execute: `/execute-plan {this-path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 5 of 5. Sibling parts:
> - part 1: `docs/features/unified-pipeline-orchestrator/plans/all-phases-unified-pipeline-orchestrator-part-1.md`
> - part 2: `docs/features/unified-pipeline-orchestrator/plans/all-phases-unified-pipeline-orchestrator-part-2.md`
> - part 3: `docs/features/unified-pipeline-orchestrator/plans/all-phases-unified-pipeline-orchestrator-part-3.md`
> - part 4: `docs/features/unified-pipeline-orchestrator/plans/all-phases-unified-pipeline-orchestrator-part-4.md`
> - part 5: `docs/features/unified-pipeline-orchestrator/plans/all-phases-unified-pipeline-orchestrator-part-5.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — unified-pipeline-orchestrator (Phase 5) (v2)

**PHASES.md files:**
- `docs/features/unified-pipeline-orchestrator/PHASES.md` (unified-pipeline-orchestrator, 5 phases — this part covers Phase 5)

**SPEC.md files:**
- `docs/features/unified-pipeline-orchestrator/SPEC.md`

**Total phases:** 5 (this part: 5)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Dispatch subagents, review, run gates, update tracking | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** Orchestrator MUST NOT `Edit`/`Write` source/test files. Direct-edit only: `PHASES.md`, `CLAUDE.md`.

> **COUPLED-PAIR + STATE-MACHINE WARNINGS.** WU-4 edits BOTH `lazy-batch/SKILL.md` and `lazy-batch-cloud/SKILL.md` — mirror per the coupled-pair rule. The `apply_pseudo` change (WU-3) is STATE-MACHINE logic — it goes in `lazy_core.py` (the source of truth), never the wrapper prose; keep `lazy-state.py --test` / `bug-state.py --test` / `test_lazy_core.py` green (the regression net), and add a fixture for the `-followups` regression + the symlink coverage case.

> **DISPATCH-LIMITED CONTEXT NOTE.** Inside a dispatch-limited cycle subagent (no `Agent` tool), implement INLINE with `Edit`/`Write`.

> **DESIGN NOTE — `--ensure-runtime` home (surfaced, not silently descoped).** The runtime-ensure dance is AlgoBooth-specific (TCP 3333, `npm run dev:restart`, `GET /health` — currently in `lazy-batch/SKILL.md` Step 1d.0), but `lazy-state.py` is the repo-agnostic harness script. The SPEC's Locked Decision puts `--ensure-runtime` ON `lazy-state.py`, so WU-1 implements it there and PARAMETERIZES the AlgoBooth specifics out of hard-coded literals (read from repo config / env, not baked in). If implementation reveals the AlgoBooth coupling cannot be cleanly parameterized (a genuine architecture fork), WRITE `NEEDS_INPUT.md` and halt — do NOT hard-code AlgoBooth into the shared harness script.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

Before executing each step, `Read` the listed component files from disk. Do NOT proceed from memory.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — SPEC `**Depends on:** (none)`.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. ALL impl/test work via Sonnet subagents (or inline under the dispatch-limited override). Trivial PASS-WITH-FIXES excepted.
2. No worktrees.
3. TDD work units go test-first.
4. PHASES.md updated after each batch.
5. Every output reviewed.
6. Mistakes fixed before next batch.
7. Integration verification after all batches.
8. CLAUDE.md updated if warranted (`user/scripts/CLAUDE.md` CLI surface — new `--ensure-runtime`/`--gate-coverage` flags + the enhanced `--apply-pseudo` behavior).
9. Phase committed + pushed before finishing.
10. Self-contained.
11. Read step components from disk first.
12. **State-machine changes go in `lazy_core.py`/`lazy-state.py`, not wrapper prose; keep `--test` green.**

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | unified-pipeline-orchestrator | P5 | First three subcommands (proven consumers) | part-4 (deterministic-only bar / promotion checklist) | Solo |

## Work Units

- [ ] WU-1 — `--ensure-runtime` subcommand (structured runtime status)
- [ ] WU-2 — `--gate-coverage <spec_path>` (symlink-resolving Gate-1 verdict) + mcp-coverage-audit.md rewire
- [ ] WU-3 — Enhance `apply_pseudo __mark_complete__`: ROADMAP strike + resolved-`spec_dir` queue trim
- [ ] WU-4 — Rewire `/lazy-batch` (+ cloud mirror) to call the three subcommands

## Phase: unified-pipeline-orchestrator P5 — First three subcommands (proven consumers)

**Goal:** Promote the three retro-named deterministic dances to `lazy-state.py` subcommands and rewire the batch skills to call them instead of hand-running the dances.

**Entry criteria:** part-4 complete — the deterministic-only bar + promotion checklist govern landing these subcommands (they are the bar's first proven consumers).

**SPEC.md references:** "First three consumers" (Technical Design + UX); Implementation Phase 5; Validation Criteria rows "`--ensure-runtime` collapses the dance", "`--gate-coverage` is deterministic + symlink-safe", "`__mark_complete__` strikes ROADMAP + trims by spec_dir"; RESEARCH_SUMMARY Locked Decision 4.

### WU-1 — `--ensure-runtime` subcommand (structured runtime status)

- **Scope:** Deliverable "`--ensure-runtime` → `{ready|booted|stale-rebuilt, mcp_tools_present}`: probe `/health` → staleness check → `dev:restart` (bg) → curl-until-200 → assert MCP tool present; returns structured status".
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy-state.py` — add `--ensure-runtime` argparse arg + dispatch in `main()` [VERIFY: grep -n "def main" user/scripts/lazy-state.py]. Reuse the existing staleness check `stale_binary.py` [VERIFY: ls user/scripts/stale_binary.py] rather than reimplementing it. Inject the probe + `now` (mirroring `lazy_coord.py`'s injected-time pattern [VERIFY: grep -n "now" user/scripts/lazy_coord.py]) so `--test` is deterministic. Parameterize AlgoBooth specifics (port, restart command, health path) out of hard-coded literals (see DESIGN NOTE above).
- **Test files:** `user/scripts/lazy-state.py` in-file `--test` harness (new fixtures) and/or `user/scripts/test_lazy_core.py`.
- **Test expectations:** runtime down → returns `booted`; runtime up + current → `ready`; runtime up + stale binary → `stale-rebuilt`; each path sets `mcp_tools_present` correctly. Driven via injected probe/now (no real network).
- **Implementation goal:** One call returns the correct structured status across all three runtime states.
- **Spec requirements:** "`--ensure-runtime`: probe `/health` → staleness check → `dev:restart` (bg) → curl-until-200 → assert MCP tool present; returns a structured status."
- **Batch:** 1 (parallel with WU-2 — both add new `lazy-state.py`/`lazy_core.py` flags but in distinct functions; if the same file makes them conflict, serialize WU-1 then WU-2).

### WU-2 — `--gate-coverage <spec_path>` (symlink-resolving Gate-1 verdict) + mcp-coverage-audit.md rewire

- **Scope:** Deliverable "`--gate-coverage <spec_path>` → deterministic Gate-1 verdict: read SPEC Locked-Decisions surface, grep `mcp-tests/*.md` resolving symlink targets (fixing the Windows 64-byte pointer-file blindspot), return covered/uncovered per decision" + "`mcp-coverage-audit.md` updated to point at `--gate-coverage`".
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy_core.py` — the symlink-resolving coverage algorithm (shared helper), promoting the `mcp-coverage-audit.md` algorithm to code. When an `mcp-tests/*.md` entry is a symlink/pointer file (64-byte text on Windows), resolve to its target before grepping.
  - `user/scripts/lazy-state.py` — `--gate-coverage` argparse arg + dispatch calling the `lazy_core` helper.
  - `user/skills/_components/mcp-coverage-audit.md` — point at `--gate-coverage` (the algorithm now lives in code) [VERIFY: ls user/skills/_components/mcp-coverage-audit.md].
- **Test files:** `user/scripts/test_lazy_core.py` with a symlink fixture.
- **Test expectations:** a SPEC with covered + uncovered Locked Decisions returns the correct covered/uncovered verdict; when an `mcp-tests/*.md` is a symlink whose target carries the keyword, coverage resolves through the pointer (the Windows 64-byte pointer-file case) rather than missing it.
- **Implementation goal:** Deterministic Gate-1 verdict, symlink-safe.
- **Spec requirements:** "`--gate-coverage`: read SPEC Locked-Decisions surface, grep `mcp-tests/*.md` (resolving symlink targets), return covered/uncovered per decision."
- **Batch:** 1 (if it conflicts with WU-1 on `lazy-state.py`/`lazy_core.py`, serialize: WU-1 Batch 1, WU-2 Batch 2).

### WU-3 — Enhance `apply_pseudo __mark_complete__`: ROADMAP strike + resolved-`spec_dir` queue trim

- **Scope:** Deliverable "`--apply-pseudo __mark_complete__ <spec_path>` enhanced: existing flip + ROADMAP strike (moved INTO apply_pseudo) + `spec_dir`-keyed queue trim (trim by RESOLVED spec_dir, not basename — killing the `-followups` queue-trim-miss class)".
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy_core.py` — extend `apply_pseudo` [VERIFY: grep -n "def apply_pseudo" user/scripts/lazy_core.py]: (a) ADD a ROADMAP strikethrough step inside the `__mark_complete__` branch (currently orchestrator-inline; move it here); (b) REFACTOR the existing queue-trim block [VERIFY: grep -n "spec_path.name" user/scripts/lazy_core.py] so the match key is the RESOLVED `spec_dir` (the queue entry's stored `spec_dir`, normalized) rather than the dir basename `spec_path.name` — so a `-followups` entry whose `spec_dir` differs from the resolved dir is still trimmed.
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** marking a feature complete strikes its ROADMAP row (assert the row becomes strikethrough+COMPLETE); a `-followups` feature whose queue `spec_dir` differs from the dir basename IS trimmed (the regression fixture — assert the entry is removed and NO `queue.no-completed`-class lingering entry remains); idempotent re-run is a no-write pass; existing flip + receipt behavior unchanged.
- **Implementation goal:** `__mark_complete__` now owns the ROADMAP strike + a resolved-`spec_dir` trim, eliminating the `-followups` recovery class.
- **Spec requirements:** Validation Criteria "`__mark_complete__` strikes ROADMAP + trims by spec_dir → ROADMAP row struck; queue trimmed by resolved spec_dir; no queue.no-completed error".
- **Batch:** 2 (distinct `lazy_core.py` function from WU-2's coverage helper, but same file — serialize after WU-2 to avoid the overlap).

### WU-4 — Rewire `/lazy-batch` (+ cloud mirror) to call the three subcommands

- **Scope:** Deliverable "Batch skills rewired: lazy-batch (+ cloud mirror) call `--ensure-runtime` / `--gate-coverage` / enhanced `--apply-pseudo __mark_complete__` instead of hand-running the dances; coupled-pair mirror maintained".
- **TDD:** no (skill-doc prose; behavior verified by the script `--test` suites + parity audit).
- **Files to create/modify:**
  - `user/skills/lazy-batch/SKILL.md` — replace the hand-run Step 1d.0 runtime dance [VERIFY: grep -n "dev:restart" user/skills/lazy-batch/SKILL.md] with a `--ensure-runtime` call; replace the Gate-1 hand-run audit with `--gate-coverage`; the `__mark_complete__` ROADMAP-strike/queue-trim now happens inside the subcommand, so remove the orchestrator-inline ROADMAP strike.
  - `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` — mirror the same rewire (coupled-pair).
- **Implementation goal:** The three dances are now single subcommand calls; the orchestrator no longer re-derives them from prose. Coupled-pair parity preserved.
- **Spec requirements:** "rewire the batch skills to call them instead of hand-running the dances. Full harness gates."
- **Batch:** 3 (after WU-1/WU-2/WU-3 — the skill calls the subcommands those WUs add).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | `lazy-state.py`/`lazy_core.py` (ensure-runtime) |
| 2 | WU-2, WU-3 | Sequential (both touch `lazy_core.py`) | WU-2 then WU-3 |
| 3 | WU-4 | Solo | `lazy-batch` + `lazy-batch-cloud` SKILLs |

> **File-overlap note:** WU-1, WU-2, WU-3 all touch `lazy-state.py`/`lazy_core.py`. They are placed in sequential batches (1 → 2 → 2) so no two same-file WUs run concurrently. WU-2 and WU-3 are sequenced WU-2-then-WU-3 within Batch 2's review window (both edit `lazy_core.py`).

## Execution Protocol

### Phase Selection Loop
Single phase. Batches in order, then Post-Phase Steps.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)
Read `~/.claude/skills/_components/task-tracking.md`. If task tools unavailable, track via `- [ ] WU-N`.

### Per-Batch Steps
- **Step B.0 — Re-read:** Read `source-reread.md`; re-read PHASES.md (Phase 5 + prior notes), SPEC.md, this plan.
- **Step B.1 — Launch:** Read `tdd-protocol.md`, `subagent-launch.md`, `tdd-test-agent.md`, `implementation-agent.md`. Dispatch (or inline). Tick `- [ ] WU-N`.
- **Step B.2 — Review (GATE):** Read `subagent-review.md` + `mount-site-verification.md`. Verdict required. Propagation check: the `apply_pseudo` signature/behavior change (WU-3) — confirm all callers (the batch skills, `lazy-state.py --apply-pseudo` dispatch) still pass correct args.
- **Step B.3 — Update PHASES.md:** Read `phases-update.md`. Check off Phase 5 deliverables; Implementation Notes incl. the `--ensure-runtime` home outcome (parameterized vs escalated).
- **Step B.4 — Quality Gates:** Read `quality-gates.md` + `.claude/skill-config/quality-gates.md`. Gates: `python3 user/scripts/lazy-state.py --test`, `python3 user/scripts/bug-state.py --test`, `python3 user/scripts/test_lazy_core.py`, `python3 user/scripts/lazy_parity_audit.py`, `python ~/.claude/scripts/project-skills.py`, `python ~/.claude/scripts/lint-skills.py --check-projected`. 100% pass.
- **Step B.4.5 — MCP:** N/A for the script subcommands themselves. The live `--ensure-runtime` runtime-state rows + `--gate-coverage` symlink-on-Windows row + the real `-followups` completion row are workstation-eligible runtime verifications → cloud-deferred (`DEFERRED_NON_CLOUD.md`), ticked by manual/Step-9 validation, NOT by `/execute-plan`.
- **Step B.5 — Commit:** Per `.claude/skill-config/commit-policy.md` (main only).
- **Step B.6 — Proceed:** Checklist.

### Post-Phase Steps
- **Integration Verification:** Read `integration-verification.md`. Confirm the three subcommands behave per their `--test` fixtures and the batch skills call them (no residual hand-run dance prose).
- **Update CLAUDE.md:** Read `claude-md-review.md`. Update `user/scripts/CLAUDE.md` CLI surface block: add `--ensure-runtime`/`--gate-coverage`; update the `--apply-pseudo __mark_complete__` line to note it now strikes ROADMAP + trims by resolved `spec_dir` (the current CLAUDE.md says "ROADMAP strikethrough … stay orchestrator-inline" — correct that line).
- **Commit and Push:** Per commit policy.

## Blocking Issue Protocol

If blocked: stop, commit completed work, print blocking report, do not work around. Blocking issues: 2-retry-exhausted subagent/gate failure; unresolvable push conflict; the `--ensure-runtime` AlgoBooth-coupling fork (write `NEEDS_INPUT.md` per the DESIGN NOTE) if the specifics cannot be cleanly parameterized out of the shared script.

## Completion

When Phase 5's batches are complete:
1. Run the full quality-gate suite one final time (all four `--test` suites + parity audit + lint + project-skills).
2. Print a completion report (WUs completed, commits, final gate status, Implementation Notes summary across all five parts).
3. This is the final part. **Completion (gate-owned):** the `__mark_complete__` gate (now enhanced) flips SPEC.md/PHASES.md `**Status:**` and writes `COMPLETED.md` after the validation tail — do NOT flip status or write the receipt from `/execute-plan`.
