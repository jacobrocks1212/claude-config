---
kind: implementation-plan
feature_id: lazy-core-package-decomposition
status: Ready
created: 2026-07-13
phases: [6]
complexity: mechanical
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-13.
> To execute: `/execute-plan C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-6.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 6 of 6. Sibling parts:
> - part 1: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-1.md`
> - part 2: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-2.md`
> - part 3: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-3.md`
> - part 4: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-4.md`
> - part 5: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-5.md`
> - part 6: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-6.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — lazy-core-package-decomposition (v2) — Part 6: Phase 6 (Lint gate + follow-up hooks)

**PHASES.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/PHASES.md`

**SPEC.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/SPEC.md`

**Total phases:** this part: Phase 6 only
**Plan version:** v2 (reference-based)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is claude-config — the gates are the Python invariant battery below, NOT `/msbuild`/`/mstest`/nx), this plan's own per-phase steps and the "Plan-specific execution notes" below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config overrides — READ FIRST)

1. **Per-commit invariant battery** (before EVERY commit; all exit 0; ZERO changes under `user/scripts/tests/baselines/`):
   ```bash
   cd C:/Users/Jacob/source/repos/claude-config
   python -m pytest user/scripts/ -q
   python3 user/scripts/lazy-state.py --test
   python3 user/scripts/bug-state.py --test
   python3 user/scripts/lazy_parity_audit.py --repo-root .
   python3 user/scripts/cli_surface_gen.py --check --repo-root .
   python3 user/scripts/doc-drift-lint.py --repo-root .
   python ~/.claude/scripts/lint-skills.py
   ```
2. **Advisory means advisory:** the ruff gate lands as a documented on-demand command producing a report and exiting informationally — it is NOT wired into the battery, any hook, or CI this phase (D6 ratified: advisory-first; flipping to enforcing is a LATER operator session, out of scope here). Do NOT add it to any gate file, settings hook, or skill contract.
3. **Commit cadence:** one commit per WU (2 commits), battery-green, directly on `main`.
4. **Importer-diff guard:** WU-1 commit ⊆ {`ruff.toml` (repo root), `user/scripts/CLAUDE.md`, root `CLAUDE.md` (scripts-table row prose only if touched), `docs/features/lazy-core-package-decomposition/PHASES.md`}; WU-2 commit ⊆ {`user/scripts/benchmark_lazy_core_import.py`, `user/scripts/tests/test_lazy_core/**` (or the benchmark's own test file if one exists), `user/scripts/CLAUDE.md`, root `CLAUDE.md`, `docs/features/lazy-core-package-decomposition/PHASES.md`}. NO `lazy_core/` production edits this phase (a ruff finding is REPORTED, not fixed — fixes are follow-up work items).
5. **Standing invariants:** as prior parts. ruff is a dev-only tool — it must NOT become a runtime import anywhere (stdlib-only runtime code is a house invariant).
6. **Halts:** `BLOCKED.md` / `NEEDS_INPUT.md` per the execution contract.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (context) | — | `docs/features/lazy-core-package-decomposition/SPEC.md` §D6/§D7 | The ratified advisory-first scope + the compute_state measurement-hook contract |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | lazy-core-package-decomposition | P6 | Ruff F-rules advisory + per-function-size hook | Phase 5 complete (part 5) | Solo |

## Work Units

- [x] WU-1 — Ruff F-rules advisory config + fixture red-check + baseline report
- [ ] WU-2 — Per-function-size measurement hook + final KPI re-measure + feature receipt notes

---

### Phase: lazy-core-package-decomposition P6 — Lint gate + follow-up hooks

**Goal:** a ruff F-rules (pyflakes-class) advisory config scoped to `user/scripts/` exists and is documented (a FORWARD guard — the headline `_current_head` F811 is already fixed, single def @6510 pre-split, guarded by `test_no_duplicate_top_level_defs_in_state_scripts`); the D7 compute_state follow-up gets its measurement baseline via a per-function-size mode on the benchmark harness; the feature's KPI proxies are re-measured and recorded.
**Entry criteria:** Phase 5 complete — `lazy_core/` is a total-map package, `_monolith.py` deleted, every seam ≤4K LoC; battery green at HEAD.
**SPEC.md references:** D6 (ratified: ruff F-rules, advisory-first, config at repo root scoped to `user/scripts/`; no style rules), D7 (measurement-only hook for `lazy-state.py::compute_state` ~2,047 ln and `bug-state.py::compute_state` ~1,099 ln), KPI Declaration proxies (1)–(4).

**Why this part is `complexity: mechanical` (per-WU justification):** WU-1 is config + documentation authoring with a fully-specified rule set (D6 locked: `F` rules only, advisory, scope `user/scripts/`) — no design decision remains. WU-2 extends an existing measurement script with a fully-specified census (top-level function LoC via `ast`, the exact two functions named by D7) plus doc/receipt rows — codegen-class work with no novel algorithm, no cross-boundary wiring, no production `lazy_core/` edits. Neither WU touches a production seam.

#### WU-1 — Ruff F-rules advisory config + fixture red-check + baseline report

- **Scope:** PHASES P6 bullet "Ruff F-rules on `user/scripts/`, advisory → enforcing. Note: headline F811 (`_current_head`) already fixed, so this is a forward guard, not a baseline-fix." (This plan lands the ADVISORY half only — enforcing is a later operator session per the ratified D6.)
- **TDD:** no in the pytest sense; the gate's own red-check is the verification: ruff MUST report F811 on a planted fixture before the config is trusted (PHASES proven-done: "gate red on a fixture F811, green on the tree" — amended by the ratification: the tree need not be F-clean; the ADVISORY report is the deliverable either way).
- **Files to create/modify:** `ruff.toml` (repo root, new), `user/scripts/CLAUDE.md` (lint-commands / conventions row documenting the advisory command), root `CLAUDE.md` "Lint Commands" block (one advisory line), `docs/features/lazy-core-package-decomposition/PHASES.md` (tick + notes).
- **Implementation goal:**
  1. Availability probe: `ruff --version` (run it — never `which`). If ruff is absent on this machine, try `python -m ruff --version`, then `pip install --user ruff`; if installation is impossible in this environment, WRITE THE CONFIG ANYWAY (it is inert without the tool), record `advisory-run: blocked — ruff unavailable on host, config landed, findings report deferred` honestly in Implementation Notes, and skip steps 3–4 (do NOT halt — advisory).
  2. `ruff.toml` at repo root: `include = ["user/scripts/**/*.py"]` (scoping the gate to the scripts plane), `lint.select = ["F"]` and nothing else (no style rules — D6: "a style gate would generate a worthless mega-diff"). Comment header naming the feature + the advisory→enforcing ratchet plan.
  3. Fixture red-check: write a THROWAWAY scratchpad file (NOT in the repo tree) with a duplicate `def` and run `ruff check --config ruff.toml <scratchpad-file>` — wait: `include` scoping would exclude a scratchpad path, so instead run the red-check as `ruff check --select F --isolated <scratchpad-file>` to prove the TOOL catches F811, and separately `ruff check .` from the repo root to prove the CONFIG resolves + scopes correctly (it must scan only `user/scripts/**`). Record both outputs.
  4. Baseline advisory report: `ruff check . --output-format concise` from the repo root; save the finding count + a category histogram into PHASES Implementation Notes (findings are EXPECTED — e.g. per-file unused imports left by the Phase-3 verbatim split; they are follow-up fodder for the later enforcing session, NOT fixed now — importer-diff guard note 4 forbids production edits this phase).
  5. Document: the advisory command in `user/scripts/CLAUDE.md` + the root `CLAUDE.md` Lint Commands block, explicitly labeled "advisory (exit code informational) — enforcing flip is a future operator decision".
- **Spec requirements:** SPEC D6-A as ratified (advisory-first); house invariant (dev/CI-only tool, not a runtime import).
- **Batch:** 1.

#### WU-2 — Per-function-size measurement hook + final KPI re-measure + feature receipt notes

- **Scope:** PHASES P6 bullet "Per-function-size measurement hook for the D7 compute_state follow-up"; SPEC Phase-6 proven-done "KPI proxies re-measured and recorded".
- **TDD:** yes (small): add a test asserting the new mode reports a plausible LoC figure (> 500) for both `compute_state` functions and includes them by name — place it in `user/scripts/tests/test_lazy_core/test_misc.py` (or the benchmark's own section there), registered per the file's `_TESTS` convention.
- **Files to create/modify:** `user/scripts/benchmark_lazy_core_import.py` (additive `--function-sizes` mode), `user/scripts/tests/test_lazy_core/test_misc.py`, `user/scripts/CLAUDE.md` + root `CLAUDE.md` (benchmark row prose: mention the new mode), `docs/features/lazy-core-package-decomposition/PHASES.md` (tick + final Implementation Notes).
- **Implementation goal:**
  1. `--function-sizes`: `ast.parse` each of `user/scripts/lazy-state.py` and `user/scripts/bug-state.py` [VERIFY: `grep -n "def compute_state" user/scripts/lazy-state.py user/scripts/bug-state.py`], report each top-level function's LoC (`end_lineno - lineno + 1`), sorted desc, flagging the two `compute_state` monoliths explicitly — the D7 follow-up's baseline, re-measurable in one command (the script's existing exit-0-always benchmark contract holds; `--dump`-style JSON via the existing `--json` flag).
  2. Final KPI proxy re-measure, recorded in PHASES Implementation Notes as the feature's closing receipt data: (a) cold `import lazy_core` ms + `--hook-surface` ms vs the 88.7/93.7 baseline and the <15 ms D4 target; (b) collection count/time (1135 preserved); (c) largest-module LoC (≤4K, `over_4k_ceiling` empty); (d) F-findings count from WU-1 (or the blocked note). State plainly which numbers met targets and which did not — honest numbers, no claims.
  3. Attempt `python3 user/scripts/kpi-scorecard.py --capture-baseline lazy-core-monolith-intervention-drag --repo-root .` for the registry row's pending baseline: if it captures, record it; if it REFUSES (no-data window for the deny-ledger selector), record the refusal verbatim — the tool never fabricates and neither does this plan.
- **Spec requirements:** SPEC D7 (measurement-only; compute_state decomposition explicitly OUT of scope — do not refactor either function), KPI Declaration notes.
- **Batch:** 2.

**Batch overview:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | CLAUDE.md files shared with WU-2 |
| 2 | WU-2 | Solo | 〃 |

**Plan-specific risk notes:** (a) do NOT let a ruff finding tempt a production "quick fix" this phase — the importer-diff guard forbids `lazy_core/` edits here; findings are follow-up items for the enforcing session; (b) `cli-surface-lint.py` scans prose `--flag` mentions against the registry — the new `--function-sizes`/`--hook-surface` flags belong to `benchmark_lazy_core_import.py`, which is NOT a roster script, so no registry regen is owed; if `lint-skills.py --check-cli-surface` were ever to flag the CLAUDE.md prose, attribute-check the sentence names the benchmark script alone (the lint only checks flags attributed to roster scripts); (c) completion machinery (SPEC status flip, COMPLETED.md, ROADMAP strike) is `__mark_complete__`-gate-owned — this plan finishes at green gates + ticked PHASES rows and writes NO completion artifacts.

**Completion (gate-owned):** after this part's commits land green, the feature's remaining lifecycle (retro / MCP-gate skip class / `__mark_complete__` receipt) belongs to the pipeline's own gates — not to this plan.
