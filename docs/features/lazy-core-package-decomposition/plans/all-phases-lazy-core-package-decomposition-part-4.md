---
kind: implementation-plan
feature_id: lazy-core-package-decomposition
status: Complete
created: 2026-07-13
phases: [4]
complexity: complex
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-13.
> To execute: `/execute-plan C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-4.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 4 of 6. Sibling parts:
> - part 1: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-1.md`
> - part 2: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-2.md`
> - part 3: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-3.md`
> - part 4: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-4.md` (this file)
> - part 5: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-5.md`
> - part 6: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-6.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — lazy-core-package-decomposition (v2) — Part 4: Phase 4 (Medium seams — write path)

**PHASES.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/PHASES.md`

**SPEC.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/SPEC.md`

**Total phases:** this part: Phase 4 only
**Plan version:** v2 (reference-based)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is claude-config — the gates are the Python invariant battery below, NOT `/msbuild`/`/mstest`/nx), this plan's own per-phase steps and the "Plan-specific execution notes" below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config overrides — READ FIRST)

1. **WRITE-PATH GATE (run FIRST, before any edit):** re-verify both hard-dep bug receipts exist — `docs/bugs/_archive/mark-complete-partial-apply-noop-unrecoverable/FIXED.md` AND `docs/bugs/_archive/production-sentinel-writes-bypass-atomic-write/FIXED.md`. If either is missing, write `BLOCKED.md` in the feature dir naming the missing receipt and STOP (SPEC Constraint 3 / PHASES P4 gate — this is the Phase-0 re-check at the first write-path move).
2. **Per-commit invariant battery** (before EVERY commit; all exit 0; ZERO changes under `user/scripts/tests/baselines/` — this phase moves the planes the byte-baselines exercise hardest, so a baseline diff means a REAL behavior change: fix the move, never the baseline):
   ```bash
   cd C:/Users/Jacob/source/repos/claude-config
   python -m pytest user/scripts/ -q
   python3 user/scripts/lazy-state.py --test
   python3 user/scripts/bug-state.py --test
   python3 user/scripts/lazy_parity_audit.py --repo-root .
   python3 user/scripts/cli_surface_gen.py --check --repo-root .
   python3 user/scripts/doc-drift-lint.py --repo-root .
   python ~/.claude/scripts/lint-skills.py
   ```
3. **Scripted-transform mandate (HARD).** Standard seam-move procedure (identical to part 2 — restated below) via throwaway scratchpad scripts; no hand-retyped bodies.
4. **Commit cadence:** ONE commit per seam (4 commits), each battery-green, directly on `main`.
5. **Importer-diff guard per commit:** file set ⊆ {`user/scripts/lazy_core/**`, `user/scripts/tests/test_lazy_core/**`} (+ `docs/features/lazy-core-package-decomposition/PHASES.md` on the final commit).
6. **Patch-redirect rule (ratified L1 mechanism 3):** re-point `lazy_core._monolith.<name>` → `lazy_core.<seam>.<name>` in the split test files for every name whose consumer-under-test moves; the suite is the mapping oracle. Cross-module reference convention for patched names as in parts 1–2 (by-value `from ._ctx import ...` + patch the consuming module; stdlib per-module imports).
7. **Standing invariants:** `lazy_coord.py` never imports `lazy_core`; `phases-slice.py` untouched; `_ctx`/`statedir` never import `_monolith`; zero baseline regeneration.
8. **Halts:** `BLOCKED.md` / `NEEDS_INPUT.md` per the execution contract.

### Standard seam-move procedure (as part 2)

(1) Re-locate the seam's ranges in `_monolith.py` by section-header/def grep (RESEARCH map is the guide; numbers are stale by construction). (2) Scratchpad script slices the ranges VERBATIM into `lazy_core/<seam>.py` (docstring + own stdlib imports + `from ._ctx import ...` + inter-seam imports per note 6), deletes them from `_monolith.py`, inserts import-backs for names remaining monolith code still references. (3) Script AST-enumerates the seam's top-level names and appends `_SUBMODULE_BY_NAME` entries + `_ALL_SUBMODULES` membership in `__init__.py`; collision = hard error. (4) Scripted test redirect in `user/scripts/tests/test_lazy_core/*.py`. (5) `lazy_core.load_all()` import check + full battery; collected count 1135 + identical name set. (6) Commit `refactor(lazy-core): Phase 4 — extract <seam>.py (move-only)` + importer-diff guard.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| mark-complete-partial-apply-noop-unrecoverable | hard | `docs/bugs/_archive/mark-complete-partial-apply-noop-unrecoverable/FIXED.md` | The write-path receipt this phase's entry gate re-verifies |
| production-sentinel-writes-bypass-atomic-write | hard | `docs/bugs/_archive/production-sentinel-writes-bypass-atomic-write/FIXED.md` | The write-path receipt this phase's entry gate re-verifies |
| (context) | — | `docs/features/lazy-core-package-decomposition/RESEARCH_SUMMARY.md` | Section map for the four medium seams |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | lazy-core-package-decomposition | P4 | Medium seams (gates, ledgers, dispatch, runtimeplane) | Phase 3 complete (part 3) + receipt re-check | Solo |

## Work Units

- [x] WU-1 — Receipt re-check + extract `gates.py` (evidence/coherence/ledger-verify)
- [x] WU-2 — Extract `ledgers.py` (provenance + deny ledger + telemetry/efficacy + canary)
- [x] WU-3 — Extract `dispatch.py` (cycle/dispatch prompts + prompt registry)
- [x] WU-4 — Extract `runtimeplane.py` (ensure_runtime + transient build) + phase receipt

---

### Phase: lazy-core-package-decomposition P4 — Medium seams

**Goal:** the four medium-risk planes (`gates`, `ledgers`, `dispatch`, `runtimeplane`) leave `_monolith.py`, with the smoke byte-baselines proven byte-identical at every commit — the first write-path moves, sanctioned by the two archived bug receipts.
**Entry criteria:** Phase 3 complete (split test tree live); WRITE-PATH GATE (note 1) passed this session.
**SPEC.md references:** D3 (medium tier), D2 Constraint 3 (write-path sequencing), Validation Criteria "Zero behavior change" row; RESEARCH section map.

**Verified ground truth (pre-Phase-1 anchors — re-locate by pattern):** gates/evidence 3052–3623 (`evaluate_completion_evidence`@3257, `autotick_verification_rows`), ledger verify 3624–3953 (`verify_ledger`@~4090) [VERIFY: `grep -n "def evaluate_completion_evidence\|def autotick_verification_rows\|def verify_ledger" user/scripts/lazy_core/_monolith.py`]; dispatch/prompt plane 6687–7522 + prompt registry 12171–13002 (`emit_cycle_prompt`, `_load_registry` consumers, skill-path resolution — note the two `_SCRIPTS_DIR` call sites from Phase 1 move with it) [VERIFY: `grep -n "def emit_cycle_prompt\|_CYCLE_TEMPLATE_DIRNAME" user/scripts/lazy_core/_monolith.py`]; runtime plane 8231–10064 (`set_active_repo_root` region already `_ctx`-backed, `ensure_runtime`@~9522, transient build, `spawn_detached`, `write_runtime_lock`) [VERIFY: `grep -n "def ensure_runtime\|def run_transient_build\|def write_runtime_lock\|def spawn_detached" user/scripts/lazy_core/_monolith.py`]; provenance/deny ledger 14183–15585 + telemetry/efficacy 15585–15985 + canary 16183–… (`append_deny_ledger_entry`, `read_deny_ledger`, `ack_oldest_deny`, `write_provenance`, `_TELEMETRY_*`, `_CANARY_CONTROL_SURFACES_FALLBACK`) [VERIFY: `grep -n "def append_deny_ledger_entry\|def write_provenance\|_TELEMETRY_ROTATE_BYTES\|_CANARY_CONTROL_SURFACES_FALLBACK" user/scripts/lazy_core/_monolith.py`].

#### WU-1 — Receipt re-check + extract `gates.py`

- **Scope:** PHASES P4 bullet (gates quarter): completion-evidence gate (`evaluate_completion_evidence`), autotick (`autotick_verification_rows` + `_UNCHECKED_ROW_RE`/`_AUTOTICK_COMMENT_PREFIX`), `verify_ledger`, coherence helpers — the contiguous verified gates ranges.
- **TDD:** no (move-only; the receipt re-check is a gate, not a test).
- **Files to create/modify:** `user/scripts/lazy_core/gates.py` (new), `_monolith.py`, `__init__.py`, `user/scripts/tests/test_lazy_core/*.py` (redirects — likely `test_gates.py` sites: `_atomic_write` spies around autotick, `time`/`os` per consumer).
- **Implementation goal:** execute note-1's WRITE-PATH GATE FIRST (halt on a missing receipt). Then the standard seam-move procedure, seam = `gates`. `evaluate_completion_evidence` reads sentinels via docmodel parsers (`from . import docmodel` / `from .docmodel import parse_sentinel` per the patched-name convention — `parse_sentinel` is not in the patched census, by-value import is fine). `autotick_verification_rows` writes via `_atomic_write` (from `_ctx` — the write-path bug fix landed on this code pre-split; the receipts certify it). `_VERIFICATION_ONLY_MARKER` consumers split across docmodel/gates — the marker constant stays where Phase 2 put it; gates imports it; the test patch site targets the module whose recognizer the test drives (suite decides).
- **Spec requirements:** SPEC D3, Constraint 3; PHASES P4.
- **Batch:** 1.

#### WU-2 — Extract `ledgers.py`

- **Scope:** PHASES P4 bullet (ledgers quarter): deny ledger (`append_deny_ledger_entry`, `read_deny_ledger`, `ack_oldest_deny`, `ack_deny_by_selector`), provenance (`write_provenance` + index), telemetry/efficacy (ledger append, `_TELEMETRY_*`, intervention capture constants `INTERVENTION_*`, `_INTERVENTIONS_DIRNAME`), the canary block (incl. `_CANARY_CONTROL_SURFACES_FALLBACK` — carrying the Phase-1-updated `lazy_core/**` glob literal with it).
- **TDD:** no (move-only).
- **Files to create/modify:** `user/scripts/lazy_core/ledgers.py` (new), `_monolith.py`, `__init__.py`, split test files (redirects: `_TELEMETRY_ROTATE_BYTES`(2), `_TELEMETRY_LEDGER_FILENAME`, `_INTERVENTIONS_DIRNAME`, the 4 `INTERVENTION_*` constants, `ack_oldest_deny`(2) → `lazy_core.ledgers.*` where the consumer moved; `time` sites whose consumers are ledger appenders).
- **Implementation goal:** standard seam-move procedure, seam = `ledgers`. State-dir paths come from `statedir` (`from .statedir import claude_state_dir` — un-patched, by-value fine); writes via `_ctx._atomic_write` import. NOTE: `ack_oldest_deny` is also called by `lazy_guard.py` via `lazy_core.ack_oldest_deny(...)` (facade attribute access [VERIFY: `grep -n "ack_oldest_deny" user/scripts/lazy_guard.py`]) — the facade map entry keeps that working with zero hook edits; the hook's per-fire cost now loads `ledgers` only on that branch (record in the receipt if measured).
- **Spec requirements:** SPEC D3; PHASES P4.
- **Batch:** 2.

#### WU-3 — Extract `dispatch.py`

- **Scope:** PHASES P4 bullet (dispatch quarter): cycle/dispatch prompt emission (`emit_cycle_prompt`, cycle-template resolution via `_SCRIPTS_DIR`, `_mcp_test_cycle_model`, `skill_declares_multi_commit`/`skill_declares_subagent_model` skill-frontmatter readers, `_CYCLE_COMMIT_*` budget constants) + the prompt registry block (registry read/write, nonce mint/consume glue that is NOT marker-plane — `consume_nonce` itself: verify where it lives; if it sits inside the marker/ownership range 10600–12330 it does NOT move this phase).
- **TDD:** no (move-only).
- **Files to create/modify:** `user/scripts/lazy_core/dispatch.py` (new), `_monolith.py`, `__init__.py`, split test files (redirects: `_CYCLE_COMMIT_MULTI`, `_CYCLE_COMMIT_BUDGET_DEFAULT` → `lazy_core.dispatch.*` where consumers moved; `time`/`os` per consumer).
- **Implementation goal:** standard seam-move procedure, seam = `dispatch`. The registry LOADER `_load_registry` moved to `statedir` in Phase 2 — dispatch imports it (`from .statedir import _load_registry`); registry WRITERS (nonce registration at emit time) move here if they live in the verified dispatch/prompt-registry ranges. **Boundary discipline:** anything inside the marker/ownership/refusals range (10600–12330, e.g. `refuse_if_cycle_active`, `read_run_marker`, `bind_marker_session`, `consume_nonce` if so located) STAYS in `_monolith` for Phase 5 — re-slice around it; a dispatch function needing it uses a function-local deferred `from ._monolith import <name>` with a `# Phase-5 re-point` comment (record each).
- **Spec requirements:** SPEC D3 (marker plane last); PHASES P4.
- **Batch:** 3.

#### WU-4 — Extract `runtimeplane.py` + phase receipt

- **Scope:** PHASES P4 bullet (runtime quarter): `ensure_runtime` + the M4 classifier/recovery helpers, transient-build contract (`run_transient_build`, `promote_artifact_atomically`), `spawn_detached`, `write_runtime_lock`, `verify_runtime_ownership`, `kernel_start_time`, the `_ENSURE_RUNTIME_DEFAULT_CONFIG` block — the verified 8231–10064 ranges; then the phase receipt.
- **TDD:** no (move-only; the production-binding test conventions are the net — see risk note c).
- **Files to create/modify:** `user/scripts/lazy_core/runtimeplane.py` (new), `_monolith.py`, `__init__.py`, split test files (redirects: `write_runtime_lock`(2), `subprocess`(1), most of the 14 `time` sites (runtime backoff/liveness consumers), `os` sites per consumer → `lazy_core.runtimeplane.*`), `docs/features/lazy-core-package-decomposition/PHASES.md` (tick P4 row + Implementation Notes).
- **Implementation goal:** standard seam-move procedure, seam = `runtimeplane`. **The production-binding meta-guards** (`test_ensure_runtime_production_tests_derive_not_inject_signal`, `test_spawn_binding_production_tests_use_faithful_double` — living in the split `test_runtimeplane.py` since Phase 3) AST-scan for tests patching `lazy_core.subprocess`/`lazy_core.time` shapes — the Phase-1/Phase-4 redirects changed those tokens to `lazy_core.runtimeplane.subprocess` etc.; UPDATE the collectors' matched token patterns in the same commit so the guards keep auditing the same population (a guard matching zero tests is a dead guard — assert the collectors still find a non-empty population; their negative-fixture twins prove non-vacuity). Phase receipt into PHASES Implementation Notes: per-seam LoC (`python3 user/scripts/benchmark_lazy_core_import.py --repo-root .` census), remaining `_monolith.py` LoC, smoke-baseline statement ("byte-identical — zero regeneration"), deferred `# Phase-5 re-point` inventory. Final commit carries the PHASES edit.
- **Spec requirements:** SPEC D3; PHASES P4 proven-done ("smoke baselines byte-identical").
- **Batch:** 4.

**Batch overview:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | all share `_monolith.py`/`__init__.py`/split tests |
| 2 | WU-2 | Solo | 〃 |
| 3 | WU-3 | Solo | 〃 |
| 4 | WU-4 | Solo | 〃 + PHASES.md |

**Plan-specific risk notes:** (a) these planes are what the byte-baselines exercise — any `--test` output diff is a real regression in the move (typically a lost import or a patched-name resolution flip): debug the move, never touch `tests/baselines/`; (b) `apply_pseudo` (4738–6099) is NOT in this phase — it is Phase 5's `pseudo.py`; if a gates/ledgers range abuts it, slice AROUND it; (c) the runtimeplane meta-guard token update (WU-4) is the one sanctioned edit to guard code — it RETARGETS, never weakens (population must stay non-empty; negative fixtures stay red-catching); (d) `harness-gate.py --staged` will flag these commits in-scope (control surfaces) — extend the feature-dir `GATE_VERDICT.md` with a per-phase entry (move-only rationale, receipts cited).
