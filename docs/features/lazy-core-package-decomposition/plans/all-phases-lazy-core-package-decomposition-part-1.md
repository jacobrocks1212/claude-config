---
kind: implementation-plan
feature_id: lazy-core-package-decomposition
status: Complete
created: 2026-07-13
phases: [1]
complexity: complex
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-13.
> To execute: `/execute-plan C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-1.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 6. Sibling parts:
> - part 1: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-1.md` (this file)
> - part 2: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-2.md`
> - part 3: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-3.md`
> - part 4: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-4.md`
> - part 5: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-5.md`
> - part 6: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-6.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — lazy-core-package-decomposition (v2) — Part 1: Phase 1 (Facade + `_ctx` skeleton)

**PHASES.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/PHASES.md` (lazy-core-package-decomposition, 7 phases; Phase 0 complete)

**SPEC.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/SPEC.md`

**Total phases:** 6 remaining (this part: Phase 1)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is claude-config — the gates are the Python invariant battery below, NOT `/msbuild`/`/mstest`/nx), this plan's own per-phase steps and the "Plan-specific execution notes" below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config overrides — READ FIRST)

1. **Quality gates for this repo = the per-commit invariant battery.** Run ALL of these before EVERY commit this plan makes; every one must be green (exit 0):
   ```bash
   cd C:/Users/Jacob/source/repos/claude-config
   python -m pytest user/scripts/ -q                      # full suite incl. the 1135-test lazy_core suite
   python3 user/scripts/lazy-state.py --test              # byte-pinned smoke baseline
   python3 user/scripts/bug-state.py --test               # byte-pinned smoke baseline
   python3 user/scripts/lazy_parity_audit.py --repo-root .
   python3 user/scripts/cli_surface_gen.py --check --repo-root .
   python3 user/scripts/doc-drift-lint.py --repo-root .
   python ~/.claude/scripts/lint-skills.py
   ```
   **ZERO baseline regeneration:** `git status --porcelain` must show NO change under `user/scripts/tests/baselines/` at any commit. If a baseline "needs" regenerating, the change broke behavior — fix the change, never the baseline.
2. **Scripted-transform mandate (HARD).** `lazy_core.py` is 20,172 lines and `test_lazy_core.py` is 37,842 lines. NO agent may hand-retype code bodies. Every move/rewrite is a throwaway Python script written to the session scratchpad directory (NEVER committed) that slices line ranges / performs anchored regex rewrites, plus `git mv` for whole-file moves. Each transform script prints a receipt (before/after occurrence counts, line counts) that the WU records. Targeted small edits (< ~20 lines) may use the Edit tool directly.
3. **Commit cadence:** this phase lands as exactly TWO commits on `main` (no branches, no PRs — repo policy): commit **1a** (WU-1..WU-4: package + facade + `_ctx` + L2 + test redirects + doc row + control-surface manifest) and commit **1b** (WU-5: `load_all()` wiring + PHASES.md ticks + Implementation Notes). The full battery runs green before EACH. Intermediate WU boundaries within 1a are expected to be red (the body move and the patch redirect are only jointly green) — do NOT commit between them.
4. **Importer-diff guard (per commit):** after each commit run `git show --name-only --format= HEAD` and assert the file set is a subset of that commit's allowed set (listed per WU below). Any extra file = stop, `git reset --soft HEAD~1`, fix, recommit.
5. **Patch-redirect rule (ratified L1 mechanism 3):** a test that patches a collaborator by module-attribute assignment must target **the module whose function-under-test resolves that name** (its defining module), not the facade. At Phase 1 the defining module for everything is `lazy_core._monolith` (except `_ctx`-owned globals). Python resolves free names from the defining module's globals, so a facade-level assignment silently stops affecting moved code — that is landmine L1.
6. **Standing invariants:** `user/scripts/lazy_coord.py` must NOT import `lazy_core` (do not touch it); `user/scripts/phases-slice.py` keeps its byte-identical private `_PHASE_HEADING_RE` copy (do not touch it); `user/scripts/tests/baselines/*` untouched.
7. **Halts:** any unresolvable red gate or design fork → write `BLOCKED.md` / `NEEDS_INPUT.md` in the feature dir per the execution contract. Never weaken a gate, never regenerate a baseline, never delete a run marker.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| mark-complete-partial-apply-noop-unrecoverable | hard | `docs/bugs/_archive/mark-complete-partial-apply-noop-unrecoverable/FIXED.md` | Write-path precondition receipt (verified present, Phase 0) — Phase 4+ gate re-checks it |
| production-sentinel-writes-bypass-atomic-write | hard | `docs/bugs/_archive/production-sentinel-writes-bypass-atomic-write/FIXED.md` | Write-path precondition receipt (verified present, Phase 0) — Phase 4+ gate re-checks it |
| (context) | — | `docs/features/lazy-core-package-decomposition/RESEARCH_SUMMARY.md` | Measured inventory, section map, the three landmines L1/L2/L3 — the authoritative anchors (SPEC's 2026-07-11 anchors are stale) |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | lazy-core-package-decomposition | P1 | Facade + `_ctx` skeleton | — (Phase 0 complete) | Solo — all WUs sequential (shared files) |

## Work Units

- [x] WU-1 — Package skeleton: body move + PEP 562 facade + scripted patch-target redirect
- [x] WU-2 — `_ctx.py`: mutable-global + shared-kernel hoist with identity/patchability tests
- [x] WU-3 — L2 `_SCRIPTS_DIR` anchor: repoint the six `__file__`-relative lookups
- [x] WU-4 — Doc row + control-surface manifest + gate verdict
- [x] WU-5 — Commit 1a battery; `load_all()` wiring (commit 1b); benchmark re-run + honest numbers

---

### Phase: lazy-core-package-decomposition P1 — Facade + `_ctx` skeleton

**Goal:** `lazy_core.py` becomes the `user/scripts/lazy_core/` package with the whole body in `lazy_core/_monolith.py` behind a PEP 562 lazy facade, `_ctx.py` owning the mutable globals + shared kernel, the L2 path anchor in place, and all 20 importers + 2 hooks + auditors green with zero edits — proving the facade contract BEFORE any seam moves.
**Entry criteria:** Phase 0 complete (✅ — receipts verified, benchmark landed, baseline stamped). Working tree clean on `main`.
**SPEC.md references:** Decision Ledger (D1 ratified: mechanism 3; D4 ratified: PEP 562), D2 (L2/L3 first-commit obligations), Technical Design tree, RESEARCH_SUMMARY "Three landmines".

**Verified ground truth this phase builds on** (all verified 2026-07-13 against HEAD `c0124ff8`):
- Patch-site census in `test_lazy_core.py` — 43 assignment sites over 25 names [VERIFY: `grep -oE "lazy_core\.[A-Za-z_]+ =" user/scripts/test_lazy_core.py | sort | uniq -c`]:
  `time`(14), `os`(6), `write_runtime_lock`(2), `consume_nonce`(2), `bind_marker_session`(2), `ack_oldest_deny`(2), `_legacy_state_migrated`(2), `_atomic_write`(2), `_TELEMETRY_ROTATE_BYTES`(2), `subprocess`(1), and 15 single-site constants (`_VERIFICATION_ONLY_MARKER`, `_TELEMETRY_LEDGER_FILENAME`, `_NOTIFY_SEND_TIMEOUT_SECONDS`, `_NOTIFY_LEDGER_FILENAME`, `_NOTIFY_ERROR_FILENAME`, `_NOTIFY_CONFIG_FILENAME`, `_NOTIFY_CLEAN_STOP_TERMINALS`, `_NOTIFY_ATTENTION_TERMINALS`, `_INTERVENTIONS_DIRNAME`, `_CYCLE_COMMIT_MULTI`, `_CYCLE_COMMIT_BUDGET_DEFAULT`, `INTERVENTION_REVIEW_AFTER_RUNS`, `INTERVENTION_MIN_SAMPLE`, `INTERVENTION_BASELINE_RUNS`, `INTERVENTION_BAND_PCT`). No `monkeypatch.setattr(lazy_core`/`patch.object(lazy_core` anywhere [VERIFY: `grep -rn "monkeypatch.setattr(lazy_core\|patch.object(lazy_core" user/scripts/` → zero hits].
- Six `__file__`-relative sites in `lazy_core.py` at lines 3197, 4092, 7776, 7988, 12361, 12456 [VERIFY: `grep -n "__file__" user/scripts/lazy_core.py`] — the PHASES bullet's line anchors (3141/3975/7659/7871/12244/12339) are STALE; use the pattern, not the number.
- Rebindables: `_active_repo_root`@8787 (rebound only in `set_active_repo_root`@8803-8810), `_legacy_state_migrated`@8790 (rebound @11694-11697) [VERIFY: `grep -n "_legacy_state_migrated\|_active_repo_root" user/scripts/lazy_core.py`]. `_DIAGNOSTICS`@~88, `_atomic_write`@111, `clear_diagnostics`@102.
- Path-reading tests needing accommodation: `test_no_duplicate_top_level_defs_in_state_scripts` (~line 38104) reads `scripts_dir / "lazy_core.py"` by literal path; test @6222 derives the scripts dir as `Path(lazy_core.__file__).resolve().parent` [VERIFY: `grep -n 'lazy_core.py"\|lazy_core\.__file__' user/scripts/test_lazy_core.py`].
- Control-surface manifest lists the literal `"user/scripts/lazy_core.py"` at `docs/gate/control-surfaces.json:8` AND in the in-body fallback constant `_CANARY_CONTROL_SURFACES_FALLBACK` (lazy_core.py:18491) [VERIFY: `grep -n "lazy_core.py" docs/gate/control-surfaces.json user/scripts/lazy_core.py | grep -i canary -A2`].
- `benchmark_lazy_core_import.py` is ALREADY package-aware (monolith-or-`lazy_core/` census, tests-dir collection fallback) — no accommodation owed this phase [VERIFY: `grep -n "lazy_core" user/scripts/benchmark_lazy_core_import.py`].
- Both state scripts do `import lazy_core` + broad `from lazy_core import (...)` (lazy-state.py:79-80, bug-state.py:97-98) [VERIFY: `grep -n "^import lazy_core\|^from lazy_core import" user/scripts/lazy-state.py user/scripts/bug-state.py`].

#### WU-1 — Package skeleton: body move + PEP 562 facade + scripted patch-target redirect

- **Scope:** PHASES P1 deliverable "Create `user/scripts/lazy_core/` package; move the monolith body into it behind the ratified facade so `import lazy_core`, `from lazy_core import _atomic_write`, `lazy_core.notify_halt(...)`, and module-attribute monkeypatching all keep working byte-identically."
- **TDD:** no (behavior-preserving move gated by the existing 1135-test suite + byte baselines; WU-2 adds the new tests).
- **Files to create/modify:** `user/scripts/lazy_core/__init__.py` (new, ~80 lines hand-authored), `user/scripts/lazy_core/_monolith.py` (via `git mv` — zero content edits in this WU), `user/scripts/test_lazy_core.py` (scripted rewrite only).
- **Test files:** `user/scripts/test_lazy_core.py` (scripted patch-target redirect + two small path accommodations).
- **Implementation goal (mechanism — follow exactly):**
  1. `mkdir user/scripts/lazy_core && git mv user/scripts/lazy_core.py user/scripts/lazy_core/_monolith.py`. Zero edits to the moved body in this WU.
  2. Author `user/scripts/lazy_core/__init__.py` — the permanent PEP 562 lazy facade:
     - Module docstring stating the facade contract (byte-compatible re-export of every public + used-private name; permanent, not transitional).
     - `_SUBMODULE_BY_NAME: dict[str, str] = {}` — explicit name→submodule overrides (empty at Phase 1; later phases' splitter scripts append entries).
     - `_FALLBACK_SUBMODULE = "_monolith"`.
     - `def __getattr__(name):` → resolve owning submodule from the map (fallback `_monolith`), `importlib.import_module(f".{mod}", __name__)`, `getattr(mod, name)`; on miss raise `AttributeError(f"module 'lazy_core' has no attribute {name!r}")`. **CRITICAL: never memoize the looked-up attribute into the facade's globals** — caching would freeze patch visibility (a later `_monolith.X = fake` patch must be seen by the next `lazy_core.X` read). Only the submodule import itself is cached (by `sys.modules`, automatically).
     - `def __dir__():` → union of facade globals, map keys, and `dir(_monolith)` (import-on-demand).
     - `def load_all():` → import every submodule in the package (iterate a module-level `_ALL_SUBMODULES` tuple; at Phase 1: `("_monolith",)`), for eager consumers (state-script `main()`, WU-5) so ImportError timing stays at process start for them.
  3. **Scripted patch-target redirect** (throwaway script in the scratchpad; do NOT commit it): for each of the 25 patched names in the census above, rewrite every occurrence of `lazy_core.<name>` in `user/scripts/test_lazy_core.py` to `lazy_core._monolith.<name>` (regex per name: `\blazy_core\.(?=<name>\b)` → `lazy_core._monolith.`; idempotence guard: skip lines already containing `lazy_core._monolith.`). This covers assignment sites AND their paired save/restore reads mechanically. Rewriting a plain read/call this way is semantically identical (the facade forwards to `_monolith`), so the blanket per-name rewrite is safe and grep-auditable. The script prints per-name before/after counts — record them in the WU report; totals must match the census (43 assignment sites; reads additional).
  4. **Two path accommodations** (Edit tool, small):
     - `test_no_duplicate_top_level_defs_in_state_scripts` (~38104): replace the `("lazy_core.py", "lazy-state.py", "bug-state.py")` file list with an expansion that iterates `sorted((scripts_dir / "lazy_core").glob("*.py"))` plus the two state scripts, asserting zero duplicate top-level defs PER MODULE. (Cross-module same-name defs are legal; the per-module F811 class is what the guard pins.)
     - Test @6222: `Path(lazy_core.__file__).resolve().parent / "lazy-state.py"` → `Path(lazy_core.__file__).resolve().parent.parent / "lazy-state.py"` (the facade's `__file__` is now `lazy_core/__init__.py`, one level deeper). (WU-3 exposes `_SCRIPTS_DIR`; this line may be re-pointed at it then, but the `.parent.parent` form is sufficient and local.)
  5. Sweep for any OTHER test reading `lazy_core.py` by literal path or via `lazy_core.__file__` (`grep -n 'lazy_core\.py\|lazy_core\.__file__' user/scripts/test_lazy_core.py user/scripts/test_hooks.py user/scripts/test_pipeline_visualizer.py user/scripts/test_efficacy_eval.py user/scripts/test_project_skills.py`) — fixture-tree writes of a FAKE `user/scripts/lazy_core.py` inside temp dirs (e.g. ~20127, ~33696, ~35034) are synthetic-path strings and stay UNTOUCHED; only reads of the REAL file need the WU-1.4 treatment.
  6. Do NOT run the full battery yet (the six `__file__` sites are broken until WU-3) — run `python -m pytest user/scripts/test_lazy_core.py -q 2>&1 | tail -5` for a progress signal only; expect failures confined to path-derived tests until WU-3.
- **Spec requirements:** SPEC D1 option A (package + permanent facade), D4 option A (PEP 562), RESEARCH L1 (the redirect is the ratified mechanism-3 obligation: "test patch-target lines are the sanctioned edit surface, 1135-test count + names preserved").
- **Batch:** 1 (solo).

#### WU-2 — `_ctx.py`: mutable-global + shared-kernel hoist with identity/patchability tests

- **Scope:** PHASES P1 deliverable "L3 fix: `_ctx.py` owns `_DIAGNOSTICS` (same list object), `_active_repo_root` + `_legacy_state_migrated` behind getter/setter; identity test `lazy_core._DIAGNOSTICS is lazy_core._ctx._DIAGNOSTICS` + a mutate-through-facade visibility test."
- **TDD:** yes — write the identity/patchability tests FIRST (they fail against the pre-`_ctx` tree), then extract.
- **Files to create/modify:** `user/scripts/lazy_core/_ctx.py` (new), `user/scripts/lazy_core/_monolith.py` (scripted slice-out + import line), `user/scripts/lazy_core/__init__.py` (eager `_ctx` import + map entries), `user/scripts/test_lazy_core.py` (new tests + 2-site redirect).
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations (register each in a `_TESTS` block per the file's convention):**
  - `test_ctx_diagnostics_identity`: `lazy_core._DIAGNOSTICS is lazy_core._ctx._DIAGNOSTICS is lazy_core._monolith._DIAGNOSTICS` — the canonical-list-object contract (`lazy-state.py` mutates that list in place [VERIFY: `sed -n '60,80p' user/scripts/lazy-state.py`]).
  - `test_ctx_mutation_visible_through_facade`: append a marker string via `lazy_core._DIAGNOSTICS.append(...)`, assert a `_monolith`-internal reader (`lazy_core.clear_diagnostics()` then a `_diag`-appending call, or direct list identity read) observes/clears the SAME list.
  - `test_ctx_rebindable_globals_via_accessors`: `lazy_core._ctx.set_legacy_state_migrated(True)` is observed by `lazy_core._ctx.legacy_state_migrated()` AND by `_monolith`'s migration guard path; direct module-attribute patching `lazy_core._ctx._legacy_state_migrated = False` also works (the getter reads the module global).
  - `test_monolith_patch_target_effective` (the mechanism-3 regression pin): patch `lazy_core._monolith.time` with a fake, call a `_monolith` function that reads `time.time()` (reuse an existing cheap one from the suite's own patch sites), assert the fake was consulted; restore.
- **Implementation goal:** Scripted slice-out (scratchpad script) of the `_ctx`-owned block from `_monolith.py` into `_ctx.py`: `_DIAGNOSTICS` list def, `_diag()`, `clear_diagnostics()`, `_atomic_write()` (the shared kernel — every future seam needs it WITHOUT importing `_monolith`, or the D4 hook cut dies), plus new storage + accessors for the rebindables: `_active_repo_root` storage with `get_active_repo_root()`/`set_active_repo_root_value()` module functions, `_legacy_state_migrated` storage with `legacy_state_migrated()`/`set_legacy_state_migrated()`. In `_monolith.py`: `from ._ctx import _DIAGNOSTICS, _diag, clear_diagnostics, _atomic_write` at the top of the sliced region; rewrite the enumerated rebind/read sites (`set_active_repo_root` body @~8803-8810, `active_repo_root` read @~8816-8817, migration guard @~11694-11697, fallback read @~17702 — re-locate by pattern, the numbers shift after the WU-1 move) to call the `_ctx` accessors — these are the L3-sanctioned logic edits, nothing else changes. **Verify `clear_diagnostics` clears IN PLACE (`del _DIAGNOSTICS[:]` / `.clear()`), never rebinds** — rebinding would fork the canonical list. Facade `__init__.py`: eager `from ._ctx import _DIAGNOSTICS` plus `_SUBMODULE_BY_NAME` entries for `_DIAGNOSTICS`/`_diag`/`clear_diagnostics`/`_atomic_write` → `_ctx`; add `_ctx` to `_ALL_SUBMODULES`. Scripted test redirect: the 2 `lazy_core._monolith._legacy_state_migrated =` sites (post-WU-1 spelling) → `lazy_core._ctx._legacy_state_migrated =`; the 2 `lazy_core._monolith._atomic_write =` patch sites STAY pointed at `_monolith` (their consumers-under-test still live there — the patch-redirect rule targets the resolving module, not the defining one; `_monolith`'s `from ._ctx import _atomic_write` binding is exactly what those patches rebind).
- **Spec requirements:** SPEC D2 Constraint 1 (`_ctx` first), RESEARCH L3.
- **Batch:** 2 (sequential — shares `_monolith.py`/test file with WU-1).

#### WU-3 — L2 `_SCRIPTS_DIR` anchor: repoint the six `__file__`-relative lookups

- **Scope:** PHASES P1 deliverable "L2 fix (required, not optional): expose a `_SCRIPTS_DIR` anchor and repoint the six `__file__`-relative lookups at it."
- **TDD:** no (existing tests covering harness-gate handoff / validate-plan shell-out / cycle-template resolution / skill-path resolution are the net — they FAIL before this WU and pass after; that red→green is the evidence).
- **Files to create/modify:** `user/scripts/lazy_core/_ctx.py` (add `_SCRIPTS_DIR = Path(__file__).resolve().parent.parent` — resolves to `user/scripts/`), `user/scripts/lazy_core/_monolith.py` (six targeted edits), `user/scripts/lazy_core/__init__.py` (map entry `_SCRIPTS_DIR` → `_ctx`), `user/scripts/test_lazy_core.py` (optionally re-point the WU-1.4 `.parent.parent` fix at `lazy_core._SCRIPTS_DIR`).
- **Implementation goal:** In `_monolith.py`, locate by PATTERN (`grep -n "__file__" user/scripts/lazy_core/_monolith.py` — six hits, pre-move lines 3197/4092/7776/7988/12361/12456) and rewrite each:
  - `Path(__file__).resolve().parent / "harness-gate.py"` → `_SCRIPTS_DIR / "harness-gate.py"`
  - `Path(__file__).resolve().parent / "validate-plan.py"` → `_SCRIPTS_DIR / "validate-plan.py"`
  - `Path(__file__).resolve().parent.parent.joinpath(*_CYCLE_TEMPLATE_DIRNAME)` → `_SCRIPTS_DIR.parent.joinpath(*_CYCLE_TEMPLATE_DIRNAME)`
  - `_here = Path(__file__).parent` (@~7988) → `_here = _SCRIPTS_DIR` (READ THE SURROUNDING LINES FIRST — preserve exactly what `_here` is used for; it must keep resolving to `user/scripts/`)
  - 2× `Path(__file__).resolve().parent.parent / "skills" / norm / "SKILL.md"` → `_SCRIPTS_DIR.parent / "skills" / norm / "SKILL.md"` (`user/skills/`)
  with `from ._ctx import _SCRIPTS_DIR` added to the `_monolith` import line from WU-2. After this WU: `python -m pytest user/scripts/test_lazy_core.py -q` must be FULLY GREEN with collected count == 1135 and the collected NAME SET identical to pre-Phase-1 (capture `python -m pytest user/scripts/test_lazy_core.py --collect-only -q` before WU-1 and diff now — the count+names receipt PHASES requires per move commit).
- **Spec requirements:** RESEARCH L2 (all six sites break when `__file__` shifts one directory down); PHASES P1 L2 bullet.
- **Batch:** 3 (sequential).

#### WU-4 — Doc row + control-surface manifest + gate verdict

- **Scope:** PHASES P1 deliverable "Update the `user/scripts/CLAUDE.md` `lazy_core.py` row → `lazy_core/` package in the SAME commit (doc-drift-lint doc→disk gate)" + the audit-discovered same-commit obligation: the harness-gate control-surface manifest must track the rename or the gate silently loses scope over the state-machine core.
- **TDD:** no (docs + manifest; `doc-drift-lint.py` and the battery are the net).
- **Files to create/modify:** `user/scripts/CLAUDE.md`, `docs/gate/control-surfaces.json`, `user/scripts/lazy_core/_monolith.py` (one literal in `_CANARY_CONTROL_SURFACES_FALLBACK`), `docs/features/lazy-core-package-decomposition/GATE_VERDICT.md` (new).
- **Implementation goal:**
  1. `user/scripts/CLAUDE.md` "Files in this directory" table: change the `lazy_core.py` row's file cell to `lazy_core/` (trailing slash — `doc-drift-lint.py` validates dir rows via `is_dir()` [VERIFY: `grep -n "is_dir() if name.endswith" user/scripts/doc-drift-lint.py`]); update the row prose minimally (package of submodules behind a byte-compatible PEP 562 facade; body currently in `_monolith.py`, seams extracted in later phases).
  2. `docs/gate/control-surfaces.json`: replace `"user/scripts/lazy_core.py"` with `"user/scripts/lazy_core/**"` (scope-preserving-and-widening — the package superset of the old single file). This file is `gate_own` — the change is itself gate-reviewed, hence item 4.
  3. Same replacement inside `_monolith.py`'s `_CANARY_CONTROL_SURFACES_FALLBACK` tuple (pre-move line 18491) — the manifest's in-body mirror.
  4. Run `python3 user/scripts/harness-gate.py --repo-root . --staged --json` over the staged Phase-1 diff; write `GATE_VERDICT.md` in the feature dir recording each flag + judgment per `~/.claude/skills/_components/harness-change-gate.md` (Read it first). Expected shape: in-scope (control surfaces touched); the manifest glob swap is NOT gate-weakening (coverage widens, nothing exempted, no test deleted, no numeric gate change); the test-file patch-redirect edits are the ratified L1 mechanism-3 sanctioned surface, count+names receipt attached. If the checker reports a genuine `gate_weakening` HIT that cannot be justified as the rename-tracking above, HALT with `NEEDS_INPUT.md` (operator sign-off is never judgment-passable).
- **Spec requirements:** SPEC D1-A ("the one `user/scripts/CLAUDE.md` row updates in the same commit — gate-enforced"); harness-change-gate D7 semantics.
- **Batch:** 4 (sequential — touches `_monolith.py`).

#### WU-5 — Commit 1a battery; `load_all()` wiring (commit 1b); benchmark re-run + honest numbers

- **Scope:** PHASES P1 deliverables "Importer-diff guard: no file outside the package + its tests (+ the doc row) is touched" + proven-done row "benchmark re-run recorded (facade alone may not cut hook cost — record the honest number)"; SPEC D4's `load_all()` eager-consumer mitigation.
- **TDD:** no.
- **Files to create/modify:** `user/scripts/lazy-state.py` + `user/scripts/bug-state.py` (one `lazy_core.load_all()` line each, commit 1b only), `docs/features/lazy-core-package-decomposition/PHASES.md` (tick Phase-1 deliverable rows + Implementation Notes numbers).
- **Implementation goal:**
  1. **Commit 1a:** run the FULL invariant battery (notes §1). All green → `git add` exactly: `user/scripts/lazy_core/` (new pkg incl. `_monolith.py` rename), `user/scripts/test_lazy_core.py`, `user/scripts/CLAUDE.md`, `docs/gate/control-surfaces.json`, `docs/features/lazy-core-package-decomposition/GATE_VERDICT.md`. Commit on `main`: `refactor(lazy-core): Phase 1 — package skeleton, PEP 562 facade, _ctx hoist, L2 anchor (move-only + sanctioned L1/L2/L3 edits)`. Importer-diff guard: `git show --name-only --format= HEAD` ⊆ that exact set (the rename shows as the pair `lazy_core.py` → `lazy_core/_monolith.py`).
  2. **Commit 1b:** add `lazy_core.load_all()` as the first statement of `main()` in BOTH `lazy-state.py` and `bug-state.py` (coupled-pair lockstep edit — the SPEC-D4 ImportError-timing mitigation for eager consumers; hooks deliberately do NOT get it). Re-run the FULL battery (parity audit especially). Tick the five Phase-1 `- [ ]` rows in PHASES.md; append an Implementation Notes entry recording: benchmark numbers from `python3 user/scripts/benchmark_lazy_core_import.py --repo-root .` (cold-import ms vs the 88.7/93.7 baseline — the facade may be a wash or slightly worse at this phase; RECORD THE HONEST NUMBER, no claim), the 1135 count+names receipt statement, the per-name redirect counts from WU-1, and the L2/L3 edit inventory. Commit: `refactor(lazy-core): Phase 1b — eager load_all() in state-script mains + phase receipt`.
  3. Verify the two hooks live: `python3 user/scripts/lazy-state.py --marker-present --repo-root .` exits 0/1 without error (exercises `lazy_guard`-adjacent import machinery via the state script; the pytest `test_hooks.py` suite in the battery is the primary net).
- **Spec requirements:** SPEC D2 Constraint 2 (importer-diff check per commit), D4-A (`load_all()` called by state-script `main()`), Validation Criteria rows 1–3.
- **Batch:** 5 (sequential).

**Batch overview:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | N/A |
| 2 | WU-2 | Solo | shares `_monolith.py`/tests with WU-1 |
| 3 | WU-3 | Solo | shares `_monolith.py`/tests |
| 4 | WU-4 | Solo | shares `_monolith.py` |
| 5 | WU-5 | Solo | state scripts + PHASES.md |

**Plan-specific risk notes:** (a) the WU-1 blanket per-name rewrite may touch prose/comments mentioning e.g. `lazy_core.time` — harmless, but the receipt counts make it auditable; (b) if any patched name turns out to be consumed by hook code via facade attribute access (e.g. `lazy_core.ack_oldest_deny` called from `lazy_guard.py`), the `_monolith` redirect is STILL correct — the facade forwards reads to `_monolith`, so both consumer classes observe the patch; (c) between WU-1 and WU-3 the tree is deliberately red — never commit there; (d) `_ctx` must never import `_monolith` (would defeat D4 and create a cycle).
