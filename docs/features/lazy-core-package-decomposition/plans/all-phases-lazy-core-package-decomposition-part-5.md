---
kind: implementation-plan
feature_id: lazy-core-package-decomposition
status: Complete
created: 2026-07-13
phases: [5]
complexity: complex
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-13.
> To execute: `/execute-plan C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-5.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 5 of 6. Sibling parts:
> - part 1: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-1.md`
> - part 2: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-2.md`
> - part 3: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-3.md`
> - part 4: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-4.md`
> - part 5: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-5.md` (this file)
> - part 6: `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-6.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — lazy-core-package-decomposition (v2) — Part 5: Phase 5 (Marker plane + pseudo; `_monolith` deleted)

**PHASES.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/PHASES.md`

**SPEC.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/SPEC.md`

**Total phases:** this part: Phase 5 only
**Plan version:** v2 (reference-based)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is claude-config — the gates are the Python invariant battery below, NOT `/msbuild`/`/mstest`/nx), this plan's own per-phase steps and the "Plan-specific execution notes" below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config overrides — READ FIRST)

1. **Per-commit invariant battery** (before EVERY commit; all exit 0; ZERO changes under `user/scripts/tests/baselines/` — this phase moves the marker plane + `apply_pseudo`, the exact code the byte-baselines and the run-marker arbitration protect; a baseline or arbitration behavior diff is a hard stop):
   ```bash
   cd C:/Users/Jacob/source/repos/claude-config
   python -m pytest user/scripts/ -q
   python3 user/scripts/lazy-state.py --test
   python3 user/scripts/bug-state.py --test
   python3 user/scripts/lazy_parity_audit.py --repo-root .
   python3 user/scripts/cli_surface_gen.py --check --repo-root .
   python3 user/scripts/doc-drift-lint.py --repo-root .
   python ~/.claude/scripts/lint-skills.py
   ```
2. **Scripted-transform mandate (HARD).** Standard seam-move procedure (restated in part 4; identical here) via throwaway scratchpad scripts. `apply_pseudo` (~1,362 lines) moves INTACT — a single verbatim slice, ZERO internal refactoring (SPEC D3: decomposing it is a separate future feature).
3. **Commit cadence:** one commit per WU (4 commits), each battery-green, directly on `main`.
4. **Importer-diff guard per commit:** file set ⊆ {`user/scripts/lazy_core/**`, `user/scripts/tests/test_lazy_core/**`} (+ `docs/features/lazy-core-package-decomposition/PHASES.md`, `user/scripts/CLAUDE.md`, `docs/features/lazy-core-package-decomposition/GATE_VERDICT.md` on the final commit).
5. **Patch-redirect rule (mechanism 3)** as prior parts. The marker plane owns the most-patched functions (`consume_nonce`, `bind_marker_session`) — their redirect targets become `lazy_core.markers.*`.
6. **Standing invariants:** `lazy_coord.py` never imports `lazy_core`; `phases-slice.py` untouched; `_ctx`/`statedir` never import `_monolith` (trivially satisfied once `_monolith` is gone); zero baseline regeneration. **Never delete or hand-edit a live run marker while testing** — hermetic tests isolate via `LAZY_STATE_DIR` + explicit `--repo-root` fixtures (the repo's documented convention).
7. **Halts:** `BLOCKED.md` / `NEEDS_INPUT.md` per the execution contract.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| mark-complete-partial-apply-noop-unrecoverable | hard | `docs/bugs/_archive/mark-complete-partial-apply-noop-unrecoverable/FIXED.md` | `apply_pseudo` moves INTACT with this fix already inside it — the receipt certifies the write path being moved |
| production-sentinel-writes-bypass-atomic-write | hard | `docs/bugs/_archive/production-sentinel-writes-bypass-atomic-write/FIXED.md` | Same — sentinel writes are `_atomic_write`-honest pre-move |
| (context) | — | `docs/features/lazy-core-package-decomposition/RESEARCH_SUMMARY.md` | Marker/pseudo/residue ranges |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | lazy-core-package-decomposition | P5 | Marker plane + pseudo; `_monolith` deleted; ≤4K census | Phase 4 complete (part 4) | Solo |

## Work Units

- [x] WU-1 — Extract `markers.py` (run-marker / ownership / refusals / nonce)
- [x] WU-2 — Extract `pseudo.py` (`apply_pseudo` moved INTACT)
- [x] WU-3 — Residue sweep: distribute the `_monolith` remainder to owning seams
- [x] WU-4 — Delete `_monolith.py`; explicit-total facade map; ≤4K LoC census; phase receipt

---

### Phase: lazy-core-package-decomposition P5 — Marker plane + pseudo (riskiest, last)

**Goal:** the marker/ownership/refusals plane and `apply_pseudo` become `markers.py` and `pseudo.py`; every remaining `_monolith` line is distributed to its owning seam; `_monolith.py` is DELETED; the facade's name→submodule map becomes explicit and total; every submodule ≤4K LoC (benchmark census asserts).
**Entry criteria:** Phase 4 complete — `gates`, `ledgers`, `dispatch`, `runtimeplane` extracted; battery green at HEAD; the two write-path receipts verified (Phase-4 gate — re-confirm they still exist before WU-1, one `ls`).
**SPEC.md references:** D3 (riskiest last; `apply_pseudo` moved intact), Technical Design tree (final roster), Validation Criteria "No new monolith" row; RESEARCH section map.

**Verified ground truth (pre-Phase-1 anchors — re-locate by pattern):** marker/ownership/refusals 10600–~12330 (`write_run_marker`, `read_run_marker`, `marker_owner_status`, `reassert_marker_owner`, `bind_marker_session`, `refuse_if_cycle_active`, `refuse_cycle_marker_mutation_if_subagent`, `refuse_run_start_clobber`, nonce mint/consume, cycle marker begin/end, checkpoint write/restore, `RUN_CONTINUITY_FIELDS`/`RUN_FRESH_FIELDS`) [VERIFY: `grep -n "def write_run_marker\|def read_run_marker\|def consume_nonce\|def bind_marker_session\|def refuse_if_cycle_active\|RUN_CONTINUITY_FIELDS" user/scripts/lazy_core/_monolith.py`]; `apply_pseudo` 4738–~6099 (~1,362 ln, ONE function + its private helpers if contiguous) [VERIFY: `grep -n "def apply_pseudo" user/scripts/lazy_core/_monolith.py`]; residue after Phases 2/4: SKIP/app-surface predicates (~1081–1400 if Phase 2 deferred them), misc detectors ~6100–6505 (`detect_noncanonical_blocker`, `write_completed_receipt`, `has_completion_receipt`), `_current_head`@~6510, `merged_priority`/`merged_worklist` ordering helpers, `_completion_postconditions_missing`, anything else `grep -c "^def \|^class " user/scripts/lazy_core/_monolith.py` still reports.

#### WU-1 — Extract `markers.py`

- **Scope:** PHASES P5 bullet (markers half): the full marker/ownership/refusals plane per the verified range.
- **TDD:** no (move-only; the marker plane has the densest existing test coverage in the suite — that IS the net).
- **Files to create/modify:** `user/scripts/lazy_core/markers.py` (new), `_monolith.py`, `__init__.py`, `user/scripts/tests/test_lazy_core/*.py` (redirects: `consume_nonce`(2), `bind_marker_session`(2) → `lazy_core.markers.*`; the remaining `time` sites (marker staleness/age consumers) and `os` sites per consumer).
- **Implementation goal:** standard seam-move procedure, seam = `markers`. Dependencies: `statedir` (keyed dir), `_ctx` (`_atomic_write`, `_diag`) — by-value imports; tests patch `markers.*`. Re-point every `# Phase-5 re-point` deferred local import recorded in parts 2/4 (`grep -rn "Phase-5 re-point" user/scripts/lazy_core/`) that names a marker-plane symbol → `from .markers import ...` (module-top where no cycle results; keep function-local where a genuine cycle exists and RECORD it). **`lazy_guard.py`/`lazy_inject.py` stay zero-edit** — they reach `read_run_marker`/`bind_marker_session`/`consume_nonce` via facade attribute access, which the map now serves from `markers` (the last chunk of the D4 hook cut: after this WU the guard's marker paths no longer load `_monolith`).
- **Spec requirements:** SPEC D3 (marker plane last); PHASES P5.
- **Batch:** 1.

#### WU-2 — Extract `pseudo.py` (`apply_pseudo` INTACT)

- **Scope:** PHASES P5 bullet: "`pseudo.py` (`apply_pseudo`@4738, ~1,362 ln, moved INTACT — no internal refactor)".
- **TDD:** no (move-only, single verbatim slice).
- **Files to create/modify:** `user/scripts/lazy_core/pseudo.py` (new), `_monolith.py`, `__init__.py`, split test files (redirects: sites whose consumer is `apply_pseudo` itself — e.g. `_atomic_write` spies exercising the receipt-write sequence → `lazy_core.pseudo._atomic_write`).
- **Implementation goal:** standard seam-move procedure, seam = `pseudo`, ONE contiguous slice covering `apply_pseudo` + only its immediately-adjacent private helpers (the `_completion_postconditions_missing` family if contiguous — verify by reading the boundary lines, not by assumption). Imports: `gates` (evidence/coherence), `docmodel` (parsers), `markers` (refusals/marker), `ledgers` (provenance/intervention capture), `statedir`, `_ctx` — per the patched-name convention. **ZERO internal edits to the function body beyond the import header** — `git diff --stat` on the commit must show `pseudo.py` insertions ≈ `_monolith.py` deletions for the slice; any body edit is a defect. The intervention-capture and provenance calls inside it keep their spelling (they resolve via the new imports).
- **Spec requirements:** SPEC D3 ("NOT internally refactored in this feature; decomposing the ~1,277-line function is its own future feature"); PHASES P5.
- **Batch:** 2.

#### WU-3 — Residue sweep

- **Scope:** implicit PHASES P5 requirement (the body module cannot be deleted while code remains): distribute every remaining `_monolith` top-level def/class/constant to its owning seam from the FIXED roster (`_ctx`, `docmodel`, `depdag`, `hostcaps`, `notifyplane`, `statedir`, `gates`, `ledgers`, `dispatch`, `runtimeplane`, `markers`, `pseudo`) — no new module names (SPEC D1-A roster; `_current_head` and git helpers → `runtimeplane` unless clearly docmodel/gates-owned; SKIP/app-surface predicates → `docmodel`; misc detectors (`detect_noncanonical_blocker`) → `docmodel`; receipt writers (`write_completed_receipt`/`has_completion_receipt`) → `gates`; ordering helpers (`merged_priority`) → `depdag` or `dispatch` — choose by call-graph proximity and RECORD the choice per symbol in the WU report).
- **TDD:** no (move-only).
- **Files to create/modify:** the seam files above, `_monolith.py` (shrinks toward empty), `__init__.py`, split test files (redirects per moved consumer).
- **Implementation goal:** scripted inventory first (`python -c` AST census of remaining `_monolith` top-level names + LoC per block); assign each block per the Scope rules; execute as ONE scripted multi-slice pass (same procedure, N slices); import-backs disappear as the file empties. Suite + battery green; 1135 receipt.
- **Spec requirements:** SPEC D1-A (final roster fixed), D3.
- **Batch:** 3.

#### WU-4 — Delete `_monolith.py`; explicit-total facade map; census; phase receipt

- **Scope:** PHASES P5 bullets "Body module deleted; every seam ≤4K LoC (benchmark census asserts)."
- **TDD:** yes — add `test_facade_map_total_and_collision_free` FIRST: asserts `_SUBMODULE_BY_NAME` has no duplicate-name mappings, every mapped module imports, every mapped name resolves, and `_FALLBACK_SUBMODULE`/`_monolith` no longer exists (getattr on a bogus name raises clean `AttributeError`). RED while `_monolith` exists as fallback, GREEN after.
- **Files to create/modify:** `user/scripts/lazy_core/_monolith.py` (DELETED — `git rm`), `user/scripts/lazy_core/__init__.py` (fallback removed; map explicit-total — scripted generation from an AST walk of every submodule's top-level names; collision check hard-errors), split test files (the new test + removal of any lingering `lazy_core._monolith` references — `grep -rn "_monolith" user/scripts/ docs/` must end with ZERO live-code hits), `docs/features/lazy-core-package-decomposition/PHASES.md` (tick P5 rows + Implementation Notes), `user/scripts/CLAUDE.md` (row prose: drop the "body currently in `_monolith.py`" clause), `docs/features/lazy-core-package-decomposition/GATE_VERDICT.md` (phase entry).
- **Implementation goal:** scripted map generation (never hand-typed: AST-enumerate each seam's top-level names; names starting `_` included when they were monolith module-globals the facade re-exported — the Phase-1 contract is EVERY public + used-private name resolves); delete the fallback branch of `__getattr__`; `git rm user/scripts/lazy_core/_monolith.py`; census via `python3 user/scripts/benchmark_lazy_core_import.py --repo-root .` — `over_4k_ceiling` must be EMPTY (its `lazy_core/` rglob census asserts the SPEC's no-new-monolith ceiling); if any seam exceeds 4K, re-slice its largest cohesive block to another roster member (record) — never raise the ceiling. Full battery; hook-surface benchmark re-run (`--hook-surface`) — record the now-complete D4 number vs the 88.7/93.7 baseline and the <15 ms target (honest number). Implementation Notes: census table, map size, redirect totals, `# Phase-5 re-point` closure inventory. Commit: `refactor(lazy-core): Phase 5 — markers + pseudo extracted, _monolith deleted, facade map total`.
- **Spec requirements:** SPEC Validation Criteria "No new monolith" + "Facade byte-compatibility"; PHASES P5 proven-done.
- **Batch:** 4.

**Batch overview:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | all share `_monolith.py`/`__init__.py`/split tests |
| 2 | WU-2 | Solo | 〃 |
| 3 | WU-3 | Solo | 〃 |
| 4 | WU-4 | Solo | 〃 + PHASES.md + CLAUDE.md + GATE_VERDICT.md |

**Plan-specific risk notes:** (a) the marker plane is the arbitration layer for LIVE runs — if this plan executes under a `/lazy-batch` run, the run's own marker machinery is the code being moved; the battery's hermetic isolation (`LAZY_STATE_DIR` fixtures) is what makes that safe — never run ad-hoc marker commands against the real state dir mid-move; (b) `refuse_if_cycle_active` moving must keep the C3 env-priority semantics byte-identical (the suite pins them); (c) if `apply_pseudo`'s helper boundary is ambiguous, move LESS into `pseudo.py` (helpers can live in `gates`) — the INTACT constraint binds the function, not a maximal closure; (d) `harness-gate.py --staged` flags each commit in-scope — extend `GATE_VERDICT.md` per commit (move-only rationale; the WU-4 `__getattr__` fallback deletion is facade-mechanics, not a gate deletion — say so explicitly).
