---
kind: implementation-plan
feature_id: harness-hardening-retro-fixes
status: In-progress
created: 2026-06-17
complexity: complex
phases: [3, 4, 5]
---

> **Mobile plan** — generated by `/write-plan` (via `/plan-feature`) on 2026-06-17.
> To execute: `/execute-plan <this-file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 2. Sibling parts:
> - part 1: `docs/features/harness-hardening-retro-fixes/plans/all-phases-harness-hardening-retro-fixes-part-1.md`
> - part 2: `docs/features/harness-hardening-retro-fixes/plans/all-phases-harness-hardening-retro-fixes-part-2.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — harness-hardening-retro-fixes (Part 2: Phases 3–5)  (v2)

**PHASES.md files:**
- `docs/features/harness-hardening-retro-fixes/PHASES.md` (harness-hardening-retro-fixes, 5 phases — this part covers 3–5)

**SPEC.md files:**
- `docs/features/harness-hardening-retro-fixes/SPEC.md`

**Total phases:** 3 (this part) of 5
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **EXECUTION-ENVIRONMENT NOTE (claude-config / cycle subagent):** when this plan is executed by a `/lazy-batch` cycle subagent that has NO `Agent` tool, perform ALL subagent work INLINE with `Read`/`Edit`/`Write` (the dispatch-level inline override). Write the failing test first, then the implementation, for each TDD work unit regardless.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `.claude/skill-config/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none) | — | — | Phases 3–5 have no completed hard upstream dependencies they inherit a contract from — they are internal harness fixes. (The feature-level hard dep on `unified-pipeline-orchestrator` is consumed by Phase 1, in part 1.) |

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit`/`Write` on source/test files (EXCEPTION: a cycle subagent with no `Agent` tool performs the work INLINE per the execution-environment note; trivial PASS-WITH-FIXES of a few lines also permitted).
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline — write the failing test FIRST, then make it pass.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's output is reviewed before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in a phase finish, integration verification confirms all changes work together.
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it.
9. Each completed phase is committed and pushed before the next phase begins.
10. This plan is self-contained — follow it exactly as written.
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
12. **Shared-import awareness:** Phase 3 touches `lazy_core.py` (`verify_ledger`); Phase 5 touches `test_lazy_core.py`. Run the FULL lazy-family gate after Phase 3 (Step B.4). Phase 4's `mcp-test/SKILL.md` is AlgoBooth-repo-scoped prose (not a coupled pair — no mirroring), but it IS picked up by `project-skills.py` per-repo projection; re-run `project-skills.py` after editing it.

## Execution Schedule

| Step | Feature | Phase(s) | Title | Blocked by | Parallel? |
|------|---------|----------|-------|------------|-----------|
| 1 | harness-hardening-retro-fixes | P3 | plan_complete ledger fix (plan-less / realign-only) | — | Solo |
| 2 | harness-hardening-retro-fixes | P4 | mcp-test haiku-tier re-scope with script-derived routing signal | — | Solo |
| 3 | harness-hardening-retro-fixes | P5 | Dead-coverage guard in the gate suite | run LAST (validates P1–P4 test registration) | Solo |

## Work Units

- [x] WU-1 — verify_ledger plan-less / absent-by-design plan_complete fix (lazy_core)
- [x] WU-2 — Phase 3 ledger tests (plan-less, realign-only, incomplete-plan regression)
- [ ] WU-3 — route_mcp_test_tier deterministic routing helper + tests (surface_resolver)
- [ ] WU-4 — mcp-test SKILL.md model-tier re-scope to the script signal
- [ ] WU-5 — dead-coverage guard + negative-fixture test (test_lazy_core)
- [ ] WU-6 — wire + document the dead-coverage guard (quality-gates + harden-harness gate list)

---

## Phase: harness-hardening-retro-fixes P3 — plan_complete ledger fix (plan-less / realign-only)

**Goal:** Make `verify_ledger`'s feature-level `plan_complete` distinguish *absent-by-design* (no implementation plan, none required) from *incomplete*, eliminating the benign-but-noisy false-alarm recovery chase.
**Entry criteria:** None.
**SPEC.md references:** §Technical Design §3; §Implementation Phases #3.

### WU-1 — verify_ledger plan-less / absent-by-design plan_complete fix
- **Scope:** PHASES P3 deliverables 1–3 (absent-by-design branch; verify scoped branch unaffected; diagnostic).
- **TDD:** yes (write the failing plan-less fixture first — WU-2).
- **Files to create/modify:** `user/scripts/lazy_core.py`.
- **Test files:** `user/scripts/test_lazy_core.py` (WU-2).
- **Implementation goal:** In `verify_ledger`'s feature-level branch (`scoped is False`) [VERIFY: grep -n "def verify_ledger" user/scripts/lazy_core.py], treat "no implementation plan present and none required" as NOT a failure: when `find_implementation_plans(spec_path)` [VERIFY: grep -n "def find_implementation_plans" user/scripts/lazy_core.py] returns zero incomplete plans AND `_has_any_complete_plan` [VERIFY: grep -n "_has_any_complete_plan" user/scripts/lazy_core.py] is False AND there is genuinely no implementation plan on disk (only `realign-*.md` / `retro-*.md` / none), set `plan_complete = True` (absent-by-design). Preserve the existing behavior for features WITH implementation plans (≥1 complete + zero incomplete → True; any incomplete → False). Emit a `_DIAGNOSTICS` note ("plan_complete: no implementation plan required (absent-by-design)") — additive, never gates. Leave the plan-SCOPED branch (`scoped is True`) untouched — it reads the named plan's own frontmatter status and is correct.
- **Spec requirements:** SPEC §3 ("treat 'no plan required' as not-a-failure (distinguish absent-by-design from incomplete)").
- **Batch:** 1.

### WU-2 — Phase 3 ledger tests (plan-less, realign-only, incomplete-plan regression)
- **Scope:** PHASES P3 deliverable 4 (tests a–c).
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/test_lazy_core.py` (+ `_TESTS` registration).
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** Build temp-dir feature fixtures (PHASES.md + varying `plans/` contents), stubbing the git checks the way the existing `verify_ledger` tests do [VERIFY: grep -n "verify_ledger" user/scripts/test_lazy_core.py]. (a) plan-less feature (PHASES.md present, all deliverables checked, NO implementation plan) → `plan_complete:true`, `ok:true`; (b) realign-only feature (only `plans/realign-*.md`) → `plan_complete:true`; (c) regression guard: a feature WITH an incomplete implementation plan still returns `plan_complete:false`. All registered in `_TESTS`.
- **Implementation goal:** Tests pass against WU-1 and are collected by the runner.
- **Spec requirements:** SPEC §Validation Criteria row "plan_complete not false-alarmed".
- **Batch:** 2 (overlaps `test_lazy_core.py` with later WUs → sequenced; depends on WU-1).

### Batch overview — Phase P3
| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | None |
| 2 | WU-2 | Solo | None (sequenced after WU-1; `test_lazy_core.py`) |

---

## Phase: harness-hardening-retro-fixes P4 — mcp-test haiku-tier re-scope with script-derived routing signal

**Goal:** Add a script-derived routing helper so scenario-authoring / `.md`-conversion / diagnosis cycles route to Sonnet by DEFAULT (not by orchestrator override); haiku only for ready-to-run YAML happy paths.
**Entry criteria:** None.
**SPEC.md references:** §Technical Design §4; §Implementation Phases #4; Open Question 3 (routing-signal source).

### WU-3 — route_mcp_test_tier deterministic routing helper + tests
- **Scope:** PHASES P4 deliverables 1, 2, 4 (enumerate the conditions; add the helper; tests).
- **TDD:** yes (write the failing routing tests first).
- **Files to create/modify:** `user/scripts/surface_resolver.py` (NEW helper `route_mcp_test_tier` — this is a deliverable to BUILD, not an existing anchor [VERIFY: grep -c "def route_mcp_test_tier" user/scripts/surface_resolver.py] expected 0 before this WU).
- **Test files:** `user/scripts/test_surface_resolver.py` [VERIFY: ls user/scripts/test_surface_resolver.py].
- **Test expectations:** `route_mcp_test_tier(scenario_path, prior_verdict=None, yaml_exists=None) -> "haiku" | "sonnet"` is a pure function. Cover each enumerated Sonnet-forcing condition: (1) resolved scenario is a legacy `.md` with NO converted `corpus/live/*.yaml` counterpart → `sonnet`; (2) prior verdict non-definitive (`uncertain` / unrepaired `harness` / post-heal `genuine`) → `sonnet`; (3) no scenario exists at all → `sonnet`; and the happy path: ready converted YAML + no adverse prior verdict → `haiku`. Register per the `test_surface_resolver.py` runner convention.
- **Implementation goal:** Add `route_mcp_test_tier` to `surface_resolver.py` implementing those conditions deterministically (no I/O beyond an optional existence check so it is hermetically testable). Enumerate the conditions in a docstring (Open Question 3 resolution).
- **Spec requirements:** SPEC §4 ("routing signal should be script-derived ... not a per-run human call"); §Validation Criteria row "Haiku tier routes diagnosis to sonnet by default".
- **Batch:** 1.

### WU-4 — mcp-test SKILL.md model-tier re-scope to the script signal
- **Scope:** PHASES P4 deliverable 3 (SKILL.md prose consults the script signal; retire the orchestrator-override reliance).
- **TDD:** no (repo-scoped skill-prose; verified by lint + project-skills re-expansion).
- **Files to create/modify:** `repos/algobooth/.claude/skills/mcp-test/SKILL.md`.
- **Implementation goal:** Update the "Model tier" section [VERIFY: grep -n "Model tier" repos/algobooth/.claude/skills/mcp-test/SKILL.md] so the tier is chosen by the `route_mcp_test_tier` script signal (cite the helper at `user/scripts/surface_resolver.py`, symlinked to `~/.claude/scripts/surface_resolver.py`), document the script-observable conditions in prose, and state explicitly that a `.md`-unconverted or prior-non-definitive scenario routes to Sonnet WITHOUT a human/orchestrator call — removing the reliance on an orchestrator override for the diagnosis/authoring case. Keep the `model: haiku` frontmatter (haiku stays the happy-path default; the helper escalates).
- **Spec requirements:** SPEC §4 ("route ... to sonnet by default, not by orchestrator override").
- **Batch:** 2 (no file conflict with WU-3 — different file — but sequenced so the helper it cites exists; could run parallel, kept sequential for citation integrity).

### Batch overview — Phase P4
| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-3 | Solo | None |
| 2 | WU-4 | Solo | None (cites WU-3's helper) |

---

## Phase: harness-hardening-retro-fixes P5 — Dead-coverage guard in the gate suite

**Goal:** Fail the gate when a `def test_*` function exists but is not registered in `_TESTS` (the Round-24 dead-coverage class), making "tests that never execute" impossible to land silently. RUN LAST — it cross-checks that Phases 1–4 actually registered their new tests.
**Entry criteria:** P1–P4 complete (so the guard validates their test registration as a built-in cross-check).
**SPEC.md references:** §Technical Design §5; §Implementation Phases #5; §Validation Criteria row "Dead coverage fails gates".

### WU-5 — dead-coverage guard + negative-fixture test
- **Scope:** PHASES P5 deliverables 1, 2, 4 (guard; self-checking `_TESTS` registration; positive + negative tests).
- **TDD:** yes (write the negative-fixture failing test first).
- **Files to create/modify:** `user/scripts/test_lazy_core.py` (guard function + `_TESTS` registration). ⚖ policy: guard scope (this-module vs all script test modules) → cover `test_lazy_core.py` (the incident's module + the suite Phases 1–3 extend) as the load-bearing case; note the generalization seam to other `_TESTS`-style modules in the guard's docstring rather than speculatively scanning every file.
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** A dead-coverage guard that `ast.parse`s the `test_lazy_core.py` module source [VERIFY: grep -n "_TESTS = \[" user/scripts/test_lazy_core.py], collects every top-level `def test_*` name, compares against the names registered across all `_TESTS = ... + [...]` assignments [VERIFY: grep -n "_TESTS = _TESTS" user/scripts/test_lazy_core.py], and FAILS naming any orphaned (defined-but-unregistered) function. (a) positive: the guard PASSES on the current fully-registered suite; (b) negative: feed synthetic module source containing `def test_orphan` not in its `_TESTS` and assert the guard reports `test_orphan` by name. The guard is itself registered in `_TESTS` (self-checking — collected and run by the same harness it guards).
- **Implementation goal:** Use `ast.parse` over the module source (robust over regex). The guard runs as a normal `_TESTS` entry so `python user/scripts/test_lazy_core.py` fails on any orphan.
- **Spec requirements:** SPEC §5 ("guard that fails ... when a test file exists ... but is not collected by the runner"); §Validation Criteria row "Dead coverage fails gates".
- **Batch:** 1.

### WU-6 — wire + document the dead-coverage guard
- **Scope:** PHASES P5 deliverable 3 (surface the guard in the gate docs / harden-harness gate list).
- **TDD:** no (documentation).
- **Files to create/modify:** `.claude/skill-config/quality-gates.md`; optionally `user/skills/harden-harness/SKILL.md` (Step 3 gate command list).
- **Implementation goal:** Document the dead-coverage guard in `.claude/skill-config/quality-gates.md` [VERIFY: ls .claude/skill-config/quality-gates.md] gate-determination list (it runs as part of `test_lazy_core.py` / the pytest gate, so note that a hardening round's gate run includes it). Optionally add it to `harden-harness/SKILL.md` Step 3's mandatory gate commands [VERIFY: grep -n "test_lazy_core.py" user/skills/harden-harness/SKILL.md] so a hardening round cannot land unregistered tests silently.
- **Spec requirements:** SPEC §5 (the guard is an anti-overfit safeguard making the evidence for a fix real).
- **Batch:** 2 (docs — no code conflict; sequenced after WU-5 so the guard it documents exists).

### Batch overview — Phase P5
| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-5 | Solo | None |
| 2 | WU-6 | Solo | None (docs; sequenced after WU-5) |

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** Execute in schedule order — P3, then P4, then P5. P5 MUST run last (it validates P1–P4 test registration). They overlap files (`test_lazy_core.py`) so run sequentially.
2. **Announce:** Print "Implementing harness-hardening-retro-fixes Phase N: [title]".
3. **Review prior context:** Re-read prior completed phases' Implementation Notes in PHASES.md. They take priority over the plan where they diverge.
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "harness-hardening-retro-fixes Phase N: [title] — committed as [hash]".
7. **Loop:** Return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly.

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)
Read `~/.claude/skills/_components/source-reread.md`. Re-read PHASES.md, SPEC.md, and this plan from disk.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — ZERO INLINE IMPLEMENTATION)
**PRE-FLIGHT CHECK:** Agent + `model: "sonnet"` for ALL code; no direct `Edit`/`Write` of source/test files — UNLESS a cycle subagent with no `Agent` tool (then INLINE, test-first). Read: `tdd-protocol.md`, `subagent-launch.md`, `tdd-test-agent.md`, `implementation-agent.md`.
**POST-DISPATCH GATE:** verify Agent/inline + TDD discipline held.

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)
Read `~/.claude/skills/_components/subagent-review.md` + `~/.claude/skills/_components/mount-site-verification.md`. Produce a structured verdict before Step B.3.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)
Read `~/.claude/skills/_components/phases-update.md`. Check off deliverables; add Implementation Notes.

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)
Read `.claude/skill-config/quality-gates.md`. Phase 3 (touches `lazy_core.py` — shared): FULL lazy-family set — `project-skills.py` + `lint-skills.py --check-projected --check-capabilities` + `pytest user/scripts/ -q` + `lazy-state.py --test` + `bug-state.py --test` + `lazy_parity_audit.py --report`. Phase 4: `pytest user/scripts/ -q` (surface_resolver suite) + `project-skills.py` + `lint-skills.py --check-projected --check-capabilities`. Phase 5: `pytest user/scripts/ -q` (or `python user/scripts/test_lazy_core.py`) + `lint-skills.py`. 100% pass required (no "preexisting" triage).

#### Step B.5: Commit Batch
Read `.claude/skill-config/commit-policy.md`. Conventional Commits, no AI attribution, push allowed.

#### Step B.6: Proceed to Next Batch
Checklist: review verdict; PHASES.md updated; gates pass; committed per policy.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)
Read `~/.claude/skills/_components/integration-verification.md`. For Phase 5, confirm the guard actually PASSES on the real suite (proving Phases 1–4 registered their tests) AND flags the negative fixture.

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)
Read `~/.claude/skills/_components/claude-md-review.md`. Phase 4 may warrant a note in `user/scripts/CLAUDE.md` (`surface_resolver.py` now owns the mcp-test tier signal). Phase 5 may warrant a note that the suite has a dead-coverage guard.

#### Commit and Push Post-Phase Changes
Read `.claude/skill-config/commit-policy.md` and commit + push. **ATOMIC GATE+COMMIT:** run the gate and commit as one chained command so an interrupted turn leaves committed, pushed state.

## Blocking Issue Protocol

If a blocking issue is encountered:
1. Stop all in-progress work. Do not launch new subagents.
2. Commit and push any completed phases not yet committed.
3. Print a blocking-issue report (Completed phases + hashes; Blocked phase; Reason; Recovery suggestion; Remaining phases).
4. Do not work around the blocker.

Blocking issues include: a subagent failure unfixable after 2 retries; a quality-gate failure unfixable after 2 retries; a `lazy_parity_audit.py --report` drift requiring an operator decision; any error needing architectural decisions beyond the specs. (Contract/policy forks → `NEEDS_INPUT.md` per the standard halt protocol.)

## Completion

When P3 + P4 + P5 are complete:
1. Run the full lazy-family quality-gate suite one final time across the repo.
2. Print a completion report (Features implemented; Total phases completed; Total commits; Final QG status; Commit log table; Implementation Notes summary).
3. The feature's implementation is then done across both parts; the pipeline routes to the validation tail (this repo's MCP gate is operator-exempt — a `SKIP_MCP_TEST.md` granted by the operator promotes to `VALIDATED.md`, then the `__mark_complete__` gate owns the Status flip + COMPLETED.md receipt + ROADMAP strike + queue trim). Do NOT flip the top-level PHASES `**Status:**` to Complete or write COMPLETED.md yourself.
