---
kind: implementation-plan
feature_id: harness-hardening-retro-fixes
status: Complete
created: 2026-06-17
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` (via `/plan-feature`) on 2026-06-17.
> To execute: `/execute-plan <this-file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 2. Sibling parts:
> - part 1: `docs/features/harness-hardening-retro-fixes/plans/all-phases-harness-hardening-retro-fixes-part-1.md` (this file)
> - part 2: `docs/features/harness-hardening-retro-fixes/plans/all-phases-harness-hardening-retro-fixes-part-2.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — harness-hardening-retro-fixes (Part 1: Phases 1–2)  (v2)

**PHASES.md files:**
- `docs/features/harness-hardening-retro-fixes/PHASES.md` (harness-hardening-retro-fixes, 5 phases — this part covers 1–2)

**SPEC.md files:**
- `docs/features/harness-hardening-retro-fixes/SPEC.md`

**Total phases:** 2 (this part) of 5
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **EXECUTION-ENVIRONMENT NOTE (claude-config / cycle subagent):** when this plan is executed by a `/lazy-batch` cycle subagent that has NO `Agent` tool, perform ALL subagent work INLINE with `Read`/`Edit`/`Write` (the dispatch-level inline override). Write the failing test first, then the implementation, for each TDD work unit regardless. The orchestrator+subagent table above is the canonical workstation model; the inline override is the documented fallback.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `.claude/skill-config/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| unified-pipeline-orchestrator | hard | `docs/features/unified-pipeline-orchestrator/toolify-bar.md` | Phase 1 cites the toolify deterministic-only bar + promotion-checklist step 7 (harness-hardening named as the in-run auto-initiator); harden-harness does its own dance detection and spins off `/spec-bug` per the checklist — does NOT call the offline miner. |
| unified-pipeline-orchestrator | hard | `user/skills/_components/adhoc-enqueue.md` | Phase 1's spin-off action reuses the shipped `--type bug` front-enqueue path (→ `bug-state.py --enqueue-adhoc`). Read-only dependency — already on disk. |

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit`/`Write` on source or test files (EXCEPTION: a cycle subagent with no `Agent` tool performs the work INLINE per the execution-environment note above; trivial PASS-WITH-FIXES of a few lines also permitted).
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline — write the failing test FIRST, then make it pass.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in a phase finish, integration verification confirms all changes work together.
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it (notably `user/scripts/CLAUDE.md` and root `CLAUDE.md` for the verification-detector change).
9. Each completed phase is committed and pushed before the next phase begins (claude-config allows push — `.claude/skill-config/commit-policy.md`).
10. This plan is self-contained — follow it exactly as written without relying on external context.
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
12. **Coupled-pair / lockstep awareness:** Phase 2 touches `lazy_core.py` (shared by `lazy-state.py` + `bug-state.py`) — run the FULL lazy-family gate (see Step B.4). The verification marker is a row annotation, not a sentinel, so it does NOT enter AlgoBooth's `SENTINEL_SCHEMAS`; if any lockstep with `check-docs-consistency.ts` surfaces, FLAG it in the report (a harness agent cannot edit the target repo).

## Execution Schedule

| Step | Feature | Phase(s) | Title | Blocked by | Parallel? |
|------|---------|----------|-------|------------|-----------|
| 1 | harness-hardening-retro-fixes | P1 | Anti-overfit engine in /harden-harness | — | Solo |
| 2 | harness-hardening-retro-fixes | P2 | Verification-section detector — structural canonical marker | — (independent of P1) | Solo |

## Work Units

- [x] WU-1 — harden-harness over-fit detector + spin-off prose (Phase 1)
- [x] WU-2 — Phase 1 over-fit-detector tests / presence gate
- [x] WU-3 — verification-only canonical marker SSOT + detector rekey (lazy_core)
- [x] WU-4 — producers emit the canonical marker (phases-runtime-verification + blocked-resolution)
- [x] WU-5 — Phase 2 detector tests (novel-header, un-migrated-warning, lockstep)

---

## Phase: harness-hardening-retro-fixes P1 — Anti-overfit engine in /harden-harness

**Goal:** Give `/harden-harness` Step 3 an over-fit detector that lands the mechanical fix first, then spins off a generalized `/spec`/`/spec-bug` (front-enqueued via `adhoc-enqueue`) when an over-fit smell trips — surfaced in the HARDENING.md round + a `PushNotification`.
**Entry criteria:** None (first phase). Upstream `unified-pipeline-orchestrator` is Complete (toolify framework + `adhoc-enqueue --type bug` on disk).
**SPEC.md references:** §Technical Design §1 (Anti-overfit harden-harness mechanism); §Implementation Phases #1; Open Question 1 (recurrence threshold).

### WU-1 — harden-harness over-fit detector + spin-off prose
- **Scope:** PHASES P1 deliverables 1–7 (over-fit detector subsection; recurrence threshold; generalization bound; spin-off action; no-double-blocking + self-recursion-guard notes; Step 4 round-template extension; toolify-candidate trigger wiring).
- **TDD:** no (skill-prose authoring; verified by WU-2's presence gate + lint).
- **Files to create/modify:** `user/skills/harden-harness/SKILL.md` (Step 3 over-fit detector + spin-off action; Step 4 HARDENING.md round template gains a patch+spinoff record; "Return format" gains a `spinoff` field).
- **Implementation goal:** Extend Step 3 ("Act by decision class") of `harden-harness/SKILL.md` [VERIFY: grep -n "Step 3: Act by decision class" user/skills/harden-harness/SKILL.md] so the mechanical fix ALWAYS lands first, then an over-fit detector decides on a spin-off. Enumerate the four smell signals (literal-phrase-to-matcher; class recurred ≥2 in the hardening log; agent self-flags narrow; repeated deterministic dance per the toolify promotion checklist). State the resolved recurrence threshold: phrase-match patches spin off on FIRST occurrence; non-phrase recurrence needs ≥2. State the generalization bound (smallest class subsuming instance + near neighbors; cite the concrete instance; name the class boundary; no speculative rewrite). State the spin-off action: compose the generalized problem statement and invoke `/spec` (structural/new-capability) or `/spec-bug` (defect/regression/toolify-this-dance) via the `adhoc-enqueue` protocol [VERIFY: grep -n "type bug" user/skills/_components/adhoc-enqueue.md], front-enqueued. State no-double-blocking (instance already fixed → spin-off never blocks the run) and self-recursion-guard-preserved (a spin-off is a `/spec`/`/spec-bug` enqueue, not a recursive hardening dispatch — does not trip the existing depth-1 guard [VERIFY: grep -n "Self-recursion guard" user/skills/harden-harness/SKILL.md]). Extend the Step 4 round template [VERIFY: grep -n "Round <N>" user/skills/harden-harness/SKILL.md] to record BOTH patch AND spin-off (a `harden(spinoff):` note + the front-enqueued item id). Extend "Return format" [VERIFY: grep -n "Return format" user/skills/harden-harness/SKILL.md] with a `spinoff:` field (id + reason, or `none`). Wire the toolify-candidate trigger to the upstream framework's deterministic-only bar + promotion checklist (cite `docs/features/unified-pipeline-orchestrator/toolify-bar.md` step 7); state explicitly that harden-harness does its OWN in-run dance-recurrence detection and does NOT shell `toolify-miner.py` mid-cycle.
- **Spec requirements:** SPEC §1 bullets (over-fit smell signals; spin-off action; generalization bound; no double-blocking; self-recursion guard preserved); PHASES P1 deliverables 1–7.
- **Batch:** 1.

### WU-2 — Phase 1 over-fit-detector tests / presence gate
- **Scope:** PHASES P1 deliverable 8 (tests).
- **TDD:** yes (test-first).
- **Files to create/modify:** `user/scripts/test_lazy_core.py` (new fixtures + `_TESTS` registration). If no extractable Python helper is warranted (the detector is LLM-executed prose), author a **presence gate** instead: a `test_harden_harness_overfit_prose_present` function asserting the four smell signals, the recurrence threshold, the generalization-bound discipline, and the `/spec` vs `/spec-bug` choice rule strings are present in `user/skills/harden-harness/SKILL.md`.
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** Prefer the presence gate (this repo's MCP gate is operator-exempt; the detector behavior is prose executed by the LLM): assert each of the four smell signals, the first-occurrence-for-phrase / ≥2-for-other threshold, the generalization-bound phrase, and the spin-off choice-rule are findable in the SKILL.md text. MUST be registered in `_TESTS` [VERIFY: grep -n "_TESTS = \[" user/scripts/test_lazy_core.py] so Phase 5's dead-coverage guard sees it. (If, during authoring, a clean pure-function over-fit *decision* helper is extracted into a script, add deterministic fixtures: phrase-match → spin-off on first occurrence; structural non-phrase + no recurrence → no spin-off; non-phrase recurred ≥2 in a synthetic hardening-log fixture → spin-off; toolify-dance signal → `/spec-bug` spin-off.)
- **Implementation goal:** The test (presence or decision) passes against WU-1's authored prose/helper and is collected by the runner.
- **Spec requirements:** SPEC §Validation Criteria rows "Fix-now + spinoff on over-fit smell", "No spin-off when fix is already structural", "Toolify candidate auto-spins a /spec-bug".
- **Batch:** 2 (depends on WU-1's prose existing; also overlaps `test_lazy_core.py` with no other P1 WU but is sequenced after WU-1 for content dependency).

### Batch overview — Phase P1
| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | None |
| 2 | WU-2 | Solo | None (sequenced after WU-1 — needs its prose) |

---

## Phase: harness-hardening-retro-fixes P2 — Verification-section detector — structural canonical marker

**Goal:** Replace the growing free-text `_VERIFICATION_SECTION_RE` with a structural canonical marker emitted by producers and keyed off by the detector; reduce the regex to a deprecation shim that surfaces un-migrated producers; SSOT in one Python constant with a lockstep test.
**Entry criteria:** None (independent of P1).
**SPEC.md references:** §Technical Design §2; §Implementation Phases #2; Open Question 2 (canonical marker form).

### WU-3 — verification-only canonical marker SSOT + detector rekey (lazy_core)
- **Scope:** PHASES P2 deliverables 1–3 (SSOT constant; rekey `remaining_unchecked_are_verification_only`; demote `_VERIFICATION_SECTION_RE` to a warning shim).
- **TDD:** yes (test-first — WU-5 authors the fixtures; if executed inline, write the failing fixture before this implementation).
- **Files to create/modify:** `user/scripts/lazy_core.py`.
- **Test files:** `user/scripts/test_lazy_core.py` (fixtures live in WU-5; for TDD ordering write the novel-header-with-marker failing test first).
- **Implementation goal:** Define `_VERIFICATION_ONLY_MARKER` as a single module constant in `lazy_core.py` (resolve Open Question 2 toward the per-row HTML comment `"<!-- verification-only -->"` — most robustly machine-detectable; document the choice + the `check-docs-consistency.ts` fallback in the constant's docstring). Rekey `remaining_unchecked_are_verification_only(phases_text)` [VERIFY: grep -n "def remaining_unchecked_are_verification_only" user/scripts/lazy_core.py] so a `- [ ]` row is verification-exempt when the row OR its enclosing subsection carries the marker, INDEPENDENT of bold-header/heading free text; preserve the existing fence-awareness and Superseded-phase handling. Demote `_VERIFICATION_SECTION_RE` [VERIFY: grep -n "_VERIFICATION_SECTION_RE" user/scripts/lazy_core.py] to a deprecation shim: when the regex matches a header while the canonical marker is ABSENT from that subsection's rows, append a diagnostic to the `_DIAGNOSTICS` list [VERIFY: grep -n "_DIAGNOSTICS" user/scripts/lazy_core.py] naming the un-migrated producer — the shim still exempts the rows (no regression for un-migrated PHASES.md) but the gap is visible.
- **Spec requirements:** SPEC §2 ("Detector keys off the marker", "Single source of truth", regex "reduced to a deprecation shim that warns").
- **Batch:** 1.

### WU-4 — producers emit the canonical marker
- **Scope:** PHASES P2 deliverables 4–5 (`/spec-phases` and `/blocked-resolution` producers emit the marker).
- **TDD:** no (component-prose authoring; covered by WU-5's lockstep test).
- **Files to create/modify:** `user/skills/_components/phases-runtime-verification.md`, `user/skills/_components/blocked-resolution.md`.
- **Implementation goal:** Update `phases-runtime-verification.md` [VERIFY: ls user/skills/_components/phases-runtime-verification.md] so `/spec-phases`-authored verification rows emit the canonical `<!-- verification-only -->` marker (reference the SSOT constant by VALUE with a note pointing at `lazy_core.py:_VERIFICATION_ONLY_MARKER`). Update `blocked-resolution.md` [VERIFY: grep -n "seam audit" user/skills/_components/blocked-resolution.md] so `/blocked-resolution`-authored verification / full-chain-seam-audit rows emit the same marker. Do NOT re-hardcode a divergent string — the marker string is the SSOT in `lazy_core.py`.
- **Spec requirements:** SPEC §2 ("Producers emit the marker"); §UX "Verification gate no longer whack-a-moles".
- **Batch:** 2 (no file conflict with WU-3, but sequenced after WU-3 so the SSOT constant value exists to reference).

### WU-5 — Phase 2 detector tests (novel-header, un-migrated-warning, lockstep)
- **Scope:** PHASES P2 deliverable 6 (tests a–c).
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/test_lazy_core.py` (+ `_TESTS` registration).
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** (a) a verification subsection with a NEVER-BEFORE-SEEN header text + the marker present → `remaining_unchecked_are_verification_only` returns True (gate passes via marker, no regex growth); (b) a verification subsection WITHOUT the marker (un-migrated) → the deprecation-shim diagnostic is emitted (assert a `_DIAGNOSTICS` entry naming the un-migrated case), does NOT silently pass clean; (c) lockstep test: read the marker string from the component prose AND the `lazy_core.py` SSOT constant and assert string equality. All registered in `_TESTS`.
- **Implementation goal:** Tests pass against WU-3 + WU-4 and are collected by the runner.
- **Spec requirements:** SPEC §Validation Criteria rows "Verification gate survives a novel phrasing", "Un-migrated producer is surfaced".
- **Batch:** 3 (depends on WU-3 + WU-4; overlaps `test_lazy_core.py` with WU-2 → separate batch).

### Batch overview — Phase P2
| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-3 | Solo | None |
| 2 | WU-4 | Solo | None (sequenced after WU-3 — references the SSOT value) |
| 3 | WU-5 | Solo | None (sequenced after WU-3 + WU-4; `test_lazy_core.py` overlaps WU-2 → separate batch) |

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** P1 and P2 are both independent and ready from the start; execute them in schedule order (P1 then P2) — they touch overlapping files (`test_lazy_core.py`) so run sequentially, not in parallel.
2. **Announce:** Print "Implementing harness-hardening-retro-fixes Phase N: [title]".
3. **Review prior context:** Re-read prior completed phases' Implementation Notes in PHASES.md. They take priority over the plan where they diverge.
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "harness-hardening-retro-fixes Phase N: [title] — committed as [hash]".
7. **Loop:** Return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (ToolSearch for task tools, create tasks for all WUs, update protocol).

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)
Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and this plan file. Do NOT rely on cached content.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — ZERO INLINE IMPLEMENTATION)
**PRE-FLIGHT CHECK:** confirm you will use `Agent` with `model: "sonnet"` for ALL code changes and will NOT `Edit`/`Write` source/test files — UNLESS you are a cycle subagent with no `Agent` tool, in which case perform the work INLINE (test-first) per the execution-environment note. Read before proceeding: `tdd-protocol.md`, `subagent-launch.md`, `tdd-test-agent.md`, `implementation-agent.md` (all under `~/.claude/skills/_components/`).
**POST-DISPATCH GATE:** verify the work was done via Agent (or inline under the override) and TDD discipline held.

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)
Read `~/.claude/skills/_components/subagent-review.md` and `~/.claude/skills/_components/mount-site-verification.md`. Produce a structured review report with a PASS / PASS-WITH-FIXES / NEEDS-REWORK verdict before Step B.3.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)
Read `~/.claude/skills/_components/phases-update.md`. Check off completed deliverables; add an Implementation Notes block (date, work completed, integration notes, pitfalls, files modified).

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)
Read `.claude/skill-config/quality-gates.md`. For Phase 1 (skill-prose + test): `python user/scripts/project-skills.py` + `python user/scripts/lint-skills.py --check-projected --check-capabilities` + `python -m pytest user/scripts/ -q` (or `python user/scripts/test_lazy_core.py`). For Phase 2 (touches `lazy_core.py` — shared): run the FULL lazy-family set — `python user/scripts/project-skills.py` + `python user/scripts/lint-skills.py --check-projected --check-capabilities` + `python -m pytest user/scripts/ -q` + `python user/scripts/lazy-state.py --test` + `python user/scripts/bug-state.py --test` + `python user/scripts/lazy_parity_audit.py --report`. 100% pass required.

#### Step B.5: Commit Batch
Read `.claude/skill-config/commit-policy.md`. Conventional Commits, no AI attribution, push allowed. Scope examples: `feat(harden-harness): ...`, `fix(lazy-core): ...`, `test(lazy-core): ...`.

#### Step B.6: Proceed to Next Batch
Checklist (all must be true): review report with verdict; PHASES.md updated; all gates pass; committed per policy.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)
Read `~/.claude/skills/_components/integration-verification.md`. For Phase 2, confirm the rekeyed detector + producer markers + lockstep test agree and the full lazy-family gate is green.

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)
Read `~/.claude/skills/_components/claude-md-review.md`. Phase 2 likely warrants a note in `user/scripts/CLAUDE.md` (the verification-detector now keys off `_VERIFICATION_ONLY_MARKER`; regex is a deprecation shim).

#### Commit and Push Post-Phase Changes
Read `.claude/skill-config/commit-policy.md` and commit + push (claude-config allows push). **ATOMIC GATE+COMMIT:** run the gate and commit as one chained command so an interrupted turn leaves committed, pushed state.

## Blocking Issue Protocol

If a blocking issue is encountered at any point:
1. Stop all in-progress work. Do not launch new subagents.
2. Commit and push any completed phases not yet committed.
3. Print a blocking-issue report (Completed phases + hashes; Blocked phase; Reason; Recovery suggestion; Remaining phases not attempted).
4. Do not work around the blocker.

Blocking issues include: a subagent failure unfixable after 2 retries; a quality-gate failure unfixable after 2 retries; a `lazy_parity_audit.py --report` drift that requires a contract/operator decision; any error needing architectural decisions beyond the specs. (For a contract/policy fork, write `NEEDS_INPUT.md` per the standard halt protocol instead.)

## Completion

When P1 + P2 are complete:
1. Run the full lazy-family quality-gate suite one final time.
2. Print a completion report (Features implemented; Total phases completed; Total commits; Final QG status; Commit log table; Implementation Notes summary).
3. Proceed to part 2 (`/execute-plan <part-2-path>`).
