---
kind: realign-plan
feature_id: harness-hardening-retro-fixes
status: Ready
created: 2026-06-17
phases: ["all"]
upstream_phases_hashes:
  unified-pipeline-orchestrator: 83a9c09f3f62f99d3c230e7cdd4dffebe4c84fe1372095a43efac22f5d601dfc
---

# Realign — harness-hardening-retro-fixes (2026-06-17)

> Read-only reality check against completed upstream dependencies. Generated by `/realign-spec`.
> No SPEC or PHASES files were modified by this analysis. (Minor patches applied separately in Step 5a — see "Recommended next step".)

## Inputs

- **Downstream SPEC:** `docs/features/harness-hardening-retro-fixes/SPEC.md`
- **Downstream PHASES:** not authored yet (state machine is at Step 4.6, pre-`/spec-phases`) — only SPEC-level drift assessed.
- **Upstream dependencies checked (kind=hard, Status=Complete):**
  - `unified-pipeline-orchestrator` — PHASES.md (+ 1 plan-relevant doc read: `toolify-bar.md`; the 5 `plans/all-phases-*.md` were not topically distinct from the PHASES Implementation Notes) — Status=Complete (ROADMAP strikethrough+✅ COMPLETE AND SPEC `**Status:** Complete`)

## Aligned

Places the downstream correctly anticipates the upstream's actual state. Listed for diff completeness and reviewer confidence.

- **Toolify framework exists as named** — the downstream's hard-dep reason ("consumes the toolify framework: miner + deterministic-only bar + candidate schema") matches exactly what upstream Phase 4 shipped: `user/scripts/toolify-miner.py` (read-only, stdlib), the deterministic-only bar (3-predicate AND-gate), and the candidate schema (9 fields incl. `above_bar`, `deterministic`, `score`) — all documented in `toolify-bar.md`.
- **`/spec-bug` auto-initiation is the upstream's planned hand-off** — `toolify-bar.md` promotion-checklist step 7 explicitly names `harness-hardening-retro-fixes` as the consumer that "may auto-initiate steps 1-4 as a `/spec-bug` when it detects a dance in-run", and RESEARCH_SUMMARY Locked Decision 5 (upstream) reciprocates. The downstream's SPEC §1 ("auto-identify toolification candidates → spin off a `/spec-bug`") plugs into the exact seam the upstream left open.
- **`--type bug` front-enqueue spin-off mechanism is shipped** — the downstream's spin-off action (front-enqueue a `/spec`/`/spec-bug` via the `adhoc-enqueue` protocol) depends on `_components/adhoc-enqueue.md` having a `--type bug` path routing to `bug-state.py --enqueue-adhoc`. Upstream Phase 3 shipped exactly this (default `feature`, additive `--type bug` → `docs/bugs/queue.json` + `docs/bugs/<id>/`), and the component prose even names "a harden-harness spin-off" as the canonical `--type bug` use case.
- **`-followups` queue-trim + `mcp-tests` symlink fixes are upstream-owned (correctly NOT duplicated)** — the downstream SPEC Executive Summary cross-references both as fixed by upstream's enhanced `__mark_complete__` (`spec_dir`-keyed trim) and `--gate-coverage` (symlink-resolving). Upstream Phase 5 shipped both. The downstream correctly scopes them out.
- **Deterministic-only bar boundary respected** — the downstream's over-fit signal "the friction is a repeated deterministic dance (toolify candidate per the miner)" aligns with the upstream bar's deterministic predicate (judgment steps — `--verify-ledger`, recovery dispatch — explicitly below-bar). The downstream does not assume the miner classifies judgment steps as toolifiable.

## Drift

### Minor

Small wording/path/name corrections. Safe to apply inline to the downstream SPEC/PHASES.

| # | Location (file:section) | Current text | Upstream reality | Suggested patch |
|---|-------------------------|--------------|------------------|-----------------|
| 1 | SPEC.md § Implementation Phases, Phase 1 | "Wire the toolify-candidate trigger to the miner from `unified-pipeline-orchestrator`." | The upstream `toolify-miner.py` is a **read-only, OFFLINE session-log miner** (it parses `~/.claude/projects/**/*.jsonl` after the fact); it is NOT an in-run hook. Upstream `toolify-bar.md` frames harden-harness's role as auto-initiating the promotion-checklist's steps 1-4 as a `/spec-bug` — i.e. harden-harness does its OWN in-run dance-recurrence detection and spins off the same `/spec-bug` the offline miner's checklist describes; it does not call `toolify-miner.py` mid-cycle. | "Wire the toolify-candidate trigger to the toolify framework's deterministic-only bar + promotion checklist from `unified-pipeline-orchestrator` (the offline `toolify-miner.py` proposes; harden-harness performs the in-run dance-recurrence detection and spins off a `/spec-bug` per the framework's promotion checklist — it does not invoke the offline miner mid-cycle)." |

### Moderate

(none)

### Severe

(none)

## Recommended next step

Exactly one of:

- `inline-edit` — apply Minor patches to SPEC.md / PHASES.md as listed above; nothing else required.

**Recommended:** `inline-edit`

**Why:** The only drift is a single SPEC wording sentence (Phase 1) that could mislead an implementer into invoking the offline `toolify-miner.py` in-run; the framework upstream actually shipped (read-only offline miner + promotion checklist with harden-harness as the named auto-initiator) is fully present and matches the downstream design — correcting the sentence is a local, mechanical edit with no design-intent change.
