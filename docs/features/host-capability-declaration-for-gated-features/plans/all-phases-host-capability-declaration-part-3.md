---
kind: implementation-plan
feature_id: host-capability-declaration-for-gated-features
status: Complete
complexity: complex
phases: [6]
---

# Implementation Plan — host-capability-declaration-for-gated-features (v2)

> Mobile plan — generated by /write-plan; executed by /execute-plan in a separate (possibly fresh) context.

> **Plan series:** part 3 of 3. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\features\host-capability-declaration-for-gated-features\plans\all-phases-host-capability-declaration-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\features\host-capability-declaration-for-gated-features\plans\all-phases-host-capability-declaration-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\features\host-capability-declaration-for-gated-features\plans\all-phases-host-capability-declaration-part-3.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\host-capability-declaration-for-gated-features\PHASES.md` (host-capability-declaration-for-gated-features, 6 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\host-capability-declaration-for-gated-features\SPEC.md`

**Total phases:** 6 (this part covers Phase 6 — the harness-contract closeout)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`. (Wrapper `SKILL.md` + `_components/*.md` + doc `CLAUDE.md` edits in this part are documentation/prose — author them directly OR via subagent per the executing context's tooling.)

> **Inline-execution override note:** When this plan is executed by a `/lazy-batch` cycle subagent that lacks the `Agent` tool, perform ALL work INLINE with Read/Edit/Write, still test-first for the `bug-state.py` parity WU.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `.claude/skill-config/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — composes/soft deps only) | — | — | The coupled-pair lockstep rule and the parity-audit contract are repo-level invariants (project `CLAUDE.md` + `user/scripts/CLAUDE.md`), not an upstream feature plan. |

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **Code/test work delegated to Sonnet subagents** (inline-override path does it inline); the `bug-state.py` parity WU is test-first. Wrapper/doc prose edits author directly.
2. All edits in the current worktree — NEVER create worktrees.
3. The parity WU goes through the test-first pipeline (mirror + a parity-audit-green assertion).
4. PHASES.md updated AFTER the batch completes.
5. Output reviewed before continuing.
6. Mistakes fixed immediately.
7. Integration verification after the phase.
8. Doc CLAUDE.md updated this phase (it IS the doc phase).
9. Phase committed + pushed.
10. (N/A.)
11. Self-contained.
12. **Read listed components before each step.**

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | host-capability-declaration | P6 | Parity mirror + wrapper glue + coupled-pair lockstep + docs | part-2 P5 | Solo |

## Work Units

- [x] WU-1 — `bug-state.py` parity mirror of shared `lazy_core` helpers + `lazy-parity-manifest.json` registration + parity audit green
- [x] WU-2 — Coupled-pair wrapper glue (4 `/lazy*` SKILL.md) + docs (`user/scripts/CLAUDE.md`, sentinel schema) + full-gate closeout

---

### Phase: host-capability-declaration P6 — Parity mirror + wrapper glue + coupled-pair lockstep + docs

**Goal:** Close the harness contracts — mirror the shared helpers into the bug pipeline, register parity, add notification/skip glue to all four coupled `/lazy*` wrappers in lockstep, and document the new axis. No new behavior.
**Entry criteria:** part-2 P5 complete (the `lazy-state.py` behavior + shared helpers to mirror exist).
**SPEC.md references:** "Where it lives" (coupled pairs + `bug-state.py` parity); the project `CLAUDE.md` Coupled Skill Pairs table; the SPEC's vN upgrade paths (documented out-of-scope).

#### WU-1 — `bug-state.py` parity + manifest registration

- **Scope:** mirror every new shared `lazy_core` helper into the bug pipeline's reach; register canonical units (or justified divergences) in the parity manifest; parity audit green.
- **TDD:** yes (a parity-audit-green assertion + any mirrored-helper test).
- **Files to create/modify:**
  - `user/scripts/bug-state.py` — mirror the shared-helper usage where the bug pipeline shares it (the `requires_host` PARSING + the unknown-id fail-fast are mirrored for parity; the capability-miss skip is feature-pipeline-shaped, so the shared `lazy_core` helpers must not diverge even if `bug-state.py` does not currently expose a capability-miss branch — register that as a justified divergence if applicable).
  - `user/scripts/lazy-parity-manifest.json` — add the new canonical behavioral units / divergences.
  - `user/scripts/test_lazy_core.py` (or `bug-state.py` in-file fixtures) — assert the mirrored helper behaves identically; `bug-state.py --test` green.
- **Test files:** `user/scripts/test_lazy_core.py` / `bug-state.py --test`.
- **Test expectations:** `python user/scripts/lazy_parity_audit.py --report` reports NO unexplained drift [VERIFY: grep -n "def audit_state_script_parity" user/scripts/lazy_parity_audit.py] (expected line ~313); a new canonical unit is mirrored to its twin or registered as a divergence.
- **Implementation goal:** the shared helpers in `lazy_core.py` are imported identically by both state machines; the manifest reflects the new surface. Per the Coupling Rule, change `lazy-state.py` is already done (parts 1-2) — this WU keeps the twin + manifest in sync.
- **Spec requirements:** "The coupled `/lazy` ↔ `/lazy-cloud` and `/lazy-batch` ↔ `/lazy-batch-cloud` pairs (and `bug-state.py`, for parity of any shared `lazy_core` helper) update in lockstep."
- **Batch:** 1.
- **Note:** if `bug-state.py --test` output changes, REGENERATE `tests/baselines/bug-state-test-baseline.txt` via `_normalize_smoke_output` — never hand-edit.

#### WU-2 — Coupled-pair wrapper glue + docs + full-gate closeout

- **Scope:** add the capability-miss probe-field → notification + live-queue skip to all four coupled `/lazy*` wrappers in lockstep; document the new host-capability axis; run the full gate set.
- **TDD:** no (prose/wrapper edits + docs; correctness gated by `project-skills.py` + `lint-skills.py` + the parity audit, not unit tests).
- **Files to create/modify:**
  - `user/skills/lazy/SKILL.md` ↔ `repos/algobooth/.claude/skills/lazy-cloud/SKILL.md` — mirror the capability-miss probe-field handling (notification + skip) per the coupled-pair rule; update each file's State Machine Summary for the new `host-capability-saturated` terminal.
  - `user/skills/lazy-batch/SKILL.md` ↔ `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` — mirror the notification/skip glue; update the "Differences from /lazy-batch" block + State Machine Summary if the terminal set changed.
  - `user/scripts/CLAUDE.md` — extend "Three environments + the device axis" with the host-capability axis; document the `DEFERRED_REQUIRES_HOST.md` re-open contract alongside `DEFERRED_REQUIRES_DEVICE.md`; note the SPEC's vN seams (namespaced-suffix versioning, OR-via-profiles, manifest override) as out-of-scope.
  - `user/skills/_components/sentinel-frontmatter.md` — confirm the part-2 `deferred-requires-host` schema is present + in the decision table (this WU finalizes any remaining doc rows).
- **Test files:** none (gate-validated).
- **Test expectations / gates:** `python user/scripts/project-skills.py` (re-expands `!cat` refs, no circular include) + `python user/scripts/lint-skills.py --check-projected --check-capabilities` (no broken injection / embedded pattern / capability mismatch) both clean; the wrapper-prose mirror passes the coupled-pair lockstep eyeball-diff.
- **Implementation goal:** the orchestrator wrappers translate the new probe field into a PushNotification + an end-of-run flush + a live-queue skip, mirrored across the coupled pairs; the docs make the new axis discoverable.
- **Spec requirements:** "the `/lazy-batch` (+ `/lazy-batch-cloud`) wrappers carrying only thin dispatch/notification glue"; the notification text from "What happens on a capability miss"; the "Documented vN upgrade paths (out of v1 scope)" block.
- **Batch:** 2.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | N/A |
| 2 | WU-2 | Solo | None (different files from WU-1) |

**Integration Notes:** Terminal phase — implementation complete. The Step-9 MCP gate is operator-exempt for `claude-config` (no MCP server; `SKIP_MCP_TEST.md` granted_by operator pointing at the passing quality gates per `.claude/skill-config/quality-gates.md`). The orchestrator's `__mark_complete__` gate owns the final SPEC/PHASES Complete flip + the COMPLETED.md receipt — do NOT flip them in this plan.

---

## Execution Protocol

This protocol governs the autonomous execution of this part. Follow it exactly.

### Phase Selection Loop

Repeat until P6 is complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** P6 (its entry criteria — parts 1-2 complete — hold once you reach this part). If not, jump to Blocking Issue Protocol.
2. **Announce:** "Implementing host-capability-declaration Phase 6: Parity + wrapper glue + docs"
3. **Review prior context:** re-read parts 1-2's Implementation Notes in PHASES.md.
4. **Execute both batches** per Per-Batch Steps.
5. **Run Post-Phase Steps.**
6. **Report:** "host-capability-declaration Phase 6 — committed as [hash]"

### Step 0: Initialize Task Tracking (MANDATORY — FIRST)
Read `~/.claude/skills/_components/task-tracking.md` and follow it exactly.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read PHASES.md, SPEC.md, this plan, and the project `CLAUDE.md` Coupled Skill Pairs table.

#### Step B.1: Implement the Work Unit
WU-1 is test-first (parity mirror + assertion). WU-2 is prose/wrapper/doc edits — author them, then validate via gates (no unit test). Read `~/.claude/skills/_components/tdd-protocol.md` + `subagent-launch.md` for WU-1.

#### Step B.2: Review Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md`. For WU-2, the review MUST include the coupled-pair eyeball-diff: confirm each `/lazy` ↔ `/lazy-cloud` and `/lazy-batch` ↔ `/lazy-batch-cloud` edit is mirrored and the only divergences are the tabulated intended ones. Produce a verdict.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `~/.claude/skills/_components/phases-update.md`. Check off Phase 6 deliverables, add Implementation Notes, tick this part's `- [ ] WU-N` rows. When the LAST phase's work lands, set the top-level PHASES `**Status:**` to `In-progress` (NOT Complete — validation/mark-complete is gate-owned).

#### Step B.4: Run Quality Gates (MANDATORY — the FULL set, this is feature completion)
Read `.claude/skill-config/quality-gates.md`. Run the FULL "mixed / feature completion" set: `python user/scripts/project-skills.py` + `python user/scripts/lint-skills.py --check-projected --check-capabilities` + `python -m pytest user/scripts/ -q` + `python user/scripts/lazy_parity_audit.py --report`. 100% pass — do NOT triage failures as preexisting.

#### Step B.5: Commit Batch
Read `.claude/skill-config/commit-policy.md` (fallback `commit-and-push.md`). Atomic gate+commit chain to `main`.

#### Step B.6: Proceed to Next Batch
Checklist: review verdict (+ coupled-pair diff for WU-2); PHASES.md + WU-N updated; full gate set passes; committed. All true before proceeding.

### Post-Phase Steps

#### Integration Verification (MANDATORY)
Read `~/.claude/skills/_components/integration-verification.md`. Confirm: both `--test` suites green, parity audit green, projected skills re-expand cleanly, lint clean.

#### Update CLAUDE.md Files (MANDATORY)
Read `~/.claude/skills/_components/claude-md-review.md`. Confirm `user/scripts/CLAUDE.md` (the host-capability axis) and the project `CLAUDE.md` (if a new sentinel/hook note is warranted) are current.

#### Commit and Push Post-Phase Changes
Per the commit policy.

---

## Blocking Issue Protocol

If a blocking issue is encountered:

1. **Stop all in-progress work.**
2. **Commit and push any completed work.**
3. **Write `BLOCKED.md`** per `~/.claude/skills/_components/sentinel-frontmatter.md` on `main`, and print:

   ## Implementation Batch — Blocked
   **Completed phases:** [list with hashes]
   **Blocked phase:** host-capability-declaration Phase 6
   **Reason:** [specific — e.g. parity drift requiring a divergence-registration decision]
   **Recovery suggestion:** [what to do]
   **Remaining work (not attempted):** [list]

4. **Do not work around the blocker.**

Blocking issues include: a parity-audit drift that needs an architectural decision (mirror vs. register-divergence is genuinely ambiguous); a `lint-skills`/`project-skills` failure unfixable after 2 retries; a coupled-pair mirror conflict the SPEC didn't anticipate.

---

## Completion

When Phase 6 is complete (feature implementation done):

1. **Run the full quality-gate suite** one final time (the four gates above).
2. Flip THIS plan part's frontmatter `status:` to `Complete` once every `- [ ] WU-N` row is ticked; set top-level PHASES `**Status:**` to `In-progress`.
3. **Do NOT** write COMPLETED.md or flip SPEC/PHASES to Complete — the `__mark_complete__` gate (operator MCP-skip + receipt) owns that.
4. **Print a completion report:**

   ## Implementation Batch — Complete (part 3 of 3 — feature implementation done)
   **Phases completed:** P6 (and the feature's P1-P5 across parts 1-2)
   **Commit log:** | Commit | Phase | Title |
   **Final quality-gate status:** all green (pytest + parity + project-skills + lint).
   **Next:** state machine routes to the Step-9 validation gate (operator-exempt for this repo) → `__mark_complete__`.
