---
kind: realign-plan
feature_id: parallel-worktree-batch-execution
status: Ready
created: 2026-07-04
phases: []
upstream_phases_hashes:
  queue-dependency-dag: be0a74ae65e1c862186b4a6b3efd5e68b87f77b21b50e280f470e387326e728a
---

# Realign — parallel-worktree-batch-execution (2026-07-04)

> Read-only reality check against completed upstream dependencies. Generated by `/realign-spec`.
> No SPEC or PHASES files were modified by this analysis.

## Inputs

- **Downstream SPEC:** `docs/features/parallel-worktree-batch-execution/SPEC.md`
- **Downstream PHASES:** `docs/features/parallel-worktree-batch-execution/PHASES.md`
- **Upstream dependencies checked (kind=hard, Status=Complete):**
  - `queue-dependency-dag` — PHASES.md (Status: Complete; COMPLETED.md receipt present) + 1 plan
    (`all-phases-queue-dependency-dag-part-1.md`; not read — the readiness/predicate contract the
    downstream consumes is fully captured in the upstream PHASES.md Implementation/Integration
    Notes, so plan-level detail added no signal).

## Aligned

Places the downstream correctly anticipates the upstream's actual state. Listed for diff
completeness and reviewer confidence. Every symbol the downstream's readiness predicate consumes
was verified present in the live `lazy_core.py` / `lazy-state.py`.

- **Readiness predicate source** — Downstream SPEC D3-A / Technical Design and PHASES Phase 1
  assume dep-readiness is computed from `lazy_core` reads (`dep_completion_status(...) ==
  complete`, i.e. "queue `deps` all Complete-with-receipt"). Upstream shipped
  `dep_completion_status(dep_id, repo_root, *, pipeline, id_dir_map=None)` in `lazy_core.py`
  (verified `lazy_core.py:660`) returning exactly `complete | incomplete |
  unsatisfiable-superseded | unsatisfiable-wont-fix | missing`. Confirmed.
- **`dep_ids` / `detect_dep_cycle` helpers** — Downstream PHASES Cross-feature Integration Notes
  name `lazy_core.dep_ids` and `detect_dep_cycle` as consumed (not re-implemented). Both present
  in `lazy_core.py` (`:398`, `:416`). Confirmed.
- **`dep_gated` probe key** — Downstream SPEC ("reads the `dep_gated` probe key + the enforced
  `deps` field as its readiness predicate") matches the upstream key emitted at
  `lazy-state.py:182` (only when non-empty). This is exactly the "additive keys only, no contract
  change owed" seam the upstream PHASES named for this downstream. Confirmed.
- **Enforced `deps` field + `--sync-deps` feeder** — Downstream assumes the `deps` field is
  script-owned and machine-enforced. Upstream shipped `validate_queue_deps` (`lazy_core.py:493`),
  the dep-gate hold in both `compute_state()` walk loops, the `queue-exhausted-dependency-gated`
  terminal (`lazy_core.py:9928`), and `--sync-deps`/`--enqueue-adhoc --deps`. Confirmed.
- **`parse_independent_marker` isolation rail** — Downstream SPEC D3-A conjoins
  `parse_independent_marker` truth with dep-readiness. That helper is present
  (`lazy_core.py:12473`); the downstream correctly attributes it to the pre-existing skip-ahead
  rail, NOT to `queue-dependency-dag` — so no mis-sourced dependency. Confirmed.

## Drift

### Minor

(none) — no wording/path/name corrections needed. The downstream SPEC and PHASES were authored
2026-07-04, the same day `queue-dependency-dag` completed, with the upstream contract already on
this branch's base; every referenced symbol/path resolves to the shipped reality.

### Moderate

(none)

### Severe

(none)

## Recommended next step

Exactly one of:

- `inline-edit` — apply Minor patches to SPEC.md / PHASES.md as listed above; nothing else
  required.
- `add-phase` — invoke `/add-phase` with a corrective phase.
- `respec` — halt and redesign.

**Recommended:** `inline-edit` (no-op — the Minor table is empty)

**Why:** The downstream perfectly anticipates the completed upstream's actual contract — every
readiness-predicate symbol it consumes (`dep_completion_status`, `dep_ids`, `detect_dep_cycle`,
`validate_queue_deps`, the `dep_gated` probe key, `--sync-deps`, and the enforced `deps` field)
was verified present in the live tree with the assumed shape, so there is nothing to correct.
