---
kind: implementation-plan
feature_id: long-build-and-runtime-ownership
status: In-progress
created: 2026-06-20
complexity: complex
phases: [5]
---

> **Mobile plan** — generated by `/write-plan` (via `/plan-feature`) on 2026-06-20.
> To execute: `/execute-plan <this-path>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 5 of 5. Sibling parts:
> - part 1: `docs/features/long-build-and-runtime-ownership/plans/all-phases-long-build-and-runtime-ownership-part-1.md`
> - part 2: `docs/features/long-build-and-runtime-ownership/plans/all-phases-long-build-and-runtime-ownership-part-2.md`
> - part 3: `docs/features/long-build-and-runtime-ownership/plans/all-phases-long-build-and-runtime-ownership-part-3.md`
> - part 4: `docs/features/long-build-and-runtime-ownership/plans/all-phases-long-build-and-runtime-ownership-part-4.md`
> - part 5: `docs/features/long-build-and-runtime-ownership/plans/all-phases-long-build-and-runtime-ownership-part-5.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — long-build-and-runtime-ownership (Phase 5)  (v2)

**PHASES.md files:** `docs/features/long-build-and-runtime-ownership/PHASES.md` (Phase 5)
**SPEC.md files:** `docs/features/long-build-and-runtime-ownership/SPEC.md`
**Total phases:** 5 (this part: Phase 5, final)
**Plan version:** v2 (reference-based)

## EXECUTION MODEL — READ THIS FIRST

Orchestrator + Sonnet subagent. Orchestrator: `Agent`, `Read`, `Bash` (gates only), task tools. Subagent: `Edit`/`Write`/`Read`/`Bash`/`Grep`/`Glob`.

**HARD CONSTRAINT:** orchestrator MUST NOT `Edit`/`Write` source/test files (compose `Agent({model:"sonnet"})`). ONLY direct edits: `PHASES.md`, `CLAUDE.md`. NOTE: the SKILL.md wrapper files edited in this part (`lazy-batch/SKILL.md`, `lazy/SKILL.md`, the cloud variants) are SKILL prose, NOT `.ts`/`.rs` source — but they ARE coupled-pair-lockstep-sensitive; treat them with the same subagent-dispatch discipline (or inline under the lazy override) and diff the coupled sibling after every edit.

> **INLINE-EXECUTION OVERRIDE (lazy pipeline):** a `/lazy-batch` cycle subagent with NO `Agent` tool implements ALL work INLINE with `Edit`/`Write`. Outside that context the orchestrator+subagent model applies.

## COMPONENT LOADING PROTOCOL
Before each step, `Read` the listed component files from disk. After compaction, re-read this plan first.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| unified-pipeline-orchestrator | composes | `docs/features/unified-pipeline-orchestrator/PHASES.md` | Phase 5 rewires that feature's `/lazy-batch` Step 1d.0 `--ensure-runtime` call to consume the new M4 verdict, mirrored into the coupled `/lazy`/`/lazy-cloud`/`/lazy-batch-cloud` wrappers per the claude-config CLAUDE.md coupled-pair rules. |

## MANDATORY RULES — DO NOT SKIP ANY STEP
1. Implementation/test via Sonnet subagents (EXCEPTION: lazy inline override; trivial PASS-WITH-FIXES). 2. No worktrees. 3. TDD where testable (this part is mostly wrapper prose — no new state-machine logic; the testable surface is the unchanged `--ensure-runtime` verdict + the parity audit). 4. PHASES.md after the phase. 5. Review every output. 6. Fix immediately. 7. Integration verification after the phase. 8. CLAUDE.md updated (`user/scripts/CLAUDE.md` notes the verdict-based Step 1d.0 routing). 9. Commit+push. 10. Self-contained. 11. Read components first.
12. **COUPLED-PAIR LOCKSTEP (HARD — claude-config CLAUDE.md):** `/lazy` ↔ `/lazy-cloud` and `/lazy-batch` ↔ `/lazy-batch-cloud` share a dispatch contract. After editing EITHER member, immediately diff the other and confirm the divergence matches the documented intended-divergence set. Any state-machine change goes into `lazy_core.py`/`lazy-state.py`, NOT the wrapper prose — this part changes ZERO state-machine logic, only the prose that ROUTES on the script's verdict JSON.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | long-build-and-runtime-ownership | P5 | Wire the orchestrator (consume the verdict + coupled-wrapper mirror) | Parts 2 (verdict), 3 (guard + transient contract), 4 (atomic promotion) | Sequential batches |

## Work Units

- [ ] WU-1 — Rewire `/lazy-batch` Step 1d.0 to consume the M4 verdict + guard-takeover wiring
- [ ] WU-2 — Coupled-pair mirror: `/lazy` ↔ `/lazy-cloud`, `/lazy-batch` ↔ `/lazy-batch-cloud`
- [ ] WU-3 — `user/scripts/CLAUDE.md` note + `lazy_parity_audit.py` clean + `project-skills.py` re-projection

## Phase: long-build-and-runtime-ownership P5 — Wire the orchestrator (Persistent Service contract consumption + coupled-pair mirror)

**Goal:** Replace the hand-rolled Step 1d.0 poll loop with the single liveness primitive call consuming the M4 verdict (READY/STALE→READY proceed; HIJACKED/BLOCKED → `BLOCKED.md` `blocker_kind: mcp-runtime-unready`); wire the guard-redirect takeover (orchestrator runs a redirected build via the Transient Build contract + atomic promotion); mirror every wrapper-prose change into the coupled variants.

**Entry criteria:** Parts 2, 3, 4 complete (the M4 verdict, the guard + `run_transient_build`, atomic promotion all exist).

**SPEC.md references:** `## Technical Design` → "Phase 5 — Wire the orchestrator"; Validation rows "Orchestrator stops hand-rolling poll loops" + "Hijacked port surfaces a blocker, never SIGKILL".

### Batch overview

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | edits `user/skills/lazy-batch/SKILL.md` |
| 2 | WU-2 | Solo | edits the four coupled wrapper SKILL.md files |
| 3 | WU-3 | Solo | edits `user/scripts/CLAUDE.md`; runs audits |

> WU-1 (the canonical `/lazy-batch` change) precedes WU-2 (the mirror) so the mirror copies a settled source. WU-3 audits last.

### WU-1 — Rewire `/lazy-batch` Step 1d.0 + guard-takeover

**Scope (PHASES deliverables):** "`user/skills/lazy-batch/SKILL.md` Step 1d.0 rewired: consume the M4 verdict from `--ensure-runtime`; map `state ∈ {HIJACKED, BLOCKED}` (and unrecoverable `mcp_tools_present: false`) to a `BLOCKED.md` `blocker_kind: mcp-runtime-unready` halt; READY/STALE→READY/booted proceed." + "Guard-takeover wiring in `/lazy-batch`: when `long-build-ownership-guard.sh` denies a subagent long build … the orchestrator session runs the build under the Transient Build contract (`run_transient_build` + `promote_artifact_atomically`) instead of the subagent."

**TDD:** no (skill prose — no new code path; routes on the part-2 verdict, which is already tested). Verification is the integration check + the unchanged `--ensure-runtime --test`.

**Files to create/modify:**
- `user/skills/lazy-batch/SKILL.md` — Step 1d.0 [VERIFY: `grep -n "1d.0" user/skills/lazy-batch/SKILL.md`]: replace the `{status: ready|booted|stale-rebuilt}` consumption with the M4 verdict consumption (READY/STALE→READY/booted proceed; HIJACKED/BLOCKED/unrecoverable → `BLOCKED.md` `blocker_kind: mcp-runtime-unready`). Add a guard-takeover paragraph in the orchestrator's long-build handling: on the part-3 guard deny (takeover signature), the orchestrator runs the build via `run_transient_build` + `promote_artifact_atomically` (part 3/4).

**Test files:** none new; the `--ensure-runtime` verdict is covered by part 2.

**Implementation goal:** Collapse the hand-rolled rebuild→health-poll→inspect loop into the single `--ensure-runtime` call consuming the verdict, and wire the guard-takeover so a redirected long build runs under orchestrator ownership. No state-machine logic moves into the wrapper — it routes on the script JSON.

**Spec requirements:** SPEC Phase 5; Validation "Orchestrator stops hand-rolling poll loops" + "Hijacked port surfaces a blocker, never SIGKILL".

**Batch:** 1.

### WU-2 — Coupled-pair mirror

**Scope (PHASES deliverables):** "Mirror into `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` per the coupled-pair rule … record the divergence (the verdict-consumption block is workstation-only) in its 'Differences from /lazy-batch' table." + "Mirror the relevant wrapper prose into `user/skills/lazy/SKILL.md` ↔ `repos/algobooth/.claude/skills/lazy-cloud/SKILL.md`." + "Update each coupled file's State Machine Summary / 'Differences' block."

**TDD:** no (coupled-pair prose).

**Files to create/modify:**
- `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` — record the divergence: cloud Step 9 returns `__write_deferred_non_cloud__` and never reaches Step 1d.0, so the verdict-consumption is workstation-only — note it in the "Differences from /lazy-batch" table [VERIFY: `grep -rn "Differences from /lazy-batch" repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md`], do NOT duplicate the runtime boot.
- `user/skills/lazy/SKILL.md` [VERIFY: `ls user/skills/lazy/SKILL.md`] ↔ `repos/algobooth/.claude/skills/lazy-cloud/SKILL.md` [VERIFY: `ls repos/algobooth/.claude/skills/lazy-cloud/SKILL.md`] — mirror only the shared dispatch glue referencing the runtime verdict; keep the `--cloud` divergence intact.
- Update each coupled file's State Machine Summary / "Differences" block so the dispatch table reflects the verdict-based routing + the `mcp-runtime-unready` BLOCKED terminal.

**Test files:** none.

**Implementation goal:** Keep the coupled pairs in lockstep per the claude-config CLAUDE.md coupling rules — the only intended divergence is `--cloud`/cloud-deferral; the verdict-consumption shape is mirrored, the cloud-specific exemption recorded in the Differences block.

**Spec requirements:** SPEC Phase 5 "mirror into the `/lazy-batch-cloud` and `/lazy`/`/lazy-cloud` coupled variants per their coupling rules".

**Batch:** 2.

### WU-3 — CLAUDE.md note + parity audit + re-projection

**Scope (PHASES deliverables):** "`lazy_parity_audit.py` run clean after the wrapper edits." + "`python ~/.claude/scripts/project-skills.py` re-run so the per-repo projection picks up the wrapper + skill-config edits." + "`user/scripts/CLAUDE.md` — note the verdict-based Step 1d.0 routing in the lifecycle/CLI doc."

**TDD:** no (doc + audits).

**Files to create/modify:**
- `user/scripts/CLAUDE.md` — note the verdict-based Step 1d.0 routing (orchestrator may edit CLAUDE.md directly).
- Run `python3 user/scripts/lazy_parity_audit.py` [VERIFY: `ls user/scripts/lazy_parity_audit.py`] → must report clean (coupled-pair + bug-state parity).
- Run `python ~/.claude/scripts/project-skills.py` → confirm the per-repo projection expands the wrapper + skill-config edits with no errors / no broken injections.

**Test files:** none; the audits ARE the gate.

**Implementation goal:** Close the loop — document the routing change and prove the coupled-pair lockstep + projection are clean.

**Spec requirements:** SPEC Phase 5; claude-config CLAUDE.md coupling rules + `project-skills.py` discipline.

**Batch:** 3.

---

## Execution Protocol

### Phase Selection Loop
One phase (P5, final). Batch 1 (WU-1) → Batch 2 (WU-2) → Batch 3 (WU-3) → Post-Phase Steps → commit → STOP. After this part lands, the feature routes to the validation tail (the `not-required` MCP gate → coverage audit → `__mark_complete__`) — owned by the orchestrator, NOT this plan.

### Step 0: Initialize Task Tracking
Read `~/.claude/skills/_components/task-tracking.md`. (No task tools → rely on `- [ ] WU-N` rows.)

### Per-Batch Steps
- **B.0** Re-read sources: PHASES Phase 5 + ALL prior Implementation Notes (the verdict schema, terminal_blocker text, takeover-signature string, compose order from parts 2-4), SPEC Phase 5, the claude-config CLAUDE.md coupled-pair table, this plan. **Read the coupled sibling before editing either member of a pair.**
- **B.1** Launch (`tdd-protocol.md`, `subagent-launch.md`, `implementation-agent.md`). These WUs are prose (TDD=no); dispatch impl agents (or edit inline under the lazy override). NO new tests — the testable surface (the verdict) is part-2-covered.
- **B.2** Review (`subagent-review.md` + `mount-site-verification.md`). Load-bearing review for THIS part: the coupled-pair DIFF — after each wrapper edit, diff the sibling and confirm ONLY the documented intended divergences (`--cloud`, cloud-deferral) differ. A divergence outside that set is `NEEDS-REWORK`. Confirm the Step 1d.0 routing reads the FULL verdict (not a field-extracted subset) and maps HIJACKED/BLOCKED → the correct sentinel.
- **B.3** Update PHASES.md (`phases-update.md`): tick Phase 5 Deliverables. When THIS (last) phase's implementation work lands, set the TOP-LEVEL PHASES `**Status:**` to `In-progress` (NOT Complete — implementation done, validation pending; the top-level Complete flip + COMPLETED.md are `__mark_complete__`-gate-owned). Do NOT tick `<!-- verification-only -->` rows.
- **B.4** Quality gates (`quality-gates.md`): full hermetic set — `lazy-state.py --test`, `bug-state.py --test`, `lazy_coord.py --test`, `test_lazy_core.py` (all must stay green — no state-machine logic changed, so they should be unaffected), PLUS `lazy_parity_audit.py` clean and `project-skills.py` clean (WU-3). 100% pass.
- **B.4.5** MCP: **skip with a note** (`**MCP runtime:** not-required`; the end-to-end orchestrator behavior is the Phase-5 Runtime Verification spike + the MCP Integration Test Assertions, both checked at the validation tail / live run transcript — not an AlgoBooth MCP-server surface).
- **B.5** Commit (`.claude/skill-config/commit-policy.md`): e.g. `feat(long-build-and-runtime-ownership): P5 wire orchestrator to M4 verdict + coupled-pair mirror`.
- **B.6** Checklist → next batch.

### Post-Phase Steps
- Integration verification (`integration-verification.md`): confirm the rewired Step 1d.0 routes correctly on each verdict state (read the routing block against the part-2 verdict schema); confirm `lazy_parity_audit.py` clean and the projection clean.
- CLAUDE.md review (`claude-md-review.md`): `user/scripts/CLAUDE.md` updated in WU-3; confirm the repo-root CLAUDE.md coupled-pair table + Hooks table remain accurate.
- Commit+push. Flip THIS plan-part `status:` to `Complete` once all `- [ ] WU-N` ticked + gates green. Set the top-level PHASES `**Status:**` to `In-progress` (last phase landed). Do NOT write COMPLETED.md or flip SPEC.md to Complete — those are `__mark_complete__`-gate-owned.

## Blocking Issue Protocol
Stop, commit+push completed batches, write `BLOCKED.md` at the feature dir, print the report, do NOT work around. Standard trigger set. A coupled-pair divergence you cannot reconcile is a blocker — surface it, do not guess.

## Completion
All three WUs landed + gates green (incl. `lazy_parity_audit.py` + `project-skills.py`): full gate suite once more, completion report (WUs, commit log, gate status). The feature's implementation is then COMPLETE — top-level PHASES `**Status:** In-progress`, awaiting the orchestrator-owned validation tail (`not-required` MCP gate → Locked-Decisions coverage audit → `__mark_complete__`, which flips SPEC.md to Complete and writes COMPLETED.md). This plan does NOT perform that tail.
