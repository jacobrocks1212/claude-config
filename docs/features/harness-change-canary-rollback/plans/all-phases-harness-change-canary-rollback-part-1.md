---
kind: implementation-plan
feature_id: harness-change-canary-rollback
status: In-progress
created: 2026-07-04
phases: [1]
complexity: complex
source_branch: claude/lazy-batch-cloud-r7mizh
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-04.
> To execute: `/execute-plan <this-path>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 3. Sibling parts:
> - part 1: `docs/features/harness-change-canary-rollback/plans/all-phases-harness-change-canary-rollback-part-1.md` (this file)
> - part 2: `docs/features/harness-change-canary-rollback/plans/all-phases-harness-change-canary-rollback-part-2.md`
> - part 3: `docs/features/harness-change-canary-rollback/plans/all-phases-harness-change-canary-rollback-part-3.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — harness-change-canary-rollback (Phase 1)  (v2)

**PHASES.md files:**
- `docs/features/harness-change-canary-rollback/PHASES.md` (harness-change-canary-rollback, 4 phases)

**SPEC.md files:**
- `docs/features/harness-change-canary-rollback/SPEC.md`

**Total phases:** 4 (this part covers Phase 1)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes

- **Quality gates (claude-config — NOT the contract's `/msbuild`/`/mstest`).** This part touches only `user/scripts/*.py`. The per-batch and completion gate is:
  - `python -m pytest user/scripts/ -q` (full script suite — includes the `test_no_orphaned_test_functions` dead-coverage guard, so every new `def test_*` in `test_lazy_core.py` MUST be registered in `_TESTS`), AND
  - `python user/scripts/lazy_parity_audit.py --report` (this part edits `lazy_core.py`, shared by the coupled state scripts — parity must stay green).
  - Run `python user/scripts/lazy-state.py --test` and `python user/scripts/bug-state.py --test` too when `lazy_core.py` changes (in-file smoke harnesses; both must stay green).
- **No MCP / no runtime.** There is no MCP server or Tauri runtime here — the Step-9 MCP gate is operator-exempt (`SKIP_MCP_TEST.md`, `spec_class: untestable-via-mcp`). Do NOT author a runtime-validation WU; the fixture suites ARE the validation.
- **`docs/gate/control-surfaces.json` does NOT exist yet.** WU-1 ships a canary-owned fallback glob constant; the manifest takes precedence when the file later appears (anti-overfit-design-gate ships it).
- **Do not hand-edit `_INTERVENTION_FIELD_ORDER`.** The record serializer appends unknown keys in insertion order, so a nested `canary:` map round-trips with no field-order change.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| intervention-efficacy-tracking | hard | `docs/features/intervention-efficacy-tracking/PHASES.md` | `record_intervention` capture host + record serializer/schema the `canary:` sub-map hangs off |
| code-doc-provenance-linkage | hard | `docs/features/code-doc-provenance-linkage/PHASES.md` | change→commit-set mapping → touched-file derivation for the manifest intersection |

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | harness-change-canary-rollback | P1 | Manifest resolution + touched-file intersection (WU-1) | — | Solo (Batch 1) |
| 2 | harness-change-canary-rollback | P1 | Pair-scope computation (WU-2) | — | Solo (Batch 2 — shares `lazy_core.py`) |
| 3 | harness-change-canary-rollback | P1 | `record_intervention` canary post-step (WU-3) | WU-1, WU-2 | Solo (Batch 3) |

> All three WUs modify `user/scripts/lazy_core.py` + `user/scripts/test_lazy_core.py`, so per the file-overlap rule they run in **separate sequential batches** (each a review gate). No cross-WU parallelism in this part.

## Work Units

- [x] WU-1 — Control-surface manifest resolution + touched-file derivation + intersection
- [x] WU-2 — Coupled-pair scope computation over the parity manifest
- [ ] WU-3 — `record_intervention` canary post-step (integrates WU-1 + WU-2)

---

### Phase: harness-change-canary-rollback P1 — Registration + revertibility metadata

**Goal:** At intervention capture, arm a `canary:` sub-map on the record when the shipped change's touched-file set intersects the control-surface manifest, carrying window config, surfaces, commit set, coupled-pair scope, and a degraded-revert note.
**Entry criteria:** none (first phase).
**SPEC.md references:** Design Decisions D1 (registration sub-map) + D5 (pair scope / degraded note); Technical Design "Registration"; Implementation Phases "Phase 1"; Validation Criteria rows "Canary arms on scope" + "Pair scope recorded".

#### WU-1 — Control-surface manifest resolution + touched-file derivation + intersection

- **Scope:** PHASES.md P1 deliverables: `_canary_control_surfaces(repo_root)` (manifest-when-present, else fallback glob constant) + touched-file derivation from the provenance change→commit-set mapping + the manifest-intersection arm decision.
- **TDD:** yes
- **Files to create/modify:** `user/scripts/lazy_core.py`
- **Test files:** `user/scripts/test_lazy_core.py`
- **Test expectations:**
  - `_canary_control_surfaces` returns the parsed globs from a fixture `docs/gate/control-surfaces.json` when present; returns the fallback constant (mirroring the anti-overfit initial set: `user/hooks/**`, `user/scripts/lazy-state.py`, `bug-state.py`, `lazy_core.py`, `lazy_guard.py`, `lazy_inject.py`, `lazy-parity-manifest.json`, `build-queue*.ps1`, `user/skills/lazy*/**`, `harden-harness/**`, gate-bearing components) when the file is absent.
  - A fixture touched-file set that intersects a glob → arm=True; a non-intersecting set (e.g. only `docs/foo.md`) → arm=False.
  - Touched-file derivation reads the provenance commit-set (fixture commit bracket) and yields repo-relative POSIX paths.
- **Implementation goal:** Add the resolver + a `_canary_touched_files(repo_root, commit_set)` derivation reusing the shipped provenance change→commit-set mapping (do NOT re-shell git ad hoc — reuse the provenance derivation path that `record_intervention` already has access to) [VERIFY: grep -n "def record_intervention" user/scripts/lazy_core.py], and a glob-intersection predicate. Writes go through `_atomic_write` where any file write is needed [VERIFY: grep -n "def _atomic_write" user/scripts/lazy_core.py].
- **Spec requirements:** SPEC Technical Design "Registration"; RESEARCH_SUMMARY "Phase 1 ships a canary-owned fallback surface-glob constant and documents that the manifest takes precedence when present."
- **Batch:** 1

#### WU-2 — Coupled-pair scope computation over the parity manifest

- **Scope:** PHASES.md P1 deliverable: `_compute_pair_scope(touched_files, manifest_path)` — a touched file matching either half of a manifest pair yields BOTH halves; fold in the root `CLAUDE.md` pairs-table entries not present in the manifest as data.
- **TDD:** yes
- **Files to create/modify:** `user/scripts/lazy_core.py`
- **Test files:** `user/scripts/test_lazy_core.py`
- **Test expectations:**
  - A touched file equal to a `pairs[].canonical` path returns `[canonical, derived]`; a touched file equal to a `.derived` path returns the same pair; both halves present, de-duplicated.
  - A touched file in NO pair returns an empty pair scope.
  - A CLAUDE.md-only pair (folded as data) is covered when a touched file matches one of its halves.
- **Implementation goal:** Read `user/scripts/lazy-parity-manifest.json` `pairs[].canonical`/`.derived` [VERIFY: python3 -c "import json;print(list(json.load(open('user/scripts/lazy-parity-manifest.json')).keys()))"] and return the union of both halves for any pair a touched file hits. Fold the CLAUDE.md pairs-table entries (the five lazy/lazy-cloud/lazy-bug pairs) in as a static data supplement for any pair absent from the manifest.
- **Spec requirements:** SPEC D5 "Coupled-pair hazard, handled at ship time"; Validation Criteria "Pair scope recorded".
- **Batch:** 2

#### WU-3 — `record_intervention` canary post-step (integrates WU-1 + WU-2)

- **Scope:** PHASES.md P1 deliverables: the canary post-step inside `record_intervention` setting `meta["canary"] = {opened, window_runs, surfaces, commit_set, pair_scope, degraded_revert_note, status: "open"}` on a scope hit, with per-record overrides from the hypothesis block and the degraded-revert-note plumbing; a non-scoped change registers NO canary.
- **TDD:** yes
- **Files to create/modify:** `user/scripts/lazy_core.py`
- **Test files:** `user/scripts/test_lazy_core.py`
- **Test expectations:**
  - A fixture capture of a control-surface change writes a record whose `canary.status == "open"` with `surfaces`, `commit_set`, and `pair_scope` (both halves) populated.
  - A fixture non-scoped change writes a record with NO `canary` key.
  - Per-record window override in the `## Intervention Hypothesis` block (e.g. `canary_window_runs: 5`) is honored over the default 10.
  - The degraded-revert note is set (non-null) for a fixture flagged revert-unsafe; null otherwise.
  - The written record round-trips through `parse_sentinel` type-preserved (the `canary:` map survives serialize→parse).
- **Implementation goal:** Add the post-step at the tail of `record_intervention` [VERIFY: grep -n "def record_intervention" user/scripts/lazy_core.py] BEFORE the existing `_atomic_write`, calling WU-1's intersection (arm decision + surfaces) and WU-2's pair scope; read overrides via the existing hypothesis precedence [VERIFY: grep -n "def parse_intervention_hypothesis" user/scripts/lazy_core.py]. Set the sub-map on `meta` so the unknown-key-append serializer emits it [VERIFY: grep -n "for key, value in meta.items()" user/scripts/lazy_core.py]. Defaults (10 runs / 30-day ceiling) live in ONE constants block. Capture parity across both completion handlers is inherited — no new parity divergence.
- **Spec requirements:** SPEC D1 Resolution; D2 defaults (10 runs / 30-day ceiling); Technical Design "Registration"; "House invariants honored".
- **Batch:** 3

**Batch overview:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | shares `lazy_core.py` with WU-2/WU-3 → sequenced |
| 2 | WU-2 | Solo | shares `lazy_core.py` |
| 3 | WU-3 | Solo | integrates WU-1 + WU-2 |

**Phase completion (gate-owned):** the `__mark_complete__` gate flips SPEC.md/PHASES.md `**Status:**` and writes `COMPLETED.md` once the whole feature's quality gates pass — never authored as a checkbox here. `/execute-plan` flips only THIS plan part's frontmatter `status:` and ticks the `- [ ] WU-N` rows.
