---
kind: implementation-plan
feature_id: feature-budget-guard-and-skip-ahead
status: Ready
complexity: mechanical
phases: [4]
---

> **Mobile plan** — generated by `/write-plan`. Execute with `/execute-plan <this-file>`.

> **Plan series:** part 3 of 3. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\features\feature-budget-guard-and-skip-ahead\plans\all-phases-feature-budget-guard-and-skip-ahead-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\features\feature-budget-guard-and-skip-ahead\plans\all-phases-feature-budget-guard-and-skip-ahead-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\features\feature-budget-guard-and-skip-ahead\plans\all-phases-feature-budget-guard-and-skip-ahead-part-3.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — Feature Budget Guard + Skip-Ahead (Phase 4)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\feature-budget-guard-and-skip-ahead\PHASES.md` (feature-budget-guard-and-skip-ahead, 4 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\feature-budget-guard-and-skip-ahead\SPEC.md`

**Total phases:** 4 (this part covers 4 — terminal)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. The wrapper SKILL.md files and CLAUDE.md edited in this part are documentation/markdown — those the orchestrator MAY edit directly per the rule "ONLY files you may modify directly: PHASES.md, CLAUDE.md". For the parity-manifest JSON (a config artifact, not source/test code), edit directly or dispatch — either is acceptable; it carries no test logic.

> **DISPATCH OVERRIDE NOTE (read if executing under `/lazy-batch`):** a no-Agent cycle subagent performs every step INLINE with `Read`/`Edit`/`Write`. This part is mostly markdown/JSON edits the orchestrator may do directly anyway.

> **COUPLED-PAIR DISCIPLINE (LOAD-BEARING — see `claude-config/CLAUDE.md` "Coupled Skill Pairs"):** `/lazy` ↔ `/lazy-cloud` and `/lazy-batch` ↔ `/lazy-batch-cloud` are coupled pairs sharing a state machine. After editing EITHER file in a pair, immediately diff its partner and confirm the only divergence is the intended one (the `--cloud` flag; the cloud wrapper's documented differences block). The state-machine logic itself lives in `lazy-state.py` (parts 1–2) — these wrapper edits are PROSE/glue only, NOT new logic.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path. **Before executing each step**, `Read` the component files listed for that step from disk. After compaction, re-read this plan file first.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — no completed hard upstream dependencies) | — | — | — |

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. Wrapper SKILL.md + CLAUDE.md edits are markdown — orchestrator may edit directly. Parity manifest JSON is config — direct edit or dispatch.
2. No worktrees.
3. After editing either file of a coupled pair, DIFF its partner and confirm only the intended divergence.
4. PHASES.md updated after the batch.
5. Output reviewed before continuing.
6. Mistakes fixed immediately.
7. Final full-gate run (smoke suites + parity audit + projection + lint) after the batch.
8. CLAUDE.md (`user/scripts/CLAUDE.md`) updated with the new CLI flags + behavior.
9. Committed + pushed.
10. Self-contained.
11. **Before each step, `Read` the listed component files from disk.**

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | feature-budget-guard-and-skip-ahead | P4 | Wrapper lockstep + parity audit | parts 1–2 (state machine), part 2 (P3) | Solo |

## Work Units

- [ ] WU-1 — Batch-wrapper coupled-pair lockstep + parity manifest mechanic set
- [ ] WU-2 — Single-step wrapper lockstep + `user/scripts/CLAUDE.md` docs + final full-gate run

---

### Phase: feature-budget-guard-and-skip-ahead P4 — Wrapper lockstep + parity audit

**Goal:** Mirror the new flags / terminals / probe fields / notification glue into the four thin wrappers in lockstep, register the new mechanic set in the parity manifest, and confirm `lazy_parity_audit.py` + all smoke suites + projection/lint are green.
**Entry criteria:** Parts 1–2 (state-machine flags, `budget_guard` probe field, terminals) and part 2 (P3 skip-ahead) complete.
**SPEC.md references:** Technical Design → Where it lives ("thin orchestrator wiring", coupled pairs updated in lockstep); PHASES Phase 4; Validation Criteria row "Parity preserved".

#### WU-1 — Batch-wrapper coupled-pair lockstep + parity manifest mechanic set
- **Scope:** PHASES P4 deliverables "`/lazy-batch` flags + budget_guard PushNotification glue + skip-ahead default note + terminal table" + "`/lazy-batch-cloud` mirror" + "`lazy-parity-manifest.json` mechanic set".
- **TDD:** no — wrapper prose + parity manifest config; the gate is `lazy_parity_audit.py` exit 0, not a new unit test.
- **Files to create/modify:**
  - `user/skills/lazy-batch/SKILL.md` [VERIFY: ls user/skills/lazy-batch/SKILL.md]
  - `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` [VERIFY: ls repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md]
  - `user/scripts/lazy-parity-manifest.json` [VERIFY: ls user/scripts/lazy-parity-manifest.json]
- **Implementation goal:**
  - `/lazy-batch`: add `--per-feature-cycle-cap <N>` and `--strict-research-halt` to its flag/CLI surface; add the `budget_guard` trip → PushNotification glue (the notification text reports the COMPUTED ceiling + `next_id`, per SPEC: `feature-budget-guard tripped — <feature-id> deferred to queue tail after <N> cycles (computed ceiling <L_task>); advancing to <next-id>`); add the skip-ahead default-on note + the gated-head end-of-run flush; update the State Machine Summary / terminal table with the new budget-exhaustion + all-gated terminals.
  - `/lazy-batch-cloud`: mirror the above. Update its "Differences from /lazy-batch" block ONLY where a genuine divergence exists — none is expected (the budget guard + skip-ahead are environment-agnostic; both wrappers pass the same flags). If no divergence, state that explicitly so the audit confirms parity.
  - `lazy-parity-manifest.json`: register the new flags (`--per-feature-cycle-cap`, `--strict-research-halt`) as a mechanic set so `lazy_parity_audit.py` [VERIFY: grep -n "mechanic_sets" user/scripts/lazy_parity_audit.py] asserts BOTH state scripts carry the parse surface (and the mirrored `compute_state()` param threading).
- **Spec requirements:** Technical Design "the coupled `/lazy` ↔ `/lazy-cloud` and `/lazy-batch` ↔ `/lazy-batch-cloud` pairs are updated in lockstep"; PHASES Phase 4.
- **Batch:** 1.

> **After editing `/lazy-batch` and `/lazy-batch-cloud`, diff them against each other** and confirm the only divergence is the documented cloud-specific block (per `claude-config/CLAUDE.md` Coupled Skill Pairs). A drift here silently breaks the pair's invariants.

#### WU-2 — Single-step wrapper lockstep + CLAUDE.md docs + final full-gate run
- **Scope:** PHASES P4 deliverables "`/lazy` ↔ `/lazy-cloud` mirror" + "`user/scripts/CLAUDE.md` docs" + "Tests: parity + smoke + projection + lint green".
- **TDD:** no — wrapper prose + docs; the gate is the full smoke/parity/projection/lint suite green.
- **Files to create/modify:**
  - `user/skills/lazy/SKILL.md` [VERIFY: ls user/skills/lazy/SKILL.md]
  - `repos/algobooth/.claude/skills/lazy-cloud/SKILL.md` [VERIFY: ls repos/algobooth/.claude/skills/lazy-cloud/SKILL.md]
  - `user/scripts/CLAUDE.md` [VERIFY: ls user/scripts/CLAUDE.md]
- **Implementation goal:**
  - `/lazy` ↔ `/lazy-cloud`: mirror any wrapper prose surfacing the new flags / terminals (single-step variants — they pass the flags through but do not loop). Diff the pair after editing.
  - `user/scripts/CLAUDE.md`: document `--per-feature-cycle-cap` and `--strict-research-halt` in the CLI-surface block, plus the per-feature budget guard (`per_feature_forward_cycles` marker map, dynamic ceiling, defer-to-tail + bounded re-trip→eviction) and dependency-aware skip-ahead (two-key predicate, default-on, `--strict-research-halt` opt-out), matching the existing `--park-*` / `--skip-needs-research` doc style.
  - Run the FULL gate set: `python3 user/scripts/lazy-state.py --test`, `python3 user/scripts/bug-state.py --test`, `python3 user/scripts/lazy_coord.py --test`, `python3 user/scripts/test_lazy_core.py`, `python3 user/scripts/lazy_parity_audit.py`, then `python3 user/scripts/project-skills.py` and `python3 user/scripts/lint-skills.py --check-projected --check-capabilities`. All green; baselines match.
- **Spec requirements:** Validation Criteria row "Parity preserved" (`lazy-state.py --test`, `bug-state.py --test`, `lazy_parity_audit.py` all green; baselines match).
- **Batch:** 1.

> **After editing `/lazy` and `/lazy-cloud`, diff them against each other** and confirm only the intended `--cloud` divergence differs.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2 | Solo (coupled-pair edits + manifest + docs) — sequential | yes (manifest + wrappers) |

---

## Execution Protocol

### Phase Selection Loop
Repeat until P4 is complete or a blocking issue triggers early exit:
1. **Select ready phase:** P4 (entry criteria satisfied by parts 1–2).
2. **Announce:** "Implementing feature-budget-guard-and-skip-ahead Phase 4: Wrapper lockstep + parity audit".
3. **Review prior context:** re-read prior phases' Implementation Notes — they take priority where they diverge.
4. **Execute the batch** per the Per-Batch Steps below.
5. **Run Post-Phase Steps.**
6. **Report:** "feature-budget-guard-and-skip-ahead Phase 4 — committed as [hash]".

### Step 0: Initialize Task Tracking (MANDATORY FIRST)
Read `~/.claude/skills/_components/task-tracking.md` and follow it exactly.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read PHASES.md (all prior Implementation Notes — they record the exact flag names / probe-field shapes parts 1–2 landed), SPEC.md, this plan file, and BOTH coupled-pair partners before editing either.

#### Step B.1: Make the Edits
Wrapper SKILL.md + CLAUDE.md are markdown — edit directly (no Agent needed). Parity manifest is config — direct edit or dispatch. Apply the coupled-pair diff discipline after each pair edit.

#### Step B.2: Review Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md`. Verify: both batch wrappers carry the new flags + glue; the coupled-pair diff shows only the intended divergence; the parity manifest mechanic set names both new flags.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `~/.claude/skills/_components/phases-update.md`. Check off P4 deliverables; add an Implementation Notes block. **When this LAST part's work lands, set the top-level PHASES `**Status:**` to `In-progress`** (implementation done, validation pending) — NOT Complete.

#### Step B.4: Run Quality Gates (MANDATORY — FULL SUITE)
Read `~/.claude/skills/_components/quality-gates.md`. Run ALL of: `lazy-state.py --test`, `bug-state.py --test`, `lazy_coord.py --test`, `test_lazy_core.py`, `lazy_parity_audit.py`, `project-skills.py`, `lint-skills.py --check-projected --check-capabilities`. 100% green required.

#### Step B.4.5: MCP Integration Test
SKIP with a note — no MCP-reachable surface.

#### Step B.5: Commit Batch
Read `.claude/skill-config/commit-policy.md` (fallback `commit-and-push.md`). Commit; push to `main`.

#### Step B.6: Proceed
Checklist: review done; PHASES.md updated (`**Status:** In-progress`); full gate suite green; committed.

### Post-Phase Steps

#### Update CLAUDE.md Files (MANDATORY)
Read `~/.claude/skills/_components/claude-md-review.md`. Confirm `user/scripts/CLAUDE.md` documents the new flags + behavior (done in WU-2). Consider whether `claude-config/CLAUDE.md`'s coupled-pair table or Hooks/Scripts tables need a note (only if the wrapper edits changed a documented contract).

#### Commit and Push Post-Phase Changes
Per the commit policy above.

---

## Blocking Issue Protocol

If a blocking issue is encountered:
1. Stop all in-progress work.
2. Commit and push completed work.
3. Print a blocking-issue report (completed work, blocked item, reason, recovery, remaining).
4. Do not work around the blocker.

Blocking issues: a parity-audit failure unfixable after 2 retries; a coupled-pair drift that cannot be reconciled without a state-machine change (that belongs in parts 1–2, not here — surface it); a projection/lint failure unfixable after 2 retries; a git push conflict unresolvable by rebase.

---

## Completion

When P4 is complete (the terminal phase):
1. Run the full gate suite one final time (smoke + parity + projection + lint) — all green.
2. Confirm the top-level PHASES `**Status:**` is `In-progress` (NOT Complete).
3. Print a completion report (features, all 4 phases completed, commits, gate status, commit log table, Implementation Notes summary).

> **Do NOT flip the top-level PHASES/SPEC `**Status:**` to Complete or write COMPLETED.md** — those are owned EXCLUSIVELY by the `__mark_complete__` gate, which fires only after the validation tail (Step-9 `/mcp-test` → coverage audit). This feature has no MCP surface, so the Step-9 gate is expected to grant a structural MCP-skip (`__grant_skip_no_mcp_surface__`); let the state machine route there. A rogue flip leaves no COMPLETED.md receipt and HARD-HALTS the state script on `completion-unverified`.
