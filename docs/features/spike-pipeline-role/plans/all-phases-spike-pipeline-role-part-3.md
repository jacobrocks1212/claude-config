---
kind: implementation-plan
feature_id: spike-pipeline-role
status: Complete
created: 2026-07-18
complexity: complex
phases: [4, 5]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\plans\all-phases-spike-pipeline-role-part-3.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 3 of 3. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\plans\all-phases-spike-pipeline-role-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\plans\all-phases-spike-pipeline-role-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\plans\all-phases-spike-pipeline-role-part-3.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — spike-pipeline-role  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\PHASES.md` (spike-pipeline-role, Phases 4 + 5 of 6)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\SPEC.md`

**Total phases:** 2 (Phases 4 + 5) in this part; 4 across the 3-part series.
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the **Plan-specific execution notes** below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

### Plan-specific execution notes (claude-config — override contract defaults)

- **Quality gates = the repo invariant battery** via `/gate-battery` (or `python3 user/scripts/gate-battery.py --repo-root .`). Trust its LAST-line banner. Battery: `pytest user/scripts/`, `lazy-state.py --test`, `bug-state.py --test`, `lazy_parity_audit.py --repo-root .`, `cli_surface_gen.py --check`, `doc-drift-lint.py --repo-root .`, `lint-skills.py`.
- **Coupled-pair discipline (HARD).** WU-2 adds the tooling-round cap. Put the SHARED bound helper in `user/scripts/lazy_core/` (like `validation_escalation` lives in `gates.py`) so BOTH `lazy-state.py` and `bug-state.py` consume it with no per-script divergence — this minimizes the coupled mirror. If any `bug-state.py`-side wiring is needed, mirror it in the same WU and keep `lazy_parity_audit.py --repo-root .` at exit 0 (battery gate). The `--test` self-tests are the body-parity guard (parity audit is surface-token only).
- **After editing skills** (`add-phase/SKILL.md`, `spike-dispatch.md`): run `python ~/.claude/scripts/project-skills.py` + `python ~/.claude/scripts/lint-skills.py` before the battery.
- **Directly-committed repo:** commit + push after EACH phase's battery is green (two commits — one for Phase 4, one for Phase 5). Conventional Commits, NO AI attribution. Regenerate `LAZY_QUEUE.md` (`python user/scripts/lazy-queue-doc.py --repo-root .`) before each commit.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this plan has no completed hard upstream dependencies) | — | — | `**Depends on:** (none)` per SPEC.md |

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | spike-pipeline-role | P4 | Tooling-existence loop + bound | Parts 1–2 (routing + orchestrator) | WU-1 ‖ WU-2 (no file overlap) |
| 2 | spike-pipeline-role | P5 | Verdict machinery reuse (claude-config-side contract) | Phase 4 | WU-3 (Solo) |

## Work Units

- [x] WU-1 — Tooling-existence assertion → `/add-phase` corrective route (add-phase `**Spike:**` authoring)
- [x] WU-2 — `spike_tooling_rounds` counter + hard cap (default 3) → `NEEDS_INPUT.md`
- [x] WU-3 — Phase 5: claude-config-side verdict/engine-reuse contract

---

### Phase: spike-pipeline-role P4 — Tooling-existence loop + bound

**Goal:** Make the Spike cycle assert the tooling it needs exists BEFORE running the proof, route a gap to the `/add-phase` corrective path (so the tooling gets built and control RETURNS to Spike), and BOUND the loop so it can never spin forever.
**Entry criteria:** Parts 1–2 landed (spike routing + orchestrator Spike Step). The tooling loop is the pre-proof gate INSIDE that Spike cycle.
**SPEC.md references:** "The tooling-existence loop (bounded)"; requirement 2 (tooling-existence loop); Integration surface (the `/add-phase` corrective route); the `spike_tooling_rounds` loop guard (default cap 3).

**Plan-specific note (drift correction):** the Spike subagent's tooling-loop BEHAVIOR is already specified in the Phase-1 components (`spike-dispatch.md` bounded-loop block; `dispatch-spike.md` tooling `@section`). What Phase 4 BUILDS is (a) the `/add-phase` authoring support so a corrective phase can carry a `**Spike:**` return line [VERIFY: the corrective-phase concept exists — `grep -n "corrective" user/skills/add-phase/SKILL.md`], and (b) the machine-enforced round counter + cap. Do not re-author the subagent contract.

#### WU-1 — Tooling-existence assertion → `/add-phase` corrective route (add-phase `**Spike:**` authoring)

- **Scope (PHASES deliverable 1):** "Spike asserts required tooling exists before the proof (reuse the `mcp-tool-catalog.md` tool-existence audit); on a gap, route to `/add-phase` corrective (tagged `corrective`, carrying a `**Spike:**` line so control returns)."
- **TDD:** no for the skill-prose portion (validated by `lint-skills.py` + `project-skills.py`); yes for any parse/authoring helper if code is added (see below).
- **Files to create/modify:** `user/skills/add-phase/SKILL.md` — teach `/add-phase` to author a corrective phase that carries a `**Spike:** required — <goal>` return line so completing the tooling phase re-routes to Spike (Part 1's `phases_spike_required` gate picks it up). Reference the existing tool-existence audit component `user/skills/_components/phases-runtime-validation.md` [VERIFY: `test -f user/skills/_components/phases-runtime-validation.md`] and the per-repo registry `repos/algobooth/.claude/skill-config/mcp-tool-catalog.md` [VERIFY: `test -f repos/algobooth/.claude/skill-config/mcp-tool-catalog.md`] as the tooling-existence source the Spike cycle greps — do NOT duplicate the audit logic; the Spike subagent contract already invokes it.
- **Test files:** if this WU adds a parse/authoring helper to `lazy_core` (e.g. a helper that stamps the `**Spike:**` return line onto an add-phase-authored corrective phase), add `user/scripts/test_spike_tooling_loop.py` covering it. If the change is purely SKILL.md prose, no unit test — note that in the WU completion.
- **Implementation goal:** Close the exact loop the operator hand-ran for hydra-overlay (missing `hydra_set_enabled`/`hydra_projector_spike_open` → add corrective phase → build → return to spike). On a tooling gap the Spike subagent writes `tooling_ok: false` + names the missing tooling, then routes to `/add-phase`; `/add-phase` authors a `**Phase kind:** corrective` phase carrying the `**Spike:**` return line. When that corrective phase completes, Part 1's Step-9.5 header gate re-routes to a spike cycle to run the proof.
- **Spec requirements:** SPEC "The tooling-existence loop (bounded)" steps 1–3; requirement 2 (the corrective-path loop); ESPECIALLY the AlgoBooth-scoped prescription intent (requirement 6) — but the `/add-phase` authoring capability itself is a generic claude-config skill change (the AlgoBooth-scoped ADDENDA are Phase-1 work, not this WU).
- **Batch:** 1 (no file overlap with WU-2).

#### WU-2 — `spike_tooling_rounds` counter + hard cap (default 3) → `NEEDS_INPUT.md`

- **Scope (PHASES deliverable 2):** "`spike_tooling_rounds` counter on the entry sentinel/feature state; hard cap (default 3) → `NEEDS_INPUT.md` instead of another loop. Loop can NEVER spin forever." Plus PHASES deliverable 3's TDD ("the loop fires, returns to Spike, and the cap halts").
- **TDD:** yes
- **Files to create/modify:** `user/scripts/lazy_core/` — a shared bound helper (mirror `validation_escalation`'s shape [VERIFY: `grep -n "def validation_escalation" user/scripts/lazy_core/gates.py`]; the retry-count-on-sentinel-frontmatter pattern is the model). Store `spike_tooling_rounds` on the entry sentinel's frontmatter (the `BLOCKED.md`/`SPIKE_VERDICT.md` the loop reads), incremented each time the tooling loop fires for the same spike. Wire the cap check into the spike routing added in Part 1 (`lazy-state.py` Step 9.5 / the blocked-resolver route) — and the shared helper means `bug-state.py` gets it for free. Possibly `user/scripts/lazy-state.py` + `user/scripts/bug-state.py` (consume the shared helper).
- **Test files:** `user/scripts/test_spike_tooling_loop.py` (new); the `lazy-state.py --test` / `bug-state.py --test` self-test scenarios.
- **Test expectations:**
  - `spike_tooling_rounds` increments across successive tooling-loop fires for the same spike.
  - At `spike_tooling_rounds < 3`, a tooling gap routes to the `/add-phase` corrective (loop continues).
  - At `spike_tooling_rounds >= 3` (the default cap), the machine writes/emits `NEEDS_INPUT.md` ("tooling gap persists after N corrective rounds — operator decision needed") INSTEAD of another loop — the loop provably halts.
  - The cap value is a single named constant (default 3, mirroring the validation retry discipline) so the operator can tune it (SPEC Open Question 2).
- **Implementation goal:** A machine-enforced loop guard that can NEVER spin forever, mirroring the validation-retry escalation discipline. The `NEEDS_INPUT.md` written here carries `written_by: spike` so the Part-1 provisional carve-out already refuses to auto-accept it under `--park --park-provisional` [VERIFY: `grep -n "written_by.*spike" user/scripts/lazy_core/docmodel.py`].
- **Spec requirements:** SPEC "Loop guard (bound)"; Open Question 2 (cap value tunable, defaulted 3).
- **Batch:** 1 (no file overlap with WU-1 — WU-1 touches `add-phase/SKILL.md`, WU-2 touches `lazy_core`/state scripts; they may run in parallel).

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-1, WU-2 | Yes      | None (add-phase/SKILL.md vs lazy_core/state scripts) |

---

### Phase: spike-pipeline-role P5 — Verdict machinery reuse (claude-config-side contract)

**Goal:** Document the claude-config-side CONTRACT for how a Spike reuses the mcp-test verdict/engine shape when it has a deterministic scenario, and states that for `/investigate`- or manual-measurement-form spikes the results doc + `INVESTIGATION.md` ledger IS the evidence (no engine). Any actual engine code lives in the AlgoBooth repo and is OUT OF SCOPE here.
**Entry criteria:** Phase 4 complete (the tooling loop that a deterministic-scenario spike would run behind).
**SPEC.md references:** "Reuse map (from the mcp-test machinery)"; Non-goals ("A bespoke deterministic spike engine … is optional and phased"); PHASES Phase 5 (scopes ONLY the claude-config-side contract).

#### WU-3 — claude-config-side verdict/engine-reuse contract

- **Scope (PHASES deliverables 1 + 2 — a single documentation deliverable):** "Where a spike has a deterministic scenario, reuse the mcp-test `run.ts`/`verdict.ts` shape (compact verdict + sibling raw-payload artifact). For `/investigate`- or manual-measurement-form spikes, the results doc + INVESTIGATION.md ledger is the evidence — no engine." Plus the scoping note: "any engine code that lives in the AlgoBooth repo is an AlgoBooth-repo deliverable (outside harden-harness scope)."
- **TDD:** no (documentation of an already-designed contract; validated by `lint-skills.py` + `project-skills.py` + doc-drift battery).
- **Files to create/modify:** extend `user/skills/_components/spike-dispatch.md` [VERIFY: `test -f user/skills/_components/spike-dispatch.md`] with a "Verdict machinery reuse" section (OR a sibling `_components/` contract doc if `spike-dispatch.md` is size-ratcheted — check `skill-size-baseline.json` before growing it). State: (a) when a spike has a deterministic scenario → reuse the mcp-test compact-verdict + sibling-raw-payload shape (the AlgoBooth-side `scripts/mcp-test/run.ts`/`verdict.ts` engine is REFERENCED, not vendored — those `.ts` files do NOT exist in claude-config and must NOT be created here); (b) for `/investigate`/manual-measurement spikes, the `SPIKE_VERDICT.md` results doc + `INVESTIGATION.md` ledger is the evidence, no engine; (c) the engine-in-AlgoBooth boundary is an AlgoBooth-repo deliverable, surfaced not built.
- **Implementation goal:** A crisp claude-config-side contract so a future spike author knows exactly which evidence shape applies and where the boundary to AlgoBooth engine code sits. This is the seam that keeps the whole role inside claude-config (harden-harness Prohibition #1) while leaving the optional engine to a normal AlgoBooth session.
- **Spec requirements:** SPEC Reuse map; Non-goals (engine optional/phased); Architecture (honesty invariant — evidence never fabricated, no static-trace substitute).
- **Batch:** 1 (Solo — Phase 5 is a distinct phase; runs after Phase 4's commit).

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-3      | Solo      | `spike-dispatch.md` (no overlap with Phase 4 WUs) |

**Plan-specific execution note:** Phase 4's WU-2 (a new machine-enforced loop-guard seam in the state machine) is `complex`; WU-1 modifies an authoring skill's behavior (design). WU-3 (Phase 5) is a documentation/contract edit (mechanical in isolation), but a single complex WU sets the part's tier and Phase 4 + Phase 5 are packed into one part (3 WUs, ≤ 8 cap, dep edges respected). This part is tagged `complexity: complex`.
