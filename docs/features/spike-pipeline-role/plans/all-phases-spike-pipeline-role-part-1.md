---
kind: implementation-plan
feature_id: spike-pipeline-role
status: Complete
created: 2026-07-18
complexity: complex
phases: [2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\plans\all-phases-spike-pipeline-role-part-1.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 3. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\plans\all-phases-spike-pipeline-role-part-1.md` (this file)
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\plans\all-phases-spike-pipeline-role-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\plans\all-phases-spike-pipeline-role-part-3.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — spike-pipeline-role  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\PHASES.md` (spike-pipeline-role, Phase 2 of 6)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\SPEC.md`

**Total phases:** 1 (Phase 2) in this part; 4 across the 3-part series (Phases 2, 3, 4, 5).
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the **Plan-specific execution notes** below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

### Plan-specific execution notes (claude-config — override contract defaults)

- **Quality gates = the repo invariant battery.** This repo has NO `/msbuild`/`/mstest`. The QG for every batch and every Post-Phase step is the 7-command invariant battery, run via `/gate-battery` (or directly `python3 user/scripts/gate-battery.py --repo-root .`). Trust its LAST stdout line banner `gate-battery: … RESULT=<PASS|FAIL> …`; do NOT grep individual gate output. The battery runs: `pytest user/scripts/`, `lazy-state.py --test`, `bug-state.py --test`, `lazy_parity_audit.py --repo-root .`, `cli_surface_gen.py --check`, `doc-drift-lint.py --repo-root .`, `lint-skills.py`. A phase is not done until the banner reads `RESULT=PASS`.
- **Coupled-pair discipline (HARD).** `lazy-state.py` ↔ `bug-state.py` are a coupled pair (shared state machine). Any `compute_state` routing change to one is mirrored into the other in the SAME phase — WU-3 owns that mirror. After WU-3, `lazy_parity_audit.py --repo-root .` MUST exit 0 (it is a battery gate). NOTE: the parity audit checks **surface-token presence** via regex, NOT `compute_state` body equivalence — the `--test` self-tests (which drive both scripts) are the real body-parity guard, so both WUs' tests must assert identical routing on each state script.
- **One-writer-per-file / directly-committed repo.** Commit + push after this phase completes and its battery is green (`git push` after every commit — claude-config keeps `origin/main` in sync; see `.claude/skill-config/commit-policy.md`). Conventional Commits, NO AI attribution footer.
- **Regenerate `LAZY_QUEUE.md`** before the phase commit: `python user/scripts/lazy-queue-doc.py --repo-root .` (byte-stable pure read; rides the commit).

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this plan has no completed hard upstream dependencies) | — | — | `**Depends on:** (none)` per SPEC.md |

Phase-1 foundational artifacts this phase CONSUMES (already landed, in-repo, verified present — not upstream deps):
- `phases_spike_required` / `_read_spike_decision` exported from `user/scripts/lazy_core/__init__.py` [VERIFY: `grep -n "phases_spike_required" user/scripts/lazy_core/__init__.py`]
- `spike` dispatch class in `user/scripts/lazy_core/dispatch.py` [VERIFY: `grep -n '"spike"' user/scripts/lazy_core/dispatch.py`]
- Spike-FAIL provisional carve-out in `user/scripts/lazy_core/docmodel.py` [VERIFY: `grep -n "spike_verdict" user/scripts/lazy_core/docmodel.py`]

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | spike-pipeline-role | P2 | State-machine routing (`compute_state` Step 9.5) | — | Solo (WU-1→WU-2→WU-3 sequential; see batch table) |

## Work Units

- [x] WU-1 — compute_state Step 9.5: prescribed `**Spike:**` header gate (lazy-state.py)
- [x] WU-2 — blocker_kind `runtime-spike-verdict-pending` routing + escalation predicate (lazy-state.py + gates.py)
- [x] WU-3 — coupled mirror into bug-state.py + parity audit

---

### Phase: spike-pipeline-role P2 — State-machine routing (`compute_state` Step 9.5)

**Goal:** Wire the (currently inert) spike parse primitives into live `compute_state` routing for BOTH entry signals — the prescribed `**Spike:** required` phase header and the ad-hoc `blocker_kind: runtime-spike-verdict-pending` blocked resolver — and mirror it into the coupled `bug-state.py`.
**Entry criteria:** Phase 1 complete (dispatch class + parse primitives + provisional carve-out landed; verified present above).
**SPEC.md references:** "The routing signals" (#1 prescribed header, #2 ad-hoc blocked); Integration surface rows "Header parse", "State-machine routing", "Blocker escalation"; Architecture "The tooling-existence loop" entry sentinel.

**Plan-specific note (anchor-grade drift correction from the touchpoint audit):** the SPEC phrase "a `runtime-spike-verdict-pending` escalation branch **beside** `validation_escalation`" describes intent, not literal structure. Ground truth: `validation_escalation` (`gates.py:1729`) is a **boolean predicate** consumed inside the Step-3 BLOCKED terminal block of `compute_state` (~lazy-state.py:3041–3066) — there is NO existing `if/elif` ladder over `blocker_kind` at that site. So WU-2 adds (a) a sibling predicate in `gates.py` and (b) a spike-route conditional in the Step-3 blocked block that fires BEFORE the generic manual-block terminal. This is mechanical grounding, not a design change.

#### WU-1 — compute_state Step 9.5: prescribed `**Spike:**` header gate

- **Scope (PHASES deliverable):** "`compute_state` Step 9.5 (`lazy-state.py`, between the Step 9 MCP-gate returns and the Step 10 `entry_ok` check): emit `sub_skill="spike"` when a phase declares `**Spike:** required` and the spike verdict is not yet PASS; gate Step 10 `entry_ok` on the spike verdict doc." Also covers the "TDD: prescribed-header entry signal" slice of PHASES deliverable 4.
- **TDD:** yes
- **Files to create/modify:** `user/scripts/lazy-state.py` (insert Step 9.5 in `compute_state` [VERIFY: `grep -n "def compute_state" user/scripts/lazy-state.py`] at the seam after the Step-9 workstation MCP-gate returns and before the Step-10 `entry_ok` check ~line 3805).
- **Test files:** `user/scripts/test_spike_state_routing.py` (new) — and register the same scenarios into the `lazy-state.py --test` self-test harness if that is where compute_state routing is exercised (grep the existing `--test` harness first and follow its fixture convention).
- **Test expectations:**
  - A fixture feature whose active PHASES.md phase carries `**Spike:** required — <goal>` AND whose spike verdict doc is absent/`PENDING`/`FAIL` → `compute_state` returns `sub_skill == "spike"` (routed BEFORE `entry_ok`/mark-complete).
  - Same fixture with a `verdict: PASS` spike results doc present → routing falls through to the existing Step-10 `entry_ok` path (spike no longer gates).
  - A phase with NO `**Spike:**` line → routing is byte-identical to today (regression guard: the primitive must be truly opt-in).
- **Implementation goal:** Mirror the existing `phases_mcp_runtime_not_required` consumption pattern [VERIFY: `grep -n "phases_mcp_runtime_not_required" user/scripts/lazy-state.py`] (consumed at lazy-state.py:3789). Call `phases_spike_required(spec_path)` [VERIFY: `grep -n "phases_spike_required" user/scripts/lazy_core/__init__.py`] and, for the goal string, `_read_spike_decision(spec_path)` [VERIFY: `grep -n "_read_spike_decision" user/scripts/lazy_core/__init__.py`]. Build the routed return through the same `_state(...)`/`common` builder every other `sub_skill` emission uses (mcp-test's `sub_skill="mcp-test"` emission is the nearest model). The verdict-doc read: PASS iff the feature's `SPIKE_VERDICT.md` (or the doc named by the `**Spike:**` line) carries `verdict: PASS` in its frontmatter (per SPEC "The structured report / verdict schema"). No fabrication path — absent/unreadable doc ⇒ not PASS ⇒ route to spike.
- **Spec requirements:** SPEC "The routing signals" #1; "PASS/FAIL orchestrator-loop branching" (PASS ticks the gated deliverable via gate-boundary-safe reconcile — this WU only ROUTES; the tick is the orchestrator's Spike Step in Part 2). Do NOT flip top-level `**Status:**` or write a receipt here — those are `__mark_complete__`-gate-owned.
- **Batch:** 1

#### WU-2 — blocker_kind `runtime-spike-verdict-pending` routing + escalation predicate

- **Scope (PHASES deliverable):** "`blocker_kind: runtime-spike-verdict-pending` routes to a `spike` cycle (not the generic manual-block terminal); a `runtime-spike-verdict-pending` escalation branch beside `validation_escalation` (`gates.py` / `lazy-state.py:~3055`)." Also covers the "TDD: blocked-resolver entry signal" slice of PHASES deliverable 4.
- **TDD:** yes
- **Files to create/modify:** `user/scripts/lazy_core/gates.py` (new `spike_escalation`-style predicate beside `validation_escalation` [VERIFY: `grep -n "def validation_escalation" user/scripts/lazy_core/gates.py`]); `user/scripts/lazy-state.py` (Step-3 BLOCKED block ~3041–3066 — spike-route conditional BEFORE the generic manual-block terminal).
- **Test files:** extend `user/scripts/test_spike_state_routing.py`; add the `--test` harness scenarios if that is the parity-driving surface.
- **Test expectations:**
  - A `BLOCKED.md` carrying `blocker_kind: runtime-spike-verdict-pending` → `compute_state` returns `sub_skill == "spike"` (NOT the generic manual-block terminal).
  - The new predicate behaves like `validation_escalation` (True at/after the retry threshold) — the escalation path is exercised only when the spike blocker recurs past threshold (feeds Part 3's tooling-round cap, but the predicate itself is testable here).
  - Every OTHER `blocker_kind` (`unknown-host-capability`, `unknown-dependency`, `plan-structural-invalid`, `mcp-validation`, `mcp-runtime-unready`) routes exactly as before (regression guard).
- **Implementation goal:** Add `runtime-spike-verdict-pending` as a NEW routed (not terminal) `blocker_kind`. Mirror `validation_escalation`'s shape for the escalation predicate. The routing conditional reads the blocked sentinel's `blocker_kind` (the state machine already parses BLOCKED.md frontmatter at this site) and emits `sub_skill="spike"` via the same `_state(...)` builder WU-1 uses.
- **Spec requirements:** SPEC "The routing signals" #2; Integration surface "Blocker escalation" row; "Architecture — The routing signals" (this is the exact `blocker_kind` hydra-overlay dead-ended on).
- **Batch:** 2 (FILE OVERLAP with WU-1 on `lazy-state.py` → must follow WU-1's batch; WU-1 establishes the spike-route return shape WU-2 reuses).

#### WU-3 — coupled mirror into bug-state.py + parity audit

- **Scope (PHASES deliverable):** "**Coupled mirror into `bug-state.py`** (the `lazy-state.py` ↔ `bug-state.py` pair; run `lazy_parity_audit.py`)." Completes PHASES deliverable 4 (the mirror's own tests).
- **TDD:** yes
- **Files to create/modify:** `user/scripts/bug-state.py` (mirror WU-1's Step-9.5-equivalent gate — bug-state's Step-10 `__mark_fixed__` is at :1942 [VERIFY: `grep -n "__mark_fixed__" user/scripts/bug-state.py`], so the spike gate goes BEFORE it — and WU-2's blocker routing); `user/scripts/lazy-parity-manifest.json` ONLY IF the parity audit's surface set needs a new spike surface-token row (check `lazy_parity_audit.py` output first — add the token row only if the audit reports the new routing as an unmirrored surface).
- **Test files:** extend `user/scripts/test_spike_state_routing.py` to drive bug-state.py fixtures; the `bug-state.py --test` self-test harness scenarios (battery gate).
- **Test expectations:**
  - `bug-state.py` `compute_state` [VERIFY: `grep -n "def compute_state" user/scripts/bug-state.py`] routes `sub_skill="spike"` on BOTH signals identically to `lazy-state.py` (bug axis uses `FIXED.md`/`__mark_fixed__` in place of `COMPLETED.md`/`__mark_complete__` — mirror the routing, not the receipt name).
  - `python3 user/scripts/lazy_parity_audit.py --repo-root .` exits 0.
- **Implementation goal:** Faithful axis-mirror of WU-1+WU-2. Because the parity audit is surface-token based (NOT body-equivalence), the mirror correctness is proven by the `--test` scenarios driving both scripts to the same routing — assert that explicitly. Do NOT diverge the routing logic between the two scripts; the only intended divergence on the bug axis is the receipt/terminal pseudo-skill name.
- **Spec requirements:** SPEC Integration surface "State-machine routing" row ("**coupled mirror into `bug-state.py`**"); the coupled-pair rule in `claude-config/CLAUDE.md`.
- **Batch:** 3 (depends on WU-1 + WU-2 being settled — it mirrors their final shape).

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-1      | Solo      | N/A (establishes lazy-state.py spike-route shape) |
> | 2     | WU-2      | Solo      | Overlaps WU-1 on `lazy-state.py` → sequential |
> | 3     | WU-3      | Solo      | `bug-state.py` (no overlap) but depends on WU-1/WU-2 design → sequential |

**Plan-specific execution note:** all three WUs are `complex` (live state-machine surgery — changes what `compute_state` returns for real features; judgment-bearing routing + coupled-pair invariants). This part is tagged `complexity: complex` accordingly.
