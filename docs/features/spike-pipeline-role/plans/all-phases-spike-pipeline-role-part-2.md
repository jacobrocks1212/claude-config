---
kind: implementation-plan
feature_id: spike-pipeline-role
status: Complete
created: 2026-07-18
complexity: complex
phases: [3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\plans\all-phases-spike-pipeline-role-part-2.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 3. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\plans\all-phases-spike-pipeline-role-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\plans\all-phases-spike-pipeline-role-part-2.md` (this file)
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\plans\all-phases-spike-pipeline-role-part-3.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — spike-pipeline-role  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\PHASES.md` (spike-pipeline-role, Phase 3 of 6)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\spike-pipeline-role\SPEC.md`

**Total phases:** 1 (Phase 3) in this part; 4 across the 3-part series.
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the **Plan-specific execution notes** below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

### Plan-specific execution notes (claude-config — override contract defaults)

- **Quality gates = the repo invariant battery** via `/gate-battery` (or `python3 user/scripts/gate-battery.py --repo-root .`). Trust its LAST-line banner; do NOT grep individual gate output. Battery: `pytest user/scripts/`, `lazy-state.py --test`, `bug-state.py --test`, `lazy_parity_audit.py --repo-root .`, `cli_surface_gen.py --check`, `doc-drift-lint.py --repo-root .`, `lint-skills.py`.
- **Coupled-pair regeneration (HARD — this phase's core mechanic).** `lazy-batch/SKILL.md` is the CANONICAL of two derived skills: `lazy-bug-batch/SKILL.md` and `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md`. The derived files are GENERATED by `generate-coupled-skills.py` from `lazy-batch/SKILL.md` + per-pair overlays in `user/scripts/coupled-overlays/`. **A canonical block ABSENT from an overlay is deleted-by-omission from the derived file** — so a new Spike Step added to the canonical will NOT appear in the derived skills until each pair's overlay carries a directive for the new heading(s). WU-2 owns this: `--extract` (rebuild overlays capturing the new step, encoding cloud's defer-to-workstation divergence) → `--write` (render derived) → `--check` (byte-diff gate). Do NOT hand-edit `lazy-bug-batch/SKILL.md` or `lazy-batch-cloud/SKILL.md` directly — the generator owns them.
- **After editing skills:** run `python ~/.claude/scripts/project-skills.py` (expand components) and `python ~/.claude/scripts/lint-skills.py` before the battery.
- **Directly-committed repo:** commit + push after this phase's battery is green. Conventional Commits, NO AI attribution. Regenerate `LAZY_QUEUE.md` (`python user/scripts/lazy-queue-doc.py --repo-root .`) before the commit.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this plan has no completed hard upstream dependencies) | — | — | `**Depends on:** (none)` per SPEC.md |

Phase-1 artifacts this phase WIRES (present, complete, currently referenced by zero consumers — verified):
- `user/skills/_components/spike-dispatch.md` (orchestrator protocol — sibling of `investigation-dispatch.md`) [VERIFY: `test -f user/skills/_components/spike-dispatch.md`]
- `user/skills/_components/lazy-batch-prompts/dispatch-spike.md` (registry-emitted Opus subagent contract; `@requires item_name, spec_path, spike_goal, next_on_pass, item_id, cwd`) [VERIFY: `test -f user/skills/_components/lazy-batch-prompts/dispatch-spike.md`]
- `spike` dispatch class (`--emit-dispatch spike` resolves it) [VERIFY: `grep -n '"spike"' user/scripts/lazy_core/dispatch.py`]

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | spike-pipeline-role | P3 | Orchestrator-loop wiring (lazy-batch + coupled mirrors) | Part 1 (Phase 2 routing) | Solo (WU-1→WU-2 sequential) |

## Work Units

- [x] WU-1 — Spike Step in `lazy-batch/SKILL.md` (ensure-runtime pre-boot + PASS/FAIL branch + emit-dispatch)
- [x] WU-2 — Coupled mirror into `lazy-bug-batch` + `lazy-batch-cloud` via the overlay generator

---

### Phase: spike-pipeline-role P3 — Orchestrator-loop wiring (lazy-batch + coupled mirrors)

**Goal:** Teach the `lazy-batch` orchestrator to handle a dispatched `spike` cycle — pre-boot the orchestrator-owned dev runtime (reuse `--ensure-runtime`), emit the registered spike dispatch, and branch on the returned verdict (PASS→continue / FAIL→NEEDS_INPUT+halt, with the `--park --park-provisional` PARK-not-accept carve-out) — then mirror it into the coupled cloud/bug orchestrators.
**Entry criteria:** Part 1 landed (Phase 2: `compute_state` emits `sub_skill="spike"` on both signals). Without it the orchestrator would never be handed a spike cycle to run.
**SPEC.md references:** "The routing signals"; "PASS/FAIL orchestrator-loop branching"; "The provisional-eligibility carve-out (Spike FAIL never auto-accepted)"; Integration surface rows "Orchestrator loop" and "Dispatch template"/"Orchestrator component"; Reuse map (orchestrator-owned runtime = `--ensure-runtime`).

#### WU-1 — Spike Step in `lazy-batch/SKILL.md`

- **Scope (PHASES deliverables 1 + 2, tightly coupled — one cohesive edit to one file):**
  - "A Spike Step in `lazy-batch/SKILL.md`: ensure-runtime pre-boot for `spike` cycles (reuse `--ensure-runtime`, orchestrator-owned), PASS→continue / FAIL→NEEDS_INPUT+halt branching, the `--park --park-provisional` PARK-not-accept path for FAIL"
  - "Emit the spike dispatch via `--emit-dispatch spike` (the registered class from Phase 1) using the `spike-dispatch.md` component"
- **TDD:** no (prose orchestrator contract in a SKILL.md; no unit-testable code — validated by `lint-skills.py` + `project-skills.py` expansion + the doc-drift battery gates). Correctness is reviewed against the already-complete component contracts.
- **Files to create/modify:** `user/skills/lazy-batch/SKILL.md` (add the Spike handling within the Step 1 Cycle Loop; add a runtime pre-boot sub-step for `sub_skill == "spike"` mirroring Step 1d.0 [VERIFY: `grep -n "1d.0" user/skills/lazy-batch/SKILL.md`]). Reference (do not duplicate) `spike-dispatch.md`.
- **Implementation goal:**
  - **Runtime pre-boot:** for a `sub_skill == "spike"` cycle on a workstation, run the orchestrator-owned `--ensure-runtime` readiness gate BEFORE dispatch — the SAME one mcp-test uses [VERIFY: `grep -n "ensure-runtime" user/skills/lazy-batch/SKILL.md`] — and proceed to dispatch ONLY on `state==READY AND health_code==200 AND mcp_tools_present` (the existing conjunction; do not invent a new one). Reuse Step 1d.0's exact pattern; a spike cycle is runtime-owning identically to mcp-test.
  - **Emit:** dispatch via `python3 ~/.claude/scripts/lazy-state.py --emit-dispatch spike --context …` [VERIFY: `grep -n "emit-dispatch" user/scripts/lazy-state.py`] passing the `@requires` tokens the emitted template declares (`item_name, spec_path, spike_goal, next_on_pass, item_id, cwd`), then dispatch the returned `dispatch_prompt` VERBATIM as an `Agent` with `model: dispatch_model` (Opus). Do NOT hand-compose the prompt (the validate-deny guard denies unregistered prompts on a marked run).
  - **Branch on the structured verdict** (the subagent returns `verdict/method/evidence/tooling_ok/results_doc/on_fail/next`): **PASS** → continue to the prescribed next cycle (the spike ticked its gated deliverable via gate-boundary-safe reconcile; the orchestrator does NOT flip status or write a receipt). **FAIL** → the spike has written `NEEDS_INPUT.md` (`written_by: spike`); halt. **Under `--park --park-provisional`** a Spike FAIL is PARKED (surfaced at the flush), NEVER auto-accepted — the `provisional_eligibility` carve-out already enforces this [VERIFY: `grep -n "written_by.*spike\|spike_verdict" user/scripts/lazy_core/docmodel.py`]; the orchestrator step states the PARK-not-accept rule explicitly so the two layers agree.
- **Spec requirements:** SPEC "PASS/FAIL orchestrator-loop branching"; "requirement 3" (no provisional auto-accept for a Spike FAIL — deliberate carve-out); `spike-dispatch.md` is the SSOT for the protocol — the SKILL.md step references it, it does not re-derive it.
- **Batch:** 1

#### WU-2 — Coupled mirror into `lazy-bug-batch` + `lazy-batch-cloud` via the overlay generator

- **Scope (PHASES deliverable 3):** "**Coupled mirror into `lazy-bug-batch` + `lazy-batch-cloud`** via the overlay generator (`generate-coupled-skills.py --write`; then `--check`). Cloud defers the runtime spike to workstation (like `/investigate` / `/mcp-test`)."
- **TDD:** no (mechanical regeneration; the `--check` drift gate + `lint-skills.py` + doc-drift battery are the verification).
- **Files to create/modify:** `user/scripts/coupled-overlays/lazy-bug-batch.overlay.json` and `user/scripts/coupled-overlays/lazy-batch-cloud.overlay.json` (rebuilt via `--extract`) [VERIFY: `ls user/scripts/coupled-overlays/lazy-batch-cloud.overlay.json user/scripts/coupled-overlays/lazy-bug-batch.overlay.json`]; the GENERATED `user/skills/lazy-bug-batch/SKILL.md` and `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` (written by `--write`, never hand-edited).
- **Implementation goal:** Run the standard coupled-pair flow [VERIFY: `test -f user/scripts/generate-coupled-skills.py`]:
  1. `python3 user/scripts/generate-coupled-skills.py --extract` — rebuild the two lazy-batch-derived overlays capturing WU-1's new Spike Step. For **`lazy-batch-cloud`**, the spike step's cloud divergence is the **defer-to-workstation** discipline (record the trigger — one cycle-log line + a results-doc/BLOCKED.md note — and DEFER the proof to a workstation run, returning PENDING; NEVER dispatch a runtime spike cloud-side or fabricate a verdict). This divergence is ALREADY encoded in `spike-dispatch.md` (cloud note) and `dispatch-spike.md`'s cloud `@section` — the overlay must preserve it as the cloud-axis `verbatim`/divergent block, exactly as the existing `/investigate` and `/mcp-test` cloud-defer blocks are stored. For **`lazy-bug-batch`**, mirror the step on the bug axis (no cloud deferral; the bug orchestrator's operator-confirmed divergences per the parity manifest).
  2. `python3 user/scripts/generate-coupled-skills.py --write` — render both derived SKILL.md files.
  3. `python3 user/scripts/generate-coupled-skills.py --check` — MUST exit 0 (byte-identical regenerated vs. committed). This is the drift gate; a non-zero exit means an overlay directive is missing.
- **Spec requirements:** SPEC Integration surface "Orchestrator loop" row ("coupled mirror … via the overlay generator"); the coupled-pair rules in `claude-config/CLAUDE.md` (Coupled Skill Pairs table); cloud defers runtime work like `/investigate`/`/mcp-test`.
- **Batch:** 2 (depends on WU-1 — the generator reads the canonical `lazy-batch/SKILL.md` WU-1 just edited; no file overlap, but a hard content dependency).

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-1      | Solo      | `lazy-batch/SKILL.md` |
> | 2     | WU-2      | Solo      | Overlays + generated derived skills; depends on WU-1's canonical edit → sequential |

**Plan-specific execution note:** WU-1 is the substantive orchestrator-contract prose (a new production dispatch seam with runtime ownership + verdict branching — `complex`). WU-2 is mechanical regeneration, but it is glued to WU-1 (the generator reads WU-1's output) and a 2-WU phase is not split mid-phase; a single `complex` WU sets the part's tier. This part is tagged `complexity: complex`.
