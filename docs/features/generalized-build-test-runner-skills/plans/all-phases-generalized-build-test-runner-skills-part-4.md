---
kind: implementation-plan
feature_id: generalized-build-test-runner-skills
status: Ready
created: 2026-07-13
phases: [4]
complexity: mechanical
---

> **Mobile plan** — generated by `/write-plan` (manual orchestration) on 2026-07-13.
> To execute: `/execute-plan C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-4.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 4 of 4. Sibling parts:
> - part 1: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-1.md`
> - part 2: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-2.md`
> - part 3: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-3.md`
> - part 4: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-4.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — generalized-build-test-runner-skills (v2) — Part 4: Phase 4 (Validation + docs + KPI wiring)

**PHASES.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/PHASES.md`

**SPEC.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/SPEC.md`

**Total phases:** this part: Phase 4 only (of 0–4)
**Plan version:** v2 (reference-based)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is claude-config — the gates are the Python invariant battery below, NOT `/msbuild`/`/mstest`/nx), this plan's own per-phase steps and the "Plan-specific execution notes" below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (READ FIRST)

1. **Entry gate:** Phases 0–3 complete (parts 1–3 executed; the Pester + Cognito-diff proofs require the workstation).
2. **Per-commit invariant battery:** `python3 user/scripts/gate-battery.py --repo-root .` — trust the LAST-line banner (`RESULT=PASS cmds=7 failed=0`); ZERO changes under `user/scripts/tests/baselines/`.
3. **Turn-end gate:** Read `~/.claude/skills/_components/turn-end-gate.md`; never end a turn on an in-flight battery/Pester run.
4. **Cognito byte-untouched guard (SPEC L6) — per commit AND as this part's WU-3 proof:** `git show --name-only --format= HEAD` must contain NOTHING under `repos/cognito-forms/` and NONE of `user/scripts/build-queue*.ps1` / `user/hooks/build-queue-enforce.sh`.
5. **Provisional decisions in force:** L2/L3/L4 + D3-precision (`docs/features/generalized-build-test-runner-skills/NEEDS_INPUT_PROVISIONAL.md`). In particular: do NOT "fix" the pinned bare-`npm run qg` ALLOW residual while ratification is pending.
6. **Root `CLAUDE.md` edits (WU-2) are surgical:** one Scripts-table row + one prose note; nothing else in that file moves.
7. **Halts:** unresolvable red gate / genuine design fork → `BLOCKED.md` / `NEEDS_INPUT.md` per the execution contract.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| build-queue-generalization | hard | `C:/Users/Jacob/source/repos/claude-config/docs/features/build-queue-generalization/COMPLETED.md` | Names the 5 Pester suites + the 3 known sandbox-environmental Job-Object failures (baseline-comparison protocol) |
| friction-kpi-registry | (context) | `C:/Users/Jacob/source/repos/claude-config/docs/kpi/registry.json` | The registry the two SPEC KPI rows are appended to (schema by example) |
| (this feature) | — | `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/SPEC.md` | `## KPI Declaration` carries the two full-schema rows VERBATIM — copy, don't re-draft |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | generalized-build-test-runner-skills | P4 | Validation + docs + KPI wiring | Phases 0–3 complete (parts 1–3) | Solo |

## Work Units

- [ ] WU-1 — KPI registry wiring (two rows) + scorecard re-render
- [ ] WU-2 — Root CLAUDE.md docs rows + doc-drift green
- [ ] WU-3 — Full validation sweep: dogfood banner, 5 Pester suites, Cognito-untouched diff proof

---

### Phase: generalized-build-test-runner-skills P4 — Validation + docs + KPI wiring

**Goal:** the SPEC's Validation Criteria are closed with recorded evidence; the KPI rows are registered; the doc tables tell the truth; L6 (Cognito byte-untouched) is PROVEN, not asserted.
**Entry criteria:** Phases 0–3 complete.
**SPEC.md references:** Validation Criteria (all rows), `## KPI Declaration`, L6, Phase 4 bullet.

**Mechanical justification (complexity: mechanical):** WU-1 copies two fully-drafted JSON rows into an existing registry and runs its linter; WU-2 is a pure documentation edit over an already-existing script; WU-3 executes existing verification commands and records outputs. No WU involves a novel design decision, algorithm, or new cross-boundary wiring (failure handling routes through the standard Blocking Issue Protocol regardless of model tier).

#### WU-1 — KPI registry wiring + scorecard re-render

- **Scope:** PHASES P4 deliverable 1.
- **TDD:** no (registry rows are fully specified in the SPEC; the deterministic linter is the gate).
- **Files to create/modify:** `docs/kpi/registry.json` (two appended rows), `docs/kpi/SCORECARD.md` (re-rendered).
- **Implementation goal:** append the two rows from SPEC `## KPI Declaration` VERBATIM (`generalized-runner-raw-invocation-deny-recurrence`, `runner-turn-end-stall-recurrence`) to `docs/kpi/registry.json` [VERIFY: `python3 -c "import json; json.load(open('docs/kpi/registry.json'))"`], preserving the existing rows byte-identically. Run `python3 user/scripts/kpi-scorecard.py --lint` → exit 0 (fix only schema-shape findings on the NEW rows; never touch existing rows). Re-render the scorecard (`python3 user/scripts/kpi-scorecard.py`) — the new rows must surface as honest NO-DATA / PENDING-BASELINE (null baselines by design; do NOT run `--capture-baseline`, there is no signal data yet). Full battery; commit.
- **Spec requirements:** `## KPI Declaration`; PHASES header "Friction-reduction feature: yes".
- **Batch:** 1.

#### WU-2 — Root CLAUDE.md docs rows + doc-drift green

- **Scope:** PHASES P4 deliverable 2.
- **TDD:** no (pure documentation).
- **Files to create/modify:** `CLAUDE.md` (repo root — one Scripts-table row for `gate-battery.py` [VERIFY: `ls user/scripts/gate-battery.py`] describing runner+banner+await+manifest opt-in, plus a one-sentence addendum to the build-queue row family noting the two AlgoBooth qg ops and the hygiene value Phase 3 locked); verify `user/scripts/CLAUDE.md` already carries the runner row (landed in part 1) and the contract pointer (part 1 Phase 0).
- **Implementation goal:** surgical edits per note 6. Then `python3 user/scripts/doc-drift-lint.py --repo-root .` → exit 0 (the new script-table row maps doc→disk cleanly). Full battery; commit.
- **Spec requirements:** Phase 4 bullet "CLAUDE.md script/hook tables updated"; doc-drift gate.
- **Batch:** 2.

#### WU-3 — Full validation sweep: dogfood banner, Pester suites, Cognito-untouched proof

- **Scope:** PHASES P4 deliverables 3–6 + the phase MVB. Evidence-recording WU — every check's verbatim output lands in PHASES `## Implementation Notes`.
- **TDD:** no (verification execution).
- **Files to create/modify:** `docs/features/generalized-build-test-runner-skills/PHASES.md` (tick P4 rows + evidence).
- **Implementation goal — run each check and record:**
  1. **Dogfood:** fresh `python3 user/scripts/gate-battery.py --repo-root .` → record the `gate-battery: run=<id> op=battery RESULT=PASS cmds=7 failed=0 (elapsed=<s>s)` last line (this also gates this WU's commit).
  2. **Pester (workstation):** run all 5 suites — `powershell.exe -NoProfile -Command "Invoke-Pester -Path user/scripts/ -TagFilter '*' " ` scoped to `build-queue*.Tests.ps1` (use the repo's established invocation; enumerate the 5 files first [VERIFY: `ls user/scripts/build-queue*.Tests.ps1`]). Expect 100% pass. If any failure reproduces: re-run the SAME suite at the pre-feature baseline commit (`git stash` / worktree at the commit recorded below) — identical-at-baseline = environmental (record BOTH runs verbatim, proceed; the upstream receipt documents 3 known sandbox-environmental Job-Object cases); new-at-HEAD = a real L6 violation → `BLOCKED.md`, do NOT rationalize.
  3. **Cognito-untouched proof (L6):** determine the pre-feature baseline commit (the parent of this feature's FIRST commit — locate via `git log --oneline -- docs/features/generalized-build-test-runner-skills | tail -1`). Run `git diff <baseline>..HEAD --stat -- repos/cognito-forms user/scripts/build-queue.ps1 user/scripts/build-queue-runner.ps1 user/scripts/build-queue-hygiene.ps1 user/scripts/build-queue-status.ps1 user/scripts/build-queue-await.ps1 user/hooks/build-queue-enforce.sh` → MUST be empty; record the baseline hash + the empty output.
  4. **Lint sweep:** `python user/scripts/lint-skills.py --check-projected --check-capabilities`, `python3 user/scripts/lint-skill-config.py --repo-root .`, `python3 user/scripts/kpi-scorecard.py --lint` → all exit 0.
  5. Tick the P4 deliverable checkboxes (the cloud-compatibility row under `**Runtime Verification**` stays UNCHECKED — it is `<!-- verification-only -->`, closed later by the first cloud-session battery run; per the pending-runtime-gates contract, enumerate it in the completion summary as a pending manual runtime gate).
- **Spec requirements:** Validation Criteria rows "Cognito untouched", "Light ops never queue" (covered by part 1's pytest — cite, don't re-prove), "Cloud compatibility" (deferred row + Phase 1 mechanical proxy — cite both); L6.
- **Batch:** 3.

**Batch overview:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | kpi files |
| 2 | WU-2 | Solo | root CLAUDE.md |
| 3 | WU-3 | Solo | PHASES.md evidence |

**Plan-specific risk notes:** (a) the Pester invocation must not recurse into unrelated `.Tests.ps1` outside the build-queue family — enumerate the 5 files explicitly; (b) `kpi-scorecard.py` renders byte-stably (no wall-clock embed) — if the re-rendered SCORECARD.md diff includes unrelated churn, STOP and check you ran the committed version; (c) completion is gate-owned: do NOT flip SPEC/PHASES `**Status:**`, do NOT write COMPLETED.md/SKIP_MCP_TEST.md — report the phase done and leave the receipts to `__mark_complete__` / the operator (SKIP_MCP_TEST.md is operator-granted per `.claude/skill-config/quality-gates.md`).
