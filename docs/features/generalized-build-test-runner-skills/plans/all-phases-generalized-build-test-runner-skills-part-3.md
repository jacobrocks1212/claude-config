---
kind: implementation-plan
feature_id: generalized-build-test-runner-skills
status: Ready
created: 2026-07-13
phases: [3]
complexity: complex
---

> **Mobile plan** — generated by `/write-plan` (manual orchestration) on 2026-07-13.
> To execute: `/execute-plan C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-3.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 3 of 4. Sibling parts:
> - part 1: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-1.md`
> - part 2: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-2.md`
> - part 3: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-3.md` (this file)
> - part 4: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-4.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — generalized-build-test-runner-skills (v2) — Part 3: Phase 3 (AlgoBooth heavy qg ops)

**PHASES.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/PHASES.md`

**SPEC.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/SPEC.md`

**Total phases:** this part: Phase 3 only (of 0–4)
**Plan version:** v2 (reference-based)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is claude-config — the gates are the Python invariant battery below, NOT `/msbuild`/`/mstest`/nx), this plan's own per-phase steps and the "Plan-specific execution notes" below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (READ FIRST — this part spans TWO repos and is WORKSTATION-ONLY)

1. **Platform gate:** this part requires the Windows workstation (`DESKTOP-GHTC5K6`) with the live AlgoBooth checkout at `C:\Users\Jacob\repos\AlgoBooth` (NON-standard path — not under `~/source/repos`). In a cloud session, do NOT attempt any WU — this phase is `DEFERRED_NON_CLOUD` class; report and stop.
2. **Entry gate:** Phases 0–2 complete (parts 1–2 executed): the contract component, runner, and `/gate-battery` skill exist at HEAD.
3. **Authoring-surface map (verified 2026-07-13 — respect it exactly):**
   - `repos/algobooth/.claude/skill-config/build-queue-ops.json`, `…/skill-config/skill-catalog.md`, `…/skills/qg-rust/`, `…/skills/qg-sidecar/` are authored IN claude-config (the live AlgoBooth `.claude/skill-config` + `.claude/skills` are SYMLINKS to these). The Edit tool refuses to write through a symlink — always edit the `repos/algobooth/...` paths, never the `C:\Users\Jacob\repos\AlgoBooth\.claude\...` symlinked spellings.
   - The exec wrappers live REPO-LOCALLY in AlgoBooth: `C:\Users\Jacob\repos\AlgoBooth\.claude\scripts\` (create the dir; it does not exist and is NOT symlinked). These are committed in the ALGOBOOTH repo per its commit policy (`repos/algobooth/.claude/skill-config/commit-policy.md` — the generic commit component; personal repo; commit only, do NOT push).
   - Hook fixture tests: `user/scripts/test_hooks.py` in claude-config (additive only).
4. **Per-commit invariant battery (claude-config commits):** `python3 user/scripts/gate-battery.py --repo-root C:/Users/Jacob/source/repos/claude-config` — trust the LAST-line banner (`RESULT=PASS cmds=7 failed=0`); ZERO changes under `user/scripts/tests/baselines/`. The AlgoBooth-repo commit (WU-2) is gated by a PowerShell parse check + the WU-4 live-fire instead (AlgoBooth's own qg gates do not cover `.claude/scripts/`).
5. **Turn-end gate (load-bearing here — heavy compiles):** Read `~/.claude/skills/_components/turn-end-gate.md`. The WU-4 live-fire runs a minutes-long compile: follow it to the authoritative banner via `build-queue-await.ps1 -Seq <N>` (exit 124 = still going — re-await); NEVER end a turn on an enqueue echo.
6. **Cognito byte-untouched guard (SPEC L6) — per claude-config commit:** `git show --name-only --format= HEAD` must contain NOTHING under `repos/cognito-forms/` and NONE of `user/scripts/build-queue*.ps1` / `user/hooks/build-queue-enforce.sh`. **The enforce hook is READ but never edited in this part** — the deny rows ride the manifest only.
7. **Provisional decisions in force (park-provisional — do NOT re-litigate, do NOT "improve"):** D3-precision (`docs/features/generalized-build-test-runner-skills/NEEDS_INPUT_PROVISIONAL.md`): deny ONLY the exact heavy forms; bare `npm run qg` is deliberately NOT denied (a bare-qg deny row provably shadows `npm run qg -- ts` under `_compile_manifest_deny`'s token-escape + trailing `(?:\s|$)` semantics, and the manifest has no allow mechanism). Do NOT widen the deny rows; do NOT touch the hook to add lookahead support.
8. **Halts:** unresolvable red gate / genuine design fork → `BLOCKED.md` / `NEEDS_INPUT.md` per the execution contract (written in the FEATURE dir in claude-config, never in AlgoBooth).

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| build-queue-generalization | hard | `C:/Users/Jacob/source/repos/claude-config/docs/features/build-queue-generalization/COMPLETED.md` | Manifest schema + `Resolve-BuildQueueOp` exec resolution + the documented outstanding AlgoBooth exec-wrapper gap (this part ships the FIRST wrappers and the FIRST live-fire) |
| build-queue-generalization | hard | `C:/Users/Jacob/source/repos/claude-config/user/hooks/build-queue-enforce.sh` | `_compile_manifest_deny` (line ~488) — the compile semantics the deny rows and fixture tests are authored against (READ-ONLY) |
| build-queue-eta-priority-lanes | soft | `C:/Users/Jacob/source/repos/claude-config/user/scripts/build-queue-status.ps1` | `lane` field surfacing + ETA mechanics the new heavy ops inherit unchanged |
| (this feature, part 1) | — | `C:/Users/Jacob/source/repos/claude-config/user/skills/_components/runner-outcome-contract.md` | The contract the new skills cite |
| (context) | — | `C:/Users/Jacob/source/repos/claude-config/repos/algobooth/.claude/skills/tauri-build/SKILL.md` | Shape template for the new queue-routed thin skills |
| (context) | — | `C:/Users/Jacob/repos/AlgoBooth/scripts/quality-gate.sh` | The qg gate dispatcher the wrappers shell and the hygiene re-check inspects |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | generalized-build-test-runner-skills | P3 | AlgoBooth qg ops + skills + deny + live-fire | Phases 0–2 (parts 1–2); workstation | Solo — WUs are order-dependent |

## Work Units

- [x] WU-1 — Hygiene re-check + manifest rows (`qg-rust`, `qg-sidecar`) + TDD hook fixture tests
- [x] WU-2 — Exec wrappers in the live AlgoBooth repo (`.claude/scripts/qg-*-filtered.ps1`)
- [x] WU-3 — Thin skills `/qg-rust` + `/qg-sidecar` + skill-catalog row + lint sweep
- [x] WU-4 — Live-fire validation spike (first queue-on-AlgoBooth run) + deny live check

---

### Phase: generalized-build-test-runner-skills P3 — AlgoBooth heavy qg ops

**Goal:** `qg-rust`/`qg-sidecar` are manifested heavy ops riding the existing queue machinery unchanged (banner, await, status/ETA), with precision deny rows proven by fixture tests, thin skills carrying the contract, and the first-ever live-fire of the queue-on-AlgoBooth path recorded.
**Entry criteria:** Phases 0–2 complete; workstation session; AlgoBooth npm workspace installed (native setup per `C:\Users\Jacob\algobooth-windows-native-setup.md`).
**SPEC.md references:** D2 (heavy class), D3 + D3-precision (deny scope), D7 (manifest additions), L2/L3 (provisional), Validation Criteria rows "AlgoBooth heavy op queue-routed", "AlgoBooth deny + redirect".

#### WU-1 — Hygiene re-check + manifest rows + TDD hook fixture tests

- **Scope:** PHASES P3 deliverables 1–3.
- **TDD:** yes (fixture tests RED against the pre-edit manifest, GREEN after the rows land).
- **Files to create/modify:** `repos/algobooth/.claude/skill-config/build-queue-ops.json` (two additive op entries — existing `tauri-build`/`cargo-release` entries byte-untouched [VERIFY: `grep -n "tauri-build" repos/algobooth/.claude/skill-config/build-queue-ops.json`]), `user/scripts/test_hooks.py` (additive fixture tests).
- **Test files:** `user/scripts/test_hooks.py`.
- **Test expectations** (tmp-repo manifest fixture carrying the two new ops; hook armed via the `BQE_PLATFORM_OVERRIDE=armed` test seam [VERIFY: `grep -n "BQE_PLATFORM_OVERRIDE" user/scripts/test_hooks.py`]):
  - DENY: `npm run qg -- rust`, `npm run qg -- sidecar`, `npm run quality-gate -- rust`, a chained `cd "C:\x" && npm run qg -- rust` — each denied with the redirect naming the op's skill (`/qg-rust` / `/qg-sidecar`);
  - ALLOW: `npm run qg -- ts`, `npm run qg -- docs`, **bare `npm run qg`** (the D3-precision pinned residual — this ALLOW test is deliberate documentation, cite the provisional decision in its docstring), `BUILD_QUEUE_BYPASS=1 npm run qg -- rust`, and a reference-only mention (`echo "npm run qg -- rust"` inside a quoted string — matches the hook's existing quoted/reference discipline; if the current hook's quote handling does not exempt it, assert the ACTUAL current behavior and note it, do not change the hook).
- **Implementation goal:** FIRST the hygiene re-check (SPEC Open Question 4): read `C:\Users\Jacob\repos\AlgoBooth\scripts\quality-gate.sh` (and the rust/sidecar gate scripts it dispatches) and determine whether `qg -- rust|sidecar` invoke cargo in a way that writes shared Tauri artifacts (`src-tauri/target` release outputs). If NO (expected — qg compiles for checking): keep `hygiene: "none"`; if YES: use `"rust-tauri"`. Record the decision + evidence (file:line of the cargo invocations) in PHASES Implementation Notes. THEN add the two manifest entries: `"qg-rust": {"exec": ".claude/scripts/qg-rust-filtered.ps1", "kind": "test", "hygiene": <per re-check>, "skill": "/qg-rust", "deny": ["npm run qg -- rust", "npm run quality-gate -- rust"], "lane": "heavy"}` and the `qg-sidecar` twin (`deny: ["npm run qg -- sidecar", "npm run quality-gate -- sidecar"]`). The deny entries are EXACT token sequences — `_compile_manifest_deny` [VERIFY: `grep -n "_compile_manifest_deny" user/hooks/build-queue-enforce.sh`] token-escapes and `\s+`-joins them onto `_CMD_START` with a trailing `(?:\s|$)`, so these forms match themselves (plus trailing args) and can never shadow `-- ts`/`-- docs`. Run the fixture tests GREEN; full battery; commit (claude-config; allowed set: the manifest + `user/scripts/test_hooks.py`).
- **Spec requirements:** D3 + D3-precision; D7; Validation Criteria "AlgoBooth deny + redirect".
- **Batch:** 1.

#### WU-2 — Exec wrappers in the live AlgoBooth repo

- **Scope:** PHASES P3 deliverable 4.
- **TDD:** no (thin PS shims; verified by parse check + WU-4 live-fire).
- **Files to create/modify:** `C:\Users\Jacob\repos\AlgoBooth\.claude\scripts\qg-rust-filtered.ps1`, `C:\Users\Jacob\repos\AlgoBooth\.claude\scripts\qg-sidecar-filtered.ps1` (new; create the `.claude\scripts\` dir — it does not exist).
- **Implementation goal:** each wrapper: `Set-Location` to the repo root (resolve from the script's own path — `$PSScriptRoot\..\..`), run `npm run qg -- rust` (resp. `sidecar`) with output streaming to the caller (the queue runner captures it to the log; the qg `QG_VERDICT:` line rides the log), and `exit $LASTEXITCODE` faithfully. Accept and forward any pass-through args after the gate name (the queue wrapper appends `$ExecArgs`). No banner logic here — the QUEUE composes the authoritative `build-queue:` banner; the wrapper's only contract is a faithful exit code. Gate before commit: `powershell.exe -NoProfile -Command "$null = [scriptblock]::Create((Get-Content -Raw '<wrapper>'))"` parses both files clean. Commit IN AlgoBooth per its commit policy (conventional message; commit only, do NOT push).
- **Spec requirements:** D7 (`kind: test`-class exec scripts — thin wrappers shelling qg).
- **Batch:** 2.

#### WU-3 — Thin skills `/qg-rust` + `/qg-sidecar` + skill-catalog + lint sweep

- **Scope:** PHASES P3 deliverable 5.
- **TDD:** no (skill prose mirroring an existing shape).
- **Files to create/modify:** `repos/algobooth/.claude/skills/qg-rust/SKILL.md`, `repos/algobooth/.claude/skills/qg-sidecar/SKILL.md` (new), `repos/algobooth/.claude/skill-config/skill-catalog.md` (two rows).
- **Implementation goal:** mirror the `/tauri-build` shape [VERIFY: `ls repos/algobooth/.claude/skills/tauri-build/SKILL.md`]: (1) announce; (2) enqueue `cd <algobooth repo root> && powershell.exe -ExecutionPolicy Bypass -File "$HOME/.claude/scripts/build-queue.ps1" -Op qg-rust` (resp. `qg-sidecar`); (3) TRUST the authoritative LAST-line `build-queue: seq=<N> op=qg-rust RESULT=…` banner — never grep the runner log or `results/<seq>.json` to disambiguate; (4) backgrounded/turn-boundary follows via `powershell.exe -ExecutionPolicy Bypass -File "$HOME/.claude/scripts/build-queue-await.ps1" -Seq <N>` (exit 124 = not done — NEVER success; re-await or `/build-queue-status`); (5) turn-end gate BY REFERENCE (`!cat ~/.claude/skills/_components/turn-end-gate.md`); (6) note the light siblings: `npm run qg -- ts|docs` stay DIRECT invocations ending in the authoritative `QG_VERDICT:` line (never pipe through tail) — only rust/sidecar are queue-routed (SPEC L2/L3); (7) cite `_components/runner-outcome-contract.md` as the governing contract. Update `skill-catalog.md`. Then re-project + lint: `python user/scripts/project-skills.py`; `python user/scripts/lint-skills.py --check-projected --check-capabilities`; `python3 user/scripts/lint-skill-config.py --repo-root .` — all exit 0. Full battery; commit (claude-config; allowed set: the two skills + skill-catalog.md).
- **Spec requirements:** D6 second bullet; Validation Criteria "Turn-end gate wired by reference".
- **Batch:** 3.

#### WU-4 — Live-fire validation spike (RUNTIME EVIDENCE REQUIRED)

- **Scope:** PHASES P3 deliverable 6 + the phase MVB. **A static code trace does NOT satisfy this WU** — it closes only on observed runtime artifacts (this is the first-ever exercise of the queue-on-AlgoBooth path; the upstream receipt records zero prior live-fires).
- **TDD:** no (runtime spike; evidence-producing).
- **Files to create/modify:** `docs/features/generalized-build-test-runner-skills/PHASES.md` (tick P3 rows + record evidence in Implementation Notes).
- **Implementation goal — run on the workstation, recording each artifact verbatim:**
  1. In AlgoBooth: enqueue `powershell.exe -ExecutionPolicy Bypass -File "$HOME/.claude/scripts/build-queue.ps1" -Op qg-rust` (background if long) and FOLLOW to the banner (`build-queue-await.ps1 -Seq <N>`, re-awaiting on 124 per the turn-end gate). Record: the authoritative `build-queue: seq=<N> op=qg-rust RESULT=<PASS|FAIL> …` LAST line, and the `~/.claude/state/build-queue/results/<seq>.json` payload (op/exit_code/duration/hygiene fields present). A FAIL verdict from qg itself is acceptable evidence of correct PLUMBING if the log shows the real gate ran and the exit propagated (record honestly; the queue contract, not AlgoBooth's code quality, is under test) — but a plumbing failure (exec not found, malformed result, no banner) is a red gate → fix within this part's files or halt per the Blocking Issue Protocol.
  2. Repeat once for `-Op qg-sidecar` (may run PASS/FAIL likewise).
  3. Live deny check in AlgoBooth (real hook, no override): attempt raw `npm run qg -- rust` → expect the PreToolUse deny naming `/qg-rust`; attempt `npm run qg -- ts` → runs (allowed) — record both observations. (These attempts are made as ordinary tool calls; the deny IS the expected outcome, not an error.)
  4. `/build-queue-status` once while a qg op is queued/active — record that the op surfaces with `lane=heavy` and ETA fields behave (soft-dep inheritance; `?` ETA is expected with <3 samples — record honestly).
  5. Tick the P3 deliverable checkboxes; write the evidence block (seq numbers, banner lines, deny screenshots/text) into PHASES `## Implementation Notes`.
- **Spec requirements:** Validation Criteria "AlgoBooth heavy op queue-routed" + "AlgoBooth deny + redirect"; PHASES P3 runtime-coupled assumption 3 (queue-on-AlgoBooth path).
- **Batch:** 4.

**Batch overview:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | manifest + test_hooks.py |
| 2 | WU-2 | Solo | AlgoBooth repo files (cross-repo commit) |
| 3 | WU-3 | Solo | skills + catalog |
| 4 | WU-4 | Solo | PHASES.md evidence only |

**Plan-specific risk notes:** (a) `Resolve-BuildQueueOp` resolves `exec` repo-root-relative from the payload cwd's git toplevel — the live-fire MUST be launched with cwd inside AlgoBooth or the manifest won't be found; (b) the tauri/cargo exec wrappers are STILL missing (documented upstream gap) — do not author them here (scope is qg only), and do not let their absence confuse a `Resolve-BuildQueueOp` smoke (`-Op tauri-build` failures are pre-existing, out of scope); (c) if `scripts/quality-gate.sh` lacks a `sidecar` gate name (SPEC ground truth says it exists — re-verify at WU-1), STOP and write `NEEDS_INPUT.md` naming the actual gate list rather than inventing an op; (d) npm on Windows may shim `npm.cmd` — the wrappers should invoke `npm` via `cmd /c npm run …` or the PowerShell-native call that resolves `.cmd` shims, whichever the existing AlgoBooth scripts use (check `scripts/` precedents); (e) two-repo work: never commit AlgoBooth files from the claude-config commit or vice versa — verify each commit's file list against its WU's allowed set.
