---
kind: realign-plan
feature_id: generalized-build-test-runner-skills
status: Ready
created: 2026-07-14
phases: []
upstream_phases_hashes:
  lazy-core-package-decomposition: 78251831f06ddb0d1213009e979e4fa421fa87b0977dae8b3e786a1db6f9142a
---

# Realign — generalized-build-test-runner-skills (2026-07-14)

> Read-only reality check against completed upstream dependencies. Generated by `/realign-spec`.
> No SPEC or PHASES files were modified by this analysis.

## Inputs

- **Downstream SPEC:** `docs/features/generalized-build-test-runner-skills/SPEC.md`
- **Downstream PHASES:** `docs/features/generalized-build-test-runner-skills/PHASES.md` (Phases 0–4, all `[x]`/COMPLETE)
- **Upstream dependencies checked (kind=hard, Status=Complete):**
  - `lazy-core-package-decomposition` — PHASES.md (156 lines, 7 phases 0–6, all COMPLETE) + 0 plans read (no plan file topically overlapped the downstream's Technical Design beyond what PHASES.md itself already recorded)
  - `build-queue-generalization` — no PHASES.md (never decomposed into phases); SPEC.md (`Status: Complete`) + `COMPLETED.md` read instead, per the skill's "no PHASES.md" fallback. 0 `plans/*.md` (dir does not exist).

Note: `build-queue-eta-priority-lanes` is a **soft** dep (needs the upstream to exist, not its impl specifics) — out of scope for reality-check per the dep-block schema; not checked here.

## Aligned

- **`user/scripts/tests/` layout** — the downstream's Cross-feature Integration Notes (written 2026-07-13, while `lazy-core-package-decomposition` was still In-progress) predicted "its Phase 3 shipped the `user/scripts/tests/` layout (new tests go there)". Confirmed on disk: `user/scripts/tests/test_gate_battery.py` and `user/scripts/tests/test_lazy_core/` (11 per-seam test files) both exist exactly where anticipated; the downstream's own Phase 1 placed its new test file there.
- **Battery manifest authored as commands, not file paths (SPEC L5)** — `.claude/skill-config/gate-battery.json` seeds all 7 gates as shell commands (`python3 -m pytest user/scripts/ -q`, `lazy-state.py --test`, etc.), never as file-path literals. This is exactly the hedge the downstream SPEC named: "precisely so its remaining Phases 4–6 layout churn cannot invalidate this feature." `lazy-core-package-decomposition` Phases 4–6 (gates.py/ledgers.py/dispatch.py/runtimeplane.py extraction, markers.py/pseudo.py extraction, `_monolith.py` deletion, ruff gate) landed real internal-layout churn — none of it touches the manifest, because the manifest never named a file.
- **`lazy_core/statedir.py::repo_key` (Phase 2 of the upstream) as the canonical repo-key convention** — the downstream's `gate-battery.py` carries a documented PRIVATE copy (`_repo_key`, "keep-in-sync" comment) rather than importing `lazy_core` (SPEC L4, cloud-compatibility — the runner must not depend on the PowerShell-adjacent lazy_core plane). Verified the private copy's docstring cites the correct location and the upstream's `repo_key` function still lives at `lazy_core/statedir.py` post-decomposition (confirmed present).
- **`build-queue-ops.json` schema / `Resolve-BuildQueueOp` / manifest-scoped `build-queue-enforce.sh` deny compile** — the downstream's Phase 3 (AlgoBooth qg ops) was implemented directly against `build-queue-generalization`'s actual shipped contracts (confirmed via `COMPLETED.md` + live manifest read at implementation time, per Phase 3's Implementation Notes), not a stale assumption from planning time.
- **Sequencing (SPEC L7)** — Phase 1's Implementation Notes record an explicit pre-edit check: "verified `lazy-core-package-decomposition` Status=Complete with `COMPLETED.md` before any edit — dep satisfied." The entire downstream implementation (Phases 0–4, all commits 2026-07-14) therefore already happened AFTER both hard upstreams reached their current completed state — there is no planning-time assumption left unreconciled against reality; the "assumption" and the "actual" are the same read.

## Drift

### Minor

(none)

### Moderate

(none)

### Severe

(none)

## Recommended next step

**Recommended:** `inline-edit`

**Why:** Both hard upstreams (`lazy-core-package-decomposition`, `build-queue-generalization`) completed BEFORE this feature's own implementation phases were executed (2026-07-14), so the downstream PHASES.md's stated integration assumptions were verified against the upstreams' final shipped state at build time, not against a stale in-progress snapshot — there is nothing left to patch; this realign exists purely to record freshness (the `upstream_phases_hashes` above) so `lazy-state.py`'s Step 4.6 gate stops re-triggering.
