---
kind: implementation-plan
feature_id: generalized-build-test-runner-skills
status: Ready
created: 2026-07-13
phases: [2]
complexity: mechanical
---

> **Mobile plan** — generated by `/write-plan` (manual orchestration) on 2026-07-13.
> To execute: `/execute-plan C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-2.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 4. Sibling parts:
> - part 1: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-1.md`
> - part 2: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-2.md` (this file)
> - part 3: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-3.md`
> - part 4: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-4.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — generalized-build-test-runner-skills (v2) — Part 2: Phase 2 (`/gate-battery` skill)

**PHASES.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/PHASES.md`

**SPEC.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/SPEC.md`

**Total phases:** this part: Phase 2 only (of 0–4)
**Plan version:** v2 (reference-based)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is claude-config — the gates are the Python invariant battery below, NOT `/msbuild`/`/mstest`/nx), this plan's own per-phase steps and the "Plan-specific execution notes" below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config overrides — READ FIRST)

1. **Entry gate:** Phases 0–1 complete (part 1 executed) — `user/skills/_components/runner-outcome-contract.md` and `user/scripts/gate-battery.py` + `.claude/skill-config/gate-battery.json` exist at HEAD. If not, STOP and report (execute part 1 first).
2. **Per-commit invariant battery** (all exit 0; ZERO changes under `user/scripts/tests/baselines/`). Preferred invocation now that the runner exists: `python3 user/scripts/gate-battery.py --repo-root .` — trust its LAST-line banner (`RESULT=PASS cmds=7 failed=0`); never grep gate output to disambiguate. Fallback: the 7 commands directly (pytest `user/scripts/`, `lazy-state.py --test`, `bug-state.py --test`, `lazy_parity_audit.py --repo-root .`, `cli_surface_gen.py --check --repo-root .`, `doc-drift-lint.py --repo-root .`, `lint-skills.py`).
3. **Turn-end gate:** Read `~/.claude/skills/_components/turn-end-gate.md`; never end a turn on an in-flight battery.
4. **Cognito byte-untouched guard (SPEC L6) — per commit:** `git show --name-only --format= HEAD` must contain NOTHING under `repos/cognito-forms/` and NONE of `user/scripts/build-queue*.ps1` / `user/hooks/build-queue-enforce.sh`.
5. **Commit cadence:** one commit per WU, directly on `main`.
6. **Provisional decisions in force:** L2/L3/L4 + D3-precision (`docs/features/generalized-build-test-runner-skills/NEEDS_INPUT_PROVISIONAL.md`) — do not re-litigate.
7. **Halts:** unresolvable red gate / genuine design fork → `BLOCKED.md` / `NEEDS_INPUT.md` per the execution contract.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (this feature, part 1) | — | `C:/Users/Jacob/source/repos/claude-config/user/skills/_components/runner-outcome-contract.md` | The contract the skill instantiates (trust-banner + await + turn-end reference) |
| (context) | — | `C:/Users/Jacob/source/repos/claude-config/repos/cognito-forms/.claude/skills/mstest/SKILL.md` | Shape template for a thin trust-the-banner skill (read-only reference — Cognito files are NEVER edited) |
| (context) | — | `C:/Users/Jacob/source/repos/claude-config/user/skills/CLAUDE.md` | Frontmatter contract + post-edit projection/lint workflow for user-level skills |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | generalized-build-test-runner-skills | P2 | `/gate-battery` thin skill | Phases 0–1 complete (part 1) | Solo |

## Work Units

- [x] WU-1 — Author `user/skills/gate-battery/SKILL.md` (thin, contract-conformant)
- [x] WU-2 — Projection + lint sweep incl. skill-config reference hygiene

---

### Phase: generalized-build-test-runner-skills P2 — `/gate-battery` thin skill

**Goal:** a generic user-level skill wraps the runner: trust the banner, background+await long runs, turn-end gate by reference, clean refusal story for manifest-less repos.
**Entry criteria:** Phases 0–1 complete (contract component + runner + seeded manifest at HEAD).
**SPEC.md references:** D6 first bullet, User Experience (claude-config block), L2 (light op — no queue admission), Validation Criteria row "Turn-end gate wired by reference".

**Mechanical justification (complexity: mechanical):** both WUs are prose/skill authoring against an already-shipped API (the runner) and an already-written contract (Phase 0), following an existing skill shape — no novel design decision, no algorithm, no cross-boundary wiring. Per the write-plan mechanical-vs-complex test these qualify (documentation/boilerplate over existing definitions).

#### WU-1 — Author `user/skills/gate-battery/SKILL.md`

- **Scope:** PHASES P2 deliverable 1.
- **TDD:** no (skill prose; behavior pinned by part 1's pytest suite).
- **Files to create/modify:** `user/skills/gate-battery/SKILL.md` (new).
- **Implementation goal:** frontmatter per `user/skills/CLAUDE.md` (name `gate-battery`, trigger-rich description, `argument-hint: [--repo-root <path>] [--await <run-id>]`). Body: (1) announce; (2) run `python3 ~/.claude/scripts/gate-battery.py --repo-root <repo-root or cwd>` [VERIFY: `ls user/scripts/gate-battery.py`] — the symlinked live path is `~/.claude/scripts/gate-battery.py`; (3) TRUST the authoritative LAST-stdout-line `gate-battery:` banner — NEVER grep gate output or `results/<run>.json` to disambiguate an exit code (quote the exact grammar from the contract component [VERIFY: `ls user/skills/_components/runner-outcome-contract.md`]); (4) backgrounding contract — a long battery may be launched `run_in_background`, then MUST be driven to the banner via `python3 ~/.claude/scripts/gate-battery.py --await <run-id>`; exit 124 = still going (re-await; NEVER success), 125 = malformed; (5) inject the turn-end gate BY REFERENCE: `!cat ~/.claude/skills/_components/turn-end-gate.md` — no copied gate text; (6) manifest-less repo → the runner refuses with exit 2 and a one-line reason; this is the correct generic behavior — the opt-in recipe is committing a `.claude/skill-config/gate-battery.json` (`{version, gates: [{id, cmd}]}`) at the repo toplevel (the file is deliberately absent in repos that have not opted in); (7) cloud note: pure Python, no PowerShell, no build-queue coupling (light op per SPEC L2). Exit-code vocabulary table: 0 pass / 1 fail / 2 no-manifest / 124–125 await-reserved.
- **Spec requirements:** D6 (`/gate-battery` bullet), D1 items 1–3 as instantiated prose.
- **Batch:** 1.

#### WU-2 — Projection + lint sweep incl. skill-config reference hygiene

- **Scope:** PHASES P2 deliverables 2–3.
- **TDD:** no (running existing linters; mechanical).
- **Files to create/modify:** none expected; conditionally `repos/*/.claude/skill-config/MANIFEST.json` (`intended_absent` rows) ONLY if the reference lint flags the new skill's `gate-battery.json` mention despite its fallback wording.
- **Implementation goal:** run `python user/scripts/project-skills.py` and spot-check `~/.claude/skills-projected/_default/gate-battery/SKILL.md` — the turn-end gate expanded from the component, no unresolved `!cat`, no circular includes. Run `python user/scripts/lint-skills.py --check-projected --check-capabilities` → exit 0. Run `python3 user/scripts/lint-skill-config.py --repo-root .` [VERIFY: `ls user/scripts/lint-skill-config.py`] → exit 0; if the new skill's `.claude/skill-config/gate-battery.json` reference is flagged as dangling/undeclared for repos that lack the manifest, resolve per the lint's own contract: prefer rephrasing the mention to the recognized fallback form ("absent → the runner refuses cleanly"); else add `intended_absent` entries (with the fallback note) to the flagging repos' `MANIFEST.json`. Confirm by grep that the skill source contains NO copied turn-end gate sentences (`grep -c "may not end while work" user/skills/gate-battery/SKILL.md` → 0). Full battery green; commit.
- **Spec requirements:** Validation Criteria row "Turn-end gate wired by reference"; L6 guard (note 4).
- **Batch:** 2.

**Batch overview:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | — |
| 2 | WU-2 | Solo | reads WU-1's file; conditional MANIFEST edits |

**Plan-specific risk notes:** (a) do not add a `.claude/skill-config/gate-battery.json` to any OTHER repo in this part — opting repos in is an operator decision (only claude-config is seeded, in part 1); (b) the skill must not promise queue admission or ETA — light ops bypass the queue by design (L2); wording that implies serialization invites a false mental model.
