---
kind: implementation-plan
feature_id: generalized-build-test-runner-skills
status: In-progress
created: 2026-07-13
phases: [0, 1]
complexity: complex
---

> **Mobile plan** — generated by `/write-plan` (manual orchestration) on 2026-07-13.
> To execute: `/execute-plan C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-1.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 4. Sibling parts:
> - part 1: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-1.md` (this file)
> - part 2: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-2.md`
> - part 3: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-3.md`
> - part 4: `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/plans/all-phases-generalized-build-test-runner-skills-part-4.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — generalized-build-test-runner-skills (v2) — Part 1: Phases 0–1 (Contract + battery runner)

**PHASES.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/PHASES.md`

**SPEC.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/features/generalized-build-test-runner-skills/SPEC.md`

**Total phases:** this part: Phases 0 + 1 (of 0–4)
**Plan version:** v2 (reference-based)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is claude-config — the gates are the Python invariant battery below, NOT `/msbuild`/`/mstest`/nx), this plan's own per-phase steps and the "Plan-specific execution notes" below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config overrides — READ FIRST)

1. **SEQUENCING GATE (SPEC L7 — check FIRST, before any edit):** this feature executes only after `lazy-core-package-decomposition` is Complete with a `COMPLETED.md` receipt (`grep -l "Complete" docs/features/lazy-core-package-decomposition/SPEC.md` + `ls docs/features/lazy-core-package-decomposition/COMPLETED.md`). If not yet complete, STOP — write `BLOCKED.md`? NO: do not write a sentinel; simply report "dep not yet complete, plan not executable" and end (the queue dep-gate owns this hold; a manual execution attempt before the dep completes is an operator sequencing error, not a blocker).
2. **Per-commit invariant battery** (run before EVERY commit in this part; all must exit 0; ZERO changes under `user/scripts/tests/baselines/`):
   ```bash
   cd C:/Users/Jacob/source/repos/claude-config
   python -m pytest user/scripts/ -q
   python3 user/scripts/lazy-state.py --test
   python3 user/scripts/bug-state.py --test
   python3 user/scripts/lazy_parity_audit.py --repo-root .
   python3 user/scripts/cli_surface_gen.py --check --repo-root .
   python3 user/scripts/doc-drift-lint.py --repo-root .
   python ~/.claude/scripts/lint-skills.py
   ```
   From WU-4 onward the battery MAY be run via the freshly-built runner (`python3 user/scripts/gate-battery.py --repo-root .` — trust its last-line banner); before that, run the commands directly.
3. **Turn-end gate (this feature's own subject matter — dogfood it):** Read `~/.claude/skills/_components/turn-end-gate.md` before the first battery run. NEVER end a turn (orchestrator or subagent) while a battery/test run is in flight; a launch echo is not an outcome.
4. **Cognito byte-untouched guard (SPEC L6) — per commit:** `git show --name-only --format= HEAD` must contain NOTHING under `repos/cognito-forms/` and NONE of `user/scripts/build-queue.ps1`, `user/scripts/build-queue-runner.ps1`, `user/scripts/build-queue-hygiene.ps1`, `user/scripts/build-queue-status.ps1`, `user/scripts/build-queue-await.ps1`, any `user/scripts/build-queue*.Tests.ps1`, `user/hooks/build-queue-enforce.sh`. Each WU below names its allowed file set.
5. **Commit cadence:** one commit per WU, directly on `main`. Conventional messages, e.g. `feat(generalized-runner): Phase 0 — runner-outcome contract component`.
6. **Contract-grammar single-sourcing:** the banner/await grammar strings are authored ONCE in WU-1's component; WU-2/WU-3 tests quote them verbatim from that file (cite the component path in test docstrings). If implementation forces a grammar change, change the COMPONENT in the same commit — never let the two drift.
7. **Provisional decisions in force (park-provisional — do NOT re-litigate):** L2 (light ops bypass the queue), L3 (deny heavy only) + D3-precision addendum, L4 (stdlib-Python runner, documented-grammar seam). See `docs/features/generalized-build-test-runner-skills/NEEDS_INPUT_PROVISIONAL.md`.
8. **Halts:** unresolvable red gate / genuine design fork → `BLOCKED.md` / `NEEDS_INPUT.md` per the execution contract.

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against. The executing session SHOULD `Read` these before starting any work unit whose Implementation goal references the corresponding upstream contract. Listed in dependency order.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| build-queue-generalization | hard | `C:/Users/Jacob/source/repos/claude-config/docs/features/build-queue-generalization/COMPLETED.md` | The banner/await/manifest reference contracts + the documented outstanding AlgoBooth exec-wrapper gap (no PHASES.md exists — receipt + SPEC are the record) |
| build-queue-generalization | hard | `C:/Users/Jacob/source/repos/claude-config/user/scripts/build-queue-await.ps1` | 124/125 await semantics mirrored by `--await` (grammar parity, zero shared code) |
| (context) | — | `C:/Users/Jacob/source/repos/claude-config/user/skills/_components/turn-end-gate.md` | Referenced (never copied) by the WU-1 contract component |
| (context) | — | `C:/Users/Jacob/source/repos/claude-config/docs/features/lazy-core-package-decomposition/plans/all-phases-lazy-core-package-decomposition-part-2.md` | Note 1 = the canonical 7-command battery list the WU-4 manifest seeds |
| (context) | — | `C:/Users/Jacob/repos/AlgoBooth/.claude/skill-config/quality-gates.md` | `QG_VERDICT:` grammar + never-pipe-through-tail rule the contract grandfathers |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | generalized-build-test-runner-skills | P0 | Runner-outcome contract component | — | Solo |
| 2 | generalized-build-test-runner-skills | P1 | Battery runner + manifest + pytest | P0 (grammar locked) | Solo — WUs share gate-battery.py + tests |

## Work Units

- [x] WU-1 — Author `runner-outcome-contract.md` + `user/scripts/CLAUDE.md` prose pointer (Phase 0)
- [x] WU-2 — TDD: `gate-battery.py` core — manifest, sequential gates, banner, results file
- [ ] WU-3 — TDD: `--await <run-id>` with 124/125 semantics
- [ ] WU-4 — Seed `gate-battery.json` + CLI-surface roster + CLAUDE.md row + dogfood run

---

### Phase: generalized-build-test-runner-skills P0 — Runner-outcome contract

**Goal:** the ONE documented contract (banner grammar + followable await + turn-end gate by reference + never-pipe-through-tail + seam statement) exists as a component; Phase 1+ instantiate it.
**Entry criteria:** none (first phase of this feature; the L7 sequencing gate in note 1 already passed).
**SPEC.md references:** D1 (the full contract content list), L1, D8 (path-discovery note), Executive Summary (the three legs).

#### WU-1 — Author `runner-outcome-contract.md` + `user/scripts/CLAUDE.md` prose pointer

- **Scope:** PHASES P0, all three deliverables.
- **TDD:** no (docs; the grammar is pinned by WU-2/WU-3 tests).
- **Files to create/modify:** `user/skills/_components/runner-outcome-contract.md` (new); `user/scripts/CLAUDE.md` (one prose paragraph, near the build-queue script rows, pointing at the component — NOT a script-table row, because `doc-drift-lint.py` maps script-table rows doc→disk onto `user/scripts/` files [VERIFY: `python3 user/scripts/doc-drift-lint.py --repo-root .`]).
- **Implementation goal:** the component specifies, surface-neutrally: (1) the authoritative LAST-stdout-line banner grammar `<runner>: <run-key> [op=<op>] RESULT=<PASS|FAIL|...> [counts] [(fidelity)] [-> next-action]` with its three conforming instances — `build-queue: seq=<N> op=<op> RESULT=... [tests=<T> failed=<F>] (result_fidelity=...) [-> next-action]` (existing — `Format-BuildQueueBanner` [VERIFY: `grep -n "function Format-BuildQueueBanner" user/scripts/build-queue-hygiene.ps1`]), `QG_VERDICT: PASS|FAIL (exit N)` (existing, grandfathered verbatim [VERIFY: `grep -n "QG_VERDICT" repos/algobooth/.claude/skill-config/quality-gates.md`]), and `gate-battery: run=<id> op=battery RESULT=<PASS|FAIL> cmds=<n> failed=<k> (elapsed=<s>s) [-> first failing gate: <id>]` (new, quoted from SPEC D1); (2) followable-await semantics — re-emit the same banner as the await's last line, exit = the run's own exit code, reserved exits 124 (result not yet present — NEVER success) / 125 (malformed result), mirroring `build-queue-await.ps1` [VERIFY: `grep -n "exit 124" user/scripts/build-queue-await.ps1`]; (3) the turn-end gate BY REFERENCE to `_components/turn-end-gate.md` [VERIFY: `ls user/skills/_components/turn-end-gate.md`] — one pointer sentence, ZERO copied gate text; (4) the never-pipe-through-tail rule (generalized from AlgoBooth's quality-gates.md); (5) the seam statement: the cross-repo seam is this documented grammar, not shared code — the PowerShell queue plane and the Python battery runner conform independently (SPEC D4); (6) the D8 note: AlgoBooth's non-standard path (`C:\Users\Jacob\repos\AlgoBooth`) is invisible to `~/source/repos/*` globs; enforcement/queue keying unaffected (payload-cwd git-toplevel); one-line `~/.claude/lazy-repos.json` pin recipe for fleet visibility (documented, not implemented). Then run `python user/scripts/project-skills.py` + `python user/scripts/lint-skills.py` (clean; no broken/circular includes).
- **Spec requirements:** SPEC D1 items 1–4 + the seam statement; L1; D8.
- **Batch:** 1.

---

### Phase: generalized-build-test-runner-skills P1 — Battery runner + manifest + pytest

**Goal:** `python3 user/scripts/gate-battery.py --repo-root .` runs the manifested battery and prints the authoritative banner as its last stdout line; `--await` re-acquires any run; a manifest-less repo is refused cleanly; all behavior pytest-pinned; cloud-compatible (zero PowerShell).
**Entry criteria:** Phase 0 complete (the contract grammar is the SSOT the tests quote).
**SPEC.md references:** D4 (runner language/seam), D5 (battery manifest SSOT), L4, L5, Validation Criteria rows 1–7, User Experience (the exact banner examples).

#### WU-2 — TDD: `gate-battery.py` core — manifest, sequential gates, banner, results file

- **Scope:** PHASES P1 deliverable 1 (runner core) + the runner half of the test deliverable.
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/gate-battery.py` (new), `user/scripts/tests/test_gate_battery.py` (new; the tests/ layout exists [VERIFY: `ls user/scripts/tests/`]).
- **Test files:** `user/scripts/tests/test_gate_battery.py`.
- **Test expectations (write RED first, against fixture manifests + fixture/tmp state dirs — never the real `~/.claude/state`):**
  - no manifest at the invoking repo's git toplevel → exit 2, one-line reason on stderr/stdout, ZERO files created under the state root (assert the tmp state root stays empty);
  - all-green fixture battery (e.g. two `python -c "pass"`-class gates) → last stdout line matches the exact `gate-battery:` grammar with `RESULT=PASS cmds=<n> failed=0`, process exit 0;
  - one forced-red gate → last line has `RESULT=FAIL`, `failed=1`, `-> first failing gate: <id>`, non-zero exit;
  - results file written at `<state-root>/gate-battery/<repo-key>/results/<run-id>.json` with per-gate `{id, exit_code, duration}` + the run's banner/exit; run-id shape `\d{8}-[0-9a-f]{4}`;
  - repo-key parity: the private key function reproduces `lazy_core.statedir.repo_key` output for a sample path [VERIFY: `grep -n "def repo_key" user/scripts/lazy_core/statedir.py`] (import lazy_core IN THE TEST ONLY — the runner itself stays stdlib-only per L4);
  - state-root unwritable (read-only dir fixture) → gates still run, banner still prints as last line, `-> next-action` notes await unavailable, exit reflects the gates;
  - cloud proxy: no `powershell`/`pwsh` token anywhere on the runner's execution path (source-scan assertion is acceptable for v1 — cite it as the mechanical proxy for the SPEC's cloud-compatibility row).
- **Implementation goal:** stdlib-only (`argparse`, `subprocess`, `json`, `pathlib`, `hashlib`, `time`, `os`, `sys`). Resolve the invoking repo's git toplevel (`git rev-parse --show-toplevel` from `--repo-root`, default cwd); load `.claude/skill-config/gate-battery.json` (`{version: 1, gates: [{id, cmd, cwd?}]}`; malformed manifest → exit 2 naming the defect, zero state). Run gates SEQUENTIALLY via `subprocess` with output streamed through to stdout/stderr; a gate failure does NOT stop the battery (all gates run; first failing id recorded). Compose the banner per the WU-1 contract grammar and print it as the LAST stdout line — guaranteed even on internal error paths (wrap in try/finally discipline; a crashed battery still emits `RESULT=FAIL` + next-action naming the crash). State root default `~/.claude/state/` with an env/flag override for tests (e.g. `--state-root` or `GATE_BATTERY_STATE_ROOT` — pick one, document in `--help`). Private `_repo_key()` copy of the SHA-1 normalized-realpath convention with a keep-in-sync comment citing `lazy_core/statedir.py` (same precedent as `phases-slice.py`'s private regex copy — the runner must not import the harness package per L4/D4).
- **Spec requirements:** D4, D5, L4, L5; Validation Criteria rows "Battery runs as one command", "Failure identification", "Manifest-less repo unaffected", "Cloud compatibility".
- **Batch:** 1.

#### WU-3 — TDD: `--await <run-id>` with 124/125 semantics

- **Scope:** PHASES P1 deliverable 2 (`--await`).
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/gate-battery.py`, `user/scripts/tests/test_gate_battery.py`.
- **Test files:** `user/scripts/tests/test_gate_battery.py`.
- **Test expectations:** `--await <run-id>` against a fixture state dir — (a) result absent (in-flight) → exit **124**, output contains NO success vocabulary (assert no `RESULT=PASS`); (b) result present → the run's banner re-emitted as the LAST stdout line, exit = the recorded run's own exit code (test both 0 and 1); (c) corrupted/truncated result JSON → exit **125**; (d) unknown repo key / missing results dir → 124 (not-yet, not an error class of its own — mirror `build-queue-await.ps1`'s not-present semantics).
- **Implementation goal:** `--await` reads `<state-root>/gate-battery/<repo-key>/results/<run-id>.json` (repo key from the SAME invoking-repo resolution as a run), optional bounded poll (`--timeout-seconds`, default single-shot or short poll — keep simpler than the PS awaiter; document). The 124/125 reservations and "an await timeout is NEVER success" language come verbatim from the WU-1 contract. Update the component ONLY if implementation forced a semantic (not editorial) adjustment — same commit (note 6).
- **Spec requirements:** D1 item 2; Validation Criteria rows "Followable await — in flight / done", "Malformed result".
- **Batch:** 2.

#### WU-4 — Seed `gate-battery.json` + CLI-surface roster + CLAUDE.md row + dogfood run

- **Scope:** PHASES P1 deliverables 3–5 + the dogfood MVB.
- **TDD:** no (config seed, registry regen, docs row — mechanical; behavior already pinned by WU-2/3).
- **Files to create/modify:** `.claude/skill-config/gate-battery.json` (new — this repo's seed), `user/scripts/cli_surface_gen.py` (one-line `ROSTER` addition [VERIFY: `grep -n "^ROSTER" user/scripts/cli_surface_gen.py`]), `docs/cli/cli-surface.json` (regenerated), `user/scripts/gate-battery.py` (adopt `DidYouMeanArgumentParser` + `--dump-cli-surface` from `cli_surface.py` [VERIFY: `ls user/scripts/cli_surface.py`]), `user/scripts/CLAUDE.md` (script-table row for `gate-battery.py`, same commit — doc-drift same-commit discipline).
- **Implementation goal:** seed the manifest with the 7-command battery AS COMMANDS (ids: `pytest`, `lazy-state-test`, `bug-state-test`, `parity-audit`, `cli-surface`, `doc-drift`, `lint-skills`; commands per Plan-specific note 2 — choose the interpreter spelling that works on BOTH Windows and cloud, `python3` preferred; the dogfood run is the proof). Register in the CLI-surface roster and regenerate `docs/cli/cli-surface.json` in the SAME commit so `cli_surface_gen.py --check` stays green. Then DOGFOOD: run `python3 user/scripts/gate-battery.py --repo-root .` — it must run all 7 gates and print `gate-battery: run=<id> op=battery RESULT=PASS cmds=7 failed=0 (elapsed=<s>s)` as its last line, exit 0 (this run IS this commit's invariant battery; record the banner line in PHASES Implementation Notes). Also exercise `--await <that run-id>` once live (banner re-emitted, exit 0) and record it.
- **Spec requirements:** L5; Validation Criteria row 1; PHASES P1 MVB.
- **Batch:** 3.

**Batch overview:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1, WU-2 | No — run sequentially (WU-2's tests quote WU-1's grammar) | none (different files) but ORDER matters |
| 2 | WU-3 | Solo | shares gate-battery.py + tests with WU-2 |
| 3 | WU-4 | Solo | shares gate-battery.py; adds manifest/registry/docs |

**Plan-specific risk notes:** (a) the last-line-banner guarantee must survive gate-output interleaving — flush stdout before printing the banner, and never print anything after it; (b) pytest gate recursion: the dogfood battery's `pytest user/scripts/` collects `test_gate_battery.py` itself — the tests MUST be hermetic (tmp state roots, fixture manifests) so the battery-inside-battery run cannot touch the outer run's state; (c) Windows paths: normalize the repo key exactly as `statedir.repo_key` does (forward slashes, lowercase drive letter) or `--await` will miss results written by a differently-spelled `--repo-root`.
