---
kind: realign-plan
feature_id: concurrent-worktree-agent-coordination
status: Ready
created: 2026-07-18
phases: ["all"]
upstream_phases_hashes:
  parallel-worktree-batch-execution: 7a6fd59f714fe6b4afa0605cd579e017dc155aff551ad1e963e1c08a16cc9586
  park-provisional-acceptance: 518e81fcf8f147fbfa0e6c2af9410fa52d2f75f00a3c4c73ff8d65486781f4c0
---

# Realign — concurrent-worktree-agent-coordination (2026-07-18)

> Read-only reality check against completed upstream dependencies. Generated by `/realign-spec`.
> No SPEC or PHASES files were modified by this analysis.

## Inputs

- **Downstream SPEC:** `docs/features/concurrent-worktree-agent-coordination/SPEC.md`
- **Downstream PHASES:** not authored yet (this feature is pre-`/spec-phases`; only SPEC-level drift assessed)
- **Upstream dependencies checked (kind=hard, Status=Complete):**
  - `parallel-worktree-batch-execution` — PHASES.md (+ 1 plan: `all-phases-parallel-worktree-batch-execution-part-1.md`; `realign-2026-07-04.md` skipped as a prior realign artifact)
  - `park-provisional-acceptance` — PHASES.md (no plans dir; single-session autonomous implementation, decisions captured inline in PHASES.md)

The four remaining declared deps are soft/composes and out of scope for reality-check
(`generalized-build-test-runner-skills` composes, `build-queue-generalization` soft,
`multi-repo-concurrent-runs` soft, `long-build-and-runtime-ownership` soft) — no design hinge on
their impl specifics.

## Aligned

Every concrete symbol/path/contract the downstream SPEC's reuse map and Locked Decisions assume
was confirmed present in the realized upstream code (not just the upstream PHASES prose).

- **`lazy_coord.py` global lock substrate** — SPEC assumes `os.mkdir` global lock (atomic on NTFS); confirmed at `user/scripts/lazy_coord.py:145` (`acquire_lock`'s `os.mkdir` lock).
- **Fencing-token leases** — SPEC assumes `leases.json` + `lease-token-watermarks.json` monotonic watermarks; confirmed (`leases.json` acquisition machinery + `_WATERMARKS_SUFFIX = "lease-token-watermarks.json"` at line 84).
- **`owner.json` stale-holder reclamation** — SPEC assumes `owner.json` `{pid, kernel_start_time, acquired_at}` reclaimed via `_confirmed_dead_owner` (pid gone OR kernel-start-time mismatch); confirmed at lines 149 (owner.json write) and 334 (`_confirmed_dead_owner`), exactly the two-condition predicate the SPEC names.
- **Lane machinery for merge-back (Locked Decision 3)** — SPEC assumes `lane/<item-id>` lanes, `merge_lane_branch` (abort-and-demote on conflict, lane branch preserved), `ledger_record_demotion` (`demoted: serial`), `lanes.json` audit ledger; all confirmed (`lane_branch` at 827, `claim_shardable` at 862, `ledger_record_demotion` at 1019, `merge_lane_branch` at 1096 returning `{merged, conflict, aborted}` with `no_ff=True`), plus `verify_fencing` (607) and `reclaim_expired` (623) for the zombie-lane fail-safe.
- **`provisional_eligibility` carve-out seam (Locked Decision 2 halt path)** — SPEC assumes the fail-closed predicate lives at `user/scripts/lazy_core/docmodel.py:2551` and the semantic-conflict halt is added "mirroring the existing Spike-FAIL / stub_origin exclusions"; confirmed EXACTLY: `provisional_eligibility` is at `docmodel.py:2551`, the Spike-FAIL carve-out is at lines 2594–2608 (`written_by: spike` / `spike_verdict: fail`), and the stub_origin carve-out is at lines 2609–2619 (fail-closed on any non-explicit-false value) — the precise two exclusions the SPEC's reuse map (line 47) names as the pattern to extend.

## Drift

### Minor

(none)

### Moderate

(none)

### Severe

(none)

## Recommended next step

- `inline-edit` — no drift detected; no patches required. The SPEC's upstream assumptions are
  fully confirmed against realized upstream code. The feature is clear to proceed to `/spec-phases`.

**Recommended:** `inline-edit`

**Why:** All concrete symbol/path/contract assumptions in the downstream SPEC's reuse map and four
Locked Decisions were confirmed present in the completed upstream code (`lazy_coord.py` lock/lease/
lane/merge plane; `provisional_eligibility`'s Spike-FAIL + stub_origin carve-out seam at the exact
cited line) — zero minor, moderate, or severe findings.
