---
kind: implementation-plan
feature_id: concurrent-worktree-agent-coordination
status: In-progress
complexity: complex
created: 2026-07-18
phases: [3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan {this-path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 3 of 6. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-3.md` (this file)
> - part 4: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-4.md`
> - part 5: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-5.md`
> - part 6: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-6.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — Concurrent Multi-Agent Worktree Coordination (Part 3: Phase 3 — FIFO file-lock)  (v2)

**PHASES.md files:**
- `docs/features/concurrent-worktree-agent-coordination/PHASES.md` (this part covers Phase 3)

**SPEC.md files:**
- `docs/features/concurrent-worktree-agent-coordination/SPEC.md`

**Total phases:** 6 (this part: Phase 3)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it (EXECUTION MODEL, COMPONENT LOADING PROTOCOL, MANDATORY RULES, Execution Protocol, Blocking Issue Protocol, Completion).

Where this plan's repo uses non-default gates (claude-config — Python/lint/test/Pester, NOT `/msbuild`/`/mstest`), the Plan-specific execution notes below override the contract's defaults.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| parallel-worktree-batch-execution | hard | `docs/features/parallel-worktree-batch-execution/plans/all-phases-parallel-worktree-batch-execution-part-1.md` | The `lazy_coord.py` global lock + fencing-lease + `_confirmed_dead_owner` reclamation the FIFO lock is built on. |
| generalized-build-test-runner-skills | composes | `docs/features/generalized-build-test-runner-skills/PHASES.md` | The runner-outcome two-implementation (PowerShell + stdlib-Python) documented-grammar pattern the lock conforms to. |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | concurrent-worktree-agent-coordination | P3 | Cross-platform FIFO file-lock (per-queue-item grain, two conforming impls) | Phase 1 | Batch 1 parallel; Batch 2 solo |

## Plan-specific execution notes

- **claude-config gates (override msbuild/mstest):** `python3 user/scripts/lazy_coord.py --test` (the 21-fixture concurrency-plane harness) + the PowerShell Pester suite for `concurrent-lock.ps1` (workstation) + `python3 ~/.claude/scripts/doc-drift-lint.py --repo-root .` (the script table must match disk after adding `concurrent-lock.ps1`). Full battery: `python3 user/scripts/gate-battery.py`.
- **HARD — `lazy_coord.py` MUST NOT import `lazy_core`** (documented separation; its atomic write is a justified duplication). Reuse `acquire_lock`/`release_lock`/`acquire_lease`/`verify_fencing`/`lane_branch` by `wi_id` keying — do NOT invent a new locking substrate. Time is injected (`now` epoch float) so `--test` reclamation is deterministic.
- **Two-implementation contract:** the PowerShell plane and the stdlib-Python plane conform INDEPENDENTLY to the ONE documented lock contract (no shared code — the runner-outcome pattern). Mirror the `build-queue.ps1` `active.lock` + confirmed-dead-reclaim precedent for the `.ps1` plane.

## Work Units

- [ ] WU-1 — Lock contract component (documented grammar)
- [ ] WU-2 — Stdlib-Python per-queue-item FIFO lock in lazy_coord.py
- [ ] WU-3 — PowerShell concurrent-lock.ps1 (workstation plane)
- [ ] WU-4 — Doc-table + component registration (root CLAUDE.md + user/scripts/CLAUDE.md)

### Phase: concurrent-worktree-agent-coordination P3 — Cross-platform FIFO file-lock

**Goal:** Agents detect write contention and coordinate via a per-queue-item FIFO lock (Locked Decision 1) built on `lazy_coord.py`, with two conforming implementations (PowerShell + stdlib-Python) per the runner-outcome two-implementation contract.
**Entry criteria:** Phase 1 complete (awareness prose references the lock as the contention arbiter).
**SPEC.md references:** Requirement 3 (FIFO file-lock); Locked Decision 1 (per-queue-item grain); Validation row 3.

#### WU-1 — Lock contract component
- **Scope:** PHASES P3 deliverable "Lock contract component `concurrent-lock-contract.md`".
- **TDD:** no (documented grammar — the sibling of `runner-outcome-contract.md`).
- **Files to create/modify:** `user/skills/_components/concurrent-lock-contract.md` **NET-NEW (create)** [VERIFY net-new: `test ! -e user/skills/_components/concurrent-lock-contract.md`]; authored in the style of `user/skills/_components/runner-outcome-contract.md` [VERIFY: `test -f user/skills/_components/runner-outcome-contract.md`].
- **Implementation goal:** Document the FIFO-lock grammar ONCE: acquire = wait-for-unlock FIFO on a per-queue-item key; fencing-token release; stale-holder reclaim via `_confirmed_dead_owner`; the authoritative acquire/timeout outcome. Name the two conforming implementations (Python `lazy_coord.py` + PowerShell `concurrent-lock.ps1`).
- **Spec requirements:** SPEC Requirement 3; the reuse-map "Runner-outcome two-implementation contract" row.
- **Batch:** 1.

#### WU-2 — Stdlib-Python per-queue-item FIFO lock
- **Scope:** PHASES P3 deliverable "Stdlib-Python FIFO-lock wiring in lazy_coord.py".
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/lazy_coord.py` [VERIFY: `grep -n "^def acquire_lock\|^def acquire_lease\|^def lane_branch\|^def verify_fencing" user/scripts/lazy_coord.py`].
- **Test files:** the in-file `--test` harness in `user/scripts/lazy_coord.py` [VERIFY: `grep -n "def test_\|--test" user/scripts/lazy_coord.py | head`].
- **Test expectations:** with injected `now` — (a) two agents contending on the SAME item key serialize (the second waits for unlock, then proceeds in turn); (b) two agents on DIFFERENT item keys never block each other; (c) a CONFIRMED-dead holder is reclaimed and the waiter proceeds; (d) a genuinely live/ambiguous holder still times out within the existing backoff budget (no false reclaim).
- **Implementation goal:** Add a per-queue-item FIFO acquire built on `acquire_lock` + `acquire_lease`/`verify_fencing`, keyed by `wi_id` (reusing the lease keying verbatim — per-item grain, Locked Decision 1), waiting on the item's lease to unlock before proceeding, with the existing confirmed-dead reclamation. No new locking substrate. Register the new `--test` fixtures.
- **Spec requirements:** SPEC Requirement 3; Locked Decision 1.
- **Batch:** 1.

#### WU-3 — PowerShell concurrent-lock.ps1
- **Scope:** PHASES P3 deliverable "PowerShell workstation implementation `concurrent-lock.ps1` conforming to the same contract".
- **TDD:** yes (Pester).
- **Files to create/modify:** `user/scripts/concurrent-lock.ps1` **NET-NEW (create)** [VERIFY net-new: `test ! -e user/scripts/concurrent-lock.ps1`], modeled on `user/scripts/build-queue.ps1` [VERIFY: `test -f user/scripts/build-queue.ps1`].
- **Test files:** `user/scripts/concurrent-lock.Tests.ps1` **NET-NEW (create)** (Pester), following the `build-queue-await.Tests.ps1` precedent [VERIFY: `test -f user/scripts/build-queue-await.Tests.ps1`].
- **Test expectations:** Pester — FIFO acquire order on the same item key; a timeout returns the authoritative outcome (never a false success); a confirmed-dead holder (`active.lock`-style, `Test-ShouldReclaimLock` precedent) is reclaimed; different item keys never block.
- **Implementation goal:** Implement the workstation FIFO lock conforming to WU-1's contract — `active.lock` + per-item key + confirmed-dead reclaim + an authoritative acquire/timeout outcome line, mirroring `build-queue.ps1`. Silently inert off-Windows is acceptable (workstation-only plane).
- **Spec requirements:** SPEC Requirement 3 (two-implementation, PowerShell workstation plane).
- **Batch:** 1.

#### WU-4 — Doc-table + component registration
- **Scope:** PHASES P3 deliverable "script tables in root CLAUDE.md + user/scripts/CLAUDE.md updated (doc-drift-lint requires the script table to match disk)".
- **TDD:** no (docs; verified by doc-drift-lint).
- **Files to create/modify:** `CLAUDE.md` [VERIFY: `grep -n "build-queue-await.ps1" CLAUDE.md`], `user/scripts/CLAUDE.md` [VERIFY: `grep -n "Files in this directory" user/scripts/CLAUDE.md`].
- **Implementation goal:** Add a script-table row for `concurrent-lock.ps1` (+ the component reference for `concurrent-lock-contract.md`) so `doc-drift-lint.py` (which cross-checks the script table against `user/scripts/` on disk) stays exit 0.
- **Spec requirements:** repo doc-consistency (doc-drift-linter contract).
- **Batch:** 2 (edits the two CLAUDE.md docs, disjoint from Batch 1; sequenced after the scripts exist so the rows describe real files).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1, WU-2, WU-3 | Yes (component md vs lazy_coord.py vs .ps1 — disjoint) | None |
| 2 | WU-4 | Solo | N/A |

**Post-phase gate:** `python3 user/scripts/lazy_coord.py --test` exit 0 (same-item-serialize + different-item-no-block fixtures green); the Pester suite passes on the workstation; `python3 ~/.claude/scripts/doc-drift-lint.py --repo-root .` exit 0. The Runtime Verification row (two real concurrent processes on the same lock proceed in FIFO order) is owned by the Step-9 gate / manual workstation testing — do NOT tick it here. Then tick Deliverables + WU checkboxes, set `status: Complete`, commit + push to main.
