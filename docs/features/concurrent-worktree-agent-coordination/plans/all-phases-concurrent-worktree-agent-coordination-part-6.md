---
kind: implementation-plan
feature_id: concurrent-worktree-agent-coordination
status: In-progress
complexity: complex
created: 2026-07-18
phases: [6]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan {this-path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 6 of 6. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-3.md`
> - part 4: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-4.md`
> - part 5: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-5.md`
> - part 6: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-6.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — Concurrent Multi-Agent Worktree Coordination (Part 6: Phase 6 — Retire self_edit_mode foreground-await)  (v2)

**PHASES.md files:**
- `docs/features/concurrent-worktree-agent-coordination/PHASES.md` (this part covers Phase 6)

**SPEC.md files:**
- `docs/features/concurrent-worktree-agent-coordination/SPEC.md`

**Total phases:** 6 (this part: Phase 6)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it (EXECUTION MODEL, MANDATORY RULES, Execution Protocol, Blocking Issue Protocol, Completion).

Where this plan's repo uses non-default gates (claude-config — lint/doc-drift/parity), the Plan-specific execution notes below override the contract's defaults.

## References — Upstream Artifacts

(none — this part depends only on this feature's own Phases 2–5, which are in earlier parts.)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | concurrent-worktree-agent-coordination | P6 | Retire the orchestrator self_edit_mode foreground-await coupling (R7) | Phases 2–5 (the coordination layer it now trusts) | Batch 1 solo; Batch 2 solo |

## Plan-specific execution notes

- **claude-config gates:** `python3 ~/.claude/scripts/project-skills.py && python3 ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities && python3 ~/.claude/scripts/doc-drift-lint.py --repo-root . && python3 user/scripts/lazy_parity_audit.py --repo-root .` (all exit 0). Full battery: `python3 user/scripts/gate-battery.py`.
- **HARD — retire ONLY the foreground/await coupling, NOT the governing-file reload.** The `self_edit_mode → force foreground/await` serialization (lazy-batch/SKILL.md:899 Concurrency EXCEPTION; lazy-bug-batch:709; lazy-batch-cloud) is REMOVED. The SEPARATE governing-file RELOAD discipline (self-edit C8, lazy-batch:666) is RETAINED UNCHANGED — a self-edit commit still staleifies the in-context copy. Confirm both facts with the final grep.
- **Coupled-pair discipline:** `/lazy-batch` ↔ `/lazy-bug-batch` ↔ `/lazy-batch-cloud` are coupled twins — mirror the retirement across all three and diff each against its canonical after editing. Run `python3 user/scripts/lazy_parity_audit.py --repo-root .` (exit 0) after.
- **Final phase:** when this part's work lands, set the top-level PHASES.md `**Status:**` to `In-progress` (implementation done, validation pending) — NOT Complete. The `__mark_complete__` gate owns the flip to Complete + COMPLETED.md after the validation tail.

## Work Units

- [ ] WU-1 — Retire foreground-await coupling in lazy-batch (keep governing-file reload)
- [ ] WU-2 — Mirror the retirement in lazy-bug-batch + lazy-batch-cloud

### Phase: concurrent-worktree-agent-coordination P6 — Retire self_edit_mode foreground-await

**Goal:** The orchestrator relies on the coordination layer (Phases 2–5) to resolve write conflicts instead of pre-serializing dispatches — retire the `self_edit_mode → foreground/await` coupling across `/lazy-batch` and its coupled twins, keeping the governing-file reload discipline.
**Entry criteria:** Phases 2–5 complete (the FIFO lock + conflict-routing + merge-back the orchestrator now trusts MUST exist before the defensiveness is retired).
**SPEC.md references:** Requirement 7 (Orchestrator parallel-dispatch trust); Validation row 6.

#### WU-1 — Retire foreground-await coupling in lazy-batch
- **Scope:** PHASES P6 deliverable "lazy-batch §1d.1 Concurrency EXCEPTION: the self_edit_mode → force foreground/await serialization is REMOVED and replaced with coordination-layer-trusted concurrent dispatch prose".
- **TDD:** no (prose).
- **Files to create/modify:** `user/skills/lazy-batch/SKILL.md` [VERIFY: `grep -n "self_edit_mode.*foreground\|force.*foreground/await" user/skills/lazy-batch/SKILL.md`].
- **Implementation goal:** Remove the `self_edit_mode → force foreground/await` serialization from the Concurrency EXCEPTION (~line 899) and replace it with prose that the FIFO lock + conflict-routing own conflict correctness (background hardens and cycle work run concurrently on the shared tree; "no monsters-in-the-closet serialization"). **RETAIN the governing-file RELOAD discipline (self-edit C8, ~line 666) UNCHANGED** — only the pre-serialization is retired.
- **Spec requirements:** SPEC Requirement 7.
- **Batch:** 1.

#### WU-2 — Mirror the retirement in lazy-bug-batch + lazy-batch-cloud
- **Scope:** PHASES P6 deliverables "lazy-bug-batch (@709) mirror" + "lazy-batch-cloud mirror".
- **TDD:** no (prose; coupled twins).
- **Files to create/modify:** `user/skills/lazy-bug-batch/SKILL.md` [VERIFY: `grep -n "self_edit_mode.*foreground\|EXCEPTION.*self_edit_mode" user/skills/lazy-bug-batch/SKILL.md`], `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` [VERIFY: `grep -n "self_edit_mode" repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md`].
- **Implementation goal:** Mirror WU-1's retirement in both coupled twins (keep each twin's governing-file reload discipline). Diff each against its canonical after editing (Coupled Skill Pairs rule).
- **Spec requirements:** SPEC Requirement 7 (coupled twins).
- **Batch:** 2 (sequenced after WU-1 so the twins mirror the settled canonical wording; disjoint files but ordered for wording parity).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | N/A |
| 2 | WU-2 | Solo | N/A |

**Post-phase gate:** the claude-config gates above exit 0; a grep confirms the `self_edit_mode → foreground/await` coupling is GONE from all three skills AND the governing-file reload discipline is still PRESENT. Then tick Deliverables + WU checkboxes, set the plan-part `status: Complete`, set the top-level PHASES.md `**Status:**` to `In-progress`, commit + push to main. **Completion (gate-owned):** the `__mark_complete__` gate flips SPEC.md **Status:** to Complete and writes COMPLETED.md after the validation tail — never authored as a checkbox here.
