---
kind: implementation-plan
feature_id: concurrent-worktree-agent-coordination
status: Complete
complexity: mechanical
created: 2026-07-18
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan {this-path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 6. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-1.md` (this file)
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-3.md`
> - part 4: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-4.md`
> - part 5: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-5.md`
> - part 6: `C:\Users\Jacob\source\repos\claude-config\docs\features\concurrent-worktree-agent-coordination\plans\all-phases-concurrent-worktree-agent-coordination-part-6.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — Concurrent Multi-Agent Worktree Coordination (Part 1: Phase 1 — Awareness)  (v2)

**PHASES.md files:**
- `docs/features/concurrent-worktree-agent-coordination/PHASES.md` (concurrent-worktree-agent-coordination, 6 phases — this part covers Phase 1)

**SPEC.md files:**
- `docs/features/concurrent-worktree-agent-coordination/SPEC.md`

**Total phases:** 6 (this part: Phase 1)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (claude-config is a harness-config repo — its gates are Python/lint/projection, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the Plan-specific execution notes below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| parallel-worktree-batch-execution | hard | `docs/features/parallel-worktree-batch-execution/PHASES.md` | The concurrency plane this feature reuses (Phases 3/5); Phase 1 documents the trust contract for it. |
| park-provisional-acceptance | hard | `docs/features/park-provisional-acceptance/PHASES.md` | The fail-closed provisional predicate the later semantic-conflict carve-out extends. |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | concurrent-worktree-agent-coordination | P1 | Awareness injection + reconcile "One writer per file" | — | Batch 1 parallel (file-disjoint); Batch 2 solo |

## Plan-specific execution notes

- **claude-config gates (override the contract's msbuild/mstest defaults):** the quality gate for this part is `python3 ~/.claude/scripts/project-skills.py && python3 ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities && python3 ~/.claude/scripts/doc-drift-lint.py --repo-root .` (all exit 0). The full 7-command invariant battery is `python3 user/scripts/gate-battery.py` (trust its last-line `RESULT=` banner). No Tauri/MCP runtime.
- **Mechanical part (Sonnet):** every WU here is pure skill/component/CLAUDE.md prose editing (no logic). Tagged `complexity: mechanical`. The awareness note is a canonical string chosen in WU-1 and reused verbatim in WU-2/3/4.
- **Coupled-pair note:** WU-4 edits AlgoBooth mirrors; keep them consistent with the user-level note. WU-2/WU-4 touch the `/lazy-batch` family — a pure additive awareness note, not a state-machine change, so no parity-manifest edit is owed, but run `python3 user/scripts/lazy_parity_audit.py --repo-root .` after to confirm exit 0.

## Work Units

- [x] WU-1 — Awareness note in cycle-base-prompt.md
- [x] WU-2 — Awareness note in lazy-batch + lazy-batch-parallel HARD CONSTRAINTS + dispatch policy
- [x] WU-3 — Reconcile user/CLAUDE.md "One writer per file"
- [x] WU-4 — Awareness note in AlgoBooth mirrors (lazy-batch-cloud + lazy-cloud)

### Phase: concurrent-worktree-agent-coordination P1 — Awareness injection + reconcile "One writer per file"

**Goal:** Bake the concurrent-writer awareness note into every agent-context surface and reconcile the single-writer `<orchestration>` block, establishing the trust contract Phase 6 relies on.
**Entry criteria:** None (first phase).
**SPEC.md references:** Requirement 1 (Awareness — injection points); User Experience "Awareness in every dispatch/context"; Requirement 7's trust-contract documentation.

#### WU-1 — Awareness note in cycle-base-prompt.md
- **Scope:** PHASES P1 deliverable "Awareness note added to cycle-base-prompt.md".
- **TDD:** no (prose).
- **Files to create/modify:** `user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` [VERIFY: `test -f user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`].
- **Implementation goal:** Near the branch/commit contract (the R5 chain region), add a concise canonical awareness note: OTHER agents may be committing to this same worktree/branch concurrently from parallel sessions — an unexpected commit / moved HEAD is EXPECTED, not a defect to panic on; genuine contention is resolved by the coordination layer (git-safety + FIFO lock + conflict-routing), not by halting. Choose the canonical phrase here (reused verbatim downstream).
- **Spec requirements:** SPEC Requirement 1.
- **Batch:** 1.

#### WU-2 — Awareness note in lazy-batch + lazy-batch-parallel HARD CONSTRAINTS + dispatch policy
- **Scope:** PHASES P1 deliverables "HARD CONSTRAINTS of lazy-batch and lazy-batch-parallel" + "sub-subagent dispatch policy prose".
- **TDD:** no (prose).
- **Files to create/modify:** `user/skills/lazy-batch/SKILL.md` [VERIFY: `grep -n "HARD CONSTRAINT" user/skills/lazy-batch/SKILL.md`], `user/skills/lazy-batch-parallel/SKILL.md` [VERIFY: `grep -n "HARD CONSTRAINT" user/skills/lazy-batch-parallel/SKILL.md`].
- **Implementation goal:** Add the canonical awareness note (WU-1's phrase) to the HARD CONSTRAINTS block of each skill and to the sub-subagent dispatch policy prose these skills carry. Purely additive — do NOT alter any existing constraint.
- **Spec requirements:** SPEC Requirement 1.
- **Batch:** 1.

#### WU-3 — Reconcile user/CLAUDE.md "One writer per file"
- **Scope:** PHASES P1 deliverable "user/CLAUDE.md `<orchestration>` reconciled".
- **TDD:** no (prose).
- **Files to create/modify:** `user/CLAUDE.md` [VERIFY: `grep -n "One writer per file" user/CLAUDE.md`].
- **Implementation goal:** In the `<orchestration>` block, keep the single-writer default for a run's OWN dispatched agents, but explicitly carve out sanctioned CONCURRENT writers on a shared worktree/branch (a second interactive/scheduled session) and point at the coordination layer as the arbiter — not panic. Reconcile the "concurrent writers silently clobber each other" wording so it no longer contradicts sanctioned concurrency.
- **Spec requirements:** SPEC Requirement 1 (the "One writer per file block ... must be reconciled with sanctioned concurrent writers").
- **Batch:** 1.

#### WU-4 — Awareness note in AlgoBooth mirrors
- **Scope:** PHASES P1 deliverable "AlgoBooth mirrors updated".
- **TDD:** no (prose).
- **Files to create/modify:** `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` [VERIFY: `test -f repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md`], `repos/algobooth/.claude/skills/lazy-cloud/SKILL.md` [VERIFY: `test -f repos/algobooth/.claude/skills/lazy-cloud/SKILL.md`].
- **Implementation goal:** Mirror the canonical awareness note into both AlgoBooth cloud skills (they consume the same contracts). Keep wording consistent with the user-level note.
- **Spec requirements:** SPEC Requirement 1 + Cross-repo scope.
- **Batch:** 2 (touches no file claimed by Batch 1; kept solo for a clean review boundary on the cross-repo mirror).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1, WU-2, WU-3 | Yes (disjoint files) | None |
| 2 | WU-4 | Solo | N/A |

**Post-phase gate:** `python3 ~/.claude/scripts/project-skills.py && python3 ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities && python3 ~/.claude/scripts/doc-drift-lint.py --repo-root . && python3 user/scripts/lazy_parity_audit.py --repo-root .` all exit 0; a `grep` confirms the canonical awareness phrase resolves in every file above. Then tick the phase's Deliverables + this part's WU checkboxes, set the plan-part frontmatter `status: Complete`, commit + push to main.
