---
kind: implementation-plan
feature_id: mobile-queue-control
status: Ready
created: 2026-06-22
phases: [1, 2, 3]
complexity: complex
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-22.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\features\mobile-queue-control\plans\all-phases-mobile-queue-control.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — mobile-queue-control (Lazy Queue Status Doc, GitHub-Mobile Readable)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\mobile-queue-control\PHASES.md` (mobile-queue-control, 3 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\mobile-queue-control\SPEC.md`

**Total phases:** 3
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **EXECUTION-ENVIRONMENT OVERRIDE (read if dispatched as a `/lazy-batch` cycle subagent):** if your dispatch briefing says you do NOT have the `Agent` tool, perform ALL work units INLINE with `Read`/`Edit`/`Write` instead of dispatching subagents. The TDD discipline (failing test first, then implementation) and the review/quality-gate steps still apply — you simply play both roles in one context. This override exists because a cycle subagent cannot recursively fan out.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against.

(none — this plan has no completed hard upstream dependencies; the SPEC's `**Depends on:** (none)`. The `pipeline_visualizer` package is an in-repo PEER reuse anchor, not a hard dep — its reuse points are cited inline in the work units with `[VERIFY: …]` anchors.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. EXCEPTIONS: trivial PASS-WITH-FIXES items (a few lines), AND the cycle-subagent inline override in the EXECUTION MODEL block above.
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents
3. Every TDD work unit goes through the test-first pipeline — failing tests written first, then implementation makes them pass
4. PHASES.md is updated AFTER EACH batch completes (not deferred)
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing
6. Mistakes are fixed immediately before launching the next batch
7. After all batches in a phase finish, integration verification confirms all changes work together
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it
9. Each completed phase is committed and pushed before the next phase begins
10. Cross-feature phases may run in parallel when dependencies are satisfied and no file conflicts exist
11. This plan is self-contained — follow it exactly as written without relying on external context
12. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory

## Touchpoint Audit Table (grounded — verified 2026-06-22)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `user/scripts/lazy-queue-doc.py` | **NO (net-new)** | — | create | The generator. Sibling to `lazy-state.py`. Imports the `pipeline_visualizer` package via the `_SCRIPTS_DIR`-on-`sys.path` pattern [VERIFY: grep -n "_SCRIPTS_DIR" user/scripts/pipeline_visualizer/__main__.py] |
| `user/scripts/pipeline_visualizer/probe.py` | yes | `probe_state(repo_root)→{features,bugs,leases,roadmap,server_time}` [VERIFY: grep -n "def probe_state" user/scripts/pipeline_visualizer/probe.py] | reuse | Generator consumes `probe_state()`'s aggregate dict directly — do NOT re-shell the state scripts |
| `user/scripts/pipeline_visualizer/curated_stage.py` | yes | `curated_stage(current_step,terminal_reason,pipeline)` [VERIFY: grep -n "def curated_stage" user/scripts/pipeline_visualizer/curated_stage.py] | reuse | The doc's `state` column = `curated_stage` verbatim |
| `user/scripts/lazy-state.py` (`--marker-present`) | yes | CLI flag, exit 0/1 [VERIFY: grep -n '"--marker-present"' user/scripts/lazy-state.py] | reuse | Run-active/idle freshness token; read-only, repo-scoped |
| `user/scripts/test_lazy_queue_doc.py` | **NO (net-new)** | — | create | Pytest sibling. Template: `test_pipeline_visualizer.py` [VERIFY: ls user/scripts/test_pipeline_visualizer.py] |
| `LAZY_QUEUE.md` (repo root) | **NO (net-new, generated)** | — | create | The generator's output artifact |
| `user/skills/lazy-batch/SKILL.md` | yes | per-cycle commit prose | refactor (Phase 3 wiring) | Document the generator invocation at the existing per-cycle `git add -A` commit step (prose only; state machine untouched) |

## Work Units

- [ ] WU-1 — Generator skeleton + state read (probe_state reuse, arg surface, byte-stable doc body)
- [ ] WU-2 — Renderer: Features/Bugs tables + SPEC links + "Needs attention" triage + freshness header
- [ ] WU-3 — Phase 1 pytest suite (state-fidelity, byte-stability, triage)
- [ ] WU-4 — Drill-in: inline curated summary (status · phase N/M · next · exec-summary) + phase-progress reader
- [ ] WU-5 — SPEC.md link + absolute-URL fallback option
- [ ] WU-6 — Phase 2 pytest extension (phase N/M, exec-summary, fallback form)
- [ ] WU-7 — Phase 3 pipeline-wiring doc edit + byte-stable no-op acceptance test

## Execution Schedule

| Step | Feature | Phase(s) | Title | Blocked by | Parallel? |
|------|---------|----------|-------|------------|-----------|
| 1 | mobile-queue-control | P1 | Pure-read generator → `LAZY_QUEUE.md` | — | Solo (sequential batches) |
| 2 | mobile-queue-control | P2 | Drill-in rendering | P1 | Solo (sequential batches) |
| 3 | mobile-queue-control | P3 | Pipeline-integrated trigger | P1, P2 | Solo |

---

### Phase: mobile-queue-control P1 — Pure-read generator → `LAZY_QUEUE.md`

**Goal:** A stdlib Python generator that reads the repo's lazy state via `probe_state()` and emits a byte-stable per-repo grouped `LAZY_QUEUE.md` with Features/Bugs tables, a "Needs attention" triage section, and a freshness header.
**Entry criteria:** None (first phase).
**SPEC.md references:** Executive Summary; Locked Decisions 1, 2, 6; Technical Design (pure-read generator); Implementation Phases → Phase 1; Validation Criteria rows 1, 2, 4, 6.

#### WU-1 — Generator skeleton + state read

- **Scope:** PHASES.md Phase 1 deliverables: `user/scripts/lazy-queue-doc.py` net-new; argument surface (`--repo-root` default cwd, `--stdout`); the `probe_state` import + call; the byte-stable doc-body design choice (no embedded wall-clock — derive freshness from git commit time, per PHASES Integration Note recommendation (a)).
- **TDD:** yes (the byte-stability + state-read behavior is testable; WU-3 holds the tests, but author the module so WU-3's tests can target it — co-author the public entrypoint signature here).
- **Files to create/modify:** `user/scripts/lazy-queue-doc.py` (create).
- **Implementation goal:** Module exposes a pure `render_doc(state: dict, repo_root) -> str` (takes the `probe_state()` aggregate, returns the full markdown — injectable so tests pass a fixture dict without shelling), plus a `main(argv)` CLI that calls `probe_state(repo_root)` [VERIFY: grep -n "def probe_state" user/scripts/pipeline_visualizer/probe.py] and writes/prints. Import the `pipeline_visualizer` package using the `_SCRIPTS_DIR`-on-`sys.path` pattern [VERIFY: grep -n "_SCRIPTS_DIR" user/scripts/pipeline_visualizer/__main__.py]. **Byte-stable rule:** the rendered string MUST be a pure function of the on-disk state — NO `datetime.now()` / wall-clock embedded in the doc body (so two renders of unchanged state are byte-identical). The "updated" freshness comes from git commit time at read (GitHub mobile shows it natively); the doc body carries only the run-active/idle marker, which is a function of state.
- **Spec requirements:** SPEC Technical Design "pure function of doc state … never re-implements inference and never mutates queue.json"; Validation row 2 "Byte-stable when unchanged".
- **Batch:** 1.

#### WU-2 — Renderer: tables, links, triage, header

- **Scope:** PHASES.md Phase 1 deliverables: Features table (`# | item | state | tier`), Bugs table (`# | item | state | sev`), grouped `## Features (N)` / `## Bugs (M)` with live counts; each item name a markdown link to its SPEC.md; "Needs attention" triage section; freshness header with run-active/idle marker.
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/lazy-queue-doc.py` (extend `render_doc`).
- **Implementation goal:** For each item in `state["features"]`/`state["bugs"]`, emit a table row: reorder index (1-based queue position), item name as `[name](<rel-spec-path>)`, the `state` cell = `curated_stage(item["current_step"], item["terminal_reason"], pipeline)` [VERIFY: grep -n "def curated_stage" user/scripts/pipeline_visualizer/curated_stage.py] (NOTE: `probe_state` already attaches `item["curated_stage"]` — reuse that attached value rather than re-calling), and the tier/severity from `item["queue_meta"]`. Resolve the SPEC link target from `item["spec_path"]` when present, else `docs/{features,bugs}/<spec_dir or id>/SPEC.md` (mirror `probe._item_dir`'s resolution logic [VERIFY: grep -n "_item_dir" user/scripts/pipeline_visualizer/probe.py]), expressed RELATIVE to repo root. "Needs attention": one bullet per item whose `curated_stage` ∈ {Blocked, Needs-input}; OMIT the whole section (header included) when none (byte-stability — no empty header). Freshness header: `# Lazy Queue — <repo-name>   (run active 🔒 | idle)` where the token is from `lazy-state.py --marker-present --repo-root <root>` [VERIFY: grep -n '"--marker-present"' user/scripts/lazy-state.py] (exit 0 → active, exit 1 → idle) — shell it in `main`, pass the boolean into `render_doc` so `render_doc` stays pure/testable.
- **Spec requirements:** SPEC "The document (per repo)" layout block; Decision 2 (per-repo grouped); Validation row 1 (state fidelity), row 4 (triage), row 6 (freshness marker).
- **Batch:** 1 (same file as WU-1 — WU-1 and WU-2 co-create `lazy-queue-doc.py`; they are tightly coupled single-file co-creation, so run as ONE subagent assignment OR sequentially WU-1 then WU-2; do NOT parallelize on the same file).

> **File-overlap note:** WU-1 and WU-2 both build `lazy-queue-doc.py`. Treat them as a single coupled assignment (one subagent writes the module end-to-end: skeleton + renderer) OR run WU-1 then WU-2 sequentially in the same batch with the second resuming the file. Never two concurrent writers on this file.

#### WU-3 — Phase 1 pytest suite

- **Scope:** PHASES.md Phase 1 "Tests" deliverable: `user/scripts/test_lazy_queue_doc.py` net-new.
- **TDD:** yes (this IS the test WU — write these FIRST, before WU-1/WU-2 implementation, so they are RED against the absent module, then GREEN once WU-1/WU-2 land).
- **Files to create/modify:** `user/scripts/test_lazy_queue_doc.py` (create).
- **Test files:** `user/scripts/test_lazy_queue_doc.py`.
- **Test expectations:** Mirror `test_pipeline_visualizer.py` structure [VERIFY: ls user/scripts/test_pipeline_visualizer.py]. (1) `render_doc` against a fixture state dict (two features, one Blocked) emits a Features table whose rows match the fixture items + their `curated_stage` cells; (2) `render_doc(state)` called twice on the same fixture returns byte-identical strings (byte-stability — Validation row 2); (3) a fixture with a Blocked / Needs-input item surfaces it under "Needs attention", a clean fixture omits the section (Validation row 4); (4) each item-name link resolves to `docs/{features,bugs}/<spec_dir>/SPEC.md`. Build fixture state dicts directly (the `probe_state` shape: `{features:[{feature_id,current_step,terminal_reason,curated_stage,queue_meta,spec_path,...}], bugs:[...], ...}`) — do NOT shell the real scripts in unit tests (keep them hermetic; an optional integration test MAY shell `lazy-queue-doc.py --repo-root <this repo> --stdout` and assert it parses).
- **Spec requirements:** Validation rows 1, 2, 4, 6.
- **Batch:** runs FIRST within Phase 1 (test-first) — but since WU-1/WU-2 co-author the module, the practical order is: write WU-3 tests (RED) → implement WU-1+WU-2 (GREEN). Document the RED-for-the-right-reason (import/attribute error against absent `render_doc`, not a passing tautology).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-3 (tests, RED first), then WU-1+WU-2 (module, GREEN) | No (WU-1/WU-2 share `lazy-queue-doc.py`; tests precede impl) | WU-1↔WU-2 share the module file (coupled co-creation) |

---

### Phase: mobile-queue-control P2 — Drill-in rendering

**Goal:** Each item row gains an inline curated summary (status · phase N/M · next action · one-line exec summary) and a SPEC.md link, with an absolute-URL fallback for GitHub-mobile relative-link risk.
**Entry criteria:** Phase 1 complete (generator + resolved SPEC paths + byte-stability approach exist).
**SPEC.md references:** Locked Decisions 3, 7; User Experience "Per-item drill-in"; Validation row 3; Open Questions (GitHub-mobile relative-link behavior).

#### WU-4 — Inline curated summary + phase-progress reader

- **Scope:** PHASES.md Phase 2 deliverables: extend the renderer with an inline curated-summary line per item (`status · phase N/M · next: <action> · <exec summary>`); a phase-progress helper returning `(checked, total)` from an item's PHASES.md.
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/lazy-queue-doc.py` (extend); `user/scripts/test_lazy_queue_doc.py` (extend — but WU-6 owns the Phase-2 test additions; co-author the helper so WU-6 can target it).
- **Implementation goal:** `status`/`next-action` derive from `curated_stage` + `current_step` (display-only mapping; do NOT re-infer pipeline state). `phase N/M`: a helper `phase_progress(phases_md_path) -> (checked, total)` that counts `### Phase` headings vs. phases with all deliverables checked — REUSE an existing `lazy_core` phase/deliverable counter if a public one exists [VERIFY: grep -nE "def (remaining_unchecked|count_|.*phase.*deliverable)" user/scripts/lazy_core.py]; only add a new display-only helper if none is reusable. Omit the `phase N/M` token when the item has no PHASES.md. One-line exec summary = the item's SPEC.md lead blockquote / `## Executive Summary` first sentence (read the SPEC file the link points at; a missing/short SPEC yields an empty token, NEVER an exception — keep the read cheap + failure-tolerant for Phase 3's per-cycle runs).
- **Spec requirements:** Decision 7 (status, phase N/M, next action, one-line exec summary inline); Validation row 1 faithfulness.
- **Batch:** 2.

#### WU-5 — SPEC link + absolute-URL fallback

- **Scope:** PHASES.md Phase 2 deliverable: the item-name SPEC.md link (relative, from Phase 1) plus a generator option/constant for the absolute-URL fallback form.
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/lazy-queue-doc.py` (extend — same file as WU-4).
- **Implementation goal:** Default link form is relative (Decision 7 / Open Question default). Add a `--link-mode {relative,absolute}` option (default `relative`); in `absolute` mode emit `github.com/<owner>/<repo>/blob/<branch>/docs/.../SPEC.md`, deriving `<owner>/<repo>` from `git remote get-url origin` and `<branch>` from the marker work-branch [VERIFY: grep -n '"--marker-work-branch"' user/scripts/lazy-state.py] (default `main`). The point is that switching is a one-line config flip if the GitHub-mobile relative-link check (Phase 2 Runtime Verification) fails — both forms implemented now.
- **Spec requirements:** Open Questions (relative-link behavior + absolute fallback); SPEC Research References (the one deferred empirical check).
- **Batch:** 2 — but shares `lazy-queue-doc.py` with WU-4. Run WU-4 then WU-5 sequentially (or as one coupled assignment); never concurrent writers on the module.

#### WU-6 — Phase 2 pytest extension

- **Scope:** PHASES.md Phase 2 "Tests" deliverable: extend `test_lazy_queue_doc.py` with phase-progress, exec-summary, and fallback-form assertions.
- **TDD:** yes (write RED before WU-4/WU-5 land).
- **Files to create/modify:** `user/scripts/test_lazy_queue_doc.py` (extend).
- **Test files:** `user/scripts/test_lazy_queue_doc.py`.
- **Test expectations:** (1) a fixture item with a 3-phase PHASES.md (1 phase fully checked) renders `phase 1/3`; (2) the exec-summary line equals the fixture SPEC's lead-blockquote first sentence; (3) `--link-mode absolute` emits the `github.com/.../blob/...` form, `relative` (default) emits the `docs/.../SPEC.md` form; (4) an item with no PHASES.md omits the `phase N/M` token without error.
- **Spec requirements:** Decision 7; Validation row 3.
- **Batch:** 2 (test file is separate from the module — but author tests RED-first, then WU-4/WU-5 GREEN).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 2 | WU-6 (tests RED), then WU-4+WU-5 (module GREEN) | No (WU-4/WU-5 share the module) | WU-4↔WU-5 share `lazy-queue-doc.py` |

---

### Phase: mobile-queue-control P3 — Pipeline-integrated trigger

**Goal:** Wire the generator to run at each lazy cycle boundary so `LAZY_QUEUE.md` rides the cycle's existing commit on `main`, with a byte-stable no-op when state is unchanged.
**Entry criteria:** Phase 1 + Phase 2 complete (full byte-stable generator).
**SPEC.md references:** Locked Decision 6; Technical Design (trigger + publish); Implementation Phases → Phase 3; Validation rows 2, 5, 6.

#### WU-7 — Pipeline-wiring doc edit + byte-stable no-op acceptance test

- **Scope:** PHASES.md Phase 3 deliverables: wire the generator invocation into the orchestrator's per-cycle commit step (claude-config side); the byte-stable-no-op acceptance test. (AlgoBooth-side wiring is a cross-repo follow-up — see Integration note below; it is NOT authored from this repo's tree.)
- **TDD:** partial — the no-op-commit byte-stability is a unit test; the git-staging wiring is prose in a SKILL/commit-policy doc (verified by the real workstation cycle, a Runtime Verification row, not a unit test).
- **Files to create/modify:** `user/skills/lazy-batch/SKILL.md` (document the generator invocation at the existing per-cycle `git add -A` commit step — prose only; NO change to `lazy-state.py`/`bug-state.py` state machine); the claude-config commit cadence prose (if `.claude/skill-config/commit-policy.md` exists, note it there; else the `/lazy-batch` commit step); `user/scripts/test_lazy_queue_doc.py` (no-op acceptance test).
- **Implementation goal:** Document that, at the per-cycle commit point where `git add -A` already stages residue [VERIFY: grep -n "git add -A" user/scripts/lazy_core.py], the orchestrator runs `python user/scripts/lazy-queue-doc.py --repo-root <repo>` so the regenerated `LAZY_QUEUE.md` is staged by the existing `git add -A` and rides the cycle's commit on `main`. The generator is orchestrator-INVOKED, never called from the state-script compute path (preserves "pure read, never writes during a probe"). The no-op acceptance test: `render_doc(state)` twice with no state change → byte-identical (re-asserts WU-3's byte-stability as the Phase-3 gate).
- **Spec requirements:** Decision 6 (pipeline-integrated, claude-config + AlgoBooth, main-based); Validation row 5 (stays current) + row 2 (byte-stable no-op).
- **Batch:** 3.

> **Cross-repo Integration note (one-writer / repo boundary):** this repo's tree cannot edit AlgoBooth's repo-scoped `/lazy-batch-cloud`. The generator is fully `--repo-root`-addressable and proven against this repo, so AlgoBooth adoption is a one-line invocation add in that repo's cycle. If AlgoBooth wiring must be authored as code, spin it off as a follow-up item and reverse-reference it in PHASES.md Implementation Notes — but the default disposition is to document the one-liner for the operator (no separate feature needed). Do NOT block this feature on the AlgoBooth edit.

> **Completion (gate-owned):** the `__mark_complete__` gate flips SPEC.md **Status:** to Complete and writes COMPLETED.md once Phase 3's runtime verification passes. This plan never authors a status-flip / receipt / ROADMAP / archive checkbox.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 3 | WU-7 | Solo | None |

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** Identify phase(s) whose entry criteria are satisfied (prerequisite phases complete — all deliverables checked off in PHASES.md). Phases here are strictly sequential (P1 → P2 → P3). If no phase is ready, jump to Blocking Issue Protocol.
2. **Announce:** Print "Implementing mobile-queue-control Phase N: [title]"
3. **Review prior context:** Re-read previously completed phases' Implementation Notes in PHASES.md. They take priority over the plan where they diverge (especially the Phase 1 byte-stability design choice, which Phases 2+3 depend on).
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "mobile-queue-control Phase N: [title] — committed as [hash]"
7. **Loop:** Return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly. It defines: task tool loading via ToolSearch, task creation for all work units, and the update protocol.

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow it. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and this plan file. Do NOT rely on cached content.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — ZERO INLINE IMPLEMENTATION)

**PRE-FLIGHT CHECK:** Confirm (1) you will use `Agent` with `model: "sonnet"` for ALL code changes, (2) you will NOT call `Edit`/`Write` on source/test files. If either is false, re-read the EXECUTION MODEL section. **(Cycle-subagent inline override: if you lack the `Agent` tool, do all WUs inline — see EXECUTION MODEL block.)**

Read ALL of these before proceeding:
1. `~/.claude/skills/_components/tdd-protocol.md`
2. `~/.claude/skills/_components/subagent-launch.md`
3. `~/.claude/skills/_components/tdd-test-agent.md`
4. `~/.claude/skills/_components/implementation-agent.md`

**TDD ORDERING for this plan:** within each phase batch, the test WU (WU-3 / WU-6) is authored FIRST (RED against the absent module/helper, for the right reason — import/attribute error, not a tautology), THEN the module WUs (WU-1+WU-2 / WU-4+WU-5) make them GREEN. Because the module WUs co-create one file, treat each phase's module work as a single coupled assignment (one writer on `lazy-queue-doc.py`).

**POST-DISPATCH GATE:** After subagents complete, verify you composed `Agent` calls and did NOT edit source/test files directly (unless under the cycle-subagent inline override). 

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` and follow its complete protocol. Also read `~/.claude/skills/_components/mount-site-verification.md` (new-file checks — `lazy-queue-doc.py` and `test_lazy_queue_doc.py` are new files; confirm the generator is invokable via `python user/scripts/lazy-queue-doc.py --help` and the test is collected by pytest). Verdict: PASS / PASS-WITH-FIXES / NEEDS-REWORK.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md` and follow it. Check off completed deliverables; add an Implementation Notes block with date, work completed, integration notes, pitfalls, files modified. **Tick the matching `- [ ] WU-N` rows in this plan's Work Units checklist as each lands.** Do NOT tick the `<!-- verification-only -->` Runtime Verification rows (those are Step-9 owned).

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/quality-gates.md` and follow it. For this repo (no `.claude/skill-config/quality-gates.md` is assumed — check; if absent, the gates are): `python -m pytest user/scripts/test_lazy_queue_doc.py` (the new suite, 100% pass) AND `python ~/.claude/scripts/lint-skills.py` if any SKILL.md was edited (Phase 3). Run `python user/scripts/lazy-state.py --test` ONLY if you touched `lazy-state.py` (you should not — the generator is additive). 100% pass required before proceeding.

#### Step B.4.5: MCP Integration Test (BLOCKING — if applicable)

N/A for this feature — claude-config has no MCP runtime (PHASES.md header: **MCP runtime: not-required**). Skip with a note. The runtime-observable checks are the PHASES.md Runtime Verification rows, owned by manual workstation observation / the pytest suite, not an MCP gate.

#### Step B.5: Commit Batch

Read the commit policy: first try `.claude/skill-config/commit-policy.md` in the project root; if absent, read `~/.claude/skills/_components/commit-and-push.md`. This repo (`claude-config`) commits + pushes to `main` per the standard pattern. Follow whichever policy is on disk.

#### Step B.6: Proceed to Next Batch

**Checklist (all must be true):**
- [ ] Review report produced with a verdict
- [ ] PHASES.md updated; matching `- [ ] WU-N` rows ticked
- [ ] All quality gates pass
- [ ] Step B.5 committed (per policy)

If any item is unchecked, complete it before the next batch.

### Propagation Awareness Note

The generator imports the `pipeline_visualizer` package (an import dependency). If `probe_state`'s aggregate shape is missing a field the doc needs, EXTEND `probe.py` with a `test_pipeline_visualizer.py` test in the SAME batch (do NOT fork the shell logic into the generator). No widely-constructed struct/field additions otherwise.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md` and follow it. For this feature: confirm `python user/scripts/lazy-queue-doc.py --repo-root . --stdout` runs end-to-end against THIS repo and emits a doc whose `mobile-queue-control` feature row's state matches `python user/scripts/lazy-state.py --repo-root . --feature-id mobile-queue-control` JSON rolled through `curated_stage` (the full-stack read-render path on real state).

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md` and follow it. Candidates: `user/scripts/CLAUDE.md` (add a row for `lazy-queue-doc.py` in the scripts table — it is a new sibling to `lazy-state.py`); the root `CLAUDE.md` Scripts table. Add concise entries describing the generator as a pure-read `LAZY_QUEUE.md` renderer over `probe_state()`.

#### Commit and Push Post-Phase Changes

Read the commit policy (as in Step B.5) and follow it.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point during execution:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed phases** that haven't been committed yet.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** mobile-queue-control Phase N: [title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining phases (not attempted):**
   - [list]

4. **Do not attempt to work around the blocker.** The user provides a resolution and triggers autonomous implementation after.

Blocking issues include:
- A subagent failure that can't be fixed after 2 retry attempts
- A quality-gate failure that can't be fixed after 2 retry attempts
- A git push conflict that can't be resolved by rebase
- `probe_state()` cannot be imported (the `pipeline_visualizer` package moved/renamed) — a real contradiction of the touchpoint audit
- Any error that would require architectural decisions beyond the scope of the specs

---

## Completion

When all phases in the Execution Schedule are complete:

1. **Run the full quality-gate suite one final time:** `python -m pytest user/scripts/test_lazy_queue_doc.py` (+ `lint-skills.py` if SKILL.md was edited) — all green.
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Features implemented:** mobile-queue-control
   **Total phases completed:** 3
   **Total commits:** M
   **Final quality-gate status:** all green

   **Commit log:**
   | Commit | Feature | Phase | Title |
   |--------|---------|-------|-------|
   | … | mobile-queue-control | P1 | Pure-read generator |
   | … | mobile-queue-control | P2 | Drill-in rendering |
   | … | mobile-queue-control | P3 | Pipeline-integrated trigger |

   **Implementation Notes summary:**
   [byte-stability design choice (no embedded wall-clock); `probe_state()` reuse; the relative-vs-absolute link default; the AlgoBooth cross-repo wiring disposition]

Do NOT flip SPEC.md / PHASES.md top-level `**Status:**` to Complete and do NOT write COMPLETED.md — those are owned by the `__mark_complete__` gate (runs after `/mcp-test`, which for this feature is a no-MCP skip → validated-from-skip). When the last phase's implementation lands, set the top-level PHASES `**Status:**` to `In-progress` (validation pending) and let the state machine route forward.
