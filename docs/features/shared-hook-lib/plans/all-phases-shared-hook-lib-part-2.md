---
kind: implementation-plan
feature_id: shared-hook-lib
status: In-progress
created: 2026-07-18
complexity: complex
phases: [3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\plans\all-phases-shared-hook-lib-part-2.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 3. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\plans\all-phases-shared-hook-lib-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\plans\all-phases-shared-hook-lib-part-2.md` (this file)
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\plans\all-phases-shared-hook-lib-part-3.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — shared-hook-lib (Phase 3)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\PHASES.md` (shared-hook-lib, 4 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\SPEC.md`

**Total phases:** 1 (this part) of 4
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL, the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol, the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is `claude-config`, a harness-config repo whose gates are Python test suites, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes

- **HARD SEQUENCING (SPEC D3):** migrate ONE hook per work unit, and run the FULL `python user/scripts/test_hooks.py` suite after EACH single-hook migration. The pipe-tests assert deny/allow output byte-identically — that IS the no-behavior-change proof, and running the full suite between each migration attributes any regression to exactly one hook. Do NOT batch two migrations together; each WU is its own batch/review gate.
- **Migration order = lowest-blast-radius first (SPEC D3):** WU-1 `block-noncanonical-blocker-write.sh` → WU-2 `block-sentinel-write-on-stray-branch.sh` → WU-3 `long-build-ownership-guard.sh` → WU-4 `build-queue-enforce.sh` → WU-5 `lazy-cycle-containment.sh` (highest blast radius, last).
- **Fail-OPEN is sacred.** Each migrated hook keeps ONLY a minimal `except ImportError: sys.exit(0)` fallback (a few lines) — the rich per-hook `_append_hook_event` inline fallback branch is gone (it lives in `hook_lib` now). A missing prelude / failed `import hook_lib` must still allow + leave a prelude-side trace.
- **Anchor collapse (SPEC D3):** WU-3/WU-4/WU-5 delete their inline `_ENV_PREFIX`/`_CMD_START` definitions and import `hook_lib.ENV_PREFIX`/`CMD_START`. The matcher-semantics now live in ONE place — coordinate with the archived `long-build-and-build-queue-matcher-bypasses` bug (its anchor fix lands once here). The segment-anchoring pipe-tests already in the suite are the regression surface for this.
- **Gate:** `python user/scripts/test_hooks.py` after each WU; `python3 ~/.claude/scripts/lint-skills.py` + `python3 ~/.claude/scripts/gate-battery.py --repo-root .` before commit (trust the banner's last line).
- **No dev runtime / no MCP** (`**MCP runtime:** not-required`).

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies.)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | shared-hook-lib | P3 | Migrate the five inline-Python enforcement hooks (one per WU, D3 order) | part 1 (prelude + `hook_lib`) | Sequential (one hook per batch) |

## Work Units

- [x] WU-1 — Migrate `block-noncanonical-blocker-write.sh`
- [x] WU-2 — Migrate `block-sentinel-write-on-stray-branch.sh`
- [x] WU-3 — Migrate `long-build-ownership-guard.sh` (+ collapse anchors)
- [x] WU-4 — Migrate `build-queue-enforce.sh` (+ collapse anchors)
- [ ] WU-5 — Migrate `lazy-cycle-containment.sh` (+ collapse anchors, + import-failure pipe-test)

---

### Phase: shared-hook-lib P3 — Migrate the five inline-Python enforcement hooks

**Goal:** Migrate each of the five inline-Python enforcement hooks onto the Phase-1 prelude (`source` + `HOOK_PYTHON`/`HOOK_SCRIPTS_DIR`) and the Phase-2 `hook_lib` (`import hook_lib` seeded from `HOOK_SCRIPTS_DIR`), collapsing the duplicated emitters/appender/breadcrumb and the `_ENV_PREFIX`/`_CMD_START` triplication, with deny/allow output byte-identical after each single-hook migration.
**Entry criteria:** part 1 complete — `hook-prelude.sh` exists and is proven fail-open; `hook_lib.py` exists with emitters, appender, breadcrumb, `ENV_PREFIX`/`CMD_START`, and is import-light.
**SPEC.md references:** D3 (hook-by-hook migration, full suite per step, no behavior change), D2 (minimal inline ImportError fallback retained), Validation Criteria rows "No behavior change per migration" / "`hook_lib` import failure fails open" / "Duplication actually removed".

#### WU-1 — Migrate `block-noncanonical-blocker-write.sh` (#1, lowest blast radius)
- **Scope:** PHASES P3 deliverable "Migrate `user/hooks/block-noncanonical-blocker-write.sh`".
- **TDD:** yes (existing pipe-tests must stay byte-identical).
- **Files to create/modify:** `user/hooks/block-noncanonical-blocker-write.sh` (refactor [VERIFY: ls user/hooks/block-noncanonical-blocker-write.sh]).
- **Test files:** `user/scripts/test_hooks.py` (run the full existing suite; no new test in this WU).
- **Test expectations:** the full `test_hooks.py` suite passes with this hook's deny/allow stdout byte-identical to the pre-migration baseline (the `BLOCKED*`-basename deny + the `_SENTINEL_SCOPE_RE` scoping both unchanged).
- **Implementation goal:** Source `hook-prelude.sh` fail-open-guarded; `import hook_lib` seeded from `HOOK_SCRIPTS_DIR` [VERIFY: grep -rn "hook_lib" user/scripts/hook_lib.py]; replace the inline `_allow`/`_deny`/`_append_hook_event`/`_breadcrumb` copies with `hook_lib.allow`/`deny`/`append_hook_event`/`breadcrumb`; retain only a minimal `except ImportError: sys.exit(0)` fallback.
- **Spec requirements:** SPEC D3, D2.
- **Batch:** 1 (solo).

#### WU-2 — Migrate `block-sentinel-write-on-stray-branch.sh` (#2)
- **Scope:** PHASES P3 deliverable "Migrate `user/hooks/block-sentinel-write-on-stray-branch.sh`".
- **TDD:** yes.
- **Files to create/modify:** `user/hooks/block-sentinel-write-on-stray-branch.sh` (refactor [VERIFY: ls user/hooks/block-sentinel-write-on-stray-branch.sh]).
- **Test files:** `user/scripts/test_hooks.py` (full suite).
- **Test expectations:** full suite passes; this hook's stray-branch deny (HEAD ≠ marker `work_branch`) and its fail-open paths byte-identical.
- **Implementation goal:** Same migration shape as WU-1 for this hook (prelude source + `hook_lib` import + minimal ImportError fallback). Uses `lazy-state.py --marker-work-branch` unchanged (Python owns branch identity; do not re-derive in bash).
- **Spec requirements:** SPEC D3, D2.
- **Batch:** 2 (after WU-1 — the full-suite gate between each is the isolation mechanism).

#### WU-3 — Migrate `long-build-ownership-guard.sh` (#3, + collapse anchors)
- **Scope:** PHASES P3 deliverable "Migrate `user/hooks/long-build-ownership-guard.sh`; collapse `_ENV_PREFIX`/`_CMD_START` (lines 153/161) into `hook_lib.ENV_PREFIX`/`CMD_START`".
- **TDD:** yes.
- **Files to create/modify:** `user/hooks/long-build-ownership-guard.sh` (refactor [VERIFY: grep -n "_CMD_START" user/hooks/long-build-ownership-guard.sh]).
- **Test files:** `user/scripts/test_hooks.py` (full suite — the segment-anchoring pipe-tests are the anchor-collapse regression surface).
- **Test expectations:** full suite passes; the `LONG-BUILD-OWNERSHIP-TAKEOVER` deny signature, the exact-long-build matching (`tauri build`/`cargo build --release`/`npm run build`), the never-match cases (`ls`/`cat`/`npm run lint`/`cargo check --release`), AND the segment-start anchoring behavior all byte-identical.
- **Implementation goal:** Migration shape as WU-1 PLUS delete the inline `_ENV_PREFIX`/`_CMD_START` definitions and consume `hook_lib.ENV_PREFIX`/`CMD_START` [VERIFY: grep -n "ENV_PREFIX\|CMD_START" user/scripts/hook_lib.py]. Preserve the PS-syntax regex audit and COMMAND_TOOL_NAMES tool-name gate exactly.
- **Spec requirements:** SPEC D3 (anchor triplication collapse), D2.
- **Batch:** 3.

#### WU-4 — Migrate `build-queue-enforce.sh` (#4, + collapse anchors)
- **Scope:** PHASES P3 deliverable "Migrate `user/hooks/build-queue-enforce.sh`; collapse anchors (lines 211/217)".
- **TDD:** yes.
- **Files to create/modify:** `user/hooks/build-queue-enforce.sh` (refactor [VERIFY: grep -n "_CMD_START" user/hooks/build-queue-enforce.sh]).
- **Test files:** `user/scripts/test_hooks.py` (full suite).
- **Test expectations:** full suite passes; the manifest-scoped deny set, the Cognito legacy-remote fallback, the segment-start "build token begins a command segment" anchoring, the `BUILD_QUEUE_BYPASS=1` override recognition (bash + PowerShell forms), and the off-Windows inert path all byte-identical.
- **Implementation goal:** Migration shape as WU-3 (prelude + `hook_lib` + anchor collapse + minimal ImportError fallback) for `build-queue-enforce.sh`.
- **Spec requirements:** SPEC D3, D2.
- **Batch:** 4.

#### WU-5 — Migrate `lazy-cycle-containment.sh` (#5, highest blast radius, + anchors + import-failure pipe-test)
- **Scope:** PHASES P3 deliverables "Migrate `user/hooks/lazy-cycle-containment.sh`; collapse anchors (lines 263/269)" + "the D2 import-failure pipe-test".
- **TDD:** yes.
- **Files to create/modify:** `user/hooks/lazy-cycle-containment.sh` (refactor [VERIFY: grep -n "_CMD_START" user/hooks/lazy-cycle-containment.sh]).
- **Test files:** `user/scripts/test_hooks.py` (full suite + add the import-failure pipe-test).
- **Test expectations:** full suite passes; the marker-gated containment deny-set (routing/lifecycle/2nd-commit ops, Skill-tool `/lazy*` deny), the `_LIFECYCLE_INVOKE_RE` anchoring, and the no-python fail-open breadcrumb all byte-identical. **New pipe-test:** with `HOOK_SCRIPTS_DIR` pointed at an empty dir so `import hook_lib` fails, the hook still allows (`exit 0`) AND a prelude-side trace line is written to `hook-events.jsonl`.
- **Implementation goal:** Migration shape as WU-3/WU-4 for `lazy-cycle-containment.sh`, PLUS author the D2 import-failure pipe-test (fail-open + traced allow) covering at least this migrated hook.
- **Spec requirements:** SPEC D3, D2 (`hook_lib` import failure fails open + leaves a trace).
- **Batch:** 5.

**Batch overview — Phase 3:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | N/A |
| 2 | WU-2 | Solo | N/A (full-suite gate between each migration — D3 mandate) |
| 3 | WU-3 | Solo | N/A |
| 4 | WU-4 | Solo | N/A |
| 5 | WU-5 | Solo | N/A |

---

## Completion

Execution protocol, per-batch review, quality gates, and blocking-issue protocol are single-sourced in `~/.claude/skills/_components/execution-contract.md` (read it before executing). This part completes when WU-1..WU-5 are landed, `python user/scripts/test_hooks.py` passes after each migration (and finally full-green), a post-migration `grep` confirms `_append_hook_event`/`_breadcrumb`/the anchor pair each exist ONCE (in `hook_lib.py`), and the plan-part frontmatter `status:` is flipped to `Complete`. Then proceed to part 3. Do NOT flip SPEC.md/PHASES.md top-level `**Status:**` or write any completion receipt.
