---
kind: implementation-plan
feature_id: shared-hook-lib
status: Ready
created: 2026-07-18
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\plans\all-phases-shared-hook-lib-part-1.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 3. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\plans\all-phases-shared-hook-lib-part-1.md` (this file)
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\plans\all-phases-shared-hook-lib-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\plans\all-phases-shared-hook-lib-part-3.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — shared-hook-lib (Phases 1–2)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\PHASES.md` (shared-hook-lib, 4 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\SPEC.md`

**Total phases:** 2 (this part) of 4
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is `claude-config`, a harness-config repo whose gates are Python test suites + linters, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes

- **Quality gates for this repo (run these, NOT `/msbuild`/`/mstest`):**
  - Phase 1 gate: `python user/scripts/test_hooks.py` (the pipe-test suite — the byte-identical deny/allow proof).
  - Phase 2 gate: `python user/scripts/test_hook_lib.py` (new unit suite incl. the import-light guard).
  - Repo-wide sanity (run before commit): `python3 ~/.claude/scripts/lint-skills.py` and the invariant battery via `python3 ~/.claude/scripts/gate-battery.py --repo-root .` (trust its last-line `RESULT=` banner; do NOT grep gate output).
- **No dev runtime / no MCP.** `**MCP runtime:** not-required` — there is no Tauri/MCP server to boot and no `/mcp-test` step. All verification is the in-session Python test suites above.
- **Fail-OPEN is the sacred invariant.** Every new path (missing prelude, no python, `import hook_lib` failure) MUST allow (`exit 0`) and leave a trace. A guard that fails CLOSED is a worse bug than the gap it guards. The pipe-tests assert this.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies; SPEC `**Depends on:** (none)`.)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | shared-hook-lib | P1 | `hook-prelude.sh` + no-python fallback (batch 1: prelude; batch 2: wire the two wrappers) | — | Batched |
| 2 | shared-hook-lib | P2 | `hook_lib.py` | P1 (HOOK_SCRIPTS_DIR seed) | Solo |

## Work Units

- [ ] WU-1 — Author `hook-prelude.sh` + its fail-open pipe-tests
- [ ] WU-2 — Wire `lazy-dispatch-guard.sh` onto the prelude
- [ ] WU-3 — Wire `lazy-route-inject.sh` onto the prelude
- [ ] WU-4 — Author `hook_lib.py` + `test_hook_lib.py` (import-light)

---

### Phase: shared-hook-lib P1 — `hook-prelude.sh` + pure-bash no-python fallback

**Goal:** Create a sourced, fail-open-guarded bash prelude (`HOOK_PYTHON`, `HOOK_SCRIPTS_DIR`, pure-bash `hook_emit_error_event`) and wire it into the two thin prelude-only wrapper hooks with byte-identical deny/allow behavior.
**Entry criteria:** None (first phase).
**SPEC.md references:** D1 (library shape — sourced bash prelude), D2 (fail-open source-site guard), Technical Design diagram, Validation Criteria rows "Missing prelude fails open" / "No-python path leaves a trace". Resolves Open Question 1 (integer `date +%s` `ts` is float-compatible for `incident-scan.py`).

#### WU-1 — Author `hook-prelude.sh` + its fail-open pipe-tests
- **Scope:** PHASES P1 deliverable "`user/hooks/hook-prelude.sh`" + its two dedicated pipe-tests (missing-prelude-allows; no-python-leaves-event-line).
- **TDD:** yes (pipe-tests drive the fail-open contract).
- **Files to create/modify:** `user/hooks/hook-prelude.sh` (net-new, create).
- **Test files:** `user/scripts/test_hooks.py` (extend — add the two prelude pipe-tests, reusing the existing `_run_bash(script, stdin_text, env)` harness [VERIFY: grep -n "def _run_bash" user/scripts/test_hooks.py]).
- **Test expectations:** (a) a hook that sources a renamed-away prelude via `. "$…/hook-prelude.sh" 2>/dev/null || exit 0` exits 0 with empty stdout (allow); (b) a run with a stripped `PATH` (no python) writes exactly one JSON-parseable line to `hook-events.jsonl` under `LAZY_STATE_DIR` with `kind:"error"` and a numeric `ts`, and still exits 0.
- **Implementation goal:** Author the prelude to provide: `HOOK_PYTHON` (resolve via `command -v python3` then `command -v python`; on total absence, call the pure-bash breadcrumb writer then `exit 0`), `HOOK_SCRIPTS_DIR` (SELF-normalized backslash→slash scripts-dir derivation, builtins-only — no external commands), and `hook_emit_error_event(hook, signature, detail)` (a printf/`date +%s`-only best-effort append of one `{"ts":<int>,"kind":"error",...}` line to `hook-events.jsonl` in `claude_state_dir()`'s residency — honoring `LAZY_STATE_DIR` when set — plus a single-line `hook-error.json` overwrite). Every function best-effort/fail-open; the prelude is sourced, never executed. Lands the `guard-fail-open-leaves-no-trace` §1 fix (no-python path leaves a trace).
- **Spec requirements:** SPEC D1 (`HOOK_PYTHON`/`HOOK_SCRIPTS_DIR`/`hook_emit_error_event()`), D2 (source-site `2>/dev/null || exit 0`), Technical Design; Open Question 1 (integer-second `ts`).
- **Batch:** 1.

#### WU-2 — Wire `lazy-dispatch-guard.sh` onto the prelude
- **Scope:** PHASES P1 deliverable "`user/hooks/lazy-dispatch-guard.sh` — source the prelude; replace inline python-resolution + SCRIPT_DIR with `HOOK_PYTHON`/`HOOK_SCRIPTS_DIR`".
- **TDD:** yes (existing dispatch-guard pipe-tests must stay byte-identical).
- **Files to create/modify:** `user/hooks/lazy-dispatch-guard.sh` (refactor — replace its inline python-resolution + SELF/SCRIPT_DIR derivation with a fail-open `source` of `hook-prelude.sh` and use of `HOOK_PYTHON`/`HOOK_SCRIPTS_DIR` [VERIFY: ls user/hooks/lazy-dispatch-guard.sh]).
- **Test files:** `user/scripts/test_hooks.py` (the existing dispatch-guard pipe-tests — assert unchanged deny/allow output; add none unless a gap is found).
- **Test expectations:** every existing `lazy-dispatch-guard` pipe-test passes with byte-identical stdout (allow/deny JSON). The marker-absent fast-path allow and the registered-prompt validation both unchanged.
- **Implementation goal:** Source the prelude fail-open-guarded as the FIRST real line; delete the hook's own python-resolution + SCRIPT_DIR lines; consume `HOOK_PYTHON` / `HOOK_SCRIPTS_DIR`. No behavior change — the pipe-test suite is the proof.
- **Spec requirements:** SPEC D1 (first consumer), D3 (thin wrapper migration).
- **Batch:** 2 (depends on WU-1's prelude existing; file-disjoint from WU-3).

#### WU-3 — Wire `lazy-route-inject.sh` onto the prelude
- **Scope:** PHASES P1 deliverable "`user/hooks/lazy-route-inject.sh` — same prelude wiring; behavior byte-identical".
- **TDD:** yes (existing route-inject pipe-tests must stay byte-identical).
- **Files to create/modify:** `user/hooks/lazy-route-inject.sh` (refactor — same prelude wiring [VERIFY: ls user/hooks/lazy-route-inject.sh]).
- **Test files:** `user/scripts/test_hooks.py` (existing route-inject pipe-tests — assert unchanged).
- **Test expectations:** every existing `lazy-route-inject` pipe-test passes with byte-identical stdout; the marker-absent fast path and banner-injection behavior unchanged.
- **Implementation goal:** Same as WU-2 for `lazy-route-inject.sh`: source the prelude, delete inline python-resolution + SCRIPT_DIR, consume `HOOK_PYTHON`/`HOOK_SCRIPTS_DIR`, byte-identical output.
- **Spec requirements:** SPEC D1 (second consumer), D3.
- **Batch:** 2 (depends on WU-1; file-disjoint from WU-2 — the two run in one batch).

**Batch overview — Phase 1:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | N/A (creates the prelude the others source) |
| 2 | WU-2, WU-3 | Yes | None (each edits its own hook file; both extend but do not conflict on `test_hooks.py` — coordinate any new shared test into WU-1's batch) |

---

### Phase: shared-hook-lib P2 — `hook_lib.py`

**Goal:** Author the imported python substrate (`allow`/`deny`, `append_hook_event`, `breadcrumb`, `ENV_PREFIX`/`CMD_START`) with import-light discipline (stdlib only at module top; `lazy_core` imported lazily), plus its unit suite including the import-light guard.
**Entry criteria:** Phase 1 complete — `HOOK_SCRIPTS_DIR` derivation established (the `sys.path` seed the Phase-3 hooks pass to `import hook_lib`).
**SPEC.md references:** D1 (`hook_lib.py` residency + surface), D2 (one inline fallback retained; import must not read stdin), D4 (import-light — `lazy_core` deferred lazily), Validation Criteria row "Import-light lib".

#### WU-4 — Author `hook_lib.py` + `test_hook_lib.py` (import-light)
- **Scope:** PHASES P2 deliverables "`user/scripts/hook_lib.py`" + "`user/scripts/test_hook_lib.py`".
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/hook_lib.py` (net-new, create — sits beside the `lazy_core/` package so the hooks' existing `*_SCRIPTS_DIR` threading resolves it [VERIFY: ls user/scripts/lazy_core/__init__.py]).
- **Test files:** `user/scripts/test_hook_lib.py` (net-new).
- **Test expectations:** (a) `allow()` / `deny(reason)` emit the exact JSON deny/allow shape the hooks currently produce (byte-shape asserted so Phase 3 migrations stay identical); (b) `append_hook_event(...)` writes the JSONL entry and returns False (never raises) on a write error; (c) `breadcrumb(hook, err)` chains into an `append_hook_event("error", …)`; (d) `ENV_PREFIX`/`CMD_START` are present module constants; (e) **import-light guard** — a fresh subprocess that runs `import hook_lib` asserts `"lazy_core" not in sys.modules` afterward.
- **Implementation goal:** Provide `allow()`, `deny(reason)` (JSON emitters), `append_hook_event(kind, hook, signature, detail, repo_root=None)` that lazily `import lazy_core` INSIDE the function and delegates to `lazy_core.append_hook_event` [VERIFY: grep -n "def append_hook_event" user/scripts/lazy_core/statedir.py] when importable, else an inline JSONL fallback (the current per-hook fallback branch, collapsed here ONCE), `breadcrumb(hook, err)`, and the module-level `ENV_PREFIX` / `CMD_START` anchor constants. **Import-light (D4):** stdlib only at module top; `hook_lib` import must NOT read stdin and must NOT pull `lazy_core`. Mirror `lazy_core.append_hook_event`'s signature exactly so the delegation is a pass-through.
- **Spec requirements:** SPEC D1, D2, D4.
- **Batch:** 1 (solo — single WU; depends on Phase 1's `HOOK_SCRIPTS_DIR`).

**Batch overview — Phase 2:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-4 | Solo | N/A |

---

## Completion

Execution protocol, per-batch review, quality gates, blocking-issue protocol, and the completion report are single-sourced in `~/.claude/skills/_components/execution-contract.md` (read it before executing). This part completes when WU-1..WU-4 are landed, `python user/scripts/test_hooks.py` and `python user/scripts/test_hook_lib.py` both pass, and the plan-part frontmatter `status:` is flipped to `Complete`. Then proceed to part 2. Do NOT flip SPEC.md/PHASES.md top-level `**Status:**` or write any completion receipt — those are `__mark_complete__`-gate-owned.
