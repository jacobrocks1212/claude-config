---
kind: implementation-plan
feature_id: shared-hook-lib
status: Complete
created: 2026-07-18
complexity: complex
phases: [4]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\plans\all-phases-shared-hook-lib-part-3.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 3 of 3. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\plans\all-phases-shared-hook-lib-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\plans\all-phases-shared-hook-lib-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\plans\all-phases-shared-hook-lib-part-3.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — shared-hook-lib (Phase 4)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\PHASES.md` (shared-hook-lib, 4 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\features\shared-hook-lib\SPEC.md`

**Total phases:** 1 (this part) of 4
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL, the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol, the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is `claude-config`, a harness-config repo whose gates are Python test suites + linters, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes

- **Quality gates for this repo:** `python user/scripts/test_kpi_scorecard.py` (the KPI-tooling unit suite), `python user/scripts/kpi-scorecard.py --lint` (registry schema/enum), and `python user/scripts/kpi-scorecard.py --lint --spec docs/features/shared-hook-lib/SPEC.md` (the SPEC KPI-declaration gate — must exit 0 once the selector is registered + the row promoted). Before commit: `python3 ~/.claude/scripts/gate-battery.py --repo-root .` (trust the banner's last line).
- **Ordering within this part (WU-1 before WU-2):** the `repo-static-scan` source + `hook-duplicated-line-count` selector must be REGISTERED in `kpi-scorecard.py` (WU-1) BEFORE the drafted registry row is promoted (WU-2), because `--lint`/`--lint --spec` validate the row's `signal.source`/`selector` against the closed `_SOURCES` enum — a promoted row referencing an unregistered selector fails `--lint`.
- **Honesty ladder (SPEC KPI Declaration):** the counter's selector must return NO-DATA (never a fabricated zero) when the scan target is absent/unrecordable. Do NOT emit a claimable zero.
- **No dev runtime / no MCP** (`**MCP runtime:** not-required`).
- **This is the terminal implementation part.** When WU-1 + WU-2 land and the gates are green, flip THIS plan-part's frontmatter `status:` to `Complete` and set the PHASES.md top-level `**Status:**` to `In-progress` (implementation done, validation pending — the state machine routes onward). Do NOT flip to `Complete` and do NOT write `COMPLETED.md` — those are `__mark_complete__`-gate-owned.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies.)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | shared-hook-lib | P4 | KPI wiring — register the static-scan source + counter (WU-1), promote the drafted registry row (WU-2) | part 2 (duplication collapsed so the counter measures a real number) | Sequential |

## Work Units

- [x] WU-1 — Register `repo-static-scan` source + `hook-duplicated-line-count` selector + counter in `kpi-scorecard.py` (+ tests)
- [x] WU-2 — Promote the `hook-plane-duplicated-lines` row into `docs/kpi/registry.json`

---

### Phase: shared-hook-lib P4 — KPI wiring

**Goal:** Register the `repo-static-scan` signal source and `hook-duplicated-line-count` selector (plus a deterministic duplicated-line counter over `user/hooks/`) in `kpi-scorecard.py`, then promote the SPEC's drafted `hook-plane-duplicated-lines` row from its non-claiming fence into `docs/kpi/registry.json`, and confirm both `--lint` gates pass.
**Entry criteria:** part 2 complete — the hook-plane duplication is actually collapsed, so the counter reports a real post-migration number.
**SPEC.md references:** `## KPI Declaration` (the drafted `hook-plane-duplicated-lines` row + the `repo-static-scan`/`hook-duplicated-line-count` selector), Phase 4 in `## Implementation Phases`, Validation Criteria row "Duplication actually removed" (grep + the Phase-4 counter). Follows the `canary-trip-precision` / `session-log-mining` precedent (register the selector alongside the feature so the drafted row becomes claimable).

#### WU-1 — Register the source + selector + counter in `kpi-scorecard.py`
- **Scope:** PHASES P4 deliverable "`user/scripts/kpi-scorecard.py` — add `repo-static-scan` to `_SOURCES` + a `_sel_*` selector computing the deterministic count" + the test deliverable.
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/kpi-scorecard.py` (extend `_SOURCES` [VERIFY: grep -n "^_SOURCES" user/scripts/kpi-scorecard.py] + add a selector function alongside the existing `_sel_*` family [VERIFY: grep -n "def _sel_" user/scripts/kpi-scorecard.py]).
- **Test files:** `user/scripts/test_kpi_scorecard.py` (extend [VERIFY: ls user/scripts/test_kpi_scorecard.py]).
- **Test expectations:** (a) `--lint` accepts a registry row whose `signal.source == "repo-static-scan"` / `selector == "hook-duplicated-line-count"`; (b) the counter returns a deterministic int on a fixture hook tree with a known duplicated-line count; (c) the selector returns NO-DATA (never a fabricated zero) when the scan target is absent/unrecordable.
- **Implementation goal:** Add `"repo-static-scan": frozenset({"hook-duplicated-line-count"})` to the `_SOURCES` map, and a `_sel_*` selector function that runs a deterministic static scan of `user/hooks/` counting the remaining duplicated scaffolding lines, wired into the source→selector dispatch exactly like the existing `_sel_telemetry` family [VERIFY: grep -n "def _sel_telemetry" user/scripts/kpi-scorecard.py]. Honor the honesty ladder (absent/unrecordable → NO-DATA). The scorecard render stays byte-stable (no wall-clock embed).
- **Spec requirements:** SPEC `## KPI Declaration` (source/selector registration), Phase 4.
- **Batch:** 1.

#### WU-2 — Promote the `hook-plane-duplicated-lines` row into `docs/kpi/registry.json`
- **Scope:** PHASES P4 deliverable "`docs/kpi/registry.json` — append the `hook-plane-duplicated-lines` row verbatim from the SPEC's KPI Declaration (idempotent — do not duplicate an existing id)".
- **TDD:** no (data promotion; validated by the `--lint`/`--lint --spec` gates).
- **Files to create/modify:** `docs/kpi/registry.json` (extend the `kpis` list [VERIFY: grep -n "hook-plane-duplicated-lines" docs/kpi/registry.json] — expect zero hits pre-work; append the row).
- **Implementation goal:** Append the drafted `hook-plane-duplicated-lines` row (SPEC `## KPI Declaration` fenced block) into the `kpis` array of `docs/kpi/registry.json`, converting it from the SPEC's non-claiming fence to a live registered row. Idempotent — if the id already exists, do nothing (never duplicate). After this WU, `python user/scripts/kpi-scorecard.py --lint` and `--lint --spec docs/features/shared-hook-lib/SPEC.md` must both exit 0.
- **Spec requirements:** SPEC `## KPI Declaration` (drafted row), Phase 4.
- **Batch:** 2 (after WU-1 — the row references the selector WU-1 registers; `--lint` fails if the source/selector is not yet in `_SOURCES`).

**Batch overview — Phase 4:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | N/A |
| 2 | WU-2 | Solo | N/A (different file, but sequenced — the row needs the registered selector to lint) |

---

## Completion

Execution protocol, per-batch review, quality gates, and blocking-issue protocol are single-sourced in `~/.claude/skills/_components/execution-contract.md` (read it before executing). This terminal part completes when WU-1 + WU-2 land, `python user/scripts/test_kpi_scorecard.py` passes, and `python user/scripts/kpi-scorecard.py --lint` + `--lint --spec docs/features/shared-hook-lib/SPEC.md` both exit 0. Then flip THIS plan-part's frontmatter `status:` to `Complete` and set PHASES.md top-level `**Status:**` to `In-progress` (implementation done; validation pending — let the state machine route onward). Do NOT flip the top-level `**Status:**` to `Complete` and do NOT write `COMPLETED.md` — those are `__mark_complete__`-gate-owned.
