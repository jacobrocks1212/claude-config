---
kind: implementation-plan
feature_id: merged-head-actionability-oracle
status: Ready
created: 2026-07-18
complexity: complex
phases: [1, 2, 3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan docs/features/merged-head-actionability-oracle/plans/all-phases-merged-head-actionability-oracle.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — merged-head-actionability-oracle  (v2)

**PHASES.md files:**
- `docs/features/merged-head-actionability-oracle/PHASES.md` (merged-head-actionability-oracle, 3 phases)

**SPEC.md files:**
- `docs/features/merged-head-actionability-oracle/SPEC.md`

**Total phases:** 3
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (e.g. a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and any local `## Component Reference Card` override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config — override the contract's default gates)

claude-config is a **Python harness repo, not a Tauri/C# app** — there is NO `/msbuild`, `/mstest`, `/nxbuild`, MCP runtime, or dev server. The contract's build/test gate rows are overridden per this plan. The authoritative gate battery for every WU/phase in this plan is:

```bash
# Fast state-machine smoke (hermetic; the primary regression net):
python3 user/scripts/lazy-state.py --test
python3 user/scripts/bug-state.py --test
# lazy_core seam suite (the oracle lives here):
python3 -m pytest user/scripts/tests/test_lazy_core/test_dispatch.py -q     # per-WU focused
python3 -m pytest user/scripts/tests/test_lazy_core/ -q                      # full seam suite (Phase 3 / final)
# Coupled-pair parity (REQUIRED after ANY edit to a state script exclude-set site — Phase 2 & 3):
python3 user/scripts/lazy_parity_audit.py --repo-root .
# Doc-drift (Phase 3, after helper deletion — no dangling symbol in any doc/script table):
python3 user/scripts/doc-drift-lint.py --repo-root .
```

- **`--test` baselines are byte-pinned.** `lazy-state.py --test` compares to `tests/baselines/lazy-state-test-baseline.txt` and `bug-state.py --test` to `tests/baselines/bug-state-test-baseline.txt` via `_normalize_smoke_output` (`tests/test_lazy_core/_util.py`). If a legitimate additive diagnostic changes the smoke output, regenerate the baseline ONLY by piping live `--test` output through that helper — NEVER hand-edit it.
- **Coupled-pair discipline is HARD (SPEC L6).** The merged marker is shared across `lazy-state.py` / `bug-state.py`. `lazy_parity_audit.py --repo-root .` MUST stay exit 0 after every state-script edit. Site-count asymmetry (feature has 3 exclude-set sites, bug has 1) is by design — parity is mechanical, not literal.
- **No MCP runtime.** Per PHASES.md `**MCP runtime:** not-required`, there is no Step-9 `/mcp-test` gate — every deliverable is closable in-session by `/execute-plan` (write code, write tests, run the gate battery, commit). No runtime-verification `<!-- verification-only -->` rows exist in PHASES.md, so nothing is deferred to a validation tail.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies; the SPEC `**Depends on:**` block is `(none)` — the merged-head machinery this refactors is already shipped/archived under `docs/bugs/_archive/merged-head-*`.)

## Touchpoint Audit (verified: inline — dispatch available; a 5-file Python read is cheaper than fan-out)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `user/scripts/lazy_core/dispatch.py` | yes | `merged_head_override` (358), `probe_skipped_ids` (434), `research_halt_head` (531) | refactor (add) | Add `is_dispatchable` + `merged_head_nondispatchable_ids`. REUSE `probe_skipped_ids` UNCHANGED (L2). Do NOT change `merged_head_override` / `research_halt_head` signatures (they still accept a pre-built `exclude_ids`). |
| `user/scripts/lazy_core/depdag.py` | yes | `next_merged` (1477), `nondispatchable_item_ids` (1496) | refactor (retire) | Keep `next_merged` (the oracle reuses its ordering). DELETE `nondispatchable_item_ids` in Phase 3 (grep-confirmed zero non-merged callers) + fix its prose cross-reference at line ~1440. |
| `user/scripts/lazy_core/__init__.py` | yes | facade name→submodule map; `"nondispatchable_item_ids": "depdag"` (492) | modify (Phase 3) | Remove the facade map entry when the symbol is deleted. |
| `user/scripts/lazy-state.py` | yes | `_nm_excluded` (12580, `--next-merged`), `_rh_excluded` (13935, `research_halt`), `_mo_excluded` (14104, `--emit-prompt` merged-override) | refactor | Rewire all three exclude-set construction sites to `merged_head_nondispatchable_ids`. |
| `user/scripts/bug-state.py` | yes | `_mo_excluded` (9715, merged-override — the ONLY exclude-set site on the bug side) | refactor | Rewire the merged-override site; coupled-pair mirror of the lazy-state.py `_mo` edit. |
| `user/scripts/lazy_core/docmodel.py` | yes | prose refs to `nondispatchable_item_ids` (2313, 2340) | modify (Phase 3) | Repoint prose to the oracle when the helper is deleted (no dangling symbol). |
| `user/scripts/tests/test_lazy_core/test_dispatch.py` | yes | facet regression fixtures + `nondispatchable_item_ids` refs | modify | Add oracle unit tests, new-category coverage, isolation fixture, byte-identity fixture; remove dead `nondispatchable_item_ids` tests in Phase 3. |

**No premise-grade contradictions.** One anchor-grade nuance corrected in-plan: SPEC L6 says "three sites... mirrored" but bug-state.py has only ONE exclude-set site (feature pipeline owns `--next-merged`/`research_halt`); parity is enforced mechanically by `lazy_parity_audit.py`, not literal per-script site-count symmetry — this is how the current code already works.

## Execution Schedule

| Step | Feature | Phase(s) | Title | Blocked by | Parallel? |
|------|---------|----------|-------|------------|-----------|
| 1 | merged-head-actionability-oracle | P1 | Oracle core (pure, hermetic) | — | Solo (WU-1) |
| 2 | merged-head-actionability-oracle | P2 | Emit-path migration + parity + byte-identity | P1 | Solo (WU-2) |
| 3 | merged-head-actionability-oracle | P3 | Remaining-site migration + helper retirement | P2 | Sequential (WU-3 → WU-4) |

Every phase's WUs modify the SAME core files (`dispatch.py`, the two state scripts) and each phase depends on the prior — so the whole plan runs **sequentially**, one WU per batch. No cross-WU parallelism is legal here (file overlap + dependency edges).

## Work Units

- [ ] WU-1 — Oracle core: `is_dispatchable` + `merged_head_nondispatchable_ids` + characterization tests
- [ ] WU-2 — Emit-path migration on both state scripts + parity + byte-identity/isolation tests
- [ ] WU-3 — Migrate `--next-merged` + `research_halt` exclude-set sites to the oracle
- [ ] WU-4 — Retire `nondispatchable_item_ids` (delete + prose repoint + facade map) + baseline re-pin

---

### Phase: merged-head-actionability-oracle P1 — Oracle core (pure, hermetic)

**Goal:** Add the pure, dependency-injected actionability oracle (`is_dispatchable` + `merged_head_nondispatchable_ids`) to `lazy_core.dispatch`, characterized against every facet fixture plus previously-uncovered categories, with the in-process isolation question resolved by a real-`compute_state` test.
**Entry criteria:** None (first phase).
**SPEC.md references:** `## Technical Design` → "Target shape (the oracle)" + "`is_dispatchable(scoped_state)` classification (L3)" + "In-process scoped-probe safety"; Locked Decisions L1, L2, L3, L5; Open Questions 1 & 3.

**Batch 1 (Solo)**

#### WU-1 — Oracle core + characterization tests

- **Scope:** PHASES.md Phase 1 deliverables — `is_dispatchable`, `merged_head_nondispatchable_ids`, the at-or-above bound + first-dispatchable-head short-circuit (L5), the research-surface preservation classification (L3 tail), the no-signature-change assertion, and the full test set (facet regressions + new-category + `is_dispatchable` predicate table + in-process isolation characterization).
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy_core/dispatch.py` — add `is_dispatchable(scoped_state)` and `merged_head_nondispatchable_ids(feature_items, bug_items, repo_root, current_item_id, *, same_pipeline, same_pipeline_state, scoped_probe, <run flags>)`. REUSE `probe_skipped_ids` [VERIFY: grep -n "^def probe_skipped_ids" user/scripts/lazy_core/dispatch.py] unchanged for the same-pipeline exclude source (L2). Do NOT touch `merged_head_override` [VERIFY: grep -n "^def merged_head_override" user/scripts/lazy_core/dispatch.py] or `research_halt_head` [VERIFY: grep -n "^def research_halt_head" user/scripts/lazy_core/dispatch.py] signatures. REUSE the canonical `next_merged` [VERIFY: grep -n "^def next_merged" user/scripts/lazy_core/depdag.py] / `merged_worklist` ordering for the at-or-above bound (L5) — never a second ordering rule.
  - `user/scripts/tests/test_lazy_core/test_dispatch.py` — characterization tests (see Test expectations).
- **Test files:** `user/scripts/tests/test_lazy_core/test_dispatch.py`.
- **Test expectations:**
  - `is_dispatchable(scoped_state)` predicate table: for each `terminal_reason` value drawn from `compute_state`'s closed terminal vocabulary + a real (`sub_skill` non-empty, non-`__`) dispatch state, assert the expected bool. Derive the non-dispatch reason set EXHAUSTIVELY from `compute_state`'s vocabulary (Open Question 3) — reuse the existing `lazy_core` sanctioned-stop / halt terminal sets rather than hand-listing a drift-prone enumeration.
  - Facet regressions: for EACH currently-enumerated facet (parked / operator-deferred / device-deferred / dep-unready / research-skipped / research-exclusion), a stub `scoped_probe` returns that facet's non-dispatch scoped state → assert the candidate id is EXCLUDED by `merged_head_nondispatchable_ids`.
  - New-category coverage: a cloud-deferred / completion-unverified scoped state → `is_dispatchable` false → candidate EXCLUDED (the previously-uncovered class this feature adds).
  - Research-surface preservation (L3 tail): a `needs-research` scoped state WITHOUT `--skip-needs-research` classifies non-dispatchable → EXCLUDED here (the `research_halt_head` re-include is Phase 2/3, asserted there).
  - Bound/short-circuit (L5): a lower-priority non-dispatchable candidate ranked BELOW the emitted item is never scoped-probed (assert the injected `scoped_probe` is not called for it).
  - Same-pipeline source unchanged: assert `probe_skipped_ids(same_pipeline_state, same_pipeline_items)` is the same-pipeline contribution and `merged_head_override` / `research_halt_head` signatures are byte-unchanged.
  - **In-process isolation characterization (Open Question 1 / L4, the runtime-coupled deliverable):** a test that drives the REAL `compute_state` scoped N times (NOT a mock) and asserts the primary emit probe's already-captured `state`, plus the module accumulators `_SKIP_AHEAD_BLOCKED` / `_GATED_HEADS` / `_DIAGNOSTICS` / `_DEP_GATED`, are UNCORRUPTED afterward. This test RESOLVES whether snapshot/restore is required or reading only the returned dict suffices — record the resolved strategy in the Implementation Notes.
- **Implementation goal:** A pure, hermetic function pair. `merged_head_nondispatchable_ids` builds the exclude set as: (1) same-pipeline → `probe_skipped_ids` UNCHANGED (L2); (2) cross-pipeline → the INJECTED `scoped_probe` per at-or-above candidate, classified non-dispatchable iff `is_dispatchable(scoped_state)` is false, honoring the SAME run flags the emit probe used; (3) `.discard(current_item_id)`. The `scoped_probe` seam is what keeps `--test` hermetic (no real subprocess). Byte-identical for dispatchable heads is guaranteed by construction (the oracle IS the dispatch decision the withhold already trusts).
- **Spec requirements:** SPEC `## Technical Design` "Target shape (the oracle)" (the 3-step exclude-set build), "`is_dispatchable(scoped_state)` classification (L3)", "In-process scoped-probe safety"; L1/L2/L3/L5.
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | N/A |

---

### Phase: merged-head-actionability-oracle P2 — Emit-path migration + parity + byte-identity

**Goal:** Rewire the `--emit-prompt` merged-override exclude-set construction on BOTH state scripts to call the oracle with the REAL cross-pipeline scoped `compute_state` bound to the injected callable; prove byte-identity for dispatchable heads and keep the coupled pair in parity. The other two feature-side sites stay on the legacy helper until Phase 3 (incremental migration).
**Entry criteria:** Phase 1 complete — `is_dispatchable` + `merged_head_nondispatchable_ids` exist in `lazy_core.dispatch` with the injected-`scoped_probe` seam and the resolved in-process isolation strategy.
**SPEC.md references:** `## Technical Design` "Files touched" (state-script rewiring, coupled-pair L6); Validation Criteria rows "Byte-identity for dispatchable heads", "No cross-probe corruption", "Coupled-pair parity"; L2, L6.

**Batch 1 (Solo)**

#### WU-2 — Emit-path migration on both state scripts + parity + byte-identity/isolation tests

- **Scope:** PHASES.md Phase 2 deliverables — rewire `lazy-state.py`'s `_mo_excluded` [VERIFY: grep -n "_mo_excluded" user/scripts/lazy-state.py] merged-override site and `bug-state.py`'s `_mo_excluded` [VERIFY: grep -n "_mo_excluded" user/scripts/bug-state.py] merged-override site to `merged_head_nondispatchable_ids`, binding the injected `scoped_probe` to the REAL cross-pipeline scoped `compute_state`; preserve the `.discard(current)` invariant and the existing observability `_diag` line; add byte-identity + real-call-site isolation tests.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy-state.py` — the `_mo_excluded` construction (~14104): replace `nondispatchable_item_ids(...) | probe_skipped_ids(state, _mo_feats)` with `merged_head_nondispatchable_ids(...)` — same-pipeline = features (`probe_skipped_ids(state, _mo_feats)` preserved INSIDE the oracle), cross-pipeline = bugs via the real bug-scoped `compute_state` (`--bug-id` in-process; the `--next-merged` importlib precedent for reaching `bug-state.py`). `nondispatchable_item_ids` [VERIFY: grep -n "^def nondispatchable_item_ids" user/scripts/lazy_core/depdag.py] stays imported for the still-legacy `_nm`/`_rh` sites this phase does not touch.
  - `user/scripts/bug-state.py` — the `_mo_excluded` construction (~9715): coupled mirror — same-pipeline = bugs (`probe_skipped_ids(state, _mo_bugs)`), cross-pipeline = features via the real feature-scoped `compute_state` (`--feature-id` in-process). Preserve the documented feature/bug divergence (bug side has no `--skip-needs-research`).
  - `user/scripts/tests/test_lazy_core/test_dispatch.py` — byte-identity + real-call-site isolation fixtures.
- **Test files:** `user/scripts/tests/test_lazy_core/test_dispatch.py` (+ both scripts' in-file `--test` suites are run as gates, not authored here).
- **Test expectations:**
  - Byte-identity for dispatchable heads: a P0 bug jumping the queue mid-feature-run → the `merged-head-diverged` withhold fires IDENTICALLY to the pre-oracle construction (the oracle IS the trusted dispatch decision).
  - Real-call-site isolation: after the emit path runs N scoped probes in-process, the primary `state` is unchanged (the Phase 1 isolation strategy applied at the real site).
  - The in-file `--test` suites (`lazy-state.py --test`, `bug-state.py --test`) stay green; `lazy_parity_audit.py --repo-root .` exits 0.
- **Implementation goal:** Both merged-override sites build their exclude set through the single oracle with the real scoped `compute_state`, honoring the SAME run flags the emit probe used (park facets, `skip_needs_research`, `cloud`, `real_device`, `strict_research_halt`), without corrupting the primary probe's already-captured `state`. Behavior byte-identical for dispatchable heads; previously-uncovered non-dispatchable categories now correctly excluded.
- **Spec requirements:** SPEC `## Technical Design` "Files touched"; Validation Criteria "Byte-identity for dispatchable heads" / "No cross-probe corruption" / "Coupled-pair parity"; L2, L6.
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-2 | Solo | N/A (different files, but coupled-pair edit is one integrated WU + parity gate) |

---

### Phase: merged-head-actionability-oracle P3 — Remaining-site migration + helper retirement + baseline re-pin

**Goal:** Migrate the two remaining feature-side exclude-set sites (`--next-merged`, `research_halt`) to the oracle, then DELETE `nondispatchable_item_ids` outright (grep-confirmed zero non-merged callers) with a `retires:` declaration (L7), repoint every prose reference, and re-pin the smoke baselines.
**Entry criteria:** Phase 2 complete — the `--emit-prompt` merged-override site on both scripts already calls the oracle, so migrating the last two sites drops `nondispatchable_item_ids` to zero callers.
**SPEC.md references:** `## Technical Design` "Files touched" (`nondispatchable_item_ids` retirement, L7 `retires:`); Validation Criteria "Coupled-pair parity"; L3 tail (research-halt re-include byte-identity); Open Question 2.

**Batch 1 (Solo) → Batch 2 (Solo)** — WU-4 depends on WU-3 (the helper cannot be deleted until its last two callers migrate) and both touch overlapping trees, so they are sequential.

#### WU-3 — Migrate `--next-merged` + `research_halt` exclude-set sites to the oracle

- **Scope:** PHASES.md Phase 3 deliverables (site migration half) — rewire `lazy-state.py`'s `_nm_excluded` [VERIFY: grep -n "_nm_excluded" user/scripts/lazy-state.py] (`--next-merged`) and `_rh_excluded` [VERIFY: grep -n "_rh_excluded" user/scripts/lazy-state.py] (`research_halt`) sites to build their exclude set via `merged_head_nondispatchable_ids`.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy-state.py` — `_nm_excluded` (~12580) and `_rh_excluded` (~13935) sites. For `_rh`, build the FULL merged-head exclude set via the oracle, then pass it to `research_halt_head` [VERIFY: grep -n "^def research_halt_head" user/scripts/lazy_core/dispatch.py] — which RE-INCLUDES the research-gated ids exactly as today (byte-identity invariant, L3 tail). Preserve each site's existing `.discard(current)`. `next_merged` / `merged_head_override` signatures unchanged.
  - `user/scripts/tests/test_lazy_core/test_dispatch.py` — `--next-merged` + `research_halt` migration fixtures.
- **Test files:** `user/scripts/tests/test_lazy_core/test_dispatch.py`.
- **Test expectations:**
  - `--next-merged` exclude set built via the oracle → merged head unchanged for the facet/new-category fixtures.
  - `research_halt`: the needs-research halt still SURFACES (a research-gated head at the merged head still returns from `research_halt_head` after the oracle-built exclude set is passed) — byte-identical to today (L3 tail).
  - `lazy-state.py --test` + `bug-state.py --test` green; `lazy_parity_audit.py --repo-root .` exit 0.
- **Implementation goal:** All three feature-side exclude-set sites now route through the single oracle; the research-halt surfacing invariant is preserved. `nondispatchable_item_ids` now has zero remaining callers (verified in WU-4).
- **Spec requirements:** SPEC `## Technical Design` "Files touched"; L3 tail; Validation Criteria "Coupled-pair parity".
- **Batch:** 1.

#### WU-4 — Retire `nondispatchable_item_ids` (delete + prose repoint + facade map) + baseline re-pin

- **Scope:** PHASES.md Phase 3 deliverables (retirement half) — re-confirm zero callers, DELETE `nondispatchable_item_ids` (definition + facade map entry + test refs), add the `retires:` declaration (L7), repoint prose cross-references, re-pin the smoke baselines, and confirm doc-drift clean.
- **TDD:** no (deletion + doc/prose cleanup + mechanical baseline re-pin with grep/lint coverage — no new behavior).
- **Files to create/modify:**
  - `user/scripts/lazy_core/depdag.py` — DELETE the `nondispatchable_item_ids` [VERIFY: grep -n "^def nondispatchable_item_ids" user/scripts/lazy_core/depdag.py] definition (~1496) AFTER re-confirming zero live callers (`grep -rn "nondispatchable_item_ids" user/scripts --include="*.py" | grep -v test` returns only prose); fix the prose cross-reference at ~1440. Add the `retires: nondispatchable_item_ids` declaration (per L7 / the anti-overfit complexity check — the deleting commit carries it).
  - `user/scripts/lazy_core/__init__.py` — remove the `"nondispatchable_item_ids": "depdag"` [VERIFY: grep -n "nondispatchable_item_ids" user/scripts/lazy_core/__init__.py] facade map entry (~492).
  - `user/scripts/lazy_core/dispatch.py` / `user/scripts/lazy_core/docmodel.py` — repoint the retired-symbol prose references (`dispatch.py` ~557, `docmodel.py` ~2313/2340) at the oracle so no dangling reference to a deleted symbol survives.
  - `user/scripts/tests/test_lazy_core/test_dispatch.py` — remove the now-dead `nondispatchable_item_ids` tests; add a "helper gone" assertion (importing `nondispatchable_item_ids` from `lazy_core` fails / it is absent from the facade map).
  - `tests/baselines/lazy-state-test-baseline.txt` / `tests/baselines/bug-state-test-baseline.txt` — full re-pin, regenerated ONLY via `_normalize_smoke_output` if the smoke output legitimately changed.
- **Implementation goal:** `nondispatchable_item_ids` no longer exists anywhere in live code or the facade; every merged-head exclude-set site routes through the oracle; no dangling symbol in any doc/script table; the recurring `merged-head-diverged-withholds-on-<X>` class is closed by construction.
- **Spec requirements:** SPEC `## Technical Design` "Files touched" (`nondispatchable_item_ids` retirement + `retires:`); Open Question 2 (grep-confirmed full deletion).
- **Verification gate (this WU):** `python3 -m pytest user/scripts/tests/test_lazy_core/ -q` green (incl. the helper-gone assertion); `lazy-state.py --test` + `bug-state.py --test` green; `lazy_parity_audit.py --repo-root .` exit 0; `doc-drift-lint.py --repo-root .` clean; a repo-wide `grep -rn "nondispatchable_item_ids" user/scripts --include="*.py"` returns only archived-bug prose (zero live code callers, zero facade entry).
- **Batch:** 2.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-3 | Solo | N/A |
| 2 | WU-4 | Solo | Depends on WU-3 (delete after last callers migrate) |

---

## Blocking Issue Protocol / Completion — single-sourced

See `~/.claude/skills/_components/execution-contract.md` (pointed to under "Execution Policy — single-sourced" above) for the Blocking Issue Protocol and the Completion report. The gate battery this plan runs at each phase/WU boundary and at completion is the "Plan-specific execution notes" block above (Python `--test` + `pytest` + `lazy_parity_audit.py` + `doc-drift-lint.py`), NOT the contract's default `/msbuild`/`/mstest` rows.

**Completion (gate-owned):** the `__mark_complete__` gate flips SPEC.md / PHASES.md top-level `**Status:**` to Complete, writes `COMPLETED.md`, strikes the ROADMAP row, and trims the queue once this plan's WUs are landed and the gate battery is green — those actions are NOT plan work units.
