---
kind: implementation-plan
feature_id: cycle-prompt-deflation
status: In-progress
created: 2026-07-19
complexity: complex
phases: [2, 3, 4]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan <this-path>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 2. Sibling parts:
> - part 1: `docs/features/cycle-prompt-deflation/plans/all-phases-cycle-prompt-deflation-part-1.md`
> - part 2: `docs/features/cycle-prompt-deflation/plans/all-phases-cycle-prompt-deflation-part-2.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — Cycle-Prompt Deflation (Part 2: Deflation + Scope-Tightening + Measured KPI)  (v2)

**PHASES.md files:**
- `docs/features/cycle-prompt-deflation/PHASES.md` (cycle-prompt-deflation, Phases 2–4)

**SPEC.md files:**
- `docs/features/cycle-prompt-deflation/SPEC.md`

**Total phases:** 3 (this part) of 4 (feature)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the Plan-specific execution notes below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

### Plan-specific execution notes (claude-config — non-default gates + coupled-pair discipline)

- **This is claude-config, not a Tauri/MCP repo.** No `/msbuild`/`/mstest`/`/tauri-build`, no live runtime. Substitute the contract's build/test gates with the claude-config invariant battery:
  ```bash
  python3 user/scripts/skill-size-ratchet.py --check
  python3 user/scripts/kpi-scorecard.py --lint
  python3 -m pytest user/scripts/ -q
  python3 user/scripts/lint-skills.py
  python3 user/scripts/lazy_parity_audit.py --repo-root .
  python3 user/scripts/generate-coupled-skills.py --check   # coupled-pair drift gate — MUST stay green after any prompt edit
  ```
  or run `python3 user/scripts/gate-battery.py` and trust its last-line `RESULT=` banner. A failing gate blocks the batch.
- **COUPLED-PAIR DISCIPLINE (HARD).** `cycle-base-prompt.md` is the shared cycle-prompt template. Every section edit MUST (a) preserve each `@section <name> pipelines=… modes=… skills=… [variant=…] [park=…] [hosts=…]` selector line verbatim so the emitter's filter still resolves, (b) preserve the marker literals other co-editing features depend on (18 features govern this file per provenance — notably `cycle-prompt-environment-dialect` owns `env-dialect-*` + the `hosts=windows` attribute, `lazy-cycle-containment` owns the terminal-stop C4 literal, plus `stub-origin-provisional-exclusion`, `code-doc-provenance-linkage`), and (c) leave `generate-coupled-skills.py --check` GREEN (run `--write` first if any prompt-describing prose in the coupled SKILL.md files shifted, then re-`--check`). After each deflation, run the `lazy_core` dispatch tests to prove `emit_cycle_prompt` still assembles every profile with no unbound-`{token}` residue.
- **MCP runtime: not-required** — every WU closes in-session. Commit + push after each phase completes (work branch `main`; pathspec-scoped `git add`, never `-A`).

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| coupled-pair-generation | hard | `docs/features/coupled-pair-generation/PHASES.md` | `generate-coupled-skills.py` + overlay store; its `--check` drift gate must stay green after every deflation phase. |
| lazy-batch-skill-deflation | composes | `docs/features/lazy-batch-skill-deflation/PHASES.md` | The prose→verdict-rule deflation playbook + the `skill-size-ratchet.py` `--lock-in` gate this part drives down. |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | cycle-prompt-deflation | P2 | Trim top-3 boilerplate sections | Part 1 (Phase 1: measurement + baseline) | Batch 1: WU-1 (solo) |
| 2    | cycle-prompt-deflation | P3 | Trim remaining + scope-tightening | Phase 2 | Batch 2: WU-2 (solo — same file as WU-1) |
| 3    | cycle-prompt-deflation | P4 | Measured KPI baseline + gate confirm | Phase 3 (final census) + Part 1 (selector) | Batch 3: WU-3 (solo) |

## Touchpoint Audit (verified inline against the live tree — dispatch skipped per Step-B fallback)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` | yes | 28 `@section` blocks; `env-dialect-windows … hosts=windows`; 8 `skills=all` boilerplate sections | refactor (deflate) | Trim prose in place; preserve every `@section` selector + marker literal. |
| `user/scripts/skill-size-ratchet.py` | yes | `lock_in` (:104), `check` (:76) | reuse (run `--lock-in`/`--check`) | Lower profile ceilings via `--lock-in` after each deflation. |
| `user/scripts/skill-size-baseline.json` | yes | assembled-profile store (added in Part 1) | refactor | `--lock-in` lowers ceilings (never raises). |
| `user/scripts/generate-coupled-skills.py` | yes | `--check`/`--write`/`--extract`/`--report` | reuse (run gate) | Keep the drift gate green after each deflation. |
| `user/scripts/lazy_core/dispatch.py` | yes | `emit_cycle_prompt` (:886) | reuse (NO edit) | The residue-guard/assembly regression surface — assert via its tests, never edit. |
| `user/scripts/tests/test_lazy_core/test_dispatch.py` | yes | emit_cycle_prompt assembly/residue tests | refactor | Add scope-tightening presence/absence assertions (WU-2). |
| `user/scripts/kpi-scorecard.py` | yes | `_cmd_capture_baseline` (:1529) — the sole computed-field writer | reuse (run `--capture-baseline`) | Stamps `provenance: measured` from the post-deflation census. |
| `docs/kpi/registry.json` | yes | the `cycle-prompt-assembled-bytes` row (added in Part 1) | refactor | `--capture-baseline` writes the measured value (script-owned). |
| `docs/kpi/SCORECARD.md` | yes/regenerated | byte-stable render | refactor | Regenerate after capture. |
| `user/scripts/test_kpi_scorecard.py` | yes | KPI test suite | refactor | Add the `--capture-baseline` measured-provenance test. |
| `docs/features/cycle-prompt-deflation/SEMANTIC_DIFF_PHASE2.md` / `..._PHASE3.md` | NO (net-new) | — | create | The old-rule → surviving-terse-rule equivalence review artifacts. |

**Contradictions:** none premise-grade.

## Work Units

- [ ] WU-1 — Deflate `turn-end` + `workstation-dispatch` + `hard-contract` (Phase 2)
- [ ] WU-2 — Deflate remaining boilerplate + evidence-backed scope-tightening (Phase 3)
- [ ] WU-3 — Capture measured KPI baseline + confirm gate wiring (Phase 4)

### Phase: cycle-prompt-deflation P2 — Trim the top-3 boilerplate sections

**Goal:** Deflate `turn-end`, `workstation-dispatch`, `hard-contract` to terse rules preserving every policy; regen coupled mirrors; lower affected ceilings.
**Entry criteria:** Part 1 Phase 1 complete (assembled-profile measurement + seed ceilings exist to measure the reduction and re-lock the floor).
**SPEC.md references:** "The eight `skills=all` boilerplate sections" (lever 1, Trim-in-place); Phase 2.

#### WU-1 — Deflate top-3 sections + regen coupled + lock-in (TDD: no — prose deflation, verified by existing emitter tests + gates)

- **Scope:** PHASES.md Phase 2 deliverables — deflate the three highest-return sections; semantic-equivalence artifact; coupled `--check` green; residue-guard passes; ceilings lowered.
- **Files to create/modify:** `user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`, `user/scripts/skill-size-baseline.json` (via `--lock-in`), `docs/features/cycle-prompt-deflation/SEMANTIC_DIFF_PHASE2.md` (net-new), and the coupled overlays/SKILL.md ONLY if `generate-coupled-skills.py --write` regenerates a shift.
- **Test expectations:** no new unit tests (prose density change); verified by (a) `python3 user/scripts/generate-coupled-skills.py --check` exit 0 [VERIFY: ls user/scripts/generate-coupled-skills.py], (b) the `lazy_core` dispatch tests green (emit assembles every profile, no residue error) [VERIFY: ls user/scripts/tests/test_lazy_core/test_dispatch.py], (c) `python3 user/scripts/skill-size-ratchet.py --check` exit 0 at the new lower ceilings.
- **Implementation goal:** Rewrite the `turn-end` (~4.9 KB), `workstation-dispatch` (~4.8 KB), `hard-contract` (~3 KB) sections in `cycle-base-prompt.md` to terse verdict-routing rules — prose density only, NO policy removal. Preserve every `@section` selector line and every depended-on marker literal (grep each governing feature's expected literal after editing). Write `SEMANTIC_DIFF_PHASE2.md` mapping each original rule → its surviving terse rule (the no-policy-lost guard). Then `generate-coupled-skills.py --write` if any prompt-describing prose in the coupled SKILL.md files shifted, re-`--check` to green. Lower the affected profile ceilings via `skill-size-ratchet.py --lock-in` [VERIFY: grep -n "^def lock_in" user/scripts/skill-size-ratchet.py].
- **Spec requirements:** SPEC "Trim-in-place" lever + "Coupled-pair constraint" + Validation Criteria rows "Assembled prompt shrank", "No policy lost", "Coupled-pair parity intact", "Emitter still assembles".
- **Batch:** 1 (solo — single-file prose edit, cannot parallelize)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1 | Solo | N/A |

**Runtime Verification** *(checked by the deterministic gate run):*
- [ ] <!-- verification-only --> `generate-coupled-skills.py --check` exits 0 and `emit_cycle_prompt` assembles every profile without a residue error (dispatch tests green).

### Phase: cycle-prompt-deflation P3 — Trim remaining boilerplate + evidence-backed scope-tightening

**Goal:** Deflate the remaining `skills=all` boilerplate and narrow any selector proven safe to exclude a cycle class; lock the new floor.
**Entry criteria:** Phase 2 complete (top-3 deflated; coupled + emitter gates green — Phase 3 continues on the same template).
**SPEC.md references:** "The eight `skills=all` boilerplate sections" (lever 2, Scope-tighten); Phase 3; Open Questions (target ceiling; scope-tightening scope).

#### WU-2 — Deflate remaining sections + scope-tighten + scope tests + lock-in (TDD: yes — new presence/absence assertions)

- **Scope:** PHASES.md Phase 3 deliverables — deflate `d7`, `env-dialect-core`, `env-dialect-windows`, `status-honesty`, `terminal-stop`, `task`; narrow proven-safe selectors with per-section safety justification; scope-tightening regression tests; lock the floor.
- **Files to create/modify:** `user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`, `user/scripts/skill-size-baseline.json` (via `--lock-in`), `docs/features/cycle-prompt-deflation/SEMANTIC_DIFF_PHASE3.md` (net-new), the coupled overlays/SKILL.md if `--write` shifts them.
- **Test files:** `user/scripts/tests/test_lazy_core/test_dispatch.py`
- **Test expectations:** for each narrowed selector, a test asserts the section is ABSENT from the excluded cycle's assembled prompt AND PRESENT in the cycles that DO consume it (the scope-tightening regression guard — a scope error silently under-briefs a cycle); emit assembles every profile with no residue error.
- **Implementation goal:** Deflate the remaining six sections to terse rules (preserving `@section` boundaries + `hosts=` attributes + the terminal-stop C4 literal). Then, for any `skills=all` selector PROVEN safe to narrow (candidate: `workstation-dispatch` for cycles that never fan out), narrow it and record a per-section safety justification in `SEMANTIC_DIFF_PHASE3.md`; leave uncertain sections at `skills=all` (trim-only — the conservative default). Add the presence/absence assertions to the dispatch tests. `generate-coupled-skills.py --check` green (via `--write` if needed). Lock the new floor via `skill-size-ratchet.py --lock-in`.
- **Spec requirements:** SPEC "Scope-tighten" lever ("Any narrowing must be evidence-backed… a scope error silently under-briefs a cycle, which is worse than a few extra KB") + Validation Criteria "Ratchet blocks re-bloat".
- **Batch:** 2 (solo — same file as WU-1, sequential after Phase 2)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 2     | WU-2 | Solo | Shares `cycle-base-prompt.md`/`skill-size-baseline.json` with WU-1 → sequential batch (Phase 3 after Phase 2) |

**Runtime Verification** *(checked by the deterministic gate run):*
- [ ] <!-- verification-only --> `skill-size-ratchet.py --check` exits 0 at the Phase-3 floor and each narrowed selector's section is asserted present/absent in exactly the right cycle classes.

### Phase: cycle-prompt-deflation P4 — Measured KPI baseline + gate confirmation

**Goal:** Capture the measured KPI baseline from the final post-Phase-3 census and confirm the assembled ratchet is wired into the lint + gate batteries.
**Entry criteria:** Phase 3 complete (final assembled census locked) AND Part 1 Phase 1 complete (selector + computation registered so `--capture-baseline` works).
**SPEC.md references:** "KPI Declaration" (`--capture-baseline` stamps measured provenance from the post-Phase-3 census); Phase 4.

#### WU-3 — Capture measured baseline + confirm gate wiring + render scorecard (TDD: yes — capture test)

- **Scope:** PHASES.md Phase 4 deliverables — run `--capture-baseline`; confirm lint/gate wiring; render `SCORECARD.md`.
- **Files to create/modify:** `docs/kpi/registry.json` (via `--capture-baseline`, script-owned), `.claude/skill-config/gate-battery.json` (only if a wiring gap remains), `docs/kpi/SCORECARD.md` (regenerated).
- **Test files:** `user/scripts/test_kpi_scorecard.py`
- **Test expectations:** `--capture-baseline` writes a `provenance: measured` value from a fixture census (refuses on no-data, never fabricates); `kpi-scorecard.py --lint` exits 0 with the row now measured.
- **Implementation goal:** Run `python3 user/scripts/kpi-scorecard.py --capture-baseline cycle-prompt-assembled-bytes` [VERIFY: grep -n "_cmd_capture_baseline" user/scripts/kpi-scorecard.py] — stamps the measured value + `captured_at` into `docs/kpi/registry.json`. Confirm `lint-skills.py --check-skill-size` and the gate-battery both exercise the assembled ratchet (wire only if a gap remains). Render `docs/kpi/SCORECARD.md` (byte-stable). Add the `--capture-baseline` measured-provenance test.
- **Spec requirements:** SPEC "KPI Declaration" note ("Phase-4 `--capture-baseline` stamps measured provenance from the post-Phase-3 census") + "A hard size gate" UX.
- **Batch:** 3 (solo — depends on Phase 3's final census)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 3     | WU-3 | Solo | N/A |

**Runtime Verification** *(checked by the deterministic gate run):*
- [ ] <!-- verification-only --> `kpi-scorecard.py --lint` exits 0 with `cycle-prompt-assembled-bytes` at `provenance: measured`, and the invariant battery returns `RESULT=PASS`.

**Completion (gate-owned):** the `__mark_complete__` gate flips SPEC.md **Status:** to Complete and writes COMPLETED.md once the whole feature's validation tail passes — never authored as a checkbox here.
