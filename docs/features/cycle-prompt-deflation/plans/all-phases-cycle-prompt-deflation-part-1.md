---
kind: implementation-plan
feature_id: cycle-prompt-deflation
status: Complete
created: 2026-07-19
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan <this-path>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 2. Sibling parts:
> - part 1: `docs/features/cycle-prompt-deflation/plans/all-phases-cycle-prompt-deflation-part-1.md` (this file)
> - part 2: `docs/features/cycle-prompt-deflation/plans/all-phases-cycle-prompt-deflation-part-2.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — Cycle-Prompt Deflation (Part 1: Measurement + Baseline + KPI/Gate Wiring)  (v2)

**PHASES.md files:**
- `docs/features/cycle-prompt-deflation/PHASES.md` (cycle-prompt-deflation, Phase 1)

**SPEC.md files:**
- `docs/features/cycle-prompt-deflation/SPEC.md`

**Total phases:** 1 (this part) of 4 (feature)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the Plan-specific execution notes below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

### Plan-specific execution notes (claude-config — non-default gates)

- **This is claude-config, not a Tauri/MCP repo.** There is NO `/msbuild`/`/mstest`/`/tauri-build` and NO live runtime. Substitute the contract's build/test gate rows with the claude-config invariant battery:
  ```bash
  python3 user/scripts/skill-size-ratchet.py --check
  python3 user/scripts/kpi-scorecard.py --lint
  python3 -m pytest user/scripts/ -q
  python3 user/scripts/lint-skills.py
  python3 user/scripts/lazy_parity_audit.py --repo-root .
  ```
  or run the whole battery via `python3 user/scripts/gate-battery.py` and trust its last-line `RESULT=` banner. A failing gate blocks the batch.
- **MCP runtime: not-required** — no `/mcp-test`, no `VALIDATED.md` work. Every WU here closes in-session (write code, write tests, run the deterministic gates, commit).
- **Commit + push after the phase completes** (per the Push Policy — this repo keeps `origin/main` in sync; `git push` after every commit). Work branch: `main`. Stage only the paths YOU changed (pathspec-scoped) — never a blanket `git add -A`.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| lazy-batch-skill-deflation | composes | `docs/features/lazy-batch-skill-deflation/PHASES.md` | Provides `skill-size-ratchet.py` + `skill-size-baseline.json` (the per-file gate this part extends to assembled-profile measurement) and the prose→verdict-rule deflation playbook. |
| coupled-pair-generation | hard | `docs/features/coupled-pair-generation/PHASES.md` | Provides `generate-coupled-skills.py` + the overlay store; its `--check` drift gate must stay green after any prompt edit (relevant to Part 2, listed here for continuity). |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | cycle-prompt-deflation | P1 | Assembled-profile measurement + baseline seed + KPI/gate wiring | — | Batch 1: WU-1, WU-2, WU-3 (file-disjoint, parallel) |

## Touchpoint Audit (verified inline against the live tree — dispatch skipped per Step-B fallback; contained, directly-verifiable set)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `user/scripts/skill-size-ratchet.py` | yes | `load_baseline` (:57), `measure` (:67), `check` (:76), `lock_in` (:104), `_write`, `main` | refactor | Add an assembled-profile mode; REUSE `check`/`lock_in`/`_write` shape + `lazy_core._atomic_write`. Import `emit_cycle_prompt` — never re-parse the template. |
| `user/scripts/skill-size-baseline.json` | yes | `{schema_version:1, files:{5 entries}}` | refactor | Add an assembled-profile ceiling store; existing `files` entries byte-untouched. |
| `user/scripts/lazy_core/dispatch.py` | yes | `emit_cycle_prompt` (:886) → `{ok, prompt, model}`/`{ok:False,refused}`/`None`; `_parse_cycle_template`; selection loop :965-997 | reuse (NO edit) | Import for measurement + profile enumeration. Forking the emitter is banned. |
| `user/scripts/kpi-scorecard.py` | yes | `_SOURCES["repo-static-scan"]` (:154), dispatch (:1089-1091), `_sel_hook_duplicated_line_count`, `_cmd_capture_baseline` (:1529) | refactor | Register the new selector id in `_SOURCES["repo-static-scan"]`; add `_sel_cycle_prompt_assembled_bytes`; wire into dispatch. |
| `docs/kpi/registry.json` | yes | 502-ln registry | refactor | Add the SPEC's `cycle-prompt-assembled-bytes` row (`baseline.provenance: pending`); `signal.selector` MUST equal the id registered in `_SOURCES`. |
| `user/scripts/lint-skills.py` | yes | `--check-skill-size` (:399, :527) loads the ratchet via importlib → `load_baseline`+`check` | refactor | Confirm/extend the `--check-skill-size` branch so the assembled-profile check runs. |
| `.claude/skill-config/gate-battery.json` | yes | 7 gates incl. `lint-skills` | reuse/confirm | Confirm the assembled ratchet is reached; edit the `lint-skills` cmd only if needed. |
| `user/scripts/test_skill_size_ratchet.py` | yes | 161 ln incl. a live self-check | refactor | Add assembled-profile tests; keep the live self-check green. |
| `user/scripts/test_kpi_scorecard.py` | yes | KPI test suite | refactor | Add the selector-registration + `--lint`-passes test. |

**Contradictions:** none premise-grade. One anchor-grade (mechanical, corrected in-plan): the registry row's `signal.selector` string MUST match the id registered in `_SOURCES["repo-static-scan"]` (the `--lint` check is `selector in _SOURCES[source]`) — pick one id and use it in both WU-2 sites.

## Work Units

- [x] WU-1 — Assembled-profile measurement + baseline store in `skill-size-ratchet.py`
- [x] WU-2 — `cycle-prompt-assembled-bytes` KPI selector + computation + registry row
- [x] WU-3 — Confirm/wire the assembled ratchet into `lint-skills --check-skill-size` + gate-battery

### Phase: cycle-prompt-deflation P1 — Assembled-profile measurement + baseline seed + KPI/gate wiring

**Goal:** Add assembled-cycle-prompt measurement to the ratchet, seed current sizes as ceilings, register the KPI selector + computation, and confirm the gate wiring — all with NO prompt edits (locks the "before" numbers, stops accretion).
**Entry criteria:** None (first phase).
**SPEC.md references:** "New capability — assembled-cycle-prompt ratchet"; "The eight `skills=all` boilerplate sections"; "KPI Declaration".

#### WU-1 — Assembled-profile measurement + baseline store (TDD: yes)

- **Scope:** PHASES.md Phase 1 deliverables — assembled-profile measurement function; profile enumeration; baseline assembled-profile ceiling store + `--lock-in` for profiles.
- **Files to create/modify:** `user/scripts/skill-size-ratchet.py`, `user/scripts/skill-size-baseline.json`
- **Test files:** `user/scripts/test_skill_size_ratchet.py`
- **Test expectations:** a known profile measures a positive byte count driving the real emitter through a fixture `template_dir` (deterministic); `check` flags a profile pushed over its ceiling; `--lock-in` only ever LOWERS a profile ceiling (never raises); a refusing/`None` emitter result is surfaced honestly (never counted as 0 bytes); the live self-check on the real baseline+tree stays green.
- **Implementation goal:** Add an assembled-profile measurement mode that, per `(pipeline, mode, skill[, variant, park, host])` profile, drives `emit_cycle_prompt` [VERIFY: grep -n "def emit_cycle_prompt" user/scripts/lazy_core/dispatch.py] and returns `len(result["prompt"].encode("utf-8"))`. Enumerate the concrete dispatchable profiles from the `@section` selector matrix (both pipelines, workstation mode; cloud where it dispatches; the two mcp-test variants; `hosts=windows`). Store profile ceilings in `skill-size-baseline.json` (new top-level key, e.g. `profiles`), seeded at current sizes. REUSE the existing `check` [VERIFY: grep -n "^def check" user/scripts/skill-size-ratchet.py] / `lock_in` [VERIFY: grep -n "^def lock_in" user/scripts/skill-size-ratchet.py] shape and `lazy_core._atomic_write` — do NOT re-parse `cycle-base-prompt.md` directly and do NOT fork the emitter.
- **Spec requirements:** SPEC "New capability — assembled-cycle-prompt ratchet" (import selection logic, never re-implement; `--lock-in` only lowers; new profiles seeded at current size).
- **Batch:** 1

#### WU-2 — `cycle-prompt-assembled-bytes` KPI selector + computation + registry row (TDD: yes)

- **Scope:** PHASES.md Phase 1 deliverables — register the new `repo-static-scan` selector, add its census computation, add the registry row (`baseline: pending`).
- **Files to create/modify:** `user/scripts/kpi-scorecard.py`, `docs/kpi/registry.json`
- **Test files:** `user/scripts/test_kpi_scorecard.py`
- **Test expectations:** `kpi-scorecard.py --lint` passes with the new row; the selector computation returns a positive `max(assembled_bytes)` over the profiles (or honest NO-DATA on an unreadable target, never a fabricated 0); an unknown selector still yields the `no computation registered` NO-DATA note.
- **Implementation goal:** Register the new selector id in `_SOURCES["repo-static-scan"]` [VERIFY: grep -n "repo-static-scan" user/scripts/kpi-scorecard.py]; add `_sel_cycle_prompt_assembled_bytes(repo_root)` computing `max(assembled_bytes)` over all profiles (reuse WU-1's measurement — import it, do not duplicate); wire it into the `repo-static-scan` dispatch branch [VERIFY: grep -n "hook-duplicated-line-count" user/scripts/kpi-scorecard.py]. Add the SPEC's KPI row to `docs/kpi/registry.json` with `baseline.provenance: pending`. **ANCHOR:** the registry row's `signal.selector` string MUST be byte-identical to the id registered in `_SOURCES["repo-static-scan"]` (the `--lint` check is `selector in _SOURCES[source]`) — use one id in both places.
- **Spec requirements:** SPEC "KPI Declaration" (the drafted row; "Phase 1 registers the selector + its assembled-profile census computation").
- **Batch:** 1

#### WU-3 — Wire the assembled ratchet into the lint battery + gate-battery (TDD: no — wiring/config)

- **Scope:** PHASES.md Phase 1 deliverable — confirm/extend `lint-skills.py --check-skill-size` so the assembled-profile check runs, and confirm the gate-battery reaches it.
- **Files to create/modify:** `user/scripts/lint-skills.py`, `.claude/skill-config/gate-battery.json`
- **Test expectations:** none new (config/wiring); verified by running `python3 user/scripts/lint-skills.py --check-skill-size` (exits 0, exercises the assembled profiles) and `python3 user/scripts/gate-battery.py` (RESULT=PASS reaching the assembled check).
- **Implementation goal:** Confirm the `--check-skill-size` branch [VERIFY: grep -n "check.skill.size" user/scripts/lint-skills.py] runs the assembled-profile `check` too (if WU-1 put profiles in the same baseline, the ratchet's `check` may already cover them — confirm, minimal change). Ensure the `gate-battery.json` `lint-skills` gate [VERIFY: grep -n "lint-skills" .claude/skill-config/gate-battery.json] reaches the assembled ratchet — add `--check-skill-size` to that gate's cmd (or a dedicated gate) only if the default invocation does not run it.
- **Spec requirements:** SPEC "Wired into `lint-skills.py --check-skill-size` and the `gate-battery.json` invariant battery so it runs every lint pass."
- **Batch:** 1

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2, WU-3 | Yes | None (skill-size-ratchet.py+baseline / kpi-scorecard.py+registry / lint-skills.py+gate-battery are disjoint) |

**Runtime Verification** *(checked by the deterministic gate run — NOT the implementation agent):*
- [x] <!-- verification-only --> `skill-size-ratchet.py --check` exits 0 naming N assembled profiles within seed ceilings.
- [x] <!-- verification-only --> `kpi-scorecard.py --lint` exits 0 with `cycle-prompt-assembled-bytes` registered (`provenance: pending`).

**Completion (gate-owned):** the `__mark_complete__` gate flips SPEC.md **Status:** and writes COMPLETED.md after the whole feature's validation tail — never authored as a checkbox here.
