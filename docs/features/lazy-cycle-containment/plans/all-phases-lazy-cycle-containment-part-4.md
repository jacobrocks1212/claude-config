---
kind: implementation-plan
feature_id: lazy-cycle-containment
status: Ready
created: 2026-06-15
complexity: complex
phases: [5, 6]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-15.
> To execute: `/execute-plan docs/features/lazy-cycle-containment/plans/all-phases-lazy-cycle-containment-part-4.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 4 of 5. Sibling parts:
> - part 1: `docs/features/lazy-cycle-containment/plans/all-phases-lazy-cycle-containment-part-1.md`
> - part 2: `docs/features/lazy-cycle-containment/plans/all-phases-lazy-cycle-containment-part-2.md`
> - part 3: `docs/features/lazy-cycle-containment/plans/all-phases-lazy-cycle-containment-part-3.md`
> - part 4: `docs/features/lazy-cycle-containment/plans/all-phases-lazy-cycle-containment-part-4.md` (this file)
> - part 5: `docs/features/lazy-cycle-containment/plans/all-phases-lazy-cycle-containment-part-5.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — lazy-cycle-containment (Phases 5–6)  (v2)

**PHASES.md files:**
- `docs/features/lazy-cycle-containment/PHASES.md` (lazy-cycle-containment, 9 phases)

**SPEC.md files:**
- `docs/features/lazy-cycle-containment/SPEC.md`

**Total phases:** 9 (this part: Phases 5, 6)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`. **Exception for this part:** Phases 5/6 edit SKILL.md / component prose (`.md`), which is documentation, not source — the orchestrator MAY edit these directly per the standard "Edit on PHASES.md/CLAUDE.md and skill prose" allowance; still review every edit.

> **INLINE-EXECUTION OVERRIDE (when run by a `/lazy-batch` cycle subagent lacking the `Agent` tool):** perform ALL implementation INLINE with `Read`/`Edit`/`Write`. Never invoke `/lazy` or `/lazy-batch` recursively.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path. **Before executing each step**, `Read` the listed component files from disk. Do NOT proceed from memory. After compaction, re-read this plan first.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this plan has no completed hard upstream dependencies) | — | — | The dispatch bracket calls the `--cycle-begin`/`--cycle-end` CLI built in part-2 (this feature, earlier phase). |

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. Implementation/test work via Agent where code is touched (this part is mostly prose — see EXECUTION MODEL exception). INLINE override applies when no `Agent` tool.
2. Edits in the current worktree — never create worktrees.
3. **COUPLED-TRIO RULE (load-bearing for Phase 5):** `lazy-batch/SKILL.md`, `lazy-bug-batch/SKILL.md`, and `lazy-batch-cloud/SKILL.md` are a coupled trio per the repo `CLAUDE.md`. The `--cycle-begin`/`--cycle-end` bracket and the State Machine Summary update MUST land identically in all three. After editing, DIFF all three and confirm the bracket shape matches.
4. PHASES.md updated AFTER EACH batch.
5. Every edit reviewed before continuing.
6. Mistakes fixed immediately before the next batch.
7. Integration verification after all batches.
8. CLAUDE.md updated per phase if warranted.
9. Phase committed+pushed before the next.
10. Plan is self-contained.
11. `Read` each step's components from disk before the step.

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | lazy-cycle-containment | P5 | Orchestrator dispatch wiring (C1 callers) | part-2 Phase 2 (`--cycle-begin`/`--cycle-end` CLI) | Solo |
| 2 | lazy-cycle-containment | P6 | Cycle-prompt terminal stop condition (C4) | part-3 Phase 4 (deny-reason wording to align with) | Solo |

## Work Units

- [ ] WU-1 — `--cycle-begin`/`--cycle-end` dispatch bracket across the coupled trio (lazy-batch + lazy-bug-batch + lazy-batch-cloud SKILLs) + State Machine Summary updates + governing-prose reload mirror
- [ ] WU-2 — docs-consistency test: all three orchestrators set begin-before + end-after on every return path
- [ ] WU-3 — cycle-prompt terminal-stop `@section` in cycle-base-prompt.md + mirror to bug/cloud variants
- [ ] WU-4 — projection lint: terminal-stop section present in every projected cycle-prompt variant + size check

---

### Phase: lazy-cycle-containment P5 — Orchestrator dispatch wiring (C1 callers)

**Goal:** Bracket EVERY dispatch in `/lazy-batch`, `/lazy-bug-batch`, `/lazy-batch-cloud` with `--cycle-begin` (immediately before each `Agent` dispatch, real AND meta) and `--cycle-end` (immediately after the return, on EVERY return path: success, halt, error) — coupled-trio mirror.

**Entry criteria:** part-2 Phase 2 complete (`--cycle-begin`/`--cycle-end` CLI exists).

**SPEC.md references:** Technical Design §C1 (callers) + §Coupling/blast radius; Validation Criteria rows "All three orchestrators bracket every dispatch", "governing-prose edit triggers re-read".

#### WU-1 — dispatch bracket across the coupled trio + summary updates + governing-prose reload mirror

**Scope:** PHASES.md P5 deliverables: the bracket in all three SKILLs; State Machine Summary / orchestration-shape updates; AND the bug/cloud mirror of part-1's governing-file reload discipline (part-1 WU-2 authored it only in `lazy-batch/SKILL.md` — mirror it to the twins here so the coupled-trio edits land in one cycle).

**TDD:** no (skill prose; verified by WU-2 docs-consistency test).

**Files to create/modify:**
- `user/skills/lazy-batch/SKILL.md` [VERIFY: test -f user/skills/lazy-batch/SKILL.md]
- `user/skills/lazy-bug-batch/SKILL.md` [VERIFY: test -f user/skills/lazy-bug-batch/SKILL.md]
- `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` [VERIFY: test -f repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md]

**Implementation goal:**
- In each of the three SKILLs, add the `lazy-state.py --cycle-begin --feature-id <id> --nonce <hex> [--kind real|meta]` call IMMEDIATELY before every `Agent` dispatch site — real-skill cycles AND meta-dispatches (input-audit, apply-resolution, recovery, hardening, coherence-recovery, needs-runtime-redispatch). The dispatch sites correspond to the existing dispatch-* prompt components [VERIFY: ls user/skills/_components/lazy-batch-prompts/]. (`lazy-batch-cloud` uses `bug-state.py`? No — it uses `lazy-state.py --cloud`; `lazy-bug-batch` uses `bug-state.py`. Pass the appropriate state script per orchestrator.)
- Add `lazy-state.py --cycle-end` (resp. `bug-state.py --cycle-end` for the bug orchestrator) IMMEDIATELY after the `Agent` returns, on EVERY return path: success, halt (sentinel written), and error. A `--cycle-begin` without a matching `--cycle-end` on a halt/error path would leave the marker set and block the orchestrator's own next ops — clear explicitly on all three paths (the script's self-healing staleness is a crash-only backstop, not a substitute).
- Update each file's State Machine Summary / orchestration-shape block at the bottom so the dispatch table reflects the new begin/end bracket (per the repo `CLAUDE.md` coupled-pair rule).
- Mirror part-1's governing-file reload discipline + new-hook-registration restart surfacing into `lazy-bug-batch/SKILL.md` and `lazy-batch-cloud/SKILL.md` (part-1 authored it only in `lazy-batch/SKILL.md`). Keep the cloud "Differences from /lazy-batch" block accurate — the bracket itself is NOT a divergence (it is identical); only note a divergence if cloud genuinely differs (it should not).
- Intended divergences to preserve (do NOT flatten): the cloud `--cloud` flag, `bug-state.py` vs `lazy-state.py`, and the tabulated cloud-only differences already in `lazy-batch-cloud`'s "Differences" block.

**Spec requirements:** SPEC §C1 — "a new `lazy-state.py --cycle-begin ...` call the orchestrator issues **immediately before** every `Agent` dispatch ... Cleared by `lazy-state.py --cycle-end` ... on **every** return path (success, halt, error)"; SPEC §Coupling — "the coupled trio, mirror per CLAUDE.md".

**Batch:** 1

#### WU-2 — docs-consistency test: begin-before + end-after on every return path

**Scope:** PHASES.md P5 deliverable: "Tests: docs-consistency grep that all three orchestrators set+clear on all return paths."

**TDD:** yes (author the check FIRST as the spec; it fails until WU-1 lands).

**Files to create/modify:**
- `user/scripts/test_lazy_parity.py` (the existing cross-orchestrator parity test module) — add a `--cycle-begin`/`--cycle-end` bracket assertion [VERIFY: test -f user/scripts/test_lazy_parity.py]. (If a dedicated docs-consistency module fits better, add it there; `test_lazy_parity.py` already audits coupled-orchestrator parity, so it is the natural home.)

**Test expectations:**
- Each of the three SKILLs contains at least one `--cycle-begin` and one `--cycle-end`, and the counts are non-zero in all three.
- The three SKILLs are mirrored: the bracket appears at a comparable shape across all three (parity — not a literal char diff, but the same set of dispatch sites bracketed). Reuse the parity-audit approach already in the module.
- Assert that no `--cycle-begin` is authored without a `--cycle-end` on the same dispatch's return paths (a structural check: every dispatch block that opens a `--cycle-begin` also references `--cycle-end` for success/halt/error). A pragmatic implementation: assert the SKILL prose documents `--cycle-end` on "every return path (success, halt, error)" near each bracket.

**Implementation goal:** Check passes against WU-1.

**Spec requirements:** SPEC Validation row "All three orchestrators bracket every dispatch: `--cycle-begin` before + `--cycle-end` after each dispatch on all return paths".

**Batch:** 1

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-2 (check first), then WU-1 | serial (WU-1 is the coupled-trio edit; review diffs all three) | WU-1 edits three SKILLs; WU-2 edits a test module |

---

### Phase: lazy-cycle-containment P6 — Cycle-prompt terminal stop condition (C4)

**Goal:** Append a terminal-stop `@section` to every cycle prompt via the shared template `cycle-base-prompt.md`, telling the subagent its dispatch ends at commit+push+report. Mirrored across feature/bug/cloud prompt variants.

**Entry criteria:** part-3 Phase 4 complete (so the prompt's terminal-stop wording can align with the hook's `permissionDecisionReason`).

**SPEC.md references:** Technical Design §C4 (Cycle-prompt terminal stop condition); Validation Criteria row "Cycle prompt carries the terminal stop section".

#### WU-3 — terminal-stop `@section` in cycle-base-prompt.md + mirror to variants

**Scope:** PHASES.md P6 deliverables: the new `@section`; confirm pickup by `emit_cycle_prompt`; mirror to bug/cloud variants.

**TDD:** no (prompt prose; verified by WU-4 projection lint).

**Files to create/modify:**
- `user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` [VERIFY: test -f user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md]
- Any bug/cloud prompt addenda that carry a SEPARATE prompt body (if the shared template is inherited, no edit needed — confirm) [VERIFY: ls user/skills/_components/lazy-batch-prompts/].

**Implementation goal:**
- Add a new `@section` (terminal stop) to `cycle-base-prompt.md` matching the SPEC's exact wording: "Your dispatch is exactly ONE cycle. After your single skill returns and you have committed + pushed + written your report, STOP. Do NOT run `lazy-state.py`/`bug-state.py` to find or route a next action. Do NOT begin a second feature. Do NOT run `--run-end`/`--run-start`/`--apply-pseudo`/`--enqueue-adhoc`/`dev:kill`/`dev:restart` — those are orchestrator-only and the harness will DENY them in-flight. Routing the next cycle is the orchestrator's job; your job ends at the report." Use the same `@section` syntax the file already uses for its other sections.
- Confirm `emit_cycle_prompt` re-reads `cycle-base-prompt.md` from disk every probe [VERIFY: grep -n "cycle-base-prompt" user/scripts/lazy_core.py] — so the section is auto-live on the next probe (no reload needed; it is an auto-refreshing surface per Phase 1's boundary).
- Mirror to bug/cloud variants: if they inherit the shared template, the section appears automatically (confirm via WU-4 projection). If a variant carries a separate prompt body, add the section there too.

**Spec requirements:** SPEC §C4 — the full terminal-stop section text + "Mirrored across the coupled prompts (feature/bug/cloud) via the shared template".

**Batch:** 2

#### WU-4 — projection lint: terminal-stop section present in every variant + size check

**Scope:** PHASES.md P6 deliverable: "Tests: project-skills.py projection lint + size check."

**TDD:** yes (author the lint assertion FIRST).

**Files to create/modify:**
- `user/scripts/test_project_skills.py` (the existing projection test module) — add an assertion that the terminal-stop section text projects into every cycle-prompt variant [VERIFY: test -f user/scripts/test_project_skills.py].

**Test expectations:**
- Run the projection (`project-skills.py`) and assert the terminal-stop section text (a stable substring, e.g. "Your dispatch is exactly ONE cycle") is present in EVERY projected cycle-prompt variant (feature/bug/cloud).
- Size check: the addition does not push any projected cycle prompt past its budget (assert the projected prompt length stays under the module's existing size ceiling, or under a reasonable absolute cap if none exists).

**Implementation goal:** Lint passes against WU-3.

**Spec requirements:** SPEC Validation row "Cycle prompt carries the terminal stop section: the stop `@section` present in every cycle prompt variant".

**Batch:** 2

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 2     | WU-4 (lint first), then WU-3 | serial | WU-3 edits the prompt component; WU-4 edits a test module |

## Execution Protocol

This protocol governs the autonomous execution of every phase in this part. Follow it exactly.

### Phase Selection Loop

Repeat until both phases are complete or a blocking issue triggers early exit:

1. **Select ready phase:** Phase 5 ready (entry: part-2 Phase 2 done). Phase 6 ready after Phase 5 (and references part-3 Phase 4's deny wording).
2. **Announce:** Print "Implementing lazy-cycle-containment Phase N: [title]"
3. **Review prior context:** Re-read PHASES.md prior Implementation Notes (esp. the marker schema + the hook deny-reason wording).
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps.**
6. **Report:** Print "lazy-cycle-containment Phase N — committed as [hash]"
7. **Loop:** Re-evaluate readiness; return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow it exactly.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `source-reread.md`. Re-read PHASES.md (current phase + prior notes), SPEC.md (§C1 / §C4 / §Coupling), and this plan. For Phase 5, also re-read all three coupled SKILLs in full before editing (the coupling rule).

#### Step B.1: Implement (prose edits — orchestrator may Edit directly; or INLINE per override)
This part is prose (`.md`). Per the EXECUTION MODEL exception, the orchestrator MAY `Edit` skill/component prose directly. For any non-prose change (the test modules in WU-2/WU-4 are `.py` source), dispatch via Agent in default mode (or INLINE per the override), test-first.

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `subagent-review.md`. For Phase 5: DIFF all three coupled SKILLs and confirm the bracket shape is mirrored (the coupling rule is the primary review focus). Produce a verdict.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `phases-update.md`. Check off the phase's Deliverables (NOT Runtime Verification rows). Tick this plan's `- [ ] WU-N` rows.

#### Step B.4: Run Quality Gates (MANDATORY)
Read `quality-gates.md`. Run `python3 -m pytest user/scripts/test_lazy_parity.py user/scripts/test_project_skills.py`, `python3 ~/.claude/scripts/lint-skills.py --check-projected`, `python3 ~/.claude/scripts/project-skills.py`. 100% pass.

#### Step B.4.5: MCP Integration Test
N/A — `**MCP runtime:** not-required`. Skip with a note.

#### Step B.5: Commit Batch
Commit policy. Atomic gate+commit to `main`.

#### Step B.6: Proceed to Next Batch
Checklist: review verdict (incl. coupled-trio diff for Phase 5); PHASES.md updated; gates pass; committed+pushed.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY)
Read `integration-verification.md`. For Phase 5: grep all three SKILLs and confirm begin/end pairing. For Phase 6: run `project-skills.py` and grep the projected prompts for the terminal-stop text.

#### Update CLAUDE.md Files (MANDATORY)
Read `claude-md-review.md`. The root `CLAUDE.md` documents the coupled pairs / orchestration shape — if the dispatch bracket changes the State Machine Summary the doc references, update it.

#### Commit and Push Post-Phase Changes
Per the commit policy. Push to `main`.

## Blocking Issue Protocol

If blocked: stop; commit+push completed work; print a blocking-issue report; do not work around it. Blocking issues: a coupled-trio mirror that cannot be reconciled; QG failure unfixable after 2 retries; push conflict unresolvable by rebase; an error requiring architectural decisions beyond the specs.

## Completion

When both phases are complete: run the full QG suite once more; print a completion report (Features / Phases / Commits / Final QG / Commit log / Notes summary).

**Completion (gate-owned):** the `__mark_complete__` gate owns the SPEC/PHASES `**Status:**` flip + `COMPLETED.md` — never a plan WU.
