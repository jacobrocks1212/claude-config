---
kind: implementation-plan
feature_id: lazy-cycle-containment
status: In-progress
created: 2026-06-15
complexity: complex
phases: [7, 8, 9]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-15.
> To execute: `/execute-plan docs/features/lazy-cycle-containment/plans/all-phases-lazy-cycle-containment-part-5.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 5 of 5. Sibling parts:
> - part 1: `docs/features/lazy-cycle-containment/plans/all-phases-lazy-cycle-containment-part-1.md`
> - part 2: `docs/features/lazy-cycle-containment/plans/all-phases-lazy-cycle-containment-part-2.md`
> - part 3: `docs/features/lazy-cycle-containment/plans/all-phases-lazy-cycle-containment-part-3.md`
> - part 4: `docs/features/lazy-cycle-containment/plans/all-phases-lazy-cycle-containment-part-4.md`
> - part 5: `docs/features/lazy-cycle-containment/plans/all-phases-lazy-cycle-containment-part-5.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — lazy-cycle-containment (Phases 7–9)  (v2)

**PHASES.md files:**
- `docs/features/lazy-cycle-containment/PHASES.md` (lazy-cycle-containment, 9 phases)

**SPEC.md files:**
- `docs/features/lazy-cycle-containment/SPEC.md`

**Total phases:** 9 (this part: Phases 7, 8, 9 — the final part)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`, and skill/component prose (`.md`). Test modules (`.py`) are source — dispatch via Agent (or INLINE per override), test-first.

> **INLINE-EXECUTION OVERRIDE (when run by a `/lazy-batch` cycle subagent lacking the `Agent` tool):** perform ALL implementation INLINE with `Read`/`Edit`/`Write`, test-first. Never invoke `/lazy` or `/lazy-batch` recursively.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path. **Before executing each step**, `Read` the listed component files from disk. Do NOT proceed from memory. After compaction, re-read this plan first.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this plan has no completed hard upstream dependencies) | — | — | The R-V-1 + ledger fixes derive from the claude-config `lazy-pipeline-visualizer` retro and the AlgoBooth overview retro (evidence in SPEC §Research References), not a queued feature. |

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. Implementation/test work via Agent where `.py` source is touched (prose `.md` may be edited directly). INLINE override applies when no `Agent` tool.
2. Edits in the current worktree — never create worktrees.
3. Every TDD work unit is test-first.
4. PHASES.md updated AFTER EACH batch.
5. Every edit reviewed before continuing.
6. Mistakes fixed immediately before the next batch.
7. Integration verification after all batches.
8. CLAUDE.md updated per phase if warranted.
9. Phase committed+pushed before the next.
10. Plan is self-contained.
11. `Read` each step's components from disk before the step.

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | lazy-cycle-containment | P7 | Recovery-dispatch scope hardening (C5) | — | Solo |
| 2 | lazy-cycle-containment | P8 | R-O-9 retro rule + force-cap (C6) | — | Solo |
| 3 | lazy-cycle-containment | P9 | Secondary voice/ledger fixes (C7) | — | Solo |

(Phases 7, 8, 9 are mutually independent; execute in order for clean commits.)

## Work Units

- [ ] WU-1 — recovery grep-and-cite gate in dispatch-recovery.md + recovery emit + docs-consistency test (Phase 7)
- [ ] WU-2 — R-O-9 rule (§4a) + force-cap (§5c) in lazy-batch-retro/SKILL.md (Phase 8)
- [ ] WU-3 — R-O-9 fixture self-test: multi-feature single dispatch → fail + force-cap (Phase 8)
- [ ] WU-4 — R-V-1 reinforcement in orchestrator-voice.md + plan-feature Decision-Classification Ledger requirement (Phase 9)
- [ ] WU-5 — docs-consistency tests for the R-V-1 seams + plan-feature ledger requirement (Phase 9)

---

### Phase: lazy-cycle-containment P7 — Recovery-dispatch scope hardening (C5)

**Goal:** Make the recovery subagent's "tick ONLY with on-disk evidence" prose self-enforcing — require a `grep`-and-cite for `VALIDATED.md` / `MCP_TEST_RESULTS.md` covering a Runtime-Verification row before ticking it.

**Entry criteria:** None (independent prose hardening).

**SPEC.md references:** Technical Design §C5 (Recovery-dispatch scope hardening).

#### WU-1 — recovery grep-and-cite gate + emit reflection + test

**Scope:** PHASES.md P7 deliverables: the grep-and-cite gate in `dispatch-recovery.md`; reflect it in the recovery emit; docs-consistency test.

**TDD:** partial (prose change + a docs-consistency assertion; author the assertion as the check).

**Files to create/modify:**
- `user/skills/_components/lazy-batch-prompts/dispatch-recovery.md` [VERIFY: test -f user/skills/_components/lazy-batch-prompts/dispatch-recovery.md]
- The recovery-emit prose (wherever the recovery prompt is composed — likely `lazy_core.py`'s emit path or the orchestrator SKILL) [VERIFY: grep -rn "dispatch-recovery\|recovery" user/scripts/lazy_core.py].
- `user/scripts/test_project_skills.py` or `test_lazy_core.py` — a docs-consistency assertion that the gate text is present.

**Implementation goal:**
- In `dispatch-recovery.md`, require: before ticking ANY Runtime-Verification row, the recovery subagent MUST `grep` for `VALIDATED.md` / `MCP_TEST_RESULTS.md` (or the equivalent on-disk evidence) covering that row; tick ONLY on a hit and CITE the evidence path; if absent, leave the box UNticked and report it. Make the existing "tick ONLY with on-disk evidence" prose grep-and-cite self-enforcing.
- Reflect the requirement in the recovery emit so the dispatched recovery subagent receives it (if the recovery prompt is assembled in `lazy_core.py`, ensure the assembled prompt carries the gate; if it's purely the component, confirm the component is injected into the recovery dispatch).
- Add a docs-consistency assertion that `dispatch-recovery.md` contains the grep-and-cite gate text.

**Spec requirements:** SPEC §C5 — "The recovery subagent MUST `grep` for `VALIDATED.md` / `MCP_TEST_RESULTS.md` covering a Runtime-Verification row before ticking it; if absent, leave the box unticked and report it. Make the existing 'tick ONLY with on-disk evidence' prose self-enforcing (grep-and-cite) ...".

**Batch:** 1

> | Batch | Work Units | Parallel? | File Conflicts? | 
> |-------|-----------|-----------|-----------------| 
> | 1     | WU-1 | Solo | none |

---

### Phase: lazy-cycle-containment P8 — R-O-9 retro rule + force-cap (C6)

**Goal:** Add R-O-9 (single-cycle containment) to `lazy-batch-retro/SKILL.md` §4a and a hard force-cap to §5c — detect runaways from git+jsonl (always available even when transcripts are reclaimed).

**Entry criteria:** None (independent retro-rule addition).

**SPEC.md references:** Technical Design §C6 (R-O-9 retro rule); Validation Criteria row "R-O-9 force-caps a runaway".

#### WU-2 — R-O-9 rule (§4a) + force-cap (§5c)

**Scope:** PHASES.md P8 deliverables: R-O-9 in §4a; force-cap in §5c; documentation that R-O-9 complements (not replaces) R-EP-1/2.

**TDD:** no (skill prose; WU-3 holds the fixture self-test).

**Files to create/modify:**
- `user/skills/lazy-batch-retro/SKILL.md` [VERIFY: test -f user/skills/lazy-batch-retro/SKILL.md] — add R-O-9 to §4a and the force-cap to §5c [VERIFY: grep -n "§4a\|## 4a\|R-EP-1\|R-O-\|§5c\|force-cap" user/skills/lazy-batch-retro/SKILL.md].

**Implementation goal:**
- §4a: add **R-O-9 (single-cycle containment)** — define the metric: from `git log <window>` + the parent jsonl dispatch list (both always available even when `/tmp` transcripts are reclaimed), compute commits-per-dispatch and features-per-dispatch.
- §5c: add a **hard force-cap** when a single dispatch touches >1 feature OR calls a run-lifecycle command (`--run-end`/`--apply-pseudo`/`--enqueue-adhoc`/`dev:kill`) → grade `fail` + force-cap.
- Document that R-O-9 is the git+jsonl-keyed detection (always available) that complements R-EP-1/2, which invert under the inline-override branch and cannot see a runaway.

**Spec requirements:** SPEC §C6 — the full R-O-9 definition + force-cap criteria.

**Batch:** 2

#### WU-3 — R-O-9 fixture self-test

**Scope:** PHASES.md P8 deliverable: "Tests: a retro self-test / fixture over a multi-feature single dispatch → grade fail + force-cap."

**TDD:** yes (write FIRST).

**Files to create/modify:**
- A retro self-test fixture + assertion. If `lazy-batch-retro` has no existing test harness, add a small `user/scripts/test_retro_ro9.py` (or fold into an existing retro test) that exercises the R-O-9 logic against a synthetic git-log + jsonl-dispatch sample [VERIFY: ls user/scripts/test_*.py].

**Test expectations:**
- A synthetic fixture: a git log + jsonl dispatch list where ONE dispatch's commit window spans 2 different feature dirs (and/or contains a `--run-end` call) → the R-O-9 logic flags it `fail` + force-cap.
- A clean fixture (each dispatch touches exactly 1 feature, no lifecycle commands) → no R-O-9 force-cap.
- If R-O-9 is purely SKILL prose (no extractable Python function), the test asserts the rule + force-cap text is present in §4a/§5c (docs-consistency) and the fixture documents the expected grading a human/retro-cycle would apply. Prefer extracting the commits-per-dispatch / features-per-dispatch computation into a testable helper if one is cheap to add.

**Implementation goal:** Self-test passes against WU-2's rule.

**Spec requirements:** SPEC Validation row "R-O-9 force-caps a runaway: grade `fail` + force-cap from git+jsonl".

**Batch:** 2

> | Batch | Work Units | Parallel? | File Conflicts? | 
> |-------|-----------|-----------|-----------------| 
> | 2     | WU-3 (test first), then WU-2 | serial | WU-2 edits the SKILL; WU-3 a test/fixture |

---

### Phase: lazy-cycle-containment P9 — Secondary voice/ledger fixes (C7)

**Goal:** R-V-1 mechanics-silent reinforcement in `orchestrator-voice.md` + the missing `plan-feature` Decision-Classification Ledger requirement in `plan-feature/SKILL.md`.

**Entry criteria:** part-1 Phase 1 complete (so `orchestrator-voice.md` — a governing-file-set member — is re-read by the running orchestrator after this edit, the SPEC's payoff for sequencing C8 first).

**SPEC.md references:** Technical Design §C7 (Secondary voice/ledger fixes); Validation Criteria rows "plan-feature emits the ledger", and the R-V-1 reinforcement.

#### WU-4 — R-V-1 reinforcement + plan-feature ledger requirement

**Scope:** PHASES.md P9 deliverables: the four reinforced seams + examples in `orchestrator-voice.md`'s hard-bans list; the `### Decision-Classification Ledger` requirement in `plan-feature/SKILL.md`.

**TDD:** no (prose; WU-5 holds the docs-consistency tests).

**Files to create/modify:**
- `user/skills/_components/orchestrator-voice.md` [VERIFY: test -f user/skills/_components/orchestrator-voice.md] [VERIFY: grep -n "hard-ban\|silent\|R-V-1\|mechanic" user/skills/_components/orchestrator-voice.md].
- `user/skills/plan-feature/SKILL.md` [VERIFY: test -f user/skills/plan-feature/SKILL.md].

**Implementation goal:**
- `orchestrator-voice.md`: tighten the silent-mechanics rule at the observed recurring seams — (1) run-start narration, (2) "Running the {ledger} guard." post-return lines, (3) marker-confirm ("the marker confirms forward_cycles=…"), (4) narrated file reads ("Reading the resolution handler"). Add all four to the hard-bans list WITH concrete examples of the banned phrasing.
- `plan-feature/SKILL.md`: require the cycle to emit the structured `### Decision-Classification Ledger` that `/spec --batch` mandates — add it to the Step 3 summary requirements so the input-audit at Step 1d.5 is not the only safety net. Reference the same ledger shape `/spec --batch` uses.

**Spec requirements:** SPEC §C7 — "R-V-1 reinforcement ... Add these to the hard-bans list with examples." and "`plan-feature` Decision-Classification Ledger ... require the cycle to emit the structured `### Decision-Classification Ledger` that `/spec --batch` mandates".

**Batch:** 3

#### WU-5 — docs-consistency tests for R-V-1 seams + plan-feature ledger requirement

**Scope:** PHASES.md P9 deliverable: "Tests: docs-consistency grep that orchestrator-voice.md hard-bans carry the four seams with examples; that plan-feature/SKILL.md requires the ledger."

**TDD:** yes (write FIRST).

**Files to create/modify:**
- `user/scripts/test_project_skills.py` or `test_lint_skills.py` — add assertions [VERIFY: ls user/scripts/test_project_skills.py user/scripts/test_lint_skills.py].

**Test expectations:**
- `orchestrator-voice.md`'s hard-bans section contains the four new seams (run-start narration, ledger-guard post-return line, marker-confirm, narrated file reads), each with an example.
- `plan-feature/SKILL.md` requires the `### Decision-Classification Ledger` in its return summary (assert the requirement text + the literal `### Decision-Classification Ledger` token).

**Implementation goal:** Tests pass against WU-4.

**Spec requirements:** SPEC Validation row "plan-feature emits the ledger" + the R-V-1 reinforcement.

**Batch:** 3

> | Batch | Work Units | Parallel? | File Conflicts? | 
> |-------|-----------|-----------|-----------------| 
> | 3     | WU-5 (tests first), then WU-4 | serial | WU-4 edits two prose files; WU-5 a test module |

## Execution Protocol

This protocol governs the autonomous execution of every phase in this part. Follow it exactly.

### Phase Selection Loop

Repeat until all three phases are complete or a blocking issue triggers early exit:

1. **Select ready phase:** Phases 7, 8, 9 are all independent — execute in order (7 → 8 → 9) for clean commits.
2. **Announce:** Print "Implementing lazy-cycle-containment Phase N: [title]"
3. **Review prior context:** Re-read PHASES.md prior Implementation Notes.
4. **Execute all batches** per the Per-Batch Steps below (TDD: failing tests/checks first).
5. **Run Post-Phase Steps.**
6. **Report:** Print "lazy-cycle-containment Phase N — committed as [hash]"
7. **Loop:** Re-evaluate; return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow it exactly.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `source-reread.md`. Re-read PHASES.md (current phase + prior notes), SPEC.md (§C5 / §C6 / §C7), and this plan.

#### Step B.1: Implement (prose edits directly; `.py` tests via Agent or INLINE per override)
Per the EXECUTION MODEL exception, prose `.md` edits may be made directly; `.py` test modules go through Agent (or INLINE per override), test-first. For TDD WUs read `tdd-protocol.md`, `subagent-launch.md`. **POST-DISPATCH GATE** afterward.

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `subagent-review.md`. Produce a verdict.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `phases-update.md`. Check off the phase's Deliverables (NOT Runtime Verification rows). Tick this plan's `- [ ] WU-N` rows.

#### Step B.4: Run Quality Gates (MANDATORY)
Read `quality-gates.md`. Run `python3 -m pytest user/scripts/` (the relevant test modules), `python3 ~/.claude/scripts/lint-skills.py --check-projected`, `python3 ~/.claude/scripts/project-skills.py`. 100% pass.

#### Step B.4.5: MCP Integration Test
N/A — `**MCP runtime:** not-required`. Skip with a note.

#### Step B.5: Commit Batch
Commit policy. Atomic gate+commit to `main`.

#### Step B.6: Proceed to Next Batch
Checklist: review verdict; PHASES.md updated; gates pass; committed+pushed.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY)
Read `integration-verification.md`. Phase 7: grep `dispatch-recovery.md` for the gate. Phase 8: run the R-O-9 self-test. Phase 9: grep both prose files + run the docs-consistency tests.

#### Update CLAUDE.md Files (MANDATORY)
Read `claude-md-review.md`. The root `CLAUDE.md` documents components (`orchestrator-voice.md`) and skills (`plan-feature`, `lazy-batch-retro`) — update the descriptions if the new rules warrant it.

#### Commit and Push Post-Phase Changes
Per the commit policy. Push to `main`.

### FINAL-PART COMPLETION NOTE (Phase 9 is the last phase of the feature)

When Phase 9's work lands and all 9 phases' Deliverables are checked off across all five parts, the IMPLEMENTATION of the feature is done — validation is still pending. Per the pipeline status-honesty rule:
- Set the **top-level PHASES.md `**Status:**` to `In-progress`** (NOT Complete) — implementation done, validation pending.
- Do NOT flip SPEC.md `**Status:**` and do NOT write `COMPLETED.md` — those are owned EXCLUSIVELY by the `__mark_complete__` gate after the validation tail (`/mcp-test` → coverage audit). For this `not-required` MCP feature, the tail resolves via the mcp-test cycle's skip/validate decision; let the state machine route there.

## Blocking Issue Protocol

If blocked: stop; commit+push completed work; print a blocking-issue report; do not work around it. Blocking issues: subagent failure unfixable after 2 retries; QG failure unfixable after 2 retries; push conflict unresolvable by rebase; an error requiring architectural decisions beyond the specs.

## Completion

When all three phases are complete: run the full QG suite once more; print a completion report (Features / Phases / Commits / Final QG / Commit log / Notes summary). Set top-level PHASES.md `**Status:**` to `In-progress` per the final-part completion note above.

**Completion (gate-owned):** the `__mark_complete__` gate owns the SPEC/PHASES `**Status:** Complete` flip + `COMPLETED.md` — never a plan WU.
