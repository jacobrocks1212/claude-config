---
kind: implementation-plan
feature_id: plan-skills-redesign
status: Ready
created: 2026-06-29
complexity: complex
phases: [5, 6]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-29.
> To execute: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-5.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 5 of 5. Sibling parts:
> - part 1: `...\plans\all-phases-plan-skills-redesign-part-1.md`
> - part 2: `...\plans\all-phases-plan-skills-redesign-part-2.md`
> - part 3: `...\plans\all-phases-plan-skills-redesign-part-3.md`
> - part 4: `...\plans\all-phases-plan-skills-redesign-part-4.md`
> - part 5: `...\plans\all-phases-plan-skills-redesign-part-5.md` (this file)
> (Parent dir: `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\`)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — plan-skills-redesign (Phases 5 + 6 — D4 parallelism + D5 ground-truth gate)  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\PHASES.md` (plan-skills-redesign, Phases 5 & 6 of 6)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\SPEC.md`

**Total phases (this part):** 2 (Phases 5 and 6 — packed; both depend only on Phase 2, no cross-edge between them)
**Plan version:** v2 (reference-based)

**Entry prerequisite:** Part 2 (Phase 2) complete — `execution-contract.md` exists (Phases 5 and 6 both add rules to it). Parts 3 and 4 also precede this by series order. If Phase 2 is not complete, STOP.

---

## REPO & EDIT CONTEXT — READ THIS FIRST (harness-internals work)

- **Real targets only** under `C:\Users\JacobMadsen\source\repos\claude-config\`; `~/.claude` / `<repo>/.claude` are symlinks — Edit/Write refuse symlink writes. Edit real `claude-config\...` paths.
- **Quality gates (Python + projection, NOT msbuild):** `project-skills.py`; `lint-skills.py`; `setup.ps1 check`; `pytest test_project_skills.py`. Re-run `pytest test_lazy_core.py` after edits as cheap insurance.
- **Commit in `claude-config`** (`cd` first); personal identity → push allowed.
- **Dogfooding note:** Phase 3 (part 3) already flipped the writer — this part's Implementation Notes go to the sibling `IMPLEMENTATION_NOTES.md`. Follow `phases-update.md` as-is.
- **Dirty-tree:** `git -C "...claude-config" status --porcelain` before Batch 1; checkpoint unrelated dirt.

---

## EXECUTION MODEL — READ THIS FIRST

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch, review, run gates, update tracking | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL skill/component edits | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** No orchestrator `Edit`/`Write` on skill/component files. Only this feature's `PHASES.md`/`CLAUDE.md` directly.

**Dispatch pattern:** `Agent({ description, model: "sonnet", prompt: "<full self-contained context>" })`

## COMPONENT LOADING PROTOCOL

Before each step, `Read` the listed components from disk. After compaction, re-read this plan first.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | (Python/projection) |
| Step B.5 | Commit Policy | `~/.claude/skills/_components/commit-and-push.md` (commit in `claude-config`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| plan-skills-redesign P2 (prior part) | hard | `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\PHASES.md` | `execution-contract.md` (authored in P2 with anticipated D4/D5 sections) is where Phases 5 & 6 add their rules |
| plan-skills-redesign P1 (prior part) | hard | (same PHASES.md) | WU-2 ensures `write-plan-cognito` authors disjoint-WU batches |

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. ALL edits delegated to Sonnet subagents; orchestrator never `Edit`/`Write`s skill/component files (exception: trivial PASS-WITH-FIXES).
2. Edits in the current `claude-config` checkout — no worktrees.
3. (No TDD WUs — prose/policy edits + an end-to-end verification WU.)
4. PHASES.md updated after each batch.
5. Every subagent output reviewed before continuing.
6. Mistakes fixed immediately before the next batch.
7. Integration verification after each phase.
8. CLAUDE.md updated after the phases if warranted.
9. Each phase committed in `claude-config` before this part ends.
10. Self-contained plan — follow exactly.
11. Before each step, `Read` the listed components from disk.
12. Edit real `claude-config\...` paths, never symlinked paths.

---

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | plan-skills-redesign | P5 | Executor parallelism + background builds (D4) | Phase 2 | Batch 1 |
| 2 | plan-skills-redesign | P6 | Lighten the ground-truth gate (D5) | Phase 2 | Batches 2–4 |

Phases 5 and 6 are independent of each other (both depend only on Phase 2). Execute Phase 5's batch, then Phase 6's batches.

---

## Work Units

- [x] WU-1 — (P5) Encode same-message disjoint batching + background builds + constraint guard
- [x] WU-2 — (P5) Ensure `write-plan-cognito` authors disjoint-WU batches the executor can exploit
- [x] WU-3 — (P6) Default per-WU cheap integrity checks + assertion-vs-intent read; tests only on mismatch
- [ ] WU-4 — (P6) 529 inline-fallback; keep review hybrid/scope-gated
- [ ] WU-5 — (P6) Final non-regression gate: run `/write-plan` + `/write-plan-cognito` end-to-end; capture evidence

---

### Phase: plan-skills-redesign P5 — Executor parallelism + background builds (D4)

**Goal:** Exploit the harness's only real-parallelism path (same-message Agent blocks) and background the build spine.

**Entry criteria:** Phase 2 — the rules live in `execution-contract.md`.

**SPEC.md references:** §D4; Validation rows "Disjoint WUs run concurrently" and "Long builds backgrounded with overlap".

#### WU-1 — Encode same-message disjoint batching + background builds + constraint guard

- **Scope (PHASES deliverables):**
  - Encode same-message file-disjoint batching in `execution-contract.md` / `subagent-launch.md`: provably disjoint WUs dispatched as multiple `Agent` blocks in one assistant message.
  - Encode background builds: long / Tier-2 / typegen builds run `run_in_background: true`; next independent agent dispatched while the build runs (settle OQ4 — which builds count as Tier-2/typegen — against the queue skills).
  - Constraint guard: a backgrounded build's output must not be consumed by a dependent agent before completion — enforced by disjoint-file + seam-classification rules, not new queue machinery.
- **TDD:** no (policy prose in components).
- **Files to create/modify:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\_components\execution-contract.md` (the D4 section authored-with-anticipation in Phase 2) [VERIFY: `grep -n "Parallel\|background\|run_in_background\|disjoint" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/_components/execution-contract.md"`]
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\_components\subagent-launch.md` (Build Concurrency rule + same-message disjoint batching) [VERIFY: `grep -n "Concurrency\|background\|same message\|disjoint" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/_components/subagent-launch.md"`]
- **Implementation goal:** Encode the contract: file-disjoint WUs are dispatched as multiple `Agent` blocks in ONE assistant message (the harness's only real-parallelism path; the existing file-overlap rule already guarantees disjoint-WU safety). Long / Tier-2 / typegen builds run `run_in_background: true` and the orchestrator dispatches the next independent agent while the build runs. Settle OQ4 by reading the queue skills to define the precise long-build signature set [VERIFY: `ls "C:/Users/JacobMadsen/source/repos/claude-config/repos/cognito-forms/.claude/skills/msbuild/SKILL.md" "C:/Users/JacobMadsen/source/repos/claude-config/repos/cognito-forms/.claude/skills/nxbuild/SKILL.md"`] — which builds count as Tier-2/typegen (e.g. full-solution `/msbuild`, server-typegen, Nx full builds). Encode the constraint guard as a rule: a backgrounded build's output is never consumed by a dependent agent before completion, enforced by the disjoint-file + seam-classification rules (no new queue machinery). Target ~1.5–2× wall-clock per phase, not unbounded fan-out — state this explicitly.
- **Spec requirements:** SPEC §D4 (all three bullets).
- **Batch:** 1

#### WU-2 — Ensure `write-plan-cognito` authors disjoint-WU batches the executor can exploit

- **Scope (PHASES deliverable):** Ensure `/write-plan-cognito` authors disjoint-WU batches the executor can exploit (seam classification gates what is parallelizable).
- **TDD:** no.
- **Files to create/modify:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\repos\cognito-forms\.claude\skills\write-plan-cognito\SKILL.md` (renamed in part 1) [VERIFY: `ls "C:/Users/JacobMadsen/source/repos/claude-config/repos/cognito-forms/.claude/skills/write-plan-cognito/SKILL.md"`]
- **Implementation goal:** Confirm/extend the Cognito lane planner so its partitioning emits provably file-disjoint WU batches with seam classification (BE/FE lanes) explicit enough that the D4-aware executor can dispatch them in one message. The planner already partitions into lanes (the touchpoint table cites lane partitioning Step 2.5 + tiered gates); ensure the batch output makes disjointness machine-evident (each WU's files listed; same-batch WUs proven non-overlapping) so the executor's same-message batching rule (WU-1) can fire. Do not change lane semantics — just make disjointness explicit and align the planner's batch shape with the executor contract.
- **Spec requirements:** SPEC §D4 "the existing file-overlap rule already guarantees disjoint-WU safety; seam classification gates what is disjoint."
- **Batch:** 1 (disjoint from WU-1's component files).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 (P5) | WU-1, WU-2 | Yes | None (components vs. write-plan-cognito) |

---

### Phase: plan-skills-redesign P6 — Lighten the ground-truth gate (D5)

**Goal:** Stop re-running the full suite per WU (0/16 catch rate); make the cheap integrity + assertion read the default.

**Entry criteria:** Phase 2 — gate policy lives in `execution-contract.md` / `subagent-review.md`.

**SPEC.md references:** §D5; Validation rows "GT gate cheap by default", "529 inline-fallback", and "Healthy parts not regressed".

#### WU-3 — Default per-WU cheap integrity checks + assertion-vs-intent read

- **Scope (PHASES deliverable):** Default per-WU verification: `git status` / `wc -l` / `grep -n` integrity checks + the assertion-vs-intent read. Re-run tests **only on integrity mismatch**.
- **TDD:** no.
- **Files to create/modify:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\_components\execution-contract.md` (the D5 gate section authored-with-anticipation in Phase 2) [VERIFY: `grep -n "verification\|ground-truth\|gate\|full suite" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/_components/execution-contract.md"`]
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\_components\subagent-review.md` (ground-truth gate L33–62) [VERIFY: `grep -n "git status\|full suite\|GROUND-TRUTH\|assertion" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/_components/subagent-review.md"`]
- **Implementation goal:** Make the per-WU verification default to cheap integrity checks (`git status` / `wc -l` / `grep -n`) PLUS the assertion-vs-intent read (the only mechanism that caught the one real defect in the corpus — keep it mandatory). Re-run the test suite ONLY when an integrity check disagrees with the subagent's report. The full-suite re-run becomes conditional, not default. Encode this in both the contract (policy home) and `subagent-review.md` (the gate mechanics).
- **Spec requirements:** SPEC §D5 "Default per WU: cheap integrity checks + the assertion-vs-intent read. Re-run tests only on integrity mismatch."
- **Batch:** 2 (overlaps WU-1 on `execution-contract.md` → must follow Phase 5's batch).

#### WU-4 — 529 inline-fallback; keep review hybrid/scope-gated

- **Scope (PHASES deliverables):**
  - 529 inline-fallback: after 1–2 failed review-subagent dispatch attempts (API 529), fall back to inline review immediately (fixes the ~16-min retry burn).
  - Keep the review mechanism hybrid/scope-gated (small batches inline, larger via review subagent at the orchestrator's model).
- **TDD:** no.
- **Files to create/modify:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\_components\subagent-review.md` (review dispatch + scope-gating) [VERIFY: `grep -n "inline\|review subagent\|529\|scope" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/_components/subagent-review.md"`]
- **Implementation goal:** Add a 1–2-strike inline-fallback fast-path: if dispatching the review subagent hits API 529 once or twice, fall back to inline review immediately rather than retrying for minutes. Preserve the existing healthy hybrid/scope-gated behavior (small batches reviewed inline; larger batches via a review subagent at the orchestrator's model). Do not weaken the substantive review — only the retry-on-529 burn changes.
- **Spec requirements:** SPEC §D5 "529 inline-fallback: after 1–2 failed review-subagent dispatch attempts, fall back to inline review immediately."
- **Batch:** 3 (overlaps WU-3 on `subagent-review.md` → must follow WU-3).

#### WU-5 — Final non-regression gate: run `/write-plan` + `/write-plan-cognito` end-to-end; capture evidence

- **Scope (PHASES deliverable):** Final non-regression gate (owns SPEC "Healthy parts not regressed", §Validation): run `/write-plan` (and `/write-plan-cognito`) end-to-end and confirm the healthy parts all still fire — partitioning, pre-draft `[VERIFY:]` anchor gates, dirty-tree handling, pre-dispatch drift reconciliation, Tasks recovery anchor, typegen seam, right-sized builds. Capture the pre-draft gate output + a generated plan as evidence.
- **TDD:** no — this is an in-session end-to-end verification WU (closeable by `/execute-plan`: invoke the redesigned planners against a small throwaway/test feature, capture output as evidence; no live Tauri/MCP runtime gate involved).
- **Files to create/modify:**
  - None (verification only). Evidence captured into this feature's `IMPLEMENTATION_NOTES.md` (or a `plans/`-adjacent evidence note).
- **Implementation goal:** Drive the redesigned generic `/write-plan` and `/write-plan-cognito` against a minimal target (a tiny throwaway PHASES.md, or a dry-run mode) and assert from the transcript that every healthy part still fires: partitioning + part-split decision, the pre-draft `[VERIFY:]` anchor-existence gate, dirty-tree handling, pre-dispatch drift reconciliation, the Tasks recovery anchor, the typegen seam (Cognito planner), and right-sized builds. Capture the pre-draft gate output and a generated plan as evidence. This is the terminal check because the healthy-parts surface is only fully wired once D1–D5 have all landed; running it earlier would not exercise the post-redesign planners end-to-end.
- **Spec requirements:** SPEC §Validation "Healthy parts not regressed"; §D-determinism "Do not regress the healthy parts (§4)."
- **Batch:** 4 (terminal — after all policy edits land so the end-to-end run exercises the final state).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 2 (P6) | WU-3 | Solo | overlaps `execution-contract.md` with WU-1 → after Batch 1 |
| 3 (P6) | WU-4 | Solo | overlaps `subagent-review.md` with WU-3 → after Batch 2 |
| 4 (P6) | WU-5 | Solo | verification only; terminal |

---

## Execution Protocol

### Phase Selection Loop

Execute Phase 5 (Batch 1: WU-1+WU-2 in one message), then Phase 6 (Batch 2: WU-3; Batch 3: WU-4; Batch 4: WU-5). Run Post-Phase Steps after Phase 5 and again after Phase 6. Then Completion.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)

Read `~/.claude/skills/_components/task-tracking.md`. Create tasks WU-1..WU-5.

### Per-Batch Steps (apply to every batch)

#### Step B.0: Re-read Source Documents (MANDATORY)

Read `~/.claude/skills/_components/source-reread.md`. Re-read the relevant PHASES.md phase slice (P5 or P6), SPEC §D4/§D5, this plan.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS)

**PRE-FLIGHT CHECK:** `Agent`/`model: "sonnet"` for ALL edits; no direct edits.

Read: `~/.claude/skills/_components/{subagent-launch,implementation-agent}.md`.

Batch 1: WU-1 + WU-2 in one message (disjoint). Batches 2, 3, 4 are solo (sequential file overlaps on `execution-contract.md` then `subagent-review.md`; WU-5 is terminal verification).

**Every subagent prompt MUST state:** edit real `claude-config\...` paths; harness config; for WU-1/WU-3 extend the D4/D5 sections Phase 2 anticipated in `execution-contract.md` (do not restructure the contract); for WU-2 do not change Cognito lane semantics; for WU-5 it is verification-only (no source edits) and must capture evidence.

**POST-DISPATCH GATE:** verify Agent-only edits (WU-5 produces evidence/notes, not source edits).

#### Step B.2: Review Batch Output (MANDATORY GATE)

Read `~/.claude/skills/_components/subagent-review.md`; structured verdict. For WU-1, confirm the same-message disjoint-batching + background-build rules are unambiguous and the OQ4 build set is settled. For WU-3/WU-4, confirm the cheap-default gate keeps the assertion-vs-intent read mandatory and the 529 fallback fires within 1–2 strikes.

#### Step B.3: Update PHASES.md (MANDATORY)

Read `~/.claude/skills/_components/phases-update.md`. Check off the batch's deliverables; append notes to the sibling `IMPLEMENTATION_NOTES.md`. Record OQ4 (the long-build signature set) under Phase 5's notes.

#### Step B.4: Run Quality Gates (MANDATORY)

Run `project-skills.py`, `lint-skills.py`, `setup.ps1 check`, `pytest test_project_skills.py` (+ `pytest test_lazy_core.py` as insurance). 100% pass.

#### Step B.4.5: MCP Integration Test

Skip (no Tauri/MCP runtime surface — harness-config edits). Record the skip. (Note: WU-5's end-to-end run is a `/write-plan` exercise, not an MCP runtime gate.)

#### Step B.5: Commit Batch

Read `~/.claude/skills/_components/commit-and-push.md`. Commit in `claude-config`.

#### Step B.6: Proceed to Next Batch

Checklist: verdict; PHASES updated; gates green; commit. Then next batch.

### Post-Phase Steps (after Phase 5, and again after Phase 6)

#### Integration Verification (MANDATORY)

Read `~/.claude/skills/_components/integration-verification.md`.
- After Phase 5: confirm (dry-run or transcript) that a phase with ≥2 file-disjoint WUs would be dispatched as multiple `Agent` blocks in ONE message, and a Tier-2/typegen build would be backgrounded with the next independent agent dispatched before it completes.
- After Phase 6: confirm per-WU verification shows cheap checks + assertion read with no full-suite run unless an integrity check disagrees; simulate/inspect a review-subagent 529 → inline fallback within 1–2 strikes. WU-5's captured evidence (healthy parts all fire) is the §Validation "Healthy parts not regressed" closure.

#### Update CLAUDE.md Files (MANDATORY)

Read `~/.claude/skills/_components/claude-md-review.md`. Document the parallelism/background-build and lightened-gate policy if a CLAUDE.md should carry it.

#### Commit and Push Post-Phase Changes

Commit in `claude-config`.

---

## Blocking Issue Protocol

If blocked: stop; commit completed batches in `claude-config`; print the blocked report (Completed / Blocked WU / Reason / Recovery / Remaining); do not work around. Blocking issues: OQ4 long-build set cannot be settled from the queue skills (escalate — design fork); the cheap-default gate cannot be encoded without dropping the assertion-vs-intent read (escalate — that read is non-negotiable); WU-5's end-to-end run reveals a regressed healthy part that cannot be fixed within this part's scope (escalate — it implies a defect in an earlier part).

---

## Completion

When Phases 5 and 6 are complete:

1. Run the full harness gate suite one final time across `claude-config` (`project-skills.py`, `lint-skills.py`, `setup.ps1 check`, `pytest test_project_skills.py`, `pytest test_lazy_core.py`).
2. Print:

   ## Implementation Batch — Complete (Part 5 of 5 — FEATURE COMPLETE)

   **Features implemented:** plan-skills-redesign (all 6 phases via parts 1–5)
   **Phases completed this part:** P5 (parallelism + background builds), P6 (lightened ground-truth gate)
   **Commits:** [hashes]
   **Final gate status:** all green

   **Commit log (this part):**
   | Commit | Phase | Title |
   |--------|-------|-------|
   | … | P5 | Executor parallelism + background builds |
   | … | P6 | Lighten the ground-truth gate |

   **Implementation Notes summary:** OQ4 long-build set; the non-regression evidence (healthy parts all fire under the redesigned `/write-plan` and `/write-plan-cognito`).

3. This is the final part. The plan-skills-redesign feature (D1–D5) is fully implemented. Recommend running `/retro` or `/realign-spec` against the feature dir if the pipeline calls for it.
