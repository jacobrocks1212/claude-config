---
kind: implementation-plan
feature_id: plan-skills-redesign
status: Ready
created: 2026-06-29
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-29.
> To execute: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-1.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 5. Sibling parts:
> - part 1: `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-1.md` (this file)
> - part 2: `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-2.md`
> - part 3: `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-3.md`
> - part 4: `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-4.md`
> - part 5: `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-5.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — plan-skills-redesign (Phase 1 — D1 deterministic planner resolution)  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\PHASES.md` (plan-skills-redesign, Phase 1 of 6)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\SPEC.md`

**Total phases (this part):** 1 (Phase 1)
**Plan version:** v2 (reference-based — components loaded from disk per step)

---

## REPO & EDIT CONTEXT — READ THIS FIRST (harness-internals work)

This feature edits **harness configuration in the `claude-config` repo**, NOT Cognito product code. Consequences that override the generic component defaults:

- **Real targets only.** Every file this plan touches lives under `C:\Users\JacobMadsen\source\repos\claude-config\`. The live `~/.claude/...` and `<repo>/.claude/...` paths are **symlinks into `claude-config`**, and the Edit/Write tools **refuse to write through symlinks**. Subagents MUST edit the real `claude-config\...` paths, never the symlinked locations.
- **Quality gates are Python + projection, NOT `/msbuild`/`/mstest`.** This repo has no .NET/Nx surface. The gate suite for this part:
  - `python ~/.claude/scripts/project-skills.py` — re-projects skills; must complete with no broken `!cat` expansion.
  - `python ~/.claude/scripts/lint-skills.py` — must exit 0.
  - `cd "C:\Users\JacobMadsen\source\repos\claude-config" && powershell -File setup.ps1 check` — symlink integrity; must report clean (run `repair` only if it flags a break).
  - `pytest "C:\Users\JacobMadsen\source\repos\claude-config\user\scripts\test_project_skills.py" -q` — projection round-trip.
  - When a WU touches `user/scripts/*.py`, also run `pytest "C:\Users\JacobMadsen\source\repos\claude-config\user\scripts\test_lazy_core.py" -q`.
  - **Ignore the Cognito-worktree raw-`dotnet`/`npx` block hook** — it does not apply; there is nothing to build here.
- **Commit context.** Changes are committed in the **`claude-config` repo** (these files are not tracked by any host repo's git). The executing session must `cd "C:\Users\JacobMadsen\source\repos\claude-config"` before any `git add`/`git commit`. claude-config is a personal-identity repo — push is allowed (no work-repo push hook). After all WUs in this part pass review + gates, commit on the current `claude-config` branch.
- **Dirty-tree note:** before launching Batch 1, run `git -C "C:\Users\JacobMadsen\source\repos\claude-config" status --porcelain`. If dirty with unrelated work, checkpoint it (`git add -A && git commit -m "chore: checkpoint before plan-skills-redesign Phase 1"`) so this part's diff is attributable. If dirty only with this feature's docs (SPEC/PHASES/plans), that is expected — leave it.

---

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and config edits | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on skill/script/config files. If you are about to modify a `.md` (skill), `.py`, `.psd1`, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: this feature's `PHASES.md` and `CLAUDE.md`.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | (this part — see REPO & EDIT CONTEXT; Python/projection, not msbuild) |
| Step B.5 | Commit Policy | `~/.claude/skills/_components/commit-and-push.md` (commit in `claude-config`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

This plan has no completed hard upstream dependencies. (none — this plan has no completed hard upstream dependencies.)

The authoritative context for this part is the feature's own SPEC.md (§D1) and PHASES.md (Phase 1 + Touchpoint Audit table). The executing session SHOULD `Read` both before WU-1.

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on skill/script files. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines).
2. All subagent edits happen in the current `claude-config` checkout — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in a phase finish, integration verification confirms all changes work together.
8. Relevant CLAUDE.md files are created/updated after the phase if changes warrant it.
9. The completed phase is committed (in `claude-config`) before this part ends.
10. This plan is self-contained — follow it exactly as written without relying on external context.
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
12. **Edit real `claude-config\...` paths, never symlinked `~/.claude` / `<repo>/.claude` paths** (Edit refuses symlink writes).

---

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | plan-skills-redesign | P1 | Rename the Cognito planner + lock resolution (D1) | — | Batches below |

This part covers exactly one phase. Execute its batches in order.

---

## Work Units

- [x] WU-1 — Rename Cognito planner dir + frontmatter + briefing self-ref + symlink re-check
- [x] WU-2 — Pin & edit pipeline dispatch sites to emit `write-plan-cognito` for Cognito
- [ ] WU-3 — Strip personal-project residue from the Cognito planner
- [ ] WU-4 — Projection/lint assertions: `write-plan-cognito` resolves; no `execute-plan-cognito`; catalog audit

---

### Phase: plan-skills-redesign P1 — Rename the Cognito planner + lock resolution (D1)

**Goal:** Make the advertised planner the one that runs. Rename the only colliding skill so the Cognito lane planner stops being shadowed by the user-level generic, and make the pipeline emit the renamed name.

**Entry criteria:** None — independent, lowest risk, executed first.

**SPEC.md references:** §D1 (Deterministic planner resolution); Validation rows "Advertised planner is the one that runs" and "One generic executor, no Cognito fork".

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2 | Yes      | None (disjoint files) |
| 2     | WU-3, WU-4 | Yes      | None (WU-3 → renamed SKILL.md; WU-4 → lint files); both depend on WU-1 |

#### WU-1 — Rename Cognito planner dir + frontmatter + briefing self-ref + symlink re-check

- **Scope (PHASES deliverables):**
  - Rename `repos/cognito-forms/.claude/skills/write-plan/` → `write-plan-cognito/` and set frontmatter `name: write-plan-cognito`.
  - Update the `lane-agent-briefing.md` self-reference path inside the renamed SKILL.md (`.claude/skills/write-plan/` → `.claude/skills/write-plan-cognito/`).
  - Run `setup.ps1 check` (and `repair` if needed) to confirm the directory symlink re-resolves.
- **TDD:** no (mechanical rename + path fix + symlink verification; no logic).
- **Files to create/modify:**
  - Rename dir: `C:\Users\JacobMadsen\source\repos\claude-config\repos\cognito-forms\.claude\skills\write-plan\` → `...\write-plan-cognito\` [VERIFY: `ls "C:/Users/JacobMadsen/source/repos/claude-config/repos/cognito-forms/.claude/skills/write-plan/SKILL.md"`]
  - `...\write-plan-cognito\SKILL.md` (frontmatter `name:` + internal `lane-agent-briefing.md` path refs)
  - `...\write-plan-cognito\lane-agent-briefing.md` (moves with the dir) [VERIFY: `ls "C:/Users/JacobMadsen/source/repos/claude-config/repos/cognito-forms/.claude/skills/write-plan/lane-agent-briefing.md"`]
  - `C:\Users\JacobMadsen\source\repos\claude-config\manifest.psd1` — directory-level symlink entry; verify `DotClaudeDirs` includes `skills` so the rename is transparent [VERIFY: `grep -n "DotClaudeDirs" "C:/Users/JacobMadsen/source/repos/claude-config/manifest.psd1"`]
- **Implementation goal:** Use `git -C "...claude-config" mv` to rename the directory (preserve history). Inside the renamed `SKILL.md`, set `name: write-plan-cognito` in frontmatter and replace every occurrence of the self-referential path segment `.claude/skills/write-plan/` with `.claude/skills/write-plan-cognito/` (the SKILL.md references `lane-agent-briefing.md` by its repo-relative path — [VERIFY: `grep -rn "skills/write-plan/lane-agent-briefing" "C:/Users/JacobMadsen/source/repos/claude-config/repos/cognito-forms/.claude/skills/write-plan/SKILL.md"`]). Because `manifest.psd1` symlinks `.claude/skills` at the directory level (`DotClaudeDirs = @('skill-config','skills','knowledge')`), the rename is transparent and needs no manifest edit — but confirm by running `setup.ps1 check`. If it flags a break, run `setup.ps1 repair`.
- **Spec requirements:** SPEC §D1 — "Rename the colliding Cognito planner to a distinct name — `/write-plan-cognito`. Distinct names do not collide, so both `/write-plan` (generic) and `/write-plan-cognito` register and are independently invocable."
- **Batch:** 1

#### WU-2 — Pin & edit pipeline dispatch sites to emit `write-plan-cognito` for Cognito

- **Scope (PHASES deliverables):**
  - **Discovery (gates the edit):** pin the exact `lazy-state.py` branch that emits the planner `sub_skill` string, and the dispatch sites in `/lazy` Step 6 and `plan-feature`.
  - Edit the pinned dispatch site(s) so Cognito repos emit `write-plan-cognito`; confirm `/lazy` Step 7 still targets generic `/execute-plan`.
- **TDD:** no for the prose edits; **yes** if `lazy-state.py`'s selection is unit-testable — if the planner-name emission is a pure function with existing coverage in `test_lazy_core.py` or a sibling, add a case asserting Cognito context → `write-plan-cognito`. If it is not cleanly unit-testable, record that and rely on the dispatch-emission transcript assertion (testing strategy below).
- **Files to create/modify:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\scripts\lazy-state.py` — the branch emitting the planner `sub_skill` [VERIFY: `grep -n "write-plan\|sub_skill\|planner" "C:/Users/JacobMadsen/source/repos/claude-config/user/scripts/lazy-state.py"`]
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\lazy\SKILL.md` — Step 6 planner dispatch (Step 7 unchanged) [VERIFY: `grep -n "write-plan" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/lazy/SKILL.md"`]
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\plan-feature\SKILL.md` — `/spec-phases + /write-plan` composition [VERIFY: `grep -n "write-plan" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/plan-feature/SKILL.md"`]
- **Implementation goal:** First DISCOVER: grep each file for the planner-name emission/dispatch site and pin the exact line/branch (the Explore sweep confirmed `lazy-state.py` emits a `sub_skill` but did not pin the deciding branch — close that gap before editing). Then edit so that **in a Cognito-repo context** the emitted/dispatched planner name is `write-plan-cognito`, while non-Cognito contexts keep emitting the generic `write-plan`. Confirm `/lazy` Step 7 (executor stage) still dispatches the generic `/execute-plan` — do NOT introduce an `execute-plan-cognito`. If the Cognito-context condition does not already exist in `lazy-state.py`, add it using the existing repo-detection mechanism the script already uses (do not invent a new one — [VERIFY: `grep -n "Cognito\|cognito\|repo_root\|repo_name" "C:/Users/JacobMadsen/source/repos/claude-config/user/scripts/lazy-state.py"`]).
- **Spec requirements:** SPEC §Downstream Coupling "Pipeline dispatch" — "the lazy pipeline (`/lazy` Step 6) dispatches the Cognito planner by name; it must emit `/write-plan-cognito` for Cognito repos (Phase 1). Step 7 already dispatches the generic `/execute-plan` and is unaffected."
- **Batch:** 1

#### WU-3 — Strip personal-project residue from the Cognito planner

- **Scope (PHASES deliverable):** Strip personal-project residue (Tauri, MCP-validation, `/lazy-batch` Step 9 references) from the Cognito planner.
- **TDD:** no (prose deletion in a skill file).
- **Files to create/modify:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\repos\cognito-forms\.claude\skills\write-plan-cognito\SKILL.md` (the renamed file from WU-1)
- **Implementation goal:** Remove sections/prose that target personal Tauri/MCP-validation workflows and `/lazy-batch` Step-9 (live MCP runtime-validation) references — none of which apply to Cognito Forms. Preserve all Cognito lane behavior (BE/FE lane partitioning, tiered gates, typegen seam, `lane-agent-briefing.md` references). [VERIFY: `grep -rni "tauri\|mcp-test\|mcp-validation\|lazy-batch.*step 9\|VALIDATED.md" "C:/Users/JacobMadsen/source/repos/claude-config/repos/cognito-forms/.claude/skills/write-plan/SKILL.md"`] (run against the pre-rename path if WU-1 has not yet landed in your verification context; against `write-plan-cognito/` after). Do not remove anything load-bearing for Cognito; when a reference is ambiguous, leave it and note it for review.
- **Spec requirements:** SPEC §D1 "Also: strip personal-project residue (Tauri, MCP-validation, `/lazy-batch` Step 9 references) from the Cognito planner."
- **Batch:** 2 (depends on WU-1 rename; shares `write-plan-cognito/SKILL.md` with WU-1 so must follow it).

#### WU-4 — Projection/lint assertions: `write-plan-cognito` resolves; no `execute-plan-cognito`; catalog audit

- **Scope (PHASES deliverables):**
  - Add a projection/lint assertion that `/write-plan-cognito` exists and resolves (no same-name collision remains).
  - Add a lint/skills-dir assertion that **no `execute-plan-cognito` exists** (closes the SPEC "One generic executor, no Cognito fork" negative invariant).
  - Discovery: confirm whether any catalog/menu advertises the bare `write-plan` name; update to `write-plan-cognito` if found; otherwise record that discovery is symlink-based (no catalog edit needed).
- **TDD:** yes — the two assertions are testable. Prefer extending the existing lint/projection test surface.
- **Files to create/modify:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\scripts\lint-skills.py` (add the positive + negative assertions) [VERIFY: `ls "C:/Users/JacobMadsen/source/repos/claude-config/user/scripts/lint-skills.py"`]
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\scripts\test_project_skills.py` (or a sibling test file — add coverage for both assertions) [VERIFY: `ls "C:/Users/JacobMadsen/source/repos/claude-config/user/scripts/test_project_skills.py"`]
  - `C:\Users\JacobMadsen\source\repos\claude-config\repos\cognito-forms\.claude\skill-config\skill-catalog.md` (edit ONLY if the catalog advertises a bare `write-plan` — the touchpoint sweep found no such ref; confirm) [VERIFY: `grep -rni "write-plan" "C:/Users/JacobMadsen/source/repos/claude-config/repos/cognito-forms/.claude/skill-config/skill-catalog.md"`]
- **Implementation goal:** Add a lint rule (and matching test) asserting (a) a skill named `write-plan-cognito` resolves under `repos/cognito-forms/.claude/skills/` with no same-name collision against the user-level generic, and (b) NO skill or directory named `execute-plan-cognito` exists anywhere under `repos/*/.claude/skills/` or `user/skills/`. For the catalog audit: grep the Cognito skill-catalog for any `write-plan` advertisement; if present, point it at `write-plan-cognito`; if absent (expected), record in the Implementation Notes that discovery is symlink-based and no catalog edit was needed.
- **Spec requirements:** SPEC Validation rows "Advertised planner is the one that runs" (both invocable, no shadowing) and "One generic executor, no Cognito fork" (no `/execute-plan-cognito` exists).
- **Batch:** 2 (depends on WU-1 — the assertion needs the renamed skill present; disjoint from WU-3's file).

---

## Execution Protocol

This protocol governs autonomous execution. Follow it exactly.

### Phase Selection Loop

This part has one phase. Execute Batch 1, then Batch 2, per the Per-Batch Steps. Then run Post-Phase Steps. Then proceed to Completion.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly. Create one task per WU (WU-1..WU-4) and update status through test/impl phases.

### Per-Batch Steps

For each batch (Batch 1: WU-1+WU-2; Batch 2: WU-3+WU-4):

#### Step B.0: Re-read Source Documents (MANDATORY)

Read `~/.claude/skills/_components/source-reread.md` and follow it. Re-read from disk: this part's PHASES.md slice (Phase 1 + Touchpoint Audit table) and the SPEC.md §D1, plus this plan file. Do NOT rely on cached content.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — ZERO INLINE IMPLEMENTATION)

**PRE-FLIGHT CHECK:** Confirm (1) you will use `Agent` with `model: "sonnet"` for ALL edits, (2) you will NOT call `Edit`/`Write` on skill/script files yourself.

Read before proceeding: `~/.claude/skills/_components/tdd-protocol.md`, `~/.claude/skills/_components/subagent-launch.md`, `~/.claude/skills/_components/tdd-test-agent.md`, `~/.claude/skills/_components/implementation-agent.md`.

For Batch 1, dispatch WU-1 and WU-2 as **two `Agent` blocks in a single assistant message** (file-disjoint — the harness runs them concurrently). For Batch 2, dispatch WU-3 and WU-4 likewise.

**Every subagent prompt MUST state:** edit only real `claude-config\...` paths (never symlinked `~/.claude` / `<repo>/.claude`); this is harness config, not Cognito code; gates are Python/projection not msbuild; verify each cited `[VERIFY:]` anchor with the given grep before relying on it.

**POST-DISPATCH GATE:** After subagents complete, verify you composed `Agent` calls and did not edit files directly.

#### Step B.2: Review Batch Output (MANDATORY GATE)

Read `~/.claude/skills/_components/subagent-review.md` and `~/.claude/skills/_components/mount-site-verification.md`; follow the full protocol to a structured verdict (PASS / PASS-WITH-FIXES / NEEDS-REWORK). For WU-1 specifically, verify the rename preserved git history (`git -C "...claude-config" log --follow` resolves) and the symlink re-resolved (`setup.ps1 check` clean).

#### Step B.3: Update PHASES.md (MANDATORY)

Read `~/.claude/skills/_components/phases-update.md` and follow it. Check off the completed Phase 1 deliverables in the feature PHASES.md and append an Implementation Notes block (date, work done, the pinned `lazy-state.py` dispatch branch, integration notes, pitfalls, files modified). **Note:** until Phase 3 (part 3) flips the writer, notes still embed in PHASES.md — that is correct for this part.

#### Step B.4: Run Quality Gates (MANDATORY)

Run the harness gate suite from REPO & EDIT CONTEXT: `project-skills.py`, `lint-skills.py`, `setup.ps1 check`, `pytest test_project_skills.py`, and (for WU-2/WU-4 Python touches) `pytest test_lazy_core.py`. 100% pass required before proceeding. If a WU added a new `!cat` reference, confirm `project-skills.py` expands it.

#### Step B.4.5: MCP Integration Test

Skip — this part produces no Tauri/MCP runtime surface (harness-config edits only). Record the skip with a one-line note.

#### Step B.5: Commit Batch

Read `~/.claude/skills/_components/commit-and-push.md`. Commit in the **`claude-config`** repo: `cd "C:\Users\JacobMadsen\source\repos\claude-config"`, stage only this part's changed files, commit. claude-config is personal-identity → push is allowed if the policy says so.

#### Step B.6: Proceed to Next Batch

Checklist (all true): review verdict produced; PHASES.md updated; gates green; commit done. Then launch the next batch.

### Post-Phase Steps (after Batch 2)

#### Integration Verification (MANDATORY)

Read `~/.claude/skills/_components/integration-verification.md`. Confirm cross-WU integration: the renamed skill resolves, the dispatch edit emits `write-plan-cognito` in a Cognito context, and the lint assertions pass together. **Dispatch assertion (not just resolution):** confirm a Cognito-repo pipeline dispatch *emits* `write-plan-cognito` — capture it from a `/lazy`/`lazy-state.py` dry-run or transcript. Resolution alone does not prove routing.

#### Update CLAUDE.md Files (MANDATORY)

Read `~/.claude/skills/_components/claude-md-review.md`. If the rename changes guidance, update the relevant `claude-config` CLAUDE.md (e.g. mention `/write-plan-cognito` as the Cognito planner).

#### Commit and Push Post-Phase Changes

Commit any post-phase doc/CLAUDE.md changes in `claude-config`.

---

## Blocking Issue Protocol

If a blocking issue is encountered:

1. Stop all in-progress work; do not launch new subagents.
2. Commit any completed batches in `claude-config`.
3. Print a blocking-issue report:

   ## Implementation Batch — Blocked

   **Completed:** [WUs/batches with commit hashes]
   **Blocked WU:** [WU-N: title]
   **Reason:** [specific]
   **Recovery suggestion:** [what the user should do]
   **Remaining (not attempted):** [list]

4. Do not work around the blocker.

Blocking issues include: a subagent failure unfixable after 2 retries; a quality-gate failure unfixable after 2 retries (e.g. `setup.ps1 check` will not repair, or `lint-skills.py` cannot be made green); the `lazy-state.py` dispatch branch cannot be located after a thorough discovery sweep (escalate — the routing edit is load-bearing); any error requiring architectural decisions beyond the spec.

---

## Completion

When Phase 1 is complete:

1. Run the full harness gate suite one final time (`project-skills.py`, `lint-skills.py`, `setup.ps1 check`, both `pytest` targets).
2. Print a completion report:

   ## Implementation Batch — Complete (Part 1 of 5)

   **Phase completed:** plan-skills-redesign P1 — Rename the Cognito planner + lock resolution (D1)
   **Commits:** [hashes]
   **Final gate status:** all green
   **Key notes:** the pinned `lazy-state.py` dispatch branch; whether a catalog edit was needed; confirmation no `execute-plan-cognito` exists.

3. Then proceed to **part 2**: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-2.md`
