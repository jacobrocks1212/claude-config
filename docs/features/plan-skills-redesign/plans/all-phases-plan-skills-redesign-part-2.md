---
kind: implementation-plan
feature_id: plan-skills-redesign
status: Ready
created: 2026-06-29
complexity: complex
phases: [2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-29.
> To execute: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-2.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 5. Sibling parts:
> - part 1: `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-1.md`
> - part 2: `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-2.md` (this file)
> - part 3: `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-3.md`
> - part 4: `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-4.md`
> - part 5: `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-5.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — plan-skills-redesign (Phase 2 — D2 single-source execution policy)  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\PHASES.md` (plan-skills-redesign, Phase 2 of 6)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\SPEC.md`

**Total phases (this part):** 1 (Phase 2)
**Plan version:** v2 (reference-based — components loaded from disk per step)

**Entry prerequisite:** Part 1 (Phase 1) complete — `write-plan-cognito/SKILL.md` must exist (WU-4 here edits it). If part 1 is not complete, STOP and run it first.

---

## REPO & EDIT CONTEXT — READ THIS FIRST (harness-internals work)

This feature edits **harness configuration in the `claude-config` repo**, NOT Cognito product code. Consequences that override generic component defaults:

- **Real targets only.** Every file lives under `C:\Users\JacobMadsen\source\repos\claude-config\`. The live `~/.claude/...` and `<repo>/.claude/...` paths are **symlinks into `claude-config`**, and Edit/Write **refuse to write through symlinks**. Subagents MUST edit the real `claude-config\...` paths.
- **Quality gates are Python + projection, NOT `/msbuild`/`/mstest`:**
  - `python ~/.claude/scripts/project-skills.py` (re-projects; no broken `!cat`)
  - `python ~/.claude/scripts/lint-skills.py` (exit 0)
  - `cd "C:\Users\JacobMadsen\source\repos\claude-config" && powershell -File setup.ps1 check`
  - `pytest "C:\Users\JacobMadsen\source\repos\claude-config\user\scripts\test_project_skills.py" -q`
- **Commit context:** commit in the **`claude-config`** repo (`cd` there first). Personal identity → push allowed.
- **Dirty-tree note:** run `git -C "C:\Users\JacobMadsen\source\repos\claude-config" status --porcelain` before Batch 1; checkpoint unrelated dirt; feature docs being dirty is expected.

---

## EXECUTION MODEL — READ THIS FIRST

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL skill/config edits | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit`/`Write` on skill/script/config files. The ONLY files you may modify directly: this feature's `PHASES.md` and `CLAUDE.md`.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

Before executing each step, `Read` the component files listed for that step from disk. Do NOT proceed from memory. After compaction, re-read this plan first, then load components.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | (this part — Python/projection, see REPO & EDIT CONTEXT) |
| Step B.5 | Commit Policy | `~/.claude/skills/_components/commit-and-push.md` (commit in `claude-config`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| plan-skills-redesign P1 (same feature, prior part) | hard | `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\PHASES.md` | WU-4 edits `write-plan-cognito/SKILL.md` renamed in Phase 1; the pointer change must match P1's name |

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. ALL implementation work MUST be delegated to Sonnet subagents via the Agent tool — orchestrator MUST NOT call `Edit`/`Write` on skill/script files. Exception: trivial PASS-WITH-FIXES (a few lines).
2. All subagent edits happen in the current `claude-config` checkout — NEVER create worktrees.
3. Every TDD work unit goes through the test-first pipeline.
4. PHASES.md is updated AFTER EACH batch completes.
5. Every subagent's output is reviewed before continuing.
6. Mistakes are fixed immediately before the next batch.
7. Integration verification confirms all changes work together after the phase.
8. CLAUDE.md files updated after the phase if warranted.
9. The completed phase is committed (in `claude-config`) before this part ends.
10. Self-contained plan — follow exactly.
11. Before each step, `Read` the listed component files from disk.
12. Edit real `claude-config\...` paths, never symlinked paths.

---

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | plan-skills-redesign | P2 | Single-source execution policy (D2) | Phase 1 (rename) | Batches below |

---

## Work Units

- [ ] WU-1 — Author `_components/execution-contract.md` (extract execution policy)
- [ ] WU-2 — Refactor generic `execute-plan/SKILL.md` to reference + read the contract
- [ ] WU-3 — Refactor generic `write-plan/SKILL.md` to emit a one-line pointer (stop emitting boilerplate)
- [ ] WU-4 — Apply the pointer change to `write-plan-cognito/SKILL.md`; verify projection/lint round-trip

---

### Phase: plan-skills-redesign P2 — Single-source execution policy (D2)

**Goal:** Policy lives in one component; plans carry a pointer, not ~150–190 verbatim lines.

**Entry criteria:** Phase 1 complete (`write-plan-cognito/SKILL.md` exists — WU-4 edits it).

**SPEC.md references:** §D2 (Single-source execution policy); Validation rows "Plans carry no duplicated policy" and "Single-source policy".

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | N/A (creates the contract everything else points at) |
| 2     | WU-2, WU-3, WU-4 | Yes | None (three different SKILL.md files); all depend on WU-1 |

#### WU-1 — Author `_components/execution-contract.md`

- **Scope (PHASES deliverable):** Author `user/skills/_components/execution-contract.md` extracting: EXECUTION MODEL, COMPONENT LOADING PROTOCOL, MANDATORY RULES, Execution Protocol / Phase-Selection Loop / per-batch steps, Blocking Issue Protocol, Completion, Work Log — sourced from the generic `write-plan` boilerplate (SKILL.md L252–554).
- **TDD:** no (authoring a prose component; validated by projection/lint, not unit tests).
- **Files to create/modify:**
  - **CREATE** `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\_components\execution-contract.md` (net-new — confirmed absent) [VERIFY: net-new — `ls "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/_components/execution-contract.md"` returns no file today]
- **Implementation goal:** Extract the verbatim execution-policy sections that today are duplicated into every generated plan and re-stated in the generic write-plan skill (the EXECUTION MODEL table, COMPONENT LOADING PROTOCOL, MANDATORY RULES, Execution Protocol with the Phase-Selection Loop and per-batch Steps B.0–B.6, Blocking Issue Protocol, Completion, Work Log) into one canonical component. Source text from the generic write-plan skill body [VERIFY: `grep -n "EXECUTION MODEL\|COMPONENT LOADING PROTOCOL\|MANDATORY RULES\|Blocking Issue Protocol\|## Completion" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/write-plan/SKILL.md"`]. **Structure it with anticipated homes for D4 (parallelism) and D5 (gate) rules** added in part 5 — leave clearly-labeled sections (e.g. "Parallelism & background builds", "Per-WU verification gate") so part 5 extends rather than restructures. The component is `!cat`-includable: ensure it round-trips through `project-skills.py`.
- **Spec requirements:** SPEC §D2 — "New shared component `_components/execution-contract.md` holds the execution policy currently duplicated into every plan." PHASES integration note: "becomes the home for the D4 and D5 rules added in phases 5–6 — author its structure with those sections anticipated."
- **Batch:** 1 (solo — WU-2/3/4 all point at this file).

#### WU-2 — Refactor generic `execute-plan/SKILL.md` to reference + read the contract

- **Scope (PHASES deliverable):** Refactor generic `execute-plan/SKILL.md` to reference + read `execution-contract.md` (cacheable).
- **TDD:** no (skill-body refactor; validated by projection/lint + a generated-plan check in Integration Verification).
- **Files to create/modify:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\execute-plan\SKILL.md` [VERIFY: `ls "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/execute-plan/SKILL.md"`]
- **Implementation goal:** Replace the inline execution-policy prose in the executor with a directive to `Read` `~/.claude/skills/_components/execution-contract.md` (or `!cat`-include it where the skill already inlines components). The executor must end up reading the single-sourced contract instead of carrying its own copy. Preserve all executor-specific logic NOT covered by the contract (PHASES.md slice reading — that lands in part 3; the ground-truth gate — refined in part 5; recovery anchors). Do not duplicate; point.
- **Spec requirements:** SPEC §D2 — "The `/execute-plan` skill body references the component by path and reads it (cacheable)."
- **Batch:** 2 (depends on WU-1).

#### WU-3 — Refactor generic `write-plan/SKILL.md` to emit a one-line pointer

- **Scope (PHASES deliverable):** Refactor generic `write-plan/SKILL.md` to stop emitting the boilerplate sections; emit a one-line pointer to `execution-contract.md` instead.
- **TDD:** no.
- **Files to create/modify:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\write-plan\SKILL.md` [VERIFY: `grep -n "Execution model section (write this verbatim)\|MANDATORY RULES section (write this verbatim)\|Component reference card" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/write-plan/SKILL.md"`]
- **Implementation goal:** Change the plan-template instructions in the generic `write-plan` so generated plans carry only unique content (header, references, execution schedule, per-phase work units, batch tables, progress checkboxes) plus a **single one-line pointer** to `execution-contract.md` — NOT the verbatim EXECUTION MODEL / COMPONENT LOADING / MANDATORY RULES / Execution Protocol / Blocking Issue / Completion blocks. Update the "write this verbatim" template sections to instead instruct emitting the pointer. Keep the Component Reference Card if still useful, but it too may be folded into the contract reference. Preserve all pre-draft healthy parts (partitioning, `[VERIFY:]` anchor gate, dirty-tree, touchpoint audit) — those are NOT execution policy and must remain.
- **Spec requirements:** SPEC §D2 — "`/write-plan` stops emitting the boilerplate sections; plans carry only unique content. Net: ~32–44% smaller plans." Validation: a generated plan contains the pointer and NO EXECUTION MODEL / MANDATORY RULES blocks.
- **Batch:** 2 (depends on WU-1; disjoint from WU-2/WU-4).

#### WU-4 — Apply the pointer change to `write-plan-cognito/SKILL.md`; verify projection/lint round-trip

- **Scope (PHASES deliverables):**
  - Apply the same one-line-pointer change to `write-plan-cognito/SKILL.md`.
  - Verify `project-skills.py` expands any new `!cat` reference and `lint-skills.py` / `test_project_skills.py` stay green.
- **TDD:** no for the prose; the round-trip verification is a gate, not a unit test (but if `test_project_skills.py` should assert `execution-contract.md` expands, add that case).
- **Files to create/modify:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\repos\cognito-forms\.claude\skills\write-plan-cognito\SKILL.md` (renamed in part 1) [VERIFY: `ls "C:/Users/JacobMadsen/source/repos/claude-config/repos/cognito-forms/.claude/skills/write-plan-cognito/SKILL.md"`]
  - Possibly `C:\Users\JacobMadsen\source\repos\claude-config\user\scripts\test_project_skills.py` (add a round-trip assertion for `execution-contract.md`)
- **Implementation goal:** Make the Cognito lane planner emit the same one-line pointer to `execution-contract.md` instead of any duplicated execution-policy prose — so Cognito-authored plans are also lean and single-sourced. Then run `project-skills.py` and confirm the new component round-trips with no broken `!cat`; add a `test_project_skills.py` case asserting `execution-contract.md` is projected/expanded if the test surface supports it. Preserve the Cognito lane-specific authoring (BE/FE lanes, tiered gates, typegen seam, `lane-agent-briefing.md`).
- **Spec requirements:** SPEC §D2 + §User-Experience "Plans are lean and policy is single-sourced" — applies to both `/write-plan` and `/write-plan-cognito`.
- **Batch:** 2 (depends on WU-1; disjoint from WU-2/WU-3).

---

## Execution Protocol

### Phase Selection Loop

One phase. Execute Batch 1 (WU-1 solo), then Batch 2 (WU-2+WU-3+WU-4 in one message). Then Post-Phase Steps, then Completion.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)

Read `~/.claude/skills/_components/task-tracking.md`. Create tasks WU-1..WU-4.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)

Read `~/.claude/skills/_components/source-reread.md`. Re-read this part's PHASES.md slice (Phase 2), SPEC §D2, and this plan file.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS)

**PRE-FLIGHT CHECK:** `Agent` with `model: "sonnet"` for ALL edits; no direct `Edit`/`Write`.

Read: `~/.claude/skills/_components/{tdd-protocol,subagent-launch,tdd-test-agent,implementation-agent}.md`.

Batch 1: dispatch WU-1 alone. Batch 2: dispatch WU-2, WU-3, WU-4 as **three `Agent` blocks in one assistant message** (file-disjoint).

**Every subagent prompt MUST state:** edit only real `claude-config\...` paths; harness config not Cognito code; gates are Python/projection; verify each `[VERIFY:]` anchor first; preserve all healthy pre-draft parts and lane-specific behavior — only the execution-policy boilerplate is being single-sourced.

**POST-DISPATCH GATE:** verify Agent-only edits.

#### Step B.2: Review Batch Output (MANDATORY GATE)

Read `~/.claude/skills/_components/{subagent-review,mount-site-verification}.md`; produce a structured verdict. For WU-1, verify `execution-contract.md` actually contains the extracted sections and is `!cat`-clean. For WU-2/3/4, verify each skill now points at the contract and no longer carries the duplicated blocks.

#### Step B.3: Update PHASES.md (MANDATORY)

Read `~/.claude/skills/_components/phases-update.md`. Check off Phase 2 deliverables; append Implementation Notes (date, line-count delta vs an old plan if measurable, files modified). Notes still embed in PHASES.md for this part (writer flips in part 3).

#### Step B.4: Run Quality Gates (MANDATORY)

Run `project-skills.py`, `lint-skills.py`, `setup.ps1 check`, `pytest test_project_skills.py`. The new `!cat` (if any) must expand. 100% pass before proceeding.

#### Step B.4.5: MCP Integration Test

Skip (no runtime surface). Record the skip.

#### Step B.5: Commit Batch

Read `~/.claude/skills/_components/commit-and-push.md`. Commit in `claude-config`.

#### Step B.6: Proceed to Next Batch

Checklist: verdict produced; PHASES.md updated; gates green; commit done.

### Post-Phase Steps

#### Integration Verification (MANDATORY)

Read `~/.claude/skills/_components/integration-verification.md`. **Single-source proof:** generate a throwaway plan via the redesigned generic `/write-plan` (against a tiny test feature or dry-run) and assert it contains the one-line pointer and NO EXECUTION MODEL / MANDATORY RULES blocks; measure the line-count delta vs an existing on-disk plan (target ~32–44% smaller). Then edit one rule in `execution-contract.md` and confirm a subsequent `/execute-plan` reading reflects it without regenerating any plan; revert the throwaway edit.

#### Update CLAUDE.md Files (MANDATORY)

Read `~/.claude/skills/_components/claude-md-review.md`. Note the new single-source component if any CLAUDE.md should reference it.

#### Commit and Push Post-Phase Changes

Commit post-phase changes in `claude-config`.

---

## Blocking Issue Protocol

If blocked: stop; commit completed batches in `claude-config`; print the blocked report (Completed / Blocked WU / Reason / Recovery / Remaining); do not work around it. Blocking issues: subagent or gate failure unfixable after 2 retries; `project-skills.py` cannot expand the new component (broken `!cat`); inability to extract the policy without losing executor-specific logic (escalate — design fork).

---

## Completion

When Phase 2 is complete:

1. Run the full harness gate suite one final time.
2. Print:

   ## Implementation Batch — Complete (Part 2 of 5)

   **Phase completed:** plan-skills-redesign P2 — Single-source execution policy (D2)
   **Commits:** [hashes]
   **Final gate status:** all green
   **Evidence:** measured plan line-count delta; confirmation a contract edit changes behavior with no plan regeneration.

3. Then proceed to **part 3**: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-3.md`
