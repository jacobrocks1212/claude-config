---
kind: implementation-plan
feature_id: plan-skills-redesign
status: Ready
created: 2026-06-29
complexity: complex
phases: [3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-29.
> To execute: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-3.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 3 of 5. Sibling parts:
> - part 1: `...\plans\all-phases-plan-skills-redesign-part-1.md`
> - part 2: `...\plans\all-phases-plan-skills-redesign-part-2.md`
> - part 3: `...\plans\all-phases-plan-skills-redesign-part-3.md` (this file)
> - part 4: `...\plans\all-phases-plan-skills-redesign-part-4.md`
> - part 5: `...\plans\all-phases-plan-skills-redesign-part-5.md`
> (Parent dir: `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\`)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — plan-skills-redesign (Phase 3 — D3 core: thin PHASES.md + sibling notes)  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\PHASES.md` (plan-skills-redesign, Phase 3 of 6)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\SPEC.md`

**Total phases (this part):** 1 (Phase 3)
**Plan version:** v2 (reference-based)

**Entry prerequisite:** None functionally (Phase 3 is independent per PHASES). Run after parts 1–2 only because parts execute in order. This part establishes the tolerant-read pattern that part 4 propagates.

---

## REPO & EDIT CONTEXT — READ THIS FIRST (harness-internals work)

- **Real targets only** under `C:\Users\JacobMadsen\source\repos\claude-config\`; `~/.claude` and `<repo>/.claude` are symlinks — Edit/Write refuse symlink writes. Edit real `claude-config\...` paths.
- **Quality gates (Python + projection, NOT msbuild):** `project-skills.py`; `lint-skills.py`; `setup.ps1 check`; `pytest test_project_skills.py`. **This part touches `lazy_core.py` and `test_lazy_core.py`, so `pytest "C:\Users\JacobMadsen\source\repos\claude-config\user\scripts\test_lazy_core.py" -q` is a primary gate.**
- **Commit in `claude-config`** (`cd` first); personal identity → push allowed.
- **Dogfooding note:** WU-1 flips the shared writer (`phases-update.md`) to a sibling `IMPLEMENTATION_NOTES.md`. AFTER this part lands, this feature's own subsequent Implementation Notes (parts 4–5) go to a sibling `IMPLEMENTATION_NOTES.md` in this feature dir — that is expected and validates the change. Within THIS part, when you reach Step B.3, follow whatever `phases-update.md` says at that moment (pre-flip for batch 1 if WU-1 has not yet committed; post-flip otherwise).
- **Dirty-tree:** `git -C "...claude-config" status --porcelain` before Batch 1; checkpoint unrelated dirt.

---

## EXECUTION MODEL — READ THIS FIRST

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch, review, run gates, update tracking | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL skill/script edits | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** No orchestrator `Edit`/`Write` on skill/script/test files. Only this feature's `PHASES.md`/`CLAUDE.md` may be edited directly.

**Dispatch pattern:** `Agent({ description, model: "sonnet", prompt: "<full self-contained context>" })`

## COMPONENT LOADING PROTOCOL

Before each step, `Read` the listed components from disk. After compaction, re-read this plan first.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | (Python/projection + `pytest test_lazy_core.py`) |
| Step B.5 | Commit Policy | `~/.claude/skills/_components/commit-and-push.md` (commit in `claude-config`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — Phase 3 is independent; no completed hard upstream dependency.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. ALL implementation delegated to Sonnet subagents; orchestrator never `Edit`/`Write`s skill/script files (exception: trivial PASS-WITH-FIXES).
2. Edits in the current `claude-config` checkout — no worktrees.
3. Every TDD work unit goes through the test-first pipeline (WU-3 is TDD).
4. PHASES.md updated after each batch.
5. Every subagent output reviewed before continuing.
6. Mistakes fixed immediately before the next batch.
7. Integration verification after the phase.
8. CLAUDE.md updated after the phase if warranted.
9. Phase committed in `claude-config` before this part ends.
10. Self-contained plan — follow exactly.
11. Before each step, `Read` the listed components from disk.
12. Edit real `claude-config\...` paths, never symlinked paths.

---

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | plan-skills-redesign | P3 | Thin PHASES.md + sibling notes: writer + harness reader (D3 core) | — | Single batch (3 disjoint WUs) |

---

## Work Units

- [x] WU-1 — Flip `_components/phases-update.md` writer → sibling `IMPLEMENTATION_NOTES.md`
- [x] WU-2 — Current-phase-slice + completed-phases-index reads in `execute-plan` + `source-reread.md` (settle OQ2 marker)
- [x] WU-3 — `lazy_core.py::phases_show_implementation()` sibling-then-embedded + `test_lazy_core.py` coverage; verify `/spec-phases` stays thin

---

### Phase: plan-skills-redesign P3 — Thin PHASES.md + sibling notes: writer + harness reader (D3 core)

**Goal:** Stop PHASES.md from growing monotonically; relocate Implementation Notes to a sibling and make the harness gate tolerate the split.

**Entry criteria:** None; establishes the tolerant-read pattern Phase 4 propagates.

**SPEC.md references:** §D3 (Thin PHASES.md + sibling Implementation Notes); Validation rows "PHASES.md stays thin; notes relocated" and "Harness state gate tolerates the split". OQ2 (phase-boundary marker) and OQ3 (one file with per-phase sections — the working default) are settled here/at impl time.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2, WU-3 | Yes | None (phases-update.md / [execute-plan+source-reread.md] / [lazy_core.py+test_lazy_core.py] are disjoint) |

> **Single batch, three `Agent` blocks in one message.** The three WUs touch disjoint files, so dispatch them concurrently. Note: `source-reread.md` is edited again in part 4 (sibling-then-embedded notes read) — that is a LATER part, so no in-part conflict; just be aware part 4 builds on WU-2's version of `source-reread.md` (merge, don't clobber — part 4 enforces this).

#### WU-1 — Flip `_components/phases-update.md` writer → sibling `IMPLEMENTATION_NOTES.md`

- **Scope (PHASES deliverable):** Flip `_components/phases-update.md` to append per-batch Implementation Notes to a sibling `IMPLEMENTATION_NOTES.md` (one file, per-phase sections — OQ3 default), NOT PHASES.md.
- **TDD:** no (the component is prose instructions the executor follows; no executable unit. Behavior is validated by Integration Verification running `/execute-plan` across batches).
- **Files to create/modify:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\_components\phases-update.md` [VERIFY: `grep -n "Implementation Notes\|PHASES.md" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/_components/phases-update.md"`]
- **Implementation goal:** Change the writer so the per-batch Implementation Notes block is appended to a sibling `IMPLEMENTATION_NOTES.md` (located next to the feature's PHASES.md), under a per-phase section heading (e.g. `## Phase N — <title>`), creating the file if absent. PHASES.md continues to receive ONLY the deliverable checkbox ticks (`- [ ]` → `- [x]`) — it stays a thin checklist. Specify the sibling path resolution clearly (sibling of the PHASES.md being updated). Keep the Notes block content identical (date, work done, integration notes, pitfalls, files). Document OQ3 resolution (one file, per-phase sections) inline.
- **Spec requirements:** SPEC §D3 "Writer (one shared seam): the shared `_components/phases-update.md` ... appends per-batch Implementation Notes to a sibling `IMPLEMENTATION_NOTES.md` ... NOT to PHASES.md. PHASES.md stays a thin checklist."
- **Batch:** 1

#### WU-2 — Current-phase-slice + completed-phases-index reads (settle OQ2 marker)

- **Scope (PHASES deliverables):**
  - Implement current-phase-slice + completed-phases-index reads in `execute-plan` / `source-reread.md` (offset/limit on a stable phase-boundary marker — settle OQ2 marker format here).
  - Record the chosen OQ2 phase-boundary marker format as an explicit output (a one-line note in this phase's eventual Implementation Notes) so downstream slice-readers key off a documented marker.
- **TDD:** no (skill/component prose; the read-narrowing is validated by transcript assertion in Integration Verification — offset/limit bounded, not whole-file).
- **Files to create/modify:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\execute-plan\SKILL.md` (startup PHASES.md read) [VERIFY: `grep -n "PHASES.md" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/execute-plan/SKILL.md"`]
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\_components\source-reread.md` (per-batch re-read) [VERIFY: `grep -n "PHASES.md\|Implementation Notes" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/_components/source-reread.md"`]
- **Implementation goal:** Change both the executor's startup read and the `source-reread.md` per-batch re-read so they read ONLY (a) the current-phase slice of PHASES.md (offset/limit anchored on a stable phase-boundary marker) plus (b) a compact completed-phases index — never the whole accumulating file. Settle OQ2 by choosing the phase-boundary marker (the existing `## Phase N —` headings are the natural anchor — confirm PHASES.md uses them consistently: [VERIFY: `grep -n "^## Phase" "C:/Users/JacobMadsen/source/repos/claude-config/docs/features/plan-skills-redesign/PHASES.md"`]) and documenting it. **This file (`source-reread.md`) is also edited in part 4** — author WU-2's slice-read edit as an additive, clearly-scoped section so part 4's sibling-then-embedded notes edit merges cleanly.
- **Spec requirements:** SPEC §D3 "Startup / `source-reread` / compaction-recovery read only the current-phase slice of PHASES.md ... plus a compact completed-phases index — never the whole file."
- **Batch:** 1

#### WU-3 — `phases_show_implementation()` sibling-then-embedded + tests; verify `/spec-phases` stays thin

- **Scope (PHASES deliverables):**
  - Update `lazy_core.py::phases_show_implementation()` to check sibling `IMPLEMENTATION_NOTES.md` first, then fall back to the embedded `## Implementation Notes` heading.
  - Extend `test_lazy_core.py`: add a sibling-file case (notes in `IMPLEMENTATION_NOTES.md`, none embedded → returns True) and keep the legacy embedded case green.
  - Keep `/spec-phases` emitting the thin-checklist PHASES.md shape (verify it does not embed notes at authoring time).
- **TDD:** **yes** — write the failing sibling-file test first, then implement the sibling-then-embedded read.
- **Files to create/modify:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\scripts\lazy_core.py` — `phases_show_implementation()` (currently greps `phases_text` for `_IMPL_NOTES_HEADING_RE`) [VERIFY: `grep -n "def phases_show_implementation\|_IMPL_NOTES_HEADING_RE" "C:/Users/JacobMadsen/source/repos/claude-config/user/scripts/lazy_core.py"`]
- **Test files:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\scripts\test_lazy_core.py` — extend around the existing `test_phases_show_impl_*` cases [VERIFY: `grep -n "phases_show_impl" "C:/Users/JacobMadsen/source/repos/claude-config/user/scripts/test_lazy_core.py"`]
- **Test expectations:** A new test where Implementation Notes live in a sibling `IMPLEMENTATION_NOTES.md` (PHASES.md has none embedded) asserts `phases_show_implementation(...)` returns True. The existing embedded-notes case stays green. A negative case (neither sibling nor embedded) returns False. Because the current signature takes `phases_text: str`, the sibling-aware variant needs a path (or sibling text) parameter — design the signature change so callers in `lazy-state.py`/`lazy_core.py` pass the PHASES.md path (or pre-read sibling text); update all callers [VERIFY: `grep -rn "phases_show_implementation" "C:/Users/JacobMadsen/source/repos/claude-config/user/scripts/"`].
- **Implementation goal:** Make the harness research-gate predicate tolerant: check the sibling `IMPLEMENTATION_NOTES.md` first (any per-phase notes present → True), then fall back to the embedded `## Implementation Notes` heading in PHASES.md (legacy in-flight features). Confirm `remaining_unchecked_are_verification_only()` and `verify_ledger_gate()` are unaffected (they never parse Notes internals — verify by reading them). Separately, verify `/spec-phases` still authors the thin-checklist PHASES.md and does not embed notes at authoring time [VERIFY: `grep -n "Implementation Notes" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/spec-phases/SKILL.md"`] — if it does embed, note it (no fix needed unless it actively writes notes).
- **Spec requirements:** SPEC §D3 "Harness reader (critical): `lazy_core.py::phases_show_implementation()` ... must check the sibling `IMPLEMENTATION_NOTES.md` first, then fall back to the embedded heading — otherwise a relocated-notes feature reads as 'not yet implemented' and gets re-routed to research."
- **Batch:** 1 (disjoint from WU-1/WU-2).

---

## Execution Protocol

### Phase Selection Loop

One phase, one batch. Execute Batch 1 (WU-1+WU-2+WU-3 in one message), then Post-Phase Steps, then Completion.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)

Read `~/.claude/skills/_components/task-tracking.md`. Create tasks WU-1..WU-3.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)

Read `~/.claude/skills/_components/source-reread.md`. Re-read PHASES.md Phase 3 slice, SPEC §D3, this plan.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS)

**PRE-FLIGHT CHECK:** `Agent`/`model: "sonnet"` for ALL edits; no direct edits.

Read: `~/.claude/skills/_components/{tdd-protocol,subagent-launch,tdd-test-agent,implementation-agent}.md`. WU-3 is TDD: dispatch a test agent (failing sibling-file test) then an impl agent per the TDD pipeline. WU-1 and WU-2 are direct-implementation.

Dispatch WU-1, WU-2, and WU-3's pipeline as concurrent `Agent` blocks where file-disjoint (WU-3's test→impl is sequential within itself but runs concurrent to WU-1/WU-2).

**Every subagent prompt MUST state:** edit real `claude-config\...` paths; harness config; gates are `pytest`/projection; verify `[VERIFY:]` anchors; for WU-3, update ALL callers of `phases_show_implementation` if the signature changes; for WU-2, author the slice-read edit so part 4 can merge a sibling-then-embedded notes edit into the same `source-reread.md` without clobbering.

**POST-DISPATCH GATE:** verify Agent-only edits.

#### Step B.2: Review Batch Output (MANDATORY GATE)

Read `~/.claude/skills/_components/{subagent-review,mount-site-verification}.md`; structured verdict. Verify WU-3's tests actually fail before impl and pass after (TDD discipline); verify no caller of `phases_show_implementation` was left on the old signature.

#### Step B.3: Update PHASES.md (MANDATORY)

Read `~/.claude/skills/_components/phases-update.md` AS IT IS ON DISK NOW. If WU-1 has already flipped + committed the writer, notes go to a sibling `IMPLEMENTATION_NOTES.md` next to this feature's PHASES.md (dogfooding — expected). Otherwise notes embed. Record the OQ2 marker format chosen (WU-2) and OQ3 resolution (WU-1) in the notes.

#### Step B.4: Run Quality Gates (MANDATORY)

Run `pytest "C:\Users\JacobMadsen\source\repos\claude-config\user\scripts\test_lazy_core.py" -q` (both notes-shapes green; sibling case + legacy embedded case), `project-skills.py`, `lint-skills.py`, `setup.ps1 check`, `pytest test_project_skills.py`. 100% pass.

#### Step B.4.5: MCP Integration Test

Skip (no runtime surface). Record the skip.

#### Step B.5: Commit Batch

Read `~/.claude/skills/_components/commit-and-push.md`. Commit in `claude-config`.

#### Step B.6: Proceed

Checklist: verdict; PHASES updated; gates green (esp. `test_lazy_core.py`); commit. (One batch — proceed to Post-Phase.)

### Post-Phase Steps

#### Integration Verification (MANDATORY)

Read `~/.claude/skills/_components/integration-verification.md`. **Read-narrowing assertion:** run `/execute-plan` across ≥2 batches against a test feature (or dry-run the startup read) and confirm from the transcript that the PHASES.md read is offset/limit-bounded to the current-phase slice + index — NOT a whole-file read — and that notes land in the sibling `IMPLEMENTATION_NOTES.md` while PHASES.md size stays flat. Confirm `phases_show_implementation()` returns True for a sibling-notes feature so `lazy-state.py` does NOT re-route it to research.

#### Update CLAUDE.md Files (MANDATORY)

Read `~/.claude/skills/_components/claude-md-review.md`. Document the new `IMPLEMENTATION_NOTES.md` sibling convention + the OQ2 marker if any CLAUDE.md should carry it.

#### Commit and Push Post-Phase Changes

Commit in `claude-config`.

---

## Blocking Issue Protocol

If blocked: stop; commit completed work in `claude-config`; print the blocked report (Completed / Blocked WU / Reason / Recovery / Remaining); do not work around. Blocking issues: `test_lazy_core.py` cannot be made green for both shapes after 2 retries; a `phases_show_implementation` caller cannot be migrated without breaking `verify_ledger_gate`/`remaining_unchecked_*` (escalate — this would be a contract regression); inability to settle a stable OQ2 marker (escalate — design fork).

---

## Completion

When Phase 3 is complete:

1. Run the full harness gate suite + `pytest test_lazy_core.py` one final time.
2. Print:

   ## Implementation Batch — Complete (Part 3 of 5)

   **Phase completed:** plan-skills-redesign P3 — Thin PHASES.md + sibling notes (D3 core)
   **Commits:** [hashes]
   **Final gate status:** all green (both notes-shapes)
   **Key outputs:** the OQ2 phase-boundary marker format chosen; confirmation the writer now targets `IMPLEMENTATION_NOTES.md`; confirmation startup reads only the current-phase slice + index.

3. Then proceed to **part 4**: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-4.md`
