---
kind: implementation-plan
feature_id: plan-skills-redesign
status: Ready
created: 2026-06-29
complexity: mechanical
phases: [4]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-29.
> To execute: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-4.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 4 of 5. Sibling parts:
> - part 1: `...\plans\all-phases-plan-skills-redesign-part-1.md`
> - part 2: `...\plans\all-phases-plan-skills-redesign-part-2.md`
> - part 3: `...\plans\all-phases-plan-skills-redesign-part-3.md`
> - part 4: `...\plans\all-phases-plan-skills-redesign-part-4.md` (this file)
> - part 5: `...\plans\all-phases-plan-skills-redesign-part-5.md`
> (Parent dir: `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\`)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — plan-skills-redesign (Phase 4 — propagate the D3 split across generic consumers)  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\PHASES.md` (plan-skills-redesign, Phase 4 of 6)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\SPEC.md`

**Total phases (this part):** 1 (Phase 4)
**Plan version:** v2 (reference-based)
**Complexity tier:** **mechanical** — every WU applies the *already-settled* sibling-then-embedded read-order idiom (defined by Phase 3) to consumer skill prose. No novel design, no algorithm, no cross-boundary wiring. WU-1 authors a shared read-order snippet whose semantics are fully fixed by Phase 3's harness reader; WU-2/WU-3 are pure idiom application + a verification sweep. Tagged mechanical so the `/execute-plan` cycle dispatches on the cheaper tier.

**Entry prerequisite:** Part 3 (Phase 3) complete — the writer is flipped and the tolerant-read pattern + sibling `IMPLEMENTATION_NOTES.md` convention exist. WU-1 builds on Phase 3's `source-reread.md` edit (merge, do not clobber). If part 3 is not complete, STOP and run it first.

---

## REPO & EDIT CONTEXT — READ THIS FIRST (harness-internals work)

- **Real targets only** under `C:\Users\JacobMadsen\source\repos\claude-config\`; `~/.claude` / `<repo>/.claude` are symlinks — Edit/Write refuse symlink writes. Edit real `claude-config\...` paths.
- **Quality gates (Python + projection, NOT msbuild):** `project-skills.py`; `lint-skills.py`; `setup.ps1 check`; `pytest test_project_skills.py`. (No `lazy_core.py` change here, but re-running `pytest test_lazy_core.py` after edits is cheap insurance.)
- **Commit in `claude-config`** (`cd` first); personal identity → push allowed.
- **Dogfooding note:** Phase 3 already flipped the writer, so THIS part's Implementation Notes go to the sibling `IMPLEMENTATION_NOTES.md` in this feature dir (expected). Follow `phases-update.md` as-is at Step B.3.
- **Dirty-tree:** `git -C "...claude-config" status --porcelain` before Batch 1; checkpoint unrelated dirt.

---

## EXECUTION MODEL — READ THIS FIRST

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch, review, run gates, update tracking | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL skill edits | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** No orchestrator `Edit`/`Write` on skill files. Only this feature's `PHASES.md`/`CLAUDE.md` directly.

**Dispatch pattern:** `Agent({ description, model: "sonnet", prompt: "<full self-contained context>" })`

## COMPONENT LOADING PROTOCOL

Before each step, `Read` the listed components from disk. After compaction, re-read this plan first.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | (Python/projection) |
| Step B.5 | Commit Policy | `~/.claude/skills/_components/commit-and-push.md` (commit in `claude-config`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

(No TDD components listed — these are prose/predicate skill edits with no unit-testable surface; correctness is verified by the consumer-trio behavior check in Integration Verification.)

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| plan-skills-redesign P3 (prior part) | hard | `C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\PHASES.md` | Defines the sibling `IMPLEMENTATION_NOTES.md` convention + tolerant-read idiom this part propagates; WU-1 merges with P3's `source-reread.md` edit |

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. ALL edits delegated to Sonnet subagents; orchestrator never `Edit`/`Write`s skill files (exception: trivial PASS-WITH-FIXES).
2. Edits in the current `claude-config` checkout — no worktrees.
3. (No TDD WUs this part — prose/predicate edits.)
4. PHASES.md updated after each batch.
5. Every subagent output reviewed before continuing.
6. Mistakes fixed immediately before the next batch.
7. Integration verification after the phase.
8. CLAUDE.md updated after the phase if warranted.
9. Phase committed in `claude-config` before this part ends.
10. Self-contained plan — follow exactly.
11. Before each step, `Read` the listed components from disk.
12. Edit real `claude-config\...` paths, never symlinked paths.

---

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | plan-skills-redesign | P4 | Propagate the D3 split across generic consumers (D3 blast radius) | Phase 3 | Batches below |

---

## Work Units

- [ ] WU-1 — Author shared sibling-then-embedded read-order snippet; apply to `source-reread.md` (merge with Phase 3)
- [ ] WU-2 — Consumer group A: `add-phase`, `lazy`, `lazy-batch` (sibling-then-embedded reads)
- [ ] WU-3 — Consumer group B + bounding sweep: `realign-spec`, `implement-phase`, `implement-phase-batch`, `spec-phases-batch`, `spec-phases` Step 1.5

---

### Phase: plan-skills-redesign P4 — Propagate the D3 split across generic consumers (D3 blast radius)

**Goal:** Every skill that reads Implementation Notes finds them after relocation. Universal generic-skill edits — **no `-cognito` variants** (locked "universal wins in generic skills" rule).

**Entry criteria:** Phase 3 (writer flipped + tolerant-read pattern established).

**SPEC.md references:** §D3 "Skill readers (tolerant sibling-then-embedded)"; §Downstream Coupling "In scope (Phase 4)"; Validation row "Consumers read relocated notes".

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | N/A (defines the shared snippet WU-2/WU-3 reference; edits `source-reread.md`) |
| 2     | WU-2, WU-3 | Yes      | None (group A and group B touch disjoint skill files) |

#### WU-1 — Author shared sibling-then-embedded read-order snippet; apply to `source-reread.md`

- **Scope (PHASES deliverable):** `source-reread.md` component: sibling-then-embedded read of prior Implementation Notes. Plus author a single shared `_components/` snippet describing the read order so the rule is single-sourced (mirroring D2's philosophy), per the PHASES integration note.
- **TDD:** no.
- **Files to create/modify:**
  - **CREATE** `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\_components\implementation-notes-read-order.md` (net-new shared snippet — name at executor's discretion; this is the single-sourced read-order rule) [VERIFY: net-new — choose a name not already under `_components/`: `ls "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/_components/"`]
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\_components\source-reread.md` (apply the read order; **merge with Phase 3's slice-read edit — do NOT clobber it**) [VERIFY: `grep -n "Implementation Notes\|slice\|offset" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/_components/source-reread.md"`]
- **Implementation goal:** Author one short shared snippet stating the canonical read order for Implementation Notes: **read the sibling `IMPLEMENTATION_NOTES.md` (per-phase sections) when present; fall back to the embedded `## Implementation Notes` heading in PHASES.md otherwise.** Then make `source-reread.md` follow that order for "prior Implementation Notes" reads — preserving Phase 3's current-phase-slice + completed-phases-index reads already in that file (open `source-reread.md`, confirm Phase 3's edit is present, and ADD the sibling-then-embedded notes read as a distinct concern; the two must coexist). Make the snippet `!cat`-includable so WU-2/WU-3 consumers can reference it rather than restating the idiom.
- **Spec requirements:** SPEC §D3 "the shared `source-reread.md` component must read the sibling when present and fall back to the embedded block otherwise"; PHASES integration note "consider a shared `_components/` snippet describing the read order so the rule is single-sourced."
- **Batch:** 1 (solo — WU-2/WU-3 reference the snippet).

#### WU-2 — Consumer group A: `add-phase`, `lazy`, `lazy-batch`

- **Scope (PHASES deliverables):**
  - `add-phase`: mine notes from sibling, fall back to embedded.
  - `lazy`: research-gate predicate checks sibling-then-embedded.
  - `lazy-batch`: ledger/notes scope reads sibling-then-embedded.
- **TDD:** no (skill prose/predicate edits).
- **Files to create/modify:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\add-phase\SKILL.md` (mines notes) [VERIFY: `grep -n "Implementation Notes" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/add-phase/SKILL.md"`]
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\lazy\SKILL.md` (research-gate predicate) [VERIFY: `grep -n "Implementation Notes" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/lazy/SKILL.md"`]
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\lazy-batch\SKILL.md` (ledger/notes scope) [VERIFY: `grep -n "Implementation Notes\|PHASES.md" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/lazy-batch/SKILL.md"`]
- **Implementation goal:** In each skill's Implementation-Notes-mining site, apply the WU-1 sibling-then-embedded read order (reference the shared snippet via `!cat` if the skill format supports it, else restate the order consistently). Each is a GENERIC-skill edit — no `-cognito` variant. Locate the exact mining site by grep before editing; do not broaden scope beyond the notes-read.
- **Spec requirements:** SPEC §D3 "Skill readers (tolerant sibling-then-embedded): `add-phase`, `lazy`, `lazy-batch` ... must read the sibling when present and fall back to the embedded block otherwise."
- **Batch:** 2 (depends on WU-1; disjoint files from WU-3).

#### WU-3 — Consumer group B + bounding sweep: `realign-spec`, `implement-phase`, `implement-phase-batch`, `spec-phases-batch`, `spec-phases` Step 1.5

- **Scope (PHASES deliverables):**
  - `realign-spec`: upstream-drift read of Implementation Notes sibling-then-embedded.
  - `implement-phase` and `implement-phase-batch`: prior-phase Implementation Notes sibling-then-embedded.
  - `spec-phases-batch` and `spec-phases` Step 1.5 (upstream look-back): sibling-then-embedded.
  - **Consumer-set bounding sweep:** before editing, sweep for ALL notes-mining phrasings (not just literal `## Implementation Notes`) to confirm the 8-consumer set is complete; record the sweep result so the set is verified, not assumed.
- **TDD:** no.
- **Files to create/modify:**
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\realign-spec\SKILL.md` [VERIFY: `grep -n "Implementation Notes" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/realign-spec/SKILL.md"`]
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\implement-phase\SKILL.md` [VERIFY: `grep -n "Implementation Notes" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/implement-phase/SKILL.md"`]
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\implement-phase-batch\SKILL.md` [VERIFY: `grep -n "Implementation Notes" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/implement-phase-batch/SKILL.md"`]
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\spec-phases-batch\SKILL.md` [VERIFY: `grep -n "Implementation Notes" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/spec-phases-batch/SKILL.md"`]
  - `C:\Users\JacobMadsen\source\repos\claude-config\user\skills\spec-phases\SKILL.md` (Step 1.5 upstream look-back) [VERIFY: `grep -n "Implementation Notes\|look-back\|Step 1.5" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/spec-phases/SKILL.md"`]
- **Implementation goal:** Apply the WU-1 sibling-then-embedded read order to each skill's notes-mining site (reference the shared snippet where possible). **Run the bounding sweep first:** `grep -rni "Implementation Notes\|notes block\|mine.*notes\|prior.*notes\|IMPLEMENTATION_NOTES" "C:/Users/JacobMadsen/source/repos/claude-config/user/skills/"` and inspect hits for any consumer NOT in the planned 8-set (e.g. `lazy-batch-retro`, `retro-feature`) that also reads notes. If a new consumer is found, add it to this WU's edits and record it; if the 8-set is confirmed complete, record that. Each edit is GENERIC — no `-cognito` variant.
- **Spec requirements:** SPEC §D3 + §Downstream Coupling "In scope (Phase 4)"; PHASES integration note "re-verify the 8-consumer set with a sweep for all notes-mining phrasings ... Record the sweep result so the set is verified, not assumed."
- **Batch:** 2 (depends on WU-1; disjoint files from WU-2).

---

## Execution Protocol

### Phase Selection Loop

One phase. Execute Batch 1 (WU-1 solo), then Batch 2 (WU-2+WU-3 in one message). Then Post-Phase Steps, then Completion.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)

Read `~/.claude/skills/_components/task-tracking.md`. Create tasks WU-1..WU-3.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)

Read `~/.claude/skills/_components/source-reread.md` (now Phase-3 + WU-1 aware). Re-read PHASES.md Phase 4 slice, SPEC §D3 + §Downstream Coupling, this plan.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS)

**PRE-FLIGHT CHECK:** `Agent`/`model: "sonnet"` for ALL edits; no direct edits.

Read: `~/.claude/skills/_components/{subagent-launch,implementation-agent}.md`. (No TDD this part.)

Batch 1: WU-1 alone. Batch 2: WU-2 and WU-3 as **two `Agent` blocks in one message** (disjoint skill files).

**Every subagent prompt MUST state:** edit real `claude-config\...` paths; harness config; GENERIC edits only (no `-cognito` variants); apply the WU-1 sibling-then-embedded read order consistently (reference the shared snippet); for WU-1, MERGE with Phase 3's existing `source-reread.md` slice-read edit (do not clobber); for WU-3, run the bounding sweep first and record the result.

**POST-DISPATCH GATE:** verify Agent-only edits.

#### Step B.2: Review Batch Output (MANDATORY GATE)

Read `~/.claude/skills/_components/subagent-review.md`; structured verdict. Verify: WU-1 preserved Phase 3's slice-read edit in `source-reread.md`; every consumer applies the SAME idiom (consistency check); the bounding sweep result is recorded; no skill was converted to a `-cognito` variant.

#### Step B.3: Update PHASES.md (MANDATORY)

Read `~/.claude/skills/_components/phases-update.md`. Check off Phase 4 deliverables; append notes to the sibling `IMPLEMENTATION_NOTES.md` (writer was flipped in Phase 3). Record the bounding-sweep result (8-set confirmed, or extra consumer added).

#### Step B.4: Run Quality Gates (MANDATORY)

Run `project-skills.py` (the new shared snippet must round-trip; consumers' `!cat` references must expand), `lint-skills.py`, `setup.ps1 check`, `pytest test_project_skills.py`. 100% pass.

#### Step B.4.5: MCP Integration Test

Skip (no runtime surface). Record the skip.

#### Step B.5: Commit Batch

Read `~/.claude/skills/_components/commit-and-push.md`. Commit in `claude-config`.

#### Step B.6: Proceed to Next Batch

Checklist: verdict; PHASES updated; gates green; commit.

### Post-Phase Steps

#### Integration Verification (MANDATORY)

Read `~/.claude/skills/_components/integration-verification.md`. For a representative trio (`add-phase`, `realign-spec`, `implement-phase`): run/dry-run against a feature with sibling notes → assert the relocated notes are surfaced; run against a legacy feature with embedded notes → assert fallback works. Confirm all edits use one consistent idiom.

#### Update CLAUDE.md Files (MANDATORY)

Read `~/.claude/skills/_components/claude-md-review.md`. Reference the shared read-order snippet if a CLAUDE.md should point at it.

#### Commit and Push Post-Phase Changes

Commit in `claude-config`.

---

## Blocking Issue Protocol

If blocked: stop; commit completed batches in `claude-config`; print the blocked report (Completed / Blocked WU / Reason / Recovery / Remaining); do not work around. Blocking issues: a consumer's notes-mining site cannot be located/edited safely after 2 retries; the bounding sweep reveals a consumer whose notes-read is structurally incompatible with sibling-then-embedded (escalate); projection cannot expand the new shared snippet.

---

## Completion

When Phase 4 is complete:

1. Run the full harness gate suite one final time.
2. Print:

   ## Implementation Batch — Complete (Part 4 of 5)

   **Phase completed:** plan-skills-redesign P4 — Propagate the D3 split across generic consumers
   **Commits:** [hashes]
   **Final gate status:** all green
   **Key outputs:** the bounding-sweep result (8-consumer set confirmed, or extras added); confirmation every consumer uses the single shared sibling-then-embedded snippet; confirmation no `-cognito` variants were created.

3. Then proceed to **part 5**: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\docs\features\plan-skills-redesign\plans\all-phases-plan-skills-redesign-part-5.md`
