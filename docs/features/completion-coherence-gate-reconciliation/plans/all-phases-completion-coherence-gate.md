---
kind: implementation-plan
feature_id: completion-coherence-gate-reconciliation
status: In-progress
created: 2026-06-19
complexity: complex
phases: [1, 2, 3]
---

> **Mobile plan** — generated by `/plan-feature` (`/write-plan`) on 2026-06-19.
> To execute: `/execute-plan docs/features/completion-coherence-gate-reconciliation/plans/all-phases-completion-coherence-gate.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — Completion / Coherence Gate Reconciliation  (v2)

**PHASES.md files:**
- `docs/features/completion-coherence-gate-reconciliation/PHASES.md` (completion-coherence-gate-reconciliation, 3 phases)

**SPEC.md files:**
- `docs/features/completion-coherence-gate-reconciliation/SPEC.md`

**Total phases:** 3
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.ts`, `.js`, `.cs`, `.vue`, `.py`, `.rs`, `.tsx`, `.jsx`, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **Pipeline override note:** when this plan is executed by a `/lazy-batch` *cycle subagent* (which has no `Agent` tool), the orchestrator-vs-subagent split collapses — perform ALL implementation INLINE with Read/Edit/Write, still test-first (write the failing fixture, then implement). The dispatch model is the only thing that changes; every other rule (TDD, review, gates, commit, PHASES.md update) holds.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `.claude/skill-config/quality-gates.md` (fallback: `~/.claude/skills/_components/quality-gates.md`) |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against. The executing session SHOULD `Read` these before starting any work unit whose Implementation goal references the corresponding upstream contract. Listed in dependency order.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| harness-hardening-retro-fixes | composes (NOT hard) | `docs/features/harness-hardening-retro-fixes/PHASES.md` | Introduced the canonical `_VERIFICATION_ONLY_MARKER` + `remaining_unchecked_are_verification_only` detector this plan extends to completion time. Read its Phase 2 only if the marker semantics are unclear during WU-1/WU-2. |

> Note: the dep is `composes`, so the `/write-plan` Step 1b.1 hard-dep read was correctly skipped; the row is recorded for traceability. The live anchors in `lazy_core.py` (verified below) are the authoritative contract, not the upstream PHASES prose.

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines). (Under a `/lazy-batch` cycle subagent with no `Agent` tool: do it all INLINE, still test-first — see the EXECUTION MODEL override note.)
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents
3. Every TDD work unit goes through the test-first pipeline — dedicated test agent writes failing tests, dedicated implementation agent makes them pass
4. PHASES.md is updated AFTER EACH batch completes (not deferred)
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing
6. Mistakes are fixed immediately before launching the next batch
7. After all batches in a phase finish, integration verification confirms all changes work together
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it
9. Each completed phase is committed and pushed before the next phase begins
10. Cross-feature phases may run in parallel when dependencies are satisfied and no file conflicts exist
11. This plan is self-contained — follow it exactly as written without relying on external context
12. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory

---

## Work Units

- [x] WU-1 — Evidence reader + authoritative-evidence decision table (Phase 1)
- [x] WU-2 — Atomic, line-anchored, audited auto-tick rewrite (Phase 2)
- [ ] WU-3 — Wire evidence-gated exemption into `_phase_completion_plan` / `__mark_complete__` + kill-switch (Phase 3)

---

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | completion-coherence-gate-reconciliation | P1 | Evidence reader + decision table | — | Solo |
| 2    | completion-coherence-gate-reconciliation | P2 | Auto-tick rewrite | P1 | Solo |
| 3    | completion-coherence-gate-reconciliation | P3 | Wire gate + kill-switch | P1, P2 | Solo |

> The three phases form a strict dependency chain (verdict → rewrite → wiring); no parallelism. Each is one work unit, but they MUST land in order — P2 consumes P1's verdict dict; P3 consumes both. All work is in `user/scripts/lazy_core.py` + its test/baseline files, so the phases share files and cannot overlap.

---

## Phase: completion-coherence-gate-reconciliation P1 — Evidence reader + authoritative-evidence decision table

**Goal:** Add a pure `evaluate_completion_evidence` helper to `lazy_core.py` that reads on-disk `/mcp-test` receipts and returns an `exempt-and-tick` / `warn-exempt` / `refuse` verdict per the SPEC's authoritative-evidence decision table.
**Entry criteria:** None (first phase).
**SPEC.md references:** "Technical Design (LOCKED) → Research-derived implementation contract" — authoritative-evidence decision table; Validation Criteria rows for missing-results, zero-test, SKIP/DEFERRED, HEAD-drift.

### WU-1 — Evidence reader + decision table

- **Scope:** PHASES Phase 1 deliverables — new `evaluate_completion_evidence(...)` helper; full decision-table logic (union of `VALIDATED.md` + `MCP_TEST_RESULTS.md`, `pass>0`, `validated_commit == HEAD`); HEAD-drift docs-only-vs-source carve-out; zero-test guard; SKIP/DEFERRED fail-closed.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy_core.py` — add `evaluate_completion_evidence(feature_dir, repo_root, ...)` near the existing `verify_ledger` / `__write_validated_from_results__` evidence-reading code. Reuses `parse_sentinel` [VERIFY: grep -n "def parse_sentinel" user/scripts/lazy_core.py] and `_current_head` [VERIFY: grep -n "def _current_head" user/scripts/lazy_core.py]. Reuse the existing `MCP_TEST_RESULTS.md` pass/total/validated_commit parse path in `__write_validated_from_results__` [VERIFY: grep -n "validated_commit" user/scripts/lazy_core.py] — do NOT duplicate the parsing.
- **Test files:** `user/scripts/test_lazy_core.py` (register new `def test_*` in `_TESTS`).
- **Test expectations:** One fixture per decision-table row. Assert: (a) `VALIDATED.md` (`kind: validated`) + passing `MCP_TEST_RESULTS.md` (`all-passing`, `pass==total>0`, `validated_commit == fixture HEAD`) → `verdict == "exempt-and-tick"`; (b) same dir, results removed → `"refuse"` (forged-attestation); (c) results present, `VALIDATED.md` absent → `"refuse"`; (d) `SKIP_MCP_TEST.md` present, no passing results → `"refuse"`, NO tick authorized; (e) `DEFERRED_NON_CLOUD.md` → `"refuse"`; (f) `pass==total==0` → `"refuse"` (zero-test); (g) `validated_commit != HEAD` with only a `*.md` diff → `"warn-exempt"`; (h) `validated_commit != HEAD` with a `.py` diff → `"refuse"` (refuse-and-revalidate); (i) neither sentinel present → `"refuse"`. Drift fixtures (g/h) build a 2-commit git temp repo.
- **Implementation goal:** Return `{verdict, reason, pass_count, validated_commit}`. The verdict literals are a LOCKED contract WU-3 branches on. Surface `pass_count` (cardinality numerator for WU-2) and `validated_commit` (audit-comment sha for WU-2) so neither is re-parsed downstream.
- **Spec requirements:** The authoritative-evidence decision table in Technical Design (LOCKED). Every row maps to a fixture above. `pass>0` mandatory. SKIP/DEFERRED fail closed (no operator-override path is implemented this cycle — fail-closed is the only path).
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | N/A             |

---

## Phase: completion-coherence-gate-reconciliation P2 — Atomic, line-anchored, audited auto-tick rewrite

**Goal:** Add `autotick_verification_rows` — an atomic, fence-safe, cardinality-locked, audited rewrite of unchecked verification-marked PHASES.md rows to `- [x]`.
**Entry criteria:** P1 complete (verdict dict supplies `validated_commit` + `pass_count`).
**SPEC.md references:** "Auto-tick rewrite contract (resolves Open Question 4 + hardens Decision 2 = A)" — atomic / line-anchored + code-fence-safe / auditable / cardinality-locked / superseded-aware.

### WU-2 — Auto-tick rewrite

- **Scope:** PHASES Phase 2 deliverables — `autotick_verification_rows(phases_path, validated_commit, pass_count)`; atomic write via `_atomic_write`; line-anchored fence-safe matcher; audit-trail comment; cardinality lock; superseded-aware; idempotent.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy_core.py` — add `autotick_verification_rows(...)` near `_phase_completion_plan` [VERIFY: grep -n "def _phase_completion_plan" user/scripts/lazy_core.py] and the verification-marker helpers. Uses `_atomic_write` [VERIFY: grep -n "def _atomic_write" user/scripts/lazy_core.py] (temp-in-same-dir → `os.replace`; NO `open('r+')`). Matches rows carrying `_VERIFICATION_ONLY_MARKER` [VERIFY: grep -n "_VERIFICATION_ONLY_MARKER =" user/scripts/lazy_core.py]. Reuse the fence-tracking pattern in `remaining_unchecked_are_verification_only` [VERIFY: grep -n "def remaining_unchecked_are_verification_only" user/scripts/lazy_core.py] — do NOT invent a new fence tracker.
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** Fixtures over temp PHASES.md: (a) two `<!-- verification-only -->` `- [ ]` rows + one fenced `- [ ]` + one plain implementation `- [ ]`; after `autotick_verification_rows(path, sha, pass_count=2)` exactly the two marker rows are `- [x]` each carrying `<!-- auto-ticked: validated_commit=<sha> -->`, fenced + implementation rows byte-unchanged, `ticked_count == 2`; (b) variable-whitespace `- [  ]` marker row IS matched; (c) cardinality abort: same 2-row file with `pass_count=1` → `ok: false`, file byte-unmodified (proves no-partial-write); (d) Superseded-phase unchecked box left untouched and not counted; (e) idempotent re-run: already-`- [x]` rows unchanged, no duplicate audit comment, `ticked_count == 0`.
- **Implementation goal:** Return `{ticked_count, ok, reason}`. Match `^\s*-\s+\[\s+\]` + marker on SAME line; skip lines inside ``` fences; NO global `.replace('- [ ]','- [x]')`. Assert `ticked_count <= pass_count` BEFORE writing (abort writing nothing on violation). Skip rows under `Superseded` phases. Append the byte-stable audit comment to each rewritten row.
- **Spec requirements:** Auto-tick rewrite contract (all five bullets: atomic, line-anchored + code-fence-safe, auditable, cardinality-locked, superseded-aware).
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-2      | Solo      | N/A             |

---

## Phase: completion-coherence-gate-reconciliation P3 — Wire gate + kill-switch

**Goal:** Wire the P1 verdict + P2 rewrite into `_phase_completion_plan` and the `__mark_complete__`/`__mark_fixed__` `--apply-pseudo` handler; record the tick count in `COMPLETED.md`/`FIXED.md`; gate the whole relaxation behind an env kill-switch.
**Entry criteria:** P1 + P2 complete.
**SPEC.md references:** "Direction A (chosen)"; "Kill-switch (research §8)"; Validation Criteria rows for validated-completes, real-implementation-refuses, verify_ledger-agreement, kill-switch.

### WU-3 — Gate wiring + receipt + kill-switch

- **Scope:** PHASES Phase 3 deliverables — `_phase_completion_plan` evidence-aware exemption; `--apply-pseudo` tick→re-check→receipt ordering; `auto_ticked_rows` in `COMPLETED.md`/`FIXED.md` + the JSON result; `LAZY_STRICT_EVIDENCE_GATE` kill-switch; `user/scripts/CLAUDE.md` docs.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy_core.py` — modify `_phase_completion_plan` [VERIFY: grep -n "def _phase_completion_plan" user/scripts/lazy_core.py] to accept + honor the evidence verdict (verification rows are NOT counted as refusals when the verdict authorizes; non-verification unchecked rows still refuse, naming the phase). Update ALL in-repo callers — `verify_ledger` [VERIFY: grep -n "_phase_completion_plan(" user/scripts/lazy_core.py] and the `__mark_complete__`/`__mark_fixed__` `--apply-pseudo` handler [VERIFY: grep -n "__mark_complete__\|__mark_fixed__" user/scripts/lazy_core.py]. Order in the handler: call `autotick_verification_rows` → re-run coherence → write receipt. Add `auto_ticked_rows` to the receipt writer [VERIFY: grep -n "def write_completed_receipt" user/scripts/lazy_core.py] output and the JSON result. Read kill-switch via `os.environ.get("LAZY_STRICT_EVIDENCE_GATE")` (and/or `LAZY_DISABLE_AUTOTICK`) once; when truthy, fall back to legacy strict (verification rows INCLUDED) and skip the rewrite.
  - `user/scripts/CLAUDE.md` — document the evidence-gated completion exemption + kill-switch under the "Verification-only canonical marker" section (keep prose lockstep with code per the Coupling Rule). This file the orchestrator MAY edit directly.
- **Test files:** `user/scripts/test_lazy_core.py`, plus `lazy-state.py` / `bug-state.py` `--test` fixtures, plus pinned `--test` baselines if output legitimately changes.
- **Test expectations:** End-to-end fixtures: (a) validated feature, only verification rows unchecked → `--apply-pseudo __mark_complete__` mints `COMPLETED.md`, rows ticked, `auto_ticked_rows` recorded in receipt + JSON, NO refusal, exit 0; (b) feature with a real implementation `- [ ]` row → refuses naming the phase, PHASES.md byte-unchanged, no receipt; (c) `verify_ledger` and `_phase_completion_plan` agree on one input (both pass, or both name the same phase); (d) `LAZY_STRICT_EVIDENCE_GATE=1` → legacy refusal, zero PHASES.md mutation; (e) the P1 refuse classes (missing results, SKIP/DEFERRED, zero-test, cardinality over-tick, source HEAD-drift) all refuse at the live gate with NO tick. Mirror the `__mark_fixed__` completion path on `bug-state.py`.
- **Implementation goal:** `_phase_completion_plan` becomes evidence-aware while preserving auto-flip-to-Complete + Superseded behavior exactly. Evidence — not the checkbox — is the source of truth; a genuine unchecked implementation row still refuses. The kill-switch restores byte-identical legacy behavior.
- **Spec requirements:** Direction A; Kill-switch; the four Validation Criteria rows named above. The `--apply-pseudo` ordering (tick → re-check → receipt) is load-bearing.
- **Batch:** 1.

**Runtime / verification note:** Phase 3's PHASES.md `**Runtime Verification**` rows (the `<!-- verification-only -->` pytest/parity-audit rows) are NOT plan WUs — they are ticked by the repo's quality-gate run (this repo's Step-9 MCP equivalent), NOT by `/execute-plan`. They stay in PHASES.md and route the phase forward to the validation tail. Do NOT author a WU to "run the full gate suite and earn VALIDATED.md" — that is the gate's job, and per the repo `quality-gates.md` the operator grants the `SKIP_MCP_TEST.md` → `VALIDATED.md` promotion.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-3      | Solo      | N/A             |

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** Identify phase(s) whose entry criteria are satisfied (prerequisite phases complete — all deliverables checked off in their PHASES.md). Here the chain is strict P1 → P2 → P3; execute in order, no parallelism. If no phase is ready, jump to Blocking Issue Protocol.
2. **Announce:** Print "Implementing completion-coherence-gate-reconciliation Phase N: [title]"
3. **Review prior context:** Re-read all previously completed phases' Implementation Notes in this feature's PHASES.md. They contain the real signatures (verdict dict shape, rewrite return shape) that take priority over the plan where they diverge.
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "completion-coherence-gate-reconciliation Phase N: [title] — committed as [hash]"
7. **Loop:** Re-evaluate which phases are now ready. Return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

**This is the first thing you do when executing this plan. Do NOT skip ahead to any phase or batch.**

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly. It defines: task tool loading via ToolSearch, task creation for all work units, and the update protocol for tracking progress through test and implementation phases.

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow its instructions. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and the plan file itself. Do NOT rely on cached/remembered content.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — ZERO INLINE IMPLEMENTATION)

**PRE-FLIGHT CHECK:** You are about to dispatch work to Sonnet subagents. Confirm: (1) you will use the `Agent` tool with `model: "sonnet"` for ALL code changes in this step, (2) you will NOT call `Edit` or `Write` on any source or test file. If either is false, re-read the EXECUTION MODEL section above. (Cycle-subagent override: no `Agent` tool → implement INLINE, test-first.)

Read ALL of these before proceeding:
1. `~/.claude/skills/_components/tdd-protocol.md` — TDD decision gate
2. `~/.claude/skills/_components/subagent-launch.md` — Launch orchestration
3. `~/.claude/skills/_components/tdd-test-agent.md` — Test agent prompt template
4. `~/.claude/skills/_components/implementation-agent.md` — Impl agent prompt template

Note: `subagent-launch.md` references the other components above via internal directives — since you've already read them, ignore those directives in the file.

**POST-DISPATCH GATE:** After all subagents complete, verify you composed `Agent` tool calls and did NOT edit source/test files directly (inline path: verify you wrote the failing test BEFORE the implementation). If you violated this, revert and re-do correctly.

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

**This is a blocking gate.** You CANNOT proceed to Step B.3 until the review protocol is fully executed and produces a structured review report with a verdict. Reading a few files and saying "looks correct" is NOT a review.

Read `~/.claude/skills/_components/subagent-review.md` and follow its complete protocol. Also read `~/.claude/skills/_components/mount-site-verification.md` (referenced within subagent-review for new-file checks). Protocol covers: batch scope measurement, review execution, propagation check, mount-site verification, and verdict handling (PASS / PASS-WITH-FIXES / NEEDS-REWORK).

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md` and follow its instructions. Check off completed deliverables, add an Implementation Notes block with date, work completed, integration notes (the exact verdict-dict shape, rewrite return shape, kill-switch env name), pitfalls, and files modified. Also tick the matching `- [ ] WU-N` row in this plan's `## Work Units` checklist.

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `.claude/skill-config/quality-gates.md` (fallback `~/.claude/skills/_components/quality-gates.md`) and follow it. For this feature (Python script changes to `lazy_core.py`), the relevant gates are:
- `python -m pytest user/scripts/ -q` — full script suite (includes the dead-coverage guard that FAILS any unregistered `def test_*`).
- `python user/scripts/lazy-state.py --test` and `python user/scripts/bug-state.py --test` — state-machine smoke (both MUST stay green; regenerate a pinned baseline ONLY via `_normalize_smoke_output`, never by hand).
- `python user/scripts/lazy_parity_audit.py --report` — no unexplained drift (lazy-family change).
- On the LAST phase / before flipping the part to Complete, run the FULL set including `python user/scripts/project-skills.py` + `python user/scripts/lint-skills.py --check-projected --check-capabilities` (the CLAUDE.md doc edit is the only skill-surface change).
100% pass required before proceeding.

#### Step B.4.5: MCP Integration Test (BLOCKING — if applicable)

Read `~/.claude/skills/_components/mcp/mcp-integration-test.md` to determine applicability. **N/A for this repo** — claude-config has no Tauri/MCP server (see `.claude/skill-config/quality-gates.md` MCP exemption). Skip with a note; the Python quality gates above are the validation surface.

#### Step B.5: Commit Batch

Read `.claude/skill-config/commit-policy.md` and follow it: Conventional Commits (`type(scope): subject`), NO AI attribution, scope `lazy-core` / `completion-coherence-gate-reconciliation`, commit after each phase, push to `main` (allowed — personal config repo).

#### Step B.6: Proceed to Next Batch

**Checklist before proceeding (all must be true):**
- [ ] Review report produced with PASS/PASS-WITH-FIXES/NEEDS-REWORK verdict
- [ ] PHASES.md updated with completed deliverables and implementation notes; the `- [ ] WU-N` row ticked
- [ ] All quality gates pass
- [ ] Step B.5 completed (commit + push per policy)

If any item is unchecked, go back and complete it. Do NOT launch the next batch.

### Propagation Awareness Note

WU-3 changes the `_phase_completion_plan` SIGNATURE — this is an import-indirection / consumer-migration hazard. The plan REQUIRES migrating ALL callers in the same batch (`verify_ledger`, the `--apply-pseudo` handler, and every `--test` fixture that calls it) and running the FULL `pytest user/scripts/ -q` suite (not just one test file) so a missed caller fails the gate immediately.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md` and follow its complete protocol. Covers cross-agent integration and spec alignment. For this feature: confirm the verdict → rewrite → gate chain works end-to-end via the WU-3 end-to-end fixture, not just the per-helper unit fixtures.

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md` and follow its instructions. `user/scripts/CLAUDE.md` MUST gain the evidence-gated completion-exemption + kill-switch documentation (WU-3 deliverable) — the Coupling Rule requires prose lockstep with the code.

#### Commit and Push Post-Phase Changes

Read `.claude/skill-config/commit-policy.md` and follow it (push to `main`).

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point during execution:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed phases** that haven't been committed yet.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** completion-coherence-gate-reconciliation Phase N: [title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining phases (not attempted):**
   - [list]

4. **Do not attempt to work around the blocker.** The user provides a resolution and triggers autonomous implementation after.

Blocking issues include:
- Circular dependency in the phase graph
- A subagent failure that can't be fixed after 2 retry attempts
- A quality-gate failure that can't be fixed after 2 retry attempts
- A git push conflict that can't be resolved by rebase
- A phase whose entry criteria reference a feature/phase not in the input set
- Any error that would require architectural decisions beyond the scope of the specs

---

## Completion

When all phases in the Execution Schedule are complete:

1. **Run the full quality-gate suite one final time** across `user/scripts/` (`project-skills.py` + `lint-skills.py --check-projected --check-capabilities` + `pytest user/scripts/ -q` + `lazy-state.py --test` + `bug-state.py --test` + `lazy_parity_audit.py --report`).
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Features implemented:** completion-coherence-gate-reconciliation
   **Total phases completed:** 3
   **Total commits:** M
   **Final quality-gate status:** all green

   **Commit log:**
   | Commit | Feature | Phase | Title |
   |--------|---------|-------|-------|
   | ... | completion-coherence-gate-reconciliation | P1 | Evidence reader + decision table |
   | ... | completion-coherence-gate-reconciliation | P2 | Auto-tick rewrite |
   | ... | completion-coherence-gate-reconciliation | P3 | Wire gate + kill-switch |

   **Implementation Notes summary:**
   [the locked verdict-dict shape, the `autotick_verification_rows` return shape, the kill-switch env name, and the `--apply-pseudo` tick→re-check→receipt ordering — collapsed for the validation tail]

Plan written to: docs/features/completion-coherence-gate-reconciliation/plans/all-phases-completion-coherence-gate.md

```
/execute-plan docs/features/completion-coherence-gate-reconciliation/plans/all-phases-completion-coherence-gate.md
```
