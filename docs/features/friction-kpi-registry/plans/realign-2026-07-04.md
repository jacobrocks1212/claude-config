---
kind: realign-plan
feature_id: friction-kpi-registry
status: Ready
created: 2026-07-04
phases: [2, 3]
upstream_phases_hashes:
  harness-telemetry-ledger: 0231fab4c4699db2b3f7627c8657c43820c3da1c3ae83cb4ba23cad3f0a33404
---

# Realign — friction-kpi-registry (2026-07-04)

> Read-only reality check against completed upstream dependencies. Generated by `/realign-spec`.
> No SPEC or PHASES files were modified by this analysis (the one Minor correction below is
> applied in the `--apply` follow-on step, not by the diff).

## Inputs

- **Downstream SPEC:** `docs/features/friction-kpi-registry/SPEC.md`
- **Downstream PHASES:** `docs/features/friction-kpi-registry/PHASES.md`
- **Upstream dependencies checked (kind=hard, Status=Complete):**
  - `harness-telemetry-ledger` — PHASES.md (0 plans read: the concrete symbol contract is fully
    captured in PHASES.md Deliverables + cross-checked against the live `lazy_core.py` /
    `pipeline_visualizer/trends.py` source, so no plan-level read was needed)

## Aligned

Places the downstream correctly anticipates the upstream's actual state. Every load-bearing
symbol the scorecard binds was verified present in the shipped source, not just the upstream
PHASES prose.

- **Ledger residency + filenames** — SPEC/PHASES assume `lazy-telemetry.jsonl` in the per-repo
  keyed state dir plus committed `docs/telemetry/cloud/*.jsonl` segments. Confirmed:
  `lazy_core._TELEMETRY_LEDGER_FILENAME = "lazy-telemetry.jsonl"`;
  `flush_cloud_telemetry_segment` writes the `docs/telemetry/cloud/<run_id>.jsonl` segment.
- **Shared emitter/reader** — `lazy_core.append_telemetry_event` / `read_telemetry_events` exist
  exactly as the PHASES Cross-feature notes claim.
- **D4-B event vocabulary** — the SPEC's bound event names (`run-start`, `run-end`, `cycle-begin`,
  `cycle-end`, `pseudo-applied`, `dispatch`, `halt`, `sentinel-resolved`, `gate-refusal`,
  `containment-refusal`) match the upstream Phase-2 emission set one-for-one.
- **Reused trends aggregation functions** — the PHASES "one computation, two renderers" intent
  (D5) names `halt_dwell`, `cycles_per_completion`, `refusal_counts`, `load_events`; all four
  exist in `pipeline_visualizer/trends.py`, along with the `_bind_lazy_core(repo_root)` binding
  pattern the PHASES cites for per-repo state-dir residency.
- **Return-shape fit** — `halt_dwell` returns per-item rows carrying `dwell_seconds` (so the
  scorecard's `halt-dwell-p50-seconds` median is directly computable); `cycles_per_completion`
  returns `{cycles, forward_cycles, meta_cycles, completions, cycles_per_completion}` (matches the
  `cycles-per-completion` selector); `refusal_counts` returns `containment_refusals` (backs the
  `containment-refusal-count` selector).
- **State-dir plumbing** — `set_active_repo_root`, `claude_state_dir`, and `read_deny_ledger`
  (Phase 2's deny-ledger source) are all present; the `LAZY_STATE_DIR` hermetic-test seam the
  PHASES relies on is the documented per-repo-keyed-state-dir contract.

## Drift

### Minor

Small wording/coherence correction. Safe to apply inline to the downstream SPEC.

| # | Location (file:section) | Current text | Upstream reality | Suggested patch |
|---|-------------------------|--------------|------------------|-----------------|
| 1 | SPEC.md § Technical Design (computability table, "runaway/containment trip rate" row) | Lists the containment trip-rate signal as computable **today** from `lazy-deny-ledger.jsonl` (guard denies + `kind: process-friction`) with "Needs from `harness-telemetry-ledger`" = `—`, implying a Phase-2 computable-today row. | The shipped upstream added a purpose-built `containment-refusal` **telemetry** event (emitted inside `refuse_if_cycle_active` / `refuse_cycle_marker_mutation_if_subagent` / `refuse_run_start_clobber`), and PHASES Phase 3 actually binds `containment-refusal-count` to that telemetry-ledger source via `trends.refusal_counts` — i.e. it is a Phase-3 ledger-backed row, not a Phase-2 deny-ledger-only row. | Append to the "Computable today from" cell: note the upstream now also emits a dedicated `containment-refusal` telemetry event (the cleaner signal PHASES Phase 3 binds via `trends.refusal_counts`); the deny-ledger `process-friction` path remains a complementary source. Keeps the SPEC table coherent with the PHASES Phase-2/Phase-3 split. |

### Moderate

(none)

### Severe

(none)

## Recommended next step

- `inline-edit` — apply the single Minor patch to SPEC.md as listed above; nothing else required.

**Recommended:** `inline-edit`

**Why:** The only divergence found is a one-cell SPEC/PHASES coherence nit — the downstream design
holds completely and every upstream symbol it binds is present as assumed; correcting the
computability-table row keeps the SPEC's Phase-2/Phase-3 signal split consistent with the PHASES.
