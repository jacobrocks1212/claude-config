---
kind: intervention
intervention_id: harden-2026-07-r67
pipeline: hardening
provenance: backfilled
shipped_date: '2026-07-17'
shipped_commit: e91bd305
commit_set: e91bd305 9819702f 306a6714
target_signal: event:cycle-end
expected_direction: decrease
signal_independence: self-emitted — the fix relocates the forward-budget advance ONTO the
  cycle-end bracket event, so forward_cycles now tracks real cycle-end(kind=real) events;
  the eliminated over-count was the probe-driven spurious advance, which emitted no event
baseline:
  status: not-computable
  reason: strict --record-intervention CLI was DENIED by the lazy-cycle-containment guard
    (dispatched harden-harness under the live /lazy-batch run marker; LAZY_ORCHESTRATOR unset),
    so the telemetry baseline window was not frozen. The measurable countable — forward_cycles
    delta per probe == 0, per completed real dispatch == 1 — is mechanically enforced by the
    regression tests. Hand-authored backfill for doc-drift-lint coverage; the orchestrator may
    re-run --record-intervention to freeze the window and supersede this record.
review_after_runs: 20
min_sample: 5
band_pct: 20
review_count: 0
status: open
escalated: false
reconsideration_enqueued: null
canary:
  opened: '2026-07-17'
  window_runs: 10
  surfaces:
  - user/scripts/lazy_core/markers.py
  - user/scripts/lazy-state.py
  - user/scripts/bug-state.py
  commit_set:
  - e91bd305
  - 9819702f
  - 306a6714
  pair_scope: []
  degraded_revert_note: null
  status: open
---

# Intervention — harden-2026-07-r67

Cycle-budget counters double-count on probes + the per-turn inject hook
(`docs/bugs/cycle-budget-counters-double-count-on-probes-and-inject-hook`). Operator
directive: offload cycle-counting to the SCRIPT so a cycle counts only on a real dispatch,
decoupled from probes. `forward_cycles` was advanced by `advance_forward_cycle` on the
`--repeat-count` probe path (fires on state-change), so inspection probes AND the per-turn
inject-hook probe inflated the budget with no dispatch. The fix relocates the increment to
the `--cycle-end` bracket (new `advance_cycle_bracket_counter`, keyed on `--kind`), keeps
`--apply-pseudo`, and removes the probe-path + emit-dispatch advances. Probes are now
budget-neutral; a real bracket counts exactly once.

**Measured effect (intended):** forward_cycles delta per inspection/inject probe == 0; per
completed real `--cycle-begin --kind real`/`--cycle-end` bracket == +1 (with the per-feature
sibling). Anchored to `event:cycle-end` (the new advance event). The durable guarantee is the
five regression tests in `test_markers.py` (a–e). Honest residual: `__flip_plan_complete_stale__`
(applied inline, unbracketed) no longer has its meta cycle counted — budget-neutral since
`meta_cycles` is uncapped/display-only.
