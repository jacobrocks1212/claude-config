---
kind: intervention
intervention_id: harden-2026-07-r112
pipeline: hardening
provenance: backfilled
shipped_date: '2026-07-19'
shipped_commit: 66151de6d0c86c254e5907a5b5856b9c4c8cfef5
commit_set: 7eb5cbb73a8d3e3f3e86bb6cc11893f254964368,66151de6d0c86c254e5907a5b5856b9c4c8cfef5
target_signal: event:dispatch
expected_direction: decrease
signal_independence: mixed — a dispatch-guard validate-deny has NO own telemetry event
  (append_deny_ledger_entry writes only the deny-ledger), so the friction's recurrence is
  observable only downstream: an improvisation deny accrues hardening debt → the next probe
  withholds the forward route (route_overridden_by: pending-hardening-debt) → a hardening
  `dispatch` telemetry event fires. The self-announcing reason aims to make the orchestrator
  stop re-improvising the subagent-model split, driving down those improvisation-caused
  pending-hardening-debt withhold dispatches — but that is a SMALL subset of total `dispatch`
  events (which are dominated by normal cycle dispatch + run length), so a total-dispatch
  decrease is only weakly attributable to this intervention (the honest available signal;
  the dispatch-guard deny is not itself in the event vocabulary).
baseline:
  status: not-computable
  reason: recorded out-of-band by a dispatched observed-friction harden subagent under a
    CONCURRENT non-harden cycle (adhoc-containment-hook-e2big-fails-open-windows-native) —
    --record-intervention was cycle-refused (refuse_if_cycle_active; the allow_hardening_subagent
    carve-out did not apply because the active cycle was not a hardening class), so the telemetry
    baseline window could not be frozen. Re-baseline via the orchestrator handback (see
    hardening-log Round 112 Return).
review_after_runs: 20
min_sample: 5
band_pct: 20
review_count: 0
status: open
escalated: false
reconsideration_enqueued: null
canary:
  opened: '2026-07-19'
  window_runs: 10
  surfaces:
  - user/scripts/lazy_guard.py
  - user/scripts/test_hooks.py
  commit_set:
  - 7eb5cbb73a8d3e3f3e86bb6cc11893f254964368
  - 66151de6d0c86c254e5907a5b5856b9c4c8cfef5
  pair_scope: []
  degraded_revert_note: null
  status: open
---

# Intervention: harden-2026-07-r112

Hypothesis: shipping `harden-2026-07-r112` (hardening pipeline) moves `event:dispatch` in direction `decrease` within 20 post-ship runs — specifically, the guard's new self-announcing deny reason for the improvisation-caught signature (armed `subagent_model` cycle + own emission NOT consumed) makes the orchestrator stop composing `/execute-plan`'s internal test-agent/impl-agent worker prompts itself and instead dispatch the single emitted `cycle_prompt`, so the improvisation-caused pending-hardening-debt route-withhold hardening dispatches trend down.

Reviews are appended below by `user/scripts/efficacy-eval.py` (`## Review <date>` sections). Do not hand-edit the frontmatter — the evaluator is its sole post-capture writer.
