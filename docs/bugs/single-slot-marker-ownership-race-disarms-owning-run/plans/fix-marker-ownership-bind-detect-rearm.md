---
kind: fix-plan
feature_id: single-slot-marker-ownership-race-disarms-owning-run
status: Ready
created: 2026-06-20
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-20.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\single-slot-marker-ownership-race-disarms-owning-run\plans\fix-marker-ownership-bind-detect-rearm.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — single-slot-marker-ownership-race-disarms-owning-run (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\single-slot-marker-ownership-race-disarms-owning-run\PHASES.md` (single-slot-marker-ownership-race-disarms-owning-run, 2 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\single-slot-marker-ownership-race-disarms-owning-run\SPEC.md`

**Total phases:** 2
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **DISPATCH-LIMITED EXECUTION OVERRIDE.** When this plan is executed by a `/lazy-bug-batch` (or `/lazy-bug`) cycle subagent that lacks the `Agent` tool, the orchestrator-vs-subagent split above is NOT achievable — perform ALL implementation, tests, and docs INLINE via `Read`/`Edit`/`Write`, test-first (write the failing fixture before the implementation edit). The "no inline Edit/Write" constraint is suspended ONLY in that dispatch-limited context; everything else in this plan (review discipline, ground-truth verification, quality gates, commit policy) still applies. Outside that context (a human or an Agent-capable session executing directly), honor the orchestrator+subagent split as written.

**Dispatch pattern (Agent-capable sessions only):** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this bug has no completed hard upstream dependencies) | — | — | The origin (`stale-marker-arms-validate-deny-on-unrelated-dispatches`) and the related `concurrent-same-branch-walkers-no-arbitration` are CONTEXT (cited in SPEC `**Related:**`), not hard deps. Their fixes already landed; this plan builds on the in-tree state they left, not on a pending upstream phase. |

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — EXCEPT under the DISPATCH-LIMITED EXECUTION OVERRIDE above (a `/lazy-bug*` cycle subagent with no Agent tool), where all work is done INLINE, test-first.
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline — the failing fixture is written and seen RED before the implementation edit.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's (or inline) output is reviewed for correctness, spec alignment, and TDD discipline before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in a phase finish, integration verification confirms all changes work together.
8. Relevant CLAUDE.md files are updated after each phase if changes warrant it.
9. Each completed phase is committed and pushed before the next phase begins.
10. This plan is self-contained — follow it exactly as written without relying on external context.
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
12. **Coupled-pair lockstep (HARD).** Any edit to a shared `lazy_core` helper OR to a `--run-start` / new CLI action on `lazy-state.py` MUST be mirrored on `bug-state.py` (the marker is shared). Run `python3 user/scripts/lazy_parity_audit.py` as part of every quality-gate pass — exit 0 required.
13. **Status honesty (HARD).** This plan NEVER flips SPEC.md / PHASES.md top-level `**Status:**` to `Fixed`/`Won't-fix` and NEVER writes `FIXED.md` — those are the `__mark_fixed__` gate's, fired only after the validation tail. You MAY tick per-WU/phase checkboxes, flip plan-part frontmatter `status:`, and set PHASES top-level `**Status:**` to `In-progress` when the last phase lands.

---

## Work Units

- [ ] WU-1 — Thread `session_id=args.session_id` into both scripts' `--run-start` `write_run_marker` call (close the bind-pending window)
- [ ] WU-2 — Repro-A-closed + legacy-unbound-preserved fixtures (Phase 1 regression net)
- [ ] WU-3 — `marker_owner_status` detect helper + `reassert_marker_owner` re-arm helper (`lazy_core.py`)
- [ ] WU-4 — Orchestrator-only `--reassert-owner` CLI action on both scripts (cycle-guarded)
- [ ] WU-5 — Detect/re-arm/Repro-B/legacy-disarm fixtures + `--reassert-owner` cycle-refusal fixture
- [ ] WU-6 — Docs: `user/scripts/CLAUDE.md` + root `CLAUDE.md` (bind/detect/re-arm model + reverse-references)

---

## Execution Schedule

| Step | Feature | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|---------|----------|----------|------------|-----------|
| 1 | single-slot-marker-ownership-race-disarms-owning-run | P1 | Bind the marker to the owning session at `--run-start` | — | Solo |
| 2 | single-slot-marker-ownership-race-disarms-owning-run | P2 | Owner-side detect + re-arm backstop | P1 | Solo |

---

## Phase: single-slot-marker-ownership-race-disarms-owning-run P1 — Bind the marker to the owning session at `--run-start`

**Goal:** Born-bound run marker — `--run-start` stamps the orchestrator's known owning session so a foreign `bind_marker_session` can never be the first writer, closing Repro A + Repro B at the source.
**Entry criteria:** None (first phase).
**SPEC.md references:** `## Reproduction Steps` (Repro A/B), `## Proven Findings` #1, #4(a); `## Open Questions` (bind-unforgeability mechanism — resolved to run-start bind).

### WU-1 — Thread `session_id=args.session_id` into both scripts' `--run-start`

- **Scope:** Pass `session_id=args.session_id` into the `write_run_marker(...)` call inside the `--run-start` handler on BOTH `lazy-state.py` (~7540) and `bug-state.py` (~4611). When `args.session_id` is non-empty the marker is born owner-bound; when absent (legacy/manual), `write_run_marker` writes `session_id: None` exactly as today.
- **TDD:** yes (behavior change — covered by WU-2's fixtures, written first).
- **Files to modify:**
  - `user/scripts/lazy-state.py` — `--run-start` handler `write_run_marker(...)` call [VERIFY: grep -n "if args.run_start" user/scripts/lazy-state.py]
  - `user/scripts/bug-state.py` — `--run-start` handler `write_run_marker(...)` call [VERIFY: grep -n "if args.run_start" user/scripts/bug-state.py]
- **Implementation goal:** The `--run-start` marker carries the orchestrator's session_id when supplied. `write_run_marker` already accepts the `session_id` kwarg [VERIFY: grep -n "session_id: str | None = None," user/scripts/lazy_core.py] and the `--session-id` arg already exists on both scripts [VERIFY: grep -n '"--session-id"' user/scripts/lazy-state.py user/scripts/bug-state.py] — the edit is purely passing the already-available arg. No signature change. Keep the existing attended/cloud/max_cycles kwargs intact.
- **Spec requirements:** SPEC `## Proven Findings` #4(a) ("make the bind UNFORGEABLE by a non-owner … a `--run-start`-time bind to the orchestrator's known session_id rather than bind-pending"); Open Question 1 RESOLVED (session_id available at run-start).
- **Batch:** 1.

### WU-2 — Repro-A-closed + legacy-unbound-preserved fixtures

- **Scope:** Deliverable rows: the two Phase-1 test fixtures. Written FIRST (RED before WU-1's edit lands GREEN).
- **TDD:** yes (this IS the test WU).
- **Test files:** `user/scripts/test_lazy_core.py` [VERIFY: grep -n "_set_state_dir" user/scripts/test_lazy_core.py]
- **Test expectations:**
  - **Repro A closed:** with `_set_state_dir` at a temp dir, `write_run_marker(pipeline="feature", cloud=False, repo_root="/r", session_id="OWNER")` then `bind_marker_session("FOREIGN")` returns `False` (marker already bound) AND the on-disk marker's `session_id` is STILL `"OWNER"`; `read_run_marker(session_id="OWNER")` returns the marker (owner reads its own run — NOT disarmed). This fixture must FAIL against pre-WU-1 code (which writes `None`, letting the foreign bind win) — confirm RED before GREEN by asserting the failing behavior is the foreign stamp winning.
  - **Legacy unbound preserved:** `write_run_marker(...)` with NO `session_id` still writes `session_id: None`, and a subsequent `bind_marker_session("FOREIGN")` returns `True` (the documented legacy bind path) — proving the fix is additive, not a silent semantic change for the no-`--session-id` caller.
- **Implementation goal:** N/A (test WU). Use the existing `_set_state_dir`/`_clear_state_dir` fixture pattern and the real `write_run_marker`/`bind_marker_session`/`read_run_marker` producers — no mocks beyond the state-dir override.
- **Spec requirements:** SPEC `## Open Questions` (deterministic fixture design — injected bind ordering).
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-2 (test, first) → WU-1 (impl) | Sequential (test-first) | WU-1 edits scripts; WU-2 edits test file — no conflict |

**Phase-1 Runtime Verification (PHASES.md-owned, ticked by validation, NOT this plan):** a run-start-bound marker survives a wrong-session `bind_marker_session` and the owner still reads its own marker. (Closed in-plan by WU-2's hermetic fixture — this is a `--test`-observable proof, not a live-runtime row.)

---

## Phase: single-slot-marker-ownership-race-disarms-owning-run P2 — Owner-side detect + re-arm backstop

**Goal:** Give the true owner a non-destructive DETECT path (distinguish "no run" from "wrong-stamped run") and a RE-ARM re-claim path, backstopping the legacy/un-threaded run-start and any future non-allow `bind_marker_session` caller.
**Entry criteria:** P1 complete (the run-start owner bind establishes the owner-on-record invariant the detect path compares against).
**SPEC.md references:** `## Proven Findings` #1, #3 (`lazy_coord` fencing precedent), #4(b) detect / #4(c) re-arm; `## Affected Area` (owner-side read silent disarm).

### WU-3 — `marker_owner_status` detect helper + `reassert_marker_owner` re-arm helper

- **Scope:** Two new pure helpers in `lazy_core.py`.
- **TDD:** yes (WU-5 fixtures written first).
- **Files to modify:** `user/scripts/lazy_core.py` [VERIFY: grep -n "def read_run_marker" user/scripts/lazy_core.py]
- **Implementation goal:**
  - `marker_owner_status(session_id, *, now=None) -> "absent" | "owned-by-me" | "foreign-stamped"`: NON-destructive. Reuse `read_run_marker`'s age/corrupt rules for `absent` (age-stale/corrupt/missing → `absent`), but do NOT delete on a session mismatch — report `foreign-stamped` instead of collapsing to `None`. `owned-by-me` = live marker with `session_id` None (bind-pending — owner's, not yet stamped) OR equal to the caller. `foreign-stamped` = live marker whose non-None `session_id` differs from the caller. Read the marker via the same `claude_state_dir(create=False) / _MARKER_FILENAME` path [VERIFY: grep -n "_MARKER_FILENAME = " user/scripts/lazy_core.py] and honor path-A age staleness so a stale foreign marker is `absent`, not `foreign-stamped`.
  - `reassert_marker_owner(session_id, *, now=None) -> bool`: when `marker_owner_status(session_id)` is `foreign-stamped`, atomically re-stamp the slot to `session_id` via `_atomic_write` [VERIFY: grep -n "def _atomic_write" user/scripts/lazy_core.py] and return True; for `absent` or `owned-by-me` it is a no-op returning False.
- **Spec requirements:** SPEC `## Proven Findings` #4(b)/(c). HARD: `marker_owner_status` MUST stay non-destructive on `foreign-stamped` (deleting there re-introduces the 2026-06-12 ~14:53Z silent-disarm-by-delete the path-B non-destructive rule exists to avoid).
- **Batch:** 1.

### WU-4 — Orchestrator-only `--reassert-owner` CLI action on both scripts

- **Scope:** Expose `reassert_marker_owner` via a new `--reassert-owner` action on `lazy-state.py` and `bug-state.py`, guarded like the other orchestrator-only ops.
- **TDD:** yes (WU-5 cycle-refusal fixture).
- **Files to modify:**
  - `user/scripts/lazy-state.py` — add the `--reassert-owner` action (requires `--session-id`) [VERIFY: grep -n "refuse_if_cycle_active" user/scripts/lazy-state.py]
  - `user/scripts/bug-state.py` — mirror the action verbatim [VERIFY: grep -n "refuse_if_cycle_active" user/scripts/bug-state.py]
- **Implementation goal:** `--reassert-owner` calls `refuse_if_cycle_active("--reassert-owner")` FIRST (a cycle subagent is refused exit 3 with zero side effects — same contract as `--run-start`/`--reorder-queue` [VERIFY: grep -n "def refuse_if_cycle_active" user/scripts/lazy_core.py]), then calls `reassert_marker_owner(args.session_id)` and prints the verdict JSON `{"reasserted": bool, "prior_status": "<status>"}`. It is read-of-marker + conditional re-stamp; exits immediately like the other run-lifecycle flags.
- **Spec requirements:** SPEC `## Proven Findings` #4(c) (re-arm path is the owner re-claiming its own run's guard).
- **Batch:** 1.

### WU-5 — Detect/re-arm/Repro-B/legacy-disarm fixtures + `--reassert-owner` cycle-refusal fixture

- **Scope:** The Phase-2 test net. Written FIRST (RED before WU-3/WU-4 land).
- **TDD:** yes (this IS the test WU).
- **Test files:** `user/scripts/test_lazy_core.py`
- **Test expectations:**
  - **Detect three-way:** `marker_owner_status` returns `absent` (no marker / age-stale), `owned-by-me` (bind-pending None AND equal session), `foreign-stamped` (live marker, non-None foreign session); assert it does NOT delete the marker on `foreign-stamped` (file still present after the call).
  - **Re-arm:** seed a live marker stamped `"FOREIGN"`; `reassert_marker_owner("OWNER")` returns True and the on-disk `session_id` is now `"OWNER"`; a second call returns False (idempotent, now `owned-by-me`); on an `absent` marker it returns False.
  - **Repro B closed:** simulate a checkpoint-resume `--run-start` with a session id — `write_run_checkpoint(...)` [VERIFY: grep -n "def write_run_checkpoint" user/scripts/lazy_core.py] then `consume_run_checkpoint()` then `write_run_marker(session_id="OWNER", ...)`; assert `marker_owner_status("OWNER") == "owned-by-me"` (the resume window no longer re-opens for an owner that passes `--session-id`).
  - **Legacy disarm + re-arm:** the legacy path (run-start with NO session_id → bind-pending → `bind_marker_session("FOREIGN")` wins) is DETECTED as `foreign-stamped` and RE-ARMED via `reassert_marker_owner("OWNER")` — proving the backstop covers the path Phase 1 leaves open.
  - **`--reassert-owner` cycle-refusal:** a subprocess fixture — with the cycle marker present and no `LAZY_ORCHESTRATOR` env, `python3 lazy-state.py --reassert-owner --session-id OWNER` exits 3 with zero side effects (marker `session_id` unchanged); the orchestrator path (`LAZY_ORCHESTRATOR=1` or no cycle marker) re-claims. Mirror the existing C3-refusal subprocess fixture style [VERIFY: grep -n "refuse_if_cycle_active\|exit 3\|returncode == 3" user/scripts/test_lazy_core.py].
- **Implementation goal:** N/A (test WU). Reuse `_set_state_dir`/`_clear_state_dir` + injected `now` for age determinism.
- **Spec requirements:** SPEC `## Affected Area` (Regression net row); deterministic fixtures per Open Questions.
- **Batch:** 1.

### WU-6 — Docs

- **Scope:** Document the bind/detect/re-arm model and add reverse-references.
- **TDD:** no (docs).
- **Files to modify:**
  - `user/scripts/CLAUDE.md` — under "Per-repo keyed state dir" (the owner-scoping D1/D2 + `bind_marker_session` prose): document the `--run-start`-time owner bind (bind-pending window eliminated), the `marker_owner_status` detect three-way + `reassert_marker_owner` re-arm + `--reassert-owner` action, and that `_bind_marker_on_allow` is now a confirming no-op for the normal path (retained for legacy). Reverse-reference `docs/bugs/single-slot-marker-ownership-race-disarms-owning-run`. [VERIFY: grep -n "bind_marker_session\|Owner-scoping" user/scripts/CLAUDE.md]
  - `CLAUDE.md` (root) — where marker ownership / the hooks architecture is described, note the closed single-slot ownership under-fire race + reverse-reference this bug. [VERIFY: grep -n "bind\|marker ownership\|session_id" CLAUDE.md]
- **Implementation goal:** Accurate prose matching the landed code; both reverse-references present (the bug doc's `**Related:**` already names these files — this closes the bidirectional cross-reference the spin-off contract requires).
- **Spec requirements:** SPEC `## Evidence Collected` → Related Documentation.
- **Batch:** 2 (docs land after the code they describe).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-5 (test, first) → WU-3 + WU-4 (impl) | Sequential (test-first) | WU-3/WU-4 edit scripts; WU-5 edits test file — no conflict |
| 2     | WU-6 (docs) | Solo | docs only |

**Phase-2 Runtime Verification (PHASES.md-owned, ticked by validation, NOT this plan):** `marker_owner_status` distinguishes `foreign-stamped` from `absent` on a live wrong-stamped marker (non-destructive); `--reassert-owner` re-claims for the orchestrator and is refused (exit 3) for a cycle subagent. (Both closed in-plan by WU-5's hermetic fixtures — `--test`-observable, not live-runtime rows.)

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until both phases are complete or a blocking issue triggers early exit:

1. **Select ready phase:** P1 first (no entry criteria); P2 only after P1's deliverables are checked off in PHASES.md.
2. **Announce:** Print "Implementing single-slot-marker-ownership-race-disarms-owning-run Phase N: [title]".
3. **Review prior context:** Re-read previously completed phases' Implementation Notes in PHASES.md — they take priority over this plan where they diverge.
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "Phase N: [title] — committed as [hash]".
7. **Loop:** Re-evaluate readiness; return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (task tool loading via ToolSearch, task creation for all WUs, the update protocol).

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow it. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and this plan file. Also re-read the live source of `lazy_core.py::read_run_marker` / `bind_marker_session` / `write_run_marker`, the `--run-start` handlers in both state scripts, and `lazy_guard.py::_bind_marker_on_allow` — line numbers in this plan are authoring-time anchors and may have drifted; the `[VERIFY: …]` greps are the ground truth.

#### Step B.1: Implement (test-first)

Read before proceeding: `~/.claude/skills/_components/tdd-protocol.md`, `~/.claude/skills/_components/subagent-launch.md`, `~/.claude/skills/_components/tdd-test-agent.md`, `~/.claude/skills/_components/implementation-agent.md`.

Test-first discipline: write the WU's failing fixture FIRST, run it, SEE IT RED, then implement until GREEN. Under the DISPATCH-LIMITED OVERRIDE, do this inline (Read/Edit/Write); otherwise dispatch test agent then impl agent via `Agent({model:"sonnet", …})`.

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` (and `~/.claude/skills/_components/mount-site-verification.md` for any new symbol). Produce a structured verdict (PASS / PASS-WITH-FIXES / NEEDS-REWORK). Specifically confirm: (a) the wrong-bind fixtures were RED before the fix; (b) `marker_owner_status` never `unlink()`s on `foreign-stamped`; (c) the diff did NOT remove or weaken `_bind_marker_on_allow` (it stays as the legacy backstop); (d) no SPEC/PHASES top-level status flip, no `FIXED.md`.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md` and follow it. Check off completed deliverables; add an Implementation Notes block (date, work completed, integration notes, pitfalls, files modified). Tick the `- [ ] WU-N` rows in this plan's Work Units list as each lands.

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/quality-gates.md` (and `.claude/skill-config/quality-gates.md`). Run the FULL coupled set — all must be green:

```
python3 user/scripts/test_lazy_core.py
python3 user/scripts/lazy-state.py --test
python3 user/scripts/bug-state.py --test
python3 user/scripts/lazy_parity_audit.py
```

The marker helpers are shared (`lazy_core`) and the `--run-start` + `--reassert-owner` edits are a coupled pair — `lazy_parity_audit.py` exit 0 is REQUIRED. If a `--test` baseline shifts intentionally (a new branch), regenerate the byte-pinned baseline ONLY via the `_normalize_smoke_output` helper, never by hand (see `user/scripts/CLAUDE.md` → Testing).

#### Step B.5: Commit Batch

Read `.claude/skill-config/commit-policy.md`. Conventional Commits (`fix(single-slot-marker-ownership-race): …`), no AI attribution, commit after each completed phase, push allowed (personal config repo). Re-assert `git rev-parse --abbrev-ref HEAD == main` immediately before each commit/push.

#### Step B.6: Proceed to Next Batch

Checklist (all must be true): review verdict produced; PHASES.md updated; all four gates green + parity exit 0; committed per policy. If any unchecked, complete it before the next batch.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md`. Confirm the bind (P1) and the detect/re-arm backstop (P2) compose: a run-start-bound owner is `owned-by-me`; a legacy unbound run that a foreign session bound is `foreign-stamped` and re-armable. No path deletes a live foreign-stamped marker.

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md`. (WU-6 already covers the two known doc surfaces; confirm no other CLAUDE.md describes the now-changed marker-ownership behavior.)

#### Commit and Push Post-Phase Changes

Per `.claude/skill-config/commit-policy.md`.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed phases** not yet committed.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** [phase] — [title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what to do]

   **Remaining phases (not attempted):**
   - [list]

4. **Do not attempt to work around the blocker.**

Blocking issues include:
- A `--test` baseline drift that cannot be reconciled as intentional (a genuine regression).
- `lazy_parity_audit.py` failing in a way that reveals the bind/`--reassert-owner` edits are NOT mirrored and cannot be made parity-equal.
- A quality-gate failure that can't be fixed after 2 retry attempts.
- A discovery that the orchestrator session_id is NOT actually available at `--run-start` in production (would contradict the resolved Open Question 1 — surface as NEEDS_INPUT, do not guess a workaround).
- Any error requiring architectural decisions beyond the SPEC's resolved scope.

---

## Completion

When both phases are complete:

1. **Run the full coupled gate suite one final time** (the four commands in Step B.4) — all green, parity exit 0.
2. **Set PHASES.md top-level `**Status:**` to `In-progress`** (implementation done, validation pending — NOT `Fixed`; the `__mark_fixed__` gate owns that flip after the validation tail).
3. **Print a completion report:**

   ## Implementation Batch — Complete

   **Bug:** single-slot-marker-ownership-race-disarms-owning-run
   **Total phases completed:** 2
   **Total commits:** M
   **Final quality-gate status:** all green (test_lazy_core / lazy-state --test / bug-state --test / lazy_parity_audit)

   **Commit log:**
   | Commit | Phase | Title |
   |--------|-------|-------|
   | … | P1 | Bind the marker to the owning session at `--run-start` |
   | … | P2 | Owner-side detect + re-arm backstop |

   **Implementation Notes summary:**
   [the run-start owner bind closed the bind-pending window for the normal path; the detect/re-arm helpers backstop legacy/un-threaded run-starts; `marker_owner_status` is non-destructive on `foreign-stamped` by contract.]
