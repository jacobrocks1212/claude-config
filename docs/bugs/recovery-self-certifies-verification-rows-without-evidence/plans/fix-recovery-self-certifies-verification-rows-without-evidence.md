---
kind: fix-plan
feature_id: recovery-self-certifies-verification-rows-without-evidence
status: Ready
created: 2026-06-19
complexity: simple
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-19.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\recovery-self-certifies-verification-rows-without-evidence\plans\fix-recovery-self-certifies-verification-rows-without-evidence.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — recovery-self-certifies-verification-rows-without-evidence

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\recovery-self-certifies-verification-rows-without-evidence\PHASES.md` (recovery-self-certifies-verification-rows-without-evidence, 1 phase)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\recovery-self-certifies-verification-rows-without-evidence\SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.ts`, `.js`, `.cs`, `.vue`, `.py`, `.rs`, `.tsx`, `.jsx`, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **DISPATCH-LIMIT OVERRIDE (when this plan is executed by a /lazy-bug-batch cycle subagent that lacks the `Agent` tool):** perform ALL implementation INLINE with Read / Edit / Write, test-first per WU. The orchestrator-vs-subagent split above is the workstation default; the cycle-subagent override supersedes it when no `Agent` tool is available. Never write `BLOCKED.md` because of the dispatch limit.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `.claude/skill-config/quality-gates.md` (fallback: `~/.claude/skills/_components/quality-gates.md`) |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

- `docs/features/lazy-cycle-containment/SPEC.md` §C5 + `PHASES.md` Phase 7 — the sanctioned design for the recovery grep-and-cite gate (already Complete with passing tests). This plan adds the SYMMETRIC pinning test for the loop-block receipt ban; it does NOT touch the §C5 work.
- The contract under test (`loop-block.md`'s receipt-authoring ban) already landed in commit `dfbcfa0`. This plan only pins it against silent regression.

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. **(See the dispatch-limit override above — when executed by an Agent-less cycle subagent, do it all inline, test-first.)**
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents
3. Every TDD work unit goes through the test-first pipeline — failing tests written and confirmed RED before implementation
4. PHASES.md is updated AFTER EACH batch completes (not deferred)
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing
6. Mistakes are fixed immediately before launching the next batch
7. After all batches in a phase finish, integration verification confirms all changes work together
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it
9. Each completed phase is committed and pushed before the next phase begins
10. Cross-feature phases may run in parallel when dependencies are satisfied and no file conflicts exist (N/A here — single phase, single WU)
11. This plan is self-contained — follow it exactly as written without relying on external context
12. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory

## Work Units

- [ ] WU-1 — Pin the loop-block receipt-authoring ban with a static regression test (Phase 1)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | recovery-self-certifies-verification-rows-without-evidence | P1 | Pin loop-block receipt ban | — | Solo |

Single phase, single WU.

---

### Phase: recovery-self-certifies-verification-rows-without-evidence P1 — Pin loop-block receipt ban

**Goal:** Add a single static (`pytest`) docs-consistency test asserting `loop-block.md` carries the receipt-authoring ban on disk — symmetric with the existing recovery-gate pinning test — so a future edit cannot silently drop the ban. No production source / prose change: the contract under test already exists on disk (`dfbcfa0`).
**Entry criteria:** None (first and only phase).
**SPEC.md references:** `## Proven Findings` item 3 (residual gap: loop-block ban has no pinning test); `## Fix Scope` (add a static docs-consistency test co-located with the recovery-gate tests); `## Affected Area` "Regression test" row; `## Open Questions` (test mechanism settled as static on-disk mirror of `test_dispatch_recovery_component_carries_grep_and_cite_gate`).

#### WU-1 — Pin the loop-block receipt-authoring ban with a static regression test

- **Scope:**
  - [ ] `user/scripts/test_project_skills.py`: add a module-level path constant `_LOOP_BLOCK_PATH` resolving to `user/skills/_components/lazy-batch-prompts/loop-block.md`, mirroring `_DISPATCH_RECOVERY_PATH`'s form (`Path(__file__).resolve().parents[1] / "skills" / "_components" / "lazy-batch-prompts" / "loop-block.md"`).
  - [ ] `user/scripts/test_project_skills.py`: add a module-level marker tuple `_LOOP_BLOCK_RECEIPT_BAN_MARKERS` enumerating the stable substrings that prove the ban: the four banned receipts (`"VALIDATED.md"`, `"SKIP_MCP_TEST.md"`, `"COMPLETED.md"`, `"FIXED.md"`) AND the two permitted-only sentinels (`"NEEDS_INPUT.md"`, `"BLOCKED.md"`), mirroring `_RECOVERY_GATE_MARKERS`.
  - [ ] `user/scripts/test_project_skills.py`: add `test_loop_block_component_carries_receipt_authoring_ban()` — read `_LOOP_BLOCK_PATH.read_text(encoding="utf-8")` and assert every marker in `_LOOP_BLOCK_RECEIPT_BAN_MARKERS` is `in` the text, with a descriptive failure message naming the missing marker (mirror `test_dispatch_recovery_component_carries_grep_and_cite_gate`).
- **TDD:** yes (static contract test). Confirm the assertion is genuinely load-bearing: it must FAIL if any banned-receipt or permitted-sentinel name is removed from `loop-block.md`, and PASS against the current on-disk file. Demonstrate RED by transiently confirming the assertion logic catches an absent marker — do NOT commit any modification to `loop-block.md`.
- **Files to create/modify:**
  - `user/scripts/test_project_skills.py` — new `_LOOP_BLOCK_PATH` constant + `_LOOP_BLOCK_RECEIPT_BAN_MARKERS` tuple + `test_loop_block_component_carries_receipt_authoring_ban`, co-located with the recovery-gate tests [VERIFY: grep -n "test_dispatch_recovery_component_carries_grep_and_cite_gate" user/scripts/test_project_skills.py] (confirmed at `:657`; `_DISPATCH_RECOVERY_PATH` at `:629`, `_RECOVERY_GATE_MARKERS` at `:635`).
- **Test files:** `user/scripts/test_project_skills.py` (same file — this WU's deliverable IS a test).
- **Test expectations:**
  - `test_loop_block_component_carries_receipt_authoring_ban` PASSES against the current `loop-block.md` (all six markers present — confirmed on disk at `user/skills/_components/lazy-batch-prompts/loop-block.md` lines 28–36).
  - The test would FAIL if any of the four banned-receipt names (`VALIDATED.md` / `SKIP_MCP_TEST.md` / `COMPLETED.md` / `FIXED.md`) or the two permitted-only sentinel names (`NEEDS_INPUT.md` / `BLOCKED.md`) were dropped from `loop-block.md` — proving it pins the ban.
  - The full file passes: `python -m pytest user/scripts/test_project_skills.py` (the existing recovery-gate tests stay GREEN — this WU only ADDS a test).
- **Implementation goal:** `test_project_skills.py` gains a static on-disk assertion that `loop-block.md` carries the receipt-authoring ban, symmetric with the existing `test_dispatch_recovery_component_carries_grep_and_cite_gate`. Both evidence side-doors (recovery grep-and-cite + loop-block receipt ban) are now regression-pinned, closing the SPEC's residual asymmetric-coverage gap.
- **Spec requirements:** SPEC `## Proven Findings` item 3; `## Fix Scope` (in-scope: the static test; out-of-scope: any prose rewrite or runtime hook); `## Affected Area` "Regression test" row.
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | N/A             |

**Quality gates (run after the batch lands):**
- `python -m pytest user/scripts/test_project_skills.py` (full file green, including the new test).

**Out of scope (do NOT do):** Re-writing `dispatch-recovery.md` / `loop-block.md` / `SKILL.md` line 789 / `completion-integrity-gate.md` prose (already correct — `## Fix Scope`); any runtime-enforcement hook; any change to `loop-block.md` itself (the ban already exists on disk via `dfbcfa0`).

---

## Definition of Done

- [ ] WU-1 complete: `test_loop_block_component_carries_receipt_authoring_ban` added to `user/scripts/test_project_skills.py`, co-located with the recovery-gate tests.
- [ ] `python -m pytest user/scripts/test_project_skills.py` passes (new test + all existing tests green).
- [ ] PHASES.md Phase 1 deliverables ticked; top-level PHASES `**Status:**` → `In-progress` (validation pending; the `__mark_fixed__` flip is the orchestrator's, never set here).
- [ ] No change to `loop-block.md` or any other production / prose file (test-only fix).
