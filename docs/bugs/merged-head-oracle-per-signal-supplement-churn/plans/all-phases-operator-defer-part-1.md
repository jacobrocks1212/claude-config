---
kind: implementation-plan
feature_id: merged-head-oracle-per-signal-supplement-churn
status: In-progress
created: 2026-07-19
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan {this-path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 2. Sibling parts:
> - part 1: `C:/Users/Jacob/source/repos/claude-config/docs/bugs/merged-head-oracle-per-signal-supplement-churn/plans/all-phases-operator-defer-part-1.md` (this file)
> - part 2: `C:/Users/Jacob/source/repos/claude-config/docs/bugs/merged-head-oracle-per-signal-supplement-churn/plans/all-phases-operator-defer-part-2.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — merged-head-oracle-per-signal-supplement-churn (part 1: feature-side branch + oracle-supplement retirement)  (v2)

**PHASES.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/bugs/merged-head-oracle-per-signal-supplement-churn/PHASES.md` (Phases 1–2 of 3)

**SPEC.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/bugs/merged-head-oracle-per-signal-supplement-churn/SPEC.md`

**Total phases in this part:** 2 (Phase 1, Phase 2)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is **claude-config**, a harness-config repo whose gates are Python/`--test`/parity, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config gates)

- **This is a coupled-pair-sensitive change.** Phase 1 edits `lazy-state.py::compute_state`. The bug pipeline (`bug-state.py`) is the parity MODEL and is **NOT edited** — the feature-side branch is a JUSTIFIED feature/bug divergence being CLOSED (feature side gains the branch the bug side already has). Run `python3 user/scripts/lazy_parity_audit.py --repo-root .` after Phase 1 and confirm exit 0 (Phase 3 in part-2 owns the final parity confirmation + any manifest divergence note).
- **Quality gates for this repo (run from repo root):**
  - `python3 user/scripts/lazy-state.py --test` (feature state-machine smoke; regenerate `user/scripts/tests/baselines/lazy-state-test-baseline.txt` ONLY via the `_normalize_smoke_output` helper in `user/scripts/tests/test_lazy_core/_util.py` — never hand-edit the baseline).
  - `python3 user/scripts/bug-state.py --test` (must stay green — shared `lazy_core`).
  - `python3 -m pytest user/scripts/tests/test_lazy_core/ -q` (shared-helper + oracle unit suite).
  - `python3 user/scripts/lazy_parity_audit.py --repo-root .` (coupled-pair parity; exit 0).
- **No MCP runtime.** This part has no MCP-reachable surface — verification is the deterministic `--test` / `pytest` battery above. Do NOT author or await any `/mcp-test` work unit.
- **Commit after each phase** to `main` (per the repo's push-after-every-commit policy; re-read `.claude/skill-config/commit-policy.md` before committing). Stage ONLY the paths this plan changed (pathspec-scoped), never `git add -A`.

## References — Upstream Artifacts

> | Upstream feature | Kind | Path | Why this plan references it |
> |------------------|------|------|------------------------------|
> | (none — this bug has no completed hard upstream dependencies) | | | |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | operator-defer | P1 | Feature-side operator-defer branch | — | Solo (Batch 1) |
| 2    | operator-defer | P2 | Retire the oracle file-predicate supplement | P1 (feature branch must land first) | Solo (Batch 2) |

## Work Units

- [ ] WU-1 — Feature-side operator-defer branch in `lazy-state.py::compute_state`
- [ ] WU-2 — Retire the `_candidate_operator_deferred` supplement in `dispatch.py`

---

### Phase: operator-defer P1 — Feature-side operator-defer branch (fixes the near-neighbor)

**Goal:** Give the FEATURE `compute_state` walk loop a bare-`DEFERRED.md` operator-defer skip branch mirroring `bug-state.py:1126–1160`, so an operator-deferred feature is non-dispatchable at its own pipeline (fixes Reproduction A directly; enables Phase 2).
**Entry criteria:** None (first phase).
**SPEC.md references:** `## Proven Findings` (root cause + serving-path trace), `## Affected Area` row "Feature state machine", `## Open Questions` (scoped-identity terminal shape).

#### WU-1 — Feature-side operator-defer branch in `lazy-state.py::compute_state`

- **Scope (PHASES Phase 1 deliverables):**
  - Feature-side scoped constants `TR_OPERATOR_DEFERRED_SCOPED` + `STEP_OPERATOR_DEFERRED_SCOPED`.
  - Module-level `_OPERATOR_DEFERRED: list[str]` accumulator + per-`compute_state` `.clear()` + an `operator_deferred` probe-dict key.
  - The bare-`DEFERRED.md` skip branch in the `compute_state` walk loop (scoped-match returns a scoped identity; unscoped appends + `_diag` + `continue`).
  - The global `all-remaining-deferred`-shaped feature terminal when only operator-deferred features remain.
  - `--test` fixtures + regenerated baseline.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy-state.py` — constants (beside the existing `TR_*_SCOPED` block ~lines 414–438), the `_OPERATOR_DEFERRED` module list + its `.clear()` at the top of `compute_state`, the walk-loop branch, the global terminal in the `current is None` block, the probe-dict `operator_deferred` key, and the new in-file `--test` fixtures + their registration in the test-list block.
  - `user/scripts/tests/baselines/lazy-state-test-baseline.txt` — regenerated output.
- **Test files:** the in-file `--test` harness inside `user/scripts/lazy-state.py` (this repo's state scripts carry their own `def test_*` smoke harness run with `--test`, NOT pytest — mirror an existing nearby fixture for scaffolding).
- **Test expectations (write these fixtures RED first — the feature currently routes `/spec` on a `DEFERRED.md` dir):**
  - `operator-deferred-skip` — two features, one carrying `DEFERRED.md` (parked, skipped) and one actionable (dispatched); assert the deferred feature is skipped and `operator_deferred` names it, the actionable one dispatches. Mirror `bug-state.py`'s `operator-deferred-skip` fixture [VERIFY: grep -n "operator-deferred-skip" user/scripts/bug-state.py].
  - `all-operator-deferred` — only feature has `DEFERRED.md` → the global `all-remaining-deferred`-shaped terminal fires, `operator_deferred` non-empty, `feature_id` is None. Mirror `bug-state.py`'s `all-operator-deferred` fixture [VERIFY: grep -n "all-operator-deferred" user/scripts/bug-state.py].
  - scoped-operator-deferred — a `--feature-id` scoped probe on the deferred feature returns its OWN identity + `terminal_reason == TR_OPERATOR_DEFERRED_SCOPED`, NOT a null-identity global terminal. Mirror `bug-state.py:1138–1150` [VERIFY: grep -n "TR_OPERATOR_DEFERRED_SCOPED" user/scripts/bug-state.py].
  - control — with `DEFERRED.md` removed, the same feature is dispatchable (routes `/spec` at Step 4), proving the branch is the only thing excluding it.
- **Implementation goal:** Reuse `docmodel.spec_dir_operator_deferred(spec_path)` [VERIFY: grep -n "def spec_dir_operator_deferred" user/scripts/lazy_core/docmodel.py] for the predicate (do NOT re-implement a `DEFERRED.md` existence check inline), and reuse `_scoped_skip_state(...)` [VERIFY: grep -n "def _scoped_skip_state" user/scripts/lazy-state.py] for the scoped return exactly as the sibling cloud/device/host scoped branches do. Add `TR_OPERATOR_DEFERRED_SCOPED` to `lazy_core.SANCTIONED_STOP_TERMINAL` [VERIFY: grep -n "SANCTIONED_STOP_TERMINAL" user/scripts/lazy_core/markers.py] ONLY if the bug-side literal `all-remaining-deferred` / `operator-deferred` is registered there for the bug pipeline (match the bug side's registration exactly — do not over-register). Place the new branch in the walk loop to mirror the bug-side ordering (before the park branches). The unscoped path must stay byte-identical for a feature with no `DEFERRED.md` (the `--test` `baseline-regression-default` fixtures must stay green).
- **Spec requirements:** SPEC `## Affected Area` — "ADD a bare-`DEFERRED.md` operator-defer skip branch mirroring `bug-state.py:1126–1160` (skip + `_OPERATOR_DEFERRED` accumulator + scoped-identity terminal + probe key + notify message)". SPEC `## Open Questions` #2 — mirror the bug-side scoped-identity terminal (resolved: mirror, per the PHASES ⚖ decision).
- **Batch:** 1.

> **Batch overview (Phase 1):**
>
> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-1      | Solo      | N/A             |

**Post-phase:** run the claude-config gate battery (`lazy-state.py --test`, `bug-state.py --test`, `pytest user/scripts/tests/test_lazy_core/`, `lazy_parity_audit.py --repo-root .`), commit + push to `main`, then proceed to Phase 2.

---

### Phase: operator-defer P2 — Retire the oracle file-predicate supplement

**Goal:** Remove the `_candidate_operator_deferred` supplement + `_op_defer_dir` plumbing from `dispatch.py::merged_head_nondispatchable_ids` so the PRIMARY `is_dispatchable(scoped_probe(iid))` mechanism carries operator-deferred-feature exclusion for both pipelines (fixes Reproduction B; the durable generalization).
**Entry criteria:** Phase 1 complete — the feature `compute_state` operator-defer branch must exist, so `is_dispatchable(scoped_probe(feature))` returns false for a deferred feature. The R102 oracle regression staying green with the supplement removed IS the acceptance gate.
**SPEC.md references:** `## Evidence Collected` → "The oracle (surface)", `## Affected Area` row "Merged-head oracle", `## Open Questions` #1 (retire vs keep — resolved: retire, per the PHASES ⚖ decision).

#### WU-2 — Retire the `_candidate_operator_deferred` supplement in `dispatch.py`

- **Scope (PHASES Phase 2 deliverables):**
  - Remove `_candidate_operator_deferred(iid)` and its `_op_defer_dir` map construction.
  - Remove the walk-loop application so each candidate falls through to the primary `is_dispatchable(scoped_probe(iid))` classification, preserving the break-on-first-dispatchable-head / exclude-and-continue control flow.
  - Confirm no other caller references the removed symbols; leave `spec_dir_operator_deferred` (docmodel) intact.
  - Keep the R102 oracle regression green with the supplement gone; add a feature-exclusion regression.
- **TDD:** yes (regression-driven — the existing R102 test is the RED-if-broken guard; add the new feature-exclusion test before/with the removal).
- **Files to create/modify:**
  - `user/scripts/lazy_core/dispatch.py` — remove `_candidate_operator_deferred` (~lines 770–774), the `_op_defer_dir` map plumbing (~lines 750–768), and the walk-loop application (~lines 822–829). [VERIFY: grep -n "_candidate_operator_deferred\|_op_defer_dir" user/scripts/lazy_core/dispatch.py]
  - `user/scripts/tests/test_lazy_core/test_dispatch.py` — retain the R102/cross-pipeline oracle regressions (must pass with the supplement gone); add a regression asserting an operator-deferred FEATURE at the cross-pipeline merged head is excluded via `is_dispatchable(scoped_probe)` alone.
- **Test files:** `user/scripts/tests/test_lazy_core/test_dispatch.py` (pytest).
- **Test expectations:**
  - The existing cross-pipeline oracle regressions stay green [VERIFY: grep -n "oracle_excludes_nondispatchable" user/scripts/tests/test_lazy_core/test_dispatch.py] — the primary mechanism now carries what the file-predicate used to.
  - New: a `--emit-prompt` subprocess test with an operator-deferred FEATURE (`DEFERRED.md`, `reason: operator-excluded`) at the merged head ABOVE a workable bug — assert NO withhold (`route_overridden_by is None`) and the workable bug dispatches, proving the feature is excluded by `is_dispatchable(scoped_probe(feature))` with NO file-predicate present. Model it on the existing `test_subprocess_emit_prompt_oracle_excludes_nondispatchable_bug_head_no_withhold` / `..._feature_head_no_withhold` fixtures (same `write_run_marker` + `LAZY_STATE_DIR` + subprocess `--repeat-count --probe --emit-prompt` idiom).
- **Implementation goal:** After removal, the oracle's walk loop classifies every candidate solely via `is_dispatchable(scoped_probe(iid))` [VERIFY: grep -n "is_dispatchable(scoped_probe" user/scripts/lazy_core/dispatch.py]. Do NOT remove `docmodel.spec_dir_operator_deferred` — it is still consumed by both `compute_state`s' own park/skip classification (Phase 1's new feature branch is now one of its consumers). If the R102 regression FAILS after removal, STOP: Phase 1's feature branch is not covering the case — that is the exact gate on the ⚖ retire decision; do not force the removal (write `BLOCKED.md` naming the failing regression).
- **Spec requirements:** SPEC `## Proven Findings` fix-site-on-path ("Once present, `is_dispatchable(scoped_probe(feature))` returns false … so the `dispatch.py:822` supplement becomes redundant and retires"). SPEC `## Open Questions` #1 (retire, gated on R102 regression green).
- **Batch:** 2 (after Phase 1; no file overlap with WU-1 but a hard dependency edge).

> **Batch overview (Phase 2):**
>
> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 2     | WU-2      | Solo      | N/A (depends on Phase 1) |

**Post-phase:** run the full gate battery, commit + push to `main`. Part 1 complete → proceed to part 2 (Phase 3: docstrings, parity confirmation, full battery).

---

## Runtime Verification

*(No runtime-observable surface — this part is pure static state-machine logic verified by the deterministic `--test` / `pytest` battery. No `<!-- verification-only -->` rows: there is no live runtime to defer to `/mcp-test`.)*

**MCP Integration Test Assertions:** N/A — no runtime-observable behavior; MCP runtime not-required (see PHASES.md header).
