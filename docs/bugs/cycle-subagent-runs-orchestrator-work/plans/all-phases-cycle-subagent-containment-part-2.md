---
kind: implementation-plan
feature_id: cycle-subagent-runs-orchestrator-work
status: In-progress
created: 2026-06-16
complexity: complex
phases: [3, 4]
---

> **Mobile plan** — generated by `/write-plan` (via `/plan-bug`) on 2026-06-16.
> To execute: `/execute-plan <this-file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 2. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-subagent-runs-orchestrator-work\plans\all-phases-cycle-subagent-containment-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-subagent-runs-orchestrator-work\plans\all-phases-cycle-subagent-containment-part-2.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — Cycle Subagent Runs Orchestrator Work (Part 2: Defense-in-Depth + Prompt Reshape)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-subagent-runs-orchestrator-work\PHASES.md` (cycle-subagent-runs-orchestrator-work, 4 phases — this part covers Phases 3–4)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-subagent-runs-orchestrator-work\SPEC.md`

**Total phases:** 4 (this part: 2 — Phases 3 and 4)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Work Units

- [x] WU-1 — Deny the `/lazy*` Skill-tool path for cycle subagents in the C2 hook (Phase 3)
- [ ] WU-2 — Reshape `cycle-base-prompt.md` to a worker brief; de-enumerate the orchestrator API (Phase 4)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.sh`, `.py`, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`, and component prose under `user/skills/_components/` (Phase 4's deliverable IS prose — treat the `cycle-base-prompt.md` edit as the WU's work product, dispatched or inline per the override).

> **INLINE-EXECUTION OVERRIDE (applies when this plan runs inside a `/lazy-bug-batch` cycle subagent):** a cycle subagent has NO `Agent` tool — perform ALL implementation INLINE with `Read`/`Edit`/`Write`, still test-first (here: lint/projection gates), still per-WU. The only surviving prohibition is never invoking another `/lazy*` orchestrator.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `.claude/skill-config/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| Part 1 (this bug) | hard | `…/plans/all-phases-cycle-subagent-containment-part-1.md` | Phases 3–4 are layered ON the mechanical keystone fixed in Part 1; do NOT start until Part 1 is Complete. |

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. ALL implementation work delegated to Sonnet subagents — UNLESS the inline-execution override applies, then inline.
2. Edits happen in the current worktree — never create worktrees.
3. PHASES.md updated AFTER each batch.
4. Every output reviewed before continuing.
5. Quality gates pass (100%) before proceeding.
6. Each completed phase committed and pushed before the next.
7. This plan is self-contained.
8. Before each step, `Read` the listed component files from disk.
9. **PARITY:** the C2 hook (WU-1) is a single shared file; the prompt component (WU-2) feeds all three orchestrators via projection — run `project-skills.py` + `lint-skills.py` + `lazy_parity_audit.py --report` after WU-2.
10. **SEQUENCING (HARD):** Phase 4's prompt reshape promises "the harness will DENY these in-flight" WITHOUT naming the commands — that promise is only TRUE after Part 1's mechanical guards land. Do NOT execute this part before Part 1 is Complete.

## Execution Schedule

| Step | Feature | Phase(s) | Title | Blocked by | Parallel? |
|------|---------|----------|-------|------------|-----------|
| 1 | cycle-subagent | P3 | Deny `/lazy*` Skill-tool path (defense-in-depth) | Part 1 (P1+P2) | Solo |
| 2 | cycle-subagent | P4 | Reshape cycle prompt (occurrence-rate) | P3 | Solo |

---

### Phase: cycle-subagent P3 — Deny the `/lazy*` Skill-tool path for cycle subagents (defense-in-depth)

**Goal:** Close the unintercepted Skill-tool path — a subagent must not invoke `/lazy*` via the Skill tool.
**Entry criteria:** Part 1 complete (mechanical keystone closed).
**SPEC.md references:** Candidate Fix #3; Theory 4 (secondary path); seam-table rows 2 + 4.

#### WU-1 — Deny the `/lazy*` Skill-tool path for cycle subagents in the C2 hook

- **Scope:** PHASES.md Phase 3 deliverables 1–3 (Skill-tool intercept branch; family regex; fail-OPEN preserved).
- **TDD:** yes.
- **Files to modify:** `user/hooks/lazy-cycle-containment.sh` — add a `Skill`-tool branch in the inline Python `main()` alongside the `Agent`/`Task` branch: when `is_subagent` (agent_id present) AND the resolved skill name matches `^/?lazy(-bug)?(-batch)?(-cloud)?$`, `_deny(CORRECTIVE)`; main-thread (no agent_id) → allow; non-lazy skill → allow. [VERIFY: grep -n 'tool_name in ("Agent", "Task")' user/hooks/lazy-cycle-containment.sh] [VERIFY: grep -n "is_subagent" user/hooks/lazy-cycle-containment.sh]
- **Test files:** the hook test module from Part 1 WU-5 (or add to it).
- **Test expectations:** `{"agent_id":"x","tool_name":"Skill","tool_input":{"skill":"lazy-batch"}}` → deny. Same without `agent_id` → allow. A non-lazy skill (`{"skill":"commit"}`) with `agent_id` → allow. Cover the family: `lazy`, `lazy-bug`, `lazy-batch`, `lazy-bug-batch`, `lazy-cloud`, `lazy-batch-cloud` all deny for a subagent. An unrecognized payload shape / missing skill-name field → allow (fail-OPEN). **Before hardcoding the field name**, confirm the exact field the `Skill` PreToolUse payload uses for the skill name (inspect a real payload or the installed hook-input schema); record the confirmed field in PHASES Implementation Notes. [VERIFY: grep -n "tool_input" user/hooks/lazy-cycle-containment.sh]
- **Implementation goal:** The Skill tool is intercepted for the lazy family from a subagent, closing the path that bypasses every Bash/Agent guard. Fail-OPEN on any internal error (consistent with the hook's existing contract).
- **Spec requirements:** SPEC Candidate Fix #3; seam row 4 (no allowlist/denylist exists; `/lazy-batch` Skill-reachable). Note the SPEC Open Question alternative (exclude the Skill tool from the subagent's tool surface entirely) — if that proves cleaner and is achievable via the dispatch's `subagent_type`/tool restriction, record it as a follow-up but ship the hook denylist here (it is the in-scope mechanical fix).
- **Batch:** 1.

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1 | WU-1 | Solo | N/A |

**Phase 3 Runtime Verification** *(checked by the hook harness):*
- [ ] `python -m pytest user/scripts/ -q` green (Skill-tool deny/allow tests).
- [ ] Manual hook check: subagent `Skill` payload invoking `lazy-batch` → deny; non-lazy skill or main-thread → allow.

---

### Phase: cycle-subagent P4 — Stop teaching the subagent the orchestrator playbook (prompt-side, occurrence-rate — NOT load-bearing)

**Goal:** Lower the trigger rate by reshaping the cycle prompt from an operator brief to a worker brief, without enumerating the orchestrator API.
**Entry criteria:** Phases 1–3 complete (the "harness will DENY in-flight" assurance is now true).
**SPEC.md references:** Candidate Fix #4; Theory 5 / Proven Finding 3 (prohibition-as-how-to).

> ⚖ policy: include SPEC fix #4 (complementary) → in-scope as final phase (scope-class — lowers occurrence, fully specified by the concluded SPEC; explicitly NOT the sole/primary fix).

#### WU-2 — Reshape `cycle-base-prompt.md` to a worker brief; de-enumerate the orchestrator API

- **Scope:** PHASES.md Phase 4 deliverables 1–4 (soften opening framing; de-enumerate TERMINAL STOP; preserve inline-override prohibition; re-expand + lint).
- **TDD:** no (prose; verified by projection + lint + parity gates).
- **Files to modify:** `user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`.
  - Opening framing: soften the "You are advancing one cycle of the autonomous {pipeline_phrase}" line (and the cloud variant) toward a worker framing ("Run exactly one skill, then stop"), keeping enough context that the worker knows which artifact it owns. [VERIFY: grep -n "advancing one cycle" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md]
  - TERMINAL STOP: replace the explicit `--run-end`/`--run-start`/`--apply-pseudo`/`--enqueue-adhoc`/`dev:kill`/`dev:restart` command list with a categorical instruction ("Do not run pipeline/orchestration or lifecycle commands, and do not invoke any `/lazy*` skill; if you finish, are blocked, or unsure, write your report and STOP"). KEEP the "the harness will DENY these in-flight" assurance (now true); DROP the command catalog. [VERIFY: grep -n "TERMINAL STOP" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md]
  - Leave the `inline-override` "never invoke another /lazy or /lazy-batch" prohibition intact; ensure it no longer reads as the ONLY surviving prohibition now that TERMINAL STOP covers lifecycle ops categorically. [VERIFY: grep -n "never invoke another /lazy" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md]
- **Test files:** none (lint/projection are the gates).
- **Test expectations / verification:** `python user/scripts/project-skills.py` re-expands with no broken `!cat` / circular include; the projected `cycle-base-prompt` shows the worker framing and the de-enumerated TERMINAL STOP (grep the projected output to confirm the literal `--run-start`/`--run-end` command list no longer appears INSIDE the cycle prompt); `{pipeline_phrase}` and the cloud-variant token substitution still resolve. `lint-skills.py --check-projected --check-capabilities` clean. `lazy_parity_audit.py --report` shows the three orchestrators stay in lockstep.
- **Implementation goal:** The cycle prompt reads as a one-shot worker brief; the orchestrator API is no longer handed to the subagent as a how-to. This is occurrence-rate reduction LAYERED on the Part 1 mechanical guards — never shipped as the sole fix.
- **Spec requirements:** SPEC Candidate Fix #4; Proven Finding 3 (the prompt raises the trigger rate by teaching the behavior).
- **Batch:** 2.

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 2 | WU-2 | Solo | N/A |

**Phase 4 Runtime Verification** *(checked by the skill-lint gates):*
- [ ] `python user/scripts/project-skills.py` clean (no circular-include / missing-component).
- [ ] `python user/scripts/lint-skills.py --check-projected --check-capabilities` clean.
- [ ] `python user/scripts/lazy_parity_audit.py --report` — no unexplained drift after the prose reshape.

**Completion (gate-owned):** When WU-2 lands (the LAST phase of the whole bug), set the top-level PHASES `**Status:**` to `In-progress` (implementation done, validation pending) and STOP. Do NOT flip SPEC `**Status:**` to Fixed, do NOT write `FIXED.md`, do NOT archive — all owned by the orchestrator's `__mark_fixed__` gate after the validation tail.

## Execution Protocol

### Phase Selection Loop

Repeat until both phases complete or a blocking issue triggers early exit:

1. **Select ready phase:** P3 first (entry: Part 1 complete), then P4 (entry: P3 complete).
2. **Announce:** "Implementing cycle-subagent Phase N: [title]".
3. **Review prior context:** Re-read PHASES.md Implementation Notes (incl. Part 1's agent_id-on-Bash resolution).
4. **Execute all batches** per Per-Batch Steps.
5. **Run Post-Phase Steps.**
6. **Report:** "cycle-subagent Phase N — committed".
7. **Loop.**

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)

Read `~/.claude/skills/_components/task-tracking.md` and follow it. Create/track tasks for WU-1 and WU-2.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)

Read `~/.claude/skills/_components/source-reread.md`. Re-read PHASES.md, SPEC.md (Theory 4 + Theory 5 sections), and this plan.

#### Step B.1: Launch Subagents (or implement INLINE if no Agent tool)

Read `~/.claude/skills/_components/subagent-launch.md`. Dispatch the hook/prose edits to a Sonnet subagent, or implement inline under the override.

#### Step B.2: Review Batch Output (MANDATORY GATE)

Read `~/.claude/skills/_components/subagent-review.md`. Produce a verdict. For WU-1, the critical review item is fail-OPEN preservation + the confirmed Skill payload field name. For WU-2, the critical item is that the assurance line is kept while the command catalog is dropped.

#### Step B.3: Update PHASES.md (MANDATORY)

Read `~/.claude/skills/_components/phases-update.md`. Check off deliverables; add Implementation Notes (incl. the Skill-payload field name confirmed in WU-1, and any follow-up note on excluding the Skill tool from the subagent surface).

#### Step B.4: Run Quality Gates (MANDATORY)

Read `.claude/skill-config/quality-gates.md`. Run:
- WU-1: `python -m pytest user/scripts/ -q`
- WU-2: `python user/scripts/project-skills.py` + `python user/scripts/lint-skills.py --check-projected --check-capabilities` + `python user/scripts/lazy_parity_audit.py --report`
100% pass required.

#### Step B.5: Commit Batch (ATOMIC GATE+COMMIT)

Read `.claude/skill-config/commit-policy.md`. Conventional Commits, no AI attribution, push to `main`. Chain gate+commit+push as ONE command.

#### Step B.6: Proceed to Next Batch

Checklist: review verdict; PHASES.md updated; gates pass; committed+pushed. If any unchecked, complete it first.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY)

Read `~/.claude/skills/_components/integration-verification.md`. For P3, confirm the Skill-tool deny composes with the Bash/Agent denies from Part 1 (all three orchestrator-emulation paths now denied for a subagent). For P4, confirm the projected prompt across all three orchestrators reads as a worker brief.

#### Update CLAUDE.md Files (MANDATORY)

Read `~/.claude/skills/_components/claude-md-review.md`. Update the C2-hook description in `CLAUDE.md` (project root, Hooks table) if the Skill-tool intercept warrants a note.

#### Commit and Push Post-Phase Changes

Per `.claude/skill-config/commit-policy.md`. Push to `main`.

## Blocking Issue Protocol

If blocked: stop, commit completed phases, print a blocking-issue report, write `BLOCKED.md` per `~/.claude/skills/_components/sentinel-frontmatter.md` if a human decision is needed. Do NOT work around it. Blocking issues: a lint/parity failure unfixable after 2 attempts; inability to confirm the Skill payload field name; any architectural decision beyond the specs.

## Completion

When both phases complete:

1. Run the full gate suite once more: `python -m pytest user/scripts/ -q`, both `--test` suites, `lazy_parity_audit.py --report`, `project-skills.py`, `lint-skills.py --check-projected --check-capabilities`.
2. Print a completion report.
3. Set the top-level PHASES `**Status:**` to `In-progress` (last phase landed; validation pending). STOP. Do NOT flip SPEC `**Status:**` or write `FIXED.md` — gate-owned.
