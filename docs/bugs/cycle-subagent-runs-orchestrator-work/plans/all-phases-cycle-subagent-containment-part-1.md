---
kind: implementation-plan
feature_id: cycle-subagent-runs-orchestrator-work
status: Ready
created: 2026-06-16
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` (via `/plan-bug`) on 2026-06-16.
> To execute: `/execute-plan <this-file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 2. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-subagent-runs-orchestrator-work\plans\all-phases-cycle-subagent-containment-part-1.md` (this file)
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-subagent-runs-orchestrator-work\plans\all-phases-cycle-subagent-containment-part-2.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — Cycle Subagent Runs Orchestrator Work (Part 1: Mechanical Keystone)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-subagent-runs-orchestrator-work\PHASES.md` (cycle-subagent-runs-orchestrator-work, 4 phases — this part covers Phases 1–2)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-subagent-runs-orchestrator-work\SPEC.md`

**Total phases:** 4 (this part: 2 — Phases 1 and 2, the load-bearing mechanical fix)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Work Units

- [ ] WU-1 — Export `LAZY_ORCHESTRATOR=1` from the three orchestrators (Phase 1)
- [ ] WU-2 — Env-var immunity/refusal unit tests + lazy_core cross-ref comment (Phase 1)
- [ ] WU-3 — `refuse_cycle_marker_mutation_if_subagent` guard helper + unit tests (Phase 2)
- [ ] WU-4 — Wire the marker-mutation guard into `--cycle-end`/`--cycle-begin` in both state machines + `--test` fixtures (Phase 2)
- [ ] WU-5 — Add `--cycle-end`/`--cycle-begin` to C2 hook `LOOP_FORMATION_FLAGS` + hook test (Phase 2)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.ts`, `.js`, `.cs`, `.vue`, `.py`, `.rs`, `.tsx`, `.jsx`, `.sh`, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **INLINE-EXECUTION OVERRIDE (applies when this plan runs inside a `/lazy-bug-batch` cycle subagent):** a cycle subagent has NO `Agent` tool. When that is the case, perform ALL implementation INLINE with `Read`/`Edit`/`Write` (the dispatch model above degrades to direct edits), still test-first, still per-WU. The only surviving prohibition is never invoking another `/lazy*` orchestrator. When run directly by a human (Agent tool available), follow the subagent dispatch model as written.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `.claude/skill-config/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| lazy-cycle-containment | related | `docs/features/lazy-cycle-containment/` | The C1–C8 machinery this bug hardens; `refuse_if_cycle_active` priority order + `CYCLE_REFUSED_OPS` are its contract. |
| hardening-blind-to-process-friction | related | `docs/bugs/_archive/hardening-blind-to-process-friction/PHASES.md` | Established the `agent_id` C2 trip + `LAZY_ORCHESTRATOR`-truthy never-refuse path WU-1/WU-3 build on. |

(No formal hard `**Depends on:**` block — this is a harness bug, not a queued feature with a dep graph.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — UNLESS the inline-execution override applies (cycle subagent with no Agent tool), in which case do it inline, still test-first.
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline — write failing tests, then make them pass.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in a phase finish, integration verification confirms all changes work together.
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it (notably `user/scripts/CLAUDE.md` for the C3 refusal contract).
9. Each completed phase is committed and pushed before the next phase begins.
10. This plan is self-contained — follow it exactly as written without relying on external context.
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
12. **COUPLING (HARD):** any handler edit to `lazy-state.py` MUST be mirrored into `bug-state.py`; shared logic lands in `lazy_core.py` ONCE. Keep BOTH `lazy-state.py --test` and `bug-state.py --test` green, re-pin the byte baselines via the `_normalize_smoke_output` helper (never by hand), and run `lazy_parity_audit.py --report`.

## Execution Schedule

| Step | Feature | Phase(s) | Title | Blocked by | Parallel? |
|------|---------|----------|-------|------------|-----------|
| 1 | cycle-subagent | P1 | Export the positive orchestrator signal | — | Solo |
| 2 | cycle-subagent | P2 | Guard `--cycle-end`/`--cycle-begin` (KEYSTONE) | P1 | Solo |

Phases are strictly sequential (P2's guard keys on P1's signal). No cross-feature parallelism.

---

### Phase: cycle-subagent P1 — Export the positive orchestrator signal (`LAZY_ORCHESTRATOR=1`)

**Goal:** Make the orchestrator assert its identity via an exported env var so containment no longer degrades to the deletable cycle marker.
**Entry criteria:** None (first phase).
**SPEC.md references:** Candidate Fix #2 ("Make subagent identity a positive, marker-independent signal"); Proven Finding 2.

#### WU-1 — Export `LAZY_ORCHESTRATOR=1` from the three orchestrators

- **Scope:** PHASES.md Phase 1 deliverables 1+2 (export in lazy-batch; mirror into lazy-bug-batch + lazy-batch-cloud).
- **TDD:** no (skill-prose edits; verified by lint/projection at QG, not unit tests).
- **Files to modify:**
  - `user/skills/lazy-batch/SKILL.md` — add an orchestrator-startup step (alongside the existing `--run-start` setup near the run-bootstrap section) that exports `export LAZY_ORCHESTRATOR=1` once for the session, documented as the C3 self-immunity signal that makes the orchestrator structurally immune to a stale cycle marker. [VERIFY: grep -n "run-start" user/skills/lazy-batch/SKILL.md]
  - `user/skills/lazy-bug-batch/SKILL.md` — mirror the same export at its orchestrator startup. [VERIFY path: user/skills/lazy-bug-batch/SKILL.md]
  - `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` — mirror the same export (parity). [VERIFY path: repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md]
- **Implementation goal:** All three orchestrators export `LAZY_ORCHESTRATOR=1` into the persistent session env BEFORE any `lazy-state.py`/`bug-state.py` lifecycle or routing call. The export must persist for the run (a session `export`, not a one-shot inline prefix on a single command — the orchestrator makes many lifecycle calls). Add a one-line rationale comment referencing this bug. If either coupled pair documents a "Differences" table, update it so the export is registered as shared (not a divergence).
- **Spec requirements:** SPEC Candidate Fix #2; the export is the reachable carrier because Claude Code does not propagate orchestrator env into the subagent Bash subprocess (SPEC gap 3d) — absence of the var marks a subagent.
- **Batch:** 1.

#### WU-2 — Env-var immunity/refusal unit tests + lazy_core cross-ref comment

- **Scope:** PHASES.md Phase 1 deliverables 3+4 (confirm priority-1 logic; add unit tests).
- **TDD:** yes.
- **Files to modify:** `user/scripts/lazy_core.py` (comment cross-ref only — the logic already exists at the `refuse_if_cycle_active` priority block).
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** With `LAZY_ORCHESTRATOR=1` set in env AND a cycle marker written, `refuse_if_cycle_active("--run-end")` returns silently (does NOT raise `SystemExit`) — structural immunity to a stale marker. With `LAZY_ORCHESTRATOR` unset/empty AND a cycle marker present, it raises `SystemExit(3)`. With `LAZY_CYCLE_SUBAGENT=1` set, it raises `SystemExit(3)` even with no marker. Use the existing test harness's env + temp-state-dir fixtures (set `LAZY_STATE_DIR` to a temp dir and write a marker via the public writer). [VERIFY: grep -n "def refuse_if_cycle_active" user/scripts/lazy_core.py] [VERIFY: grep -n "LAZY_ORCHESTRATOR" user/scripts/test_lazy_core.py]
- **Implementation goal:** Tests pass against the EXISTING priority logic (`lazy_core.py` priority-1 `_env_truthy("LAZY_ORCHESTRATOR")` → return). Add a comment in `lazy_core.py` near that block noting this bug confirmed the signal was read-but-never-set until Phase 1 wired the export. No behavior change to `lazy_core.py` itself.
- **Spec requirements:** Proven Finding 2; SPEC gap 3d (env-var hole).
- **Batch:** 1 (independent of WU-1; can run in the same batch — different files, no conflict).

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1 | WU-1, WU-2 | Yes (when Agent tool available) | None (skill .md vs. lazy_core/test_lazy_core) |

**Phase 1 Runtime Verification** *(checked by the test/lint gates, not /execute-plan-blocking):*
- [ ] `python -m pytest user/scripts/test_lazy_core.py -q` green including the new immunity/refusal assertions.
- [ ] `python user/scripts/project-skills.py` + `python user/scripts/lint-skills.py --check-projected --check-capabilities` clean (skill-prose edits).
- [ ] `python user/scripts/lazy_parity_audit.py --report` — the export registered as shared across the three orchestrators, no unexplained drift.

---

### Phase: cycle-subagent P2 — Guard `--cycle-end`/`--cycle-begin` against subagent callers (KEYSTONE)

**Goal:** Make a cycle subagent structurally unable to clear or arm the containment marker (closes the confirmed root cause).
**Entry criteria:** Phase 1 complete (`LAZY_ORCHESTRATOR=1` exported — the guard's allow-path keys on it).
**SPEC.md references:** Candidate Fix #1 (KEYSTONE); Theory 1 / Proven Finding 1; seam-table rows 2, 3c, 5.

#### WU-3 — `refuse_cycle_marker_mutation_if_subagent` guard helper + unit tests

- **Scope:** PHASES.md Phase 2 deliverable 1 (new guard helper in lazy_core).
- **TDD:** yes.
- **Files to modify:** `user/scripts/lazy_core.py` — add `refuse_cycle_marker_mutation_if_subagent(op_name)`.
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** The helper returns silently when `LAZY_ORCHESTRATOR` is truthy (orchestrator clears under a live marker — legitimate). It raises `SystemExit(3)` (corrective stderr) when `LAZY_ORCHESTRATOR` is NOT truthy AND a subagent context is indicated: either `LAZY_CYCLE_SUBAGENT` truthy, OR a cycle marker is present (the reachable fallback signal). It returns silently when no marker is present AND no subagent env signal (the genuinely-uncontained main-thread case with no marker). Assert ZERO side effects on refusal (no marker mutation). [VERIFY: grep -n "_env_truthy" user/scripts/lazy_core.py] [VERIFY: grep -n "def refuse_if_cycle_active" user/scripts/lazy_core.py]
- **Implementation goal:** A new guard that differs from `refuse_if_cycle_active` in ONE way: `--cycle-end`/`--cycle-begin` are operations the orchestrator runs WHILE its own marker is present, so the plain marker-fallback (which would refuse anyone-with-a-marker) cannot be reused. The new guard's allow-path keys on the POSITIVE `LAZY_ORCHESTRATOR` signal (orchestrator allowed even with marker present); the deny-path fires for explicit subagent env OR marker-present-without-orchestrator-env. Document in a module comment why this helper exists separately from `CYCLE_REFUSED_OPS` and that the C2/C3 deny SCOPE still matches (subagent cannot clear/arm).
- **Spec requirements:** SPEC Candidate Fix #1; seam row 3c (handler clears before refusal check); Proven Finding 2 (marker can't carry its own protection → key on the positive signal).
- **Batch:** 1.

#### WU-4 — Wire the marker-mutation guard into `--cycle-end`/`--cycle-begin` in both state machines + `--test` fixtures

- **Scope:** PHASES.md Phase 2 deliverables 2+3 (wire into lazy-state + bug-state handlers; cover both `--cycle-end` and `--cycle-begin`).
- **TDD:** yes (new `--test` fixtures, written before wiring).
- **Files to modify:**
  - `user/scripts/lazy-state.py` — call the guard at the ENTRY of the `--cycle-end` handler BEFORE `cycle_end_friction_check`/`clear_cycle_marker`, and at the ENTRY of the `--cycle-begin` handler. [VERIFY: grep -n "args.cycle_end" user/scripts/lazy-state.py] [VERIFY: grep -n "args.cycle_begin" user/scripts/lazy-state.py]
  - `user/scripts/bug-state.py` — mirror both call-sites. [VERIFY: grep -n "args.cycle_end" user/scripts/bug-state.py] [VERIFY: grep -n "args.cycle_begin" user/scripts/bug-state.py]
- **Test files:** the in-file `--test` harness in BOTH `lazy-state.py` and `bug-state.py`; baselines `tests/baselines/lazy-state-test-baseline.txt` and `tests/baselines/bug-state-test-baseline.txt`.
- **Test expectations:** New fixtures: (a) subagent-context `--cycle-end` (env `LAZY_ORCHESTRATOR` unset, marker on disk) is refused (exit 3) and the marker file STILL EXISTS afterward (zero side effects — the friction check + clear never run); (b) orchestrator `--cycle-end` (`LAZY_ORCHESTRATOR=1`) clears the marker and exits 0 (existing bracket teardown unaffected — guard against regressing the orchestrator's own flow); (c) same pair for `--cycle-begin`. Preserve the existing `--cycle-begin` self-healing-overwrite behavior for the orchestrator. Re-pin BOTH baselines via the `_normalize_smoke_output` helper.
- **Implementation goal:** A subagent cannot delete or re-arm the marker; the orchestrator's bracket (`--cycle-begin` before dispatch, `--cycle-end` after) is unaffected because it exports `LAZY_ORCHESTRATOR=1` (Phase 1). The guard runs before ANY side effect.
- **Spec requirements:** SPEC Candidate Fix #1; seam rows 2 + 5 (KEYSTONE — deleting the marker un-arms every downstream guard at once).
- **Batch:** 2 (depends on WU-3's helper).

#### WU-5 — Add `--cycle-end`/`--cycle-begin` to C2 hook `LOOP_FORMATION_FLAGS` + hook test

- **Scope:** PHASES.md Phase 2 deliverables 4+5 (C2 hook flag-set; document the lockstep note).
- **TDD:** yes.
- **Files to modify:** `user/hooks/lazy-cycle-containment.sh` — add `"--cycle-end"` and `"--cycle-begin"` to the `LOOP_FORMATION_FLAGS` tuple; update the module comment near `CYCLE_REFUSED_OPS` in `lazy_core.py` documenting that `--cycle-end`/`--cycle-begin` are guarded by the dedicated marker-mutation helper (not added to `CYCLE_REFUSED_OPS`), so the C2/C3 deny scope stays in lockstep. [VERIFY: grep -n "LOOP_FORMATION_FLAGS" user/hooks/lazy-cycle-containment.sh] [VERIFY: grep -n "CYCLE_REFUSED_OPS" user/scripts/lazy_core.py]
- **Test files:** the existing hook test module if one exists (search `user/scripts/` and `user/hooks/` for a hook test; e.g. `test_*containment*` or a hooks test harness). If none exists, add a small pytest that pipes a synthetic PreToolUse payload through the hook script and asserts the JSON decision. [VERIFY: find user -name "*containment*" -o -name "test_*hook*"]
- **Test expectations:** A payload `{"agent_id":"x","tool_name":"Bash","tool_input":{"command":"python3 lazy-state.py --cycle-end"}}` piped through `lazy-cycle-containment.sh` emits `permissionDecision: deny`. The same payload WITHOUT `agent_id` (main-thread orchestrator) emits NO decision (allow). Same pair for `--cycle-begin`. Reuse the hook's hermetic test style (the `LAZY_CYCLE_STAGED_PATHS` override pattern).
- **Implementation goal:** The arming-free `agent_id` subagent trip in the hook now denies `lazy-state.py --cycle-end`/`--cycle-begin` regardless of marker state — belt-and-suspenders with WU-4's C3 guard. Fail-OPEN preserved.
- **Spec requirements:** SPEC Candidate Fix #1 (C2 side); seam row 2 (`--cycle-end` omitted from `LOOP_FORMATION_FLAGS`). Resolve SPEC Open Question: confirm the Bash PreToolUse payload carries `agent_id` for the subagent's `lazy-state.py` calls; if it does NOT, record in PHASES Implementation Notes that the C3 env-var guard (WU-3/WU-4) is the load-bearing carrier and the hook flag-set is the secondary layer.
- **Batch:** 2 (independent of WU-4 — different files; may run parallel with WU-4 when Agent tool available).

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1 | WU-3 | Solo | N/A |
> | 2 | WU-4, WU-5 | Yes | None (state .py vs. hook .sh; both touch lazy_core comment — sequence the comment edit into WU-5 only) |

**Phase 2 Runtime Verification** *(checked by the test/lint gates):*
- [ ] `python user/scripts/lazy-state.py --test` and `python user/scripts/bug-state.py --test` green (new refusal fixtures); baselines re-pinned via `_normalize_smoke_output`.
- [ ] `python -m pytest user/scripts/ -q` green (lazy_core guard tests + hook test).
- [ ] Manual hook check: subagent `--cycle-end` payload → deny; main-thread → allow.
- [ ] `python user/scripts/lazy_parity_audit.py --report` — both twins mirror the guard wiring, no unexplained drift.

**Completion (gate-owned):** Do NOT flip the SPEC `**Status:**`, write `FIXED.md`, or archive. After Phase 2's work lands and Part 2 (Phases 3–4) completes, the LAST phase sets the top-level PHASES `**Status:**` to `In-progress`; the state machine routes to validation; `__mark_fixed__` owns the terminal flip.

## Execution Protocol

This protocol governs autonomous execution. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in this part's Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase:** P1 first (no entry criteria), then P2 (entry criterion: P1 complete — all P1 deliverables checked in PHASES.md). Phases here are strictly sequential.
2. **Announce:** Print "Implementing cycle-subagent Phase N: [title]".
3. **Review prior context:** Re-read prior phases' Implementation Notes in PHASES.md — they take priority over the plan where they diverge.
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "cycle-subagent Phase N: [title] — committed".
7. **Loop:** Return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (ToolSearch for task tools; create tasks for all WUs; update protocol). If the inline-execution override is active (no Agent tool), still create/track tasks.

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and this plan file. Do NOT rely on cached content.

#### Step B.1: Launch Subagents (or implement INLINE if no Agent tool)

**PRE-FLIGHT CHECK:** If the `Agent` tool is available, dispatch ALL code changes to Sonnet subagents (`model: "sonnet"`); do NOT `Edit`/`Write` source/test files yourself. If the inline-execution override applies (cycle subagent, no Agent tool), implement inline with `Edit`/`Write`, still test-first.

Read before proceeding: `~/.claude/skills/_components/tdd-protocol.md`, `~/.claude/skills/_components/subagent-launch.md`, `~/.claude/skills/_components/tdd-test-agent.md`, `~/.claude/skills/_components/implementation-agent.md`.

**Test-first discipline (HARD):** for every TDD WU, write the failing test(s) FIRST, confirm they fail for the right reason, THEN implement until green.

#### Step B.2: Review Batch Output (MANDATORY GATE)

Read `~/.claude/skills/_components/subagent-review.md` and `~/.claude/skills/_components/mount-site-verification.md`. Produce a structured review with a PASS / PASS-WITH-FIXES / NEEDS-REWORK verdict. (Under inline execution, self-review against the same protocol.)

#### Step B.3: Update PHASES.md (MANDATORY)

Read `~/.claude/skills/_components/phases-update.md`. Check off completed deliverables; add an Implementation Notes block (date, work completed, integration notes, pitfalls, files modified). Record the SPEC Open Question resolution (agent_id-on-Bash) here when WU-5 lands.

#### Step B.4: Run Quality Gates (MANDATORY)

Read `.claude/skill-config/quality-gates.md`. For these phases run, at minimum:
- `python -m pytest user/scripts/ -q`
- `python user/scripts/lazy-state.py --test` and `python user/scripts/bug-state.py --test`
- `python user/scripts/lazy_parity_audit.py --report`
- for skill-prose WUs: `python user/scripts/project-skills.py` + `python user/scripts/lint-skills.py --check-projected --check-capabilities`

100% pass required before proceeding. Do NOT triage failures as "preexisting".

#### Step B.5: Commit Batch (ATOMIC GATE+COMMIT)

Read `.claude/skill-config/commit-policy.md`. Conventional Commits, no AI attribution, push to `main`. Run the gate and commit as ONE chained command so an interrupted turn still leaves committed+pushed state, e.g.:
`python -m pytest user/scripts/ -q && python user/scripts/lazy-state.py --test && python user/scripts/bug-state.py --test && git add -A && git commit -m "fix(lazy-containment): ..." && git push`

#### Step B.6: Proceed to Next Batch

Checklist (all must be true): review verdict produced; PHASES.md updated; all gates pass; committed+pushed. If any unchecked, complete it before the next batch.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY)

Read `~/.claude/skills/_components/integration-verification.md`. Confirm the C2 hook deny + C3 script refusal agree on scope (both deny a subagent's marker mutation; both allow the orchestrator). For Phase 2, the cross-cutting integration check is: orchestrator flow (`--cycle-begin` → dispatch → `--cycle-end`) still works end-to-end with `LAZY_ORCHESTRATOR=1`, and a simulated subagent (no env var, marker present) is refused at BOTH layers.

#### Update CLAUDE.md Files (MANDATORY)

Read `~/.claude/skills/_components/claude-md-review.md`. Update `user/scripts/CLAUDE.md`'s C3 refuse-by-construction documentation to describe the new `--cycle-end`/`--cycle-begin` marker-mutation guard and the `LAZY_ORCHESTRATOR` export requirement.

#### Commit and Push Post-Phase Changes

Per `.claude/skill-config/commit-policy.md`. Push to `main`.

## Blocking Issue Protocol

If a blocking issue is encountered:

1. Stop all in-progress work. Do not launch new subagents.
2. Commit and push any completed phases.
3. Print a blocking-issue report (completed phases, blocked phase, reason, recovery suggestion, remaining phases).
4. Write `BLOCKED.md` per the sentinel schema (`~/.claude/skills/_components/sentinel-frontmatter.md`) in the bug directory if a human decision is required. Do NOT work around the blocker.

Blocking issues include: a quality-gate/test failure that can't be fixed after 2 attempts; a discovery that `agent_id` is NOT carried on Bash payloads AND the env-var carrier is insufficient (architectural — surface it); a git push conflict unresolvable by rebase; any architectural decision beyond the specs.

## Completion

When all phases in this part are complete:

1. Run the full quality-gate suite once more: `python -m pytest user/scripts/ -q`, both `--test` suites, `lazy_parity_audit.py --report`, `project-skills.py`, `lint-skills.py --check-projected --check-capabilities`.
2. Print a completion report (phases completed, commits, final gate status, key integration notes).
3. STOP. Do NOT flip SPEC `**Status:**` or write `FIXED.md`. Proceed to Part 2 (Phases 3–4) on the next cycle.
