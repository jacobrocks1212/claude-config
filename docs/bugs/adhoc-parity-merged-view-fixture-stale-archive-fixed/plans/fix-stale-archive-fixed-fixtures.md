---
kind: fix-plan
feature_id: adhoc-parity-merged-view-fixture-stale-archive-fixed
status: Ready
created: 2026-06-19
complexity: mechanical
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-19.
> To execute: `/execute-plan docs/bugs/adhoc-parity-merged-view-fixture-stale-archive-fixed/plans/fix-stale-archive-fixed-fixtures.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — adhoc-parity-merged-view-fixture-stale-archive-fixed  (v2)

**PHASES.md files:**
- `docs/bugs/adhoc-parity-merged-view-fixture-stale-archive-fixed/PHASES.md` (1 phase)

**SPEC.md files:**
- `docs/bugs/adhoc-parity-merged-view-fixture-stale-archive-fixed/SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Work Units

- [ ] WU-1 — Add `--archive-fixed` clause to all 4 stale workstation merged-view fixtures

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.ts`, `.js`, `.cs`, `.vue`, `.py`, `.rs`, `.tsx`, `.jsx`, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **DISPATCH OVERRIDE (this harness repo, autonomous bug pipeline):** when this plan is executed by a cycle subagent that does NOT have the `Agent` tool, perform ALL implementation INLINE with `Read`/`Edit`/`Write` — the no-Agent constraint above yields to the inline override. The TDD/test-first discipline still holds: write/adjust the failing assertion first, confirm RED, then make it GREEN.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies. The `--archive-fixed` predicate's origin feature `lazy-batch-unified-driver-parity-and-accounting` is cited for provenance only, not consumed as a contract.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines). (Subordinate to the DISPATCH OVERRIDE above: a no-Agent cycle subagent does this work INLINE.)
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents
3. Every TDD work unit goes through the test-first pipeline — confirm the targeted assertion is RED before making it GREEN
4. PHASES.md is updated AFTER EACH batch completes (not deferred)
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing
6. Mistakes are fixed immediately before launching the next batch
7. After all batches in a phase finish, integration verification confirms all changes work together
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it
9. Each completed phase is committed and pushed before the next phase begins
10. This plan is self-contained — follow it exactly as written without relying on external context
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | adhoc-parity-merged-view-fixture-stale-archive-fixed | P1 | Add `--archive-fixed` clause to stale workstation merged-view fixtures | — | Solo |

---

### Phase: adhoc-parity-merged-view-fixture-stale-archive-fixed P1 — Add `--archive-fixed` clause to stale workstation merged-view fixtures

**Goal:** Extend each WORKSTATION-side `full`/consistent hermetic fixture string in `TestMergedViewDispatchParity` so its merged-view dispatch prose names `--archive-fixed`, satisfying the 6th `_MERGED_VIEW_PREDICATES` predicate; the failing test turns green and the three latent ones stay correct.

**Entry criteria:** None (first and only phase).

**SPEC.md references:** `## Proven Findings` (root cause + fix scope CONFIRMED), `## Scope decision (D7 completeness)` (fix all stale "full" fixtures), and PHASES.md `## Scope note — completeness` (D7 extension to the 4th workstation fixture in `test_fires_when_cloud_missing_merged_branch`).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | None            |

#### WU-1 — Add `--archive-fixed` clause to all 4 stale workstation merged-view fixtures

- **Scope:** Covers all four PHASES.md Phase-1 deliverables (the failing fixture + three latent workstation fixtures). One file, one cohesive concern (add the same predicate-satisfying clause to each workstation `full`/`skills` fixture string), so it is a single work unit.
- **TDD:** yes (the edited fixtures ARE the test data; the test `test_passes_when_both_drivers_consistent` is RED today and the edit turns it GREEN — confirm RED-before-GREEN by running the targeted reproduction command first).
- **Files to create/modify:** `user/scripts/test_lazy_parity.py` [VERIFY: `grep -n "class TestMergedViewDispatchParity" user/scripts/test_lazy_parity.py`]
- **Test files:** same file — `user/scripts/test_lazy_parity.py` (this is a test-fixture edit; no separate test file).
- **Test expectations:** After the edit, `audit_merged_view_dispatch_parity` [VERIFY: `grep -n "def audit_merged_view_dispatch_parity" user/scripts/lazy_parity_audit.py`] returns `[]` for the both-consistent fixture (`test_passes_when_both_drivers_consistent` passes), and each `*_fires_*` test STILL fires on its own predicate (single-type / `__mark_fixed__` / `next-merged`) because the cloud-side fixtures are untouched.
- **Implementation goal:** In each of the four workstation fixture strings, append an `--archive-fixed` bug-archive clause to the merged-view dispatch prose. The four fixture strings (the WORKSTATION/`skills`/`full` side only — NEVER the deliberately-broken cloud side):
  1. `test_passes_when_both_drivers_consistent` — the `full` string at ~L812-818 (written to BOTH `skills` and `cloud`). FAILS today.
  2. `test_fires_when_no_regression_guard_absent` — the `full` string at ~L784-789 (written to `skills`; cloud at ~L792-796 deliberately drops single-type — leave it).
  3. `test_fires_when_terminal_dispatch_inconsistent` — the `full` string at ~L756-761 (written to `skills`; cloud at ~L764-768 deliberately drops `__mark_fixed__` — leave it).
  4. `test_fires_when_cloud_missing_merged_branch` — the `skills` string at ~L727-733 (D7 completeness; cloud at ~L735-737 is `## Cloud driver` with no merged branch — leave it).
  Suggested clause (uniform across all four, phrased to satisfy the `r"--archive-fixed"` regex literally): add to the end of each workstation string, before the closing `\n`, something like `" A fixed bug chains the `--archive-fixed` archive + de-queue follow-up."`. Match the existing string's punctuation/concatenation style (these are implicitly-concatenated Python string literals — keep the trailing `\n` on the LAST segment and preserve the `encoding="utf-8"` write call). Do NOT touch `_MERGED_VIEW_PREDICATES`, `audit_merged_view_dispatch_parity`, or any production SKILL.md.
- **Spec requirements:** SPEC `## Proven Findings` — "Add an `--archive-fixed` bug-archive clause to the `full` fixture SKILL.md text in BOTH named tests … No production SKILL.md, no `lazy_parity_audit.py` change … required." Extended by PHASES `## Scope note` to all four workstation fixtures (D7 completeness, no product divergence).
- **Batch:** 1

**Runtime Verification** *(none — this is a pure-Python unit-test fixture edit with no runtime-observable surface):*

N/A — no runtime-observable behavior. Verification is the `pytest` suite turning green (see Step B.4). The bug's `**MCP runtime:** not-required` line in PHASES.md applies — no Tauri/MCP runtime, no device.

**MCP Integration Test Assertions:**
N/A — no runtime-observable behavior in this phase (pure unit-test fixture text edit).

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** Phase 1 has no prerequisites — it is ready immediately.
2. **Announce:** Print "Implementing adhoc-parity-merged-view-fixture-stale-archive-fixed Phase 1: Add `--archive-fixed` clause to stale workstation merged-view fixtures"
3. **Review prior context:** Re-read PHASES.md (Phase 1 + any Implementation Notes) and the SPEC's Proven Findings.
4. **Execute the single batch** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "adhoc-parity-merged-view-fixture-stale-archive-fixed Phase 1 — committed as [hash]"
7. **Loop:** Only one phase — done after step 6.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (load task tools via ToolSearch, create tasks for WU-1, update through test/impl phases).

### Per-Batch Steps

For Batch 1:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow it. Re-read from disk: PHASES.md (Phase 1 + Implementation Notes), SPEC.md (`## Proven Findings`, `## Scope decision`), `user/scripts/test_lazy_parity.py` (the `TestMergedViewDispatchParity` class), and `user/scripts/lazy_parity_audit.py` (`_MERGED_VIEW_PREDICATES`). Do NOT rely on cached content.

#### Step B.1: Launch Subagents (or implement INLINE per the DISPATCH OVERRIDE)

**RED-FIRST (TDD):** before editing, run the reproduction command and confirm RED:
```
python -m pytest user/scripts/test_lazy_parity.py -q -k "test_passes_when_both_drivers_consistent"
```
Expect `1 failed` with `missing bug archive --archive-fixed chain (pattern '--archive-fixed')`. Record this RED evidence.

Then make the edit (WU-1) — the four workstation fixture strings as detailed in the WU above. Read the TDD/launch/agent component files listed in the card before dispatching (or, for a no-Agent cycle subagent, implement inline).

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` (and `mount-site-verification.md` — N/A here, no new files) and follow the protocol. Produce a structured review with a PASS/PASS-WITH-FIXES/NEEDS-REWORK verdict. Specifically verify: (a) ONLY the four workstation fixture strings changed; (b) the deliberately-broken cloud fixtures are byte-unchanged; (c) `_MERGED_VIEW_PREDICATES` and `audit_merged_view_dispatch_parity` are untouched.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md`. Check off the four Phase-1 deliverables, add an Implementation Notes block (date, the four fixtures edited, the clause used, RED→GREEN evidence). When the last phase's work lands, set the top-level PHASES `**Status:**` to `In-progress` (NOT Fixed — validation pending).

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/quality-gates.md`. For this repo the gate is the Python test suite. Run, in order:
```
python -m pytest user/scripts/test_lazy_parity.py -q -k "test_passes_when_both_drivers_consistent"   # must now be 1 passed
python -m pytest user/scripts/test_lazy_parity.py -q                                                 # whole module green
python -m pytest user/scripts/ -q                                                                    # whole suite green (0 failed)
```
100% pass required (`0 failed`) before proceeding. Also run the skills lint if convenient (`python user/scripts/lint-skills.py`) — but the load-bearing gate is the pytest suite.

#### Step B.5: Commit Batch

Read the commit policy: first try `.claude/skill-config/commit-policy.md`; if absent, `~/.claude/skills/_components/commit-and-push.md`. Commit to `main` and push (this is a personal config repo — push allowed). Use the ATOMIC GATE+COMMIT pattern: chain the suite-green gate with the commit so an interrupted turn leaves pushed state.

#### Step B.6: Proceed to Next Batch

Only one batch — proceed to Post-Phase Steps.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md`. The integration check here is the full `python -m pytest user/scripts/ -q` run reporting `0 failed` — the symptom (red suite) is resolved and no other test regressed.

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md`. Likely a no-op — this is a test-fixture correction, not a contract change. Confirm `user/scripts/CLAUDE.md` needs no update (the predicate set and audit are unchanged).

#### Commit and Push Post-Phase Changes

Read the commit policy and commit/push any residual doc updates (PHASES.md Implementation Notes / status flip) to `main`.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point during execution:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed work** that hasn't been committed yet.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** adhoc-parity-merged-view-fixture-stale-archive-fixed Phase 1
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

4. **Do not attempt to work around the blocker.**

Blocking issues include: a quality-gate failure that can't be fixed after 2 retry attempts; a git push conflict that can't be resolved by rebase; any error requiring architectural decisions beyond the spec. (Unlikely here — the fix is a deterministic test-fixture edit.)

---

## Completion

When Phase 1 is complete:

1. **Run the full quality-gate suite one final time:** `python -m pytest user/scripts/ -q` → `0 failed`.
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Features implemented:** adhoc-parity-merged-view-fixture-stale-archive-fixed (bug fix)
   **Total phases completed:** 1
   **Total commits:** M
   **Final quality-gate status:** all green (`0 failed`)

   **Commit log:**
   | Commit | Feature | Phase | Title |
   |--------|---------|-------|-------|
   | … | adhoc-parity-merged-view-fixture-stale-archive-fixed | P1 | Add `--archive-fixed` clause to stale workstation merged-view fixtures |

   **Implementation Notes summary:** four workstation `full`/`skills` merged-view fixtures in `TestMergedViewDispatchParity` now name `--archive-fixed`; the failing `test_passes_when_both_drivers_consistent` is green and the three `*_fires_*` tests still fire on their own predicates. No production SKILL.md / `lazy_parity_audit.py` change. Status left `In-progress` — validation tail (`/mcp-test` → coverage audit → `__mark_fixed__`) is the orchestrator's, not this plan's.
