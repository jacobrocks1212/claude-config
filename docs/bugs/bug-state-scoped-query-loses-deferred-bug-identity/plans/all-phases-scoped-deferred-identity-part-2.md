---
kind: implementation-plan
feature_id: bug-state-scoped-query-loses-deferred-bug-identity
status: Ready
created: 2026-06-22
complexity: complex
phases: [2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-22.
> To execute: `/execute-plan <this-file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 3. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\plans\all-phases-scoped-deferred-identity-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\plans\all-phases-scoped-deferred-identity-part-2.md` (this file)
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\plans\all-phases-scoped-deferred-identity-part-3.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — bug-state-scoped-query-loses-deferred-bug-identity (Part 2: lazy-state.py feature-side parity)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\PHASES.md` (bug-state-scoped-query-loses-deferred-bug-identity, 4 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\SPEC.md`

**Total phases:** 4 (this part covers Phase 2)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **EXECUTION-CONTEXT OVERRIDE (autonomous bug pipeline).** When executed by a `/lazy-bug` / `/lazy-bug-batch` cycle subagent with no `Agent` tool, perform ALL implementation and test-writing INLINE with `Read`/`Edit`/`Write` (test-first: failing fixture before implementation). The inline override takes precedence when no `Agent` tool is available.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory. After compaction, re-read this plan first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies. Part 1 of this same series establishes the scoped-return pattern + the scoped terminal_reason naming convention this part mirrors onto the feature side — read Part 1's PHASES P1 Implementation Notes for the exact bug-side literals before authoring the feature-side analogs.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation/test work via Sonnet Agent subagents** — UNLESS no Agent tool (autonomous cycle), then INLINE per the override, test-first.
2. Edits in the current worktree — never create worktrees.
3. Test-first per WU.
4. PHASES.md updated after each batch.
5. Review every change before continuing.
6. Fix mistakes immediately.
7. Integration verification after the phase.
8. Update CLAUDE.md if warranted.
9. Commit + push the completed phase before the next part.
10. Self-contained plan.
11. Read components from disk before each step.
12. **COUPLING RULE (HARD):** this is the feature-side parity mirror of Part 1. Keep `lazy-state.py --test` green and re-byte-pin `tests/baselines/lazy-state-test-baseline.txt` ONLY via `_normalize_smoke_output`. Keep the `baseline-regression-default` smoke fixtures green (they guard unscoped byte-identity). After landing, **RUN `python user/scripts/lazy_parity_audit.py`** — the scoped-skip identity behavior is a coupled-pair surface; if the audit flags a missing parity entry, add a `lazy-parity-manifest.json` entry OR record a justified divergence (the feature/bug terminal literal names may legitimately differ — feature has no operator-`DEFERRED.md` axis). The audit MUST end green (or with a documented, manifest-recorded divergence).

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | bug-state-scoped-query-loses-deferred-bug-identity | P2 | Feature-side parity twin in lazy-state.py | Part 1 (P1) | Solo |

## Work Units

- [ ] WU-1 — Scoped per-feature deferred terminal_reason constant(s) + cloud/device scoped early-returns in lazy-state.py
- [ ] WU-2 — Host-capability + park scoped-match early-returns (feature-side)
- [ ] WU-3 — `lazy-state.py --test` fixtures (scoped feature identity) + `lazy_parity_audit.py` green

---

### Phase: bug-state-scoped-query-loses-deferred-bug-identity P2 — Feature-side parity twin in lazy-state.py

**Goal:** Mirror Part 1's identity-preserving scoped-return onto the feature pipeline's `--feature-id` query for its cloud / device / host-capability / park skip branches, so a scoped feature query in a global-deferral state returns the feature's own identity rather than the null-identity global terminal. UNSCOPED feature behavior stays byte-identical.

**Entry criteria:** Part 1 (P1) complete — the scoped-return pattern + scoped terminal naming convention is established on the bug side; this part mirrors it (read Part 1's PHASES Implementation Notes for the exact literals).

**SPEC.md references:** `## Proven Findings 4` (latent parity twin); `## Affected Area` (Coupled-pair parity row); `## Fix Scope → Parity (HARD)`; `## Open Questions` Q3 (confirm the feature-side twin + fix in the same change per the coupling rule).

#### WU-1 — Scoped cloud/device early-returns + scoped per-feature terminal constants

- **Scope:** PHASES P2 deliverables: cloud-saturated + device-saturated scoped early-returns; add scoped per-feature terminal_reason constant(s).
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/lazy-state.py`.
- **Implementation goal:**
  - In `compute_state`'s queue loop, the cloud-saturated branch (`if cloud:` → `cloud_saturated_skipped.append(name); continue`, `~1701–1713`): add `if scope_feature_id is not None and feature_id == scope_feature_id:` BEFORE the unscoped append+continue, returning a scoped `_state(feature_id=feature_id, feature_name=name, spec_path=str(spec_path), current_step=<per-feature cloud-deferred step>, terminal_reason=<scoped cloud-deferred>)`. [VERIFY: grep -n "cloud_saturated_skipped.append" user/scripts/lazy-state.py] [VERIFY scoped-return model: grep -n "completion-unverified\|completion claimed without receipt" user/scripts/lazy-state.py]
  - Device-saturated branch (`if not real_device:` → `device_saturated_skipped.append(...)`, `~1714+`): same scoped early-return with a scoped device-deferred terminal. [VERIFY: grep -n "device_saturated_skipped.append" user/scripts/lazy-state.py]
  - Add the scoped per-feature terminal_reason literal(s) (feature-side analogs — keep names parity-aligned with Part 1's bug-side literals where the axis matches; e.g. `cloud-queue-exhausted-scoped`, `device-queue-exhausted-scoped`). The UNSCOPED branches still emit the global `cloud-queue-exhausted` / `device-queue-exhausted`.
  - Record each new literal in a comment block (Part 3's `curated_stage.py` maps them).
- **Spec requirements:** SPEC `## Fix Scope → Parity (HARD)`; `## Proven Findings 4`.
- **Batch:** 1.

#### WU-2 — Host-capability + park scoped-match early-returns (feature-side)

- **Scope:** PHASES P2 deliverable: host-capability-miss DEFER branch (`DEFERRED_REQUIRES_HOST.md` → `host-capability-saturated`) + feature-side park branches get the scoped early-return.
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/lazy-state.py`.
- **Implementation goal:**
  - The host-capability-miss DEFER skip branch (the one writing `DEFERRED_REQUIRES_HOST.md` and recording the skip toward the `host-capability-saturated` terminal): on a scoped `--feature-id` match, return a scoped `_state` carrying the feature id + a scoped host-capability-deferred terminal, instead of skip-ahead into the global `host-capability-saturated`. [VERIFY: grep -n "DEFERRED_REQUIRES_HOST\|host-capability-saturated\|host_deferred_features" user/scripts/lazy-state.py]
  - Feature-side park branches (`--park-blocked` / `--park-needs-input`, the feature-pipeline analogs): scoped-match returns a scoped state naming the feature + park reason, mapping to the feature's actual side-state (`Blocked` / `Needs-input`, NOT `Deferred`). [VERIFY: grep -n "build_parked_entry\|park_blocked\|park_needs_input" user/scripts/lazy-state.py]
  - **Explicitly do NOT invent a feature-side operator-`DEFERRED.md` branch** — that axis is bug-pipeline-only (⚖ recorded in PHASES). The feature deferral axes are cloud / device / host-capability only.
- **Spec requirements:** SPEC `## Affected Area` (Coupled-pair parity); `## Fix Scope → Parity`.
- **Batch:** 1 (same file as WU-1 — serialize).

#### WU-3 — `lazy-state.py --test` fixtures + parity audit green

- **Scope:** PHASES P2 deliverables: scoped feature-identity `--test` fixtures; preserve UNSCOPED byte-identity; run `lazy_parity_audit.py`.
- **TDD:** yes (author fixtures RED on pre-fix code).
- **Files to create/modify:** `user/scripts/lazy-state.py` (`--test` harness), `tests/baselines/lazy-state-test-baseline.txt`, possibly `user/scripts/lazy-parity-manifest.json` (only if the audit requires a new parity/divergence entry).
- **Test expectations:**
  - Fixture A (scoped cloud): a cloud-saturated feature queried with `--feature-id` returns its own id + the scoped cloud-deferred terminal (NOT `feature_id: null` / global `cloud-queue-exhausted`).
  - Fixture B (scoped device): analogous for device-saturated.
  - Fixture C (scoped host-capability): a `DEFERRED_REQUIRES_HOST.md` feature queried with `--feature-id` returns its id + scoped host-capability-deferred terminal.
  - Fixture D (unscoped baseline regression): the existing `baseline-regression-default` fixtures still pass — unscoped global terminals byte-identical.
- **Implementation goal:** add fixtures to the in-file `--test` harness; run `python user/scripts/lazy-state.py --test` green; re-pin `tests/baselines/lazy-state-test-baseline.txt` via `_normalize_smoke_output`; **run `python user/scripts/lazy_parity_audit.py` and confirm it exits green** (resolve any flagged scoped-terminal parity gap by a manifest entry or a documented divergence). [VERIFY: grep -n "baseline-regression-default\|def _run_tests\|--test" user/scripts/lazy-state.py] [VERIFY: grep -n "audit_state_script_parity\|scoped" user/scripts/lazy_parity_audit.py]
- **Spec requirements:** SPEC `## Fix Scope → Parity (HARD)` ("run `lazy_parity_audit.py`; keep both `--test` suites green").
- **Batch:** 2.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2 | No (same file `lazy-state.py`) | Yes — serialize |
| 2     | WU-3       | Solo | N/A |

## Execution Protocol

### Phase Selection Loop
This part has ONE phase (P2). Execute its batches in order, run Post-Phase Steps, commit, and STOP (Part 3 is a separate cycle).

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)
Read `~/.claude/skills/_components/task-tracking.md` and follow it exactly.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read: PHASES.md (P2 + P1 Implementation Notes — the bug-side literals), SPEC.md, this plan, the CURRENT `lazy-state.py` skip branches + completion-unverified scoped-return model.

#### Step B.1: Implement (Agent subagents OR inline per override)
Read `tdd-protocol.md`, `subagent-launch.md`, `tdd-test-agent.md`, `implementation-agent.md`. Failing fixtures first (WU-3 RED), then WU-1/WU-2 to GREEN.

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `subagent-review.md` + `mount-site-verification.md`. Verdict required. Confirm unscoped byte-identity (baseline-regression fixtures green) and parity alignment with Part 1.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `phases-update.md`. Tick P2 deliverables; Implementation Notes record the feature-side scoped terminal literals + the parity-audit outcome (green / divergence-recorded).

#### Step B.4: Run Quality Gates (MANDATORY)
Read `quality-gates.md`. Run: `python user/scripts/lazy-state.py --test`, `python user/scripts/bug-state.py --test`, `python test_lazy_core.py`, **`python user/scripts/lazy_parity_audit.py`**. 100% pass + parity green required. Re-pin the lazy-state baseline via `_normalize_smoke_output` if smoke output changed.

#### Step B.4.5: MCP Integration Test
SKIP — `MCP runtime: not-required`. Note the skip.

#### Step B.5: Commit Batch
Read `.claude/skill-config/commit-policy.md`. Conventional Commit, no AI attribution, push to `main`. Example: `fix(lazy-state): mirror scoped --feature-id deferred identity (P2 parity)`. Confirm HEAD == `main` before committing.

#### Step B.6: Proceed to Next Batch
Checklist: review verdict; PHASES.md updated; all `--test` + parity green; baseline re-pinned if needed; committed+pushed.

### Post-Phase Steps

#### Integration Verification (MANDATORY)
Read `integration-verification.md`. Confirm feature scoped + unscoped paths coexist; full `--test` trio + parity audit green.

#### Update CLAUDE.md Files (MANDATORY)
Read `claude-md-review.md`. Update `user/scripts/CLAUDE.md` if the feature-side scoped terminals materially extend the documented terminal set / coupling notes.

#### Commit and Push Post-Phase Changes
Per commit policy, on `main`.

## Blocking Issue Protocol

If blocked: 1. Stop. 2. Commit+push completed work. 3. Print blocking-issue report. 4. Do NOT work around. Blocking issues: unresolvable `--test`/parity failure after 2 retries; unresolvable QG failure; git push conflict unresolvable by rebase; an architectural decision beyond SPEC scope → write `BLOCKED.md` per the sentinel contract. NOTE: if `lazy_parity_audit.py` flags a scoped-terminal asymmetry that genuinely cannot be reconciled (feature/bug terminal sets legitimately diverge AND no manifest divergence entry resolves it cleanly), that is a NEEDS_INPUT-class design fork — write `NEEDS_INPUT.md`, not a workaround.

## Completion

When P2's batches are complete and committed:
1. Run the full `--test` trio + `lazy_parity_audit.py` one final time.
2. Print a completion report (WUs, commits, gate + parity status).
3. STOP — Part 3 (curated-stage mapping + e2e guard) is a separate `/execute-plan` cycle.

```
/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\plans\all-phases-scoped-deferred-identity-part-2.md
```
