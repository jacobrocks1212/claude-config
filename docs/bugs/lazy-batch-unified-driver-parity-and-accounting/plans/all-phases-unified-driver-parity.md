---
kind: implementation-plan
feature_id: lazy-batch-unified-driver-parity-and-accounting
status: Complete
created: 2026-06-17
complexity: complex
phases: [1, 2, 3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-17.
> To execute: `/execute-plan docs/bugs/lazy-batch-unified-driver-parity-and-accounting/plans/all-phases-unified-driver-parity.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — lazy-batch unified-driver parity & cycle-accounting gaps  (v2)

**PHASES.md files:**
- `docs/bugs/lazy-batch-unified-driver-parity-and-accounting/PHASES.md` (3 phases)

**SPEC.md files:**
- `docs/bugs/lazy-batch-unified-driver-parity-and-accounting/SPEC.md`

**Total phases:** 3
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py`, `.md` SKILL file, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **DISPATCH-ENVIRONMENT OVERRIDE (read if executed under `/lazy-bug-batch`):** when this plan is executed by a cycle subagent that does NOT have the `Agent` tool, perform ALL work units INLINE with `Read`/`Edit`/`Write` (the dispatch-level prohibition is only on invoking another `/lazy*` skill). Write failing tests BEFORE implementing each TDD work unit even when inline. Everything else in this plan (batch ordering, review gate, quality gates, commit policy) stays authoritative.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies; it is a harness defect with no `**Depends on:**` block.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines). (When executed by an Agent-less cycle subagent, see the DISPATCH-ENVIRONMENT OVERRIDE above — do the work inline, test-first.)
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents
3. Every TDD work unit goes through the test-first pipeline — failing tests written FIRST, then implementation makes them pass
4. PHASES.md is updated AFTER EACH batch completes (not deferred)
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing
6. Mistakes are fixed immediately before launching the next batch
7. After all batches in a phase finish, integration verification confirms all changes work together
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it (`user/scripts/CLAUDE.md` documents the lazy state machine + counter semantics — update it for Phase 1; the lazy-batch coupling table in the repo-root `CLAUDE.md` for Phases 2–3)
9. Each completed phase is committed and pushed before the next phase begins
10. Cross-feature phases may run in parallel when dependencies are satisfied and no file conflicts exist
11. This plan is self-contained — follow it exactly as written without relying on external context
12. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory

## Project quality gates (claude-config harness repo)

This repo has no Tauri/MCP runtime. The quality gates for these changes are:

```bash
python3 user/scripts/test_lazy_core.py          # shared lazy_core unit suite (must exit 0)
python3 user/scripts/lazy-state.py --test       # feature state-machine smoke harness (must exit 0)
python3 user/scripts/bug-state.py --test        # bug state-machine smoke harness (must exit 0)
python3 user/scripts/lazy_parity_audit.py --repo-root . # full parity audit (must exit 0)
python3 user/scripts/lint-skills.py --check-projected   # skill-injection lint (must exit 0)
```

The first three MUST stay green for ANY `lazy_core.py` change (shared by both state machines — the Coupling Rule in `user/scripts/CLAUDE.md`). If `lazy-state.py --test` / `bug-state.py --test` output is byte-pinned against `tests/baselines/*.txt` and the change legitimately alters output, regenerate the baseline ONLY by piping live `--test` through `_normalize_smoke_output` (in `test_lazy_core.py`) — never by hand.

## Execution Schedule

| Step | Phase | Title | Blocked by | Parallel? |
|------|-------|-------|------------|-----------|
| 1 | P1 | Item 1 — forward-cycle counter advance (Fix-A) | — | Solo |
| 2 | P2 | Item 3 — on-disk bug pickup (breadcrumb + driver fallthrough) | — | Solo |
| 3 | P3 | Item 2 — archive fixed bugs (`--archive-fixed` chain + parity predicate) | — | Solo |

All three phases are independent (no cross-phase dependency edges). They are executed in numeric order for clean per-phase commits; none blocks another.

## Work Units

- [x] WU-1 — Item 1: consume-independent forward/meta counter advance in `lazy_core` + `--apply-pseudo` call-site (Fix-A)
- [x] WU-2 — Item 1: hermetic tests for the state-change advance + idempotence + meta-routing + Theory-1b
- [x] WU-3 — Item 3: `_load_bug_queue_for_merged` bare-except → `_diag` breadcrumb + breadcrumb test
- [x] WU-4 — Item 3: unified-driver-loop fallthrough prose mirrored across `/lazy-batch` ↔ `/lazy-batch-cloud` (option b)
- [x] WU-5 — Item 2: `__mark_fixed__` block (incl. `--archive-fixed` chain) added to `/lazy-batch` + `/lazy-batch-cloud` Step 1c.5
- [x] WU-6 — Item 2: `--archive-fixed` parity predicate in `lazy_parity_audit.py` + parity-predicate test

---

### Phase: P1 — Item 1: forward-cycle counter advance for inline pseudo-skills (Fix-A)

**Goal:** Make `forward_cycles`/`meta_cycles` advance correctly for forward-advancing inline pseudo-skill cycles (which trigger no Agent dispatch / no registry consume), via a consume-independent advance keyed on `(feature_id, current_step, sub_skill)` change (Fix-A — also closes the Theory-1b verbatim-real-skill miss).
**Entry criteria:** None (first phase).
**SPEC.md references:** Verified Symptom 1, Theory 1 + 1b, Proven Findings item 1. ⚖ policy resolution in PHASES.md: Fix-A.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | None (WU-1 owns `lazy_core.py` + `lazy-state.py`) |
| 2     | WU-2      | Solo      | WU-2 owns `test_lazy_core.py` (depends on WU-1's symbol) |

> WU-1 and WU-2 are sequential: WU-2's tests assert WU-1's new symbol/behavior. WU-1 ships its own minimal failing-test-then-green if executed as one TDD unit; WU-2 broadens coverage. Order: WU-1 then WU-2 (the test broadening reads the final symbol shape).

#### WU-1 — consume-independent forward/meta advance (Fix-A) + `--apply-pseudo` call-site

- **Scope:** PHASES P1 deliverables 1 + 2 — add the consume-independent advance to `lazy_core.py` and call it from the `lazy-state.py --apply-pseudo` handler.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy_core.py` — add a consume-independent forward/meta advance. Implement EITHER as a new `advance_forward_cycle(state)` mirroring `advance_meta_cycle()` [VERIFY: grep -n "def advance_meta_cycle" user/scripts/lazy_core.py] OR by extending `advance_run_counters(state)` [VERIFY: grep -n "def advance_run_counters" user/scripts/lazy_core.py] so it ALSO advances when the marker-recorded last-advance tuple `(feature_id, current_step, sub_skill)` differs from the current state — even when `consumed_emission_count()` [VERIFY: grep -n "def consumed_emission_count" user/scripts/lazy_core.py] did not rise. Persist a new `last_advance_state_key` field in the marker. Reuse the existing classifier (real `sub_skill` → `forward_cycles`; `__`-prefixed/falsy → `meta_cycles`) [VERIFY: grep -n "startswith(\"__\")" user/scripts/lazy_core.py] and the existing `read_run_marker` + `_atomic_write` marker I/O [VERIFY: grep -n "_MARKER_FILENAME" user/scripts/lazy_core.py].
  - `user/scripts/lazy-state.py` — in the `--apply-pseudo` handler [VERIFY: grep -n "args.apply_pseudo is not None" user/scripts/lazy-state.py], after a successful forward-advancing pseudo-skill apply, call the new advance so `__mark_complete__`/`__mark_fixed__`/`__write_validated_from_skip__`/`__write_validated_from_results__`/`__grant_skip_no_mcp_surface__`/`__flip_plan_complete_cloud_saturated__` increment `forward_cycles`. Marker-gated (no-op when no run marker).
- **Test files:** `user/scripts/test_lazy_core.py` (WU-1 writes ONE failing test proving the regression: a forward-advancing pseudo-skill apply with NO consume increment still advances `forward_cycles`; WU-2 broadens).
- **Test expectations (WU-1's RED test):** with a marker present and `consumed_emission_count()` unchanged since the last advance, calling the new advance with a real `sub_skill` and a CHANGED `(feature_id, current_step, sub_skill)` tuple increments `forward_cycles` by exactly 1. Against pre-fix `advance_run_counters` this FAILS (it returns the marker unchanged because `current_consume <= prior_consume`) — RED-for-the-right-reason.
- **Implementation goal:** the counter advances on a genuine state change independent of the consume oracle, while a bare probe/inject re-fire (no state change AND no consume) remains a no-op (preserve the `test_advance_run_counters_consume_gated` invariant [VERIFY: grep -n "test_advance_run_counters_consume_gated" user/scripts/test_lazy_core.py]). A legacy marker without `last_advance_state_key` defaults to None → first state-change always advances.
- **Spec requirements:** SPEC Proven Findings item 1 ("Add `advance_forward_cycle()` … Strongly consider the more robust Fix-A variant"); PHASES P1 ⚖ policy: Fix-A.
- **Batch:** 1.

#### WU-2 — broaden the advance test coverage

- **Scope:** PHASES P1 deliverable 3 — hermetic tests for cases (a)–(d).
- **TDD:** yes (these ARE the tests).
- **Files to create/modify:** `user/scripts/test_lazy_core.py` — add tests in a new `_TESTS = _TESTS + [...]` registration block [VERIFY: grep -n "_TESTS = _TESTS + " user/scripts/test_lazy_core.py] (named in the orphan-guard's coverage [VERIFY: grep -n "orphan" user/scripts/test_lazy_core.py]).
- **Test expectations:** (a) forward-advancing pseudo-skill apply with no consume still advances `forward_cycles` by 1; (b) a repeated identical `(feature_id, current_step, sub_skill)` with no consume does NOT advance (idempotent across re-fires); (c) a `__`-prefixed cleanup pseudo-skill routes to `meta_cycles`, not `forward_cycles`; (d) a real-skill `sub_skill` change advances `forward_cycles` once on a verbatim (consume-missed) dispatch (Theory-1b closure). Follow the existing advance-test pattern [VERIFY: grep -n "def test_fold_and_advance_run_counters" user/scripts/test_lazy_core.py].
- **Implementation goal:** all four cases pass against WU-1's implementation; the suite stays green.
- **Spec requirements:** SPEC Theory 1 + 1b; PHASES P1 deliverable 3.
- **Batch:** 2.

**P1 Runtime Verification (ticked by the hermetic test harness, NOT by `/execute-plan` — left for the runtime/validation gate):** these are PHASES P1's `<!-- verification-only -->` rows (test-harness exit-0 assertions). `/execute-plan` runs the gates in Step B.4 as part of landing WU-1/WU-2; the PHASES rows are the deferred-verification record.

---

### Phase: P2 — Item 3: on-disk bug pickup in the unified driver (breadcrumb + driver fallthrough, option b)

**Goal:** Make `/lazy-batch` pick up an on-disk bug absent from `docs/bugs/queue.json` (bugs-only; features stay queue.json-driven). Replace the silent bare-except in `_load_bug_queue_for_merged` with a `_diag` breadcrumb, and add an option-(b) driver-loop fallthrough so a single-type queue-exhausted terminal probes the other type before the run stops — keeping `--next-merged` pure.
**Entry criteria:** None (independent of P1).
**SPEC.md references:** Verified Symptom 3 + 4, Theory 3, Proven Findings item 3. ⚖ policy resolution in PHASES.md: option (b).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-3, WU-4 | Yes      | None — WU-3 owns `lazy-state.py` + `test_lazy_core.py`; WU-4 owns the two SKILL.md files |

> WU-3 (Python breadcrumb + test) and WU-4 (SKILL prose) touch disjoint files — they may run in parallel in one batch.

#### WU-3 — `_load_bug_queue_for_merged` bare-except → `_diag` breadcrumb + test

- **Scope:** PHASES P2 deliverable 1 + the breadcrumb test.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy-state.py` — in `_load_bug_queue_for_merged` [VERIFY: grep -n "_load_bug_queue_for_merged" user/scripts/lazy-state.py], replace the bare `except Exception: return []` [VERIFY: grep -n "except Exception:  # noqa: BLE001" user/scripts/lazy-state.py] with a handler that emits `lazy_core._diag(...)` [VERIFY: grep -n "def _diag" user/scripts/lazy_core.py] naming the load failure BEFORE returning `[]`. Preserve fail-open (still degrade to features-only, never crash). Keep the existing `not bug_state_path.exists() → return []` early-out unchanged (expected no-op, no breadcrumb).
  - `user/scripts/test_lazy_core.py` — breadcrumb test (new `_TESTS = _TESTS + [...]` block).
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** forcing the bug-side loader to raise (e.g. monkeypatch the dynamically-loaded `load_bug_queue` to raise, or point at an import that fails) makes `_load_bug_queue_for_merged` emit a `_DIAGNOSTICS` breadcrumb AND still return `[]`. RED against the pre-fix bare-except (no breadcrumb emitted).
- **Implementation goal:** a silent bug-side load failure becomes observable in merged-view diagnostics while the merged view still degrades gracefully.
- **Spec requirements:** SPEC Proven Findings item 3.1; PHASES P2 deliverable 1.
- **Batch:** 1.

#### WU-4 — unified-driver-loop fallthrough prose (option b), mirrored across the coupled pair

- **Scope:** PHASES P2 deliverables 2 + 3 — driver-loop fallthrough prose in `/lazy-batch` Step 1 + mirror into `/lazy-batch-cloud`.
- **TDD:** no (SKILL prose — validated structurally by parity audit + lint, not unit tests).
- **Files to create/modify:**
  - `user/skills/lazy-batch/SKILL.md` — add Step 1 driver-loop prose: when the merged head's type-state-script returns a single-type queue-exhausted terminal (`all-features-complete` for a feature head; `all-bugs-fixed` for a bug head) while the OTHER type still has an actionable item (including on-disk bugs via `load_bug_queue`), the driver probes the other type before declaring the run terminal; only BOTH-types-exhausted stops the run. Keep `--next-merged`/`merged_worklist` ordering-only (no state inference). [VERIFY: grep -n "next-merged" user/skills/lazy-batch/SKILL.md]
  - `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` — mirror the fallthrough (coupled-pair rule), adapted for cloud terminal vocabulary (`cloud-queue-exhausted` stays the cloud-defensive stop; the fallthrough has the same shape with `--cloud` on both state scripts). [VERIFY: grep -n "next-merged" repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md]
- **Implementation goal:** the unified driver no longer stops on a single-type terminal while the other type has actionable work; the masking defect (a stale-Complete feature head hiding an actionable bug) is resolved at the driver-loop layer without touching the pure ordering helpers.
- **Spec requirements:** SPEC Proven Findings item 3.2 option (b); PHASES P2 ⚖ policy: option (b). The coupled-pair mirror rule is in the repo-root `CLAUDE.md` (`/lazy-batch` ↔ `/lazy-batch-cloud`).
- **Batch:** 1.

**P2 propagation note:** WU-4 edits two coupled SKILL files — both must land in the same batch so `lazy_parity_audit.py --merged-view` sees a symmetric pair (an asymmetry is a finding). The Step B.4 quality gates MUST include `lazy_parity_audit.py --repo-root .` and `lint-skills.py --check-projected`.

---

### Phase: P3 — Item 2: unified driver archives fixed bugs (`--archive-fixed` chain + parity predicate)

**Goal:** Wire the `--archive-fixed` follow-up into `/lazy-batch` so a `type==bug` `__mark_fixed__` cycle archives + de-queues the fixed bug with full parity to `/lazy-bug-batch`. SKILL prose + one parity-audit predicate only — NO `lazy_core.py`/`bug-state.py` change (the trim predicate + `archive_fixed` CLI are already correct).
**Entry criteria:** None (independent of P1/P2). Reads `/lazy-bug-batch` Step 1c.5 as the verbatim parity reference.
**SPEC.md references:** Verified Symptom 2, Theory 2, Proven Findings item 2, Affected Area row "Fixed-bug archive (item 2)" + "Coupling/parity".

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-5      | Solo      | WU-5 owns the two SKILL.md files (lazy-batch + lazy-batch-cloud) |
| 2     | WU-6      | Solo      | WU-6 owns `lazy_parity_audit.py` + `test_lazy_core.py` |

> WU-5 must land before WU-6's gate runs green: WU-6 adds the `--archive-fixed` parity predicate which only passes once WU-5 has added the chain to both SKILLs. Order: WU-5 then WU-6.

#### WU-5 — `__mark_fixed__` block (incl. `--archive-fixed` chain) in `/lazy-batch` + `/lazy-batch-cloud` Step 1c.5

- **Scope:** PHASES P3 deliverables 1 + 2.
- **TDD:** no (SKILL prose — validated by parity audit + lint).
- **Files to create/modify:**
  - `user/skills/lazy-batch/SKILL.md` — add a `__mark_fixed__` pseudo-skill block to Step 1c.5 [VERIFY: grep -n "1c.5. Inline pseudo-skill handling" user/skills/lazy-batch/SKILL.md], mirroring `/lazy-bug-batch` Step 1c.5's `__mark_fixed__` block [VERIFY: grep -n "__mark_fixed__" user/skills/lazy-bug-batch/SKILL.md]: the two inline docs-only gates (Gate 1 MCP-coverage audit, Gate 2 completion-integrity, adapted `kind: fixed`/`filename: FIXED.md`), then `bug-state.py --apply-pseudo __mark_fixed__ {spec_path}` (script-owned receipt + status flip + sentinel cleanup), then the chained `bug-state.py --repo-root {repo_root} --archive-fixed {spec_path}` [VERIFY: grep -n "archive-fixed" user/scripts/bug-state.py] (script-owned `git mv` + ref repoint + queue trim + commit), with the `ok: false` → `BLOCKED.md (blocker_kind: archive-failure)` handling + push backstop. Add `__mark_fixed__` to the Step-1c.5 forward-cycle counter list [VERIFY: grep -n "forward_cycles\` for pipeline-advancing" user/skills/lazy-batch/SKILL.md].
  - `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` — mirror the same `__mark_fixed__` block into its Step 1c.5 [VERIFY: grep -n "1c.5. Inline pseudo-skill handling" repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md], adapted for cloud (`bug-state.py --cloud` where the gates probe state; bug validation is docs-only and reachable in cloud).
- **Implementation goal:** a `type==bug` cycle reaching `__mark_fixed__` under the unified driver (and its cloud twin) archives + de-queues the fixed bug exactly as `/lazy-bug-batch` does — closing the hand-trim workaround. The archive mechanics stay fully script-owned (zero hand edits by the orchestrator beyond the `BLOCKED.md` sentinel on `ok: false`).
- **Spec requirements:** SPEC Proven Findings item 2 ("Wire the `--archive-fixed` follow-up … mirror into `/lazy-batch-cloud` per the coupling rule"); Affected Area item-2 row.
- **Batch:** 1.

#### WU-6 — `--archive-fixed` parity predicate + test

- **Scope:** PHASES P3 deliverables 3 + 4.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy_parity_audit.py` — add `(r"--archive-fixed", "bug archive --archive-fixed chain")` to `_MERGED_VIEW_PREDICATES` [VERIFY: grep -n "_MERGED_VIEW_PREDICATES" user/scripts/lazy_parity_audit.py] so `audit_merged_view_dispatch_parity` [VERIFY: grep -n "def audit_merged_view_dispatch_parity" user/scripts/lazy_parity_audit.py] asserts BOTH drivers chain `--archive-fixed`. Reuse the existing predicate loop — do NOT add a new audit function.
  - `user/scripts/test_lazy_core.py` — parity-predicate test (new `_TESTS = _TESTS + [...]` block).
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** over a fixture (or temp SKILL copies) — a driver SKILL MISSING the `--archive-fixed` mention produces a `[merged-view] … missing bug archive --archive-fixed chain` finding; both drivers WITH the chain produce zero findings. RED against the pre-WU-5 `/lazy-batch` (no `__mark_fixed__` block → no `--archive-fixed` → finding). Follow any existing parity-audit test pattern [VERIFY: grep -n "audit_merged_view_dispatch_parity\|merged_view" user/scripts/test_lazy_core.py]; if none, call `lazy_parity_audit.audit_merged_view_dispatch_parity` directly against a temp repo-root fixture with two SKILL stubs.
- **Implementation goal:** the parity audit mechanically guards the `--archive-fixed` chain so future drift (a driver dropping the chain) becomes a CI-visible finding — the SPEC Coupling/parity requirement.
- **Spec requirements:** SPEC Affected Area "Coupling/parity" row; PHASES P3 deliverables 3 + 4.
- **Batch:** 2.

**P3 Runtime Verification (ticked by parity audit + lint, NOT by `/execute-plan`):** PHASES P3's `<!-- verification-only -->` rows are the `lazy_parity_audit.py --merged-view` exit-0 + full-audit exit-0 + lint exit-0 assertions, run as Step B.4 gates while landing WU-5/WU-6.

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** all three phases are independent and ready from the start; execute in numeric order P1 → P2 → P3 for clean per-phase commits. If no phases are ready, jump to Blocking Issue Protocol.
2. **Announce:** Print "Implementing Phase N: [title]"
3. **Review prior context:** Re-read previously completed phases' Implementation Notes in PHASES.md — actual symbol names / marker field names may differ from the plan; they take priority where they diverge.
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "Phase N: [title] — committed as [hash]"
7. **Loop:** Return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (task tool loading via ToolSearch, task creation for all work units, update protocol). NOTE: if ToolSearch returns no task tools in this environment, track progress via the `- [ ] WU-N` checkboxes in this plan instead — do not block on missing task tools.

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and this plan file. Do NOT rely on cached content.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — ZERO INLINE IMPLEMENTATION)

**PRE-FLIGHT CHECK:** confirm (1) you will use `Agent` with `model: "sonnet"` for ALL code changes, (2) you will NOT call `Edit`/`Write` on source/test files. If either is false, re-read EXECUTION MODEL. (If running under an Agent-less cycle subagent — see the DISPATCH-ENVIRONMENT OVERRIDE at the top — do the work INLINE, test-first, instead.)

Read before proceeding: `tdd-protocol.md`, `subagent-launch.md`, `tdd-test-agent.md`, `implementation-agent.md` (paths in the Component Reference Card). For TDD work units (WU-1, WU-2, WU-3, WU-6) the test agent writes failing tests FIRST; the impl agent makes them pass. WU-4 and WU-5 are non-TDD SKILL-prose units (validated by the parity audit + lint at Step B.4).

**POST-DISPATCH GATE:** verify you composed `Agent` calls and did not edit source/test files directly (unless under the inline override).

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` and `~/.claude/skills/_components/mount-site-verification.md`. Execute the full protocol: scope measurement, ground-truth verification, propagation check, mount-site verification, verdict (PASS / PASS-WITH-FIXES / NEEDS-REWORK). Do NOT proceed on a NEEDS-REWORK.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md`. Check off completed implementation deliverables (NOT the `<!-- verification-only -->` runtime rows — those are gate-owned) and add an Implementation Notes block (date, work completed, integration notes, pitfalls, files modified). Also tick the matching `- [ ] WU-N` row in this plan's `## Work Units` checklist.

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/quality-gates.md`, then run the harness gates listed in "Project quality gates" above. 100% pass required before proceeding. For ANY `lazy_core.py` change (Phase 1), run ALL of `test_lazy_core.py` + `lazy-state.py --test` + `bug-state.py --test` (Coupling Rule). For Phases 2–3 (SKILL prose + parity), run `lazy_parity_audit.py --repo-root .` + `lint-skills.py --check-projected` in addition to the unit suite.

#### Step B.4.5: MCP Integration Test (BLOCKING — if applicable)

N/A for this repo — claude-config has no Tauri/MCP runtime (PHASES `**MCP runtime:** not-required`). Skip with a note: "no MCP surface — hermetic `--test` + parity audit + lint are the runtime gates."

#### Step B.5: Commit Batch

Read the commit policy: first try `.claude/skill-config/commit-policy.md` in the project root; if absent, `~/.claude/skills/_components/commit-and-push.md`. Work branch is `main` (this repo's default; commits + push go to `main` only — never branch, never force).

#### Step B.6: Proceed to Next Batch

**Checklist before proceeding (all must be true):** review verdict produced; PHASES.md + `- [ ] WU-N` updated; all quality gates pass; Step B.5 committed. If any unchecked, go back.

### Propagation Awareness Note

- Phase 1 adds a `last_advance_state_key` marker field — any marker reader must treat it as optional (legacy markers lack it). Both `lazy-state.py --test` and `bug-state.py --test` are gates (shared `lazy_core`).
- Phase 2 WU-4 and Phase 3 WU-5 each edit a coupled SKILL pair — both files of a pair MUST land in the same batch so the parity audit sees a symmetric pair.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md`. For this repo, integration verification = the full gate suite green (`test_lazy_core.py`, both `--test` harnesses, `lazy_parity_audit.py`, `lint-skills.py`) + a spot `python3 user/scripts/project-skills.py` to confirm the SKILL edits project cleanly.

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md`. For Phase 1, update `user/scripts/CLAUDE.md` (the counter-advance semantics + the new `last_advance_state_key` marker field). For Phases 2–3, confirm the repo-root `CLAUDE.md` lazy-batch coupling table + the `mark-fixed-archive.md` consumer list still describe reality (the unified driver now also chains `--archive-fixed`).

#### Commit and Push Post-Phase Changes

Read the commit policy (as Step B.5). Commit + push to `main`.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point during execution:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed phases** not yet committed.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** Phase N: [title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining phases (not attempted):**
   - [list]

4. **Do not attempt to work around the blocker.**

Blocking issues include: a `lazy_core` change that breaks `lazy-state.py --test` or `bug-state.py --test` in a way that can't be fixed in 2 retries (a real behavior regression — investigate, do NOT regenerate the baseline to mask it); a parity-audit finding that can't be resolved in 2 retries; a subagent failure unrecoverable after 2 retries; a git push conflict unresolvable by rebase; any error requiring architectural decisions beyond the specs.

---

## Completion

When all three phases are complete:

1. **Run the full quality-gate suite one final time:** `python3 user/scripts/test_lazy_core.py && python3 user/scripts/lazy-state.py --test && python3 user/scripts/bug-state.py --test && python3 user/scripts/lazy_parity_audit.py --repo-root . && python3 user/scripts/lint-skills.py --check-projected`.
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Items fixed:** item 1 (counter advance), item 2 (archive fixed bugs), item 3 (on-disk bug pickup)
   **Total phases completed:** 3
   **Total commits:** M
   **Final quality-gate status:** all green

   **Commit log:** | Commit | Phase | Title | table

   **Implementation Notes summary:** key cross-cutting notes (the `last_advance_state_key` marker field; the coupled-pair mirrors for items 2 + 3; NO `lazy_core`/`bug-state` change for item 2).

   Note: this plan does NOT flip SPEC/PHASES `**Status:**` to `Fixed` or write `FIXED.md` — those are owned by the orchestrator's `__mark_fixed__` gate after the validation tail. `/execute-plan` flips only the plan-part frontmatter `status:` → `Complete` and the per-phase checkboxes; when the LAST phase lands, set the top-level PHASES `**Status:**` to `In-progress` (implementation done, validation pending) and let the state machine route forward.
