---
kind: implementation-plan
feature_id: build-queue-false-green-on-silent-build-failure
status: Complete
created: 2026-07-03
phases: [4]
complexity: mechanical
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-03.
> To execute: `/execute-plan <this-file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 3 of 3. Sibling parts:
> - part 1: `docs/bugs/build-queue-false-green-on-silent-build-failure/plans/all-phases-false-green-part-1.md`
> - part 2: `docs/bugs/build-queue-false-green-on-silent-build-failure/plans/all-phases-false-green-part-2.md`
> - part 3: `docs/bugs/build-queue-false-green-on-silent-build-failure/plans/all-phases-false-green-part-3.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — build-queue-false-green-on-silent-build-failure (Phase 4)  (v2)

**PHASES.md files:**
- `docs/bugs/build-queue-false-green-on-silent-build-failure/PHASES.md` (build-queue-false-green, phase 4 of 4)

**SPEC.md files:**
- `docs/bugs/build-queue-false-green-on-silent-build-failure/SPEC.md`

**Total phases (this part):** 1 (Phase 4 — re-enable enforcement hook + documentation)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract. This is the **claude-config harness repo** — gates are Python + Pester, NOT `/msbuild`/`/mstest`; the **Plan-specific execution notes** below override the contract's default gate rows.

## Plan-specific execution notes (gate overrides — READ FIRST)

| Gate | Command | Applies to |
|------|---------|-----------|
| Hook deny/allow suite | `python user/scripts/test_hooks.py` | WU-1 (the `test_bqe_*` tests re-pass only against the re-enabled hook) |
| Pester (full accumulated suite) | `powershell.exe -Command "Invoke-Pester user/scripts/build-queue-hygiene.Tests.ps1 -Output Detailed"` and `powershell.exe -Command "Invoke-Pester repos/cognito-forms/.claude/scripts/test-filtered.Tests.ps1 -Output Detailed"` | Regression check (Phases 1-3 coverage stays green) |

- **Commit/push:** personal identity (`jacobmadsen12321@gmail.com`) — pushes NOT hook-blocked. Push after gates green.
- **Complexity = `mechanical` — why every WU qualifies (for the `lazy-batch-retro` audit):** WU-1 deletes a precisely-specified temporary disable block (lines 2-7, a `>>> TEMPORARILY DISABLED <<<` banner ending in `exit 0`) — restoring pre-existing enforcement, no novel design, no new seam, and guarded by 20 existing `test_bqe_*` tests. WU-2 is pure documentation prose (two Scripts-table notes). Neither involves an algorithm, DSP, or cross-boundary wiring. `mechanical` → Sonnet dispatch is safe here.
- **Drift correction (WU-2 — grounded at plan time, do NOT re-litigate):** PHASES deliverable 2 says to update a "Scripts-table hygiene note" in `~/source/repos/CLAUDE.md` (= `workspace/CLAUDE.md`). A plan-time grep confirmed that file carries **NO build-queue / hygiene / fidelity content at all** (it is a cross-repo *navigation* doc — Key Repositories, Git Identity, Aliases, Navigation Pattern). The build-queue Scripts-table hygiene note exists **only** in the repo-root `claude-config/CLAUDE.md` (lines 178-180) — which is what PHASES deliverable 3 names. So deliverables 2 and 3 resolve to **one real target**: the repo-root `CLAUDE.md`. Do **NOT** fabricate a net-new build-queue section in `workspace/CLAUDE.md` — that is out of scope for a navigation doc and would drift it from its purpose. If (and only if) `workspace/CLAUDE.md` has since gained a build-queue hygiene note by execution time, update it too; otherwise record deliverable 2 as N/A (misattribution) in the final report.
- **Symlink caveat:** `~/source/repos/CLAUDE.md` is a **symlink** to `workspace/CLAUDE.md`; the Edit tool refuses to write through the `~/.claude`/`~/source/repos` symlinks — always edit the repo path (`workspace/CLAUDE.md`, `CLAUDE.md`) directly.
- **Prerequisite gate — DO NOT re-enable the hook before Parts 1-2 land:** WU-1 restores `build-queue-enforce.sh` enforcement, which the SPEC (Git History) says is gated on this bug being FIXED. Parts 1 and 2 (Phases 1-3) MUST be Complete and Pester-green first (enforced by plan-series ordering). Re-enabling with the fixes absent would resurrect the false-green the hook is meant to backstop.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — no completed hard upstream deps) | | | Intra-bug dependency on Parts 1-2 enforced by plan-series ordering. |

## Execution Schedule

| Step | Phase | Batch | Work Units | Blocked by | Parallel? |
|------|-------|-------|-----------|------------|-----------|
| 1 | P4 | 1 | WU-1, WU-2 | Parts 1-2 Complete | Yes (disjoint files) |

---

### Phase: P4 — Re-enable enforcement hook + documentation

**Goal:** With Root Causes A/B/C fixed and Pester-covered, restore `build-queue-enforce.sh` to enforcing and update the harness documentation that describes the hygiene sweep scope and the fidelity domain. (Live end-to-end verification in a Cognito worktree is the Runtime Verification rows below — human/manual, NOT closed by `/execute-plan`.)

**Entry criteria:** Parts 1-2 Complete (per-project sweep, `Read-WithRetry`, and build-output fidelity gate all merged and Pester-green).

**SPEC.md references:** Proven Finding 4 ("Re-enabling `build-queue-enforce.sh` is gated on this bug being FIXED"); Git History note (hook temporarily disabled at user's request 2026-07-03); Affected Area rows "Enforcement gate" + "Tests."

**Verified reality (grounding):** the disable block is `user/hooks/build-queue-enforce.sh` lines 2-7 — a `# >>> TEMPORARILY DISABLED ... <<<` banner with `exit 0` at line 6 and a `# <<< END TEMPORARY DISABLE >>>` marker at line 7, before all hook logic. `test_hooks.py` contains **20** `def test_bqe_*` tests (PHASES estimated ~22; actual 20) that cannot meaningfully pass against the `exit 0` short-circuit. The **only** build-queue Scripts-table hygiene note is in the repo-root `CLAUDE.md` (lines 178-180); `workspace/CLAUDE.md` has no such note (see WU-2 drift correction above).

#### WU-1 — Remove the temporary disable block; re-enable enforcement

- **Scope (PHASES deliverable 1):**
  - [ ] Remove the temporary disable block in `user/hooks/build-queue-enforce.sh` — the `>>> TEMPORARILY DISABLED <<<` banner through the `# <<< END TEMPORARY DISABLE >>>` marker (lines 2-7, incl. `exit 0` at line 6), restoring the hook to its prior enforcing behavior. Leave the shebang (line 1) and everything from the original `# =====` header onward intact [VERIFY: `grep -n "TEMPORARILY DISABLED\|END TEMPORARY DISABLE" user/hooks/build-queue-enforce.sh`].
- **TDD:** no (mechanical block deletion validated by the existing `test_bqe_*` suite).
- **Files to create/modify:** `user/hooks/build-queue-enforce.sh`.
- **Test files:** none new. Gate = `python user/scripts/test_hooks.py` — the ~20 `test_bqe_*` deny/allow tests must pass (they cannot against the disabled `exit 0` block).
- **Implementation goal:** After the edit, the hook runs its real deny/allow logic (no early `exit 0`); a raw `dotnet build`/`nx build`/`*-filtered.ps1` in a Cognito worktree is denied and redirected to the skill, and the allowlisted commands (`dotnet restore`, `nx lint`, the wrapper, `BUILD_QUEUE_BYPASS=1 …`) still pass — as asserted by `test_bqe_*`.
- **Spec requirements:** SPEC Proven Finding 4; Git History note.
- **Batch:** 1.

#### WU-2 — Update the Scripts-table hygiene note (repo-root CLAUDE.md)

- **Scope (PHASES deliverables 2-3, reconciled to one real target — see drift correction above):**
  - [ ] Update the Scripts-table hygiene note in the repo-root `CLAUDE.md` (lines 178-180 — the `build-queue-status.ps1` row + the "Each build now auto-reaps…" paragraph) to reflect (a) the per-project sweep scope from Phase 1 (`<root>/**/bin` + `<root>/**/obj`, no longer worktree-root-only) and (b) the new `no-output` build-output fidelity value from Phase 3 (add it to the `build_fidelity` domain alongside `log-failure-override` / `verified`) [VERIFY: `grep -n "build_fidelity\|no-tests-matched\|quarantin" CLAUDE.md`].
  - [ ] `workspace/CLAUDE.md`: **no-op unless a build-queue hygiene note has appeared there** — plan-time grep confirmed none exists. Do NOT fabricate one. Record as N/A (PHASES deliverable-2 misattribution) if still absent [VERIFY: `grep -ni "build-queue\|hygiene\|fidelity" workspace/CLAUDE.md` — expect zero hits].
- **TDD:** no (documentation prose).
- **Files to create/modify:** `CLAUDE.md` (repo root) — the single real target.
- **Test files:** none. No code gate for `.md` prose; ensure the note matches the shipped Phase 1 sweep scope + Phase 3 fidelity domain.
- **Implementation goal:** The repo-root `CLAUDE.md` hygiene note describes the quarantine sweep as per-project (`<root>/**/bin` + `<root>/**/obj`) and lists `no-output` in the `build_fidelity` domain, so a future reader/agent knows the false-green defect is closed and how the fidelity now reads.
- **Spec requirements:** SPEC Affected Area; PHASES "Integration Notes for Next Phase" (this doc is the durable record).
- **Batch:** 1 (disjoint files from WU-1 → parallel).

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-1, WU-2 | Yes      | None (`build-queue-enforce.sh` vs repo-root `CLAUDE.md`) |

**Runtime Verification (NOT closed by `/execute-plan` — human/manual live e2e in a Cognito worktree; these close the `__mark_fixed__` gate):**
- [ ] <!-- verification-only --> Seed a poisoned per-project DLL (0-byte `Cognito/bin/Debug/netstandard2.0/Cognito.dll`) and run `/msbuild` via the queue — confirm the banner reports `RESULT=FAIL` and the artifact is quarantined.
- [ ] <!-- verification-only --> Run a real clean `/msbuild` in the same worktree — confirm `RESULT=PASS` with `build_fidelity: verified`.
- [ ] <!-- verification-only --> Force/observe a build that exits 0 with no compiled output — confirm `RESULT=FAIL` with `build_fidelity: no-output` and the corrective next-action text.
- [ ] <!-- verification-only --> Confirm `python user/scripts/test_hooks.py`'s `test_bqe_*` suite passes with the hook re-enabled.

**Completion (gate-owned — do NOT author as a WU):** the `__mark_fixed__` gate flips SPEC.md `**Status:**` and writes `FIXED.md` once all four Runtime Verification rows above pass. `/execute-plan` does NOT flip status or write the sentinel.

---

## Work Units

- [x] WU-1 — Remove the temporary disable block in build-queue-enforce.sh (re-enable enforcement)
- [x] WU-2 — Update the Scripts-table hygiene note in repo-root CLAUDE.md (per-project sweep + no-output fidelity)

---

When both WUs are checked and gates are green (`test_hooks.py` `test_bqe_*` passing + full Pester suite green), the implementation of this bug fix is done. The `__mark_fixed__` gate handles the SPEC status flip / `FIXED.md` write after the live Runtime Verification rows pass — this plan does NOT do that.
