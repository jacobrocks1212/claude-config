---
kind: implementation-plan
feature_id: hardening-intervention-records-unmeasurable-or-missing
status: In-progress
created: 2026-07-12
complexity: complex
phases: [1, 2, 3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-12.
> To execute: `/execute-plan {path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 2. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\hardening-intervention-records-unmeasurable-or-missing\plans\all-phases-hardening-intervention-records-part-1.md` (this file)
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\hardening-intervention-records-unmeasurable-or-missing\plans\all-phases-hardening-intervention-records-part-2.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — Hardening intervention records unmeasurable or missing (part 1: complex core)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\hardening-intervention-records-unmeasurable-or-missing\PHASES.md` (5 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\hardening-intervention-records-unmeasurable-or-missing\SPEC.md`

**Total phases:** 5 (this part covers the complex core of Phases 1–3)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is **claude-config**, a harness-config repo whose gates are Python/projection, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the Plan-specific execution notes below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config gate overrides)

- **Quality gates for this repo** (run from the repo root `C:\Users\Jacob\source\repos\claude-config`, NOT the default build gates):
  - `python user/scripts/test_lazy_core.py` (or `pytest user/scripts/test_lazy_core.py`) — the `lazy_core` pytest suite.
  - `python user/scripts/test_doc_drift_lint.py` (or `pytest`) — the doc-drift-lint pytest suite (includes a repo-clean self-check).
  - `python user/scripts/lazy-state.py --test` and `python user/scripts/bug-state.py --test` — the in-file smoke harnesses (byte-baseline pinned; regenerate a baseline ONLY via `_normalize_smoke_output`, never by hand).
  - `python user/scripts/lazy_parity_audit.py --repo-root .` — REQUIRED after any edit to a coupled state-script pair (WU-3 touches this). `--repo-root` is required; there is NO `--report` flag.
  - `python ~/.claude/scripts/doc-drift-lint.py --repo-root .` — the linter itself (WU-4 adds a check to it).
- **No MCP runtime** — this repo has no Tauri app / MCP server. Bug-completion evidence is the serving-path regression tests below, per `symptom-reproduction-gate.md`. There is NO `/mcp-test` step for this bug.
- **Coupled-pair discipline:** WU-2 (`lazy-state.py`) and WU-3 (`bug-state.py`) are a coupled pair for the `--record-intervention` CLI. WU-3 is a separate batch so it mirrors the landed WU-2 verbatim; run `lazy_parity_audit.py` after WU-3.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies; the SPEC carries no `**Depends on:**` block, only prose `**Related:**` siblings that are not machine deps.)

## Verified Touchpoint Audit (carried from PHASES.md — `verified: inline`)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `user/scripts/lazy_core.py` | yes | `record_intervention` (:16863), `_intervention_signal_event` (:16469), `_HOST_CAPABILITY_REGISTRY` (:13609), `_diag` | refactor | Add `_INTERVENTION_EVENT_VOCABULARY` frozenset + `validate_intervention_target_signal`; REUSE the closed-registry precedent |
| `user/scripts/lazy-state.py` | yes | `--record-intervention` handler (:12469), arg defs (:10947) | refactor | Reject on the CLI path via `_die`-free exit-1 |
| `user/scripts/bug-state.py` | yes | mirror `--record-intervention` (4 refs) | refactor | Coupled-pair mirror |
| `user/scripts/doc-drift-lint.py` | yes | `check_*(repo_root)` + `run_checks` (:624), `Finding` (:72) | refactor | Add `check_intervention_coverage`; register in `run_checks` |
| `user/scripts/test_lazy_core.py`, `test_doc_drift_lint.py` | yes | pytest suites | refactor | New reject/degrade + coverage-lint fixtures |

## Execution Schedule

| Step | Feature | Phase(s) | Title | Blocked by | Parallel? |
|------|---------|----------|-------|------------|-----------|
| 1 | this bug | P1 | Vocabulary SSOT + `record_intervention` degrade | — | Batch 1 (solo — WU-1) |
| 2 | this bug | P2 | CLI reject in `lazy-state.py` | WU-1 | Batch 2 (solo — WU-2) |
| 3 | this bug | P2 | `bug-state.py` coupled-pair mirror | WU-2 | Batch 3 (solo — WU-3) |
| 4 | this bug | P3 | Coverage lint in `doc-drift-lint.py` | — | Batch 4 (solo — WU-4) |

> Batches are solo because WU-1→WU-2→WU-3 form a dependency chain (validator → CLI consumer → parity mirror) and WU-4 is independent but ordered last in this part. Each batch is a review gate.

## Work Units

- [ ] WU-1 — lazy_core vocabulary SSOT + validator + `record_intervention` degrade
- [ ] WU-2 — `lazy-state.py` `--record-intervention` CLI reject + hardening hard-fail
- [ ] WU-3 — `bug-state.py` coupled-pair mirror + parity audit
- [ ] WU-4 — `doc-drift-lint.py` `check_intervention_coverage` + fixture

---

### Phase: this bug P1 — Vocabulary SSOT + authoring-time validation

**Goal:** Enforce the D4-B event vocabulary at record-authoring time; an unknown `event:` degrades to `undeclared` (never a frozen bogus zero) and a reusable validator is exposed for the CLI path.
**Entry criteria:** none (first work unit).
**SPEC.md references:** Fix Scope #1; Decision D1 (reject vs degrade split).

#### WU-1 — lazy_core vocabulary SSOT + validator + `record_intervention` degrade
- **Scope:** PHASES Phase 1 deliverables — the `_INTERVENTION_EVENT_VOCABULARY` frozenset, `validate_intervention_target_signal()`, and the `record_intervention` degrade-on-unknown-event branch.
- **TDD:** yes
- **Files to create/modify:** `user/scripts/lazy_core.py`, `user/scripts/test_lazy_core.py`
- **Test files:** `user/scripts/test_lazy_core.py` (new test group)
- **Test expectations (write these RED first):**
  1. `validate_intervention_target_signal` returns `None` for each of the 10 known events (`event:run-start` … `event:containment-refusal`), for a `kpi:foo.bar` target, and for `undeclared`; returns a non-None error string NAMING the valid set for `event:no-route` and `event:route-loop` (the exact r5/r7 mistakes).
  2. `record_intervention(...)` called with `hypothesis_overrides={"target_signal": "event:no-route"}` on a hermetic temp repo writes a record whose frontmatter `target_signal` is `undeclared` and whose `baseline.status` is `not-computable` — assert on the actual file on disk (this is the serving-path regression for Verified Symptom (a): the silently-accepted bogus zero baseline is GONE).
  3. A lock test asserting `_INTERVENTION_EVENT_VOCABULARY` equals the live emit vocabulary — collect the event-type string literals passed to `append_telemetry_event(` across `lazy_core.py` / `lazy-state.py` / `bug-state.py` (mirror the `_collect_*` AST/meta-test precedent, e.g. `test_no_orphaned_test_functions`) and assert set-equality, so the constant can never silently drift from the emitters.
- **Implementation goal:** Add `_INTERVENTION_EVENT_VOCABULARY = frozenset({...})` beside `_intervention_signal_event` [VERIFY: grep -n "def _intervention_signal_event" user/scripts/lazy_core.py], modeled on `_HOST_CAPABILITY_REGISTRY` [VERIFY: grep -n "_HOST_CAPABILITY_REGISTRY" user/scripts/lazy_core.py]. Add `validate_intervention_target_signal(target_signal) -> str | None` (pure; `kpi:` and `undeclared` pass, `event:<known>` passes, `event:<unknown>` returns an error naming the valid set). In `record_intervention` [VERIFY: grep -n "def record_intervention" user/scripts/lazy_core.py], after `target_signal` is resolved (`:~16952`), when it is an unknown `event:` type, degrade it to `undeclared` and append a `_diag` naming the rejected type — the fail-open completion path (D2-A) never raises, but never freezes a measurable-looking zero either.
- **Spec requirements:** SPEC Fix Scope #1 ("closed-set check … single source of truth: a `lazy_core` constant the emit sites and the check both use … completion-gate path stays fail-open but degrades … with a loud diagnostic").
- **Batch:** 1

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | N/A |

---

### Phase: this bug P2 — CLI hard-fail (reject unknown vocabulary + undeclared hardening)

**Goal:** On the CLI path, reject an unknown `event:` target and refuse an undeclared `--pipeline hardening` capture; the completion-gate path (WU-1 degrade) is unchanged. Mirror on `bug-state.py`.
**Entry criteria:** WU-1 complete (`validate_intervention_target_signal` exists).
**SPEC.md references:** Fix Scope #2; Decision D1.

#### WU-2 — `lazy-state.py` `--record-intervention` CLI reject + hardening hard-fail
- **Scope:** PHASES Phase 2 deliverables for `lazy-state.py` — reject unknown `event:` (exit 1, name the valid set), hard-fail undeclared `--pipeline hardening`, accept explicit `--target-signal undeclared` escape hatch.
- **TDD:** yes (in-file `--test` smoke fixtures)
- **Files to create/modify:** `user/scripts/lazy-state.py` (handler at `--record-intervention` + the in-file `--test` block); `tests/baselines/lazy-state-test-baseline.txt` (re-pin via `_normalize_smoke_output` only)
- **Test files:** the in-file `def test_<name>()` block registered in the script's `_TESTS` list.
- **Test expectations (RED first):**
  1. `--record-intervention --id harden-x --pipeline hardening` with NO `--target-signal` → exit 1, no `docs/interventions/harden-x.md` written, stderr carries the sibling-D2 guidance (serving-path regression for Verified Symptom (b): an undeclared hardening record can no longer be authored via the CLI).
  2. `--record-intervention --id harden-x --pipeline hardening --target-signal undeclared` → exit 0, record written with `target_signal: undeclared` (the deliberate, typed escape hatch).
  3. `--record-intervention --id harden-x --target-signal event:route-loop` → exit 1 naming the valid vocabulary set (serving-path regression for Verified Symptom (a): the r7 mistake is now blocked at the CLI).
- **Implementation goal:** In the `--record-intervention` handler [VERIFY: grep -n "if args.record_intervention" user/scripts/lazy-state.py] (`:12469`), after `overrides` is assembled, resolve the effective `target_signal`, call `validate_intervention_target_signal` [VERIFY: grep -n "def validate_intervention_target_signal" user/scripts/lazy_core.py — created in WU-1], and on error write the message to stderr and `return 1` (exit 1 per SPEC D1 — do NOT use `_die`, which exits 2 = malformed-input). Add the hardening hard-fail: when `args.intervention_pipeline == "hardening"` AND the resolved target would be `undeclared` because `--target-signal` was OMITTED (distinguish an omitted flag from an explicit `--target-signal undeclared`), `return 1` with the sibling-D2 guidance. An explicit `--target-signal undeclared` must NOT trip the hard-fail.
- **Spec requirements:** SPEC Fix Scope #2, Decision D1.
- **Batch:** 2

#### WU-3 — `bug-state.py` coupled-pair mirror + parity audit
- **Scope:** PHASES Phase 2 deliverables for `bug-state.py` — mirror WU-2's reject + hard-fail verbatim in `bug-state.py`'s `--record-intervention` handler.
- **TDD:** yes (in-file `--test` smoke fixtures mirrored)
- **Files to create/modify:** `user/scripts/bug-state.py` (handler + in-file `--test`); `tests/baselines/bug-state-test-baseline.txt` (re-pin via `_normalize_smoke_output` only)
- **Test files:** the in-file `--test` block registered in `bug-state.py`'s `_TESTS`.
- **Test expectations (RED first):** the same three cases as WU-2, against `bug-state.py --record-intervention` (which defaults `--pipeline bug`; drive `--pipeline hardening` for the hard-fail cases).
- **Implementation goal:** Mirror WU-2's exit-1 reject + hardening hard-fail into `bug-state.py`'s `--record-intervention` handler [VERIFY: grep -n "record_intervention" user/scripts/bug-state.py]. This is a coupled-pair edit — the two handlers must stay byte-parallel. After landing, run `python user/scripts/lazy_parity_audit.py --repo-root .` and confirm exit 0.
- **Spec requirements:** SPEC Fix Scope #2; the coupled-pair parity rule (root `CLAUDE.md` → `--record-intervention` is "PRESENT ON BOTH SCRIPTS (coupled pair; parity-audited)").
- **Batch:** 3 (separate from WU-2 so the mirror copies landed code; same conceptual file-family, sequenced for parity)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 2 | WU-2 | Solo | N/A |
| 3 | WU-3 | Solo | N/A (different file from WU-2, but sequenced for parity) |

---

### Phase: this bug P3 — Mechanical round-vs-record coverage lint (complex half)

**Goal:** Add `check_intervention_coverage(repo_root)` to `doc-drift-lint.py` (D2-preferred home) so every `Mechanical fix applied:` round in the current month's hardening-log has a matching record or an explicit exemption marker.
**Entry criteria:** none structurally (independent of WU-1/2/3), ordered last in this part.
**SPEC.md references:** Fix Scope #3; Decision D2 (doc-drift-lint preferred), Decision D3 (exemption marker).

#### WU-4 — `doc-drift-lint.py` `check_intervention_coverage` + fixture
- **Scope:** PHASES Phase 3 lint deliverable — the new check function + `run_checks` registration + the three-round hermetic fixture.
- **TDD:** yes
- **Files to create/modify:** `user/scripts/doc-drift-lint.py`, `user/scripts/test_doc_drift_lint.py`
- **Test files:** `user/scripts/test_doc_drift_lint.py` (new coverage fixture)
- **Test expectations (RED first):**
  1. A hermetic fixture hardening-log with THREE rounds — one covered (`## Round 1 …` + `Mechanical fix applied:` + a matching `docs/interventions/harden-<YYYY-MM>-r1.md`), one exempt (`## Round 2 …` + `**Intervention record:** none`), one HOLE (`## Round 3 …` + `Mechanical fix applied:` + NO record + NO exemption) — asserts `check_intervention_coverage` flags EXACTLY the hole (a `Finding` naming round 3 + the expected record path) and passes the covered + exempt rounds (serving-path regression for Verified Symptom (c): a missing-record hole is now mechanically caught).
  2. The existing repo-clean self-check in `test_doc_drift_lint.py` still passes (reconciled in part-2 WU-6 after the live r1-r3/r5/r7 data is repaired — until then, if the live tree has holes, pin the self-check with the known-hole exemption or run WU-4's fixture-only test; part-2 closes the live holes).
- **Implementation goal:** Add `check_intervention_coverage(repo_root)` to `doc-drift-lint.py` [VERIFY: grep -n "def run_checks" user/scripts/doc-drift-lint.py], REUSING the `Finding` dataclass [VERIFY: grep -n "class Finding" user/scripts/doc-drift-lint.py] and `_read_text` [VERIFY: grep -n "def _read_text" user/scripts/doc-drift-lint.py]. Parse `docs/specs/turn-routing-enforcement/hardening-log/<current-month>.md` — round header `## Round N — <date> — <kind>`; for each round whose Action carries the `Mechanical fix applied:` form, assert either `docs/interventions/harden-<YYYY-MM>-rN.md` exists OR the round body carries a `**Intervention record:** none` line; neither → a `Finding` (drift). Register the new check in `run_checks` [VERIFY: grep -n "def run_checks" user/scripts/doc-drift-lint.py]. Deliberate divergences use the existing `doc-drift:deliberate-divergence` marker convention.
- **Spec requirements:** SPEC Fix Scope #3, Decisions D2 + D3.
- **Batch:** 4

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 4 | WU-4 | Solo | N/A |

---

## End of part 1

After WU-4's batch review passes and the claude-config gates are green (`test_lazy_core.py`, `test_doc_drift_lint.py`, `lazy-state.py --test`, `bug-state.py --test`, `lazy_parity_audit.py --repo-root .`), commit + push per the execution contract, then proceed to part 2 (`/execute-plan …-part-2.md`). Do NOT flip any top-level `**Status:**` or write `FIXED.md` — those are `__mark_fixed__`-gate-owned.
