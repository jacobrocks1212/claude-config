---
kind: implementation-plan
feature_id: external-owner-contracts-locked-without-consultation
status: Ready
created: 2026-07-18
phases: [1]
complexity: mechanical
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan docs/bugs/external-owner-contracts-locked-without-consultation/plans/fix-external-owner-consultation-gate.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — external-owner-contracts-locked-without-consultation  (v2)

**PHASES.md files:**
- `docs/bugs/external-owner-contracts-locked-without-consultation/PHASES.md` (1 phase)

**SPEC.md files:**
- `docs/bugs/external-owner-contracts-locked-without-consultation/SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (claude-config is a harness-config repo whose gates are Python/projection lints, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the Plan-specific execution notes below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies. The SPEC declares no `**Depends on:**` block; the only cross-item relationship is a same-file ordering constraint with sibling bug `premise-contradictions-demoted-not-escalated` on `user/skills/spec/SKILL.md`, already honored by the out-of-pipeline fix.)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | external-owner-contracts-locked-without-consultation | P1 | External-owner consultation gate + Cognito OW-sync convention | — | Yes (WU-1, WU-2 file-disjoint) |

## Work Units

- [ ] WU-1 — Verify/author external-owner surface rule in spec/SKILL.md
- [ ] WU-2 — Verify/author Overwatch OW-sync convention line in Cognito CLAUDE.local.md

## Plan-specific execution notes

- **Fix already landed out-of-pipeline (2026-07-10).** Both deliverables' content is already present on disk (verified at planning time: `user/skills/spec/SKILL.md:401-410`; `repos/cognito-forms/Cognito/CLAUDE.local.md:8`). Each WU below is therefore a **verify-and-confirm** task: grep for the required text, read the surrounding context to confirm the semantics match the SPEC Fix Scope, and tick the WU. Only author the prose if a grep unexpectedly MISSES — in which case write it per the exact Fix Scope wording. Do NOT duplicate content that is already present.
- **Gates (claude-config, NOT the contract's msbuild/mstest defaults).** This is a docs/skills-only change. The relevant gates are the skill lints — run from the repo root:
  - `python ~/.claude/scripts/lint-skills.py` (broken injections / embedded patterns — `user/skills/spec/SKILL.md` is a skill file)
  - `python3 ~/.claude/scripts/doc-drift-lint.py --repo-root .` (doc-claim cross-check; the Cognito file is a repo-projected doc)
  There is NO compiled build/test for this change; "quality gates" = the two lints above pass (exit 0). No MCP runtime (`**MCP runtime:** not-required`).
- **One writer per file.** `user/skills/spec/SKILL.md` is shared with sibling bug `premise-contradictions-demoted-not-escalated`; since the external-owner rule is already on disk, no concurrent edit is needed. If (and only if) authoring is required, respect that the sibling bug's edits to this file take precedence in ordering.
- **Commit/push is orchestrator-owned** (this plan's executing session), per `.claude/skill-config/commit-policy.md`: conventional commit, no AI attribution, `git push` after commit, regen `LAZY_QUEUE.md` before the commit.

### Phase: external-owner-contracts-locked-without-consultation P1 — External-owner consultation gate + Cognito OW-sync convention

**Goal:** Confirm on disk (author only if missing) the two-layer fix that stops `/spec` locking an externally-consumed contract without owner consultation: a generic decision-protocol rule + Cognito-specific OW-sync convention knowledge.

**Entry criteria:** None (single phase, first phase). The shared-file ordering constraint on `spec/SKILL.md` (sibling bug `premise-contradictions-demoted-not-escalated`) is already satisfied.

**SPEC.md references:** `## Fix Scope (locked with operator, 2026-07-10)` items 1 and 2; `## Proven Findings` #1 (the tell is in the option text — no new detection machinery) and #3 (two layers required).

#### WU-1 — Verify/author external-owner surface rule in spec/SKILL.md

- **Scope:** PHASES.md Phase 1 deliverable: `user/skills/spec/SKILL.md` carries the External-owner surface rule (tag `external-owner: <team/repo>` on a decision creating/changing a contract consumed outside this repo; NOT lockable on in-repo evidence alone; (a) confirm owner conventions cited in SPEC OR (b) operator picks "lock WITHOUT owner confirmation — risk accepted" recorded verbatim; trigger keys on the model's own "external contract change"/"other-team work" recognition; 57077 LD4 named as anti-pattern).
- **TDD:** no (skill-prose rule; no runtime-testable behavior — verified by grep + semantic read, not a unit test).
- **Files to create/modify:** `user/skills/spec/SKILL.md` — verify the External-owner surface rule is present [VERIFY: grep -n "external-owner" user/skills/spec/SKILL.md] (confirmed present at lines 401–410 at planning time). Author per Fix Scope #1 wording ONLY if the grep misses.
- **Implementation goal:** The decision/brainstorm protocol in `spec/SKILL.md` refuses to lock an externally-consumed contract on in-repo evidence alone and offers the risk-accepted escape hatch, per SPEC Fix Scope #1. Content already present — do NOT duplicate; confirm and tick.
- **Spec requirements:** SPEC Fix Scope item 1 (verbatim wording of the rule), Proven Findings #1 and #3.
- **Batch:** 1

#### WU-2 — Verify/author Overwatch OW-sync convention line in Cognito CLAUDE.local.md

- **Scope:** PHASES.md Phase 1 deliverable: `repos/cognito-forms/Cognito/CLAUDE.local.md` carries the Overwatch integration convention line (new Cognito columns surface in Overwatch via OW sync / `[Overwatch]` attribute mirroring, added automatically; classic `CognitoEvent`s being phased out; consult the OW team before adding a new classic event; source: OW team / George Perez, Slack 2026-07-10).
- **TDD:** no (repo-knowledge prose line; verified by grep + read).
- **Files to create/modify:** `repos/cognito-forms/Cognito/CLAUDE.local.md` — verify the Overwatch integration convention line is present [VERIFY: grep -ni "phased out" repos/cognito-forms/Cognito/CLAUDE.local.md] (confirmed present at line 8 at planning time). Author per Fix Scope #2 wording ONLY if the grep misses.
- **Implementation goal:** The Cognito repo-knowledge file records the OW-sync-vs-classic-events convention so the wrong default (a new classic CognitoEvent) is never drafted, per SPEC Fix Scope #2. Content already present — do NOT duplicate; confirm and tick.
- **Spec requirements:** SPEC Fix Scope item 2 (verbatim wording), Proven Findings #3 (repo-specific knowledge layer).
- **Batch:** 1

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2 | Yes       | None (different files) |

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md **Status:** to `Fixed`, writes `FIXED.md`, and archives once both WUs are verified and the validation tail passes. This plan authors no Status-flip / receipt / archive work unit.
