---
kind: fix-plan
feature_id: concurrent-same-branch-walkers-no-arbitration
status: Ready
created: 2026-06-20
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-20.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\concurrent-same-branch-walkers-no-arbitration\plans\fix-checkpoint-discriminated-same-pipeline-refusal.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — concurrent-same-branch-walkers-no-arbitration (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\concurrent-same-branch-walkers-no-arbitration\PHASES.md` (concurrent-same-branch-walkers-no-arbitration, 1 phase)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\concurrent-same-branch-walkers-no-arbitration\SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **DISPATCH-LIMITED EXECUTION OVERRIDE.** When this plan is executed by a `/lazy-bug-batch` (or `/lazy-bug`) cycle subagent that lacks the `Agent` tool, the orchestrator-vs-subagent split above is NOT achievable — perform ALL implementation, tests, and docs INLINE via `Read`/`Edit`/`Write`, test-first (write the failing fixture before the implementation edit). The "no inline Edit/Write" constraint is suspended ONLY in that dispatch-limited context; everything else in this plan (review discipline, ground-truth verification, quality gates, commit policy) still applies. Outside that context (a human or an Agent-capable session executing directly), honor the orchestrator+subagent split as written.

**Dispatch pattern (Agent-capable sessions only):** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against.

(none — this bug's SPEC carries no `**Depends on:**` block; it is a self-contained harness-script fix. The conceptual prior art — `docs/features/multi-repo-concurrent-runs/` — is NOT in this repo's `docs/features/` tree and is cited only descriptively in the SPEC; no upstream artifact read was possible or needed.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — EXCEPT under the DISPATCH-LIMITED EXECUTION OVERRIDE above (a `/lazy-bug*` cycle subagent with no `Agent` tool), where all work is performed INLINE, test-first.
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline — failing tests written BEFORE the implementation that makes them pass.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's (or inline) output is reviewed for correctness, spec alignment, and TDD discipline before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in a phase finish, integration verification confirms all changes work together.
8. Relevant CLAUDE.md files are updated after the phase (this plan explicitly schedules `user/scripts/CLAUDE.md` + root `CLAUDE.md` doc edits as WU deliverables).
9. The completed phase is committed and pushed before reporting done.
10. This plan is self-contained — follow it exactly as written without relying on external context.
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
12. **`refuse_run_start_clobber` is a SHARED `lazy_core` helper consumed by BOTH `lazy-state.py` (pipeline "feature") and `bug-state.py` (pipeline "bug") — this is a COUPLED PAIR.** Run the FULL coupled test set after the change: `lazy-state.py --test`, `bug-state.py --test`, `test_lazy_core.py`, and `lazy_parity_audit.py`. The fix lands on both pipelines for free; do NOT special-case one pipeline.
13. **The checkpoint read MUST be non-destructive (existence only).** NEVER call `consume_run_checkpoint()` from the refuse path — it deletes the `lazy-run-checkpoint.json` resume signal that the `--run-start` handler legitimately consumes LATER (~`lazy-state.py:7403`). Use `(claude_state_dir(create=False) / _CHECKPOINT_FILENAME).exists()`.

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | concurrent-same-branch-walkers-no-arbitration | P1 | Checkpoint-discriminated same-pipeline concurrent-walker refusal | — | Solo |

## Work Units

- [ ] WU-1 — Checkpoint-discriminated same-pipeline concurrent-walker refusal in `refuse_run_start_clobber` (impl + tests + docs)

---

### Phase: concurrent-same-branch-walkers-no-arbitration P1 — Checkpoint-discriminated same-pipeline concurrent-walker refusal

**Goal:** Make `refuse_run_start_clobber` REFUSE a same-pipeline `--run-start` when the existing marker is live + age-fresh AND no `lazy-run-checkpoint.json` is present (a genuinely-concurrent second walker), while still ALLOWING it when the checkpoint file IS present (a sanctioned resume).

**Entry criteria:** None (first and only phase). SPEC investigation is `Concluded`; PHASES.md is on disk.

**SPEC.md references:** `## Proven Findings` (root cause + the checkpoint discriminator), `## Affected Area` (the single fix site + the test matrix), `## Open Questions` (both points pre-resolved scope-class — refusal mechanism + checkpoint-file-existence discriminator).

#### Work Unit WU-1 — Checkpoint-discriminated same-pipeline refusal

**Scope (covers every PHASES.md Phase-1 deliverable):**
- Split the unconditional same-pipeline early return in `refuse_run_start_clobber` into a checkpoint-discriminated branch.
- Add the same-pipeline-concurrent refusal (exit 3, zero side effects, names the in-flight run).
- Correct the docstring.
- Preserve every existing allow path (missing/corrupt marker, age-stale, blank pipeline, cross-pipeline refusal unchanged).
- Add the four same-pipeline test fixtures + update the existing same-pipeline-resume fixture to seed a checkpoint.
- Update `user/scripts/CLAUDE.md` + root `CLAUDE.md` docs with the closed gap + reverse-reference.

This is ONE work unit: the impl change, its tests, and its doc edits are a single tightly-coupled fix concern (one function, one behavior, one shared helper). Splitting would produce a failing-test WU with no impl to satisfy it. TDD discipline is preserved WITHIN the WU (tests authored RED before the impl edit).

**TDD:** yes.

**Files to create/modify:**
- `user/scripts/lazy_core.py` — modify `refuse_run_start_clobber` (≈ line 8665; the same-pipeline early return at ≈ 8735–8736). [VERIFY: grep -n "def refuse_run_start_clobber" user/scripts/lazy_core.py] [VERIFY: grep -n "_CHECKPOINT_FILENAME" user/scripts/lazy_core.py]
- `user/scripts/test_lazy_core.py` — add 4 same-pipeline fixtures near the existing clobber suite (≈ line 17491); update `test_run_start_clobber_allows_same_pipeline_resume` to seed a checkpoint. [VERIFY: grep -n "_capture_clobber_refusal" user/scripts/test_lazy_core.py] [VERIFY: grep -n "def test_run_start_clobber_allows_same_pipeline_resume" user/scripts/test_lazy_core.py]
- `user/scripts/CLAUDE.md` — update "Same-repo refusal / cross-repo concurrency" prose + reverse-reference this bug. [VERIFY: grep -n "Same-repo refusal" user/scripts/CLAUDE.md]
- `CLAUDE.md` (repo root) — note the same-pipeline-concurrent refusal leg + reverse-reference this bug. [VERIFY: grep -n "refuse_run_start_clobber" CLAUDE.md]

**Test files (TDD):** `user/scripts/test_lazy_core.py` (extend in place — do NOT create a new test module; the clobber suite + `_capture_clobber_refusal` helper + `_set_state_dir`/`_clear_state_dir`/`write_run_marker`/`write_run_checkpoint` fixtures already live here). [VERIFY: grep -n "def write_run_checkpoint" user/scripts/lazy_core.py]

**Test expectations (write these RED first — they must FAIL against the current unconditional-allow code for the RIGHT reason, i.e. the same-pipeline-no-checkpoint case currently returns None instead of exiting 3):**

1. `test_run_start_clobber_refuses_same_pipeline_concurrent_no_checkpoint` — seed a LIVE same-pipeline marker (`write_run_marker(pipeline="feature", cloud=False, repo_root="/r", now=1_000_000.0)`), NO checkpoint file on disk, call `_capture_clobber_refusal("feature", now=1_000_010.0)`. ASSERT `code == 3`; ASSERT the marker file is byte-for-byte untouched (read before/after); ASSERT the stderr message names the in-flight run (contains the `started_at` value and/or `feature`). This is the core new behavior — currently returns `None` (defect).
2. `test_run_start_clobber_allows_same_pipeline_with_checkpoint_present` — seed a LIVE same-pipeline marker AND a checkpoint via `lazy_core.write_run_checkpoint("Step 7a: execute plan", {"forward_cycles": 3, "meta_cycles": 0, "max_cycles": 20}, now=1_000_000.0)`. Call `_capture_clobber_refusal("feature", now=1_000_010.0)`. ASSERT `code is None` (sanctioned resume preserved). ASSERT the checkpoint file STILL EXISTS afterward (the refuse path must NOT consume/delete it).
3. `test_run_start_clobber_allows_same_pipeline_age_stale` — seed a same-pipeline marker with `started_at` >24h before `now` (`now=1_000_000.0 + 90_000.0`), no checkpoint. ASSERT `code is None` (age-stale reclaim preserved — the age gate runs before the pipeline check, so this never reaches the new branch).
4. `test_run_start_clobber_cross_pipeline_unchanged_with_checkpoint` — regression guard: seed a LIVE *bug* marker + a checkpoint file, call `_capture_clobber_refusal("feature", ...)`. ASSERT `code == 3` (cross-pipeline still refuses — the new checkpoint discriminator must NOT leak into the cross-pipeline branch; a checkpoint present does not excuse a cross-pipeline clobber).
5. Update `test_run_start_clobber_allows_same_pipeline_resume` — it must now seed a checkpoint via `write_run_checkpoint(...)` before asserting `code is None`. Its name now means "resume WITH checkpoint present"; WITHOUT a checkpoint the same setup would (correctly) refuse per fixture 1. Add a one-line comment documenting why the checkpoint seed was added.

**Would-it-fail check:** fixture 1 must FAIL on the pre-fix code (current code returns None on the same-pipeline branch unconditionally) and PASS only after the discriminator lands. Fixtures 2–4 must pass on the post-fix code and (2 except the file-still-exists assert, 4) characterize behavior that must NOT regress.

**Implementation goal:** In `refuse_run_start_clobber`, after the age-staleness gate and the blank-pipeline fail-open, replace:
```python
if existing_pipeline == incoming_pipeline:
    return  # same-pipeline re-run-start (checkpoint resume) → allow overwrite
```
with a checkpoint-discriminated split:
- Compute `checkpoint_present = (claude_state_dir(create=False) / _CHECKPOINT_FILENAME).exists()` [VERIFY: grep -n "_CHECKPOINT_FILENAME" user/scripts/lazy_core.py] — existence ONLY, never `consume_run_checkpoint()` (see MANDATORY RULE 13).
- `if existing_pipeline == incoming_pipeline and checkpoint_present: return` — sanctioned resume, allow overwrite (current correct behavior).
- `if existing_pipeline == incoming_pipeline and not checkpoint_present:` — write a `sys.stderr` diagnostic naming the in-flight run (`started_at`, `forward_cycles` if present in the marker dict, `session_id`) and the operator instruction (a second autonomous walker is live on this same repo+branch+pipeline; STOP and do not start a second walker; if the prior run is genuinely dead, `--run-end` it from its own orchestrator first; this op was refused with ZERO side effects), then `sys.exit(3)`. Mirror the message shape + the zero-side-effects guarantee of the existing cross-pipeline refusal block directly below it. Realize the `multi-repo-concurrent-runs` "Same-repo second run" UX promise (name `started_at` / `forward_cycles`).
- The cross-pipeline refusal that follows stays EXACTLY as-is (no checkpoint check on that path — a checkpoint never excuses a cross-pipeline clobber).
- Correct the docstring: the same-pipeline branch is now checkpoint-discriminated; remove the "A SAME-pipeline re-`--run-start` … is ALLOWED to overwrite" unconditional-allow wording; document the checkpoint-file-absence discriminator and the non-destructive-read requirement.

**Spec requirements:** `## Proven Findings` — "The discriminator that closes the gap": checkpoint file present → allow; absent + live + age-fresh → REFUSE (exit 3, zero side effects). "The refuse function must read the checkpoint NON-destructively (existence check only — NOT `consume_run_checkpoint`)." `## Affected Area` row 1 (the single fix site, shared by both pipelines) + row 4 (the four test fixtures) + row 5 (docs). `## Open Questions` — refusal mechanism (resolved: refusal) + checkpoint-file existence discriminator (resolved).

**Docs deliverables (same WU — these are `CLAUDE.md` edits, allowed for the orchestrator/inline):**
- `user/scripts/CLAUDE.md` "Same-repo refusal / cross-repo concurrency" bullet currently says the refusal covers "a live, non-stale, DIFFERENT-pipeline marker." Update it to add: a live, non-stale, SAME-pipeline marker is ALSO refused now UNLESS a `lazy-run-checkpoint.json` is present (the sanctioned-resume discriminator) — closing the `multi-repo-concurrent-runs` same-pipeline-concurrent residual gap. Add a reverse-reference: `docs/bugs/concurrent-same-branch-walkers-no-arbitration`.
- Root `CLAUDE.md`: wherever the run-start clobber refusal is described, note the same-pipeline-concurrent leg (checkpoint-discriminated) and reverse-reference this bug. (If no such description exists in root `CLAUDE.md`, add a one-line note in the most relevant hooks/architecture location; do not invent a new section.)

**Batch:** 1 (solo — single WU).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | None |

## Execution Protocol

This protocol governs the autonomous execution of the phase. Follow it exactly.

### Phase Selection Loop

Repeat until the single phase is complete or a blocking issue triggers early exit:

1. **Select ready phase:** P1 is ready immediately (no entry criteria).
2. **Announce:** Print "Implementing concurrent-same-branch-walkers-no-arbitration Phase 1: Checkpoint-discriminated same-pipeline concurrent-walker refusal".
3. **Review prior context:** none (first phase).
4. **Execute Batch 1** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "concurrent-same-branch-walkers-no-arbitration Phase 1 — committed as [hash]".
7. **Loop:** no further phases — proceed to Completion.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly. (Under the dispatch-limited override, task tools may be unavailable — if `ToolSearch` cannot load them after two attempts, proceed without them, tracking progress via the `- [ ] WU-N` checkbox in this file.)

### Per-Batch Steps

For Batch 1:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow it. Re-read from disk: this plan, the PHASES.md, the SPEC.md `## Proven Findings` + `## Affected Area`, and the real `refuse_run_start_clobber` body in `user/scripts/lazy_core.py` (do NOT work from memory of its current shape).

#### Step B.1: Implement WU-1 (test-first)

Read (Agent-capable sessions): `tdd-protocol.md`, `subagent-launch.md`, `tdd-test-agent.md`, `implementation-agent.md`, then dispatch one Sonnet test agent and one Sonnet impl agent per those briefings.

Under the DISPATCH-LIMITED EXECUTION OVERRIDE (no `Agent` tool): perform inline, test-first —
1. Write the 5 fixtures (4 new + 1 updated) into `user/scripts/test_lazy_core.py`. Run `python user/scripts/test_lazy_core.py` (or the targeted runner) and CONFIRM fixture 1 FAILS for the right reason (same-pipeline-no-checkpoint currently returns None, expected exit 3) — capture the RED output.
2. Implement the `refuse_run_start_clobber` change in `user/scripts/lazy_core.py`.
3. Re-run the tests; all 5 fixtures GREEN.
4. Apply the two `CLAUDE.md` doc edits.

#### Step B.2: Review Batch Output (MANDATORY GATE)

Read `~/.claude/skills/_components/subagent-review.md` + `mount-site-verification.md` and follow the protocol (inline review is fine — this batch is < 2 files of source change + 1 test file + 2 doc files; measure scope with `git diff --stat` first and print the mandated message). Verify: the checkpoint read is existence-only (grep the diff for `consume_run_checkpoint` — it MUST NOT appear in the refuse path); the cross-pipeline branch is unchanged; fixture 1 genuinely went RED→GREEN.

#### Step B.3: Update PHASES.md (MANDATORY)

Read `~/.claude/skills/_components/phases-update.md`. Tick every Phase-1 deliverable checkbox in PHASES.md, add an `#### Implementation Notes` block (date 2026-06-20, work completed, the review verdict line, files modified, the non-destructive-read invariant as a pitfall note). Tick `- [ ] WU-1` → `- [x] WU-1` in THIS plan file. Set this plan's frontmatter `status:` to `Complete` once WU-1 lands (that flip is `/execute-plan`'s, i.e. this executing session's, to make — NOT a top-level SPEC/PHASES status flip, which is gate-owned).

#### Step B.4: Run Quality Gates (MANDATORY — FULL COUPLED SET)

Read `~/.claude/skills/_components/quality-gates.md`. Because `refuse_run_start_clobber` is a SHARED coupled helper, run ALL FOUR:
```
python user/scripts/test_lazy_core.py
python user/scripts/lazy-state.py --test
python user/scripts/bug-state.py --test
python user/scripts/lazy_parity_audit.py
```
Also run the skill lint if docs/skills were touched is N/A here (no skill edits) — but the two `--test` suites are byte-pinned against `tests/baselines/`; if a baseline diff appears, confirm it reflects ONLY the intended new fixture output and regenerate the baseline via the documented `_normalize_smoke_output` path (NEVER by hand). 100% green required before commit.

#### Step B.5: Commit Batch

Read commit policy: try `.claude/skill-config/commit-policy.md` first; fallback `~/.claude/skills/_components/commit-and-push.md`. Commit to `main` (work branch). Use the R5 atomic gate+commit shape: run the full coupled test set AND the commit AND the push as one chained command so an interrupted turn still leaves committed+pushed state.

#### Step B.6: Proceed

Single batch — proceed to Post-Phase Steps.

### Post-Phase Steps

#### Integration Verification (MANDATORY)

Read `~/.claude/skills/_components/integration-verification.md`. Confirm the fix lands on BOTH pipelines: `lazy_parity_audit.py` green is the mechanical proof that `bug-state.py` inherits the shared helper. Spot-check that `lazy-state.py --run-start` and `bug-state.py --run-start` both still call `refuse_run_start_clobber` (no signature drift broke a caller).

#### Update CLAUDE.md Files (MANDATORY)

Already covered by WU-1's doc deliverables (`user/scripts/CLAUDE.md` + root `CLAUDE.md`). Read `~/.claude/skills/_components/claude-md-review.md` and confirm no OTHER CLAUDE.md needs a note.

#### Commit and Push Post-Phase Changes

Commit any residual doc/PHASES updates and push to `main`.

## Blocking Issue Protocol

If a blocking issue is encountered:

1. **Stop all in-progress work.**
2. **Commit and push** any completed work.
3. **Print a blocking-issue report** (completed work, blocked WU, reason, recovery suggestion, remaining work).
4. **Do not work around the blocker.**

Blocking issues include: a `--test`/`lazy_parity_audit.py` failure unfixable after 2 retries; a baseline drift that is NOT explained by the intended new fixture; a git push conflict unresolvable by rebase; any requirement for an architectural decision beyond this SPEC's scope.

## Completion

When the phase is complete:

1. **Run the full coupled gate suite one final time** (`test_lazy_core.py`, `lazy-state.py --test`, `bug-state.py --test`, `lazy_parity_audit.py`) — all green.
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Feature fixed:** concurrent-same-branch-walkers-no-arbitration
   **Total phases completed:** 1
   **Total commits:** [M]
   **Final quality-gate status:** all green (coupled set)

   **Commit log:**
   | Commit | Feature | Phase | Title |
   |--------|---------|-------|-------|
   | [hash] | concurrent-same-branch-walkers-no-arbitration | P1 | Checkpoint-discriminated same-pipeline concurrent-walker refusal |

   **Implementation Notes summary:** the same-pipeline `--run-start` clobber is now checkpoint-discriminated; the resume signal (`lazy-run-checkpoint.json`) is read non-destructively (existence only); the fix lands on both pipelines via the shared `lazy_core` helper.

> **NOTE on terminal status:** This plan's execution flips ITS OWN frontmatter `status:` to `Complete` and ticks PHASES.md deliverables. It does NOT flip the top-level SPEC.md / PHASES.md `**Status:**` to `Fixed` and does NOT write `FIXED.md` — those are owned EXCLUSIVELY by the orchestrator's `__mark_fixed__` gate, which fires only after the validation tail. When the last phase's work lands, set the top-level PHASES `**Status:**` to `In-progress` (implementation done, validation pending) and let the state machine route forward.
