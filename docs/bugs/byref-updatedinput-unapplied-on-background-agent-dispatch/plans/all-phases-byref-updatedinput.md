---
kind: implementation-plan
feature_id: byref-updatedinput-unapplied-on-background-agent-dispatch
status: In-progress
created: 2026-07-19
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan docs/bugs/byref-updatedinput-unapplied-on-background-agent-dispatch/plans/all-phases-byref-updatedinput.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — byref-updatedinput-unapplied-on-background-agent-dispatch (v2)

**PHASES.md files:**
- `docs/bugs/byref-updatedinput-unapplied-on-background-agent-dispatch/PHASES.md` (2 phases)

**SPEC.md files:**
- `docs/bugs/byref-updatedinput-unapplied-on-background-agent-dispatch/SPEC.md`

**Total phases:** 2
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the **Plan-specific execution notes** below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config — NON-default gates)

This is the **claude-config** repo, NOT AlgoBooth/Cognito. There is no `/msbuild`/`/mstest`/`/nxbuild` and no MCP runtime. The quality gates for every batch are:

```bash
python3 user/scripts/lazy-state.py --test
python3 user/scripts/bug-state.py --test
python3 -m pytest user/scripts/tests/test_lazy_core/ -q
python3 user/scripts/lazy_parity_audit.py --repo-root .
python3 user/scripts/lint-skills.py
python3 user/scripts/doc-drift-lint.py --repo-root .
python3 user/scripts/project-skills.py
```

Or run the repo's declared invariant battery in one shot and trust its last-line banner:

```bash
python3 user/scripts/gate-battery.py --repo-root .   # RESULT=PASS|FAIL banner is authoritative
```

Each of these completes well under the Bash cap; run them **foreground** (never background a gate from inside a cycle — the process tree dies at turn end). After editing either state script or `lazy_core`, the parity audit + both `--test` harnesses are the load-bearing regression net; after editing any SKILL.md, re-run `project-skills.py` + `lint-skills.py`. Regenerate a byte-pinned `--test` baseline ONLY by piping live `--test` output through `tests/test_lazy_core/_util.py::_normalize_smoke_output` — never by hand.

## References — Upstream Artifacts

(none — this bug has no completed hard upstream dependencies. Fix scope is operator-locked in `SPEC.md` → "Fix scope — RESOLVED", `NEEDS_INPUT_RESOLVED_2026-07-18.md` → "## Resolution (final — fix fork)", and `PLATFORM_CONFIRMATION.md`.)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | byref-updatedinput | P1 | Consumed-nonce reader (WU-1) | — | Solo |
| 2 | byref-updatedinput | P1 | `--resolve-ref` CLI both scripts + parity (WU-2) | WU-1 | Solo |
| 3 | byref-updatedinput | P2 | Self-resolving dispatch template (WU-3) | WU-2 | Solo |
| 4 | byref-updatedinput | P2 | Coupled-skill prose (WU-4) | WU-3 | Solo |

All four WUs serialize: WU-1→WU-2→WU-3 share `dispatch.py` / the state scripts (file overlap + dependency); WU-4 documents the mechanism WU-3 builds. No two run in the same batch, so there is no in-batch file conflict.

## Work Units

- [x] WU-1 — Consumed-nonce reader in `lazy_core.dispatch`
- [x] WU-2 — `--resolve-ref` CLI on both state scripts + parity registration
- [ ] WU-3 — Self-resolving `@@lazy-ref` dispatch-template wrapper
- [ ] WU-4 — Coupled dispatch-skill prose (by-reference stays PREFERRED)

---

### Phase: byref-updatedinput P1 — Consumed-nonce resolver + `--resolve-ref` CLI

**Goal:** Provide the sanctioned read that returns a consumed nonce's registered prompt bytes, exposed as `--resolve-ref` on both state scripts (parity).
**Entry criteria:** None (first phase).
**SPEC.md references:** "Fix scope — RESOLVED" items 1 (consumed-nonce read) — the mechanism a subagent uses after the platform drops the `updatedInput` rewrite.

#### WU-1 — Consumed-nonce reader in `lazy_core.dispatch`

- **Scope:** PHASES P1 deliverable 1 — new consumed-tolerant reader returning `prompt_raw` for a consumed + TTL-fresh + run-start-gated registry entry.
- **TDD:** yes
- **Files to create/modify:**
  - `user/scripts/lazy_core/dispatch.py` — add `resolve_consumed_emission_by_nonce(nonce, *, now=None)` beside `resolve_emission_by_nonce` [VERIFY: grep -n "def resolve_emission_by_nonce" user/scripts/lazy_core/dispatch.py] (returns the entry's `prompt_raw`, fallback `prompt_norm` [VERIFY: grep -n "\"prompt_raw\"" user/scripts/lazy_core/dispatch.py]).
- **Test files:** `user/scripts/tests/test_lazy_core/test_dispatch.py` [VERIFY: test -f user/scripts/tests/test_lazy_core/test_dispatch.py]
- **Test expectations:** RED first. Register an emission, consume its nonce (`consume_nonce` / mark consumed), assert the new reader RETURNS the exact stored `prompt_raw`. Assert it returns `None` for: unknown nonce, an entry beyond `REGISTRY_ENTRY_TTL_SECONDS`, and an entry predating the run marker's `started_at`. Assert it NEVER mutates `consumed` (read the registry back and confirm the flag/`consumed_by` are unchanged).
- **Implementation goal:** REUSE the TTL + run-start gate logic pattern from `resolve_emission_by_nonce` (do not duplicate the datetime math beyond what parity needs); INVERT only Gate-1 (require `consumed` truthy instead of falsy). READ-ONLY, fail-safe (any error → `None`). **Do NOT modify `resolve_emission_by_nonce`** — its consumed-filter (Gate-1) is load-bearing for the guard's dispatch path.
- **Spec requirements:** SPEC "Fix scope — RESOLVED" item 1: "a nonce the guard already ALLOW+consumed this run; read-only, run-scoped, never un-consumes… the existing `resolve_emission_by_nonce` filters consumed entries, so this is a distinct sanctioned read surface."
- **Batch:** 1

#### WU-2 — `--resolve-ref` CLI on both state scripts + parity registration

- **Scope:** PHASES P1 deliverables 2, 3, 4 — `--resolve-ref <nonce>` action on `lazy-state.py` and `bug-state.py`; register the coupled CLI surface in the parity manifest.
- **TDD:** yes
- **Files to create/modify:**
  - `user/scripts/lazy-state.py` — add `--resolve-ref <nonce>` to `build_parser()` + a handler that calls WU-1's reader and prints the bytes to stdout (exit 0 hit / exit 1 miss). NOT gated by `refuse_if_cycle_active` (a read a subagent must be able to run).
  - `user/scripts/bug-state.py` — identical `--resolve-ref <nonce>` action (coupled-pair mirror; nonce is the key, so the `--feature-id`/`--bug-id` divergence does not apply) [VERIFY: grep -n "@@lazy-ref nonce=" user/scripts/bug-state.py].
  - `user/scripts/lazy-parity-manifest.json` — register `--resolve-ref` so `audit_state_script_parity` [VERIFY: grep -n "def audit_state_script_parity" user/scripts/lazy_parity_audit.py] asserts both scripts expose it.
- **Test files:** each state script's in-file `--test` harness (add `def test_resolve_ref_*`); `tests/baselines/{lazy-state,bug-state}-test-baseline.txt` (regenerate).
- **Test expectations:** RED first. A fixture registers+consumes a nonce, invokes the `--resolve-ref` action, and asserts stdout == the registered bytes + exit 0; an unknown nonce asserts empty stdout + exit 1. Register the parity row and assert `lazy_parity_audit.py --repo-root .` is green (exit 0) after.
- **Implementation goal:** One shared behavior across both scripts (delegate to WU-1's `lazy_core` reader — no per-script reimplementation). Regenerate the byte-pinned baselines ONLY via `_normalize_smoke_output`.
- **Spec requirements:** SPEC item 1 — "`--resolve-ref <nonce>`… coupled/parity-audited."
- **Batch:** 2

---

### Phase: byref-updatedinput P2 — By-reference delivery contract

**Goal:** Make by-reference dispatch self-delivering — the emitted `@@lazy-ref` surface instructs the subagent to resolve its instructions first; coupled skills document the mechanism.
**Entry criteria:** Phase 1 complete (`--resolve-ref` exists so the emitted instruction points at a real command).
**SPEC.md references:** "Fix scope — RESOLVED" items 2 (contractual first step), 3 (coupled-skill prose), 4 (regression incl. zero-tool-use near-miss).

#### WU-3 — Self-resolving `@@lazy-ref` dispatch-template wrapper

- **Scope:** PHASES P2 deliverable 1 — a shared `lazy_core` helper wraps the emitted `@@lazy-ref nonce=<hex>` token with the first-step resolve instruction, consumed by both emit handlers.
- **TDD:** yes
- **Files to create/modify:**
  - `user/scripts/lazy_core/dispatch.py` — add a single ref-surface-wrapper helper (the sole author of the emitted ref-surface string) that returns `@@lazy-ref nonce=<hex>` + a runnable first-step instruction ("your full instructions are registered under this nonce; FIRST run `python3 ~/.claude/scripts/<state-script>.py --repo-root <root> --resolve-ref <hex>` and follow the returned prompt before anything else").
  - `user/scripts/lazy-state.py` — the `@@lazy-ref nonce=` emit twins [VERIFY: grep -n "@@lazy-ref nonce=" user/scripts/lazy-state.py] consume the wrapper so `cycle_prompt_ref`/`dispatch_prompt_ref` carry the instruction.
  - `user/scripts/bug-state.py` — same emit sites [VERIFY: grep -n "@@lazy-ref nonce=" user/scripts/bug-state.py] consume the wrapper (parity; the wrapper names the correct per-pipeline script).
- **Test files:** `user/scripts/tests/test_lazy_core/test_dispatch.py` (+ in-file `--test` fixtures where the emit path is exercised).
- **Test expectations:** RED first. Assert the emitted ref surface for BOTH pipelines contains the `@@lazy-ref nonce=` token AND a runnable `--resolve-ref` instruction naming the correct script. The zero-tool-use dead-return near-miss guard: a bare token surface with NO resolve instruction must FAIL the test (this is the failure mode being fixed).
- **Implementation goal:** Single-author the ref-surface string in `lazy_core` so both scripts stay parity; keep the raw `@@lazy-ref nonce=` token substring intact (existing consumers/tests that match it must still match).
- **Spec requirements:** SPEC item 2 — "emitted dispatch templates gain a contractual FIRST STEP for by-ref dispatches… never zero tool-uses / 'no task attached'."
- **Batch:** 3

#### WU-4 — Coupled dispatch-skill prose (by-reference stays PREFERRED)

- **Scope:** PHASES P2 deliverables 2–4 (prose) — update the three coupled dispatch skills so by-reference stays PREFERRED with the new delivery mechanism, verbatim is the documented fallback, and the subagent-side resolve-first contract is stated.
- **TDD:** no (documentation / contract prose)
- **Files to create/modify:**
  - `user/skills/lazy-batch/SKILL.md` — §1d by-reference dispatch prose.
  - `user/skills/lazy-bug-batch/SKILL.md` — coupled-pair mirror.
  - `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` — coupled-pair mirror (per the CLAUDE.md coupled-pairs table).
- **Implementation goal:** Mirror the same by-reference-stays-PREFERRED + verbatim-fallback + subagent-resolves-first wording across all three, keeping the coupled prose in lockstep. After editing, re-run `python3 user/scripts/project-skills.py` and `python3 user/scripts/lint-skills.py`; confirm `lazy_parity_audit.py --repo-root .` stays green.
- **Spec requirements:** SPEC item 3 — "by-reference stays PREFERRED with the new delivery mechanism; verbatim remains the documented fallback."
- **Batch:** 4

---

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | None |
| 2 | WU-2 | Solo | None (depends on WU-1) |
| 3 | WU-3 | Solo | None (depends on WU-2; shares dispatch.py + state scripts) |
| 4 | WU-4 | Solo | None (docs; depends on WU-3) |

## Completion (gate-owned)

The `__mark_fixed__` gate flips `SPEC.md` **Status:** to Fixed and writes `FIXED.md` once this plan's work lands and the validation tail passes — do NOT author a status-flip or receipt-write work unit. When Phase 2's work lands, set the top-level PHASES.md `**Status:**` to `In-progress` (implementation done, validation pending), never Fixed.
