---
kind: fix-plan
feature_id: build-queue-no-artifact-or-process-hygiene-on-crash
status: Complete
created: 2026-06-30
complexity: complex
phases: [3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-30.
> To execute: `/execute-plan <this-path>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 3. Sibling parts:
> - part 1: `docs/bugs/build-queue-no-artifact-or-process-hygiene-on-crash/plans/fix-hygiene-part-1.md`
> - part 2: `docs/bugs/build-queue-no-artifact-or-process-hygiene-on-crash/plans/fix-hygiene-part-2.md` (this file)
> - part 3: `docs/bugs/build-queue-no-artifact-or-process-hygiene-on-crash/plans/fix-hygiene-part-3.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — build-queue artifact/process hygiene (Part 2: Phase 3)  (v2)

**PHASES.md files:**
- `docs/bugs/build-queue-no-artifact-or-process-hygiene-on-crash/PHASES.md` (5 phases; this part covers Phase 3)

**SPEC.md files:**
- `docs/bugs/build-queue-no-artifact-or-process-hygiene-on-crash/SPEC.md`

**Total phases (this part):** 1 of 5
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL, the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (the **claude-config** harness repo — PowerShell-parse + Pester + manual operator runtime, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the **Plan-specific execution notes** below override the contract's defaults.

## Plan-specific execution notes (claude-config gate overrides)

Same gate substitutions as Part 1 (claude-config has no `/msbuild`/`/mstest`):

1. **PowerShell parse gate (every edited `*.ps1`):**
   ```
   powershell.exe -NoProfile -Command "$t=$null;$e=$null;[void][System.Management.Automation.PSParser]::Tokenize((Get-Content -Raw '<abs-path.ps1>'),[ref]$e);if($e){$e|ForEach-Object{Write-Error $_.Message};exit 1}else{'parse-ok'}"
   ```
2. **Pester smoke** — `Remove-PoisonedArtifacts` is **pure file-I/O and IS unit-testable in isolation** (this is the one genuinely TDD-able WU in the whole fix). Author and run its Pester test against a temp dir.
3. **Commit + `git push origin main`** after the phase (personal repo).
4. **`__mark_fixed__` is gate-owned** — author no status-flip/receipt/ROADMAP/archive WU.

**Prerequisite from Part 1 (assumed Complete before this part runs):** `build-queue-runner.ps1` already (a) dot-sources `build-queue-hygiene.ps1`, (b) owns a single `try/finally` build bracket with the Job-Object reap, (c) accepts a `-Worktree` parameter (stubbed in Part-1 WU-2), and (d) writes a `hygiene` sub-object on the result body carrying `vbcscompiler_recycled`. This part hooks the quarantine sweep into that SAME `finally` and extends the SAME `hygiene` sub-object — do NOT create a new `finally` or a second result-body write.

## References — Upstream Artifacts

(none — SPEC `**Depends on:** (none)`.)

## Touchpoint Audit (verified — first-hand reads this session)

| Planned file | Exists? | Real symbols (verified) | Action |
|--------------|---------|-------------------------|--------|
| `user/scripts/build-queue-hygiene.ps1` | yes (created Part 1) | `New-BuildJobObject`, `Add-ProcessToBuildJob`, `Stop-BuildJobTree`, `Reset-CompilerServer` | add `Remove-PoisonedArtifacts` |
| `user/scripts/build-queue-runner.ps1` | yes | `try/finally` build bracket + `hygiene` result sub-object (Part 1); `-Worktree` param (Part 1) | refactor — call sweep in `finally` on non-zero/abort |
| `user/scripts/build-queue.ps1` | yes | `$worktree` (`:72-75`), runner launch `$procArgList`/`Start-Process` (`:280-292`) | refactor — pass `-Worktree $worktree` to runner |
| `user/scripts/build-queue-hygiene.Tests.ps1` | yes (created Part 1) | Pester smoke | extend — `Remove-PoisonedArtifacts` unit test |

## Execution Schedule

| Step | Phase | Work Units | Title | Blocked by | Parallel? |
|------|-------|-----------|-------|------------|-----------|
| 1 | P3 | WU-1 | `Remove-PoisonedArtifacts` sweep + Pester unit test | — (Part 1 module exists) | Solo |
| 2 | P3 | WU-2 | Runner: call sweep in `finally` on non-zero/abort + `quarantined_artifacts` | WU-1 | Solo (runner.ps1) |
| 3 | P3 | WU-3 | Wrapper passes `-Worktree $worktree` to runner launch | WU-2 | Solo (build-queue.ps1) |

Sequential, single-WU batches (all serialize on the runner's `finally` + result body, or feed the `-Worktree` thread).

## Work Units

- [x] WU-1 — `Remove-PoisonedArtifacts` sweep + Pester unit test
- [x] WU-2 — Runner: call sweep in `finally` on non-zero/abort + `quarantined_artifacts`
- [x] WU-3 — Wrapper passes `-Worktree $worktree` to runner launch

---

### Phase: P3 — Artifact validation / quarantine (0-byte + truncated `*.dll`, `bin/` and `obj/`)

**Goal:** On a non-zero or aborted build exit, sweep the worktree's `bin/` **and `obj/`** for 0-byte and truncated (invalid-PE) `*.dll` and delete them, so MSBuild's timestamp-based incremental up-to-date check cannot treat a poisoned artifact as current.
**Entry criteria:** Part 1 complete — the runner's `try/finally` bracket, the `-Worktree` param, and the `hygiene` result sub-object all exist.
**SPEC.md references:** Verified Symptom 1 (0-byte DLL in BOTH `bin/Debug` and `obj/Debug`; "CoreCompile is being skipped because all output files are up-to-date"); Locked Decision 3 (targeted 0-byte/truncated sweep over `bin/` and `obj/`, not a blanket force-clean); Open Question 2 (cheap PE-validity check — resolved in WU-1 below).

#### WU-1 — `Remove-PoisonedArtifacts` sweep + Pester unit test

- **Scope (PHASES P3 deliverables 1, 5):**
  - [x] Add `Remove-PoisonedArtifacts -WorktreeRoot <path>` to `build-queue-hygiene.ps1`: enumerate `*.dll` under `bin/` **and** `obj/` (recursively); delete any that are **0-byte** OR fail a cheap PE-validity probe; return the list of quarantined absolute paths. Fail-open per-file (a delete failure on one file logs and continues; never aborts the sweep).
  - [x] **Resolve SPEC Open Question 2 (PE-validity check):** use a **two-tier cheap probe** — (a) 0-byte ⇒ poisoned; (b) nonzero-but-truncated ⇒ read the first 2 bytes and require the `MZ` DOS-header magic (`0x4D 0x5A`); a `*.dll` lacking `MZ` is not a valid PE image (covers the `CS0009 "PE image doesn't contain managed metadata"` truncation). Do NOT attempt full PE/CLI-header parsing — the `MZ`-magic + 0-byte pair is the agreed cheap check.
  - [x] Pester smoke (`build-queue-hygiene.Tests.ps1`) against a temp dir seeded with: a 0-byte DLL, a truncated DLL (a few bytes, no `MZ`), and a valid DLL (starts with `MZ`, nonzero) — assert ONLY the first two are removed and the valid one survives.
- **TDD:** **yes** — pure file-I/O; the temp-dir Pester test drives the function directly. Write the test first (RED), then the function (GREEN).
- **Files to create/modify:**
  - `user/scripts/build-queue-hygiene.ps1` — add `Remove-PoisonedArtifacts`.
  - `user/scripts/build-queue-hygiene.Tests.ps1` — add the sweep unit test.
- **Test files:** `user/scripts/build-queue-hygiene.Tests.ps1`.
- **Test expectations (Pester):**
  - Seed a `TestDrive:`/temp worktree with `bin/Debug/x.dll` (0 bytes), `obj/Debug/y.dll` (3 bytes, content `0x00 0x01 0x02` — no `MZ`), and `bin/Debug/good.dll` (starts with `MZ` + padding, nonzero).
  - After `Remove-PoisonedArtifacts -WorktreeRoot <temp>`: the 0-byte and truncated DLLs are gone; `good.dll` exists.
  - The returned list contains exactly the two quarantined paths.
  - A seeded read-only/locked file (simulated delete failure) does NOT throw — the sweep completes and the surviving files are still classified correctly (fail-open per-file).
- **Implementation goal:** A pure, unit-testable quarantine sweep owned by the hygiene module. It MUST cover the `obj/` tree as well as `bin/` — Verified Symptom 1 required deleting the 0-byte DLL from BOTH `Cognito\bin\Debug\netstandard2.0\Cognito.dll` AND `Cognito\obj\Debug\netstandard2.0\Cognito.dll`. Enumerate with `Get-ChildItem -Recurse -Filter *.dll` rooted at `Join-Path $WorktreeRoot 'bin'` and `Join-Path $WorktreeRoot 'obj'` (skip a root that does not exist — a fresh worktree may lack `obj/`). Read the first 2 bytes with `[System.IO.File]::OpenRead` + a 2-byte read (cheap; no full file load). Reuse the module's fail-open `Get-SafeValue` idiom for per-file deletes.
- **Spec requirements:** Locked Decision 3; Verified Symptom 1; Open Question 2 (resolved: 0-byte OR missing `MZ` magic).
- **Batch:** 1 (solo — edits hygiene.ps1 + its test; the Part-1 module already exists).

#### WU-2 — Runner: call sweep in `finally` on non-zero/abort + `quarantined_artifacts`

- **Scope (PHASES P3 deliverables 2, 4):**
  - [x] In the runner's existing `finally` bracket (Part 1), call `Remove-PoisonedArtifacts -WorktreeRoot $Worktree` **ONLY** when the build exit was non-zero OR the runner was aborted (a clean exit-0 leaves artifacts untouched). The sweep runs after the Job reap and the recycle, using the `-Worktree` value threaded from the wrapper (WU-3).
  - [x] Extend the result body's `hygiene` sub-object with `quarantined_artifacts: [<paths>]` (the list returned by the sweep; empty array on a clean exit / no poison).
- **TDD:** no (the runner wiring is process-orchestration; the sweep logic itself is unit-tested in WU-1). Parse gate + manual runtime (PHASES Runtime Verification).
- **Files to create/modify:**
  - `user/scripts/build-queue-runner.ps1` — conditional sweep call in `finally`; add `quarantined_artifacts` to the `hygiene` sub-object.
- **Test files:** none (sweep covered by WU-1).
- **Test expectations:** N/A (parse gate + PHASES Runtime Verification rows).
- **Implementation goal:** Gate the sweep on `non-zero OR abort` inside the SAME `finally` Part 1 established — a clean build must NOT lose its valid artifacts. The exit code the runner now awaits (from Part-1 WU-2's `Start-Process` object) is the gate; an abort/trap path treats the build as failed and sweeps. Append `quarantined_artifacts` to the existing `hygiene` sub-object on the result body literal [VERIFY: grep -n "exit_code = " user/scripts/build-queue-runner.ps1] (the body Part-1 WU-3 extended at runner.ps1:52-56) — do NOT add a second result write. `-Worktree` is the runner param stubbed in Part-1 WU-2 [VERIFY: grep -n "Worktree" user/scripts/build-queue-runner.ps1].
- **Spec requirements:** Locked Decision 3 ("sweep runs … ONLY on non-zero/aborted exit; a clean exit leaves artifacts untouched"); Record-the-hygiene-outcome fix direction (`quarantined_artifacts`).
- **Batch:** 2 (solo — edits runner.ps1; depends on WU-1's sweep function).

#### WU-3 — Wrapper passes `-Worktree $worktree` to runner launch

- **Scope (PHASES P3 deliverable 3):**
  - [x] `build-queue.ps1` passes `-Worktree $worktree` (already computed at `:72-75`) into the detached runner invocation (the `$procArgList` built at `:280-285`), so the runner's sweep (WU-2) knows which worktree to clean.
- **TDD:** no (argument threading; manual runtime). Parse gate is the mechanical check.
- **Files to create/modify:**
  - `user/scripts/build-queue.ps1` — add `-Worktree`, `(Format-ProcArg $worktree)` to the runner arg list.
- **Test files:** none.
- **Test expectations:** N/A (parse gate; PHASES Runtime Verification confirms the end-to-end "next build recompiles cleanly").
- **Implementation goal:** Thread the wrapper's already-resolved worktree path to the runner. The wrapper computes `$worktree` from `git rev-parse --show-toplevel` [VERIFY: grep -n "\$worktree = Get-SafeValue" user/scripts/build-queue.ps1] (build-queue.ps1:72-75) and builds the detached runner's argument list with the existing `Format-ProcArg` quoting helper [VERIFY: grep -n "Start-Process -FilePath" user/scripts/build-queue.ps1] (the launch at build-queue.ps1:287). Add `'-Worktree', (Format-ProcArg $worktree)` to `$procArgList` (built at build-queue.ps1:280-285) so the value survives the space-quoting that the worktree path (e.g. `C:\Users\…\Cognito Forms-B`) requires. The runner already declares the `-Worktree` param (Part-1 WU-2), so this is pure argument threading.
- **Spec requirements:** PHASES P3 deliverable 3; Locked Decision 3 (the sweep targets the build's own worktree).
- **Batch:** 3 (solo — edits build-queue.ps1; depends on WU-2 consuming the param).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | None (hygiene.ps1 + test; module from Part 1) |
| 2 | WU-2 | Solo | None (runner.ps1; needs WU-1) |
| 3 | WU-3 | Solo | None (build-queue.ps1; needs WU-2) |

---

## Plan-specific execution note (recap for the executor)

- WU-1 is genuinely TDD — write the Pester sweep test first, watch it fail, then implement `Remove-PoisonedArtifacts`.
- After the phase: parse-gate every edited `*.ps1`, run Pester, commit, `git push origin main`.
- The end-to-end "crashed build leaves a 0-byte DLL → next build recompiles cleanly (no `CS0009`/`CS0234`)" and "clean exit-0 build does NOT trigger the sweep" are the PHASES.md **Runtime Verification** rows — operator-owned manual runs, NOT `/execute-plan` WUs.
- When this part's 3 WUs are all `- [x]` and the gates are green, STOP — the orchestrator advances to Part 3 (`fix-hygiene-part-3.md`).
