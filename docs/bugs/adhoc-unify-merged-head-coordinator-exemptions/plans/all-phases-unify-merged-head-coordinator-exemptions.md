---
kind: implementation-plan
feature_id: adhoc-unify-merged-head-coordinator-exemptions
status: Complete
created: 2026-07-19
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan docs/bugs/adhoc-unify-merged-head-coordinator-exemptions/plans/all-phases-unify-merged-head-coordinator-exemptions.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — Unify merged-head coordinator-emission exemptions  (v2)

**PHASES.md files:**
- `docs/bugs/adhoc-unify-merged-head-coordinator-exemptions/PHASES.md` (unify-merged-head-coordinator-exemptions, 2 phases)

**SPEC.md files:**
- `docs/bugs/adhoc-unify-merged-head-coordinator-exemptions/SPEC.md`

**Total phases:** 2
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the local `## Component Reference Card` / Plan-specific execution notes override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

### Plan-specific execution notes (claude-config — Python harness repo)

- **No compiled app / no Tauri / no MCP server.** "Validation" is this repo's own Python test + lint suite. The `/mcp-test` Step-9 gate is N/A here (see `.claude/skill-config/quality-gates.md` → "MCP exemption").
- **Quality gates for THIS plan** (Python-script + lazy-family changes — `.claude/skill-config/quality-gates.md`), each an independent under-cap command — run them in the FOREGROUND, never backgrounded from a cycle subagent:
  1. `python -m pytest user/scripts/tests/test_lazy_core/test_dispatch.py -q` — the new-predicate unit tests (fast; the primary per-WU gate).
  2. `python user/scripts/lazy-state.py --test` — feature state-script baseline; MUST stay byte-identical to `user/scripts/tests/baselines/lazy-state-test-baseline.txt`.
  3. `python user/scripts/bug-state.py --test` — bug state-script baseline; MUST stay byte-identical to `user/scripts/tests/baselines/bug-state-test-baseline.txt`.
  4. `python user/scripts/lazy_parity_audit.py --report` — coupled-pair drift check; MUST exit 0 (the two callers stay byte-parallel, or a new divergence is registered in `user/scripts/lazy-parity-manifest.json`).
  5. Before flipping the final phase: the FULL script suite `python -m pytest user/scripts/ -q` (catches any collateral in `lazy_core`/callers).
- **Behavior-preservation is the correctness contract.** This is a pure refactor: gates 2 and 3 (byte-identical baselines) are the load-bearing proof that `--emit-prompt`/`--probe` output did not change. A moved baseline = the refactor changed behavior and MUST be corrected until the baseline matches the committed net exactly. Do NOT regenerate/overwrite a baseline to make a gate pass.
- **Commit policy** (`.claude/skill-config/commit-policy.md`): Conventional Commits, no AI attribution, **push after every commit** (claude-config keeps `origin/main` in sync — the work-repo "commits stay local" rule does NOT apply here). Stage only this plan's own changed paths (pathspec-scoped), never a blanket `git add -A` under a shared worktree.

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against.

(none — this plan has no completed hard upstream dependencies. Related prior art — `parallel-worktree-batch-execution` / `lazy-batch-parallel-run-harness-gaps` rounds 1 & 2, `merged-head-actionability-oracle`, `docs/bugs/dispatch-probe-and-inject-bypass-merged-head` — is read-context only and does not constrain a work unit here.)

## Touchpoint audit (verified: inline — dispatch unavailable, cycle-subagent inline override)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|---|---|---|---|---|
| `user/scripts/lazy_core/dispatch.py` | yes | `merged_head_override` (line 358, pure + fail-safe; local `from .depdag import next_merged`) | refactor (add fn) | Add `coordinator_arbitrated_emission(marker, feature_id, leases_path)` adjacent to `merged_head_override`; reuse its pure/fail-safe idiom (local import of the one external dep) |
| `user/scripts/lazy_coord.py` | yes | `has_live_lease(leases_path, wi_id, *, now=None)` (line 970, READ-ONLY, fail-safe) | reuse | The new predicate's `"lease"` branch calls this; obtain via a local `import lazy_coord` inside a `try/except` — do NOT add a top-level import to `dispatch.py` |
| `user/scripts/lazy-state.py` | yes | guard prologue (5 `_emit_is_lease_held` refs): `_emit_is_lane` @14803, `_emit_is_lease_held` @14828, guard cond @14838, observability elifs @14944/@14954 | refactor | Replace the 2 booleans + inline AND + 2 elifs with one predicate call + a reason→diag map |
| `user/scripts/bug-state.py` | yes | verbatim coupled mirror (5 `_emit_is_lease_held` refs), ~10062-10225 | refactor | Identical parity-mirrored replacement |
| `user/scripts/tests/test_lazy_core/test_dispatch.py` | yes | pytest shard, imports `lazy_core`; `tempfile`/`json` helpers present | modify | Add lane/lease/neither/fail-safe + lane-precedence cases; seed a temp `leases.json` for the `"lease"` case |
| `user/scripts/lazy_parity_audit.py` | yes | coupled-pair auditor (`--report`) | run + reconcile | Run after WU-2; reconcile `lazy-parity-manifest.json` only if the audit flags the changed blocks |
| `user/scripts/tests/baselines/{lazy,bug}-state-test-baseline.txt` | yes | committed regression net | verify-unchanged | Byte-identical before/after = behavior preserved (do NOT overwrite) |

No contradictions surfaced — every planned path exists; the predicate's home + reason-return shape is a locked SPEC decision (`## Proven Findings` ⚖ policy: shared `lazy_core.dispatch` helper). No anchor-grade or premise-grade drift.

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | unify-merged-head | P1 | Extract `coordinator_arbitrated_emission` predicate + unit tests | — | Solo (WU-1) |
| 2    | unify-merged-head | P2 | Switch both state-script callers to the predicate | Phase 1 | Solo (WU-2) |

## Work Units

- [x] WU-1 — Add `coordinator_arbitrated_emission` predicate + unit tests (Phase 1)
- [x] WU-2 — Switch both state-script callers to the predicate; parity + baselines green (Phase 2)

---

### Phase: unify-merged-head P1 — Extract `coordinator_arbitrated_emission` predicate

**Goal:** Add one pure-ish predicate to `lazy_core/dispatch.py` that answers "is this a coordinator-arbitrated emission the serial merged-head divergence premise does not apply to?", returning the exemption reason or `None`, fully unit-tested — with NO caller switched yet (baselines untouched).
**Entry criteria:** None (first phase).
**SPEC.md references:** `## Proven Findings` (the shared-`lazy_core.dispatch` home + `coordinator_arbitrated_emission(marker, feature_id, leases_path) -> None | "<reason>"` signature), `## Evidence Collected → Source Code` (the two exemptions being unified), Theory 1 (CONFIRMED, `traced`).

#### WU-1 — Add `coordinator_arbitrated_emission` predicate + unit tests

- **Scope (PHASES.md Phase 1 deliverables):**
  - [x] `coordinator_arbitrated_emission(marker, feature_id, leases_path)` added to `lazy_core/dispatch.py`, adjacent to `merged_head_override` [VERIFY: grep -n "def merged_head_override" user/scripts/lazy_core/dispatch.py], returning `None | "lane" | "lease"` (lane precedence over lease — matches the current `not _emit_is_lane` guard on the lease compute).
  - [x] Fail-safe by construction: a `parent_run`/marker read error → not a lane; a lease-check exception / unavailable `lazy_coord` / no `leases.json` / no live lease → not a lease → `None`. The predicate must NEVER raise into the base probe.
  - [x] Docstring recording this predicate as the single home for coordinator-arbitration exemptions and that a future third exemption (demoted-serial-rerun) is a one-line addition HERE returning a new reason string (the anti-accretion contract).
  - [x] Tests covering all four outcomes + lane precedence + fail-safe paths.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy_core/dispatch.py` — add the predicate next to `merged_head_override`. Reuse the fail-safe idiom; delegate the lease facet to `lazy_coord.has_live_lease(leases_path, feature_id)` [VERIFY: grep -n "def has_live_lease" user/scripts/lazy_coord.py] via a LOCAL `import lazy_coord` inside the lease branch's `try/except` (ImportError / any read error → no lease). Do NOT add a top-level `import lazy_coord` to `dispatch.py` (layering: `lazy_coord` is a top-level module, not under `lazy_core/`).
- **Test files:**
  - `user/scripts/tests/test_lazy_core/test_dispatch.py` — extend (the file already imports `lazy_core` and has `tempfile`/`json` helpers).
- **Test expectations (assert against SPEC intent, not the impl shape):**
  - `test_coordinator_arbitrated_emission_lane`: marker `{"parent_run": <truthy>}` → returns `"lane"` (regardless of feature_id / leases).
  - `test_coordinator_arbitrated_emission_lease`: marker without `parent_run` (e.g. `{}` or a non-lane dict), `feature_id` set, a temp `leases.json` seeded with a LIVE lease for that id → returns `"lease"`. (Seed via the file's temp helpers; use `has_live_lease`'s `now=` override or a fresh heartbeat so the lease reads live.)
  - `test_coordinator_arbitrated_emission_none`: marker without `parent_run`, no live lease (absent/expired `leases.json`) → returns `None`.
  - `test_coordinator_arbitrated_emission_lane_precedes_lease`: BOTH a `parent_run` AND a live lease qualify → returns `"lane"` (lane is evaluated first; the lease I/O is not even required).
  - `test_coordinator_arbitrated_emission_failsafe`: marker `None`; missing/empty `feature_id`; and a `leases_path` that raises on read (or points at a missing dir) → each returns `None` and never raises.
- **Implementation goal:** A single predicate whose return value drives BOTH the caller's skip decision AND its observability diagnostic (Phase 2 maps the reason string to the existing per-reason diag lines). Because no caller uses it yet, the `--test` baselines are untouched this phase.
- **Spec requirements:** SPEC `## Proven Findings` (signature + shared home), `## Evidence Collected → Source Code` (the current two-boolean shape being unified).
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | N/A             |

**Runtime Verification** *(checked by integration test or manual testing — NOT by the implementation agent):*
- [ ] <!-- verification-only --> `python -m pytest user/scripts/tests/test_lazy_core/test_dispatch.py -k coordinator_arbitrated -q` runs GREEN (the new predicate returns the correct reason / `None` for lane / lease / neither / fail-safe / lane-precedence inputs, driven through the real function).

---

### Phase: unify-merged-head P2 — Switch both state-script callers to the predicate

**Goal:** Replace the two ad-hoc booleans + inline AND + two observability `elif`s — in BOTH state scripts — with a single `coordinator_arbitrated_emission(...)` call plus a reason→diagnostic map, preserving byte-identical `--emit-prompt`/`--probe` output and keeping the coupled pair parallel.
**Entry criteria:** Phase 1 complete — `coordinator_arbitrated_emission` exists in `lazy_core/dispatch.py` with stable `"lane"`/`"lease"` reason strings and passing unit tests.
**SPEC.md references:** `## Affected Area` (both caller prologues + parity), `## Reproduction Steps` (the byte-unchanged `--test` baselines are the regression net), `## Proven Findings` (pure refactor — product output MUST be byte-identical).

#### WU-2 — Switch both callers; parity + baselines green

- **Scope (PHASES.md Phase 2 deliverables):**
  - [x] `lazy-state.py` guard prologue rewritten to one `coordinator_arbitrated_emission(...)` call; guard becomes `if _emit_marker is not None and _emit_exempt_reason is None:`; the two `elif _emit_is_lane` / `elif _emit_is_lease_held` observability branches become a single `elif _emit_exempt_reason is not None:` selecting the existing per-reason diag text (lane vs. lease), preserving both messages' content.
  - [x] `bug-state.py` given the byte-parallel identical replacement (coupled mirror), including mirror comments updated to point at the shared predicate.
  - [x] Mirror-comment blocks + the `lazy_coord` import annotations (`lazy-state.py:77-82`, `bug-state.py:97-100`) updated to reflect the single shared home. ⚖ The top-level `import lazy_coord` became dead (its sole consumer moved into the predicate's local import) so it was REMOVED from both scripts with an annotation recording the move (behavior-preserving; baselines byte-unchanged prove it).
  - [x] `lazy_parity_audit.py --repo-root .` exits 0; the manifest does not pin the changed block content, so no `lazy-parity-manifest.json` edit was needed.
  - [x] Caller-facing reason→diagnostic mapping (`"lane"` / `"lease"`) covered so a future reason rename cannot silently drop a diagnostic; an UNRECOGNIZED reason still skips the guard and emits a generic "coordinator-arbitrated emission" diag (forward-compat for the future third exemption).
- **TDD:** yes (the correctness net is baseline byte-identity + parity exit 0 + the reason→diag test; extend `test_dispatch.py` for the caller-facing mapping).
- **Files to create/modify:**
  - `user/scripts/lazy-state.py` — replace the guard prologue booleans + inline AND + two elifs with one predicate call + reason→diag map [VERIFY: grep -n "_emit_is_lease_held" user/scripts/lazy-state.py] (~14803-14965).
  - `user/scripts/bug-state.py` — identical parity-mirrored replacement [VERIFY: grep -n "_emit_is_lease_held" user/scripts/bug-state.py] (~10062-10225).
  - `user/scripts/lazy-parity-manifest.json` — reconcile ONLY if `lazy_parity_audit.py --report` flags the changed blocks (run the audit first; edit only on a flag).
  - `user/scripts/tests/test_lazy_core/test_dispatch.py` — add/extend a caller-facing reason→diag coverage assertion (unrecognized reason → generic diag + still-skip).
- **Test expectations:** the Phase-1 predicate unit tests continue GREEN; add a test asserting the reason→diagnostic mapping resolves `"lane"` and `"lease"` to their existing diag substances and that an unknown reason maps to the generic exemption diag while still skipping the guard. No new baseline rows (the `--test` baselines MUST NOT move).
- **Implementation goal:** A behavior-preserving swap — the caller computes the exemption reason once, skips the merged-head guard when the reason is non-`None`, and emits the matching diagnostic from a small map. Both callers get the identical unified shape (the coupled pair stays parallel).
- **Spec requirements:** SPEC `## Proven Findings` (pure refactor — byte-identical product output), `## Affected Area` (parity-mirrored edit; run the audit after).
- **Batch:** 1 (single WU; the two files are edited together as one logical parity-mirrored change so the pair never drifts through an intermediate half-state).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-2      | Solo      | N/A             |

**Runtime Verification** *(checked by integration test or manual testing — NOT by the implementation agent):*
- [ ] <!-- verification-only --> `python user/scripts/lazy-state.py --test` and `python user/scripts/bug-state.py --test` both PASS with their committed baselines byte-UNCHANGED (`user/scripts/tests/baselines/{lazy,bug}-state-test-baseline.txt`).
- [ ] <!-- verification-only --> `python user/scripts/lazy_parity_audit.py --report` exits 0 (coupled pair still parallel, or a new divergence registered in `lazy-parity-manifest.json`).

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md **Status:** to `Fixed` and writes `FIXED.md` once this phase's regression net (both `--test` baselines unchanged + parity audit green + the full `user/scripts/` pytest suite) passes through the validation tail. This plan never flips the top-level status itself.
