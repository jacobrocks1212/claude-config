---
kind: implementation-plan
feature_id: bug-state-scoped-query-loses-deferred-bug-identity
status: Complete
created: 2026-06-22
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-22.
> To execute: `/execute-plan <this-file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 3. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\plans\all-phases-scoped-deferred-identity-part-1.md` (this file)
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\plans\all-phases-scoped-deferred-identity-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\plans\all-phases-scoped-deferred-identity-part-3.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — bug-state-scoped-query-loses-deferred-bug-identity (Part 1: bug-state.py scoped identity)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\PHASES.md` (bug-state-scoped-query-loses-deferred-bug-identity, 4 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\SPEC.md`

**Total phases:** 4 (this part covers Phase 1)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **EXECUTION-CONTEXT OVERRIDE (autonomous bug pipeline).** When this plan is executed by a `/lazy-bug` / `/lazy-bug-batch` cycle subagent that does NOT have the `Agent` tool, perform ALL implementation and test-writing INLINE with `Read`/`Edit`/`Write` (still test-first: write the failing `--test` fixture/assertion before the implementation edit). The orchestrator-subagent split above is the interactive-session model; the dispatch-level inline override takes precedence when no `Agent` tool is available.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — UNLESS the executing context has no Agent tool (autonomous bug-pipeline cycle), in which case do it INLINE per the EXECUTION-CONTEXT OVERRIDE, still test-first.
2. All edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline — write the failing `--test` fixture/assertion first, then make it pass.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every code change is reviewed for correctness, spec alignment, and TDD discipline before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in a phase finish, integration verification confirms all changes work together.
8. Relevant CLAUDE.md files are created/updated after the phase if changes warrant it (`user/scripts/CLAUDE.md` documents the state-machine; update it if a new scoped terminal is added).
9. The completed phase is committed and pushed before the next part begins.
10. This plan is self-contained — follow it exactly as written without relying on external context.
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
12. **COUPLING RULE (HARD):** changes here are to the bug-pipeline state machine. Keep `bug-state.py --test` green and re-byte-pin `tests/baselines/bug-state-test-baseline.txt` ONLY via the `_normalize_smoke_output` helper in `test_lazy_core.py` (never by hand). Also keep `test_lazy_core.py` green (shared `lazy_core`). The feature-side parity mirror is Part 2 — do NOT attempt it here.

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | bug-state-scoped-query-loses-deferred-bug-identity | P1 | Identity-preserving scoped skip-branches in bug-state.py | — | Solo |

## Work Units

- [x] WU-1 — Add scoped per-bug deferred terminal_reason constant(s) + scoped operator-deferred early-return
- [x] WU-2 — Mirror scoped early-return into the cloud/device/park scoped-match branches
- [x] WU-3 — `bug-state.py --test` fixtures: scoped identity preserved + unscoped baseline regression

---

### Phase: bug-state-scoped-query-loses-deferred-bug-identity P1 — Identity-preserving scoped skip-branches in bug-state.py

**Goal:** When `--bug-id` matches a skipped-but-matched queue entry (operator-deferred / cloud / device / parked), `compute_state` returns a scoped `_bug_state` carrying the bug's `feature_id`/`feature_name`/`spec_path` + a per-bug deferred terminal, instead of falling through to the global null-identity terminal. UNSCOPED behavior stays byte-identical.

**Entry criteria:** None (first phase).

**SPEC.md references:** `## Evidence Collected → Part 1`, `## Proven Findings 1`, `## Affected Area` (Scoped-query identity row), `## Fix Scope → Primary`, `## Open Questions` (resolved in PHASES Implementation Notes ⚖ lines).

#### WU-1 — Scoped operator-deferred early-return (the reproduced primary case)

- **Scope:** PHASES P1 deliverables: "Add scoped per-bug terminal_reason constant(s)" + "return a scoped `_bug_state(...)` instead of `continue`" for the operator-deferred (`DEFERRED.md`) branch.
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/bug-state.py`.
- **Implementation goal:**
  - Add a scoped per-bug deferred terminal_reason constant next to the existing `TR_*` constants. Settled literal: `TR_OPERATOR_DEFERRED_SCOPED = "operator-deferred"` (the SPEC Open-Question 1 resolution — a NEW scoped literal, NOT a reuse of the global `TR_ALL_DEFERRED`/`all-remaining-deferred`). Also add a scoped `current_step` literal, e.g. `STEP_OPERATOR_DEFERRED_SCOPED = "Operator-deferred (scoped)"` (kept generic so the curated stage resolves from the terminal, which dominates, not the step). [VERIFY existing pattern: grep -n "TR_ALL_DEFERRED" user/scripts/bug-state.py]
  - In the operator-deferred skip branch (the `if deferred_md.exists():` block, currently `~766–775`, which does `_OPERATOR_DEFERRED.append(bug_name)` then `continue`): BEFORE the `continue`, add a guard — `if scope_bug_id is not None and str(bug_id) == str(scope_bug_id):` return a scoped `_bug_state(feature_id=bug_id, feature_name=bug_name, spec_path=str(spec_dir), current_step=STEP_OPERATOR_DEFERRED_SCOPED, terminal_reason=TR_OPERATOR_DEFERRED_SCOPED, notify_message=f"{bug_name}: operator-deferred (DEFERRED.md). Scoped query returns its identity; re-include by deleting DEFERRED.md.")`. The unscoped path (`scope_bug_id is None`) still falls through to `_OPERATOR_DEFERRED.append(...)` + `continue` unchanged. [VERIFY branch: grep -n "deferred_md = spec_dir" user/scripts/bug-state.py] [VERIFY scoped-return model: grep -n "STEP_COMPLETION_UNVERIFIED" user/scripts/bug-state.py]
  - Model the early-return EXACTLY on the existing completion-unverified scoped return (`~642–654`), which already returns a scoped `_bug_state` from inside the loop.
- **Spec requirements:** SPEC `## Fix Scope → Primary` ("when `scope_bug_id` is set and equals the current entry's id, the operator-deferred branch returns a scoped `_bug_state` ... instead of `continue`-ing into the global terminal. Preserve today's UNSCOPED behavior exactly"). SPEC `## Open Questions` Q1 → new `operator-deferred` scoped terminal.
- **Batch:** 1.

#### WU-2 — Mirror the scoped early-return into the cloud / device / park scoped-match branches

- **Scope:** PHASES P1 deliverable: "Mirror the same scoped-return treatment in the cloud-saturated skip branch, the device-saturated skip branch, and the two park branches for a scoped match."
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/bug-state.py`.
- **Implementation goal:**
  - Cloud-saturated branch (`if cloud:` → `cloud_saturated_skipped.append(bug_name); continue`, `~666–675`): add the same `if scope_bug_id is not None and str(bug_id) == str(scope_bug_id):` scoped early-return with `terminal_reason="cloud-queue-exhausted-scoped"` (or reuse a distinct constant `TR_CLOUD_DEFERRED_SCOPED`) + scoped fields, BEFORE the unscoped append+continue. [VERIFY: grep -n "cloud_saturated_skipped.append" user/scripts/bug-state.py]
  - Device-saturated branch (`if not real_device:` → `device_saturated_skipped.append(bug_name); ...; continue`, `~682–696`): same treatment with `TR_DEVICE_DEFERRED_SCOPED` (literal e.g. `"device-queue-exhausted-scoped"`). [VERIFY: grep -n "device_saturated_skipped.append" user/scripts/bug-state.py]
  - Park branches (`if park_blocked and (spec_dir / "BLOCKED.md").exists():` and the `park_needs_input` branch, `~782–824`): on a scoped match, return a scoped `_bug_state` naming the bug + its park reason. The park scoped terminal should map to the bug's actual side-state (a blocked-park scoped match → a scoped `blocked`-family terminal; a needs-input-park scoped match → a scoped `needs-input`-family terminal). Choose literals that Part 3's curated-stage mapping can route (`Blocked` / `Needs-input`, NOT `Deferred`, for the park cases). [VERIFY: grep -n "build_parked_entry" user/scripts/bug-state.py]
  - Use ONE shared helper or a consistent inline pattern across all four branches so the scoped-return shape is uniform. Each branch's UNSCOPED path stays byte-identical.
  - **Record every NEW scoped terminal_reason literal in a code comment block** at the top of the branch cluster — Part 3 (`curated_stage.py`) maps each literal verbatim, and Part 2 (`lazy-state.py`) mirrors the analogous feature-side literals.
- **Spec requirements:** SPEC `## Fix Scope → Primary` ("analogously cloud/device/parked"); SPEC `## Open Questions` Q2 (recommend fixing all four for consistency); SPEC `## Theories → Theory 1` (the cloud/device/parked variants are structurally identical and suspected to share the defect).
- **Batch:** 1.

#### WU-3 — `bug-state.py --test` fixtures (scoped identity + unscoped baseline regression)

- **Scope:** PHASES P1 deliverable: "Tests: add `bug-state.py --test` fixtures asserting `--bug-id <deferred-bug>` returns `feature_id == <bug-id>` + non-null `spec_path` + the scoped deferred terminal; plus a regression fixture asserting the UNSCOPED query still returns the global `all-remaining-deferred` terminal."
- **TDD:** yes (these fixtures are the test-first artifact — author them RED before WU-1/WU-2 land, or alongside; they must FAIL on the pre-fix code and PASS after).
- **Files to create/modify:** `user/scripts/bug-state.py` (the in-file `--test` smoke harness), `tests/baselines/bug-state-test-baseline.txt` (re-byte-pinned).
- **Test expectations:**
  - Fixture A (scoped operator-deferred): a temp-repo bug queue with one `DEFERRED.md` bug; call `compute_state(..., scope_bug_id=<that-bug-id>)`; assert `feature_id == <bug-id>`, `spec_path` is non-null and ends in `<bug-id>`, `terminal_reason == "operator-deferred"` (the scoped literal), NOT `feature_id is None` / `all-remaining-deferred`.
  - Fixture B (unscoped baseline regression): SAME fixture, `scope_bug_id=None`; assert `terminal_reason == "all-remaining-deferred"`, `feature_id is None` (byte-identical to pre-fix global behavior).
  - Fixture C (scoped cloud/device — at least one): a deferred-non-cloud (or device-deferred) bug queried with `--bug-id` under the matching env returns the bug's id + the scoped cloud/device terminal, not the null-identity global exhausted terminal.
  - (Park-scoped fixtures optional but preferred if WU-2 added park scoped returns.)
- **Implementation goal:** add these fixtures to the existing `--test` harness following its established fixture shape; run `python user/scripts/bug-state.py --test` and confirm green; regenerate `tests/baselines/bug-state-test-baseline.txt` by piping live `--test` output through `_normalize_smoke_output` (NEVER hand-edit the baseline). [VERIFY harness shape: grep -n "def _run_tests\|--test\|_normalize_smoke_output" user/scripts/bug-state.py test_lazy_core.py]
- **Spec requirements:** SPEC `## Affected Area → Tests` row; SPEC `## Verified Symptoms 2`; SPEC `## Reproduction Steps`.
- **Batch:** 2 (after WU-1/WU-2 implementation, OR author RED in batch 1 and flip GREEN in batch 2 per TDD).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2 | No (same file `bug-state.py`) | Yes — serialize |
| 2     | WU-3       | Solo | N/A |

## Execution Protocol

This protocol governs the autonomous execution of this part. Follow it exactly.

### Phase Selection Loop

This part has ONE phase (P1). Execute its batches in order, then run Post-Phase Steps, commit, and STOP (Part 2 is a separate `/execute-plan` cycle).

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (task tool loading via ToolSearch, task creation for all work units, update protocol).

### Per-Batch Steps

For each batch within the phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)
Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: PHASES.md (P1 + prior Implementation Notes), SPEC.md (relevant sections), this plan file, and the CURRENT `user/scripts/bug-state.py` around the four skip branches + the completion-unverified scoped-return model.

#### Step B.1: Implement (Agent subagents, OR inline per the EXECUTION-CONTEXT OVERRIDE)
Read `~/.claude/skills/_components/tdd-protocol.md`, `subagent-launch.md`, `tdd-test-agent.md`, `implementation-agent.md`. Write the failing `--test` fixture FIRST (WU-3 RED), then implement WU-1/WU-2 to GREEN. In an autonomous no-Agent context, do this inline with Read/Edit/Write, still test-first.

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md` + `mount-site-verification.md`. Produce a structured review verdict. Confirm: the unscoped path is byte-identical (Fixture B green), the scoped early-return matches the completion-unverified pattern, and no branch lost its unscoped `continue`.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `~/.claude/skills/_components/phases-update.md`. Tick P1's landed deliverable checkboxes, add an Implementation Notes block (date, scoped terminal literals chosen, files modified, the exact constant names so Parts 2/3 consume them).

#### Step B.4: Run Quality Gates (MANDATORY)
Read `~/.claude/skills/_components/quality-gates.md`. Run: `python user/scripts/bug-state.py --test`, `python user/scripts/lazy-state.py --test` (shared `lazy_core` — confirm no regression), `python test_lazy_core.py` (or the repo's pytest entry). 100% pass required. Re-byte-pin `tests/baselines/bug-state-test-baseline.txt` via `_normalize_smoke_output` if the smoke output changed.

#### Step B.4.5: MCP Integration Test
SKIP — `MCP runtime: not-required` (pure CLI/tooling, no app surface). Note the skip.

#### Step B.5: Commit Batch
Read `.claude/skill-config/commit-policy.md`. Conventional Commit, no AI attribution, push to `main`. Example: `fix(bug-state): scoped --bug-id returns deferred bug identity (P1)`. Confirm `git rev-parse --abbrev-ref HEAD` == `main` before committing.

#### Step B.6: Proceed to Next Batch
Checklist (all true): review verdict produced; PHASES.md updated; all `--test` suites green; baseline re-pinned if needed; committed+pushed.

### Post-Phase Steps (after all batches)

#### Integration Verification (MANDATORY)
Read `~/.claude/skills/_components/integration-verification.md`. Confirm scoped + unscoped paths coexist correctly; run the full `--test` trio once more.

#### Update CLAUDE.md Files (MANDATORY)
Read `~/.claude/skills/_components/claude-md-review.md`. `user/scripts/CLAUDE.md` documents the state machine + terminals — add a one-paragraph note on the new scoped per-bug deferred terminals if it materially changes the documented terminal set.

#### Commit and Push Post-Phase Changes
Per commit policy, on `main`.

## Blocking Issue Protocol

If a blocking issue is encountered:
1. Stop all in-progress work. 2. Commit + push any completed work. 3. Print a blocking-issue report (completed WUs, blocked WU, reason, recovery suggestion, remaining WUs). 4. Do NOT work around the blocker.

Blocking issues include: an unresolvable `--test` failure after 2 retries; a quality-gate failure unfixable after 2 retries; a git push conflict unresolvable by rebase; any error requiring an architectural decision beyond the SPEC's scope (write `BLOCKED.md` per the sentinel contract).

## Completion

When P1's batches are complete and committed:
1. Run the full `--test` trio one final time (`bug-state.py --test`, `lazy-state.py --test`, `test_lazy_core.py`).
2. Print a completion report (WUs completed, commits, final gate status).
3. STOP — Part 2 (`lazy-state.py` parity) is a separate `/execute-plan` cycle.

```
/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\plans\all-phases-scoped-deferred-identity-part-1.md
```
