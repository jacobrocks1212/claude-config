---
kind: implementation-plan
feature_id: bug-state-scoped-query-loses-deferred-bug-identity
status: Complete
created: 2026-06-22
complexity: complex
phases: [3, 4]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-22.
> To execute: `/execute-plan <this-file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 3 of 3. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\plans\all-phases-scoped-deferred-identity-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\plans\all-phases-scoped-deferred-identity-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\plans\all-phases-scoped-deferred-identity-part-3.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — bug-state-scoped-query-loses-deferred-bug-identity (Part 3: curated-stage Deferred rollup + e2e render guard)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\PHASES.md` (bug-state-scoped-query-loses-deferred-bug-identity, 4 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\SPEC.md`

**Total phases:** 4 (this part covers Phases 3 + 4)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **EXECUTION-CONTEXT OVERRIDE (autonomous bug pipeline).** When executed by a `/lazy-bug` / `/lazy-bug-batch` cycle subagent with no `Agent` tool, perform ALL implementation and test-writing INLINE with `Read`/`Edit`/`Write` (test-first). The inline override takes precedence when no `Agent` tool is available.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path. **Before executing each step**, `Read` the component files from disk. Do NOT proceed from memory. After compaction, re-read this plan first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies. Parts 1 & 2 of this series produce the scoped deferred terminal_reason literals this part maps + asserts end-to-end — read their PHASES Implementation Notes for the exact literal strings before authoring the `_SIDE_STATE_BY_TERMINAL` additions.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation/test work via Sonnet Agent subagents** — UNLESS no Agent tool (autonomous cycle), then INLINE, test-first.
2. Edits in the current worktree — never create worktrees.
3. Test-first per WU.
4. PHASES.md updated after each batch.
5. Review every change before continuing.
6. Fix mistakes immediately.
7. Integration verification after each phase.
8. Update CLAUDE.md if warranted.
9. Commit + push each completed phase.
10. Self-contained plan.
11. Read components from disk before each step.
12. Pure-function display + generator/probe layer — no state-machine change here. Keep `curated_stage` unit tests + the generator/probe tests green. The `unknown` fallback in `lazy-queue-doc.py` is RETAINED as defensive code (assert it is no longer REACHED for the deferred case, do not delete it).

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | bug-state-scoped-query-loses-deferred-bug-identity | P3 | Curated-stage Deferred rollup mapping | Parts 1 & 2 (scoped terminal literals) | Solo |
| 2    | bug-state-scoped-query-loses-deferred-bug-identity | P4 | End-to-end regression guard (no `unknown` row) | P3 | Solo |

## Work Units

- [x] WU-1 — Add `all-remaining-deferred` + scoped deferred terminals → `Deferred` (and park scoped → Blocked/Needs-input) in `_SIDE_STATE_BY_TERMINAL` + curated-stage unit tests
- [x] WU-2 — End-to-end regression guard: `lazy-queue-doc.py` emits a real SPEC link + `⏸ Deferred`, zero `docs/bugs/unknown/SPEC.md`

---

### Phase: bug-state-scoped-query-loses-deferred-bug-identity P3 — Curated-stage Deferred rollup mapping

**Goal:** `_SIDE_STATE_BY_TERMINAL` maps every deferred terminal_reason (the global `all-remaining-deferred` + the scoped per-bug/per-feature deferred literals from Parts 1 & 2) → `Deferred`, and routes the scoped park literals to their correct side-state.

**Entry criteria:** Parts 1 & 2 complete — the exact scoped terminal_reason literal strings are finalized (read their PHASES Implementation Notes).

**SPEC.md references:** `## Evidence Collected → Part 2`; `## Proven Findings 2`; `## Affected Area` (Curated stage rollup row); `## Fix Scope → Secondary`.

#### WU-1 — `_SIDE_STATE_BY_TERMINAL` additions + curated-stage unit tests

- **Scope:** PHASES P3 deliverables: add `all-remaining-deferred → Deferred`; add each scoped deferred terminal literal from Parts 1 & 2 → `Deferred`; route park scoped literals to `Blocked` / `Needs-input`; extend curated-stage tests.
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/pipeline_visualizer/curated_stage.py`; the curated-stage test module.
- **Implementation goal:**
  - In `_SIDE_STATE_BY_TERMINAL` (`~25–32`), add `"all-remaining-deferred": "Deferred"` (fixes the rollup even on the UNSCOPED display path that the original symptom exercised). [VERIFY: grep -n "_SIDE_STATE_BY_TERMINAL" user/scripts/pipeline_visualizer/curated_stage.py]
  - Add each NEW scoped deferred terminal_reason literal introduced in Parts 1 & 2 (e.g. `operator-deferred`, `cloud-queue-exhausted-scoped`, `device-queue-exhausted-scoped`, the scoped host-capability-deferred literal) → `"Deferred"` — VERBATIM string match to the constants those parts chose (read their Implementation Notes; do NOT guess). [VERIFY scoped literals exist: grep -rn "operator-deferred\|-scoped" user/scripts/bug-state.py user/scripts/lazy-state.py]
  - For any scoped PARK literal (blocked-park / needs-input-park scoped match): map to `"Blocked"` / `"Needs-input"` respectively (NOT `Deferred`) — a parked bug is in a blocked/needs-input side-state, not a deferred one.
  - Confirm `host-capability-saturated` and its scoped variant map appropriately (`Deferred` is correct for the host-capability axis — it is the host-axis mirror of `device-queue-exhausted`, already `Deferred`).
- **Test expectations:** extend the curated-stage test module: assert `curated_stage(...)`/the mapping returns `"Deferred"` for `all-remaining-deferred` and for each new scoped deferred literal; assert the park scoped literals return `"Blocked"`/`"Needs-input"`. Author RED (the current code returns `Pending` for `all-remaining-deferred`). [VERIFY test module: grep -rln "curated_stage\|_SIDE_STATE_BY_TERMINAL" user/scripts/pipeline_visualizer/ user/scripts/]
- **Spec requirements:** SPEC `## Fix Scope → Secondary` ("add the `all-remaining-deferred → Deferred` curated-stage mapping (and any new per-bug deferred terminal name)").
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo | N/A |

---

### Phase: bug-state-scoped-query-loses-deferred-bug-identity P4 — End-to-end regression guard

**Goal:** A repo with a `DEFERRED.md` bug renders that bug as `[<bug-id>](docs/bugs/<bug-id>/SPEC.md)` with `⏸ Deferred` and a working SPEC link — and the generated output contains ZERO `docs/bugs/unknown/SPEC.md`. This closes the loop across all three layers and asserts the downstream (probe + generator) needs no production change.

**Entry criteria:** P3 complete (Deferred rollup present); Parts 1 & 2 complete (scoped identity emitted).

**SPEC.md references:** `## Evidence Collected → Downstream`; `## Proven Findings 3`; `## Affected Area` (Downstream + Tests rows); `## Fix Scope → Guard`.

#### WU-2 — End-to-end regression guard test

- **Scope:** PHASES P4 deliverables: add the generator/probe regression fixture; assert the `unknown` fallback is retained but no longer reached for the deferred case; opportunistically assert the feature-side scoped deferral renders its feature id.
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/test_lazy_queue_doc.py` (and/or the probe test module). **No production change to `probe.py` / `lazy-queue-doc.py`** (the SPEC proves they are already correct given good upstream data).
- **Test expectations:**
  - Build a hermetic temp-repo fixture: `docs/bugs/queue.json` (or autodiscovered dirs) with one bug carrying a `DEFERRED.md` sentinel + a `SPEC.md`.
  - Drive `probe_state` → `curated_stage` → `_render_table` (the exact path `lazy-queue-doc.py` uses), or invoke the generator's render entrypoint.
  - Assert the rendered output CONTAINS `docs/bugs/<bug-id>/SPEC.md` (real link) and the `⏸` Deferred glyph, and CONTAINS zero occurrences of the substring `docs/bugs/unknown/SPEC.md`.
  - Assert `_item_id`/`_rel_spec_path` `"unknown"` fallback is retained in source (still defined) but not exercised for this fixture (absence of `unknown` in output). [VERIFY: grep -n "_item_id\|unknown\|_rel_spec_path\|_STAGE_GLYPH" user/scripts/lazy-queue-doc.py]
  - (If feasible in-fixture) add a feature-side deferred fixture asserting the feature id renders likewise (exercises Part 2's mirror through the generator).
- **Implementation goal:** add the fixture(s) to `test_lazy_queue_doc.py` (and/or the probe test) following the existing test shape; run them green. [VERIFY existing tests: grep -n "DEFERRED\|def test_\|unknown" user/scripts/test_lazy_queue_doc.py]
- **Spec requirements:** SPEC `## Fix Scope → Guard` ("add a regression test asserting `lazy-queue-doc.py` emits no `docs/bugs/unknown/SPEC.md` link for a repo containing a `DEFERRED.md` bug").
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-2      | Solo | N/A |

---

## Execution Protocol

### Phase Selection Loop
This part has TWO phases (P3 then P4). Execute P3's batch → Post-Phase Steps → commit; then P4's batch → Post-Phase Steps → commit. Then run Completion and STOP. P4's entry criteria require P3 complete.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)
Read `~/.claude/skills/_components/task-tracking.md` and follow it exactly.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read: PHASES.md (P3/P4 + Parts 1–2 Implementation Notes for the scoped literals), SPEC.md, this plan, the CURRENT `curated_stage.py` / `lazy-queue-doc.py` / `probe.py`.

#### Step B.1: Implement (Agent subagents OR inline per override)
Read `tdd-protocol.md`, `subagent-launch.md`, `tdd-test-agent.md`, `implementation-agent.md`. Failing tests first, then implement to GREEN. P3 changes `curated_stage.py` only; P4 adds tests only (no production change).

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `subagent-review.md` + `mount-site-verification.md`. Verdict required. Confirm the scoped literals mapped VERBATIM (no typo vs. Parts 1/2), and that P4 added no production change to the downstream renderers.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `phases-update.md`. Tick the phase's deliverables; Implementation Notes record the mapped literals + the e2e fixture name.

#### Step B.4: Run Quality Gates (MANDATORY)
Read `quality-gates.md`. Run the curated-stage tests, `test_lazy_queue_doc.py` (+ probe tests), and the full `--test` trio (`bug-state.py --test`, `lazy-state.py --test`, `test_lazy_core.py`) to confirm no regression. 100% pass required.

#### Step B.4.5: MCP Integration Test
SKIP — `MCP runtime: not-required`. Note the skip.

#### Step B.5: Commit Batch
Read `.claude/skill-config/commit-policy.md`. Conventional Commit, no AI attribution, push to `main`. Examples: `fix(lazy-pipeline-visualizer): map deferred terminals to Deferred stage (P3)`; `test(lazy-queue-doc): regression guard for DEFERRED.md bug render (P4)`. Confirm HEAD == `main` before committing.

#### Step B.6: Proceed to Next Batch/Phase
Checklist: review verdict; PHASES.md updated; all tests green; committed+pushed. After P4 lands, when the LAST phase's implementation is on disk, set the top-level PHASES `**Status:**` to `In-progress` (NOT Fixed — the `__mark_fixed__` gate owns that flip after the validation tail).

### Post-Phase Steps (after each phase)

#### Integration Verification (MANDATORY)
Read `integration-verification.md`. After P4: drive a `DEFERRED.md` fixture end-to-end and confirm the rendered row is correct + the `--test` trio + curated-stage + queue-doc tests all green.

#### Update CLAUDE.md Files (MANDATORY)
Read `claude-md-review.md`. Update `user/scripts/CLAUDE.md` if the curated-stage mapping set is materially documented there.

#### Commit and Push Post-Phase Changes
Per commit policy, on `main`.

## Blocking Issue Protocol

If blocked: 1. Stop. 2. Commit+push completed work. 3. Print blocking-issue report. 4. Do NOT work around. Blocking issues: a scoped literal from Parts 1/2 that cannot be located (the parts did not record it — re-read their Implementation Notes; if genuinely absent, the upstream part is incomplete — write `BLOCKED.md`); an unresolvable test failure after 2 retries; a git push conflict unresolvable by rebase.

## Completion

When P3 + P4 are complete and committed:
1. Run the curated-stage tests, `test_lazy_queue_doc.py`, the probe tests, and the full `--test` trio one final time.
2. Set the top-level PHASES `**Status:**` to `In-progress` (implementation done, validation pending — the `__mark_fixed__` gate flips to Fixed after the validation tail).
3. Print a completion report (WUs, commits, final gate status across all three parts).
4. STOP — the state machine routes onward to validation.

```
/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-state-scoped-query-loses-deferred-bug-identity\plans\all-phases-scoped-deferred-identity-part-3.md
```
