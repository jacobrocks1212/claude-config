---
kind: implementation-plan
feature_id: adhoc-cycle-begin-real-requires-sub-skill
status: Complete
created: 2026-07-06
phases: [1]
complexity: mechanical
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-06.
> To execute: `/execute-plan docs/bugs/_archive/adhoc-cycle-begin-real-requires-sub-skill/plans/fix-cycle-begin-real-requires-sub-skill.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — adhoc-cycle-begin-real-requires-sub-skill  (v2)

**PHASES.md files:**
- `docs/bugs/_archive/adhoc-cycle-begin-real-requires-sub-skill/PHASES.md` (adhoc-cycle-begin-real-requires-sub-skill, 1 phase)

**SPEC.md files:**
- `docs/bugs/_archive/adhoc-cycle-begin-real-requires-sub-skill/SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (a harness-config repo whose gates are Python `--test` / parity-audit rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes

- **Quality gates for THIS repo are Python, not msbuild/nx.** After the code changes land, the gates are:
  - `python3 user/scripts/lazy-state.py --test` — exit 0
  - `python3 user/scripts/bug-state.py --test` — exit 0
  - `python3 user/scripts/lazy_parity_audit.py --repo-root .` — exit 0
  - `python3 user/scripts/doc-drift-lint.py --repo-root .` — exit 0 (docs edit touches CLAUDE.md; keep it clean)
- **Coupled-pair discipline (HARD).** WU-1 (`lazy-state.py`) and WU-2 (`bug-state.py`) are a coupled-pair edit — the guard AND its `--test` fixtures must land symmetrically on both scripts or `lazy_parity_audit.py` fails. Do not close the batch until the parity audit is exit 0.
- **Read-side guard is UNCHANGED.** Do NOT modify `lazy_core.py:10972-10993` (the Round-3 fail-open guard). It is retained as defense-in-depth for legacy/meta/degraded markers — the SPEC Proven Findings explicitly keeps it.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | adhoc-cycle-begin-real-requires-sub-skill | P1 | Require `--sub-skill` on real cycles + fixtures + docs | — | Yes (WU-1/WU-2/WU-3 file-disjoint) |

## Work Units

- [x] WU-1 — lazy-state.py: `--kind real` requires `--sub-skill` guard + two `--test` fixtures
- [x] WU-2 — bug-state.py: coupled-pair mirror of the guard + two `--test` fixtures
- [x] WU-3 — user/scripts/CLAUDE.md: promote prose contract to documented hard enforcement

---

### Phase: adhoc-cycle-begin-real-requires-sub-skill P1 — Require `--sub-skill` on real cycles + fixtures + docs

**Goal:** Harden the write side so a `--kind real` cycle marker can never be born with `sub_skill=None`, mirrored across both state scripts with `--test` smoke fixtures and a docs promotion.

**Entry criteria:** None (first and only phase).

**SPEC.md references:** `## Proven Findings` (recommended fix + coupled-pair/parity note), `## Reproduction Steps`, `## Evidence Collected → Source Code` (serving-path trace hops 1–4), `## Affected Area`.

#### WU-1 — lazy-state.py guard + fixtures

- **Scope:** PHASES.md deliverables 1 and 3 (feature-pipeline guard + its two `--test` fixtures).
- **TDD:** yes (the `--test` fixtures ARE the tests — write the refusal fixture first; it fails until the guard exists, then passes).
- **Files to create/modify:** `user/scripts/lazy-state.py`
- **Test files:** in-file `--test` smoke harness of `user/scripts/lazy-state.py` (add fixtures alongside the existing `--cycle-begin` fixtures near line ~9625).
- **Test expectations:**
  1. `--cycle-begin --feature-id feat-x --nonce <n> --kind real` WITHOUT `--sub-skill` exits **non-zero** AND writes **no** cycle marker (assert the cycle-marker file is absent / unchanged after the failed call).
  2. `--cycle-begin --feature-id feat-x --nonce <n> --kind meta` WITHOUT `--sub-skill` exits **0** (meta exemption preserved).
  3. (Regression) the existing real-cycle fixtures that pass `--sub-skill execute-plan` stay green.
- **Implementation goal:** In the `--cycle-begin` handler [VERIFY: `grep -n "if args.cycle_begin:" user/scripts/lazy-state.py`], immediately after the existing id+nonce check [VERIFY: `grep -n "cycle-begin requires --feature-id and --nonce" user/scripts/lazy-state.py`], add:
  ```python
  if args.kind == "real" and not (args.sub_skill or "").strip():
      _die("--cycle-begin --kind real requires --sub-skill")
  ```
  The guard MUST run BEFORE the run-marker read and the `write_cycle_marker` call [VERIFY: `grep -n "write_cycle_marker" user/scripts/lazy-state.py`] so a refused real cycle mutates zero marker state. Reuse the imported `_die` helper [VERIFY: `grep -n "_die," user/scripts/lazy-state.py`] — do not write a new error path.
- **Spec requirements:** SPEC `## Proven Findings` → "Recommended fix (for `/plan-bug`)" (exact condition + `--kind meta` exemption) and the `⚖ policy` line (hard `_die`, not warn-and-proceed).
- **Batch:** 1

#### WU-2 — bug-state.py guard + fixtures (coupled-pair mirror)

- **Scope:** PHASES.md deliverables 2 and 4 (bug-pipeline guard + its two `--test` fixtures).
- **TDD:** yes (same shape as WU-1, bug-id variant).
- **Files to create/modify:** `user/scripts/bug-state.py`
- **Test files:** in-file `--test` smoke harness of `user/scripts/bug-state.py` (add fixtures alongside the existing `--cycle-begin` fixtures near line ~5227).
- **Test expectations:**
  1. `--cycle-begin --bug-id bug-x --nonce <n> --kind real` WITHOUT `--sub-skill` exits **non-zero** AND writes **no** cycle marker.
  2. `--cycle-begin --bug-id bug-x --nonce <n> --kind meta` WITHOUT `--sub-skill` exits **0**.
  3. (Regression) existing real-cycle fixtures passing `--sub-skill execute-plan` stay green.
- **Implementation goal:** In the `--cycle-begin` handler [VERIFY: `grep -n "if args.cycle_begin:" user/scripts/bug-state.py`], immediately after the existing id+nonce check [VERIFY: `grep -n "cycle-begin requires --bug-id and --nonce" user/scripts/bug-state.py`], add the byte-parallel guard (same condition, **same message text** `--cycle-begin --kind real requires --sub-skill`). Same ordering rule: guard before `write_cycle_marker` [VERIFY: `grep -n "write_cycle_marker" user/scripts/bug-state.py`]. Reuse the imported `_die` helper [VERIFY: `grep -n "_die," user/scripts/bug-state.py`].
- **Spec requirements:** SPEC `## Proven Findings` → "Coupled-pair + parity" (both scripts, `lazy_parity_audit.py` exit 0).
- **Batch:** 1

#### WU-3 — user/scripts/CLAUDE.md docs promotion

- **Scope:** PHASES.md deliverable 5 (promote the prose `--sub-skill` mandate to documented hard enforcement).
- **TDD:** no (pure documentation edit).
- **Files to create/modify:** `user/scripts/CLAUDE.md`
- **Implementation goal:** In the `--cycle-begin` CLI reference / "Per-sub_skill commit budget is DERIVED" note [VERIFY: `grep -n "cycle-begin" user/scripts/CLAUDE.md`], update the prose from "the orchestrator MANDATES `--sub-skill`" (a prose-only contract) to a documented HARD enforcement: a `--kind real` cycle now script-refuses (`_die`) a missing `--sub-skill`; `--kind meta` remains exempt. Note that the Round-3 read-side fail-open guard (`lazy_core.py:10972-10993`) is RETAINED as defense-in-depth for legacy/meta/degraded markers. Keep `doc-drift-lint.py` exit 0.
- **Spec requirements:** SPEC `## Affected Area` (Docs row) and `## Evidence Collected → Related Documentation`.
- **Batch:** 1

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2, WU-3 | Yes (three disjoint files) | None |

> Note: although WU-1/WU-2/WU-3 are file-disjoint and dispatch in parallel, WU-1 and WU-2 are a **coupled pair** — the batch does not close until `lazy_parity_audit.py --repo-root .` is exit 0, confirming the guard + fixtures landed symmetrically.

## Completion (gate-owned)

The `__mark_fixed__` gate flips SPEC.md `**Status:**` to Fixed and writes FIXED.md once this phase's validation tail (`/mcp-test` → coverage audit) passes. This plan never flips top-level status itself. When WU-1/WU-2/WU-3 land and the Python gates above are green, `/execute-plan` sets the plan-part `status:` to Complete and the state machine routes to the validation tail.
