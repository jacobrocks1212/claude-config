---
kind: implementation-plan
feature_id: hardening-blind-to-process-friction
status: Complete
created: 2026-06-15
complexity: mechanical
phases: [3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-15.
> To execute: `/execute-plan <this-file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 3 of 3. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\hardening-blind-to-process-friction\plans\all-phases-hardening-process-friction-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\hardening-blind-to-process-friction\plans\all-phases-hardening-process-friction-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\hardening-blind-to-process-friction\plans\all-phases-hardening-process-friction-part-3.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — hardening-blind-to-process-friction  (v2)

**PHASES.md files:**
- `docs/bugs/_archive/hardening-blind-to-process-friction/PHASES.md` (hardening-blind-to-process-friction, 3 phases)

**SPEC.md files:**
- `docs/bugs/_archive/hardening-blind-to-process-friction/SPEC.md`

**Total phases:** 3 (this part covers Phase 3)
**Plan version:** v2 (reference-based — components loaded from disk per step)

> **`complexity: mechanical` rationale:** every WU in this part is pure skill-prose / component-doc authoring that DOCUMENTS an already-defined trigger (the `kind: process-friction` ledger entry built in Phase 2). No novel design decision, no algorithm/DSP, no cross-boundary wiring — only adding a trigger row to existing trigger lists and a `trigger_kind` value to a dispatch template, validated by the repo's docs gates (`project-skills.py`, `lint-skills.py`, `lazy_parity_audit.py`). Qualifies as mechanical per the write-plan mechanical-vs-complex test.

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and doc-prose edits | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** The orchestrator may directly edit `PHASES.md` and `CLAUDE.md`. This part's primary deliverables are SKILL.md / component `.md` prose edits — under the standard `/write-plan` contract these are delegated to subagents; under a no-`Agent` inline cycle they are performed inline with Edit/Write. (Note: SKILL.md / `_components/*.md` are documentation, not source/test `.py`/`.sh` files, so the inline-edit prohibition that protects code does not bind here — but follow the review gate regardless.)

> **EXECUTION-CONTEXT NOTE (claude-config bug pipeline):** a `/lazy-bug`/`/lazy-bug-batch` cycle subagent with NO `Agent` tool performs all edits inline. This part has no test-first WUs (it is prose + gate-validation), so the discipline here is: edit → run the docs gates → fix → commit.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path. **Before executing each step**, `Read` the listed component files from disk. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `.claude/skill-config/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| turn-routing-enforcement | hard | `docs/specs/turn-routing-enforcement/` | The existing 3-trigger hardening-routing surface (`validate-deny`/`no-route`/`inject-hook-error`) this phase extends with a 4th (process-friction) class. |

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. Implementation (prose edits) delegated to Sonnet subagents via Agent — OR inline in a no-`Agent` cycle.
2. All edits in the current worktree — NEVER create worktrees.
3. PHASES.md updated AFTER EACH batch.
4. Every subagent's output reviewed before continuing.
5. Mistakes fixed immediately before the next batch.
6. **Coupled-pair rule (HARD):** `lazy-batch` ↔ `lazy-batch-cloud` are a coupled pair — any change to the trigger prose in one MUST be mirrored or registered as a divergence; `lazy_parity_audit.py --report` MUST show no unexplained drift.
7. The completed phase is committed and pushed before finalizing.
8. Self-contained — follow exactly.
9. **Before each step, `Read` the component files listed** — do NOT rely on memory.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | hardening-blind-to-process-friction | P3 | Process-friction hardening trigger | P2 (part 2) | Solo |

## Work Units

- [x] WU-1 — Add process-friction trigger to hardening-routing prose (lazy-batch, harden-harness, lazy-bug-batch, lazy-batch-cloud)
- [x] WU-2 — Add `trigger_kind=process-friction` to dispatch templates + update trigger-surface docs

---

### Phase: hardening-blind-to-process-friction P3 — Process-friction hardening trigger

**Goal:** Wire the `kind: process-friction` ledger entry (built in Phase 2) into the hardening-routing prose so the orchestrator deterministically routes `/harden-harness` on it (D2: every occurrence is signal, fires even when the runaway's work was salvaged). No new routing machinery — only the prose that names the trigger + the dispatch context.

**Entry criteria:** Phase 2 complete (the `kind: process-friction` ledger entry exists for the prose to route on).

**SPEC.md references:** Locked Decision **D2** (always harden — signal not noise); Proven Finding #1 (root-cause class `missing-contract` + `hook-defect`); Affected Area "Hardening trigger prose" + "Dispatch template".

#### WU-1 — Add process-friction trigger to hardening-routing prose

- **Scope:** PHASES Phase 3 deliverables 1 + 2 + 3 + 4.
- **TDD:** no (prose; validated by docs gates).
- **Files to create/modify:**
  - `user/skills/lazy-batch/SKILL.md` §1d.1 [VERIFY: `grep -n "validate-deny" user/skills/lazy-batch/SKILL.md`] — add a 4th trigger class "process-friction" (a `kind: process-friction` deny-ledger entry from a torn cycle bracket / unexpected commits) routing the same hardening dispatch as the existing three.
  - `user/skills/harden-harness/SKILL.md` [VERIFY: `grep -n "Triggers" user/skills/harden-harness/SKILL.md`] — add the process-friction trigger to Triggers (1–4 → 1–5) and to the Step-2 root-cause taxonomy (this class is `missing-contract` + `hook-defect`).
  - `user/skills/lazy-bug-batch/SKILL.md` [VERIFY: `ls user/skills/lazy-bug-batch/SKILL.md`] — mirror the §1d.1 process-friction trigger (bug pipeline runs the same `bug-state.py` ledger).
  - `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` [VERIFY: `ls repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md`] — keep consistent with `lazy-batch` per the coupled-pair rule (the process-friction trigger is shared, NOT a cloud divergence; update its "Differences from /lazy-batch" block only if a genuine divergence arises — none expected).
- **Implementation goal:** Each skill's hardening-trigger list gains the process-friction class with a one-line description tying it to the `kind: process-friction` ledger entry; the routing target is the existing hardening dispatch (the probe already withholds the forward route on the entry per Phase 2). Mirror identically across the coupled pair.
- **Spec requirements:** Locked Decision D2; Affected Area "Hardening trigger prose".
- **Batch:** 1.

#### WU-2 — Add `trigger_kind=process-friction` to dispatch templates + update trigger-surface docs

- **Scope:** PHASES Phase 3 deliverables 5 + 6.
- **TDD:** no (prose + docs).
- **Files to create/modify:**
  - `user/skills/_components/lazy-batch-prompts/dispatch-hardening.md` [VERIFY: `ls user/skills/_components/lazy-batch-prompts/dispatch-hardening.md`] + `user/skills/_components/hardening-dispatch.md` [VERIFY: `ls user/skills/_components/hardening-dispatch.md`] — add `trigger_kind=process-friction` as a valid value and document the friction-signal `--context` bindings (the cycle-bracket-break / unexpected-commit summary, matching the `build_hardening_emit_command` binding from Phase 2 WU-3).
  - `user/scripts/CLAUDE.md` + `CLAUDE.md` [VERIFY: `grep -rn "routing-layer" user/scripts/CLAUDE.md CLAUDE.md`] — update any "routing-layer only" / trigger-surface description so it reflects that process-friction is now also a trigger.
- **Implementation goal:** The dispatch templates accept and document the new `trigger_kind`; the repo docs no longer assert the hardening trigger surface is routing-layer-only. Keep the `--context` key names consistent with what `build_hardening_emit_command` emits for a process-friction entry.
- **Spec requirements:** Affected Area "Dispatch template"; SPEC Evidence "trigger surface is routing-layer only" (now extended).
- **Batch:** 1 (parallel-eligible with WU-1 — disjoint files; if inline, sequential).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1, WU-2 | Parallel-eligible (disjoint files) | None |

---

## Execution Protocol

### Phase Selection Loop

This part covers a single phase (P3, the terminal phase). Execute its batch, run Post-Phase Steps, then perform the final completion.

1. **Announce:** "Implementing hardening-blind-to-process-friction Phase 3: Process-friction hardening trigger".
2. **Review prior context:** Re-read PHASES.md Phase 3 + Phase 2 Implementation Notes (the `kind: process-friction` ledger reason strings + the `build_hardening_emit_command` `--context` keys), and the SPEC's D2.
3. **Execute the batch** per the Per-Batch Steps below.
4. **Run Post-Phase Steps.**
5. **Report:** "hardening-blind-to-process-friction Phase 3 — committed as [hash]".

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)
Read `~/.claude/skills/_components/task-tracking.md` and follow it.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read PHASES.md (Phase 3 + Phase 2 Implementation Notes), SPEC.md (D2), this plan, and the EXISTING trigger lists in each target SKILL.md so the new class matches their style.

#### Step B.1: Make the Prose Edits
Read `~/.claude/skills/_components/subagent-launch.md` (delegation orchestration) when the `Agent` tool is available; otherwise edit inline. Apply WU-1 + WU-2 edits.

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md`. Confirm: the new trigger reads consistently across all four skills; the coupled pair (`lazy-batch` ↔ `lazy-batch-cloud`) is mirrored; the dispatch-template `--context` keys match the Phase-2 `build_hardening_emit_command` output. Produce a verdict before B.3.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `~/.claude/skills/_components/phases-update.md`. Check off Phase 3 deliverables; add an Implementation Notes block (files edited, the exact trigger wording, the `--context` keys). **When this LAST phase's work lands, set the top-level PHASES.md `**Status:**` to `In-progress`** (implementation done, validation pending) — do NOT set Fixed/Won't-fix (gate-owned).

#### Step B.4: Run Quality Gates (MANDATORY — the acceptance gate for this phase)
Read `.claude/skill-config/quality-gates.md`. This is a skill/component + docs change, so run:
- `python user/scripts/project-skills.py` — re-expand all `!cat` refs; no circular-include / missing-component errors.
- `python user/scripts/lint-skills.py --check-projected --check-capabilities` — broken injections / embedded patterns / capability mismatches; must pass clean.
- `python user/scripts/lazy_parity_audit.py --report` — no unexplained drift on the coupled `lazy-batch` ↔ `lazy-batch-cloud` pair.
100% pass required before committing.

#### Step B.4.5: MCP Integration Test
N/A — no MCP server. Skip with a note.

#### Step B.5: Commit Batch
Read `.claude/skill-config/commit-policy.md`; commit (Conventional Commits, no AI attribution) and push to `main`.

### Post-Phase Steps (after the batch in Phase 3)

#### Update CLAUDE.md Files (MANDATORY)
Read `~/.claude/skills/_components/claude-md-review.md`. Confirm `CLAUDE.md` / `user/scripts/CLAUDE.md` trigger-surface descriptions are consistent with the new process-friction trigger (WU-2 covers the edits; this is the verification pass).

#### Commit and Push Post-Phase Changes
Read `.claude/skill-config/commit-policy.md`; commit and push to `main`.

---

## Blocking Issue Protocol

If a blocking issue is encountered: stop; commit completed work; print a blocking report (completed WUs + hashes, blocked WU, reason, recovery, remaining WUs); do not work around it. Blocking issues: `lazy_parity_audit.py` reports a drift that cannot be resolved without a product-class decision (→ `NEEDS_INPUT.md`); a docs gate failure unfixable after 2 attempts; a git push conflict unresolvable by rebase.

---

## Completion

When Phase 3's WU are complete (the whole bug fix is implemented):
1. Run the FULL quality-gate suite (`project-skills.py` + `lint-skills.py --check-projected --check-capabilities` + `python -m pytest user/scripts/ -q` + `lazy_parity_audit.py --report`).
2. Confirm the top-level PHASES.md `**Status:**` is `In-progress` (NOT Fixed — gate-owned; the orchestrator's `__mark_fixed__` validation tail owns the terminal flip + `FIXED.md` receipt).
3. Print a completion report (all phases done, commits, gates green). Let the state machine route to the validation tail.

Plan written to: C:\Users\Jacob\source\repos\claude-config\docs\bugs\hardening-blind-to-process-friction\plans\all-phases-hardening-process-friction-part-3.md
