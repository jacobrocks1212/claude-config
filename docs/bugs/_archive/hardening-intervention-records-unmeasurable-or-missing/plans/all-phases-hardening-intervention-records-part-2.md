---
kind: implementation-plan
feature_id: hardening-intervention-records-unmeasurable-or-missing
status: Complete
created: 2026-07-12
complexity: mechanical
phases: [3, 4, 5]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-12.
> To execute: `/execute-plan {path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 2. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\hardening-intervention-records-unmeasurable-or-missing\plans\all-phases-hardening-intervention-records-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\hardening-intervention-records-unmeasurable-or-missing\plans\all-phases-hardening-intervention-records-part-2.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — Hardening intervention records unmeasurable or missing (part 2: mechanical tail)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\hardening-intervention-records-unmeasurable-or-missing\PHASES.md` (5 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\hardening-intervention-records-unmeasurable-or-missing\SPEC.md`

**Total phases:** 5 (this part covers the mechanical tail of Phase 3 + Phases 4–5)
**Plan version:** v2 (reference-based — components loaded from disk per step)

**Why `complexity: mechanical`:** every WU in this part is a mechanical edit — WU-5 is run-end-flush prose wiring against the lint that part-1 already built, WU-6 is a data-repair/backfill act using the CLI part-1 already shipped, WU-7 is skill-prose + a deterministic re-projection + running the gate sweep. None involves a novel design decision, algorithm, or new cross-boundary seam (all seams landed in part 1). The part is dispatch-eligible for Sonnet.

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract (EXECUTION MODEL, COMPONENT LOADING PROTOCOL, Component Reference Card, MANDATORY RULES, Execution Protocol, Blocking Issue Protocol, Completion).

Where this plan's repo uses non-default gates (this is **claude-config** — Python/projection gates, NOT `/msbuild`/`/mstest`), the Plan-specific execution notes below override the contract's defaults.

## Plan-specific execution notes (claude-config gate overrides)

- **Quality gates** (from repo root `C:\Users\Jacob\source\repos\claude-config`): `python user/scripts/test_lazy_core.py`, `python user/scripts/test_doc_drift_lint.py`, `python user/scripts/lazy-state.py --test`, `python user/scripts/bug-state.py --test`, `python user/scripts/lazy_parity_audit.py --repo-root .`, `python ~/.claude/scripts/doc-drift-lint.py --repo-root .`.
- **Skill re-projection is a gate for WU-7:** after editing any `SKILL.md`, run `python ~/.claude/scripts/project-skills.py` then `python ~/.claude/scripts/lint-skills.py` — both must be clean, and the regenerated `skills-projected/**` is committed (never hand-edited).
- **No MCP runtime / no `/mcp-test`** — bug-completion evidence is the part-1 regression tests + the live `doc-drift-lint.py` going coverage-clean here in WU-6.
- **Part-1 must be Complete before this part runs** — every WU here depends on the seams part-1 shipped (the validator, the CLI reject, the coverage lint). Do not start if part-1's plan `status:` is not `Complete`.

## References — Upstream Artifacts

(none — no completed hard upstream dependencies. This part depends only on sibling part-1, executed first per the plan series.)

## Verified Touchpoint Audit (carried from PHASES.md — `verified: inline`)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `user/skills/lazy-batch/SKILL.md`, `user/skills/lazy-bug-batch/SKILL.md` | yes | §1c.6 end-of-run flush prose | refactor | Add a fail-open coverage-lint call beside `incident-scan.py`/`efficacy-eval.py` |
| `docs/interventions/harden-2026-07-r5.md`, `-r7.md` | yes | invalid `target_signal` | refactor (data) | Explicit re-declaration onto real vocabulary |
| `docs/interventions/harden-2026-07-r1.md`, `-r2.md`, `-r3.md` | **NO (net-new)** | — | create (data) | D9 backfill so the coverage lint sees a record |
| `user/skills/harden-harness/SKILL.md` | yes | Step-4 capture prose (:311-332) | refactor | State the now-mechanical contract |

## Execution Schedule

| Step | Feature | Phase(s) | Title | Blocked by | Parallel? |
|------|---------|----------|-------|------------|-----------|
| 1 | this bug | P3 | Run-end flush wiring (coverage lint) | part-1 WU-4 | Batch 1 (WU-5) |
| 2 | this bug | P4 | Data repair — re-declare r5/r7, backfill r1-r3 | part-1 WU-1/2, WU-4 | Batch 2 (WU-6) |
| 3 | this bug | P5 | harden-harness prose + re-projection + gate sweep | WU-5, WU-6 | Batch 3 (WU-7) |

> WU-5 and WU-6 touch disjoint files (SKILL prose vs `docs/interventions/*`) and could parallelize, but WU-6's verification step runs the live `doc-drift-lint.py` and WU-7 describes both — kept sequential for a clean review gate per WU.

## Work Units

- [x] WU-5 — run-end flush wiring for the coverage lint (`lazy-batch` + `lazy-bug-batch` prose)
- [x] WU-6 — data repair: re-declare r5/r7, backfill r1-r3, verify live coverage-clean
- [x] WU-7 — harden-harness prose + re-projection + full gate sweep

---

### Phase: this bug P3 — coverage lint run-end wiring (mechanical half)

**Goal:** The `/lazy-batch(-cloud)` end-of-run flush shells the coverage lint fail-open (a lint failure warns, never blocks `--run-end`).
**Entry criteria:** part-1 WU-4 (`check_intervention_coverage` exists).
**SPEC.md references:** Fix Scope #3 ("Runnable standalone and at the `--run-end` flush (fail-open there)"); Decision D2.

#### WU-5 — run-end flush wiring (coverage-lint call)
- **Scope:** Add the fail-open coverage-lint invocation to the §1c.6 end-of-run flush prose, mirrored across the coupled orchestrator pair.
- **TDD:** no (skill prose — no testable logic; the lint's own behavior is tested in part-1 WU-4)
- **Files to create/modify:** `user/skills/lazy-batch/SKILL.md`, `user/skills/lazy-bug-batch/SKILL.md`
- **Implementation goal:** In the §1c.6 end-of-run flush of `lazy-batch/SKILL.md` [VERIFY: grep -n "incident-scan" user/skills/lazy-batch/SKILL.md], beside the existing `incident-scan.py` / `efficacy-eval.py` calls, add a fail-open call to `python ~/.claude/scripts/doc-drift-lint.py --repo-root <repo>` (or the coverage check specifically) whose non-zero exit is surfaced as a warning and NEVER blocks `--run-end` (house posture). Mirror the same prose into `lazy-bug-batch/SKILL.md` [VERIFY: grep -n "incident-scan" user/skills/lazy-bug-batch/SKILL.md] (coupled pair). Prefer surfacing via the existing flush prose over a state-script compute-path change (the SPEC keeps the lint OFF the compute path). Re-project after (WU-7 also runs the full projection, but re-projecting here keeps the batch self-consistent).
- **Spec requirements:** SPEC Fix Scope #3, Decision D2 ("runnable both standalone and from the run-end flush").
- **Batch:** 1

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-5 | Solo | N/A |

---

### Phase: this bug P4 — Data repair (re-declare r5/r7, backfill r1-r3)

**Goal:** Fix the poisoned/missing records so the ledger is gradeable and the coverage lint passes over the LIVE tree.
**Entry criteria:** part-1 WU-1 (validator), WU-2 (CLI enforces vocab on backfill), WU-4 (lint to verify against).
**SPEC.md references:** Fix Scope #4 (r1-r3 backfill), #5 (r5/r7 repair); Decisions D4-adjacent.

#### WU-6 — data repair: re-declare r5/r7, backfill r1-r3, verify live coverage-clean
- **Scope:** Re-declare r5/r7 onto real vocabulary (explicit act); create r1-r3 backfill records via the D9 CLI; verify `doc-drift-lint.py` is coverage-clean over the live tree.
- **TDD:** no (data act — verified by the part-1 lint going clean, not by a new unit test)
- **Files to create/modify:** `docs/interventions/harden-2026-07-r5.md`, `-r7.md` (re-declared), `docs/interventions/harden-2026-07-r1.md`, `-r2.md`, `-r3.md` (net-new)
- **Implementation goal:**
  1. **r5/r7 re-declaration (explicit, never silent):** change `harden-2026-07-r5.md` `target_signal` from `event:no-route` and `harden-2026-07-r7.md` from `event:route-loop` to the closest REAL vocabulary event today (r5's no-route deadlock recurrence and r7's write-plan/execute-plan churn have no exact ledger event yet — use `event:gate-refusal` only if genuinely on that path, else set explicit `undeclared`). Record the re-declaration act (a `## Re-declaration 2026-07-12` note in the record body citing this bug + the split-brain sibling's D3 pattern). Re-validate each edited target against WU-1's `validate_intervention_target_signal` (must return `None`).
  2. **r1-r3 backfill (D9 manual path):** create `harden-2026-07-r1.md`, `-r2.md`, `-r3.md` via `python user/scripts/lazy-state.py --record-intervention --id harden-2026-07-rN --pipeline hardening --shipped-commit <round-N-commit> --shipped-date <round-N-date> [--target-signal event:<real> | --target-signal undeclared] --repo-root .` (stamps `provenance: backfilled`). The round commits/dates are in `docs/specs/turn-routing-enforcement/hardening-log/2026-07.md` (Round 1 `5ff653a` 2026-07-03; Round 2 `fbc4f0e` 2026-07-03; Round 3 `d650926` 2026-07-04, was `event:process-friction`-flavored → use explicit `--target-signal undeclared` since `process-friction` is NOT in the vocabulary, or the closest real event). Use `--target-signal undeclared` where no real signal fits — deliberate, typed, retro-visible.
  3. **Boundary cross-reference:** the poisoned r14-r21 re-baselining belongs to the `interventions-telemetry-repo-scope-split-brain` sibling, NOT here — note that boundary in the PHASES.md Implementation Notes so it is not double-fixed.
  4. **Verify:** run `python ~/.claude/scripts/doc-drift-lint.py --repo-root .` and confirm ZERO intervention-coverage findings over the live tree; confirm `test_doc_drift_lint.py`'s repo-clean self-check passes.
- **Spec requirements:** SPEC Fix Scope #4, #5.
- **Batch:** 2

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 2 | WU-6 | Solo | N/A |

---

### Phase: this bug P5 — harden-harness prose + re-projection + gate sweep

**Goal:** State the now-mechanical capture contract in the skill prose, re-project, and run the full gate sweep so the whole change lands coherent.
**Entry criteria:** WU-5, WU-6 (the prose describes their shipped behavior).
**SPEC.md references:** Fix Scope #6.

#### WU-7 — harden-harness prose + re-projection + full gate sweep
- **Scope:** Update `harden-harness/SKILL.md:311-332`; re-project + lint-skills; run the full gate sweep.
- **TDD:** no (prose + deterministic projection + running existing gates)
- **Files to create/modify:** `user/skills/harden-harness/SKILL.md`; `skills-projected/**` (regenerated, not hand-edited)
- **Implementation goal:** Rewrite the Step-4 capture prose [VERIFY: grep -n "Intervention record for the round" user/skills/harden-harness/SKILL.md] (`:311-332`) to state the MECHANICAL contract: an unknown `event:` type is rejected at the CLI (name the valid set); an undeclared `--pipeline hardening` capture is refused unless `--target-signal undeclared` is passed explicitly; round↔record coverage is now lint-enforced (`doc-drift-lint.py`, runs standalone + at `--run-end`, fail-open). Then run `python ~/.claude/scripts/project-skills.py` and `python ~/.claude/scripts/lint-skills.py` (both clean) and commit the regenerated projections.
- **Runtime verification (gate sweep — the phase's proof):** run the full claude-config gate sweep and confirm all green: `test_lazy_core.py`, `test_doc_drift_lint.py`, `lazy-state.py --test`, `bug-state.py --test`, `lazy_parity_audit.py --repo-root .`, `doc-drift-lint.py --repo-root .`, `lint-skills.py`.
- **Spec requirements:** SPEC Fix Scope #6.
- **Batch:** 3

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md/PHASES.md `**Status:**` and writes `FIXED.md` once the serving-path regression evidence (part-1 WU-1/2/4 tests + WU-6's live coverage-clean) is green. Do NOT author a status-flip or receipt row; do NOT write `FIXED.md` yourself.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 3 | WU-7 | Solo | N/A |

---

## End of part 2 (final part)

After WU-7's gate sweep is green, commit + push per the execution contract. When the last phase's work lands, set the top-level PHASES.md `**Status:**` to `In-progress` (implementation done, validation pending) — NOT `Fixed`. Let the state machine route to the completion gate; the `__mark_fixed__` gate owns the `FIXED.md` receipt + the terminal status flip.
