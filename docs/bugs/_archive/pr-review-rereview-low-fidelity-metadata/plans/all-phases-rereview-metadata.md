---
kind: implementation-plan
feature_id: pr-review-rereview-low-fidelity-metadata
status: Complete
created: 2026-07-18
complexity: complex
phases: [1, 2, 3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan C:/Users/Jacob/source/repos/claude-config/docs/bugs/_archive/pr-review-rereview-low-fidelity-metadata/plans/all-phases-rereview-metadata.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — PR-Review Re-Review Low-Fidelity Metadata (v2)

**PHASES.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/bugs/_archive/pr-review-rereview-low-fidelity-metadata/PHASES.md` (pr-review-rereview-low-fidelity-metadata, 3 phases)

**SPEC.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/bugs/_archive/pr-review-rereview-low-fidelity-metadata/SPEC.md`

**Total phases:** 3
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (claude-config is a harness-config repo — its gates are Python/projection, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Component Reference Card` override below the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes

- **Repo:** `claude-config` (`C:/Users/Jacob/source/repos/claude-config`). Work branch: `main`. Commit + push to `main` after each phase (personal config repo — push policy is push-after-every-commit).
- **Target subsystem:** the `cognito-pr-review` plugin at `user/plugins/local-tools/plugins/cognito-pr-review/`. Scripts run via `npx tsx` (no compile step); `package.json` at `scripts/` declares `"type": "module"` with `js-yaml` available.
- **This is a BUG fix, not a feature.** Do NOT flip the SPEC/PHASES top-level `**Status:**` to `Fixed` — that is the `__mark_fixed__` gate's job after the validation tail. You MAY tick per-WU checkboxes, flip this plan's frontmatter `status:`, and (when the last phase lands) set the PHASES top-level `**Status:**` to `In-progress`.
- **MCP runtime: not-required.** No Tauri/MCP surface. Verification is deterministic `node:test` fixtures (see Quality Gates below). There is no `/mcp-test` step to defer to; the phase tests ARE the verification.

## Component Reference Card (claude-config overrides)

| Concern | Command / path |
|--------|----------------|
| Run the plugin script tests | `cd user/plugins/local-tools/plugins/cognito-pr-review/scripts && npx tsx --test <test-file>` (Node built-in `node:test` runner) |
| Full plugin-script test sweep | `cd user/plugins/local-tools/plugins/cognito-pr-review/scripts && npx tsx --test *.test.ts` |
| Repo skill/doc lint (if any skill/component touched — N/A here) | `python ~/.claude/scripts/lint-skills.py` |
| Structural plan lint | `python3 ~/.claude/scripts/validate-plan.py --structural <plan-or-phases>` |

> **Note:** these are the quality gates for THIS plan. The contract's default `/msbuild`/`/mstest`/`/nxbuild` rows do NOT apply — claude-config has no .NET/Nx surface, and the touched code is TypeScript run under `npx tsx`.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies.)

## Touchpoint Audit (verified: inline — carried from /spec-phases; re-confirmed this cycle)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `scripts/post-process.ts` | yes | `parsePreviousReview()`:284, `step6_annotateLifespan()`:640, `main()` manifest load :717 | refactor | Thread `manifest.pr.iterationId`; fix `total_iterations` :656; fix `maxIteration` regex :298 |
| `scripts/post-process.test.ts` | yes | `node:test` suite; `test-fixtures/phase1-*` | extend | Add lifespan regression cases + inflated-marker fixture |
| `scripts/prep-pr.ts` | yes | `getIterations()`:607, `computeIterationDiff()`:739, `detectReReview()`:789, base compare :352, call site :1614-1621 | refactor | SHA anchor in `detectReReview`; base-relative delta in `computeIterationDiff` |
| `commands/review-pr.md` | yes | Step 12.6 REVIEWED.md template :564 | refactor | Add `reviewed_sha:` to frontmatter |
| `scripts/prep-pr.test.ts` | **NO (net-new)** | — | create | New fixture test for `detectReReview` anchor + `computeIterationDiff` merge-head |

No contradiction (anchor- or premise-grade). All paths `exists: yes` except the net-new `prep-pr.test.ts`.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | pr-review-rereview | P1 | Lifespan Counter Fidelity | — | Solo |
| 2 | pr-review-rereview | P2 | Re-Review Anchor via Reviewed SHA | — | Solo |
| 3 | pr-review-rereview | P3 | Merge-Safe Iteration Diff | P2 (shared file `prep-pr.ts`; correct anchor is prereq) | Solo |

> All three phases are Solo batches: Phases 2 and 3 both modify `scripts/prep-pr.ts` and `scripts/prep-pr.test.ts`, so the file-overlap rule forces sequential batches. Phase 1 is independent but kept solo for clarity (single WU).

## Work Units

- [x] WU-1 — Lifespan counter fidelity (post-process.ts + tests)
- [x] WU-2 — Reviewed-SHA re-review anchor (review-pr.md + prep-pr.ts + net-new test)
- [x] WU-3 — Merge-safe base-relative iteration diff (prep-pr.ts + test)

---

### Phase: pr-review-rereview P1 — Lifespan Counter Fidelity

**Goal:** Make `processed-findings.json` lifespan counters real and bounded; stop the self-amplifying re-review feedback loop.
**Entry criteria:** none.
**SPEC.md references:** RC-1a (`post-process.ts:656`), RC-1b (`post-process.ts:298`); Reproduction "Symptom 1"; Convergence Targets row 1.

#### WU-1 — Lifespan counter fidelity

- **Scope (PHASES P1 deliverables):** thread real iteration count into lifespan annotation; `total_iterations` from `manifest.pr.iterationId`; break the `maxIteration` scrape feedback loop; clamp `raised_in ≤ total_iterations`; guard no-previous / missing-`iterationId`; add regression tests.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/plugins/local-tools/plugins/cognito-pr-review/scripts/post-process.ts` — refactor `step6_annotateLifespan()` [VERIFY: grep -n "function step6_annotateLifespan" user/plugins/local-tools/plugins/cognito-pr-review/scripts/post-process.ts] and `parsePreviousReview()` [VERIFY: grep -n "function parsePreviousReview" user/plugins/local-tools/plugins/cognito-pr-review/scripts/post-process.ts] to accept the real iteration count, sourced from the manifest already loaded in `main()` [VERIFY: grep -n "loadJSON<Manifest>(manifestPath" user/plugins/local-tools/plugins/cognito-pr-review/scripts/post-process.ts].
  - `user/plugins/local-tools/plugins/cognito-pr-review/scripts/post-process.test.ts` — add lifespan cases.
  - `user/plugins/local-tools/plugins/cognito-pr-review/scripts/test-fixtures/` — add a previous-review markdown fixture carrying an inflated `raised_in: 777` marker (+ a manifest fixture with a small real `pr.iterationId`, e.g. 5).
- **Test files:** `scripts/post-process.test.ts`.
- **Test expectations (RED first):**
  1. Given a carried-forward finding that matches a previous-review ref, the emitted `lifespan.total_iterations` equals the fixture manifest's `pr.iterationId` (NOT `prevMatch.iteration + 1`), and `lifespan.raised_in ≤ total_iterations`. (Fails today: current code emits `raised_in + 1`.)
  2. Given a previous-review fixture containing `raised_in: 777`, the new run's emitted `lifespan.raised_in` is NOT 777 and NOT `> total_iterations` — the scrape no longer reads back its own prior marker. (Fails today: RC-1b feedback loop.)
  3. Missing `pr.iterationId` / no `--previous-review` path degrades without throwing (existing empty-refs early return preserved).
- **Implementation goal:** `total_iterations` derives from the real PR iteration count (`manifest.pr.iterationId`); `raised_in` is a real, bounded value (≤ `total_iterations`), no longer inflated by scanning the previous review's own `raised_in:` markers. Keep the `{raised_in, total_iterations}` object shape unchanged (no consumer contract change). Do NOT invent a new count source — reuse the manifest already in scope.
- **Spec requirements:** SPEC "Proven Findings" RC-1a/RC-1b; "Convergence Targets" row 1 (`total_iterations` → `manifest.pr.iterationId`).
- **Batch:** 1 (Solo).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | None |

---

### Phase: pr-review-rereview P2 — Re-Review Anchor via Reviewed SHA

**Goal:** Persist the actually-reviewed head SHA in `REVIEWED.md` and anchor the incremental diff on it, eliminating the review-round-number-as-commit-index mismatch.
**Entry criteria:** none (independent of P1).
**SPEC.md references:** RC-2a (`prep-pr.ts:749` + `:805-812`); Convergence Targets row 2; Open Question 1.

#### WU-2 — Reviewed-SHA re-review anchor

- **Scope (PHASES P2 deliverables):** extend `REVIEWED.md` frontmatter with `reviewed_sha:`; `detectReReview` reads it and returns it as the anchor; call site uses the persisted SHA as `previousSha` when present, else falls back to the journey `### Iteration N` scrape; `ReReviewInfo` carries the SHA; net-new test.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/plugins/local-tools/plugins/cognito-pr-review/commands/review-pr.md` — Step 12.6 `REVIEWED.md` template [VERIFY: grep -n "kind: reviewed" user/plugins/local-tools/plugins/cognito-pr-review/commands/review-pr.md]: add a `reviewed_sha: "<head commit SHA>"` line sourced from the source/head commit already resolved during prep (document where the value comes from).
  - `user/plugins/local-tools/plugins/cognito-pr-review/scripts/prep-pr.ts` — `detectReReview()` [VERIFY: grep -n "function detectReReview" user/plugins/local-tools/plugins/cognito-pr-review/scripts/prep-pr.ts] reads `REVIEWED.md` (sibling of the journey in `cogDocsItemDir`) and parses `reviewed_sha`; extend `ReReviewInfo` [VERIFY: grep -n "ReReviewInfo" user/plugins/local-tools/plugins/cognito-pr-review/scripts/prep-pr.ts] with the SHA; at the call site [VERIFY: grep -n "reReviewInfo.isReReview && reReviewInfo.previousIterationId" user/plugins/local-tools/plugins/cognito-pr-review/scripts/prep-pr.ts] pass the persisted SHA to `computeIterationDiff` as `previousSha` directly, bypassing the `iterations[previousIterationId - 1]` index when the SHA is present.
  - `user/plugins/local-tools/plugins/cognito-pr-review/scripts/prep-pr.test.ts` — **net-new** test file.
  - `user/plugins/local-tools/plugins/cognito-pr-review/scripts/test-fixtures/` — `REVIEWED.md` fixtures (one with `reviewed_sha`, one legacy without) + a journey fixture with `### Iteration N` headers.
- **Test files:** `scripts/prep-pr.test.ts` (net-new).
- **Test expectations (RED first):**
  1. A `REVIEWED.md` fixture carrying `reviewed_sha: <sha>` makes `detectReReview` resolve the previous anchor to `<sha>` (not a journey round number). (Fails today: no SHA read exists.)
  2. A legacy `REVIEWED.md` without `reviewed_sha` (or none present) falls back to the journey `### Iteration N` max-scrape — existing behavior preserved. (Regression guard.)
- **Implementation goal:** the previous-reviewed anchor is the durable reviewed head SHA, not the journey review-round integer indexing a commit array. Journey-scrape remains ONLY as the legacy fallback. Preserve the empty-diff guard and the initial-review (`isReReview: false`) path.
- **Spec requirements:** SPEC RC-2a; Convergence Targets row 2 ("extend the sentinel to record reviewed SHA, anchor the diff on it").
- **Batch:** 1 (Solo — shares `prep-pr.ts`/`prep-pr.test.ts` with WU-3, so must precede it).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-2 | Solo | Shares `prep-pr.ts` + `prep-pr.test.ts` with WU-3 → sequential |

---

### Phase: pr-review-rereview P3 — Merge-Safe Iteration Diff

**Goal:** Replace three-dot merge-base compare with a base-relative delta so merged-in `main` churn is excluded and genuine branch-only changes are retained.
**Entry criteria:** P2 complete (correct `previousSha` anchor; both endpoints must be right for the delta to be meaningful; also shares `prep-pr.ts`).
**SPEC.md references:** RC-2b (`prep-pr.ts:757`); Convergence Targets row 3; Open Question 2; Reproduction "Symptom 2".

#### WU-3 — Merge-safe base-relative iteration diff

- **Scope (PHASES P3 deliverables):** refactor `computeIterationDiff` to a base-relative file-set delta mirroring the base-branch compare pattern; preserve `IterationDiffData` shape + the `iteration-diff.json` write + the empty-diff guard; add a merge-commit-head fixture test.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/plugins/local-tools/plugins/cognito-pr-review/scripts/prep-pr.ts` — refactor `computeIterationDiff()` [VERIFY: grep -n "async function computeIterationDiff" user/plugins/local-tools/plugins/cognito-pr-review/scripts/prep-pr.ts]: replace the three-dot `/compare/${previousSha}...${currentSha}` call [VERIFY: grep -n "compare/\${previousSha}" user/plugins/local-tools/plugins/cognito-pr-review/scripts/prep-pr.ts] with a base-relative delta — the file set changed up to `currentSha` minus that changed up to `previousSha` — mirroring the existing correct base-branch compare pattern [VERIFY: grep -n "diff --name-status \${baseBranch}...HEAD" user/plugins/local-tools/plugins/cognito-pr-review/scripts/prep-pr.ts]. Merged-in `main` churn (present relative to base at both endpoints) cancels; branch-only changes remain.
  - `user/plugins/local-tools/plugins/cognito-pr-review/scripts/prep-pr.test.ts` — extend with the merge-head case.
  - `user/plugins/local-tools/plugins/cognito-pr-review/scripts/test-fixtures/` — recorded/mocked `/compare` responses (a merge-polluted three-dot response + the base-relative endpoints) to drive the assertion deterministically without a live API call. Use a thin injectable fetch shim if the module's `ghFetch` is not already parameterizable — do NOT hit the network in tests.
- **Test files:** `scripts/prep-pr.test.ts` (extends WU-2's file).
- **Test expectations (RED first):**
  1. Merge-commit-head case: the computed delta INCLUDES the branch-only changed files (e.g. the `Cognito/*` rewrite class from the SPEC repro) and EXCLUDES the merged-in `main` churn (billing test-clock / AI form-gen / marketing templates). (Fails today: three-dot surfaces main churn and drops branch files.)
  2. Non-merge-head case: the delta equals the straightforward branch changes (no regression for the common case).
  3. Empty-diff guard: when either endpoint SHA is unresolved, an empty `IterationDiffData` is returned (existing behavior preserved).
- **Implementation goal:** `iteration-diff.json` reflects what genuinely changed on the PR branch between the previously-reviewed state and the current head — robust when the head iteration is a merge commit. Keep the emitted shape (`filesAdded`/`filesRemoved`/`filesModified`) and the write path unchanged so `reReviewScope`/triage consumers are untouched.
- **Spec requirements:** SPEC RC-2b; Convergence Targets row 3 ("two-dot / base-relative delta"). Note: two-dot alone still diffs *through* a merge — the base-relative delta is the chosen fidelity bar (PHASES "Fix-shape decisions" Q2).
- **Batch:** 1 (Solo — runs after WU-2, same file).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-3 | Solo | Shares `prep-pr.ts` + `prep-pr.test.ts` with WU-2 → runs after WU-2 |

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md `**Status:**` to `Fixed` and writes `FIXED.md` after the validation tail (the phase tests + symptom-reproduction gate confirm the bogus counters and mis-scoped diff are gone at their reported surfaces). This plan never flips the top-level status.

---

## Quality Gates (per phase — claude-config overrides)

Run after each phase's WU lands, from the plugin scripts dir:

```bash
cd user/plugins/local-tools/plugins/cognito-pr-review/scripts
npx tsx --test post-process.test.ts     # after Phase 1
npx tsx --test prep-pr.test.ts          # after Phase 2 and Phase 3
npx tsx --test *.test.ts                # full sweep before the final commit
```

All tests must pass (GREEN) before committing the phase. There is no `/msbuild`/`/mstest`/`/mcp-test` gate for this plan — the `node:test` fixtures are the authoritative verification (MCP runtime: not-required).

## Blocking Issue Protocol / Completion

Single-sourced in `~/.claude/skills/_components/execution-contract.md` (read it before executing). On a genuine blocker, write `BLOCKED.md` (canonical name) in the bug dir and stop. On completion of all three phases, set the PHASES.md top-level `**Status:**` to `In-progress` (NOT `Fixed`) and let the state machine route to the validation tail.
