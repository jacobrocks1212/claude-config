---
kind: implementation-plan
feature_id: decision-11-dispatch-time-forward-advance
status: Complete
created: 2026-07-19
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan docs/bugs/_archive/decision-11-dispatch-time-forward-advance/plans/all-phases-dispatch-time-forward-advance.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — decision-11-dispatch-time-forward-advance  (v2)

**PHASES.md files:**
- `docs/bugs/_archive/decision-11-dispatch-time-forward-advance/PHASES.md` (decision-11-dispatch-time-forward-advance, 1 phase)

**SPEC.md files:**
- `docs/bugs/_archive/decision-11-dispatch-time-forward-advance/SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the local `## Component Reference Card` override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config gates override the contract defaults)

This is the **claude-config** repo — a Python/skills harness with **no Tauri/MCP surface and no `/msbuild`/`/mstest` gates**. The contract's default build/test gate rows do NOT apply. This plan's quality gates are:

```bash
# 1. The changed module's own unit tests (fast, deterministic):
python3 user/scripts/tests/test_lazy_core/test_markers.py

# 2. Both state-script self-test harnesses (must stay byte-stable — this change is test-file + dead-code + docs, no state-machine branch change):
python3 user/scripts/lazy-state.py --test
python3 user/scripts/bug-state.py --test

# 3. Parity audit (the change touches the shared lazy_core.markers surface + the --cycle-end bracket present on both scripts):
python3 user/scripts/lazy_parity_audit.py --repo-root .

# 4. Skill/doc lint (a CLAUDE.md doc edit is in scope):
python3 user/scripts/lint-skills.py
```

All four must pass (each exits 0) before the phase's commit. Run them synchronously in the foreground — none exceeds the Bash cap, so none is auto-backgrounded. There is **no live runtime to validate** (`MCP runtime: not-required`); the deterministic gates above ARE the verification for this bug.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies. The SPEC's `**Related:**` cross-links are archived prior-art bugs, not build dependencies.)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | decision-11-dispatch-time-forward-advance | P1 | Retarget pinned tests, retire dead `consume_gate` trigger, reconcile doc | — | Sequenced (WU-1 → WU-2; both touch this cycle's shared review but disjoint files — see batch table) |

## Work Units

- [x] WU-1 — Retarget pinned tests to the dispatch-time bracket + retire the dead `consume_gate` trigger
- [x] WU-2 — Reconcile the stale `user/scripts/CLAUDE.md` "two orthogonal triggers" doc

---

### Phase: decision-11-dispatch-time-forward-advance P1 — Retarget pinned tests, retire dead `consume_gate` trigger, reconcile doc

**Goal:** Complete the decision-11 residual — move the two pinned tests onto the already-shipped `advance_cycle_bracket_counter` dispatch-time advance, delete the now-dead `consume_gate` parameter + its census-rise branch + its `consume_gate`-only test(s), and reconcile the stale doc — with no change to any production advance behavior.

**Entry criteria:** None. The dispatch-time mechanism (`advance_cycle_bracket_counter` wired at both `--cycle-end` handlers) already shipped in commit `e91bd305`; this phase consumes it.

**SPEC.md references:** `## Residual Work (for /plan-bug)` items 1–4; `## Proven Findings` (the traced serving path + the disjoint two-advance-path design); `## Affected Area` table.

#### WU-1 — Retarget pinned tests to the dispatch-time bracket + retire the dead `consume_gate` trigger

**Scope (covers PHASES deliverables 1–4):**
- Retarget `test_advance_forward_cycle_verbatim_real_skill_theory_1b` and `test_advance_forward_cycle_consume_gate_advances_multicycle_same_step` to assert the dispatch-time advance via `advance_cycle_bracket_counter`.
- Remove the `consume_gate` keyword-only param + its census-rise branch from `advance_forward_cycle`.
- Drop the `consume_gate`-only test(s) and their `TESTS`-registry entries.

**TDD:** yes (test-centric — the retargeted tests define the new contract; author/adjust the tests to the live mechanism, then remove the dead param so the suite is green against the shipped behavior).

**Files to create/modify:**
- `user/scripts/lazy_core/markers.py` — in `advance_forward_cycle` (`def advance_forward_cycle(state: dict, *, consume_gate: bool = False)` [VERIFY: grep -n "def advance_forward_cycle" user/scripts/lazy_core/markers.py]): remove the `consume_gate` keyword-only parameter and delete its census-rise second-trigger branch (the `if consume_gate:` block near `markers.py:3385` and the `if consume_gate and current_consume is not None:` block near `markers.py:3421` [VERIFY: grep -n "consume_gate" user/scripts/lazy_core/markers.py]) plus any now-unreferenced locals and the docstring lines describing the `consume_gate` trigger. **KEEP** the `last_advance_state_key` state-change trigger intact — it still serves `--apply-pseudo`. Do **NOT** touch `advance_cycle_bracket_counter` (`def advance_cycle_bracket_counter` [VERIFY: grep -n "def advance_cycle_bracket_counter" user/scripts/lazy_core/markers.py]) — it is the live authority and must remain byte-unchanged.
- `user/scripts/tests/test_lazy_core/test_markers.py` — retarget the two pinned tests [VERIFY: grep -n "def test_advance_forward_cycle_verbatim_real_skill_theory_1b\|def test_advance_forward_cycle_consume_gate_advances_multicycle_same_step" user/scripts/tests/test_lazy_core/test_markers.py]; drop `test_advance_forward_cycle_consume_gate_default_off_preserves_freeze` [VERIFY: grep -n "def test_advance_forward_cycle_consume_gate_default_off_preserves_freeze" user/scripts/tests/test_lazy_core/test_markers.py] (and any other test whose SOLE purpose is exercising `consume_gate`); update the module `TESTS` registry list [VERIFY: grep -n "test_advance_forward_cycle_consume_gate" user/scripts/tests/test_lazy_core/test_markers.py] to remove the dropped entry and rename the retargeted-test entries to match.

**Test files:** `user/scripts/tests/test_lazy_core/test_markers.py` (same file — retargeted in place).

**Test expectations:**
- The retarget of `..._verbatim_real_skill_theory_1b`: assert that a verbatim (consume-missed) real dispatch advances `forward_cycles` **once per completed `--cycle-end` real bracket** through `advance_cycle_bracket_counter(cycle_marker)` with a cycle marker whose `kind == "real"` — reproducing a marker + cycle-marker, calling the bracket counter, and asserting `forward_cycles` incremented by exactly 1 (plus the sibling `per_feature_forward_cycles[feature_id]` bump). Preserve the encoded intent: a consume-missed real dispatch still advances exactly once. Rename the test to reflect the mechanism if `consume_gate`/`verbatim` no longer describe it accurately (e.g. keep `_verbatim_real_skill` but assert via the bracket).
- The retarget of `..._consume_gate_advances_multicycle_same_step`: assert that a multi-part `/execute-plan` (the SAME `(feature_id, current_step, sub_skill)` tuple across parts) advances `forward_cycles` **once per completed `--cycle-end` real bracket** via `advance_cycle_bracket_counter` — call the bracket counter N times with N real cycle markers at the unchanging tuple and assert `forward_cycles == N` (not frozen, not over-counted). Rename away from `consume_gate` (e.g. `test_bracket_counter_advances_once_per_multicycle_same_step_bracket`).
- Both retargeted tests must be **RED** if pointed at a hypothetical un-relocated (probe-path) mechanism and **GREEN** against the shipped `advance_cycle_bracket_counter` — do NOT invert the assertions; preserve "each genuine dispatch cycle advances exactly once."
- Do NOT modify or weaken the existing binder tests `test_inject_hook_probe_on_non_dispatch_turn_does_not_advance_forward_budget` and `test_cycle_end_real_bracket_advances_forward_and_per_feature` [VERIFY: grep -n "def test_inject_hook_probe_on_non_dispatch_turn_does_not_advance_forward_budget\|def test_cycle_end_real_bracket_advances_forward_and_per_feature" user/scripts/tests/test_lazy_core/test_markers.py] — they must stay GREEN and unchanged.
- Do NOT touch `test_advance_run_counters_consume_gated` [VERIFY: grep -n "def test_advance_run_counters_consume_gated" user/scripts/tests/test_lazy_core/test_markers.py] — it exercises the SEPARATE `advance_run_counters` consume-oracle path, out of scope.

**Implementation goal:** After this WU, `grep -n "consume_gate" user/scripts/lazy_core/markers.py` returns no hits inside `advance_forward_cycle` (param + both branches gone; the state-change path preserved), the two pinned tests assert dispatch-time advance through `advance_cycle_bracket_counter`, the `consume_gate`-only test is gone with a clean `TESTS` registry (no dangling reference), and `python3 user/scripts/tests/test_lazy_core/test_markers.py` is green.

**Spec requirements:** SPEC `## Residual Work` items 1 (retarget, do NOT invert) + 2 (retire dead `consume_gate` trigger, keep the state-change path); `## Proven Findings` (the two advance paths are disjoint — Agent dispatches advance at `--cycle-end`, inline pseudo-skills at `--apply-pseudo`); `⚖ policy: retire dead consume_gate vs leave orphaned param → retire it in /plan-bug scope`.

**Batch:** 1

#### WU-2 — Reconcile the stale `user/scripts/CLAUDE.md` "two orthogonal triggers" doc

**Scope (covers PHASES deliverable 5):** Update `user/scripts/CLAUDE.md` §788 "Cycle-counter advance: two orthogonal triggers" so it describes the post-`e91bd305` wiring: `advance_cycle_bracket_counter` at `--cycle-end` is the live forward-budget authority for Agent dispatches; the `--repeat-count` probe path is a PEEK (no forward-advance).

**TDD:** no (documentation edit — no testable behavior).

**Files to create/modify:**
- `user/scripts/CLAUDE.md` — in the "Cycle-counter advance: two orthogonal triggers" section [VERIFY: grep -n "Cycle-counter advance: two orthogonal triggers" user/scripts/CLAUDE.md]: remove/rewrite the stale claim that trigger 2 (`advance_forward_cycle`) is "ALSO the authoritative forward-advance on the `--repeat-count` real-skill probe path" [VERIFY: grep -n "ALSO the authoritative" user/scripts/CLAUDE.md] and "the state-change advance is wired into ... the `--repeat-count` real-skill probe path" [VERIFY: grep -n "real-skill probe path" user/scripts/CLAUDE.md]. Replace with: the probe path is now a **PEEK** (no forward-advance — that advance was REMOVED by `e91bd305`); the live budget authority for Agent dispatches is `advance_cycle_bracket_counter` at `--cycle-end` (one `--cycle-begin`/`--cycle-end` bracket == one dispatch). Keep the trigger-1 (consume-oracle `advance_run_counters`) description and the trigger-2 (state-change `advance_forward_cycle`, still serving `--apply-pseudo` forward-advancing pseudo-skills) description accurate; do NOT claim `consume_gate` exists (it is removed in WU-1).

**Implementation goal:** The §788 doc block accurately reflects the shipped wiring — a reader learns that `forward_cycles` advances at the `--cycle-end` dispatch bracket for Agent dispatches and at `--apply-pseudo` for inline pseudo-skills, and that the `--repeat-count` probe path advances nothing (PEEK). No mention of a `consume_gate` forward-advance trigger survives.

**Spec requirements:** SPEC `## Residual Work` item 3; `## Evidence Collected → Related Documentation` (the stale-doc note); `## Affected Area` (Docs row).

**Batch:** 2 (sequenced after WU-1 so the doc describes the final code state — disjoint file from WU-1, but ordered so the prose matches the landed removal).

#### Batch Overview

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | None (markers.py + test_markers.py — both owned solely by WU-1) |
| 2 | WU-2 | Solo | None (CLAUDE.md only) |

WU-1 and WU-2 touch disjoint files (no overlap), but are sequenced (WU-1 → WU-2) so the doc reconcile in WU-2 describes the final post-removal code state. Neither is a slow shared backend build (Python + docs), so there is no build-contention concern.

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md/PHASES.md top-level `**Status:**` to `Fixed` and writes `FIXED.md` after the validation tail passes — never authored as a work unit here. When both WUs land and the four gates in "Plan-specific execution notes" pass, `/execute-plan` sets the plan-part `status:` to `Complete`; the top-level PHASES `**Status:**` moves to `In-progress` (implementation done, validation pending) and the state machine routes onward.
