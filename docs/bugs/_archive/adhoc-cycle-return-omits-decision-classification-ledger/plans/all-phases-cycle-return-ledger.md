---
kind: implementation-plan
feature_id: adhoc-cycle-return-omits-decision-classification-ledger
status: Complete
created: 2026-07-19
complexity: mechanical
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\adhoc-cycle-return-omits-decision-classification-ledger\plans\all-phases-cycle-return-ledger.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — adhoc-cycle-return-omits-decision-classification-ledger (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\adhoc-cycle-return-omits-decision-classification-ledger\PHASES.md` (2 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\adhoc-cycle-return-omits-decision-classification-ledger\SPEC.md`

**Total phases:** 2
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (e.g. a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and any local `## Component Reference Card` override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (override the contract's default gates)

This is the **claude-config harness-config repo** — there is NO `/msbuild`/`/mstest`/`nx` build. All work units are **pure documentation edits** to skill/component markdown files. The quality gates for EVERY batch are the deterministic projection/lint checks below, run from the repo root (`C:\Users\Jacob\source\repos\claude-config`):

```bash
python ~/.claude/scripts/project-skills.py        # re-expand !cat component refs into skills-projected/ (must succeed)
python ~/.claude/scripts/lint-skills.py           # broken injections / embedded patterns (must be clean)
python3 user/scripts/lazy_parity_audit.py --repo-root .   # bug-axis coupled-pair parity (must exit 0) — Phase 2 only
python3 ~/.claude/scripts/validate-plan.py --structural docs/bugs/_archive/adhoc-cycle-return-omits-decision-classification-ledger/PHASES.md   # structural gate on PHASES.md (must exit 0)
```

- **No TDD test files** exist for markdown prose. "Tests" here = the grep-based Minimum Verifiable Behavior checks in PHASES.md plus the projection/lint/parity gates above. There is no red→green test cycle; each WU's acceptance is the deterministic presence check named in its scope.
- **MCP runtime: not-required** (per PHASES.md header) — no `/mcp-test` runtime boot; the state machine's validation tail handles the no-MCP routing.
- **Commit + push to `main`** after each phase's WUs land and the gates above are green (claude-config push policy: sync remote every commit). Stage ONLY the paths this plan changed (pathspec-scoped) — never `git add -A` (a concurrent writer's `sentinel-frontmatter.md` change may be present in the tree; do not absorb it).

## References — Upstream Artifacts

(none — this bug has no completed hard upstream dependencies.)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | this bug  | P1 | Add ledger to authoritative return contract | — | Solo (WU-1) |
| 2    | this bug  | P2 | Close bug-axis skill-body mandate gap | P1 (wording reference) | Yes (WU-2, WU-3 file-disjoint) |

## Work Units

- [x] WU-1 — Add Decision-Classification Ledger to cycle-base-prompt.md item 4 REPORT (both hard-contract blocks)
- [x] WU-2 — Add ledger return mandate to spec-bug/SKILL.md
- [x] WU-3 — Add ledger return mandate to plan-bug/SKILL.md + confirm bug-axis parity

---

### Phase: this bug P1 — Add the ledger to the authoritative return contract (root fix)

**Goal:** Make the Decision-Classification Ledger a required return element of the batch cycle-subagent's REPORT contract, so decision-bearing cycles emit it and the Step 1d.5 input-audit runs its stronger diff-vs-ledger cross-check instead of the diff-only fallback.
**Entry criteria:** None (first phase).
**SPEC.md references:** "Recommended Fix Scope" item 1; "Proven Findings" serving-path trace (fix site = `cycle-base-prompt.md` item 4 REPORT, both `@section hard-contract` blocks).

#### WU-1 — Add ledger requirement to cycle-base-prompt.md item 4 REPORT (both hard-contract blocks)

- **Scope:** In `user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`, extend item 4 "REPORT" in BOTH `@section hard-contract` blocks (workstation, item-4 at line ~575 `[VERIFY: grep -n "REPORT — one paragraph" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md]`; cloud, item-4 at line ~621 — same grep returns both) to require the `### Decision-Classification Ledger` section on decision-bearing cycles. Mirror the shape of the NEEDS_INPUT disposition already mandated in the same item: name the decision-bearing cycles (`/spec`, `/spec-phases`, `/write-plan`, `/add-phase`, `/plan-feature`, `/spec-bug`, `/plan-bug`; the cloud block already adds `/retro` to its NEEDS_INPUT set — keep the cloud's own enumeration convention), state the return summary MUST carry a `### Decision-Classification Ledger` section, and give the empty-ledger fallback line `_(no decisions surfaced this cycle — auto-finalized)_`. Keep it concise (the REPORT item is a ≤8-line paragraph contract — add a sentence or two, do not bloat it).
- **TDD:** no (documentation edit; verification is the grep MVB + projection/lint gate).
- **Files to create/modify:** `user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`
- **Implementation goal:** After the edit, both hard-contract item-4 REPORT blocks name the ledger as a required return element for decision-bearing cycles. Reuse the existing NEEDS_INPUT-disposition sentence directly above as the structural model — do NOT invent a new format; the ledger table shape itself lives in the skill bodies (`spec/SKILL.md` "Decision-Classification Ledger" `[VERIFY: grep -n "Decision-Classification Ledger" user/skills/spec/SKILL.md]`), so the REPORT contract only needs to REQUIRE the section, not redefine the table.
- **Spec requirements:** SPEC "Recommended Fix Scope" item 1 — "make the ledger a hard element of the authoritative return contract".
- **Batch:** 1 (solo).
- **Acceptance (deterministic):** `grep -c "Decision-Classification Ledger" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` returns ≥ 2 (was 0 in the return-contract sections); `project-skills.py` + `lint-skills.py` clean.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | N/A             |

---

### Phase: this bug P2 — Close the bug-axis skill-body mandate gap

**Goal:** Add the Decision-Classification Ledger return mandate to the bug-axis planning skill bodies (`spec-bug`, `plan-bug`) so they match the feature axis, satisfying the bug-axis parity contract.
**Entry criteria:** Phase 1 wording (the canonical decision-bearing-cycles enumeration + the requirement phrasing) is settled — Phase 2 authors its mandates to match. Phases touch disjoint files, so P2 is independently landable, but authored consistently with P1.
**SPEC.md references:** "Recommended Fix Scope" item 2; "Affected Area" (bug-axis planning skills have no ledger mandate); coupled-pair obligation note (`spec-bug`↔`spec`, `plan-bug`↔`plan-feature`).

#### WU-2 — Add ledger return mandate to spec-bug/SKILL.md

- **Scope:** Add a "Decision-Classification Ledger (MANDATORY return under `--batch`)" block to `user/skills/spec-bug/SKILL.md`, mirroring `spec/SKILL.md:117-135` `[VERIFY: grep -n "Decision-Classification Ledger" user/skills/spec/SKILL.md]`: the same `### Decision-Classification Ledger` markdown table (`# | Decision | Classification | Chosen option | Surfaced via | Rationale`), the `product-behavior ⇒ Surfaced via must be NEEDS_INPUT.md` rule, the empty-ledger fallback line, and the "ledger lives in the return summary, not a committed doc" note. Adapt vocabulary to the bug axis (investigation / root-cause-scope decisions; `NEEDS_INPUT.md` sentinel). Place it near `spec-bug`'s existing `--batch` / NEEDS_INPUT halt-protocol section for coherence.
- **TDD:** no (documentation edit).
- **Files to create/modify:** `user/skills/spec-bug/SKILL.md`
- **Implementation goal:** `spec-bug/SKILL.md` carries the ledger mandate mirroring the feature-axis `spec` skill, so a `/spec-bug --batch` cycle knows to emit the ledger in its return summary. Reuse the `spec/SKILL.md` block as the template — do NOT re-derive the table format.
- **Spec requirements:** SPEC "Recommended Fix Scope" item 2.
- **Batch:** 2.
- **Acceptance (deterministic):** `grep -l "Decision-Classification Ledger" user/skills/spec-bug/SKILL.md` lists the file (was absent); `project-skills.py` + `lint-skills.py` clean.

#### WU-3 — Add ledger return mandate to plan-bug/SKILL.md + confirm bug-axis parity

- **Scope:** Add the analogous ledger return mandate to `user/skills/plan-bug/SKILL.md`, mirroring `plan-feature/SKILL.md:114-126` `[VERIFY: grep -n "Decision-Classification Ledger" user/skills/plan-feature/SKILL.md]` ("same contract as `/spec --batch`"), covering the decisions a bug planning cycle considers (phase boundaries, partition cuts, helper/anchor choices). Then run `python3 user/scripts/lazy_parity_audit.py --repo-root .` `[VERIFY: ls user/scripts/lazy_parity_audit.py]` and confirm exit 0. If the audit flags the new heading as an unregistered divergence, update `user/scripts/lazy-parity-manifest.json` `[VERIFY: ls user/scripts/lazy-parity-manifest.json]` to register it as a mirrored/diverged block so the audit returns to exit 0 (do NOT pre-edit the manifest — only touch it if the audit requires it).
- **TDD:** no (documentation edit + deterministic parity gate).
- **Files to create/modify:** `user/skills/plan-bug/SKILL.md`; conditionally `user/scripts/lazy-parity-manifest.json` (only if the parity audit requires registration).
- **Implementation goal:** `plan-bug/SKILL.md` carries the ledger mandate mirroring `plan-feature`, and `lazy_parity_audit.py --repo-root .` exits 0 (bug-axis coupled pairs `spec-bug`↔`spec` and `plan-bug`↔`plan-feature` stay in sync). Reuse the `plan-feature/SKILL.md` block as the template.
- **Spec requirements:** SPEC "Recommended Fix Scope" item 2 + coupled-pair obligation note.
- **Batch:** 2.
- **Acceptance (deterministic):** `grep -l "Decision-Classification Ledger" user/skills/plan-bug/SKILL.md` lists the file (was absent); `python3 user/scripts/lazy_parity_audit.py --repo-root .` exits 0; `project-skills.py` + `lint-skills.py` clean.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 2     | WU-2, WU-3 | Yes (disjoint: spec-bug/SKILL.md vs plan-bug/SKILL.md; the parity audit in WU-3 reads both but WU-3 only WRITES plan-bug + optionally the manifest) | None |

**Note on Batch 2 parallelism:** WU-2 and WU-3 write disjoint skill files. WU-3's parity audit READS both skills, so if run truly concurrently the audit may observe WU-2 mid-flight — the executor should let WU-2 land first (or run the parity audit as the batch's post-step after both WUs complete) so the audit sees the final state of both. Either ordering is acceptable; the batch-level gate re-runs the parity audit regardless.

---

**Completion note (gate-owned):** the `__mark_fixed__` gate owns the SPEC/PHASES top-level `**Status:**` flip to Fixed, the `FIXED.md` receipt, and the archive move — this plan never writes them. When the last phase's WUs land and the gates are green, `/execute-plan` sets the top-level PHASES `**Status:**` to `In-progress` (implementation done, validation pending) and flips this plan-part frontmatter `status:` to `Complete`; the state machine then routes to the validation tail.
