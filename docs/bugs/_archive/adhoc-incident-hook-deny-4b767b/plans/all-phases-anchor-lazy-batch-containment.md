---
kind: implementation-plan
feature_id: adhoc-incident-hook-deny-4b767b
status: Complete
created: 2026-07-06
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-06.
> To execute: `/execute-plan /home/user/claude-config/docs/bugs/_archive/adhoc-incident-hook-deny-4b767b/plans/all-phases-anchor-lazy-batch-containment.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — adhoc-incident-hook-deny-4b767b (anchor `_LAZY_BATCH_RE` to a command-segment start)  (v2)

**PHASES.md files:**
- `/home/user/claude-config/docs/bugs/_archive/adhoc-incident-hook-deny-4b767b/PHASES.md` (adhoc-incident-hook-deny-4b767b, 1 phase)

**SPEC.md files:**
- `/home/user/claude-config/docs/bugs/_archive/adhoc-incident-hook-deny-4b767b/SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (e.g. a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and any local `## Component Reference Card` override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

### Plan-specific execution notes (override the contract's default gates)

claude-config is the **harness repo** — it has NO Tauri/MCP dev runtime and NO `/msbuild`/`/mstest`/`nx` gates. The quality gates for this plan are:

- **Primary gate (hook + tests):** `python3 user/scripts/test_hooks.py` — the in-file test harness (NOT pytest). It must exit 0 (all tests pass), INCLUDING the new benign-reference-allow test and the preserved/widened invocation-deny tests.
- **Manual serving-path reproduction (SPEC Reproduction Steps):** exercise the REAL hook directly —
  ```bash
  export LAZY_STATE_DIR="$(mktemp -d)"
  # benign reference → must ALLOW (no deny in output):
  printf '{"tool_name":"Bash","agent_id":"agent-xyz","cwd":"%s","tool_input":{"command":"cat user/skills/lazy-batch/SKILL.md"}}' "$PWD" | bash user/hooks/lazy-cycle-containment.sh
  # real runaway → must still DENY (CORRECTIVE / lazy-batch-invocation):
  printf '{"tool_name":"Bash","agent_id":"agent-xyz","cwd":"%s","tool_input":{"command":"/lazy-batch 5"}}' "$PWD" | bash user/hooks/lazy-cycle-containment.sh
  ```
- There is no MCP/runtime validation tail for this bug — the `**Status:**` flip + `FIXED.md` receipt are `__mark_fixed__`-gate-owned and fire once the PHASES runtime-verification rows pass.

## References — Upstream Artifacts

(none — this bug has no completed hard upstream dependencies. The reuse SOURCE — not a dependency — is `user/hooks/build-queue-enforce.sh:111-138`, the proven `_CMD_START` command-position anchor this fix mirrors.)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | adhoc-incident-hook-deny-4b767b | P1 | Anchor `_LAZY_BATCH_RE` recursion trip (fix + regression net) | — | Solo |

## Work Units

- [x] WU-1 — Anchor `_LAZY_BATCH_RE` to a command-segment start + regression tests

---

### Phase: adhoc-incident-hook-deny-4b767b P1 — Anchor `_LAZY_BATCH_RE` recursion trip (fix + regression net)

**Goal:** Replace the unanchored `_LAZY_BATCH_RE` substring match in `lazy-cycle-containment.sh` with a command-position–anchored regex pair so benign `lazy-batch*` file-path references by a cycle subagent are ALLOWED while actual nested `/lazy-batch` invocations stay DENIED — closing the recurring `lazy-batch-invocation` false-deny incident.

**Entry criteria:** none (first and only phase; working tree clean).

**SPEC.md references:** `## Proven Findings` (validated fix direction — the `_CMD_START` anchored direct + nested-spawn pair), `## Reproduction Steps` (the serving-path the regression test binds to), `## Affected Area` (the two files).

#### WU-1 — Anchor `_LAZY_BATCH_RE` to a command-segment start + regression tests

**Scope (covers the phase's Deliverables):**
- Introduce the command-position anchor pair in `lazy-cycle-containment.sh`, reusing the exact form from `build-queue-enforce.sh` [VERIFY: grep -n "_CMD_START" user/hooks/build-queue-enforce.sh] — `_ENV_PREFIX = r"(?:[A-Za-z_][A-Za-z0-9_]*=\S+\s+)*"` and `_CMD_START = r"(?:^|[\n;&|({])\s*" + _ENV_PREFIX`.
- Replace the unanchored `_LAZY_BATCH_RE` [VERIFY: grep -n "_LAZY_BATCH_RE" user/hooks/lazy-cycle-containment.sh] (def at `:186`) with a PAIR:
  - Direct form: `re.compile(_CMD_START + r"/lazy(?:-bug)?-batch(?:-cloud)?\b(?!/)")` — `(?!/)` so a `.../lazy-batch/...` path segment never matches.
  - Nested-spawn form: `re.compile(_CMD_START + r"claude\b[^\n;&|]*/lazy(?:-bug)?-batch(?:-cloud)?\b")` — `claude` ALSO anchored to a command-segment start so the `.claude/` path component does not false-match; preserves the `claude -p '/lazy-batch 25'` deny the existing test covers.
- Update the trip at `:392-393` so the `_deny(CORRECTIVE, "lazy-batch-invocation")` fires when EITHER regex matches. The signature token `"lazy-batch-invocation"` MUST stay byte-identical (it is `incident-scan.py`'s cluster key).
- Preserve fail-OPEN and deny-is-JSON invariants (per `user/hooks/CLAUDE.md`) — the change is regex-only; introduce no new error path and no exit-code deny.
- Add benign-reference-allow test coverage + widen the runaway-deny coverage in `test_hooks.py` [VERIFY: grep -n "test_containment_agentid_present_denies_lazy_batch_invocation" user/scripts/test_hooks.py], and register every new test in the registration list near `:5593`.

**TDD:** yes.

**Test files:** `user/scripts/test_hooks.py` (in-file harness — extend, do not create a new file).

**Test expectations (write these FIRST; they are RED against the current unanchored regex):**
1. `test_containment_agentid_present_allows_lazy_batch_path_reference` — a SUBAGENT payload (`agent_id=_SUBAGENT_AGENT_ID`) whose Bash `command` is each of:
   - `cat user/skills/lazy-batch/SKILL.md`
   - `cat ~/.claude/skills/lazy-batch/SKILL.md`
   - `grep -rn foo repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md`
   - `ls user/skills/lazy-bug-batch/`
   - `git add user/skills/lazy-batch/SKILL.md`
   MUST be ALLOWED — assert `_containment_decision(result) != "deny"` for each. (RED today: benign `cat` currently DENIES with the `lazy-batch-invocation` signature — this is the exact incident.)
2. Widened runaway deny coverage — either extend the existing `test_containment_agentid_present_denies_lazy_batch_invocation` command tuple or add `test_containment_agentid_present_denies_lazy_batch_invocation_extra_forms`: SUBAGENT payloads for `cd foo && /lazy-batch`, `/lazy-bug-batch 10`, and `claude --dangerously-skip-permissions -p '/lazy-bug-batch 10'` MUST all DENY (`== "deny"`). Keep the ORIGINAL test's `claude -p '/lazy-batch 25'` and `/lazy-batch 10` deny assertions intact.
   Reuse the existing helpers `_bash_preToolUse_json`, `_run_containment`, `_containment_decision`, `_SUBAGENT_AGENT_ID`, `_guard` (do not re-invent scaffolding — mirror the neighboring `test_containment_agentid_present_denies_lazy_batch_invocation`).
   RED-for-the-right-reason: the benign-reference test must fail because the CURRENT regex denies (not a compile/import error). Confirm RED before implementing.

**Implementation goal:** the anchored pair makes `_containment_decision` return ALLOW for every benign-reference command and DENY for every runaway form. `python3 user/scripts/test_hooks.py` exits 0 with the new + preserved tests green. The manual SPEC reproduction (benign `cat` → allow; `/lazy-batch 5` → CORRECTIVE deny) holds.

**Spec requirements:** SPEC `## Proven Findings` — "replace the single unanchored regex with a command-position–anchored pair, mirroring `build-queue-enforce.sh`'s `_CMD_START` approach"; the design constraint that a naive `_CMD_START` anchor on the DIRECT form alone would break the `claude -p` case (hence the two-signal pair).

**Files to create/modify:**
- `user/hooks/lazy-cycle-containment.sh` — regex pair + trip update (`:186`, `:392-393`). Do NOT touch `_LAZY_SKILL_RE`, `_STATE_PY_RE`, `LIFECYCLE_PATTERNS`.
- `user/scripts/test_hooks.py` — new benign-reference-allow test + widened runaway coverage + registration near `:5593`. Do NOT weaken the existing `test_containment_agentid_present_denies_lazy_batch_invocation` assertions.

**Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | N/A (single WU) |

**Runtime verification (PHASES-owned, ticked by manual/harness reproduction — NOT by `/execute-plan`):** see PHASES.md Phase 1 `**Runtime Verification**` rows (benign-reference serving-path allow; true-positive deny preserved). These are exercised by `python3 user/scripts/test_hooks.py` + the manual SPEC reproduction, not closed as plan WUs.

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md/PHASES.md **Status:** and writes `FIXED.md` — never authored as a WU here.
