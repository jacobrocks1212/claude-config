---
kind: fix-plan
feature_id: run-end-gate-refusals-no-telemetry-event
status: Complete
created: 2026-07-11
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-11.
> To execute: `/execute-plan docs/bugs/_archive/run-end-gate-refusals-no-telemetry-event/plans/fix-run-end-gate-refusal-telemetry.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — run-end-gate-refusals-no-telemetry-event (v2)

**PHASES.md files:**
- `docs/bugs/_archive/run-end-gate-refusals-no-telemetry-event/PHASES.md` (run-end-gate-refusals-no-telemetry-event, 1 phase)

**SPEC.md files:**
- `docs/bugs/_archive/run-end-gate-refusals-no-telemetry-event/SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (e.g. a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and any local `## Component Reference Card` override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config — override the contract's default gates)

claude-config is a **config/harness repo with no Tauri/MCP dev runtime** and no `/msbuild`/`/mstest` gates. For THIS plan the quality gates are Python, run from the repo root:

- **Tests:** `python user/scripts/test_lazy_core.py` — must pass, including the new run-end-refusal telemetry tests and the `test_no_orphaned_test_functions` orphan guard.
- **Coupled-pair parity:** `python user/scripts/lazy_parity_audit.py --repo-root .` — must exit 0. The three telemetry emissions MUST be mirrored across `lazy-state.py` and `bug-state.py` or `audit_state_script_parity` fails.
- **Doc-drift sanity:** `python user/scripts/doc-drift-lint.py --repo-root .` — must exit 0 (no doc claims change here; sanity check).

There is **no MCP/runtime validation step** for this repo — the pytest tests reading back the telemetry ledger ARE the runtime-observable verification (they arm a live marker, drive a real `--run-end` refusal via subprocess, and assert the appended `gate-refusal` event). Do NOT author or expect a `/mcp-test` / `VALIDATED.md` step.

**Commit policy (`.claude/skill-config/commit-policy.md`):** Conventional Commits (`fix(lazy-state): …`), **NO AI attribution footer**, push allowed (personal repo). Commit after the phase completes with gates green. The working tree may carry pre-existing unrelated changes (lint-skills.py / project-skills.py / skill_repos.py) — do NOT sweep those into this fix's commit; stage only the files this plan touches.

## References — Upstream Artifacts

(none — this bug has no completed hard upstream dependencies; the SPEC carries no `**Depends on:**` block.)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | run-end-gate-refusals-no-telemetry-event | P1 | Emit gate-refusal telemetry at the three run-end refusal sites (coupled pair) + tests | — | Solo |

## Work Units

- [x] WU-1 — Emit `gate-refusal` telemetry at the three run-end refusal sites in both state scripts + subprocess tests

---

### Phase: run-end-gate-refusals-no-telemetry-event P1 — Emit gate-refusal telemetry at the three run-end refusal sites (coupled pair) + tests

**Goal:** Add an observability-only `append_telemetry_event("gate-refusal", …)` emission at each of the three `--run-end` refusal sites in BOTH `lazy-state.py` and `bug-state.py`, mirrored (parity-audited), with subprocess tests asserting each refusal path appends the event — refusal behavior and exit codes unchanged.

**Entry criteria:** None (single phase, no prior phase).

**SPEC.md references:**
- `## Verified Symptom` — the three run-end refusal sites (unacked-hardening, efficacy-flush-missing, checkpoint-auth) that refuse (exit 1, marker kept) without emitting telemetry.
- `## Fix Scope (Concluded)` — emit `append_telemetry_event("gate-refusal", item_id=…, data={"gate": …, "op": "--run-end", "reason": <short>})` at each site; coupled-pair mirror; tests in `test_lazy_core.py`.
- `## Decisions` — D1 (reuse the `gate-refusal` kind with a `data.gate` discriminator), D2 (observability-only, fail-open, zero state side effects, gate behavior unchanged).
- **Drift correction (from PHASES `## Validated Assumptions`):** use `item_id=None` (run-level) — no `feature_id`/`bug_id` exists on the run marker at these sites; matches the existing run-level `run-end`/`run-start` emissions and `refuse_run_start_clobber`.

#### WU-1 — Emit gate-refusal telemetry at the three run-end refusal sites in both state scripts + subprocess tests

**Scope (covers the whole phase — these deliverables are tightly coupled: the coupled-pair parity gate binds both scripts, and the tests bind to the emissions):**
- [x] `lazy-state.py` — emit at the unacked-hardening refusal (`data.gate = "unacked-hardening"`), the efficacy-flush refusal (`"efficacy-flush-missing"`), and the checkpoint-auth refusal (`"checkpoint-auth"`), each immediately before the existing `return 1`, inside the refusal branch.
- [x] `bug-state.py` — mirror all three emissions at the corresponding refusal branches (identical `data.gate` strings + call shape).
- [x] Tests (`test_lazy_core.py`): assert each of the three refusal paths appends a `gate-refusal` event with the matching `data.gate`, for BOTH scripts, plus a guard that a successful `--run-end` emits `run-end` (not `gate-refusal`).

**TDD:** yes.

**Files to create/modify (implementation):**
- `user/scripts/lazy-state.py` — three `lazy_core.append_telemetry_event(...)` calls inserted in the `if args.run_end:` block (`main()`, block begins line ~11855). Refusal sites: unacked-hardening ~11869–11885 (before its `return 1`), efficacy-flush ~11912–11929, checkpoint-auth ~11957–11985. Reuses `lazy_core.append_telemetry_event` [VERIFY: grep -n "def append_telemetry_event" user/scripts/lazy_core.py]; the pass-path already calls it for the `"run-end"` event as the pattern to copy [VERIFY: grep -n 'append_telemetry_event("run-end"' user/scripts/lazy-state.py].
- `user/scripts/bug-state.py` — three mirrored calls in its `if args.run_end:` block (`main()`, begins line ~7239). Refusal sites: ~7252–7267, ~7292–7309, ~7323–7344. [VERIFY: grep -n "if args.run_end" user/scripts/bug-state.py]

**Test files (TDD):**
- `user/scripts/test_lazy_core.py` — new `def test_*` functions, each registered via a `_TESTS = _TESTS + [("test_name", test_name)]` block [VERIFY: grep -n "^_TESTS = \[" user/scripts/test_lazy_core.py] (the orphan guard `test_no_orphaned_test_functions` fails any top-level `def test_*` not in a `_TESTS` tuple [VERIFY: grep -n "def test_no_orphaned_test_functions" user/scripts/test_lazy_core.py]).

**Test expectations (write these RED first — they fail because no `gate-refusal` line is appended on a run-end refusal today, then GREEN after the emissions land):**
Mirror the existing telemetry-append pattern `test_telemetry_append_envelope_shape_and_now_injection` [VERIFY: grep -n "def test_telemetry_append_envelope_shape_and_now_injection" user/scripts/test_lazy_core.py] — isolate the state dir, arm a live run marker with `lazy_core.write_run_marker(...)` [VERIFY: grep -n "def write_run_marker" user/scripts/lazy_core.py] (so the marker-gated append actually fires — the marker is still present at refusal time because the gates keep it), drive the target refusal via a subprocess `python <script> --run-end …`, then read the ledger with `lazy_core.read_telemetry_events()` [VERIFY: grep -n "def read_telemetry_events" user/scripts/lazy_core.py] and assert.

Per refusal path (for EACH of `lazy-state.py` and `bug-state.py`):
1. **unacked-hardening** — seed the deny ledger so `pending_hardening() > 0` [VERIFY: grep -n "def pending_hardening" user/scripts/lazy_core.py], invoke `--run-end` WITHOUT `--ack-unhardened`; assert the process returns 1 (refusal unchanged, marker NOT deleted) AND the last telemetry event is `{"event": "gate-refusal", "data": {"gate": "unacked-hardening", ...}}`.
2. **efficacy-flush-missing** — clear gate 1 (`--ack-unhardened` or zero pending), ensure no efficacy breadcrumb present [VERIFY: grep -n "def efficacy_breadcrumb_present" user/scripts/lazy_core.py], invoke `--run-end` WITHOUT `--efficacy-skip-authorized`; assert refusal (exit 1) AND last event `gate` == `"efficacy-flush-missing"`.
3. **checkpoint-auth** — clear gates 1 and 2 (`--ack-unhardened --efficacy-skip-authorized`), arm an ATTENDED marker, invoke `--run-end --reason checkpoint` WITHOUT `--operator-authorized`; assert refusal (exit 1) AND last event `gate` == `"checkpoint-auth"`.
4. **over-emission guard** — a successful `--run-end` (all gates satisfied/overridden) emits the existing `"run-end"` event and NO `"gate-refusal"` event.

Note the gate-ordering dependency above: later gates are only reachable once earlier gates are satisfied. The in-script `telemetry-ledger-chokepoints` `--test` fixture (`lazy-state.py` ~10190–10380 / `bug-state.py` ~5599–5771) is the reference for subprocess-driving a run-end refusal and asserting the appended event; extending that per-script fixture instead of / in addition to `test_lazy_core.py` is acceptable as long as each refusal path is asserted.

**Implementation goal:** At each of the three refusal branches, immediately before the existing `return 1` (inside the `if not <override>:` block), insert:

```python
lazy_core.append_telemetry_event(
    "gate-refusal",
    item_id=None,
    data={"gate": "<unacked-hardening|efficacy-flush-missing|checkpoint-auth>", "op": "--run-end", "reason": "<short>"},
)
```

Constraints:
- The call MUST sit INSIDE the refusal branch, immediately before `return 1` — never before the guarding `if`, or it would fire on the pass path too.
- `item_id=None` (run-level) — matches the existing run-level emissions; no item id exists on the run marker here.
- The three `data.gate` discriminator strings MUST be byte-identical between `lazy-state.py` and `bug-state.py` (D1 — one aggregatable signal family; `pipeline_visualizer/trends.py` and `kpi-scorecard.py` already aggregate `gate-refusal`).
- Do NOT change any refusal decision, exit code, message, or marker handling. The emission is purely additive; `append_telemetry_event` is already marker-gated and fail-open, so no extra error handling is needed.
- Do NOT write new telemetry code — reuse `lazy_core.append_telemetry_event` as-is.
- **Out of scope:** the adjacent terminal-reason gate (`elif reason == "terminal": … return 1`, `lazy-state.py` ~11988–12009 / `bug-state.py` ~7346–7362) is NOT part of this fix — the SPEC names exactly three gates. Do not add an emission there.

**Spec requirements:** SPEC `## Fix Scope (Concluded)` bullets 1–4; Decisions D1 (reuse `gate-refusal` kind + `data.gate` discriminator) and D2 (observability-only, fail-open, zero state side effects, gate behavior unchanged).

**Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | N/A (single WU) |

**Runtime Verification** *(these are exercised by the WU-1 subprocess tests themselves — this repo has no separate MCP/runtime gate; listed for completeness, closed by the passing test suite):*
- [ ] <!-- verification-only --> After a real `--run-end` refusal on each of the three gates (both scripts), `lazy_core.read_telemetry_events()` shows a `gate-refusal` event carrying the matching `data.gate`, while the refusal itself is unchanged (exit 1, marker kept).

**Completion (gate-owned):** the `__mark_fixed__` gate flips `SPEC.md` **Status:** to `Fixed`, writes `FIXED.md`, updates ROADMAP, and archives once this phase's deliverables + quality gates are green. Do NOT author any status-flip / receipt / archive checkbox row.
