---
kind: implementation-plan
feature_id: merged-head-oracle-per-signal-supplement-churn
status: Complete
created: 2026-07-19
complexity: mechanical
phases: [3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan {this-path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 2. Sibling parts:
> - part 1: `C:/Users/Jacob/source/repos/claude-config/docs/bugs/_archive/merged-head-oracle-per-signal-supplement-churn/plans/all-phases-operator-defer-part-1.md`
> - part 2: `C:/Users/Jacob/source/repos/claude-config/docs/bugs/_archive/merged-head-oracle-per-signal-supplement-churn/plans/all-phases-operator-defer-part-2.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — merged-head-oracle-per-signal-supplement-churn (part 2: docstrings, parity, battery)  (v2)

**PHASES.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/bugs/_archive/merged-head-oracle-per-signal-supplement-churn/PHASES.md` (Phase 3 of 3)

**SPEC.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/bugs/_archive/merged-head-oracle-per-signal-supplement-churn/SPEC.md`

**Total phases in this part:** 1 (Phase 3)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL, the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

This is **claude-config** (a harness-config repo whose gates are Python/`--test`/parity, NOT `/msbuild`/`/mstest`); the `## Plan-specific execution notes` below override the contract's default gate rows.

## Plan-specific execution notes (claude-config gates)

- **This part is documentation + verification only.** It corrects docstrings/comments/prose that Phase 2 (part 1) made stale, confirms coupled-pair parity, and runs the full invariant battery. No production state-machine logic changes here.
- **Prerequisite:** part 1 (Phases 1–2) must be complete — the "ONLY thing that excludes such a feature" docstrings cannot be corrected truthfully until the supplement is removed and the feature branch models operator-defer.
- **Quality gates for this repo (run from repo root):**
  - `python3 user/scripts/lazy-state.py --test` and `python3 user/scripts/bug-state.py --test` (both green).
  - `python3 -m pytest user/scripts/tests/test_lazy_core/ -q`.
  - `python3 user/scripts/lazy_parity_audit.py --repo-root .` (exit 0 — the feature-side branch is a justified divergence or a mirrored surface).
  - `python3 user/scripts/doc-drift-lint.py --repo-root .` (the guard that CLAUDE.md prose edits stay consistent with reality).
  - `python3 ~/.claude/scripts/lint-skills.py` (skills lint).
- **No MCP runtime.** Verification is the deterministic battery above.
- **Commit to `main`** (re-read `.claude/skill-config/commit-policy.md` first); stage only changed paths.

## References — Upstream Artifacts

> | Upstream feature | Kind | Path | Why this plan references it |
> |------------------|------|------|------------------------------|
> | (none — this bug has no completed hard upstream dependencies) | | | |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | operator-defer | P3 | Docstring correction, parity confirmation, full battery | part 1 (Phases 1–2 complete) | Solo (Batch 1) |

## Work Units

- [x] WU-3 — Correct docstrings/comments/prose, confirm parity, run the full invariant battery

---

### Phase: operator-defer P3 — Docstring correction, parity confirmation, and full gate battery

**Goal:** Update the docstrings/comments/prose that asserted the feature `compute_state` "ignores" operator-defer, confirm state-script parity treats the new feature branch correctly, and run the full claude-config invariant battery to certify no regression.
**Entry criteria:** part 1 complete (the supplement is removed and the feature `compute_state` models operator-defer).
**SPEC.md references:** `## Affected Area` rows "Docstrings" and "Parity + tests".

#### WU-3 — Correct docstrings/comments/prose, confirm parity, run the full invariant battery

- **Scope (PHASES Phase 3 deliverables):**
  - Update `docmodel.py::spec_dir_operator_deferred` docstring (the "the FEATURE `compute_state` … ignores it, so the merged-head oracle's file-predicate is the ONLY thing that excludes such a feature" / "the feature pipeline has no operator-`DEFERRED.md` dispatch branch" language) to state the feature `compute_state` now models operator-defer; note the predicate is still consumed by both `compute_state`s.
  - Update `depdag.py` merged-worklist docstring to reflect that the primary `is_dispatchable` re-inference now covers both pipelines.
  - Update the `lazy-state.py` `~:405–407` comment that documents the absent operator-defer branch as a "JUSTIFIED divergence" — it now describes the PRESENT branch.
  - Update the root `CLAUDE.md` / `user/scripts/CLAUDE.md` prose that describes the feature side as having "NO operator-`DEFERRED.md` branch (bug-pipeline-only — JUSTIFIED divergence)", so the docs no longer describe the now-closed divergence.
  - Confirm `lazy_parity_audit.py --repo-root .` is exit 0; add a divergence note to `lazy-parity-manifest.json` ONLY if the audit requires one for the new branch.
- **TDD:** no (documentation edits + verification via the parity audit and doc-drift lint, which are the mechanical backstops).
- **Files to create/modify:**
  - `user/scripts/lazy_core/docmodel.py` — the `spec_dir_operator_deferred` docstring (~lines 2299–2334). [VERIFY: grep -n "ONLY thing that excludes" user/scripts/lazy_core/docmodel.py]
  - `user/scripts/lazy_core/depdag.py` — the merged-worklist docstring (~lines 1437–1445). [VERIFY: grep -n "spec_dir_operator_deferred" user/scripts/lazy_core/depdag.py]
  - `user/scripts/lazy-state.py` — the `~:405–407` "no operator-DEFERRED branch" comment. [VERIFY: grep -n "JUSTIFIED divergence" user/scripts/lazy-state.py]
  - `CLAUDE.md` (repo root) and `user/scripts/CLAUDE.md` — parity-divergence prose, where it names this specific divergence. [VERIFY: grep -rn "no operator-DEFERRED\|operator-DEFERRED.md branch\|JUSTIFIED divergence" CLAUDE.md user/scripts/CLAUDE.md]
  - `user/scripts/lazy-parity-manifest.json` — ONLY if a divergence note is needed for the new branch (else leave untouched).
- **Implementation goal:** Every stale claim that the feature pipeline "has no operator-`DEFERRED.md` branch" / that the oracle file-predicate is "the ONLY thing that excludes such a feature" is corrected to reflect: the feature `compute_state` now models operator-defer (mirroring the bug pipeline), and the merged-head oracle's primary `is_dispatchable(scoped_probe)` re-inference covers both pipelines. The `spec_dir_operator_deferred` predicate STAYS and its docstring must still document that it is consumed by both `compute_state`s. Do NOT introduce new behavior — this WU is edits + verification only.
- **Spec requirements:** SPEC `## Affected Area` — "Docstrings" row (update the `spec_dir_operator_deferred` / `depdag` false-premise docstrings) and "Parity + tests" row (confirm justified divergence or mirrored surface per `audit_state_script_parity`; keep the R102 oracle regression green with the primary mechanism).
- **Batch:** 1.

> **Batch overview (Phase 3):**
>
> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-3      | Solo      | N/A             |

**Post-phase:** run the full invariant battery (`lazy-state.py --test`, `bug-state.py --test`, `pytest user/scripts/tests/test_lazy_core/`, `lazy_parity_audit.py --repo-root .`, `doc-drift-lint.py --repo-root .`, `lint-skills.py`). Commit + push to `main`. When this final phase's work lands, set the top-level PHASES.md `**Status:**` to `In-progress` (implementation done, validation pending) and let the state machine route to the validation tail.

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md `**Status:**` to Fixed, writes `FIXED.md`, and archives the bug dir once the validation tail passes — never authored as a checkbox here.

---

## Runtime Verification

*(No runtime-observable surface — documentation + deterministic verification only.)*

**MCP Integration Test Assertions:** N/A — no runtime-observable behavior; MCP runtime not-required (see PHASES.md header).
