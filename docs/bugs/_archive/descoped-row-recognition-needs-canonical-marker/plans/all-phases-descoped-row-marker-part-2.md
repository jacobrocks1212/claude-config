---
kind: implementation-plan
feature_id: descoped-row-recognition-needs-canonical-marker
status: Complete
created: 2026-07-12
phases: [2]
complexity: mechanical
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-12.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\descoped-row-recognition-needs-canonical-marker\plans\all-phases-descoped-row-marker-part-2.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 2. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\descoped-row-recognition-needs-canonical-marker\plans\all-phases-descoped-row-marker-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\descoped-row-recognition-needs-canonical-marker\plans\all-phases-descoped-row-marker-part-2.md` (this file)
>
> Execute parts strictly in order — this part assumes part 1 (the `_DESCOPED_MARKER` constant) has already landed. Do NOT cross-reference siblings during execution beyond this stated precondition.

# Implementation Plan — descoped-row-recognition-needs-canonical-marker (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\descoped-row-recognition-needs-canonical-marker\PHASES.md` (descoped-row-recognition-needs-canonical-marker, 2 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\descoped-row-recognition-needs-canonical-marker\SPEC.md`

**Total phases:** 2 (this part covers Phase 2 only)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract. This repo has no `/msbuild`/`/nxbuild`/Tauri gate; the quality gate for this part is:

```bash
python3 user/scripts/test_lazy_core.py                      # pytest suite — new lockstep test
python ~/.claude/scripts/project-skills.py                   # re-expand !cat refs after the component edit
python ~/.claude/scripts/lint-skills.py                      # confirm no broken injections / capability regressions
```

Everything else the contract specifies that this plan does not override still applies.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies.) This part depends on **part 1 of this same plan series** (the `_DESCOPED_MARKER` constant in `user/scripts/lazy_core.py`), NOT on a separate feature/bug — do not start this part until part 1's commit has landed.

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | descoped-row-recognition-needs-canonical-marker | P2 | Producers emit the canonical marker + lockstep test | Part 1 (WU-2, `_DESCOPED_MARKER` must exist) | Solo (WU-3 then WU-4, sequential — TDD test-then-impl) |

### Phase: descoped-row-recognition-needs-canonical-marker P2 — Producers emit the canonical marker + lockstep test

**Goal:** Update the ad hoc descope-authoring guidance in `_components/completeness-policy.md` so future descope-in-place PHASES.md rows carry the canonical marker by name, and add a lockstep test proving the component references the SSOT constant.

**Entry criteria:** Part 1 complete (`_DESCOPED_MARKER` exists in `lazy_core.py`).

**SPEC.md references:** "Proven Findings → Fix scope" item 3 (producers emit the marker) and the remainder of item 4 (lockstep test analog of `test_ruvonly_marker_lockstep_producers_match_ssot`).

## Work Units

- [x] WU-3 — Lockstep test asserting `completeness-policy.md` references `_DESCOPED_MARKER` (TDD test)
- [x] WU-4 — Update `completeness-policy.md`'s descope-resolution guidance (implementation)

---

### WU-3 — Lockstep test asserting `completeness-policy.md` references `_DESCOPED_MARKER`

**Scope:** Add one new pytest fixture to `user/scripts/test_lazy_core.py`, co-located near the existing lockstep-test precedent `test_ruvonly_marker_lockstep_producers_match_ssot` [VERIFY: grep -n "def test_ruvonly_marker_lockstep_producers_match_ssot" user/scripts/test_lazy_core.py — line 24237], mirroring its EXACT structure.

**TDD:** yes — this test MUST fail against the CURRENT `completeness-policy.md` (which does not yet reference `_DESCOPED_MARKER` [VERIFY: grep -n "_DESCOPED_MARKER" user/skills/_components/completeness-policy.md — expect ZERO hits before this plan runs]) before WU-4 lands.

**Files to create/modify:**
- `user/scripts/test_lazy_core.py` — add `test_descoped_marker_lockstep_producer_matches_ssot` immediately after `test_ruvonly_marker_lockstep_producers_match_ssot`'s existing test body [VERIFY: grep -n "test_ruvonly_marker_lockstep_producers_match_ssot" user/scripts/test_lazy_core.py — def at 24237, registered in a `_TESTS = _TESTS + [` block at line 24280]. Register the new test in a sibling `_TESTS = _TESTS + [...]` append block immediately after that existing registration (do NOT edit the existing registration tuple — append a new one, mirroring the file's own append-block convention seen at lines 24280 and 32713/32766/32948/33117/33188).

**Files NOT to touch:** `user/skills/_components/completeness-policy.md` (WU-4 owns this file).

**Test expectations:**

```python
def test_descoped_marker_lockstep_producer_matches_ssot():
    """The descope-authoring guidance in completeness-policy.md references the
    canonical marker lazy_core._DESCOPED_MARKER BY VALUE — no divergent
    hardcoding. Mirrors test_ruvonly_marker_lockstep_producers_match_ssot."""
    _guard()
    marker = lazy_core._DESCOPED_MARKER

    assert _COMPLETENESS_POLICY_PATH.exists(), (
        f"missing producer component: {_COMPLETENESS_POLICY_PATH}"
    )
    text = _COMPLETENESS_POLICY_PATH.read_text(encoding="utf-8")

    assert marker in text, (
        "completeness-policy.md must reference the canonical descope marker "
        f"{marker!r} by value; not found"
    )
```

Resolve `_COMPLETENESS_POLICY_PATH` the same way `_PHASES_RUNTIME_VERIFICATION_PATH`/`_BLOCKED_RESOLUTION_PATH` are resolved near the existing lockstep test [VERIFY: grep -n "_PHASES_RUNTIME_VERIFICATION_PATH\s*=" user/scripts/test_lazy_core.py] — same base-dir-relative construction, pointed at `user/skills/_components/completeness-policy.md`. If those path constants are defined once near the top of the file (module-level), add `_COMPLETENESS_POLICY_PATH` alongside them rather than inlining the path string in the test body.

**Test commands to run for verification:**
```bash
python3 user/scripts/test_lazy_core.py
```
Confirm the new test FAILS (marker not yet present in the component) and `test_ruvonly_marker_lockstep_producers_match_ssot` itself is untouched and still passes.

---

### WU-4 — Update `completeness-policy.md`'s descope-resolution guidance

**Scope:** Edit `user/skills/_components/completeness-policy.md` so the two ad hoc descope-authoring sites the SPEC's Evidence Collected section identifies both instruct authoring the canonical marker.

**TDD:** no (pure documentation/prose edit — no testable logic; satisfies WU-3's test via string-presence, not behavior).

**Files to create/modify:**
- `user/skills/_components/completeness-policy.md`:
  1. In "Resolution rules by decision site" item 1 (cycle subagents) [VERIFY: grep -n "Cycle subagents (source suppression" user/skills/_components/completeness-policy.md — line ~28], add a sentence: when the resolved-in-cycle scope decision results in a PHASES.md row being struck through/dropped rather than implemented, the row MUST carry the canonical structural marker `<!-- descoped -->` (name it: "the SSOT constant `lazy_core:_DESCOPED_MARKER`") in ADDITION to the existing human-readable `**DROPPED**`/`**DESCOPED**`/`**WON'T-FIX**` note — the free-text note stays for human readability; the marker is for machine recognition.
  2. In item 2 (NEEDS_INPUT `class: scope` auto-resolution) [VERIFY: grep -n "NEEDS_INPUT.md carrying \`class: scope\`" user/skills/_components/completeness-policy.md — line ~33], add the identical instruction — this is the exact site the live incident (`live-settings-split-brain-disarms-enforcement-plane` PHASES line 128, per SPEC.md's Evidence Collected → Producers analysis) goes through.
  3. Add a worked-example row inline in the updated prose, e.g.:
     ```
     - [ ] ~~<text>~~ **DROPPED** <!-- descoped --> (decision N, NEEDS_INPUT.md resolution, <date>)
     ```
     so a future producer sees the exact shape, not just prose.
  4. Do NOT restate the literal marker string as an independent source of truth anywhere else in the file — reference `lazy_core:_DESCOPED_MARKER` by name in the prose sentence, with the literal `<!-- descoped -->` shown only in the worked-example code block (mirrors how the verification-marker precedent is typically documented — string shown once in an example, named by constant everywhere else).

**Implementation goal:** WU-3's new lockstep test passes; the 3 pre-existing `completeness-policy.md`-adjacent behaviors (items 3–5 of "Resolution rules by decision site") are untouched; `lint-skills.py` reports no new findings against this component.

**Spec requirements:** SPEC.md "Proven Findings → Fix scope" item 3 verbatim: "Producers emit the marker... referencing the SSOT constant BY NAME (never re-hardcoding the string)."

**Test commands to run for verification:**
```bash
python3 user/scripts/test_lazy_core.py
python ~/.claude/scripts/project-skills.py
python ~/.claude/scripts/lint-skills.py
```
All three must exit clean; the new lockstep test must now PASS.

---

## Batch overview (Phase 2)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|------------------|
| 1     | WU-3      | Solo      | N/A (test file only) |
| 2     | WU-4      | Solo      | N/A (component file only; sequenced after WU-3 per TDD) |

**Plan-specific execution notes:** Pure docs + a single string-presence assertion — no build, no MCP runtime. Per PHASES.md's `**MCP runtime:** not-required` line, skip any MCP-related contract steps that assume a runtime/device axis. This part is tagged `complexity: mechanical` — the executing orchestrator should dispatch its WU subagents on the cheaper model tier per the plan-frontmatter contract.

## Post-Phase-2 Steps (final phase)

After WU-3 and WU-4 both pass review:

1. Tick `- [ ] WU-3` and `- [ ] WU-4` above to `- [x]`.
2. In the sibling `PHASES.md`, tick Phase 2's remaining deliverable checkboxes (producer updates, worked example, lockstep test, skill re-projection) that this batch closed.
3. Set this plan part's frontmatter `status:` to `Complete` only if both work units are done and all quality-gate commands are green.
4. Since this is the LAST phase, per PHASES.md's Phase 2 "Completion (gate-owned)" note: set the top-level `PHASES.md` **Status:** to `In-progress` (NOT `Fixed`) — implementation done, validation/gate pending. Do NOT flip SPEC.md's `**Status:**` and do NOT write `FIXED.md` — those are the orchestrator's `__mark_fixed__` gate, fired only after the validation tail.
5. Commit + push per the repo's commit policy (`.claude/skill-config/commit-policy.md`).

## Blocking Issue Protocol

Per `~/.claude/skills/_components/execution-contract.md`'s Blocking Issue Protocol. No anticipated blockers — this is a well-scoped docs + single-assertion change following an exact existing precedent in the same file.

## Completion

Per `~/.claude/skills/_components/execution-contract.md`'s Completion report format. This part's completion report additionally states: "Phase 2 of descoped-row-recognition-needs-canonical-marker complete — both plan parts done. PHASES.md Status set to In-progress; awaiting the pipeline's completion gate (no MCP surface — this item has `**MCP runtime:** not-required`)."
