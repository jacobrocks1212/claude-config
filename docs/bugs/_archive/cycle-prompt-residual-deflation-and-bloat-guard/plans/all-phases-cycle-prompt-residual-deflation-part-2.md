---
kind: implementation-plan
feature_id: cycle-prompt-residual-deflation-and-bloat-guard
status: Complete
created: 2026-07-20
phases: [3]
complexity: mechanical
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-20.
> To execute: `/execute-plan {path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 3. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-prompt-residual-deflation-and-bloat-guard\plans\all-phases-cycle-prompt-residual-deflation-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-prompt-residual-deflation-and-bloat-guard\plans\all-phases-cycle-prompt-residual-deflation-part-2.md` (this file)
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-prompt-residual-deflation-and-bloat-guard\plans\all-phases-cycle-prompt-residual-deflation-part-3.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings
> during execution. This part (Phase 3) is DISJOINT from parts 1 and 3 — it touches only a new
> file and has no entry-criteria dependency on either — but "execute strictly in order" still
> governs the series' `series_index` ordering; do not reorder this part ahead of part 1.

# Implementation Plan — Cycle-Prompt Residual Deflation + Anti-Bloat Guard (part 2 of 3)  (v2)

**PHASES.md files:**
- `docs/bugs/_archive/cycle-prompt-residual-deflation-and-bloat-guard/PHASES.md` (cycle-prompt-residual-deflation-and-bloat-guard, 3 phases)

**SPEC.md files:**
- `docs/bugs/_archive/cycle-prompt-residual-deflation-and-bloat-guard/SPEC.md`

**Total phases:** 3 (this part covers Phase 3 only)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (e.g. a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and any local `## Component Reference Card` override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

**claude-config-specific override:** this repo's gates are Python/projection — `pytest`, `python3 user/scripts/lazy-state.py --test`, `python3 user/scripts/lint-skills.py`, `python3 user/scripts/generate-coupled-skills.py --check`, `python3 user/scripts/skill-size-ratchet.py --check`, `python3 user/scripts/validate-plan.py --structural <path>` — NOT `msbuild`/`mstest`/`nx`. Step B.4 (Quality Gates) and this part's per-phase Minimum Verifiable Behavior below govern which of these run per batch.

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against. The executing session SHOULD `Read` these before starting any work unit whose Implementation goal references the corresponding upstream contract. Listed in dependency order.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| cycle-prompt-deflation | composes | `docs/features/cycle-prompt-deflation/PHASES.md` | Parent feature — the deflation playbook this bug's `CLAUDE.md` documents originates here. |
| cycle-prompt-deflation | composes | `docs/features/cycle-prompt-deflation/IMPLEMENTATION_NOTES.md` | The trim-in-place / no-reference-by-path discipline this new `CLAUDE.md` states at the edit site. |

(No hard deps on Complete upstreams beyond the composed parent feature above — Phase 3 is disjoint from Phase 1/2 of this bug and needs neither complete first.)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | cycle-prompt-residual-deflation-and-bloat-guard | P3 | Authoring-contract CLAUDE.md (D2b) | — | Solo |

### Phase: cycle-prompt-residual-deflation-and-bloat-guard P3 — Authoring-contract CLAUDE.md (D2b)

**Goal:** Author a new directory-level `CLAUDE.md` at `user/skills/_components/lazy-batch-prompts/` stating the dispatched-prompt authoring contract at the edit site (none exists there today).

**Entry criteria:** None — disjoint from Phase 1 (this bug's PHASES.md: "Prerequisites: None (disjoint from Phase 1)").

**SPEC.md references:** "Proposed Fix Shape (for /plan-bug)" → Deliverable 2, "2b. Authoring-contract `CLAUDE.md`"; PHASES.md Phase 3.

**Scope note:** documentation-only deliverable. `TDD: no`.

#### WU-1 — Write `user/skills/_components/lazy-batch-prompts/CLAUDE.md` (net-new)

**Scope:** Write a new nested `CLAUDE.md` covering, per PHASES.md Phase 3 deliverable and SPEC Deliverable 2b:
1. **Verbatim-dispatch contract:** these files are assembled by `lazy_core.emit_cycle_prompt` [VERIFY: `grep -n "def emit_cycle_prompt" user/scripts/lazy_core/dispatch.py` → line 886] and DISPATCHED VERBATIM to a subagent — they carry imperative rules + load-bearing marker literals ONLY.
2. **Imperative-only / no-provenance-narrative rule:** incident/provenance/dated-history narrative belongs in the SPEC/IMPLEMENTATION_NOTES, never the prompt. Point at the enforcing mechanism: `python3 user/scripts/skill-size-ratchet.py --check` / `python3 user/scripts/lint-skills.py --check-skill-size` [VERIFY: `grep -n "check-skill-size" user/scripts/lint-skills.py` → lines 399, 527] — the war-story pattern detector + per-section byte ceiling landed by Phase 2 (part 3 of this series).
3. **`@section` grammar + emitter contract:** the marker line format (`<!-- @section <name> pipelines=<feature|bug|feature,bug> modes=<workstation|cloud|workstation,cloud> skills=<all|csv> ... -->`) [VERIFY: `grep -n "@section <name> pipelines" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` → line 14, the documented grammar comment], how the emitter parses/selects/binds sections [VERIFY: `grep -n "_SECTION_MARKER_RE\|def _parse_sections\|def emit_cycle_prompt" user/scripts/lazy_core/dispatch.py`].
4. **Deflation playbook:** trim-in-place, never reference-by-path — cite the `phases-slice-scoped-reads` precedent failure (a prose read-mandate failed in this exact prompt; SPEC "Related" list, first bullet under `docs/features/phases-slice-scoped-reads`) as the reason externalization is rejected.
5. **Preserved-load-bearing-literal list** (mirroring `SEMANTIC_DIFF.md`'s list from part 1 of this series): every `@section` selector line; `WORKSTATION DISPATCH — LOAD-BEARING`; tokens `{cwd}`/`{work_branch}`/`{receipt_name}`/`{item_label}`; the R5 chained-command form; `git_safe_push`; the `git add -A` ban; `classify_conflict` + `conflict_kind: semantic` + `--park-provisional`; the `--verify-ledger` + `ok:true` certification; `cycle-subagent-bg-gate-guard.sh`; the `series_index` prerequisite-ordering algorithm.
6. **Per-section + assembled-profile ratchet pointer:** how to re-lock a ceiling after a legitimate edit — `--lock-in-profile <profile-id>` for the whole-assembled ceiling [VERIFY: `grep -n -- "--lock-in-profile" user/scripts/skill-size-ratchet.py` → line 431] and `--lock-in <path>` for a per-file/per-section ceiling [VERIFY: `grep -n -- "--lock-in " user/scripts/skill-size-ratchet.py` → line 430] — both NEVER hand-raise.

**Style:** match the sibling `user/skills/_components/CLAUDE.md` [VERIFY: `test -f user/skills/_components/CLAUDE.md`] — concise, load-bearing facts only, not a restatement of the SPEC.

**TDD:** no.

**Files to create/modify:**
- `user/skills/_components/lazy-batch-prompts/CLAUDE.md` (new)

**Implementation goal:** A future editor of any file under `user/skills/_components/lazy-batch-prompts/` reads this `CLAUDE.md` first and understands: (a) the file is dispatched verbatim, (b) narrative prose is banned and mechanically gated, (c) the `@section` grammar, (d) how to lock in a ceiling after a legitimate edit — without needing to re-derive any of it from the SPEC or from `dispatch.py` source.

**Spec requirements:** SPEC "Proposed Fix Shape" Deliverable 2, "2b. Authoring-contract `CLAUDE.md`"; PHASES.md Phase 3 Deliverables list (verbatim content checklist).

**Batch:** 1.

**Minimum Verifiable Behavior:**
```bash
python3 user/scripts/lint-skills.py
python3 user/scripts/doc-drift-lint.py --repo-root .
```
Both exit 0 — the new file introduces no broken injection, no undeclared reference, no doc-drift finding.

**Testing Strategy:** No new tests — a documentation-only deliverable verified by the two lint gates above staying green (they would catch a malformed/broken reference in the new file).

**Runtime Verification:** none.

### Batch overview — Phase 3

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1 | Solo | N/A |

## Work Units

- [x] WU-1 — Write user/skills/_components/lazy-batch-prompts/CLAUDE.md (net-new)

## Plan-specific execution notes

- This part is genuinely mechanical: one net-new documentation file, no source/test code, no matcher-set or detector edits. Tagged `complexity: mechanical` per the Step 2.5 test in `/write-plan` (boilerplate/pure-documentation authoring where the API/contract being documented — `emit_cycle_prompt`, the `@section` grammar, `skill-size-ratchet.py`'s CLI flags — already exists).
- Do not reference Phase 2's (part 3 of this series) detector/ceiling function names speculatively — PHASES.md's own "Integration Notes for Next Phase" on Phase 2 warns the reverse (hold Phase 3 authoring until Phase 2's names are settled) precisely because this part is written first in the series and must not assert unverified function names. Item 2 and item 6 above cite only the CLI flags (`--check-skill-size`, `--lock-in-profile`, `--lock-in`) that already exist today [VERIFIED above] — do not name Phase 2's not-yet-landed detector internals.
