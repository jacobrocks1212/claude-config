---
kind: implementation-plan
feature_id: live-settings-split-brain-disarms-enforcement-plane
status: Complete
created: 2026-07-12
complexity: complex
phases: [3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-12.
> To execute: `/execute-plan <this-path>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 3. Sibling parts:
> - part 1: `docs/bugs/_archive/live-settings-split-brain-disarms-enforcement-plane/plans/fix-settings-split-brain-part-1.md`
> - part 2: `docs/bugs/_archive/live-settings-split-brain-disarms-enforcement-plane/plans/fix-settings-split-brain-part-2.md` (this file)
> - part 3: `docs/bugs/_archive/live-settings-split-brain-disarms-enforcement-plane/plans/fix-settings-split-brain-part-3.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — Live settings split-brain (Phase 3)  (v2)

**PHASES.md files:**
- `docs/bugs/_archive/live-settings-split-brain-disarms-enforcement-plane/PHASES.md` (5 phases; this part covers 3)

**SPEC.md files:**
- `docs/bugs/_archive/live-settings-split-brain-disarms-enforcement-plane/SPEC.md`

**Total phases:** 5 (this part: 1)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract. Where this **claude-config** harness repo uses non-default gates (Python/PowerShell tests + linters, NOT `/msbuild`/`/mstest`/MCP), this plan's `## Plan-specific execution notes` override the contract's defaults; everything else in the contract still applies.

## Plan-specific execution notes (claude-config harness repo)

- **This phase includes a live-machine operation** — restoring the `~/.claude/settings.json` symlink on the workstation. This is a REAL, runnable command (`python3 setup.py repair` or `powershell.exe -File setup.ps1 repair`), not runtime validation. It is workstation-only; a cloud/non-Windows executor CANNOT restore a Windows symlink — if this executor is not on the target Windows workstation, complete the tracked-code deliverables (setup.ps1/setup.py check extension) and record the symlink-restore deliverable as `deferred: workstation-only` in the phase's Implementation Notes rather than faking it.
- **ONE genuine design fork lives here (D1):** the settings.local.json `statusLine` merge-precedence question. See WU-6. If it cannot be resolved from Claude Code docs/observation, write `NEEDS_INPUT.md` and halt — do NOT guess the overlay behavior.
- **Quality Gates (from repo root):**
  - `python3 setup.py check` (settings mapping reports OK / symlink, no `REAL`) — after restore
  - `python3 user/scripts/doc-drift-lint.py --live --repo-root .` (exit 0 after restore)
  - `python3 -m pytest user/scripts/test_doc_drift_lint.py -q` (unchanged green — any pure-logic setup-check unit tests added)
  - `python3 user/scripts/doc-drift-lint.py --repo-root .` (default check stays exit 0)
  - If `setup.ps1` changed and Pester is available: `powershell.exe -Command "Invoke-Pester <relevant test>"` (only if a test exists; otherwise a `-WhatIf`/dry `setup.ps1 check` smoke).
- **Commit after the phase** on `main` (no branch/PR — repo Branching Policy). Keep the pre-restore live file's `.bak` until a session confirms statusLine + autocompact behave.

## References — Upstream Artifacts

(none — no completed hard upstream dependencies. Depends on part 1 having landed the complete tracked SSOT, which is an intra-plan ordering, not an upstream feature dep.)

## Touchpoint Audit (verified — Explore pass, 2026-07-12; zero contradictions)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `~/.claude/settings.json` | yes (live REAL file, mtime 2026-06-11) | — | machine-op | Restore symlink → `user/settings.json` via `setup.py repair`/`setup.ps1 repair`; prior real file → `.bak` |
| `user/settings.local.json` | manifest-mapped (`manifest.psd1:15`) | — | maybe-create | D1 overlay option for the divergent pwsh `statusLine` |
| `user/settings.json` | yes | top-level `statusLine` = `ccstatusline` (:121-125); no `env` block | refactor (maybe) | D1: fold acceptable per-machine values (env `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE`) if chosen here rather than the overlay |
| `setup.ps1` | yes | mapping loop `REAL (not symlinked)` (:195-198); warn pass (:215-259), `$hookScripts = @('lazy-route-inject.sh','lazy-dispatch-guard.sh')` (:220); REGISTRATION.md citation (:253) | refactor | Extend the warn pass to verify the FULL tracked hook set is live, OR delegate to `doc-drift-lint.py --live` |
| `setup.py` | yes | `cmd_check(mappings)` (:507), `_is_link`, `_targets_equal` (symlink-resolution only, NO hook-content/warn pass) | refactor | Add the parallel live hook/symlink check (net-new logic — mirror the ps1 warn-pass extension) |

Per-machine content to reconcile (D1, from live file): pwsh one-liner `statusLine` (live :2-5) and `env.CLAUDE_AUTOCOMPACT_PCT_OVERRIDE = "75"` (live :10-12). Tracked file's `statusLine` is `ccstatusline` and it has no `env` block.

## Execution Schedule

| Step | Phase | Title | Blocked by | Parallel? |
|------|-------|-------|------------|-----------|
| 1 | P3 Batch 1 | WU-6 D1 fold + live symlink restore | part 1 complete | Solo (machine op + decision) |
| 2 | P3 Batch 2 | WU-7 setup.ps1 warn-pass extension + WU-8 setup.py parallel check | WU-6 | Yes (file-disjoint) |

## Work Units

- [x] WU-6 — Resolve D1 per-machine content + restore the live symlink
- [x] WU-7 — Extend `setup.ps1` warn pass to the full hook set (or delegate to `--live`)
- [x] WU-8 — Add the parallel live check to `setup.py` (resolved as DROPPED per `NEEDS_INPUT_RESOLVED_2026-07-12.md` decision 2 — respects `cross-platform-setup` D6; no code change)

---

### Phase: live-settings-split-brain P3 — Restore the live symlink + fold per-machine content + extend setup checks

**Goal:** Un-break this workstation (symlink restored to the reconciled SSOT) and harden the setup-time check so this exact failure re-announces.
**Entry criteria:** Part 1 complete — the tracked SSOT MUST already carry all 12 hooks (restoring before that would strip the turn-routing pair off the laptop). Part-1 `--live` mode exists (verification vehicle + delegation target).
**SPEC.md references:** Fix Scope 2 & 5; Decisions D1; Detection points 6b & 6c.

#### WU-6 — Resolve D1 per-machine content + restore the live symlink
- **Scope:** PHASES P3 deliverables 1–2. (a) Decide the home for the divergent per-machine content — fold `env.CLAUDE_AUTOCOMPACT_PCT_OVERRIDE` into the tracked SSOT (it already tolerates machine-specific values) and decide the pwsh `statusLine`: fold vs move to the manifest-mapped `settings.local.json` overlay. **Before relying on the overlay, confirm Claude Code's settings.json ↔ settings.local.json merge precedence for `statusLine`** (D1 open sub-question). (b) Land the chosen per-machine content in its home. (c) Restore the symlink via `setup.py repair` (or `setup.ps1 repair`); confirm the prior real file was preserved as `.bak`.
- **TDD:** no (machine operation + a decision; verified by MVB / Quality Gates).
- **Files to create/modify:** `~/.claude/settings.json` (live, restored to symlink); `user/settings.json` and/or `user/settings.local.json` [VERIFY: grep -n "settings.local.json" manifest.psd1] (per-machine content home).
- **Implementation goal:** after restore, `~/.claude/settings.json` is a symlink resolving to `user/settings.json` carrying all 12 hooks, AND statusLine + autocompact-override behavior is preserved (no per-machine regression).
- **Design fork (D1 — NEEDS_INPUT-eligible):** if the settings.local.json `statusLine` merge precedence cannot be determined from docs/observation such that a safe overlay choice is impossible, write `NEEDS_INPUT.md` (`written_by: write-plan`/decision title "D1 — per-machine statusLine home") and halt. Folding `env` is unambiguous and never halts; only the statusLine-overlay question can.
- **Spec requirements:** SPEC Fix Scope 2 + Decision D1.
- **Batch:** 1.
- **MVB:** `Get-Item ~/.claude/settings.json` shows `LinkType: SymbolicLink`; `python3 user/scripts/doc-drift-lint.py --live --repo-root .` exits 0.

#### WU-7 — Extend `setup.ps1` warn pass to the full hook set (or delegate to `--live`)
- **Scope:** PHASES P3 deliverable 3. Replace/extend the warn pass (:215-259) so it verifies the FULL tracked hook set is live — preferably by shelling `python3 user/scripts/doc-drift-lint.py --live` and reporting its verdict, rather than re-listing hook names (avoids a second SSOT of hook names). Also update the REGISTRATION.md citation at :253 to reflect that registration now ships tracked (coordinated with part-3's REGISTRATION.md retire — leave a clear pointer; part 3 finalizes the doc).
- **TDD:** no (PowerShell setup script; verified by a `setup.ps1 check` smoke + Pester if present).
- **Files to create/modify:** `setup.ps1` [VERIFY: grep -n 'hookScripts' setup.ps1].
- **Implementation goal:** `setup.ps1 check` FAILS (or warns) when the live file is missing any tracked hook — not just the two turn-routing hooks (the current check passes on exactly this failure).
- **Spec requirements:** SPEC Fix Scope 5 — "Extend `setup.ps1:215-259`'s warn pass … to verify the FULL tracked hook set is live, not just the turn-routing pair."
- **Batch:** 2 (file-disjoint from WU-8).

#### WU-8 — Add the parallel live check to `setup.py`
- **Scope:** PHASES P3 deliverable 4. `setup.py` currently only compares symlink resolution (`cmd_check`); add a live hook/symlink content check parallel to the ps1 warn-pass extension — ideally the same delegation to `doc-drift-lint.py --live` (or an equivalent stdlib check), so the cross-platform port stays behavior-parallel with `setup.ps1`.
- **TDD:** yes where the check logic is pure (hook-set diff / delegation wiring) — add unit coverage; the symlink-stat portion is integration-verified.
- **Files to create/modify:** `setup.py` [VERIFY: grep -n 'def cmd_check' setup.py].
- **Test files:** a `setup.py` test if one exists, else assert via a QG smoke (`python3 setup.py check`).
- **Implementation goal:** `setup.py check` surfaces the same "live file missing tracked hooks / not a symlink" signal the ps1 warn pass does.
- **Spec requirements:** SPEC Fix Scope 5 (the cross-platform half — the port must not lag the ps1 check).
- **Batch:** 2 (file-disjoint from WU-7).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-6 | Solo | N/A (machine op) |
| 2 | WU-7, WU-8 | Yes | None (setup.ps1 vs setup.py) |

**Completion (gate-owned):** none authored here.

---

## Blocking Issue Protocol & Completion

Single-sourced in `~/.claude/skills/_components/execution-contract.md`. The D1 statusLine-overlay fork (WU-6) is the one NEEDS_INPUT-eligible point in this part — halt there rather than guessing overlay precedence. On completion with Quality Gates green (symlink restored OR the restore explicitly deferred `workstation-only` with the setup-check code landed) and the commit on `main`, proceed to part 3.
