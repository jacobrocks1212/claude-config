---
kind: implementation-plan
feature_id: harness-gate-gate-weakening-false-positives-rename-and-docstring
status: Complete
created: 2026-07-18
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan {this-file-path}`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — harness-gate `gate_weakening` false-positives (rename + docstring/list-literal)  (v2)

**PHASES.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/bugs/_archive/harness-gate-gate-weakening-false-positives-rename-and-docstring/PHASES.md` (1 phase)

**SPEC.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/bugs/_archive/harness-gate-gate-weakening-false-positives-rename-and-docstring/SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (a harness-config repo whose gates are Python/pytest rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes

- **This is a claude-config harness repo, NOT AlgoBooth/Cognito.** There is no Tauri/MCP runtime, no `/msbuild`/`/mstest`/`/nxbuild`. The quality gate for this plan is the Python/pytest suite. Ignore any contract row that assumes a Tauri or .NET build.
- **The fix already landed out-of-pipeline (commits `7dd6ad78` + `cf105d9a`, both `harden(script)` rounds).** The SPEC's `## Fix scope` describes exactly what is on disk in `user/scripts/harness-gate.py` today. This plan's single work unit is therefore a **verify-and-confirm** unit, not a build-from-scratch unit: confirm each fix element and each regression fixture is present, run the suite green, tick the WU. Author NO new production or test code unless a confirmation step finds an element ABSENT or REGRESSED — only then re-apply the specific SPEC fix-scope element and re-run the suite.
- **Gate command (the QG for this plan):** `python -m pytest user/scripts/test_harness_gate.py -q` — run from the repo root `C:/Users/Jacob/source/repos/claude-config`. Baseline observed at authoring time (2026-07-18): **32 passed**.
- **Commit/push:** claude-config lands all work on `main` and pushes after each commit (repo push policy). One commit for this single-phase plan is expected (the docs/plan artifacts + any contingency code fix).

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this plan has no completed hard upstream dependencies) | | | |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | harness-gate-gate-weakening-false-positives-rename-and-docstring | P1 | Verify the landed gate_weakening FP fix + confirm regression coverage | — | Solo |

## Work Units

- [x] WU-1 — Verify landed `detect_gate_weakening` FP fix + regression fixtures, run the suite green

---

### Phase: harness-gate-gate-weakening-false-positives-rename-and-docstring P1 — Verify the landed gate_weakening FP fix + confirm regression coverage

**Goal:** Certify — against the live tree — that the four heuristic tightenings and the FP-false/TP-true regression fixtures the SPEC specifies are present in `harness-gate.py` / `test_harness_gate.py`, and that the full suite passes; the certification IS the fix (the durable guarantee is the regression fixtures, since `gate_weakening` emits no run-time ledger event).

**Entry criteria:** None (first and only phase).

**SPEC.md references:** `## Fix scope` (the four tightenings), `## Verified symptom → target signal` (the FP-false and TP-true fixture sets).

#### WU-1 — Verify landed `detect_gate_weakening` FP fix + regression fixtures, run the suite green

- **Scope:** Covers the whole of Phase 1. Confirm all six Phase-1 deliverables:
  - Rename/strengthen NET-count guard for `def test_*` removals present in `detect_gate_weakening` (per-file `removed − added > 0` gate; a 1/1 rename and 1/2 split do NOT HIT, a 1/0 removal still HITs).
  - Deny-construct reformat NET-count guard present (`_DENY_BRANCH_RE` per-file match counts, `removed > added` gate; a single-line→multi-line `refuse_*(...)` reformat does NOT HIT).
  - Docstring exclusion (`_TRIPLE_QUOTE_RE`) skips a triple-quoted line before the `_LIST_ELEMENT_RE` membership check.
  - Collection-opening requirement (`_exemption_opens_collection`) gates the membership HIT to a definition/extension position, so a bare `in`/reference near an exemption name does not trip.
  - All FP-false and TP-true regression fixtures named in the SPEC exist in `test_harness_gate.py`.
  - `python -m pytest user/scripts/test_harness_gate.py` passes with every named fixture green.
- **TDD:** no — the regression tests already exist and are green; this WU VERIFIES them, it does not author new tests. (Contingency only: if a named fixture is found MISSING, author it against the SPEC's stated FP-false/TP-true expectation, confirm it RED against the missing behavior, then restore the SPEC fix-scope element to make it green.)
- **Files to create/modify:**
  - `user/scripts/harness-gate.py` — verify only (fix present at lines ~67-114 and ~255-328). Modify ONLY on a confirmed regression, restoring the specific SPEC `## Fix scope` element.
  - `user/scripts/test_harness_gate.py` — verify only (all named fixtures present). Modify ONLY if a named fixture is found missing.
- **Implementation goal:** Prove the FP class is closed without blunting the TP path. Confirm each of the four heuristics reads as the SPEC's `## Fix scope` describes `[VERIFY: grep -n "_TRIPLE_QUOTE_RE\|_exemption_opens_collection\|removed_test_defs\|removed_deny" user/scripts/harness-gate.py]`, confirm each named fixture is present `[VERIFY: grep -n "def test_gate_weakening_renamed_test_def_not_flagged\|def test_gate_weakening_added_docstring_not_membership\|def test_gate_weakening_fixture_list_near_bare_reference_not_membership\|def test_gate_weakening_reformatted_refuse_call_not_flagged\|def test_gate_weakening_exemption_add_to_real_set_still_hits\|def test_gate_weakening_genuine_test_removal_still_hits\|def test_gate_weakening_removed_refuse_construct_still_hits" user/scripts/test_harness_gate.py]`, then run the suite and confirm it is green. Tick WU-1 once the suite passes.
- **Spec requirements:** `## Fix scope` (Rename/strengthen guard; Docstring exclusion; Collection-opening requirement; Deny-construct reformat guard) and `## Verified symptom → target signal` (FP fixtures must be `gate_weakening_hit: false`; TP fixtures must STILL hit).
- **Batch:** 1

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-1      | Solo      | None            |

**Testing Strategy:** Run the hermetic suite `python -m pytest user/scripts/test_harness_gate.py -q` from the repo root. Green suite = FP class closed and TP path intact — the deterministic proof the phase is done.
