---
kind: implementation-plan
feature_id: cycle-subagent-leaves-work-uncommitted-or-unticked
status: Ready
created: 2026-06-19
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-19.
> To execute: `/execute-plan <this-path>`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — cycle-subagent-leaves-work-uncommitted-or-unticked (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-subagent-leaves-work-uncommitted-or-unticked\PHASES.md` (cycle-subagent-leaves-work-uncommitted-or-unticked, 1 phase)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-subagent-leaves-work-uncommitted-or-unticked\SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

> **Note on `complexity: complex`.** This part has a single WU but it edits LOAD-BEARING harness
> contract prose (the cycle-subagent turn-end contract that every `/lazy*` cycle subagent runs under)
> AND must keep the `@section` marker grammar + 14-token contract of `cycle-base-prompt.md` intact so
> `emit_cycle_prompt` still parses. A mistake here silently breaks every emitted cycle prompt. That is
> not a mechanical/codegen/doc-prose-only change in the Step 2.5 sense — it is contract authoring with
> a parser invariant — so the part is tagged `complex` (Opus) deliberately, not by default.

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture in the normal case. **However, when
this plan is executed by a `/lazy-bug-batch` / `/lazy-bug` cycle subagent (the INLINE OVERRIDE dispatch
context), the executing agent has NO `Agent` tool** — it performs ALL work INLINE with Read / Edit /
Write, keeping test-first discipline manually. Both paths are valid; the inline path is the expected
one for this harness-repo bug.

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, (dispatch subagents OR edit inline), review, run gates, update PHASES.md | `Agent` (if available), `Read`, `Edit`, `Write`, `Bash` |
| **Sonnet subagent** (non-inline path only) | Write the component edits | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT (non-inline path):** delegate code/prose edits to a Sonnet subagent. **Inline path:**
perform the edits directly with Edit/Write — this is the sanctioned override, not a violation.

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. Before
executing each step, `Read` the component files listed for that step from disk. After context
compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.2 | Subagent Review (skip the falsification re-run on the inline path) | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. On the non-inline path, delegate edits to Sonnet subagents; on the INLINE OVERRIDE path (lazy-bug
   cycle subagent with no `Agent` tool), perform edits inline with Edit/Write — that is sanctioned.
2. All edits happen in the current worktree — NEVER create worktrees.
3. The reused verifier `lazy_core.verify_ledger` is NOT modified — this is a prose/contract change only.
4. PHASES.md is updated AFTER the batch completes (tick deliverables + add Implementation Notes).
5. The work is reviewed for correctness (parser-grammar integrity + both sections mirrored) before commit.
6. After the edit, run the projection + lint gates and confirm they pass before committing.
7. This plan is self-contained — follow it exactly without relying on external context.
8. Before each step, `Read` the component files listed for that step from disk.
9. **Verification-only PHASES rows stay `- [ ]`** — the two Runtime Verification rows (projection
   round-trip + lint-clean) are ticked by whoever runs those gates; they are NOT plan-execution work and
   NOT to be flipped just because the edit landed. The top-level PHASES `**Status:**` flip to
   `In-progress` is owed when the WU lands (implementation done, validation pending). NEVER write
   `Fixed` / `FIXED.md` — that is the orchestrator's `__mark_fixed__` gate.

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | cycle-subagent-leaves-work-uncommitted-or-unticked | P1 | In-turn terminal `--verify-ledger` gate in both `turn-end` sections | — | Solo |

## Work Units

- [ ] WU-1 — Convert both `turn-end` sections to an executed terminal `--verify-ledger` gate (+ reconcile RULE INVENTORY)

## Phase: cycle-subagent-leaves-work-uncommitted-or-unticked P1 — In-turn terminal `--verify-ledger` gate

**Goal:** Make the cycle subagent's pre-return checklist a mandatory EXECUTED gate (run `--verify-ledger`, reconcile if not `ok`, re-verify, then return) in both the workstation and cloud `turn-end` sections of `cycle-base-prompt.md`, reusing the existing verifier unchanged.
**Entry criteria:** None (first and only phase).
**SPEC.md references:** `## Fix Scope` table (rows 1-2 — convert checklist to executed command; finalize-before-verify ordering); `## Proven Findings` 1, 3, 4; `## Affected Area` (primary file = `cycle-base-prompt.md`).

### WU-1 — Convert both `turn-end` sections to an executed terminal `--verify-ledger` gate

**Scope (PHASES.md deliverables covered):**
- [ ] `modes=workstation` `turn-end` section: pre-return checklist (item 3) converted to a mandatory executed `--verify-ledger` terminal gate with finalize-before-verify ordering and a verify→reconcile→re-verify→return loop.
- [ ] `modes=cloud` `turn-end` section: same conversion mirrored, preserving the existing cloud divergences (push-each-flip durability; `&& git push` in the R5 chain).
- [ ] Both sections name the verifier as a pipeline-correct command (`bug-state.py` for the bug pipeline, `lazy-state.py` for the feature pipeline — use the section's existing pipeline-selection convention, do NOT hardcode one); `--plan <plan_file>` documented as optional (plan-scoped/execute-plan cycles only).
- [ ] RULE INVENTORY header reconciled: R13 description + the "ONE sanctioned restatement is the pre-return checklist in `turn-end`" note (line ~61) updated to reflect the checklist is now an EXECUTED gate, not advisory prose.

**TDD:** no — this is harness contract/prose, not code with unit-testable behavior. Verification is the projection + lint gates (Step B.4) and a structural grep, not a test file.

**Files to create/modify:**
- `C:\Users\Jacob\source\repos\claude-config\user\skills\_components\lazy-batch-prompts\cycle-base-prompt.md` [VERIFY: path exists — confirmed during planning] — edit the two `@section turn-end` blocks and the RULE INVENTORY header. This is the ONLY edited file.

**Implementation goal (precise edit spec):**

1. **Workstation `turn-end` section** (the block under `<!-- @section turn-end pipelines=feature,bug modes=workstation skills=all -->`, currently ~lines 388-405). Rewrite item 3 (the pre-return checklist) so the subagent's FINAL action is an EXECUTED gate, not a self-walked mental check. The new shape, in prose:
   - First **finalize all reconciliation writes** — tick the landed WU/PHASES checkboxes, flip the plan-part `status:`, write the owed result sentinel — BEFORE the terminal verify (so no post-gate write can strand).
   - Then run, as a SEPARATE final step (NOT appended to the R5 atomic gate+commit chain — a non-zero verify exit must not abort the commit), the verifier. Use the pipeline-selection convention already in the file: `delegates to lazy_core.verify_ledger` [VERIFY: grep -n "def verify_ledger" C:\Users\Jacob\source\repos\claude-config\user\scripts\lazy_core.py] via
     `python3 ~/.claude/scripts/{lazy-state.py for the feature pipeline | bug-state.py for the bug pipeline} --repo-root <cwd> --verify-ledger <spec_path> [--plan <plan_file> when plan-scoped]`.
   - If the result's `ok` is false, **reconcile the named failing check in-turn** (commit + push residue, tick the boxes, flip plan status) and RE-RUN the verifier until `ok` — only then return. A return without an `ok:true` terminal verdict is a resultless return.
   - Reframe the four sub-points (a) no background job, (b) `git status --short` empty, (c) branch pushed, (d) sentinel/flip on disk as "what the terminal `ok:true` verdict certifies", so the checklist is the EXECUTED gate's contract, not separate advisory prose. Keep items 1 (never end while a process runs) and 2 (R5 atomic gate+commit) intact — the verify is ADDED after them.

2. **Cloud `turn-end` section** (the block under `<!-- @section turn-end pipelines=feature,bug modes=cloud skills=all -->`, currently ~lines 407-423). Apply the SAME conversion, preserving the cloud-specific text already there (the `<gate> && git add -A && git commit -m "..." && git push` chain that already includes push; the reclaim-durability framing). The cloud terminal verify uses the same separate-final-step ordering and the same verify→reconcile→re-verify→return loop.

3. **RULE INVENTORY header** (~lines 42-62). Update the R13 line (`R13 turn-end contract ... section: turn-end`) description if needed and the line ~61 note ("The ONE sanctioned restatement is the pre-return checklist in `turn-end`") so it accurately says the checklist is now an executed `--verify-ledger` gate. Do NOT change R5's wording or its `referenced by skill-execute-plan` note — the terminal verify is a separate step after the R5 chain, not a change to R5.

**Editing discipline (parser invariants — HARD):**
- Do NOT add, remove, reorder, or reword any `<!-- @section ... -->` marker line. `emit_cycle_prompt` parses these exactly; a changed marker breaks emission. After editing, the file MUST still contain EXACTLY two `@section turn-end` markers (one `modes=workstation`, one `modes=cloud`).
- Introduce NO new literal `{…}` brace token that is not one of the 14 emitter tokens listed in the file header (lines ~32-40). Use `<angle-bracket>` placeholders for non-token braces (e.g. `<cwd>`, `<spec_path>`, `<plan_file>`). The `<cwd>` / `<spec_path>` here are deliberately angle-bracket prose placeholders, mirroring how the existing checklist already phrases `<cwd>`.
- Keep both sections' edits MIRRORED — the coupling rule is that this one component emits both the workstation and cloud cycle prompts, so a divergence between the two sections (beyond the pre-existing cloud push-each-flip / `&& git push` divergence) is a defect.

**Spec requirements:** `SPEC.md` Fix Scope rows 1-2 (convert checklist to executed command; finalize-before-verify ordering; mirror in cloud variant); Findings 1, 3, 4.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | None            |

## Execution Protocol

### Step 0: Initialize Task Tracking
Read `~/.claude/skills/_components/task-tracking.md` and follow it (load task tools, create a task for WU-1, update as it progresses). On the inline-override dispatch path where task tools are unavailable, skip silently.

### Per-Batch Steps (Batch 1 — WU-1)

#### Step B.0: Re-read Source Documents
Re-read from disk: this plan file, `PHASES.md`, the relevant `SPEC.md` Fix Scope rows, AND the target `cycle-base-prompt.md` in full (Read-before-Edit — compaction resets read state).

#### Step B.1: Make the edit
- **Inline path (lazy-bug cycle subagent, no `Agent` tool):** perform the WU-1 edit directly with `Edit` on `cycle-base-prompt.md` per the Implementation goal above.
- **Non-inline path:** dispatch one Sonnet subagent with the full WU-1 Implementation goal + the parser-invariant editing discipline verbatim; the subagent does NOT commit or push.

#### Step B.2: Review
Read `~/.claude/skills/_components/subagent-review.md`. On the inline path, SKIP its Step 1.5 falsification re-run (you wrote the edit yourself) but DO the substantive review: (a) both `turn-end` sections converted and mirrored; (b) exactly two `@section turn-end` markers still present; (c) no new non-token `{…}` brace introduced; (d) RULE INVENTORY note reconciled. Verdict PASS / PASS-WITH-FIXES / NEEDS-REWORK.

#### Step B.3: Update PHASES.md
Read `~/.claude/skills/_components/phases-update.md`. Tick the four implementation deliverable checkboxes under Phase 1 Deliverables (the prose-edit rows), add an Implementation Notes block (date, what changed, files modified). LEAVE the two `## Runtime Verification` rows UNCHECKED — they are gate-owned verification rows ticked by whoever runs projection + lint. Since this is the last (only) phase and its implementation work landed, set the top-level PHASES `**Status:**` to `In-progress` (NOT `Fixed`).

#### Step B.4: Run Quality Gates
Run from the repo root:
```
python ~/.claude/scripts/project-skills.py
python ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities
```
Both MUST succeed with no new broken-injection / residue-token / embedded-pattern finding against `cycle-base-prompt.md`. Also run the structural grep:
```
git grep -n "verify-ledger" -- user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md
```
and confirm at least one hit falls inside EACH `turn-end` section. If `emit_cycle_prompt` has a quick self-check available, prefer it; otherwise the projection round-trip is the emission proof. 100% pass required before commit.

#### Step B.4.5: MCP Integration Test
SKIP — `**MCP runtime:** not-required` (harness component prose; no MCP-reachable app surface). No MCP scenario authored, so no surface lint either.

#### Step B.5: Commit Batch
Read commit policy: try `.claude/skill-config/commit-policy.md` in the repo root; fallback `~/.claude/skills/_components/commit-and-push.md`. Commit the edited `cycle-base-prompt.md` + PHASES.md + this plan to `main` and push. Use the ATOMIC GATE+COMMIT shape: run the gate and commit as one chained command so an interrupted turn still leaves committed, pushed state. Then run the terminal `--verify-ledger` self-check on THIS bug's spec/plan and reconcile until `ok` before returning (dogfooding the very contract this fix installs).

#### Step B.6: Proceed
Checklist: review verdict produced; PHASES.md updated (impl rows ticked, RV rows left unchecked, Status → In-progress); projection + lint green; committed + pushed. Single batch — no next batch.

### Post-Phase Steps

#### Integration Verification
Confirm the projected `cycle-base-prompt` (under `~/.claude/skills-projected/`) shows BOTH `turn-end` variants now carrying the executed `--verify-ledger` terminal step. That is the integration proof — there is no app runtime.

#### Update CLAUDE.md Files
Review whether `user/scripts/CLAUDE.md` or the repo CLAUDE.md "Coupled Skill Pairs" / hooks notes need a one-line mention that the turn-end contract now executes `--verify-ledger` in-turn. Add only if it genuinely aids the next reader; otherwise note "no CLAUDE.md change warranted".

#### Commit and Push Post-Phase Changes
Already handled in Step B.5 (single batch / single phase). If the CLAUDE.md review produced an edit, commit + push it.

## Blocking Issue Protocol

If a blocking issue is encountered:
1. Stop all in-progress work.
2. Commit + push any completed work.
3. Print a blocking-issue report (completed work, blocked WU, reason, recovery suggestion, remaining work).
4. Do not work around the blocker.

Blocking issues here include: the projection/lint gate failing in a way that can't be fixed in 2 attempts; an `@section` marker grammar break you can't resolve; a git push conflict unresolvable by rebase; a discovered need to change `verify_ledger` itself (out of scope per SPEC — escalate rather than expand).

## Completion

When WU-1 is complete:
1. Run the projection + lint gates one final time across the repo.
2. Print a completion report:

   ## Implementation Batch — Complete

   **Feature implemented:** cycle-subagent-leaves-work-uncommitted-or-unticked (P1)
   **Total phases completed:** 1
   **Final quality-gate status:** projection clean, lint clean
   **Implementation Notes summary:** both `turn-end` sections (workstation + cloud) now instruct the cycle subagent to finalize reconciliation writes, then run `--verify-ledger` as a separate terminal step, reconcile-and-re-verify until `ok`, and only then return; RULE INVENTORY note reconciled; reused `verify_ledger` unchanged; orchestrator Step 1e.4a backstop untouched (demoted to exception path).
