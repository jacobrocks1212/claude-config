---
kind: implementation-plan
feature_id: build-queue-copy-lock-stale-dll-false-success
status: Ready
created: 2026-07-01
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-01.
> To execute: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\docs\bugs\build-queue-copy-lock-stale-dll-false-success\plans\all-phases-copy-lock-part-1.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 3. Sibling parts:
> - part 1: `C:\Users\JacobMadsen\source\repos\claude-config\docs\bugs\build-queue-copy-lock-stale-dll-false-success\plans\all-phases-copy-lock-part-1.md` (this file)
> - part 2: `C:\Users\JacobMadsen\source\repos\claude-config\docs\bugs\build-queue-copy-lock-stale-dll-false-success\plans\all-phases-copy-lock-part-2.md`
> - part 3: `C:\Users\JacobMadsen\source\repos\claude-config\docs\bugs\build-queue-copy-lock-stale-dll-false-success\plans\all-phases-copy-lock-part-3.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — build-queue-copy-lock-stale-dll-false-success (Phase 1: Build honesty) (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\bugs\build-queue-copy-lock-stale-dll-false-success\PHASES.md` (build-queue-copy-lock-stale-dll-false-success, 4 phases — this part covers Phase 1)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\bugs\build-queue-copy-lock-stale-dll-false-success\SPEC.md`

**Total phases:** 1 (of 4 across this bug)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is **claude-config**, a PowerShell/Python harness repo — its gates are Pester + Python lint/projection, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the **Plan-specific execution notes** below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config harness repo)

- **This repo is `claude-config`, NOT a Cognito worktree.** The `build-filtered.ps1` / `test-filtered.ps1` edits are made against the source under `repos/cognito-forms/.claude/scripts/` (write through the claude-config symlink source — NEVER edit the live symlinked worktree path).
- **Quality gate = Pester, not `/msbuild`/`/mstest`.** After each work unit that touches PowerShell logic, the gate is:
  ```
  powershell.exe -NoProfile -Command "Invoke-Pester -Path 'C:\Users\JacobMadsen\source\repos\claude-config\user\scripts\build-queue-hygiene.Tests.ps1' -Output Detailed"
  ```
  All existing + new `Describe` blocks must be green before a batch is considered complete.
- **No live-build runtime step here.** The Phase-1 `<!-- verification-only -->` runtime row (drive a real locked-DLL build) is owned by manual verification, NOT `/execute-plan`. Do NOT author a WU for it and do NOT attempt to boot a real Cognito build from this plan.
- **Commit after the phase** with the git identity of this repo (`git config user.email` — claude-config is a **personal** repo, so `git push origin main` IS allowed after the phase's gates are green). Never squash the unrelated pre-existing dirty files (`user/plugins/.../weights.yaml`, `user/skills/babysit/`) into this plan's commits — stage only the files this plan modifies.

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this plan has no completed hard upstream dependencies) | — | — | The SPEC declares no `**Depends on:**` block; `**Related:**` names the Concluded `build-queue-no-artifact-or-process-hygiene-on-crash` bug as prior-art context only, not a hard dep. |

**Prior-art context (read only if a WU's directive is unclear):** the Concluded hygiene bug established the `Remove-PoisonedArtifacts` quarantine, the Job-Object reap machinery, `Reset-CompilerServer`, and the `hygiene` result-block shape this plan extends. Those symbols already exist in `user/scripts/build-queue-hygiene.ps1` / `build-queue-runner.ps1`; this phase adds NEW functions/fields alongside them, never restructuring them.

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | copy-lock | P1 | Build honesty — build-filtered exit + `Test-BuildLogFailure` | — | Yes (WU-1 ∥ WU-2) |
| 2 | copy-lock | P1 | Build honesty — runner log-parse override | WU-2 (needs `Test-BuildLogFailure`) | Solo (WU-3) |

## Work Units

- [ ] WU-1 — build-filtered.ps1 exit-code propagation + failure-signature scan
- [ ] WU-2 — `Test-BuildLogFailure` pure function + Pester block
- [ ] WU-3 — runner log-parse override + `build_fidelity` hygiene field

---

### Phase: copy-lock P1 — Build honesty (a failed build reports failure)

**Goal:** Stop the queue from certifying a copy-lock (MSB3027) failure as success — propagate the real MSBuild failure and override a bogus `exit_code: 0` so the existing `$buildFailed`-gated quarantine fires.
**Entry criteria:** None (first phase).
**SPEC.md references:** "Proven Findings" → two-layer fix, Primary (scripts) item; Verified Symptom 1 (seq-346 MSB3027 exit-0 lie); Theory 1 & 2 (exit-code trust + `$buildFailed`-gated guards blind to exit-0).

#### WU-1 — build-filtered.ps1 exit-code propagation + failure-signature scan

- **Scope (PHASES deliverables):**
  - [ ] `build-filtered.ps1`: capture `$LASTEXITCODE` immediately after the `dotnet build` invocation and `exit` with it (replacing the implicit fall-off-end exit 0).
  - [ ] `build-filtered.ps1`: scan the streamed build output for failure signatures — `Build FAILED`, `error MSB3027`, `error MSB3021`, and a nonzero `\d+ Error\(s\)` summary — and force a nonzero exit when any fire even if `dotnet` returned 0.
- **TDD:** no (this script streams `dotnet build` output through a pipeline; it is not unit-testable in isolation — the failure-scan/exit correctness is certified by the Phase-1 manual runtime row, a real locked-DLL build, which is out of scope for `/execute-plan`). Implement carefully against the real code shape below.
- **Files to create/modify:** `repos/cognito-forms/.claude/scripts/build-filtered.ps1` [VERIFY: `ls "C:/Users/JacobMadsen/source/repos/claude-config/repos/cognito-forms/.claude/scripts/build-filtered.ps1"`]
- **Implementation goal:**
  - The `dotnet @buildArgs 2>&1 | ForEach-Object { … }` stream loop currently ends at the `if (-not $hasOutput) { "Build SUCCEEDED" }` block with NO explicit `exit`, so PowerShell falls off the end inheriting whatever `$LASTEXITCODE` `dotnet` left — which under MSB3027 can be 0 despite "Build FAILED". Refactors the streaming block so that, as each line is inspected, a `$buildLogFailure` flag is set `$true` when the line matches any of: `'Build FAILED'`, `'error MSB3027'`, `'error MSB3021'`, or a `'^\s*\d+\s+Error'` line whose leading count is `> 0`. (The existing `$pattern` at the top of the file already surfaces these lines to the stream — extend the per-line inspection, do NOT rewrite the filter pipeline. [VERIFY: `grep -n "MSB" "C:/Users/JacobMadsen/source/repos/claude-config/repos/cognito-forms/.claude/scripts/build-filtered.ps1"` — confirm the current `$pattern` covers `MSB` before relying on it; if not, widen the pipeline `$pattern` too so the failure lines reach the loop.])
  - Immediately after the stream loop, capture `$buildExit = $LASTEXITCODE`. Then compute the effective exit: if `$buildLogFailure` OR (`$buildExit -ne 0`) → `exit` a nonzero code (use `$buildExit` when nonzero, else `1`); otherwise `exit 0`. This must be the LAST statement of the build path so the runner's `$proc.ExitCode` reflects the real result.
  - Preserve the existing `-Test` block that follows (the `if ($Test)` tail): the new `exit` must not run before an intended `-Test` invocation. Since the current file only runs tests when `-Test` is passed and there is no explicit build-only exit today, place the build-exit logic so a `-Test` run still executes tests and then exits appropriately (a `-Test` build failure should still surface nonzero). Simplest correct shape: after the build stream, set the effective exit into a variable; run the `-Test` block if requested; then `exit` the max-severity code at the very end.
- **Spec requirements:** SPEC "Evidence Collected → Source Code": *"`build-filtered.ps1` has no explicit `exit`, so PowerShell inherits `$LASTEXITCODE` … Under MSB3027, `dotnet build` can log 'Build FAILED' yet exit 0 → the runner sees 0."* This WU closes that seam at the source.
- **Batch:** 1

#### WU-2 — `Test-BuildLogFailure` pure function + Pester block

- **Scope (PHASES deliverables):**
  - [ ] `build-queue-hygiene.ps1`: new `Test-BuildLogFailure` function — pure, reads a captured build log (string or line array) and returns a typed result (`$true` + matched signature) using the `Get-SafeValue` fail-open pattern. No side effects.
  - [ ] Tests: extend `build-queue-hygiene.Tests.ps1` with a `Describe 'Test-BuildLogFailure'` block — asserts a captured seq-346-style MSB3027 log → failure, a clean "Build succeeded" log → no failure, and fail-open on unreadable input.
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/build-queue-hygiene.ps1` [VERIFY: `grep -n "function Get-SafeValue" "C:/Users/JacobMadsen/source/repos/claude-config/user/scripts/build-queue-hygiene.ps1"` → L39]
- **Test files:** `user/scripts/build-queue-hygiene.Tests.ps1` [VERIFY: `grep -n "Describe 'Remove-PoisonedArtifacts'" "C:/Users/JacobMadsen/source/repos/claude-config/user/scripts/build-queue-hygiene.Tests.ps1"` → L83, the fixture pattern to mirror]
- **Test expectations (write FIRST, RED):** a new `Describe 'Test-BuildLogFailure'` block asserting:
  - A here-string fixture containing `"Build FAILED"` + `"error MSB3027"` + `"2 Error(s)"` (the seq-346 shape) → the function returns a truthy failure result, and the matched-signature field names the first matched token.
  - A clean fixture containing `"Build succeeded"` / `"0 Error(s)"` → returns a non-failure result.
  - `$null` / empty / a non-string bad input → does NOT throw and returns a non-failure (fail-open) result (mirrors the existing `Get-SafeValue`-wrapped `Should -Not -Throw` assertions in the file).
- **Implementation goal:** Add `Test-BuildLogFailure` near the other public functions (~after `Remove-PoisonedArtifacts` at L330 [VERIFY: `grep -n "function Remove-PoisonedArtifacts" "C:/Users/JacobMadsen/source/repos/claude-config/user/scripts/build-queue-hygiene.ps1"` → L330]). Signature accepts a log as `[string]` or `[string[]]` (normalize to lines). Return an ordered/typed object like `@{ failed = $bool; signature = $matchedTokenOrNull }`. Match against the same signature set as WU-1 (`Build FAILED`, `error MSB3027`, `error MSB3021`, `\d+ Error\(s\)` with count > 0). Wrap the body in `Get-SafeValue { … } <benign-default>` so any parse error fails OPEN to a non-failure result [VERIFY: `grep -n "Get-SafeValue" "C:/Users/JacobMadsen/source/repos/claude-config/user/scripts/build-queue-hygiene.ps1"` — reuse this wrapper, do NOT hand-roll try/catch]. Pure: no file writes, no process calls.
- **Spec requirements:** SPEC "Proven Findings → Primary (scripts)": *"parse the build log for `Build FAILED` / `MSB3021` / `MSB3027` … and override a bogus `exit_code: 0` to a failure."* This WU is the pure, testable half of that override (WU-3 wires it).
- **Batch:** 1

#### WU-3 — runner log-parse override + `build_fidelity` hygiene field

- **Scope (PHASES deliverables):**
  - [ ] `build-queue-runner.ps1`: after `$exitCode` is read (L89–90), for build ops (`$execLeaf -match 'build-filtered\.ps1$'`) call `Test-BuildLogFailure` on the run's log; if it reports failure, set `$exitCode`/`$buildFailed` to failure so the existing L96–98 quarantine runs. Add a `build_fidelity` field (`verified` | `log-failure-override` | `n/a`) to the hygiene block, mirroring `$resultFidelity`.
- **TDD:** partial — the pure classifier (`Test-BuildLogFailure`) is already tested in WU-2. The runner wiring is verified by feeding a saved MSB3027 log through the override path (the Phase-1 MVB) and asserting `exit_code != 0` + non-empty `quarantined_artifacts`. If a Pester harness for the runner override is feasible (dot-source `Test-BuildLogFailure`, drive a synthetic log), add it to `build-queue-hygiene.Tests.ps1`; otherwise assert the classifier contract there and treat the end-to-end override as covered by the MVB below.
- **Files to create/modify:** `user/scripts/build-queue-runner.ps1` [VERIFY: `grep -n "resultFidelity\|buildFailed\|Remove-PoisonedArtifacts" "C:/Users/JacobMadsen/source/repos/claude-config/user/scripts/build-queue-runner.ps1"` → `$exitCode` L89-90, `$buildFailed` L95, quarantine gate L96-98, `$resultFidelity` L101-107]
- **Test files:** `user/scripts/build-queue-hygiene.Tests.ps1` (extend only if a runner-override harness is feasible; else no new file).
- **Implementation goal:**
  - This WU depends on WU-2's `Test-BuildLogFailure` — it must run AFTER WU-2 (Batch 2). The runner already dot-sources / imports `build-queue-hygiene.ps1` (it calls `Remove-PoisonedArtifacts`, `Reset-CompilerServer`, `New-BuildJobObject` from it) so the function is in scope [VERIFY: `grep -n "hygiene" "C:/Users/JacobMadsen/source/repos/claude-config/user/scripts/build-queue-runner.ps1"` — confirm the import line before assuming scope].
  - The runner streams the child build's output to a log/transcript. Determine where the run's build log is captured (the `Exec`'d `build-filtered.ps1` output). If the runner already tees the child output to a file, read it; if not, this WU must capture it (e.g. redirect the `Start-Process` stdout to a temp log under `$StateRoot`) so `Test-BuildLogFailure` has a log to parse. Keep it minimal — reuse `Get-SafeValue` for every read.
  - After `$exitCode = $proc.ExitCode` (L89-90) and inside/after the `finally`, compute `$execLeaf = Split-Path -Leaf $Exec` (the pattern already used for `$resultFidelity` at L101-107). For a build op (`$execLeaf -match 'build-filtered\.ps1$'`): call `Test-BuildLogFailure` on the captured log; if it reports failure AND `$exitCode -eq 0`, set `$exitCode = 1` and `$buildFailed = $true` BEFORE the L96-98 `if ($buildFailed …) { Remove-PoisonedArtifacts … }` gate so quarantine fires. Record which path was taken.
  - Add `build_fidelity` to the hygiene `[ordered]@{ … }` block (alongside `vbcscompiler_recycled` / `quarantined_artifacts` / `result_fidelity` at L118-127): `'log-failure-override'` when the override fired, `'verified'` for a build op that did not need override, `'n/a'` for non-build ops. Mirror the exact `Get-SafeValue { … } 'n/a'` shape used for `$resultFidelity` — do NOT restructure the block.
- **Spec requirements:** SPEC Theory 2 (*"Quarantine … only runs when `$buildFailed`, so any failure that exits 0 bypasses cleanup"*) — the override makes the copy-lock failure set `$buildFailed`, un-gating the existing quarantine. `build_fidelity` is the surfaced provenance (Part 3 renders it).
- **Batch:** 2

**Minimum Verifiable Behavior (phase gate):** `Invoke-Pester build-queue-hygiene.Tests.ps1` passes the new `Describe 'Test-BuildLogFailure'` block; feeding a saved MSB3027-style build log through the runner's override path yields `exit_code != 0` and a non-empty `quarantined_artifacts`.

**Deferred to manual verification (NOT a WU):** the Phase-1 `<!-- verification-only -->` runtime row — drive `build-filtered.ps1` against a project whose `bin/Debug` DLL is locked and confirm it exits nonzero. This is owned by manual runtime verification and MUST NOT be authored as a plan work unit.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1, WU-2 | Yes | None (build-filtered.ps1 vs hygiene.ps1+Tests.ps1) |
| 2 | WU-3 | Solo | Depends on WU-2's `Test-BuildLogFailure` |

**Prerequisites:** None (first part).
