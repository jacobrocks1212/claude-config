---
kind: implementation-plan
feature_id: operator-checkpoint-resume-counter-reset
status: Complete
created: 2026-06-17
complexity: complex
phases: [1, 2, 3, 4]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-17.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\operator-checkpoint-resume-counter-reset\plans\fix-operator-checkpoint-resume-counter-reset.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — operator-checkpoint-resume-counter-reset  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\operator-checkpoint-resume-counter-reset\PHASES.md` (operator-checkpoint-resume-counter-reset, 4 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\operator-checkpoint-resume-counter-reset\SPEC.md`

**Total phases:** 4
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.ts`, `.js`, `.cs`, `.vue`, `.py`, `.rs`, `.tsx`, `.jsx`, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **DISPATCH-LIMIT OVERRIDE (when this plan is executed by a /lazy-bug-batch cycle subagent that lacks the `Agent` tool):** perform ALL implementation INLINE with Read / Edit / Write, test-first per WU. The orchestrator-vs-subagent split above is the workstation default; the cycle-subagent override supersedes it when no `Agent` tool is available. Never write `BLOCKED.md` because of the dispatch limit.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `.claude/skill-config/quality-gates.md` (fallback: `~/.claude/skills/_components/quality-gates.md`) |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines). **(See the dispatch-limit override above — when executed by an Agent-less cycle subagent, do it all inline, test-first.)**
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents
3. Every TDD work unit goes through the test-first pipeline — failing tests written and confirmed RED before implementation
4. PHASES.md is updated AFTER EACH batch completes (not deferred)
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing
6. Mistakes are fixed immediately before launching the next batch
7. After all batches in a phase finish, integration verification confirms all changes work together
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it
9. Each completed phase is committed and pushed before the next phase begins
10. Cross-feature phases may run in parallel when dependencies are satisfied and no file conflicts exist (N/A here — strict linear chain)
11. This plan is self-contained — follow it exactly as written without relying on external context
12. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory

## Work Units

- [x] WU-1 — Persist `operator_authorized` in the checkpoint payload (Phase 1)
- [x] WU-2 — Branch `restore_checkpoint_counters` on provenance + fixtures (Phase 2)
- [x] WU-3 — Confirm bug-pipeline parity (Phase 3)
- [x] WU-4 — Reconcile SKILL prose for the coupled pair (Phase 4)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | operator-checkpoint-resume-counter-reset | P1 | Persist checkpoint provenance | — | Solo |
| 2 | operator-checkpoint-resume-counter-reset | P2 | Branch restore on provenance | P1 | Solo |
| 3 | operator-checkpoint-resume-counter-reset | P3 | Confirm bug-pipeline parity | P2 | Solo |
| 4 | operator-checkpoint-resume-counter-reset | P4 | Reconcile SKILL prose | P2, P3 | Solo |

Strictly sequential. Each WU is its own batch.

---

### Phase: operator-checkpoint-resume-counter-reset P1 — Persist checkpoint provenance

**Goal:** Record whether a checkpoint was operator-authorized at write time so the resume path can branch on it later. No behavior change yet — restore still carries forward unconditionally until Phase 2.
**Entry criteria:** None (first phase).
**SPEC.md references:** `## Proven Findings` item 1 (thread `args.operator_authorized` into checkpoint write + add field to schema); `## Evidence Collected → Source Code` (the flag is in scope at `lazy-state.py:6018`/`:6305`).

#### WU-1 — Persist `operator_authorized` in the checkpoint payload

- **Scope:**
  - [ ] `lazy_core.write_run_checkpoint`: add `operator_authorized: bool = False` parameter; persist as a top-level `operator_authorized` field in the checkpoint dict.
  - [ ] `lazy-state.py` checkpoint-write site: pass `bool(args.operator_authorized)` to `write_run_checkpoint`.
  - [ ] Tests: round-trip assertions in `test_lazy_core.py`.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy_core.py` — `write_run_checkpoint` signature + payload [VERIFY: grep -n "def write_run_checkpoint" user/scripts/lazy_core.py] (confirmed at `lazy_core.py:8368`).
  - `user/scripts/lazy-state.py` — checkpoint-write call site, the `reason == "checkpoint"` block that calls `write_run_checkpoint(args.next_route, counters,)` [VERIFY: grep -n "write_run_checkpoint" user/scripts/lazy-state.py] (confirmed ~`lazy-state.py:6369`). `args.operator_authorized` already in scope [VERIFY: grep -n "args.operator_authorized" user/scripts/lazy-state.py] (confirmed `:6305`, `:6335`).
- **Test files:** `user/scripts/test_lazy_core.py` [VERIFY: ls user/scripts/test_lazy_core.py].
- **Test expectations:**
  - `write_run_checkpoint(next_route, counters, operator_authorized=True)` writes a dict with `operator_authorized == True`; round-trips through `consume_run_checkpoint()` [VERIFY: grep -n "def consume_run_checkpoint" user/scripts/lazy_core.py] (confirmed `:8397`) as `True`.
  - Default (omitted-arg) write reads back `operator_authorized == False`.
  - Use the existing hermetic `LAZY_STATE_DIR`-override temp-dir pattern already used by checkpoint tests in `test_lazy_core.py` (do NOT touch the real `~/.claude/state` dir).
- **Implementation goal:** The checkpoint JSON gains a top-level boolean `operator_authorized` that defaults `False`, set from `args.operator_authorized` at the `--run-end --reason checkpoint` site. Backward-compatible: a pre-fix checkpoint file lacking the field is read as falsy by Phase 2's branch.
- **Spec requirements:** SPEC `## Proven Findings` item 1; `## Affected Area` "Checkpoint write" row.
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | N/A             |

---

### Phase: operator-checkpoint-resume-counter-reset P2 — Branch restore on provenance

**Goal:** In `restore_checkpoint_counters`, skip the counter carry-forward when the checkpoint was operator-authorized (fresh `0/0` budget); keep carry-forward when falsy/absent (automatic reliability pause + pre-fix backward compat).
**Entry criteria:** P1 complete — `operator_authorized` persisted in the checkpoint payload.
**SPEC.md references:** `## Proven Findings` item 2 (skip restore when `checkpoint["operator_authorized"]` truthy); `## Theories → Theory 1 (CONFIRMED)`; `## Open Questions` (placement: inside the helper — settled in PHASES.md provenance note).

#### WU-2 — Branch `restore_checkpoint_counters` on provenance + fixtures

- **Scope:**
  - [ ] `lazy_core.restore_checkpoint_counters`: after confirming usable `counters` + active marker, read `checkpoint.get("operator_authorized")`; truthy → return WITHOUT overwriting `forward_cycles`/`meta_cycles` (marker keeps `0/0`); falsy/absent → existing carry-forward behavior unchanged.
  - [ ] Update the helper docstring to the two-branch semantics (supersede the "ALWAYS carry forward" prose).
  - [ ] `lazy-state.py` `--run-start` consume-site: comment-only update noting carry-forward now fires only for non-authorized resumes (NO logic change — the branch lives in the helper).
  - [ ] New `--test` + unit fixtures for both branches + the legacy-file case.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy_core.py` — `restore_checkpoint_counters` provenance branch + docstring [VERIFY: grep -n "def restore_checkpoint_counters" user/scripts/lazy_core.py] (confirmed `:8427`).
  - `user/scripts/lazy-state.py` — `--run-start` consume-site comment [VERIFY: grep -n "restore_checkpoint_counters" user/scripts/lazy-state.py] (confirmed `:6220`).
- **Test files:** `user/scripts/test_lazy_core.py`; new fixture(s) in `user/scripts/lazy-state.py --test` in-file harness; possibly `user/scripts/tests/baselines/lazy-state-test-baseline.txt` [VERIFY: ls user/scripts/tests/baselines/lazy-state-test-baseline.txt].
- **Test expectations:**
  - operator-authorized checkpoint → after `--run-start`, marker `forward_cycles == 0` and `meta_cycles == 0` (helper returns without overwrite, OR returns the unchanged-by-restore marker — assert the resulting marker counters are 0/0).
  - non-authorized checkpoint → restores the paused counts (the EXISTING monotonic-carry fixture stays GREEN — this is the regression guard; do not weaken it).
  - pre-fix checkpoint file (no `operator_authorized` field) → restores (backward compat).
  - If a new `--test` fixture is added, regenerate `lazy-state-test-baseline.txt` ONLY by piping live `--test` output through the `_normalize_smoke_output` helper in `test_lazy_core.py` (NEVER hand-edit the baseline). Same rule for any bug-state baseline.
- **Implementation goal:** A checkpoint resume now respects provenance: operator-authorized → fresh authorized budget (`0/0`); automatic/legacy → monotonic carry-forward (unchanged). The truthy check (`if checkpoint.get("operator_authorized"):`) makes `False` and a missing field take the carry-forward path uniformly. HARD CONSTRAINT 8 (no within-run reset) is NOT violated: the operator-authorized resume is a NEW authorized run, not a within-run transition.
- **Spec requirements:** SPEC `## Proven Findings` item 2 + `## Affected Area` "Checkpoint restore" row; VERIFIED symptoms #2 (both counters reset) and #3 (only operator-authorized resets).
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-2      | Solo      | N/A             |

---

### Phase: operator-checkpoint-resume-counter-reset P3 — Confirm bug-pipeline parity

**Goal:** Verify the shared-helper change does not regress `bug-state.py` and behaves identically there (inherited via `lazy_core` import). No new bug-state source logic expected.
**Entry criteria:** P2 complete — the `restore_checkpoint_counters` branch is in place.
**SPEC.md references:** `## Proven Findings → Coupling note` (the provenance-conditional reset applies to the bug pipeline for free; verify `bug-state.py --test` stays green).

#### WU-3 — Confirm bug-pipeline parity

- **Scope:**
  - [ ] Run `python user/scripts/bug-state.py --test`; confirm green vs `tests/baselines/bug-state-test-baseline.txt` [VERIFY: ls user/scripts/tests/baselines/bug-state-test-baseline.txt].
  - [ ] Run `python user/scripts/lazy_parity_audit.py` [VERIFY: ls user/scripts/lazy_parity_audit.py]; confirm parity holds after the shared-helper edit.
  - [ ] If `bug-state.py` warrants its own checkpoint-resume fixture, add an operator-authorized-reset assertion mirroring WU-2; otherwise record a one-line note (in the PHASES.md Implementation Notes) that the bug pipeline shares coverage via `lazy_core` and needs no separate fixture.
- **TDD:** no (verification + guard; a fixture is added only if a gap is found, which then follows test-first).
- **Files to create/modify:**
  - `user/scripts/bug-state.py` — only if a bug-side checkpoint fixture is added (likely none) [VERIFY: ls user/scripts/bug-state.py].
  - `user/scripts/tests/baselines/bug-state-test-baseline.txt` — regenerate iff a fixture was added (via `_normalize_smoke_output`).
- **Test files:** in-file `bug-state.py --test` harness (only if a fixture is added).
- **Implementation goal:** Prove no regression in the bug pipeline and that its inherited branch logic matches the feature pipeline. The shared helper guarantees identical logic; this WU certifies it rather than re-implementing it.
- **Spec requirements:** SPEC `## Proven Findings → Coupling note`.
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-3      | Solo      | N/A             |

---

### Phase: operator-checkpoint-resume-counter-reset P4 — Reconcile SKILL prose (coupled pair)

**Goal:** Make `/lazy-batch` Step 1f / Step 5 prose accurate (fresh budget on operator-authorized re-invoke; monotonic carry-forward on automatic reliability resume) and mirror into `/lazy-batch-cloud` per the coupling rule; document the cloud operator-pause threading decision.
**Entry criteria:** P2 + P3 complete — code is correct and all state-machine tests green (reconcile prose to verified code, never to unverified code).
**SPEC.md references:** `## Proven Findings` item 3 (reconcile the Step 1f / Step 5 contradiction; mirror to cloud); `## Open Questions` (cloud operator-pause threading).

#### WU-4 — Reconcile SKILL prose for the coupled pair

- **Scope:**
  - [ ] `user/skills/lazy-batch/SKILL.md` Step 1f and Step 5: replace the "previous session's cycle count is gone / does NOT preserve cycle accounting" prose with the accurate two-class statement (operator-authorized resume → fresh `0/0`; automatic reliability pause → monotonic carry-forward, cannot exceed authorized `max_cycles`).
  - [ ] `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md`: mirror the same two-class statement per the coupled-pair rule; in the cloud checkpoint-write description, document that cloud RELIABILITY checkpoints stay carry-forward (no `--operator-authorized`), and state the decision for a cloud OPERATOR-PAUSE checkpoint — whether it passes `--operator-authorized` (and if so thread it into the cloud `--run-end --reason checkpoint` command), or defer with a one-line documented rationale. Phase 1's mechanism already supports either choice (⚖ scope-class).
  - [ ] Update each skill's State Machine Summary / resume-semantics block so the dispatch description reflects the two-class behavior (coupled-pair contract in `CLAUDE.md`).
  - [ ] Run `python user/scripts/project-skills.py` and spot-check the projected `/lazy-batch` + `/lazy-batch-cloud` output (both `_default/` and the algobooth per-repo projection).
  - [ ] Run `python user/scripts/lint-skills.py` and `python user/scripts/lint-skills.py --check-projected --check-capabilities`; resolve any finding.
- **TDD:** no (prose / skill files — verified by skill-lint + projection spot-check + coupled-pair diff, not unit tests).
- **Files to create/modify:**
  - `user/skills/lazy-batch/SKILL.md` — Step 1f, Step 5, State Machine Summary [VERIFY: grep -n "does NOT preserve cycle accounting" user/skills/lazy-batch/SKILL.md] (the Step 5 contradiction; SPEC cites Step 5 ~`:1291`).
  - `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` — mirrored prose + cloud checkpoint-write decision; State Machine Summary [VERIFY: grep -n "run-end --reason checkpoint" repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md] (the cloud checkpoint-write description ~`:989`).
- **Implementation goal:** The harness's documented contract matches the corrected code: two resume classes with distinct counter semantics, mirrored across the coupled pair, with the cloud operator-pause decision explicitly recorded (and wired if chosen).
- **Spec requirements:** SPEC `## Proven Findings` item 3 + `## Contradiction in the harness (root cause class)`; `## Affected Area` "SKILL prose" row; `## Open Questions` cloud bullet.
- **Completion (gate-owned):** when this terminal phase's work lands, set the top-level PHASES.md `**Status:**` to `In-progress` (implementation done, validation pending). The flip to `Fixed` and the `FIXED.md` receipt are owned EXCLUSIVELY by the orchestrator's `__mark_fixed__` validation-tail gate — never set here.
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-4      | Solo      | N/A             |

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** Identify the lowest-numbered phase whose entry criteria are satisfied (prerequisite phases complete — all WU checkboxes ticked). This plan is a strict linear chain P1→P2→P3→P4. If no phase is ready, jump to the Blocking Issue Protocol.
2. **Announce:** Print "Implementing operator-checkpoint-resume-counter-reset Phase N: [title]".
3. **Review prior context:** Re-read previously completed phases' Implementation Notes in PHASES.md. They take priority over the plan where they diverge.
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "Phase N: [title] — committed as [hash]".
7. **Loop:** Return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

**This is the first thing you do when executing this plan. Do NOT skip ahead to any phase or batch.**

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly. It defines: task tool loading via ToolSearch, task creation for all work units, and the update protocol for tracking progress through test and implementation phases.

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow its instructions. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and the plan file itself. Do NOT rely on cached/remembered content.

For this plan specifically, also re-read the exact source seams before editing: `user/scripts/lazy_core.py` lines around `write_run_checkpoint` (`:8368`), `consume_run_checkpoint` (`:8397`), `restore_checkpoint_counters` (`:8427`); and `user/scripts/lazy-state.py` lines around the checkpoint-write site (`:6358-6371`) and the `--run-start` consume site (`:6210-6226`). Line numbers may have drifted — re-grep, do not trust the cached offsets.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — ZERO INLINE IMPLEMENTATION)

> **CYCLE-SUBAGENT OVERRIDE:** if you are an Agent-less /lazy-bug-batch cycle subagent, IGNORE the orchestrator/subagent split and implement this WU INLINE (Read/Edit/Write), test-first. Proceed to write the failing test, confirm RED, then implement to GREEN.

**PRE-FLIGHT CHECK:** Confirm: (1) you will use the `Agent` tool with `model: "sonnet"` for ALL code changes (workstation path), OR you are an Agent-less cycle subagent following the override; (2) you will not silently skip the test-first discipline.

Read ALL of these before proceeding:
1. `~/.claude/skills/_components/tdd-protocol.md` — TDD decision gate.
2. `~/.claude/skills/_components/subagent-launch.md` — Launch orchestration.
3. `~/.claude/skills/_components/tdd-test-agent.md` — Test agent prompt template.
4. `~/.claude/skills/_components/implementation-agent.md` — Impl agent prompt template.

Note: `subagent-launch.md` references the other components above via internal directives — since you've already read them, ignore those directives in the file.

**TEST-FIRST (HARD for WU-1 and WU-2):** write the failing test(s) FIRST, run them, confirm RED (the assertion fails for the right reason — e.g. `operator_authorized` field absent / counters carried forward), THEN implement to GREEN. WU-3 is verification (run existing suites); WU-4 is prose (no unit test — lint + projection + diff).

**POST-DISPATCH GATE:** After implementation, verify the test-first order was honored and (workstation path) that you composed `Agent` calls rather than editing source directly.

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

**This is a blocking gate.** Read `~/.claude/skills/_components/subagent-review.md` and follow its complete protocol. Also read `~/.claude/skills/_components/mount-site-verification.md`. Produce a structured review report with a PASS / PASS-WITH-FIXES / NEEDS-REWORK verdict. For WU-2 specifically, the review MUST confirm the non-authorized monotonic-carry fixture is still GREEN (regression guard).

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md` and follow its instructions. Check off completed deliverables for the phase, add an Implementation Notes block (date, work completed, integration notes, pitfalls, files modified). Tick the matching `- [ ] WU-N` row in this plan's `## Work Units` checklist.

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `.claude/skill-config/quality-gates.md` (fallback `~/.claude/skills/_components/quality-gates.md`) and follow it. For THIS repo the load-bearing gates after any `lazy_core.py` / `lazy-state.py` / `bug-state.py` change are ALL of:

```
python user/scripts/lazy-state.py --test
python user/scripts/bug-state.py --test
python user/scripts/test_lazy_core.py
python user/scripts/lazy_parity_audit.py
```

100% pass required before proceeding. After WU-4 (prose), the gate is instead `python user/scripts/lint-skills.py --check-projected --check-capabilities` (run `project-skills.py` first).

#### Step B.4.5: MCP Integration Test (BLOCKING — if applicable)

N/A — this bug has `**MCP runtime:** not-required` (harness state-machine + skill-prose; no AlgoBooth app surface). Skip with this note.

#### Step B.5: Commit Batch

Read `.claude/skill-config/commit-policy.md` (fallback `~/.claude/skills/_components/commit-and-push.md`) and follow it. Work branch is `main`; commit + push to `main` only; never branch, never `--force`.

**ATOMIC GATE+COMMIT (R5):** launch the gate as ONE chained command carrying its own commit, e.g.:
`python user/scripts/lazy-state.py --test && python user/scripts/bug-state.py --test && python user/scripts/test_lazy_core.py && git add -A && git commit -m "..." && git push`

#### Step B.6: Proceed to Next Batch

**Checklist before proceeding (all must be true):**
- [ ] Review report produced with PASS/PASS-WITH-FIXES/NEEDS-REWORK verdict
- [ ] PHASES.md updated with completed deliverables and implementation notes; the `- [ ] WU-N` row ticked
- [ ] All quality gates pass (the 4 state-machine gates above; lint for WU-4)
- [ ] Step B.5 completed (commit + push to main)

If any item is unchecked, go back and complete it. Do NOT launch the next batch.

### Propagation Awareness Note

The only cross-module surface here is `lazy_core.write_run_checkpoint` / `restore_checkpoint_counters`, imported by both `lazy-state.py` and `bug-state.py`. WU-1 adds a parameter with a default — no caller breakage — but the QG step MUST run the FULL set (`lazy-state.py --test`, `bug-state.py --test`, `test_lazy_core.py`, `lazy_parity_audit.py`) after WU-1 and WU-2, not just one, because the helper is shared.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md` and follow its complete protocol. For this plan, integration verification = the full 4-gate state-machine suite green (WU-1/2/3) and skill-lint + projection clean (WU-4).

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md` and follow its instructions. Specifically check whether `user/scripts/CLAUDE.md` needs an update: it documents the run-marker / checkpoint state. If the resume semantics now branch on provenance, add a sentence to the relevant section so the doc reflects the two-class behavior.

#### Commit and Push Post-Phase Changes

Read the commit policy and commit + push to `main`.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point during execution:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed phases** that haven't been committed yet.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** operator-checkpoint-resume-counter-reset Phase N: [title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining phases (not attempted):**
   - [list]

4. **Do not attempt to work around the blocker.** Note: a missing `Agent` tool is NOT a blocker — implement inline per the override and continue.

Blocking issues include:
- A quality-gate failure (any of the 4 state-machine suites red) that can't be fixed after 2 retry attempts
- A git push conflict that can't be resolved by rebase
- An ambiguity that would require a PRODUCT-class decision diverging from a SPEC Locked Decision (write NEEDS_INPUT.md, not BLOCKED.md)
- Any error that would require architectural decisions beyond the scope of the spec

---

## Completion

When all phases in the Execution Schedule are complete:

1. **Run the full quality-gate suite one final time:** `lazy-state.py --test`, `bug-state.py --test`, `test_lazy_core.py`, `lazy_parity_audit.py`, and `lint-skills.py --check-projected --check-capabilities`.
2. **Set the top-level PHASES.md `**Status:**` to `In-progress`** (implementation done, validation pending). Do NOT flip to `Fixed` and do NOT write `FIXED.md` — those are owned exclusively by the orchestrator's `__mark_fixed__` validation-tail gate.
3. **Print a completion report:**

   ## Implementation Batch — Complete

   **Bug fixed (pending validation):** operator-checkpoint-resume-counter-reset
   **Total phases completed:** 4
   **Total commits:** M
   **Final quality-gate status:** all green

   **Commit log:**
   | Commit | Phase | Title |
   |--------|-------|-------|
   | abc1234 | P1 | Persist checkpoint provenance |
   | def5678 | P2 | Branch restore on provenance |
   | ...     | ... | ... |

   **Implementation Notes summary:**
   [key integration notes — provenance field name, the truthy-branch backward-compat behavior, the cloud operator-pause decision recorded in P4]
