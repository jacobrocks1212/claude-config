---
kind: fix-plan
feature_id: adhoc-harness-gate-gate-weakening-blind-to-cross-file-construct-move
status: Ready
created: 2026-07-19
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan docs/bugs/adhoc-harness-gate-gate-weakening-blind-to-cross-file-construct-move/plans/fix-cross-file-move-reconciliation.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — harness-gate `gate_weakening` blind to a cross-file construct move (v2)

**PHASES.md files:**
- `docs/bugs/adhoc-harness-gate-gate-weakening-blind-to-cross-file-construct-move/PHASES.md` (1 phase)

**SPEC.md files:**
- `docs/bugs/adhoc-harness-gate-gate-weakening-blind-to-cross-file-construct-move/SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (claude-config is a harness-config repo whose gates are Python/pytest, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the Plan-specific execution notes below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes

- **Quality gates for this repo are Python, not MSBuild/Nx.** The authoritative gate for this change is:
  ```bash
  python3 user/scripts/test_harness_gate.py        # or: pytest user/scripts/test_harness_gate.py -q
  ```
  This is fast, hermetic, and under the Bash cap — run it FOREGROUND. Do NOT background it. There is no Tauri/MCP runtime in this repo; ignore any contract row that assumes one.
- **`harness-gate.py` is a control surface + this is a detector RELAXATION.** The change makes `gate_weakening` `hit` on strictly fewer diffs, so at the completion-gate ship seam it routes to an operator `GATE_VERDICT.md` sign-off (`_components/harness-change-gate.md` D7, gate-weakening always halts for sign-off). That is expected and correct — it is NOT a reason to weaken the change. The regression fixtures are the adversarial evidence that the relaxation is bounded to a content-identical cross-file move only.
- **Locked Decision (SPEC):** content-identity move detection (option b) ONLY. Do NOT implement aggregate-net (a) or exemption-marker (c). A removal in file A is reconciled as a MOVE only when the *same construct text* is added in file B within the same change.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | adhoc-harness-gate-gate-weakening-blind-to-cross-file-construct-move | P1 | Content-identity cross-file move reconciliation for `gate_weakening` | — | Solo |

## Work Units

- [ ] WU-1 — Cross-file move reconciliation + regression fixtures for `gate_weakening`

### Phase: adhoc-harness-gate-gate-weakening-blind-to-cross-file-construct-move P1 — Content-identity cross-file move reconciliation for `gate_weakening`

**Goal:** Make `detect_gate_weakening` treat a content-identical construct/test-def MOVE between files within one change as net-zero (not a removal), closing the false-positive `hit`, while preserving same-file reformat handling and every genuine-removal true positive.
**Entry criteria:** none (single-phase fix).
**SPEC.md references:** `## Proven Findings` (traced serving path + fix site, lines 86–98), `## Locked Decisions` (option b — content-identity), `## Reproduction Steps` (the one-shot repro), `### Evidence Collected` (the per-file net loops).

#### WU-1 — Cross-file move reconciliation + regression fixtures for `gate_weakening`

- **Scope:** Both PHASES.md Phase-1 deliverables — the `detect_gate_weakening` reconciliation change AND its regression fixtures. Merged into ONE TDD work unit because they are mutually dependent: the new fixtures define the exact contract the reconciliation must satisfy (red→green), and the two files (`harness-gate.py`, `test_harness_gate.py`) are disjoint so there is no in-batch file overlap. Deliverables covered:
  - `detect_gate_weakening` reconciles removed-vs-added gate-refusal constructs and `def test_*` defs across the whole change's file set (content-identity, option b).
  - Cross-file-move FP fixtures + the cross-file NON-move TP fixture added; all existing fixtures stay green.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/test_harness_gate.py` — add the new fixtures (test-first).
  - `user/scripts/harness-gate.py` — modify `detect_gate_weakening` (the two per-file net-removal loops).
- **Test files:** `user/scripts/test_harness_gate.py` (existing file — append fixtures).
- **Test expectations (write these first, confirm RED):**
  1. `test_gate_weakening_cross_file_deny_move_not_flagged` — a diff that REMOVES `"permissionDecision": "deny",` from `user/hooks/block-sentinel-write-on-stray-branch.sh` AND ADDS the same line to `user/hooks/hook-prelude.sh` (one diff, two file-hunks) → asserts `hg.detect_gate_weakening(hg.parse_diff(diff))["result"] == "pass"`. This is the SPEC's exact reproduction; it is RED on the current per-file loop (the hook nets +1, the sibling add is never seen).
  2. `test_gate_weakening_cross_file_test_def_move_not_flagged` — a diff that removes `def test_important_gate():` from one test file and adds it to another test file → asserts `result == "pass"` (the `def test_*` loop is blind the same way).
  3. `test_gate_weakening_cross_file_nonmove_still_hits` — a genuine removal of a deny construct from file A PLUS an add of a *DIFFERENT* deny construct text (e.g. `exit 3` where file A lost `refuse_x(`) in file B → asserts `result == "hit"`. This pins option (b)'s bounded false-negative surface: an unrelated add must NOT mask a genuine removal.
  - The existing `_diff` helper builds a single-file diff. Add a small multi-file diff builder (or inline the two-hunk diff string, mirroring the SPEC's repro) so these fixtures can express two file-hunks in one `parse_diff` input. Keep the helper minimal.
- **Implementation goal:** In `detect_gate_weakening` [VERIFY: `grep -n "def detect_gate_weakening" user/scripts/harness-gate.py`], the two per-file net-removal loops [VERIFY: `grep -n "removed_deny\[f\] - added_deny" user/scripts/harness-gate.py`] currently reconcile a file's removed constructs ONLY against that same file's adds. Change them to ALSO reconcile against the same construct text added in OTHER files within the change (content-identity, option b):
  - Preserve the existing per-file COUNT-net reconciliation (it handles same-file single→multi-line reformats where the removed and added TEXT differ but the match COUNTS are equal — the `test_gate_weakening_reformatted_refuse_call_not_flagged` [VERIFY: `grep -n "reformatted_refuse_call" user/scripts/test_harness_gate.py`] fixture depends on this). Do NOT replace count-net with pure text-identity — that would break the reformat FP.
  - ADD, on top of it: track the actual removed/added construct TEXTS (the `_DENY_BRANCH_RE` [VERIFY: `grep -n "_DENY_BRANCH_RE =" user/scripts/harness-gate.py`]-matched substrings, and the `_TEST_DEF_RE` [VERIFY: `grep -n "_TEST_DEF_RE =" user/scripts/harness-gate.py`]-matched lines) per file. Before appending evidence for a file's residual net removal, subtract removals whose exact construct text appears among the ADDS in a DIFFERENT file. Only the residual (a construct removed with no equivalent re-add anywhere) still HITs.
  - Apply the IDENTICAL shape to BOTH the `def test_*` loop and the deny-construct loop — both are blind the same way (SPEC fix site: the structurally-identical `def test_*` loop must be fixed in lockstep).
  - Reuse `Hunk.file` / `Hunk.removed` / `Hunk.added` [VERIFY: `grep -n "self.file" user/scripts/harness-gate.py`] and the existing regexes; do not add a new construct matcher.
- **Spec requirements:** `## Locked Decisions` item 1 (content-identity ONLY, not aggregate-net or exemption-marker); `## Proven Findings` fix-site-on-path (the net loop's added-construct denominator must become a whole-change total for the specific construct text, not a same-file count).
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | None            |

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md **Status:** to `Fixed`, writes `FIXED.md`, and archives the bug dir once WU-1's tests pass and the validation tail clears; the ship seam additionally routes this control-surface / gate-relaxing edit to an operator `GATE_VERDICT.md` sign-off.
