---
kind: implementation-plan
feature_id: loop-detected-false-positives-from-probe-and-reboot-churn
status: In-progress
created: 2026-06-19
complexity: complex
phases: [1, 2, 3]
---

> **Mobile plan** — generated by `/write-plan` (via `/plan-bug`) on 2026-06-19.
> To execute: `/execute-plan docs/bugs/loop-detected-false-positives-from-probe-and-reboot-churn/plans/all-phases-resolution-aware-reset.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — loop-detected-false-positives-from-probe-and-reboot-churn (v2)

**PHASES.md files:**
- `docs/bugs/loop-detected-false-positives-from-probe-and-reboot-churn/PHASES.md` (loop-detected-false-positives, 3 phases)

**SPEC.md files:**
- `docs/bugs/loop-detected-false-positives-from-probe-and-reboot-churn/SPEC.md`

**Total phases:** 3
**Plan version:** v2 (reference-based — components loaded from disk per step)

---

## HARNESS-REPO ADAPTATION (READ FIRST — this is a claude-config bug, not an app feature)

This bug lives in the **claude-config harness repo**, NOT in an app with a Tauri/MCP runtime. The generic plan template below references TS/Vue/MCP machinery (`Step B.4.5 MCP Integration Test`, `qg:*` gates, `src-tauri/`); for THIS plan those are **N/A**. The concrete reality:

- **Source under test:** `user/scripts/lazy_core.py` (`update_repeat_counts`), `user/skills/lazy-batch/SKILL.md` + `user/skills/lazy-bug-batch/SKILL.md` (coupled pair — Step 1g signal-production edit).
- **Quality gates (the ONLY gates; substitute these wherever the template says "quality gates"):**
  - `python3 user/scripts/test_lazy_core.py` — shared-helper characterization (PRIMARY).
  - `python3 user/scripts/lazy-state.py --test` — feature state-machine smoke (byte-pinned baseline).
  - `python3 user/scripts/bug-state.py --test` — bug state-machine smoke (byte-pinned baseline).
  - `python3 user/scripts/lazy_coord.py --test` — concurrency-plane smoke (run on the final phase per `user/scripts/CLAUDE.md` shared-import rule).
  - `python ~/.claude/scripts/lint-skills.py` — only if a skill .md changed (Phase 2).
- **Baseline regeneration:** the `--test` baselines (`tests/baselines/lazy-state-test-baseline.txt`, `tests/baselines/bug-state-test-baseline.txt`) are byte-pinned via the `_normalize_smoke_output` helper in `test_lazy_core.py`. Regenerate ONLY through that helper (never by hand) and ONLY when the change legitimately alters output. The no-marker path must stay byte-identical — a marker-field addition that touches only the marked path should leave both baselines unchanged.
- **MCP runtime:** not-required (see PHASES.md header). Skip Step B.4.5 entirely with a one-line note on every batch.
- **No `Agent` tool available in the autonomous pipeline cycle.** When this plan is executed by a `/lazy-bug` / `/lazy-bug-batch` cycle subagent, the EXECUTION MODEL's "dispatch to Sonnet subagents" is overridden by the dispatch-level inline override: perform ALL test-writing and implementation INLINE with Read/Edit/Write, test-first (write the failing test before the code). A human running `/execute-plan` directly may use Agent per the template; the cycle subagent does it inline.
- **Commit policy:** `.claude/skill-config/commit-policy.md` — Conventional Commits (`fix(lazy-state): …` / `test(lazy-state): …`), **NO AI attribution footer**, push allowed (personal repo).

---

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture WHEN an Agent tool is available (human-driven `/execute-plan`). **In the autonomous pipeline cycle the Agent tool is NOT available — do ALL work inline (Read/Edit/Write), test-first** (the dispatch-level inline override governs; see HARNESS-REPO ADAPTATION above).

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, (dispatch subagents IF Agent available), review output, run gates, update tracking docs | `Agent`, `Read`, `Bash` (gates), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent / inline** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT (Agent-available mode only):** delegate source/test edits to subagents. **Inline-override mode: write the code yourself, test-first.** The files you edit directly are always allowed for the orchestrator anyway: `PHASES.md`. For THIS plan the source files are `.py` and skill `.md` — in inline mode you edit them directly per the override.

**Dispatch pattern (Agent mode):** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch (Agent mode only) | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing (Agent mode only) | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing (Agent mode only) | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` |

## References — Upstream Artifacts

No `**Depends on:**` block on the SPEC — this is a standalone harness fix building on already-landed work that is documented inline in the SPEC's Evidence section (the F1/F2 debounce commits 973339b + 774ef23, and the ordered-advance exemption). No external upstream PHASES.md to load.

(none — this plan has no completed hard upstream dependencies)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. Implementation/test work is delegated to Sonnet subagents (Agent mode) OR done inline test-first (autonomous cycle — Agent unavailable). The orchestrator never silently skips test-first.
2. All edits happen in the current worktree — NEVER create worktrees.
3. Every TDD work unit is test-first — the failing test is written and SEEN to fail before the implementation lands.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every output is reviewed for correctness, spec alignment, and TDD discipline before continuing.
6. Mistakes are fixed immediately before the next batch.
7. After all batches in a phase finish, run the full gate set (the harness-repo gates listed in HARNESS-REPO ADAPTATION).
8. Update relevant CLAUDE.md (`user/scripts/CLAUDE.md` documents `update_repeat_counts` + the cycle-counter design — Phase 2 MUST update it if the reset branch / marker field changes that contract).
9. Each completed phase is committed and pushed before the next begins.
10. `lazy_core.py` is shared by both state scripts — ANY change there keeps BOTH `lazy-state.py --test` and `bug-state.py --test` green (Coupling Rule).
11. This plan is self-contained — follow it exactly without relying on external context.
12. Before each step, `Read` the component files listed for that step from disk — do NOT rely on memory.
13. `lazy-batch` ↔ `lazy-bug-batch` are a COUPLED PAIR — the Phase-2 Step-1g signal edit is mirrored in both; diff the sibling immediately after editing either.

---

## Work Units

- [ ] WU-1 — Phase 1: symptom-2 regression fixture (reboot re-probe, no dispatch → step_count HELD)
- [ ] WU-2 — Phase 1: symptom-4 regression fixture (double-probe hygiene, no consume → neither counter inflates)
- [ ] WU-3 — Phase 2: persist the resolution signal (run-marker field at the Step-1g resolution bracket)
- [ ] WU-4 — Phase 2: symptom-3 reset branch in `update_repeat_counts` (test-first) + docstring + CLAUDE.md
- [ ] WU-5 — Phase 2: lazy-batch + lazy-bug-batch Step-1g signal-production edit (coupled pair) + confirm Open Question 2
- [ ] WU-6 — Phase 3: negative fixture — d8 commit-masked loop STILL trips (no reset taken)
- [ ] WU-7 — Phase 3: negative fixture — reset inert without the resolution signal (signal-gated)

---

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | loop-detected | P1 | Regression-confirm symptoms 2 & 4 hold | — | Solo |
| 2    | loop-detected | P2 | Resolution-aware step_count reset (symptom 3) | P1 | Solo |
| 3    | loop-detected | P3 | Negative fixture — d8 loop still trips | P2 | Solo |

---

## Per-Phase Plans

### Phase: loop-detected P1 — Regression-confirm symptoms 2 & 4 hold under the landed F1/F2 debounce

**Goal:** Lock in (with fresh fixtures) that the already-landed consume-debounce holds the counters for the no-dispatch-between-probes classes; no production code changes.
**Entry criteria:** None (first phase). Confirm the two new fixtures are GREEN against `HEAD` as-is — they characterize existing correct behavior.
**SPEC.md references:** Verified Symptoms 2 & 4; Proven Finding 1; Reproduction Steps case (1).

**WU-1 — symptom-2 regression fixture (reboot re-probe, no dispatch)**
- **Scope:** PHASES.md Phase-1 deliverable "Fixture: two MARKED probes with the SAME step signature … UNCHANGED consume-count between them → assert `step_repeat_count` is HELD".
- **TDD:** yes (a test fixture, characterizing existing behavior — green on first run).
- **Files to modify:** `user/scripts/test_lazy_core.py` — add `test_symptom2_reboot_reprobe_no_inflation` (or similarly named) near the existing `test_update_repeat_counts_debounce_holds_step_count_no_consume_between` [VERIFY: grep -n "def test_update_repeat_counts_debounce_holds_step_count_no_consume_between" user/scripts/test_lazy_core.py].
- **Test expectations:** Build a temp signature file + state dict and a run marker matching the probe's `repo_root` (reuse the existing debounce-test fixture scaffolding). Two marked probes, same `(feature_id, current_step)`, `consumed_emission_count()` UNCHANGED between them → assert the second probe's returned `step_repeat_count == prior` (HELD, not incremented).
- **Implementation goal:** No production code. The fixture exercises the real `update_repeat_counts` [VERIFY: grep -n "def update_repeat_counts" user/scripts/lazy_core.py] and asserts the HOLD.
- **Spec requirements:** SPEC Reproduction Steps case (1) — "F2 debounce HOLDS step_count".
- **Batch:** 1.

**WU-2 — symptom-4 regression fixture (double-probe hygiene)**
- **Scope:** PHASES.md Phase-1 deliverable "Fixture: two MARKED probes for one cycle with no consume between → assert neither `repeat_count` nor `step_repeat_count` inflates".
- **TDD:** yes (characterization fixture, green on first run).
- **Files to modify:** `user/scripts/test_lazy_core.py` — add `test_symptom4_double_probe_hygiene_no_inflation`.
- **Test expectations:** Two marked probes for one cycle, no consume between → assert BOTH `repeat_count` and `step_repeat_count` are HELD (F1 holds dispatch-tuple count, F2 holds step count). Mirror the existing `test_f1_repeat_count_debounce_holds_no_consume_between` [VERIFY: grep -n "def test_f1_repeat_count_debounce_holds_no_consume_between" user/scripts/test_lazy_core.py] + the step-debounce fixture scaffolding.
- **Implementation goal:** No production code.
- **Spec requirements:** SPEC Verified Symptom 4; Proven Finding 1.
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2 | Yes (same file — author sequentially if inline) | Both edit `test_lazy_core.py` — if inline, write both then run gates once |

**Phase-1 gate:** `python3 user/scripts/test_lazy_core.py` green with both new fixtures. (No state-machine output changed → `--test` baselines untouched, but run `lazy-state.py --test` + `bug-state.py --test` anyway to confirm.) Commit: `test(lazy-state): regression-pin symptoms 2 & 4 hold under F1/F2 debounce`.

---

### Phase: loop-detected P2 — Resolution-aware `step_count` reset (the residual fix, symptom 3)

**Goal:** Add a narrow, signal-gated `step_count` reset that fires once across a needs-input resolution meta-cycle, scoped exactly like the ordered-advance exemption — preserving HEAD-blindness for the general oscillation case.
**Entry criteria:** P1 fixtures green (so the reset can be proven not to regress the HOLD).
**SPEC.md references:** Verified Symptom 3; Theory 2 (Confirmed); Proven Findings 2 & 3; Affected Area rows 1 & 2; Open Questions 1 & 2.

**WU-3 — persist the resolution signal (run-marker field)**
- **Scope:** PHASES.md Phase-2 deliverable "persist the resolution signal (marker field) at the resolution meta-cycle bracket site".
- **TDD:** yes — write a `lazy_core` helper test first.
- **Files to modify:** `user/scripts/lazy_core.py` — add a recorded resolution-signal field to the run marker (the deterministic discriminator per the ⚖ D7 policy: a persisted field, NOT probe-time inference). Wire a small helper that the resolution-dispatch bracket calls to SET the signal, and ensure it is read by `update_repeat_counts` in WU-4. Mirror the existing `last_advance_state_key` marker-field pattern [VERIFY: grep -n "last_advance_state_key" user/scripts/lazy_core.py].
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** A test that sets the signal via the helper, reads the marker back, and asserts the field is present + truthy; a complementary test that an ordinary cycle does NOT leave it asserted (one-shot semantics — it fires once across the resolution, not on every subsequent probe).
- **Implementation goal:** Deterministic, marker-persisted signal that `update_repeat_counts` can key on. Legacy markers lacking the field default to "no resolution" (the reset never fires on an old marker — same legacy-tolerance as `head`/`step_*`/`consume_count`).
- **Spec requirements:** Affected Area row 2 ("surface a signal the counter can key on … deterministic, not inferred"); Open Question 1 (resolved → persisted field).
- **Batch:** 1.

**WU-4 — symptom-3 reset branch in `update_repeat_counts` (test-first) + docstring + CLAUDE.md**
- **Scope:** PHASES.md Phase-2 deliverables "new `step_count` reset branch keyed on the persisted resolution signal + unchanged step signature, ordered BEFORE the F2 debounce branch and AFTER the ordered-advance exemption" + docstring update + the Open-Question-2 confirmation.
- **TDD:** yes — write the symptom-3 RED fixture FIRST (it must increment pre-fix), then add the branch (GREEN).
- **Files to modify:** `user/scripts/lazy_core.py` (`update_repeat_counts`, the step-count block ~3755–3805 [VERIFY: grep -n "Compute the step-level count" user/scripts/lazy_core.py]); `user/scripts/CLAUDE.md` ("Cycle-counter advance" section + the `update_repeat_counts` contract).
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** `test_symptom3_resolution_reset` — two probes, SAME step signature, a DISPATCH (consume-count RISES) between them via a resolution meta-cycle that set the WU-3 signal → assert `step_repeat_count` RESETS to 1 (pre-fix: increments to 2). Write it RED first (run, see it fail with the branch absent), then add the branch and see GREEN.
- **Implementation goal:** A new `elif` in the step-count computation, placed AFTER the ordered-advance exemption [VERIFY: grep -n "ORDERED-ADVANCE EXEMPTION" user/scripts/lazy_core.py] and BEFORE the F2 debounce branch [VERIFY: grep -n "F2 double-probe debounce" user/scripts/lazy_core.py], that resets `step_count = 1` when the persisted resolution signal indicates the prior cycle was a needs-input resolution AND the step signature is unchanged. Guarded on a recorded/known prior — never resets on a missing/legacy signal. Adds NO head/commit reset (preserves Proven Finding 3 / symptom-5 design constraint). Update the docstring's reset-paths enumeration to include this third path.
- **Open Question 2 confirmation:** with a fixture, check whether the HEAD-aware `repeat_count` is ALSO exposed to symptom 3 (a resolution that commits advances HEAD → `repeat_count` already resets on its own). SPEC's stated likelihood: `step_repeat_count`-only. Add a `repeat_count` reset ONLY if a fixture proves exposure; record the finding in PHASES.md Implementation Notes either way.
- **Spec requirements:** Theory 2; Proven Findings 2 & 3; Affected Area row 1.
- **Batch:** 2 (depends on WU-3's signal field).

**WU-5 — lazy-batch + lazy-bug-batch Step-1g signal-production edit (coupled pair) + lockstep diff**
- **Scope:** PHASES.md Phase-2 deliverable "`lazy-batch/SKILL.md` Step 1g: surface the signal-production step … with the coupled mirror in `/lazy-bug-batch`".
- **TDD:** no (skill-prose edit — no executable test; covered by the WU-3/WU-4 `lazy_core` tests + `lint-skills.py`).
- **Files to modify:** `user/skills/lazy-batch/SKILL.md` (Step 1g apply-resolution dispatch path [VERIFY: grep -n "Apply-resolution dispatch" user/skills/lazy-batch/SKILL.md]); `user/skills/lazy-bug-batch/SKILL.md` (the mirrored Step 1g).
- **Implementation goal:** Add a single instruction at the resolution-dispatch bracket: when applying a `needs-input` resolution, set the persisted resolution signal (call the WU-3 helper / pass the flag through the existing `--cycle-begin --kind meta` or resolution-dispatch site) so the next probe's `step_count` resets. Keep the two orchestrators byte-aligned on this edit (coupled pair — diff the sibling immediately after editing either; the divergences are tabulated in lazy-bug-batch's "Differences" block and this signal edit is NOT a divergence — it mirrors).
- **Spec requirements:** Affected Area row 2.
- **Batch:** 2.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-3 | Solo | `lazy_core.py` + `test_lazy_core.py` |
| 2     | WU-4, WU-5 | No (WU-4 first — it owns `lazy_core.py`; WU-5 edits skills) | WU-4 ↔ WU-3 share `lazy_core.py` — sequential |

**Phase-2 gate:** test-first proven (symptom-3 fixture seen RED then GREEN); `python3 user/scripts/test_lazy_core.py`, `lazy-state.py --test`, `bug-state.py --test` ALL green; `lint-skills.py` clean (skills changed). Regenerate the `--test` baselines via `_normalize_smoke_output` ONLY if the marked-path output legitimately changed (the no-marker path must stay byte-identical — likely no baseline change). Commit: `fix(lazy-state): reset step_repeat_count across a needs-input resolution (symptom 3)`.

---

### Phase: loop-detected P3 — Negative fixture — d8 commit-masked oscillation STILL trips

**Goal:** Prove the Phase-2 reset did NOT re-introduce HEAD-advance immunity for the general oscillation case, and that the reset is strictly signal-gated.
**Entry criteria:** P2 reset branch + signal field present (the negatives assert the branch is correctly NOT taken; they need the settled field name).
**SPEC.md references:** Verified Symptom 5 (design constraint); Proven Finding 3.

**WU-6 — negative fixture: d8 commit-masked loop still trips**
- **Scope:** PHASES.md Phase-3 deliverable "Fixture: repeated probes with the SAME step signature, HEAD advancing each iteration, NO resolution signal present → assert `step_repeat_count` KEEPS INCREMENTING".
- **TDD:** yes (negative regression fixture).
- **Files to modify:** `user/scripts/test_lazy_core.py` — add `test_symptom5_d8_commit_masked_loop_still_trips`.
- **Test expectations:** Repeated probes, same `(feature_id, current_step)`, a DISTINCT HEAD each probe (commits landing — drive `_current_head` movement [VERIFY: grep -n "def _current_head" user/scripts/lazy_core.py]), NO resolution signal → assert `step_repeat_count` increments every iteration (the reset branch is NOT taken; the masking-detection design constraint holds).
- **Implementation goal:** No production code (assertion-only; relies on Phase-2 code).
- **Spec requirements:** Symptom 5; Proven Finding 3.
- **Batch:** 1.

**WU-7 — negative fixture: reset inert without the resolution signal**
- **Scope:** PHASES.md Phase-3 deliverable "Fixture: a marked probe with the resolution signal ABSENT and unchanged step signature → assert the reset branch is not taken".
- **TDD:** yes.
- **Files to modify:** `user/scripts/test_lazy_core.py` — add `test_resolution_reset_inert_without_signal`.
- **Test expectations:** Marked probe, unchanged step signature, resolution signal ABSENT (legacy/normal marker) → assert `step_repeat_count` follows the normal HOLD-or-increment path (reset NOT taken — signal-gated, mirroring the ordered-advance "known prior" guard discipline).
- **Implementation goal:** No production code.
- **Spec requirements:** Proven Finding 2 ("never resets on a missing/legacy signal").
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-6, WU-7 | Yes (same file — sequential if inline) | Both edit `test_lazy_core.py` |

**Phase-3 gate (FULL shared-import set per `user/scripts/CLAUDE.md`):** `python3 user/scripts/lazy_coord.py --test`, `python3 user/scripts/lazy-state.py --test`, `python3 user/scripts/bug-state.py --test`, `python3 user/scripts/test_lazy_core.py` — ALL green. Commit: `test(lazy-state): pin d8 commit-masked loop still trips + reset is signal-gated`.

**On landing the last fixture:** set PHASES.md top-level `**Status:**` to `In-progress` (implementation done, validation pending) — the state machine routes the validation tail; the `__mark_fixed__` gate owns the terminal `Fixed` flip + FIXED.md receipt. Do NOT write FIXED.md or flip to Fixed here.

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** the schedule is strictly sequential (P1 → P2 → P3). Pick the lowest-numbered phase whose entry criteria are satisfied.
2. **Announce:** Print "Implementing loop-detected Phase N: [title]".
3. **Review prior context:** Re-read prior phases' Implementation Notes in PHASES.md — actual field name / branch ordering decided in P2 takes priority over this plan's prose where they diverge.
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "loop-detected Phase N: [title] — committed as [hash]".
7. **Loop:** return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)

Read `~/.claude/skills/_components/task-tracking.md` and follow it exactly (load task tools via ToolSearch, create one task per WU, update through test/impl).

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), this plan file, and the target `lazy_core.py` region.

#### Step B.1: Write Tests + Implementation (test-first)
- **Agent mode (human `/execute-plan`):** read `tdd-protocol.md`, `subagent-launch.md`, `tdd-test-agent.md`, `implementation-agent.md`; dispatch Sonnet test agent then impl agent.
- **Autonomous cycle (Agent unavailable):** read `tdd-protocol.md` only; write the failing test inline (Write/Edit), RUN it, SEE it fail, then write the implementation inline and SEE it pass. Never author the implementation before the failing test.

#### Step B.2: Review Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md` and produce a structured verdict (PASS / PASS-WITH-FIXES / NEEDS-REWORK). For inline mode, self-review against the WU spec requirements + the branch-ordering constraint (ordered-advance → resolution reset → F2 debounce → increment).

#### Step B.3: Update PHASES.md (MANDATORY)
Read `~/.claude/skills/_components/phases-update.md`. Tick the landed deliverables, add an Implementation Notes block (date, what landed, the settled marker-field name, the Open-Question-2 finding, files modified).

#### Step B.4: Run Quality Gates (MANDATORY — harness-repo gate set)
Run the gates from HARNESS-REPO ADAPTATION for the phase (Phase 3 runs the FULL set incl. `lazy_coord.py --test`). 100% green required. Do NOT regenerate a `--test` baseline by hand — only via `_normalize_smoke_output`.

#### Step B.4.5: MCP Integration Test
**N/A for this plan** (MCP runtime: not-required — harness fix, no app surface). Skip with a one-line note.

#### Step B.5: Commit Batch
Read `.claude/skill-config/commit-policy.md`. Conventional Commits, NO AI-attribution footer, push allowed. Use the per-phase commit messages above.

#### Step B.6: Proceed to Next Batch
Checklist (all true): review verdict produced; PHASES.md updated; all gates green; commit+push done. Else go back.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY)
Read `~/.claude/skills/_components/integration-verification.md`. For this plan: confirm the reset branch interacts correctly with the existing ordered-advance + F2 branches (the P1 HOLD fixtures + P3 negatives together cover the interaction surface — all green).

#### Update CLAUDE.md Files (MANDATORY)
Read `~/.claude/skills/_components/claude-md-review.md`. Phase 2 MUST update `user/scripts/CLAUDE.md` if the reset branch / marker field changes the documented `update_repeat_counts` / cycle-counter contract.

#### Commit and Push Post-Phase Changes
Per commit policy.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point:

1. Stop all in-progress work.
2. Commit and push any completed phases.
3. Write `BLOCKED.md` (canonical name — per `~/.claude/skills/_components/sentinel-frontmatter.md`) in the bug dir and print a blocking-issue report (completed phases + hashes, blocked phase, reason, recovery suggestion, remaining phases).
4. Do NOT work around the blocker.

Blocking issues include: a gate failure that can't be fixed after 2 attempts; a push conflict unresolvable by rebase; an entry criterion referencing work not in this plan; any change requiring an architectural decision beyond the SPEC. NOTE: the absence of the Agent tool in the autonomous cycle is NOT a blocker — handle it inline per the override.

---

## Completion

When all three phases are complete:

1. Run the full harness gate set one final time: `lazy_coord.py --test`, `lazy-state.py --test`, `bug-state.py --test`, `test_lazy_core.py` (+ `lint-skills.py` if skills changed) — all green.
2. Confirm PHASES.md top-level `**Status:**` is `In-progress` (NOT Fixed — the `__mark_fixed__` gate owns the terminal flip after the validation tail).
3. Print a completion report:

   ## Implementation Batch — Complete

   **Bug:** loop-detected-false-positives-from-probe-and-reboot-churn
   **Total phases completed:** 3
   **Total commits:** ~3 (one per phase)
   **Final gate status:** all green

   **Implementation Notes summary:** the settled resolution-signal marker-field name; the Open-Question-2 finding (whether `repeat_count` needed the reset); confirmation that the P1 HOLD fixtures + the P3 d8-still-trips negative are both green (the design constraint held).
