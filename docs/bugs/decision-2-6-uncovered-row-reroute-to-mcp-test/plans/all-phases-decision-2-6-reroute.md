---
kind: fix-plan
feature_id: decision-2-6-uncovered-row-reroute-to-mcp-test
status: In-progress
created: 2026-07-19
complexity: complex
phases: [1, 2, 3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan docs/bugs/decision-2-6-uncovered-row-reroute-to-mcp-test/plans/all-phases-decision-2-6-reroute.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — Step-10 → mcp-test re-route on uncovered verification rows  (v2)

**PHASES.md files:**
- `docs/bugs/decision-2-6-uncovered-row-reroute-to-mcp-test/PHASES.md` (3 phases)

**SPEC.md files:**
- `docs/bugs/decision-2-6-uncovered-row-reroute-to-mcp-test/SPEC.md`

**Total phases:** 3
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is **claude-config**, a harness-config repo whose gates are Python `--test` smoke harnesses + `pytest`, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the local `## Plan-specific execution notes` override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| — | — | — | (none — this plan has no completed hard upstream dependencies. The decision-5 sibling bug `feature-operator-host-defer-not-honored-over-validated` is referenced by the SPEC but, per SPEC Locked Decision 1, is NOT a queue dep — its minimal per-row recognizer is landed here as Phase 1.) |

## Plan-specific execution notes

- **Gates (claude-config — override the contract's build/test defaults):** the quality gates for every batch in this plan are:
  1. `python3 user/scripts/lazy-state.py --test` (in-file smoke harness — must stay GREEN)
  2. `python3 user/scripts/bug-state.py --test` (coupled — must stay GREEN)
  3. `python3 -m pytest user/scripts/tests/test_lazy_core/ -q` (seam suites — the new helpers' unit tests)
  4. `python3 user/scripts/lazy_parity_audit.py --repo-root .` (coupled-pair parity — MUST be exit 0)
  5. `python3 user/scripts/lint-skills.py` + `python3 user/scripts/doc-drift-lint.py --repo-root .` (advisory; run if any skill/doc/table changed — this plan touches none, so informational only)
  Run each FOREGROUND and UNDER the ~10-min Bash cap (each drives to a real pass/fail). Never background a gate from a cycle subagent.
- **No MCP runtime.** claude-config has no Tauri/dev-runtime/MCP surface; there is NO `/mcp-test` boot. The state machine IS validated by the `--test` harness + pytest. The PHASES.md Runtime Verification rows in Phase 3 are `<!-- verification-only -->` rows the state machine falls through on — do NOT author a WU that "runs mcp-test".
- **Baselines:** if a new `--test` fixture changes the byte-pinned smoke output, regenerate the baseline ONLY by piping live `--test` output through `tests/test_lazy_core/_util.py::_normalize_smoke_output` (never by hand), per `user/scripts/CLAUDE.md` → Testing.
- **Atomic writes:** every new queue/marker/sentinel/PHASES write in production code goes through `lazy_core._atomic_write` — but this plan's predicate is PURE (no writes); do not add a write path.
- **Coupled-pair discipline (HARD):** Phase 3 edits BOTH `lazy-state.py` and `bug-state.py` in lockstep. Run the parity audit (gate 4) after WU-3 — it MUST be exit 0 before the phase is marked complete.

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | decision-2-6 | P1 | Per-row host-defer recognizer | — | Solo (Batch 1) |
| 2 | decision-2-6 | P2 | Shared uncovered-row predicate | P1 (uses `row_requires_host`; shares `__init__.py`) | Solo (Batch 2) |
| 3 | decision-2-6 | P3 | Wire re-route into both state scripts + fixtures | P2 (uses the predicate) | Solo (Batch 3) |

## Work Units

- [x] WU-1 — `row_requires_host` per-row host-defer recognizer (Phase 1)
- [ ] WU-2 — `uncovered_verification_rows_remain` shared predicate (Phase 2)
- [ ] WU-3 — Wire the shared re-route into both state scripts + `--test` fixtures + parity (Phase 3)

---

### Phase: decision-2-6 P1 — Per-row host-defer recognizer

**Goal:** Land a minimal, pure per-row `<!-- requires-host: <cap> -->` recognizer (SPEC Locked Decision 1 — as a phase of THIS bug, no queue dep on the decision-5 sibling).
**Entry criteria:** none.
**SPEC.md references:** `## Locked Decisions` (1), `## Affected Area` → "Host-deferred clause", `## Proven Findings` → fix-shape clause (b).

#### WU-1 — `row_requires_host` per-row host-defer recognizer

- **Scope:** PHASES.md Phase 1 deliverables — `_REQUIRES_HOST_ROW_RE` module constant + `row_requires_host(row_text) -> str | None` + facade export + unit tests.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy_core/docmodel.py` — add the constant + function beside `_DESCOPED_MARKER` [VERIFY: grep -n "_DESCOPED_MARKER = " user/scripts/lazy_core/docmodel.py] (confirmed :1163), mirroring the `_VERIFICATION_ONLY_MARKER` pattern [VERIFY: grep -n "_VERIFICATION_ONLY_MARKER = " user/scripts/lazy_core/docmodel.py] (confirmed :1069).
  - `user/scripts/lazy_core/__init__.py` — add `_REQUIRES_HOST_ROW_RE` + `row_requires_host` to the name→submodule map (`docmodel`) [VERIFY: grep -n "\"remaining_unchecked_are_verification_only\": \"docmodel\"" user/scripts/lazy_core/__init__.py].
- **Test files:** `user/scripts/tests/test_lazy_core/test_docmodel.py` [VERIFY: ls user/scripts/tests/test_lazy_core/test_docmodel.py].
- **Test expectations:**
  - a bare row (no marker) ⇒ `None`;
  - `- [ ] <!-- verification-only --> <!-- requires-host: real-audio-device --> foo` ⇒ `"real-audio-device"`;
  - an invalid capability id (e.g. `<!-- requires-host: BadId! -->`) ⇒ `None`;
  - a marker on the enclosing subsection header (header-scope) is honored, mirroring `remaining_unchecked_are_verification_only`'s header-scope behavior;
  - a row inside a ``` fence ⇒ `None` (illustrative-example convention).
- **Implementation goal:** a pure string recognizer. The capability id matches the closed-registry id shape `^[a-z0-9][a-z0-9-]*$` used by `lazy_core.parse_requires_host` [VERIFY: grep -rn "def parse_requires_host" user/scripts/lazy_core/]. Return the id string when present, else `None`. NO I/O, NO PHASES.md mutation. This is the "non-host-deferred" clause (b) input for WU-2.
- **Spec requirements:** SPEC `## Proven Findings` fix-shape clause (b): "NOT host-deferred (`<!-- requires-host: <cap> -->`, decision 5)"; SPEC Locked Decision 1: land the minimal recognizer here.
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | N/A             |

---

### Phase: decision-2-6 P2 — Shared uncovered-row predicate

**Goal:** Add ONE pure `lazy_core` predicate composing the existing verification/observation-gap/autotick helpers + WU-1's recognizer — the single predicate serving both symptoms.
**Entry criteria:** Phase 1 complete (WU-2 imports `row_requires_host`; shares `__init__.py`).
**SPEC.md references:** `## Proven Findings` (fix shape, three clauses a/b/c), `## Affected Area` → "Shared predicate", `## Evidence Collected` → "Reusable predicate ingredients".

#### WU-2 — `uncovered_verification_rows_remain` shared predicate

- **Scope:** PHASES.md Phase 2 deliverables — the pure predicate helper + facade export + unit tests covering all coverage/exempt/host-defer/termination branches.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy_core/gates.py` — add `uncovered_verification_rows_remain(feature_dir, phases_text, repo_root) -> dict` composing: the row-walk of `remaining_unchecked_are_verification_only` [VERIFY: grep -n "def remaining_unchecked_are_verification_only" user/scripts/lazy_core/docmodel.py] (confirmed :1183); clause (a) `observation_gap_promotable(meta)` [VERIFY: grep -n "def observation_gap_promotable" user/scripts/lazy_core/gates.py] (confirmed :608); clause (b) `docmodel.row_requires_host` (WU-1); clause (c) evidence-coverage reasoning over `evaluate_completion_evidence` [VERIFY: grep -n "def evaluate_completion_evidence" user/scripts/lazy_core/gates.py] + `autotick_verification_rows`'s cardinality logic [VERIFY: grep -n "def autotick_verification_rows" user/scripts/lazy_core/gates.py] (confirmed :781). Reuse `_UNCHECKED_ROW_RE` [VERIFY: grep -n "_UNCHECKED_ROW_RE" user/scripts/lazy_core/gates.py] (confirmed :774) + `_VERIFICATION_ONLY_MARKER` — do NOT hand-roll a new checkbox regex.
  - `user/scripts/lazy_core/__init__.py` — add `uncovered_verification_rows_remain` to the name→submodule map (`gates`).
- **Test files:** `user/scripts/tests/test_lazy_core/test_gates.py` [VERIFY: ls user/scripts/tests/test_lazy_core/test_gates.py].
- **Test expectations (the six branches — assertion must match intent):**
  1. partial `VALIDATED.md` over ≥2 verification rows, evidence covers only 1 ⇒ result `reroute: True`, `uncovered` lists the excess row(s);
  2. all rows covered by evidence (`pass_count >=` covered candidate count) ⇒ `reroute: False` (**TERMINATION**);
  3. an uncovered row whose text carries `<!-- requires-host: <cap> -->` ⇒ excluded ⇒ `reroute: False`;
  4. `result: partial` with a valid non-empty `observation_gap_exemptions` (each carrying `spec_class`) ⇒ excluded ⇒ `reroute: False`;
  5. all remaining unchecked rows inside a `Superseded`/`<!-- descoped -->` phase ⇒ `reroute: False`;
  6. document (in a test or comment) that "no `VALIDATED.md`" is a CALLER precondition (Step-10 entry gate), not this helper's concern.
  A test whose name claims `reroute` must actually assert `reroute is True`; a `terminates` test must assert `reroute is False` (would-fail-if-inverted).
- **Implementation goal:** PURE, side-effect-free (reasons about what `autotick_verification_rows` WOULD tick given the recorded evidence; NEVER mutates PHASES.md — it is safe on the read-only `compute_state` probe path). CONSERVATIVE per the operator-locked fix shape: "still uncovered after evidence reasoning" ⇒ re-route; one redundant mcp-test pass on a genuinely-complete-but-unticked matrix is tolerable. Return a small dict (e.g. `{reroute: bool, uncovered: [...], reason: str}`) so WU-3's routing `_diag` can name why.
- **Spec requirements:** SPEC `## Proven Findings` fix shape: predicate = "a non-Superseded phase has an unchecked runtime-verification row that is (a) NOT observation-gap-exempt AND (b) NOT host-deferred AND (c) not covered by recorded evidence (still unchecked after `autotick_verification_rows`)". Termination is the load-bearing contract.
- **Batch:** 2 (shares `__init__.py` with WU-1; also depends on WU-1's `row_requires_host`).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 2     | WU-2      | Solo      | shares `__init__.py` with WU-1 → sequential after Batch 1 |

---

### Phase: decision-2-6 P3 — Wire the re-route into both state scripts + regression fixtures

**Goal:** Insert the shared re-route branch before the terminal-pseudo-skill dispatch in BOTH state scripts (coupled-pair mirror), routing `sub_skill="mcp-test"` when the predicate fires; add `--test` fixtures pinning both the re-route AND its termination.
**Entry criteria:** Phase 2 complete (uses `uncovered_verification_rows_remain`).
**SPEC.md references:** `## Evidence Collected` (serving-path trace `lazy-state.py:4034→4087`), `## Affected Area` → "Step-10 routing" + "Coupled pipeline" + "Regression net", `## Open Questions` → "bug-state.py coupling" (resolved to MIRROR at plan time).

#### WU-3 — Wire the shared re-route into both state scripts + `--test` fixtures + parity

- **Scope:** PHASES.md Phase 3 deliverables. This is ONE work unit spanning both state scripts because they are a coupled pair — implementing one without the other fails the parity audit (mutually dependent per the sizing rule). Covers: the `lazy-state.py` branch, the `bug-state.py` mirror, both scripts' `--test` fixtures, and the parity-audit-green gate.
- **TDD:** yes (via the in-file `--test` smoke harness — the state machine's hermetic regression net).
- **Files to create/modify:**
  - `user/scripts/lazy-state.py` — insert the re-route branch between the provisional-sentinel guard (~:4083) and the `__mark_complete__` dispatch [VERIFY: grep -n "sub_skill=\"__mark_complete__\"" user/scripts/lazy-state.py] (confirmed :4090, dispatch block :4087–4092). On `uncovered_verification_rows_remain(...).reroute` True, `return _state(..., current_step="Step 10: re-route to mcp-test (uncovered verification rows)", sub_skill="mcp-test", sub_skill_args=<spec_path_str>)` with a `lazy_core._diag(...)` naming the uncovered row(s). Add the predicate name to the top-of-file `from lazy_core import (...)` block [VERIFY: grep -n "observation_gap_promotable," user/scripts/lazy-state.py] (import block ~:109–121).
  - `user/scripts/bug-state.py` — insert the byte-mirrored branch before the `__mark_fixed__` dispatch [VERIFY: grep -n "sub_skill=SKILL_MARK_FIXED" user/scripts/bug-state.py] (confirmed dispatch block :2148–2153), same predicate, same `mcp-test` route, mirrored `_diag`. Add the import if needed.
- **Test files (in-file `--test` harnesses, NOT pytest):** `user/scripts/lazy-state.py` (`--test` list block) + `user/scripts/bug-state.py` (`--test` list block). Register each new `def test_<name>()` in its script's test-list block per `user/scripts/CLAUDE.md` → "Adding a test to the in-file `--test` harness".
- **Test expectations:**
  - `lazy-state.py --test`: (a) fixture with a matrix-incomplete `VALIDATED.md` + `MCP_TEST_RESULTS.md` certifying only a subset + ≥1 uncovered non-exempt non-host-deferred verification row ⇒ probe returns `sub_skill="mcp-test"` (NOT `__mark_complete__`); (b) fixture where every verification row is covered/host-deferred/exempt ⇒ probe returns `sub_skill="__mark_complete__"` (**TERMINATION** — the re-route does NOT fire); (c) a host-deferred / observation-gap fixture ⇒ never re-triggers the route.
  - `bug-state.py --test`: the mirrored fixtures with `sub_skill="__mark_fixed__"` in place of `__mark_complete__`.
- **Implementation goal:** the SPEC's missing branch, inserted at the exact fix-site-on-path node so it changes the `sub_skill` value read on the symptom's serving path. TERMINATION is load-bearing: the re-route fires while an uncovered row remains, then falls through to the terminal pseudo-skill once every row is covered/host-deferred-marked/exempt (never an infinite loop). Coupled-pair lockstep — the two branches are structurally identical modulo the terminal pseudo-skill name.
- **Spec requirements:** SPEC `## Proven Findings` fix shape (insert before the line-4087 dispatch); `## Affected Area` "Coupled pipeline" (bug-state.py mirror — resolved to MIRROR, parity, NOT a divergence: both scripts share the identical unconditional-dispatch shape after the same cloud/provisional guards); `## Reproduction Steps` Expected behavior (re-route + TERMINATE).
- **Batch:** 3 (depends on WU-2; file-disjoint from WU-1/WU-2 but sequenced last).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 3     | WU-3      | Solo      | none (own files) — sequenced after Batch 2 |

**Runtime Verification** *(claude-config has no MCP runtime — these are validated by the `--test` harness + parity audit, NOT a live boot):*
- [ ] <!-- verification-only --> Both `--test` harnesses GREEN (`python3 user/scripts/lazy-state.py --test` and `python3 user/scripts/bug-state.py --test`) and `pytest user/scripts/tests/test_lazy_core/` passes.
- [ ] <!-- verification-only --> `python3 user/scripts/lazy_parity_audit.py --repo-root .` is exit 0 (coupled-pair mirror clean).

**Completion (gate-owned):** the top-level PHASES/SPEC `**Status:**` flip and the `FIXED.md` receipt are owned by the `__mark_fixed__` gate after the validation tail — this plan NEVER flips top-level status or writes `FIXED.md`. On WU-3 landing, set PHASES top-level `**Status:**` to `In-progress` (implementation done, validation pending) and let the state machine route onward.

---

## Blocking Issue Protocol / Completion

Single-sourced in `~/.claude/skills/_components/execution-contract.md` (see the Execution Policy pointer above). Do not re-derive here.
