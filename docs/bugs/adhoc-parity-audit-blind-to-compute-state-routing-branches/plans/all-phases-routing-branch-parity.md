---
kind: fix-plan
feature_id: adhoc-parity-audit-blind-to-compute-state-routing-branches
status: Ready
complexity: complex
created: 2026-07-19
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan docs/bugs/adhoc-parity-audit-blind-to-compute-state-routing-branches/plans/all-phases-routing-branch-parity.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — adhoc-parity-audit-blind-to-compute-state-routing-branches  (v2)

**PHASES.md files:**
- `docs/bugs/adhoc-parity-audit-blind-to-compute-state-routing-branches/PHASES.md` (2 phases)

**SPEC.md files:**
- `docs/bugs/adhoc-parity-audit-blind-to-compute-state-routing-branches/SPEC.md`

**Total phases:** 2
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is `claude-config`, a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the local `## Plan-specific execution notes` override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes

- **Quality gates for `claude-config` (run these, NOT `/msbuild` / `/mstest`):**
  1. `python3 user/scripts/lazy_parity_audit.py --repo-root .` — MUST exit 0 (the whole-repo parity audit; the new routing-branch check runs inside `audit_all_pairs`). This is the headline gate — its exit 0 on the current correctly-mirrored tree is the fix's green baseline.
  2. `python3 -m pytest user/scripts/test_lazy_parity.py -q` — the parity-audit test suite incl. the new `TestComputeStateRoutingParity`.
  3. `python3 user/scripts/lazy-state.py --test` and `python3 user/scripts/bug-state.py --test` — the two state-script smoke harnesses; unaffected by this fix (the two `compute_state` functions are read-only audited inputs), run as a regression net.
  4. `python3 user/scripts/doc-drift-lint.py --repo-root .` — MUST exit 0 after Phase 2's doc edits.
- **No MCP / runtime / dev-server** — this repo has none. Do NOT attempt `/mcp-test`, `--ensure-runtime`, or any Tauri boot. There is no Step-9 MCP gate for this repo; implementation-done routes to `__mark_fixed__` after `/execute-plan`.
- **All commits land on `main`.** Never create a branch. Stage only paths this plan changed (pathspec-scoped), never `git add -A` (shared worktree — a blanket add can absorb a concurrent writer's files).
- **`compute_state` read-only rule.** WU-1's allowlist READS the two `compute_state` functions to classify branches; do NOT edit `lazy-state.py::compute_state` or `bug-state.py::compute_state` — they are the audited surface, unchanged by this fix.

## References — Upstream Artifacts

(none — this bug has no completed hard upstream dependencies; its `**Depends on:**` block is absent.)

## Touchpoint Audit (verified: inline — cycle-subagent inline path, dispatch deferred)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `user/scripts/compute-state-routing-parity.json` | **NO (net-new)** | — | create | The declared allowlist; no existing file — create next to `lazy-parity-manifest.json` |
| `user/scripts/lazy_parity_audit.py` | yes | `audit_all_pairs` (L590), `audit_state_script_parity` (L384), `audit_merged_view_dispatch_parity` (L548), `_STATE_SCRIPTS` (L304), `apply_tokens` (L51), `load_manifest` (L41) | refactor | Add `audit_compute_state_routing_parity()` structured like `audit_merged_view_dispatch_parity`; wire into `audit_all_pairs` after the L617 merged-view call. Reuse `_STATE_SCRIPTS`; do NOT write a new script-reader loop from scratch |
| `user/scripts/bug-state.py` | yes | `compute_state` verification-only bypass (`cloud_bypass`/`workstation_bypass`, ~L1729) | read-only input | Audited surface — census reads its routing branches; NO edit |
| `user/scripts/lazy-state.py` | yes | `compute_state` `_feature_past_implementation` (L1747) | read-only input | Audited surface — NO edit |
| `user/scripts/test_lazy_parity.py` | yes | `TestStateScriptParity` (L623), `TestLiveZeroDrift` (L466), `_make_pair` (L40) | refactor | Add `TestComputeStateRoutingParity` mirroring `TestStateScriptParity`'s fires/clean pattern + a live-tree-clean test like `TestLiveZeroDrift` |
| `user/scripts/CLAUDE.md` | yes | "Coupling Rule (HARD REQUIREMENT)" section; CLI quick-reference block | refactor (docs) | Document new routing-branch coverage + allowlist location |
| `CLAUDE.md` (repo root) | yes | Coupled Skill Pairs / parity-audit references | refactor (docs) | One-line note that routing-branch symmetry is now audited |

No contradictions found — the SPEC's root cause is `traced` and every anchor resolved to a verified `file:symbol`. The census-mechanism choice (declared allowlist + structural-token census) is recorded in PHASES.md as a D7 scope-class decision (product behavior identical across the SPEC's three options).

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | routing-branch-parity | P1 | Declared allowlist (WU-1) | — | Solo |
| 2 | routing-branch-parity | P1 | Audit function + wiring + tests (WU-2) | WU-1 | Solo |
| 3 | routing-branch-parity | P2 | Document coverage + allowlist location (WU-3) | WU-2 | Solo |

## Work Units

- [ ] WU-1 — Declared routing-branch allowlist + classification of all coupled compute_state branches
- [ ] WU-2 — audit_compute_state_routing_parity() + wire into audit_all_pairs + tests
- [ ] WU-3 — Document the new routing-branch coverage + allowlist location

---

### Phase: routing-branch-parity P1 — Routing-branch symmetry census + tabulated-divergence allowlist

**Goal:** Add a `compute_state` routing-branch symmetry check to `lazy_parity_audit.py`, backed by a committed declared allowlist, seeded so the current tree passes green and a future unmirrored routing branch fails the audit.
**Entry criteria:** None (first phase).
**SPEC.md references:** `## Proven Findings` (root cause + serving-path trace, lines 110–135), `## Affected Area` (the four-row impact table), `## Open Questions` (census granularity — resolved in PHASES.md as D7 scope-class).

#### WU-1 — Declared routing-branch allowlist + classification

- **Scope:** PHASES.md Phase-1 deliverables: "New committed allowlist file …" and "Classify every existing coupled `compute_state` routing branch into the allowlist …".
- **TDD:** no (a committed data/config file — its behavior is exercised by WU-2's tests).
- **Files to create/modify:** `user/scripts/compute-state-routing-parity.json` (net-new).
- **Implementation goal:** Author a JSON allowlist that enumerates each coupled `compute_state` routing branch as a row with: a stable `id`; a `signature` (the token or regex the branch is structurally recognized by in a script's source, expressed in canonical vocab so `apply_tokens` [VERIFY: `grep -n "def apply_tokens" user/scripts/lazy_parity_audit.py`] can substitute the legitimate `--feature-id`/`--bug-id`-class differences); a `classification` of `mirrored` (signature MUST appear in BOTH scripts) or `tabulated-divergence` (present in ONE script only); and a REQUIRED `reason` string on every `tabulated-divergence` row. Seed it so the CURRENT tree is fully classified: (a) the Round-93 Step-7 verification-only bypass — `bug-state.py`'s `cloud_bypass`/`workstation_bypass` conjuncts [VERIFY: `grep -n "workstation_bypass = not cloud" user/scripts/bug-state.py`] ↔ `lazy-state.py`'s `_feature_past_implementation` [VERIFY: `grep -n "_feature_past_implementation\|_has_any_complete_plan" user/scripts/lazy-state.py`] — as a `mirrored` branch; (b) the Round-92 research-pending exclusion, classified `mirrored` or `tabulated-divergence` per a code-read of whether the bug pipeline legitimately omits it (bugs have no research step — likely `tabulated-divergence` owned by `lazy-state.py`, but CONFIRM by reading both `compute_state` bodies before writing the row); (c) any other clearly-coupled routing branch you observe while reading (the shared skips: completion / cloud / device / host-capability / dep-gate). Include a top-of-file `_comment`/header field documenting the schema and the "add a `tabulated-divergence` row with a reason for a new justified divergence" workflow. The allowlist need not be exhaustive of every line in `compute_state` — it must cover the shared routing branches whose asymmetry would stall a live run (the two field recurrences plus the shared skip family), and it MUST be seeded so WU-2's audit exits 0 on the live tree.
- **Spec requirements:** SPEC `## Proven Findings` "Design constraint carried into `/plan-bug`: the census must be divergence-aware — a tabulated allowlist of the 11+ documented justified `compute_state` divergences" (lines 131–135); SPEC `## Evidence Collected` divergence inventory (lines 92–96).
- **Batch:** 1.

#### WU-2 — `audit_compute_state_routing_parity()` + wiring + tests

- **Scope:** PHASES.md Phase-1 deliverables: "`audit_compute_state_routing_parity(repo_root)` in `lazy_parity_audit.py` …", "Wire … into `audit_all_pairs` …", and the "Tests in `test_lazy_parity.py`" deliverable.
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/lazy_parity_audit.py`.
- **Test files:** `user/scripts/test_lazy_parity.py`.
- **Test expectations:** Add a `TestComputeStateRoutingParity` class modeled on `TestStateScriptParity` [VERIFY: `grep -n "class TestStateScriptParity" user/scripts/test_lazy_parity.py`]. Assert: (i) FIRES — given a fixture repo whose two scripts drop a `mirrored` branch's signature from one script, `audit_compute_state_routing_parity` returns ≥1 finding naming that branch + the missing script; (ii) TABULATED PASSES — a `tabulated-divergence` row present only in its declared owning script produces NO finding; (iii) MALFORMED — a missing/unparseable allowlist yields a loud ERROR finding (never a silent empty pass), mirroring the `except OSError` rows in `audit_state_script_parity` [VERIFY: `grep -n "cannot read" user/scripts/lazy_parity_audit.py`]; (iv) LIVE ZERO-DRIFT — a real-tree test (like `TestLiveZeroDrift` [VERIFY: `grep -n "class TestLiveZeroDrift" user/scripts/test_lazy_parity.py`]) asserting `audit_compute_state_routing_parity(REPO_ROOT)` returns `[]`; (v) INCLUDED — `audit_all_pairs` output includes the new check's findings (extend or mirror `test_included_in_audit_all_pairs` [VERIFY: `grep -n "def test_included_in_audit_all_pairs" user/scripts/test_lazy_parity.py`]). Tests MUST be RED before implementation for the right reason (the function/allowlist does not yet exist), not a collection/import error.
- **Implementation goal:** Add `audit_compute_state_routing_parity(repo_root)` to `lazy_parity_audit.py`, structured like `audit_merged_view_dispatch_parity` [VERIFY: `grep -n "def audit_merged_view_dispatch_parity" user/scripts/lazy_parity_audit.py`]: load the WU-1 allowlist (a `load_*` helper next to `load_manifest` [VERIFY: `grep -n "def load_manifest" user/scripts/lazy_parity_audit.py`]), read both scripts in `_STATE_SCRIPTS` [VERIFY: `grep -n "_STATE_SCRIPTS: tuple" user/scripts/lazy_parity_audit.py`], and for each `mirrored` row assert its `signature` (applied through `apply_tokens` for the per-script vocab) is present in BOTH scripts' `compute_state` region — emit one finding per (script, missing mirrored branch); a `tabulated-divergence` row is asserted present in its declared owning script only (and, defensively, absent-or-tolerated in the other). A missing/unreadable/malformed allowlist appends a loud ERROR finding. Wire the new function into `audit_all_pairs` [VERIFY: `grep -n "def audit_all_pairs" user/scripts/lazy_parity_audit.py`] immediately after the existing merged-view call [VERIFY: `grep -n "audit_merged_view_dispatch_parity(repo_root)" user/scripts/lazy_parity_audit.py`], so it runs in the default whole-repo invocation. Do NOT re-implement script-reading — reuse the `_STATE_SCRIPTS` + `path.read_text` pattern already in `audit_state_script_parity`.
- **Spec requirements:** SPEC `## Proven Findings` — the fix site "lies directly ON this serving path: `audit_all_pairs` (line 611) calls `audit_state_script_parity`, whose empty return IS the clean exit; a census added here (or in a sibling called from `audit_all_pairs`) is exactly what converts an unmirrored branch into a finding" (lines 116–119). SPEC `## Reproduction Steps` (the audit must now fail on an unmirrored branch in the same commit).
- **Batch:** 2 (depends on WU-1 — reads its allowlist; also file-disjoint from WU-1 but sequenced by the data dependency).

> **Batch overview — Phase P1**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | None            |
| 2     | WU-2      | Solo      | None (WU-2 depends on WU-1's allowlist schema) |

---

### Phase: routing-branch-parity P2 — Document the new coverage + allowlist location

**Goal:** Document that `lazy_parity_audit.py` now audits `compute_state` routing-branch symmetry and where the tabulated-divergence allowlist lives.
**Entry criteria:** Phase P1 complete (the allowlist file exists and its final path + the Round-92 classification outcome are settled — Phase 2 documents both).
**SPEC.md references:** `## Related Documentation` (lines 87–91 — the Coupling Rule is what this coverage mechanically enforces), `## Affected Area` docs row (line 144).

#### WU-3 — Document the routing-branch coverage + allowlist location

- **Scope:** PHASES.md Phase-2 deliverables: update `user/scripts/CLAUDE.md`, update root `CLAUDE.md`, and confirm `doc-drift-lint.py` stays exit 0.
- **TDD:** no (documentation edits, verified by the doc-drift linter).
- **Files to create/modify:** `user/scripts/CLAUDE.md`, `CLAUDE.md` (repo root).
- **Implementation goal:** In `user/scripts/CLAUDE.md` "Coupling Rule (HARD REQUIREMENT)" section [VERIFY: `grep -n "Coupling Rule (HARD REQUIREMENT)" user/scripts/CLAUDE.md`] (and/or the CLI quick-reference block [VERIFY: `grep -n "CLI quick reference" user/scripts/CLAUDE.md`]), state that the parity audit now asserts `compute_state` routing-branch symmetry via the declared allowlist, NAME the allowlist file (the exact path WU-1 created), and explain the `mirrored` vs `tabulated-divergence` classification + the "add a row with a reason" workflow for a new justified divergence. In the root `CLAUDE.md`, add a one-line note near the parity-audit / Coupled Skill Pairs description [VERIFY: `grep -n "lazy_parity_audit\|Coupled Skill Pairs" CLAUDE.md`] that routing-branch symmetry — the highest-churn coupled surface — is now mechanically enforced. Then run `python3 user/scripts/doc-drift-lint.py --repo-root .` and confirm exit 0 (fix any drift the edit introduces).
- **Spec requirements:** SPEC `## Related Documentation` — "The audit is the mechanical enforcement of this rule — but it enforces it only for named CLI/call-site surfaces, not for `compute_state` routing logic, which is the highest-churn coupled surface" (lines 88–91); SPEC `## Affected Area` docs row.
- **Batch:** 1 (Phase P2 has a single WU; no intra-phase conflicts).

> **Batch overview — Phase P2**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-3      | Solo      | None            |

---

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md/PHASES.md `**Status:**` to `Fixed`, writes the `FIXED.md` receipt, and archives the bug dir once implementation lands and the state machine routes completion — this plan authors NO status/receipt/archive rows.
