---
kind: fix-plan
feature_id: stub-spec-route-loops-until-queue-stub-cleared
status: Ready
created: 2026-06-20
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-20.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\stub-spec-route-loops-until-queue-stub-cleared\plans\fix-stub-clear-owner.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — stub-spec-route-loops-until-queue-stub-cleared (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\stub-spec-route-loops-until-queue-stub-cleared\PHASES.md` (1 phase)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\stub-spec-route-loops-until-queue-stub-cleared\SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Work Units

- [ ] WU-1 — Script-owned clear-and-advance at the Step-4.5 → Step-5 boundary

(Single WU: the helper, discriminator, routing wire, and tests are one mutually-dependent fix — the `clear_queue_stub` helper is untestable in isolation from its sole caller and the discriminator, and the smoke fixture asserts the end-to-end clear-and-advance. Splitting would create untestable intermediate states. This satisfies the partitioning "merge only when tightly coupled / single-file co-creation" exception, not "merge for convenience".)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`.

> **DISPATCH-LIMIT OVERRIDE (read if executed inside a lazy-bug cycle subagent):** if the executing session does NOT have the `Agent` tool (a lazy-bug cycle subagent), the HARD CONSTRAINT above is waived for THIS run only — perform all implementation and test-writing INLINE with `Read`/`Edit`/`Write` (test-first: write the failing fixture/test before the implementation). Never invoke `/lazy*`. The rest of this plan's contract (review, gates, commit policy, PHASES updates, sentinel discipline) stays authoritative.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies. This is a `docs/bugs/` harness-defect fix with no `**Depends on:**` block.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit`/`Write` on source/test files. EXCEPTIONS: (a) trivial PASS-WITH-FIXES items (a few lines); (b) the DISPATCH-LIMIT OVERRIDE above when run inside an Agent-less cycle subagent (then all work is inline + test-first).
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline — failing tests written BEFORE implementation.
4. PHASES.md is updated AFTER the batch completes (not deferred).
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing.
6. Mistakes are fixed immediately before proceeding.
7. After all batches finish, integration verification confirms changes work together.
8. Relevant CLAUDE.md files are created/updated after the phase if changes warrant it.
9. The completed phase is committed and pushed before completion.
10. This plan is self-contained — follow it exactly as written without relying on external context.
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1    | stub-spec-route-loops-until-queue-stub-cleared | P1 | Script-owned clear-and-advance at the Step-4.5 → Step-5 boundary | — | Solo |

## Phase: stub-spec-route-loops-until-queue-stub-cleared P1 — Script-owned clear-and-advance at the Step-4.5 → Step-5 boundary

**Goal:** Add a script-owned, deterministic clear of the `queue.json` `"stub"` flag at the moment the baseline is detected as locked (queue flag is the *lone surviving* stub marker), advancing the route to Step 5 in the same probe — closing the commit-masked Step-4.5 loop.

**Entry criteria:** None (first and only phase).

**SPEC.md references:**
- `## Proven Findings` (1–4): confirmed root cause, script-owned mutation, "baseline locked" clear-point, SPEC-text markers self-clear while the queue flag survives.
- `## Affected Area`: `lazy-state.py` (is_stub_spec L823/859, Step 4.5 L1980-1990, Step 5 L2072-2081), `lazy_core.py` (`_atomic_write` L104, `reorder_queue` L130 pattern), smoke fixtures.
- `## How "baseline locked" is detected`: the lone-surviving-marker discriminator.

### Batch 1 — WU-1 (Solo)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | N/A             |

#### WU-1: Script-owned clear-and-advance at the Step-4.5 → Step-5 boundary

**Scope (PHASES.md Phase 1 deliverables):**
- [ ] `clear_queue_stub(queue_path, feature_id)` in `lazy_core.py` mirroring `reorder_queue` [VERIFY: grep -n "def reorder_queue" C:/Users/Jacob/source/repos/claude-config/user/scripts/lazy_core.py] (load → validate list → find entry by `id == feature_id` → pop `"stub"` key → `_atomic_write` [VERIFY: grep -n "def _atomic_write" C:/Users/Jacob/source/repos/claude-config/user/scripts/lazy_core.py]). Missing `feature_id` / malformed JSON → `_die` [VERIFY: grep -n "def _die" C:/Users/Jacob/source/repos/claude-config/user/scripts/lazy_core.py] (exit 2, zero mutation). Entry without `"stub"` → byte-stable no-op (`cleared: False`). Returns `{cleared, feature_id, queue_length}`. Emits a `_diag` line on a real clear.
- [ ] Lone-surviving-marker discriminator `_stub_is_queue_flag_only(spec_text, queue_entry)` in `lazy-state.py`: True iff `queue_entry.get("stub") is True` AND none of the three SPEC-text stub markers match. Factor the SPEC-text-marker check into a shared predicate that BOTH `is_stub_spec` [VERIFY: grep -n "def is_stub_spec" C:/Users/Jacob/source/repos/claude-config/user/scripts/lazy-state.py] and this discriminator call (no duplicated conditions).
- [ ] Wire the clear-and-advance into the Step 4.5 branch (`lazy-state.py`, the `if is_stub_spec(spec_text, current.get("queue_entry")):` block) [VERIFY: grep -n "Step 4.5: Stub spec" C:/Users/Jacob/source/repos/claude-config/user/scripts/lazy-state.py]: BEFORE returning the Step-4.5 `/spec` dispatch, if `_stub_is_queue_flag_only(spec_text, current["queue_entry"])` is True, call `clear_queue_stub(repo_root / "docs" / "features" / "queue.json", feature_id)`, emit a `_diag` line, and FALL THROUGH to Step 4.6/Step 5 (do NOT return). When a SPEC-text marker IS present, behavior is byte-identical to today (dispatch `/spec` at Step 4.5).
- [ ] Tests: new `lazy-state.py --test` fixture `stub-queue-flag-lone-survivor` (queue `"stub": true` + structured SPEC with NO SPEC-text marker + no research) → assert `current_step == "Step 5: generate research prompt"`, `sub_skill == "spec"`, AND the on-disk queue entry no longer carries `"stub"`. Adjust the existing `stub-queue-flag-only` fixture's SPEC to carry a genuine pre-baseline SPEC-text marker (so it still asserts Step 4.5). Add `clear_queue_stub` unit coverage to `test_lazy_core.py` (present-clears / absent-no-op / `_die` on malformed-or-missing-id).

**TDD:** yes — write the failing `stub-queue-flag-lone-survivor` smoke fixture + the `test_lazy_core.py` `clear_queue_stub` cases FIRST (they fail: the discriminator routes to Step 4.5 today, and `clear_queue_stub` does not exist), THEN implement to green.

**Files to create/modify:**
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\lazy_core.py` — add `clear_queue_stub(queue_path, feature_id)` (mirror `reorder_queue`'s load→validate→mutate→`_atomic_write`; reuse `_die`/`_diag`) + a shared SPEC-text-stub-marker predicate factored out of `is_stub_spec`'s first four branches.
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\lazy-state.py` — add `_stub_is_queue_flag_only`; wire the clear-and-advance at the Step-4.5 branch; refactor `is_stub_spec` to call the shared predicate (behavior-preserving); add the new `--test` fixture + assertion; adjust the existing `stub-queue-flag-only` fixture's SPEC.
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\test_lazy_core.py` — add `clear_queue_stub` unit coverage.
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\tests\baselines\lazy-state-test-baseline.txt` — regenerate from live `--test` output piped through `_normalize_smoke_output` (NOT by hand). [VERIFY: ls C:/Users/Jacob/source/repos/claude-config/user/scripts/tests/baselines/lazy-state-test-baseline.txt]

**Test files:**
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\test_lazy_core.py` (unit cases for `clear_queue_stub`)
- The in-file `--test` harness inside `lazy-state.py` (the `stub-queue-flag-lone-survivor` fixture + assertion)

**Test expectations:**
- `lazy-state.py --test`: the new fixture drives the REAL `compute_state()` over a real temp-dir `queue.json` + structured SPEC and asserts (a) the route advanced to `Step 5: generate research prompt` / `sub_skill: spec` (NOT a Step-4.5 re-fire), (b) the on-disk queue entry's `"stub"` key was removed.
- `test_lazy_core.py`: `clear_queue_stub` removes `"stub"` when present (`cleared: True`); is a byte-stable no-op when absent (`cleared: False`); `_die`s (exit 2) on malformed queue JSON and on a missing `feature_id`; confirms `is_stub_spec` is behavior-preserving after the shared-predicate refactor (the existing stub-detection fixtures stay green).

**Implementation goal:** Close the commit-masked Step-4.5 loop by clearing the surviving queue flag exactly once, at baseline-lock, under script ownership — so the next probe advances to Step 5 instead of re-firing Step 4.5. The discriminator MUST be reachable only post-baseline (SPEC-text markers gone), so it never fires on a true pre-baseline stub. The mutation MUST live in `lazy_core` (per HARD CONSTRAINT 1 — never an orchestrator hand-edit of queue.json).

**Spec requirements:** SPEC `## Proven Findings` 1–4; `## How "baseline locked" is detected`; `## Affected Area`.

**Authoring notes (carried from PHASES.md — do NOT re-derive):**
- **Do NOT clear unconditionally inside `is_stub_spec`** — that would mutate during a true pre-baseline stub and break the Step-4.5 design. Clear ONLY via the discriminator (queue flag is lone surviving marker).
- **bug-state.py parity:** intentionally NOT mirrored — the bug pipeline has no stub step. `clear_queue_stub` lives in shared `lazy_core` but is invoked only by `lazy-state.py`. `bug-state.py --test` MUST stay green (the shared-module change must not regress it). This is the correct divergence, not a parity gap.
- ⚖ policy: secondary substring tightening (Theory 3) → in-cycle ONLY if trivially free while factoring the shared SPEC-text predicate; otherwise leave as the SPEC's logged follow-up (SPEC Open Questions: "do not gate the primary fix on it").

**Batch:** 1

## Execution Protocol

This protocol governs autonomous execution. Follow it exactly.

### Phase Selection Loop

Single phase. Execute Step 0, then Batch 1 (WU-1) per the Per-Batch Steps, then the Post-Phase Steps, then Completion.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (task tool loading via ToolSearch, task creation for WU-1's test + impl, update protocol). (If executed in an Agent-less cycle subagent without task tools, skip task-tool loading and track progress via the `- [ ] WU-N` checkbox + PHASES.md boxes instead.)

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)
Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: PHASES.md, SPEC.md (relevant sections), and this plan file. Also re-read `user/scripts/CLAUDE.md` (the Coupling Rule + Testing section — the baseline-regen procedure is load-bearing).

#### Step B.1: Launch Subagents (or implement INLINE under the dispatch-limit override)
Read `~/.claude/skills/_components/tdd-protocol.md`, `subagent-launch.md`, `tdd-test-agent.md`, `implementation-agent.md`. WU-1 is TDD=yes: write the failing `stub-queue-flag-lone-survivor` fixture + `test_lazy_core.py` cases FIRST (confirm RED for the right reason — the discriminator routes to Step 4.5 today and `clear_queue_stub` is undefined), THEN implement to green.

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md` + `mount-site-verification.md`. Run the full protocol (scope measurement, ground-truth verification, propagation check, verdict). Confirm the discriminator never fires on a pre-baseline stub (a SPEC-text marker present ⇒ no clear), and that `is_stub_spec` is behavior-preserving.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `~/.claude/skills/_components/phases-update.md`. Check off Phase 1 deliverables, add an Implementation Notes block (date, work completed, files modified, the parity rationale for not mirroring bug-state.py).

#### Step B.4: Run Quality Gates (MANDATORY)
Read `~/.claude/skills/_components/quality-gates.md`, then read `.claude/skill-config/quality-gates.md` in the repo root if present. The load-bearing gates for THIS change (run all three — a shared-`lazy_core` edit touches both state machines):
```
python user/scripts/lazy-state.py --test
python user/scripts/bug-state.py --test
python user/scripts/test_lazy_core.py
```
All must pass. After they pass, REGENERATE the byte-pinned baseline per `user/scripts/CLAUDE.md` → Testing: pipe live `lazy-state.py --test` output through `_normalize_smoke_output` (in `test_lazy_core.py`) and write the result to `user/scripts/tests/baselines/lazy-state-test-baseline.txt` — never hand-edit it. Then re-run `python user/scripts/test_lazy_core.py` to confirm the baseline comparison is green. Optionally run `python ~/.claude/scripts/lint-skills.py` (no skill files changed, so expected clean).

#### Step B.4.5: MCP Integration Test
N/A — no MCP surface (claude-config has no MCP server; PHASES.md `MCP runtime: not-required`). Skip with a note.

#### Step B.5: Commit Batch
Read `.claude/skill-config/commit-policy.md` (repo root) if present; else `~/.claude/skills/_components/commit-and-push.md`. Commit + push to `main` per policy. Re-assert `git rev-parse --abbrev-ref HEAD` == `main` immediately before committing.

#### Step B.6: Proceed
- [ ] Review report with verdict produced
- [ ] PHASES.md updated
- [ ] All three `--test` gates pass + baseline regenerated and green
- [ ] Committed per policy

### Post-Phase Steps

#### Integration Verification (MANDATORY)
Read `~/.claude/skills/_components/integration-verification.md`. Confirm the end-to-end loop closure: the `stub-queue-flag-lone-survivor` fixture proves a stub-shaped queue entry + structured SPEC now advances to Step 5 with the flag cleared, AND the existing `stub-pre-gemini-marker` / adjusted `stub-queue-flag-only` fixtures still assert Step 4.5 (no over-clearing of true pre-baseline stubs).

#### Update CLAUDE.md Files (MANDATORY)
Read `~/.claude/skills/_components/claude-md-review.md`. Consider a one-line note in `user/scripts/CLAUDE.md` documenting the Step-4.5 clear-owner (`clear_queue_stub` + the lone-surviving-marker discriminator) so the new state branch is discoverable, consistent with how other state-machine branches are documented there.

#### Commit and Push Post-Phase Changes
Commit + push any PHASES.md / CLAUDE.md updates to `main` per the commit policy.

## Blocking Issue Protocol

If a blocking issue is encountered:
1. Stop all in-progress work.
2. Commit + push any completed work.
3. Print a blocking-issue report (completed work, blocked WU, reason, recovery suggestion, remaining work).
4. Do not work around the blocker.

Blocking issues include: a subagent failure unfixable after 2 retries; a `--test` failure unfixable after 2 retries; a git push conflict unresolvable by rebase; any error requiring architectural decisions beyond the spec's scope (e.g. the discriminator cannot be made pre-baseline-safe without a SPEC change).

## Completion

When the phase is complete:
1. Run the full gate suite one final time: `lazy-state.py --test`, `bug-state.py --test`, `test_lazy_core.py` — all green, baseline regenerated and comparison passing.
2. Print a completion report (feature implemented, phase completed, commits, final gate status all green, the loop-closure note + the bug-state.py parity rationale).
