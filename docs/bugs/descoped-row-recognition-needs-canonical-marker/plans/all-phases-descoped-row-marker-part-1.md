---
kind: implementation-plan
feature_id: descoped-row-recognition-needs-canonical-marker
status: Ready
created: 2026-07-12
phases: [1]
complexity: complex
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-12.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\descoped-row-recognition-needs-canonical-marker\plans\all-phases-descoped-row-marker-part-1.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 2. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\descoped-row-recognition-needs-canonical-marker\plans\all-phases-descoped-row-marker-part-1.md` (this file)
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\descoped-row-recognition-needs-canonical-marker\plans\all-phases-descoped-row-marker-part-2.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — descoped-row-recognition-needs-canonical-marker (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\descoped-row-recognition-needs-canonical-marker\PHASES.md` (descoped-row-recognition-needs-canonical-marker, 2 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\descoped-row-recognition-needs-canonical-marker\SPEC.md`

**Total phases:** 2 (this part covers Phase 1 only)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths: **claude-config is NOT a Cognito/Nx/Tauri repo** — there is no `/msbuild`/`/mstest`/`/nxbuild`/`/nxtest`/`/tauri-build` gate here. This repo's quality gate is:

```bash
python3 user/scripts/test_lazy_core.py          # pytest suite covering lazy_core.py helpers
python3 user/scripts/lazy-state.py --test       # in-file smoke harness (imports lazy_core)
python3 user/scripts/bug-state.py --test        # in-file smoke harness (imports lazy_core; coupled pair)
```

Run all three after every batch in this part — `lazy_core.py` is shared by both state scripts, so a change here must keep BOTH `--test` suites green, not just `test_lazy_core.py`. Everything else the contract specifies that this plan does not override still applies.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies; SPEC.md's `**Related:**` links to `docs/bugs/verification-only-bypass-blind-to-descoped-rows` and the `_VERIFICATION_ONLY_MARKER` precedent are informational prior-art, not a dependency edge requiring look-back.)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | descoped-row-recognition-needs-canonical-marker | P1 | Canonical structural descope marker + detector promotion + regression fixtures | — | Solo (WU-1 then WU-2, sequential — TDD test-then-impl on the same function) |

### Phase: descoped-row-recognition-needs-canonical-marker P1 — Canonical structural descope marker + detector promotion + regression fixtures

**Goal:** Add the SSOT structural marker `_DESCOPED_MARKER = "<!-- descoped -->"` to `lazy_core.py`, make it the PRIMARY signal in the descope-recognition path (row-scope AND header-scope), and demote the existing `_DESCOPE_STRIKETHROUGH_RE`/`_DESCOPE_MARKER_RE` free-text pair to a deprecation shim that still exempts (no regression) but emits a migration diagnostic when it (not the marker) is what exempted a row.

**Entry criteria:** None — first phase of this bug fix.

**SPEC.md references:** "Proven Findings" → "Fix scope" items 1, 2, 4 (marker addition, regex demotion, regression fixtures); "Root cause" trace citing `lazy_core.py:2259` (`_DESCOPE_MARKER_RE`) as the fix site.

## Work Units

- [ ] WU-1 — Regression fixtures encoding the new marker/shim/header-scope behavior (TDD tests)
- [ ] WU-2 — `_DESCOPED_MARKER` constant + marker-primary detection + shim demotion (implementation)

---

### WU-1 — Regression fixtures encoding the new marker/shim/header-scope behavior

**Scope:** Add new pytest fixtures to `user/scripts/test_lazy_core.py`, co-located immediately after the existing descoped-in-place block (the 3 tests at lines 926–981: `test_verification_only_descoped_dropped_row_is_true`, `test_verification_only_plain_unchecked_row_still_false`, `test_verification_only_struck_without_descope_marker_still_false` — DO NOT MODIFY these 3; they are the conservatism regression net and must keep passing unchanged).

**TDD:** yes — these tests MUST fail (or error) against the CURRENT `lazy_core.py` before WU-2 lands, because `lazy_core._DESCOPED_MARKER` does not exist yet [VERIFY: grep -n "_DESCOPED_MARKER" user/scripts/lazy_core.py — expect ZERO hits before this plan runs]. An `AttributeError: module 'lazy_core' has no attribute '_DESCOPED_MARKER'` (or equivalent `False`-when-it-should-be-`True` assertion failure) is the correct RED reason for this greenfield addition — not a compile/import error masking a wrong assumption.

**Files to create/modify:**
- `user/scripts/test_lazy_core.py` — add new test functions immediately after line 981 (end of `test_verification_only_struck_without_descope_marker_still_false`), and register each in the `_TESTS` list immediately after the existing registration block at lines 16453–16456 [VERIFY: grep -n "test_verification_only_struck_without_descope_marker_still_false" user/scripts/test_lazy_core.py — confirm both the def (line 966) and the `_TESTS` tuple registration (line 16456) locations before inserting].

**Files NOT to touch:** `user/scripts/lazy_core.py` (WU-2 owns this file). Do not modify the 3 existing sibling tests.

**Test expectations — write these 4 new test functions (mirror the existing 3's exact style: `_guard()` first line, a hand-built `phases_text` string, call `lazy_core.remaining_unchecked_are_verification_only(text)`, assert with a descriptive f-string message):**

1. `test_verification_only_descoped_marker_only_row_is_true` — a row carrying `<!-- descoped -->` with NO free-text keyword (`**DROPPED**`/`**DESCOPED**`/`**WON'T-FIX**` absent) and no strikethrough required either (the marker path does not need strikethrough — mirror `_VERIFICATION_ONLY_MARKER`, which needs no accompanying regex match). Example row: `"- [ ] <!-- descoped --> some deliverable dropped by decision 2\n"`. Assert `remaining_unchecked_are_verification_only(text) is True`.
2. `test_verification_only_legacy_dropped_row_still_true_with_migration_diagnostic` — the EXACT same struck `**DROPPED**` row shape as `test_verification_only_descoped_dropped_row_is_true` (legacy path, no marker). Assert the result is still `True` (no regression) AND assert a migration diagnostic was recorded. First inspect how the existing `_VERIFICATION_SECTION_RE` shim's diagnostic is asserted elsewhere in this file (search for the diagnostics-read pattern used by any existing test asserting on the verification-marker shim's warning — e.g. search for `_DIAGNOSTICS` or a diagnostics-accessor used near the verification-only shim tests) [VERIFY: grep -n "_DIAGNOSTICS" user/scripts/lazy_core.py user/scripts/test_lazy_core.py] and reuse that EXACT mechanism (do not invent a second diagnostics-read path). If no existing test asserts the verification shim's diagnostic today, assert directly against whatever `_diag`-populated list/accessor `lazy_core.py` exposes (read the `_diag` function's definition to find where it appends and what test code can read back).
3. `test_verification_only_descoped_marker_no_diagnostic` — the marker-only row from test 1 must NOT emit a migration diagnostic (the marker path is the primary, non-deprecated path — no warning belongs there). Assert the diagnostics list/accessor is empty (or unchanged) after calling `remaining_unchecked_are_verification_only` on marker-only text.
4. `test_verification_only_descoped_header_scope_marker_exempts_rows_beneath` — a bold or heading subsection line carrying `<!-- descoped -->` (e.g. `"**Descoped:** <!-- descoped -->\n"` or `"### Descoped <!-- descoped -->\n"` — pick whichever form mirrors the verification marker's header-scope test most closely; read `test_verification_only_bold_marker_format_preserved` / the heading-branch tests near lines 16445–16449 for the exact header shape convention used elsewhere in this file) followed by one or more plain `- [ ]` rows with NO per-row marker and NO free-text keyword. Assert `True` (the header-scope marker exempts every row beneath it, mirroring `section_has_marker` for verification rows).

Also verify the 3 EXISTING tests (`test_verification_only_descoped_dropped_row_is_true`, `test_verification_only_plain_unchecked_row_still_false`, `test_verification_only_struck_without_descope_marker_still_false`) are unmodified and still present at their original lines — run them now (pre-WU-2) to confirm they currently PASS (they test existing behavior, not new behavior, so they are green today and must stay green).

**Test commands to run for verification:**
```bash
python3 user/scripts/test_lazy_core.py
```
Confirm the 4 new tests FAIL (or error on the missing `_DESCOPED_MARKER` attribute) for the right reason, and the 3 existing sibling tests still PASS.

---

### WU-2 — `_DESCOPED_MARKER` constant + marker-primary detection + shim demotion

**Scope:** Implement the production logic in `user/scripts/lazy_core.py` that makes WU-1's 4 new tests pass while keeping the 3 existing tests green (zero regression).

**TDD:** yes — implements against WU-1's failing tests.

**Files to create/modify:**
- `user/scripts/lazy_core.py`:
  1. Add `_DESCOPED_MARKER = "<!-- descoped -->"` as a module-level constant immediately after `_DESCOPE_MARKER_RE` [VERIFY: grep -n "_DESCOPE_MARKER_RE = re.compile" user/scripts/lazy_core.py — line 2259], with a comment block mirroring `_VERIFICATION_ONLY_MARKER`'s rationale comment [VERIFY: grep -n "_VERIFICATION_ONLY_MARKER = " user/scripts/lazy_core.py — line 2192; read lines 2164–2191 for the comment shape to mirror] — including the same "check-docs-consistency.ts fallback: row annotation, not a sentinel" note.
  2. Update `_row_is_descoped_in_place` [VERIFY: grep -n "def _row_is_descoped_in_place" user/scripts/lazy_core.py — line 2265] and/or the row-scanning branch inside `remaining_unchecked_are_verification_only` that calls it [VERIFY: grep -n "_row_is_descoped_in_place(stripped)" user/scripts/lazy_core.py — line 2423] so a row carrying `_DESCOPED_MARKER` is exempt as the PRIMARY signal — independent of `_DESCOPE_MARKER_RE` and independent of the strikethrough requirement. Mirror the existing `row_has_marker`/`section_has_marker` split used for verification rows [VERIFY: grep -n "row_has_marker = _VERIFICATION_ONLY_MARKER in line" user/scripts/lazy_core.py — line 2427] to add an analogous `row_has_descope_marker` / `section_has_descope_marker` pair for the descope axis. Header-scope: extend the existing bold/heading-header branches [VERIFY: grep -n "section_has_marker = _VERIFICATION_ONLY_MARKER in line" user/scripts/lazy_core.py — line 2369; grep -n "if _VERIFICATION_ONLY_MARKER in line:" user/scripts/lazy_core.py — line 2391] to ALSO check for `_DESCOPED_MARKER` on the same header lines, tracking a parallel `section_has_descope_marker` flag that resets on the same phase/section boundaries as `section_has_marker`.
  3. Demote `_DESCOPE_STRIKETHROUGH_RE`/`_DESCOPE_MARKER_RE` to a deprecation shim: when a row is exempted via the LEGACY strikethrough+keyword path (i.e., `_row_is_descoped_in_place(stripped)` returns True) and the marker is ABSENT (neither `row_has_descope_marker` nor `section_has_descope_marker`), emit a `_diag(...)` warning naming the row text — reuse the EXACT dedup mechanism already used for the verification shim's `warned_headers` set [VERIFY: grep -n "warned_headers" user/scripts/lazy_core.py — lines 2326, 2436, 2437] (either reuse `warned_headers` itself if row-text dedup fits its existing per-key semantics, or add a small sibling dedup set scoped identically — do not invent a structurally different mechanism).
  4. Update the constant/comment block and the `_row_is_descoped_in_place` docstring to document that the marker path requires NO strikethrough (only the legacy free-text path requires BOTH strikethrough and keyword) — so a future reader does not conflate the two paths.
  5. Update `remaining_unchecked_are_verification_only`'s docstring (currently describing the descoped-in-place behavior at lines ~2315–2321) to mention the new marker-primary path alongside the existing legacy-keyword description.

**Files NOT to touch:** `user/scripts/test_lazy_core.py` (WU-1 owns the tests; this WU makes them pass without editing them, except if a WU-1 test's diagnostic-assertion mechanism needs a matching read-accessor added to `lazy_core.py` itself — that accessor addition belongs here if it doesn't already exist).

**Implementation goal:** All 4 of WU-1's new tests pass, AND the 3 pre-existing descoped-in-place tests (lines 926–981) still pass unmodified, AND `python3 user/scripts/lazy-state.py --test` and `python3 user/scripts/bug-state.py --test` both stay green (both import `lazy_core` and exercise this function transitively via their own smoke fixtures).

**Spec requirements:** SPEC.md "Proven Findings → Fix scope" items 1–2 (canonical marker + regex demotion), item 4 (regression fixtures — covered jointly with WU-1). SPEC.md Open Question 2 ("Header-scope vs. row-scope for the marker") is resolved toward **supporting both**, per the SPEC's own recommendation to mirror the `_VERIFICATION_ONLY_MARKER` precedent.

**Test commands to run for verification:**
```bash
python3 user/scripts/test_lazy_core.py
python3 user/scripts/lazy-state.py --test
python3 user/scripts/bug-state.py --test
```
All three must exit clean/green.

---

## Batch overview (Phase 1)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|------------------|
| 1     | WU-1      | Solo      | N/A (test file only) |
| 2     | WU-2      | Solo      | N/A (implementation file only; sequenced after WU-1 per TDD — WU-2 makes WU-1's RED tests GREEN) |

**Plan-specific execution notes:** This is a pure Python logic change with a hermetic pytest-style regression suite (`test_lazy_core.py`'s own `_guard()`/`_TESTS` convention, not standard pytest discovery — follow the file's existing idiom exactly, do not introduce `pytest.mark` decorators or a different test style). No build step, no MCP runtime, no quality-gate script beyond the three commands above. Per PHASES.md's `**MCP runtime:** not-required` line, skip any MCP-related steps in the execution contract that assume a runtime/device axis.

## Post-Phase-1 Steps

After WU-1 and WU-2 both pass review (per the Batch Review protocol in `execution-contract.md`):

1. Tick `- [ ] WU-1` and `- [ ] WU-2` above to `- [x]`.
2. In the sibling `PHASES.md`, tick Phase 1's deliverable checkboxes that are now satisfied (the marker constant, detection promotion, shim demotion, strikethrough-requirement documentation, and regression fixtures rows) — re-read `PHASES.md` Phase 1's `### Deliverables` list and check off each row this batch actually closed.
3. Set this plan part's frontmatter `status:` to `Complete` only if BOTH work units are done and all three test commands are green — otherwise leave `In-progress` and report what remains.
4. Commit + push per the repo's commit policy (`.claude/skill-config/commit-policy.md` — Conventional Commits, no AI attribution, push allowed).
5. Proceed to **part 2** (`all-phases-descoped-row-marker-part-2.md`) — do NOT start part 2's work until this part's commit has landed.

## Blocking Issue Protocol

Per `~/.claude/skills/_components/execution-contract.md`'s Blocking Issue Protocol. If WU-1's diagnostic-assertion mechanism cannot be determined from reading `lazy_core.py`'s `_diag` function (e.g. no test-readable accessor exists at all for the verification-marker shim's own diagnostic today), this is a **mechanical discovery gap, not a product fork** — read `_diag`'s definition, add a minimal test-readable accessor if genuinely absent (e.g. exposing the per-invocation `_DIAGNOSTICS` list), and proceed; do not halt for this.

## Completion

Per `~/.claude/skills/_components/execution-contract.md`'s Completion report format. This part's completion report additionally states: "Phase 1 of descoped-row-recognition-needs-canonical-marker complete — proceed to part 2."
