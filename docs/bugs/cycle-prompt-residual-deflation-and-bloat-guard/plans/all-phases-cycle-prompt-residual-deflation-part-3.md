---
kind: implementation-plan
feature_id: cycle-prompt-residual-deflation-and-bloat-guard
status: Complete
created: 2026-07-20
phases: [2]
complexity: complex
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-20.
> To execute: `/execute-plan {path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 3 of 3. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-prompt-residual-deflation-and-bloat-guard\plans\all-phases-cycle-prompt-residual-deflation-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-prompt-residual-deflation-and-bloat-guard\plans\all-phases-cycle-prompt-residual-deflation-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-prompt-residual-deflation-and-bloat-guard\plans\all-phases-cycle-prompt-residual-deflation-part-3.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings
> during execution. **This part (Phase 2) DEPENDS on part 1 (Phase 1) being complete** — its
> per-section byte ceiling is seeded from the deflated sizes part 1 produces, and both phases
> write the shared `skill-size-baseline.json` (single-writer discipline — do not run concurrently
> with part 1). Do NOT begin this part until part 1's `--lock-in-profile` re-lock has landed.

# Implementation Plan — Cycle-Prompt Residual Deflation + Anti-Bloat Guard (part 3 of 3)  (v2)

**PHASES.md files:**
- `docs/bugs/cycle-prompt-residual-deflation-and-bloat-guard/PHASES.md` (cycle-prompt-residual-deflation-and-bloat-guard, 3 phases)

**SPEC.md files:**
- `docs/bugs/cycle-prompt-residual-deflation-and-bloat-guard/SPEC.md`

**Total phases:** 3 (this part covers Phase 2 only)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (e.g. a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and any local `## Component Reference Card` override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

**claude-config-specific override:** this repo's gates are Python/projection — `pytest`, `python3 user/scripts/lazy-state.py --test`, `python3 user/scripts/lint-skills.py`, `python3 user/scripts/generate-coupled-skills.py --check`, `python3 user/scripts/skill-size-ratchet.py --check`, `python3 user/scripts/validate-plan.py --structural <path>` — NOT `msbuild`/`mstest`/`nx`. Step B.4 (Quality Gates) and this part's per-phase Minimum Verifiable Behavior below govern which of these run per batch.

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against. The executing session SHOULD `Read` these before starting any work unit whose Implementation goal references the corresponding upstream contract. Listed in dependency order.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| cycle-prompt-deflation | composes | `docs/features/cycle-prompt-deflation/PHASES.md` | Parent feature — `skill-size-ratchet.py`'s existing per-profile ratchet (extended here to per-section + per-pattern) originates here. |
| cycle-prompt-deflation | composes | `docs/features/cycle-prompt-deflation/IMPLEMENTATION_NOTES.md` | The ratchet mechanics (`check_profiles`, `lock_in_profile`, baseline JSON shape) this phase's new checks compose with. |
| lazy-batch-skill-deflation | composes | (prior art per PHASES.md Cross-feature Integration Notes — the prose→verdict-rule playbook and `skill-size-ratchet.py` gate both this bug and the parent feature extend; no new pattern invented here) | Confirms the per-section + per-pattern generalization is extending an established gate, not inventing a new mechanism. |
| anti-overfit-design-gate | hard (design constraint) | `docs/gate/control-surfaces.json` + `user/scripts/harness-gate.py` | This phase's war-story detector edits a matcher/pattern set inside `skill-size-ratchet.py`, a file this phase adds to the control-surface manifest — `harness-gate.py --staged` MUST run on the diff; if it flags `overfit` (or any detector), `GATE_VERDICT.md` must be authored per `_components/harness-change-gate.md` before this phase is done. |

**Part 1 dependency (this bug, same series):** `docs/bugs/cycle-prompt-residual-deflation-and-bloat-guard/plans/all-phases-cycle-prompt-residual-deflation-part-1.md` MUST be `status: Complete` before this part begins — its `skill-size-baseline.json` profile re-lock is the seed floor this phase's per-section ceiling is measured against, and `docs/bugs/cycle-prompt-residual-deflation-and-bloat-guard/SEMANTIC_DIFF.md` (part 1's WU-3 output) is the record of what survived deflation that this phase's fixtures should not re-flag as war-story.

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | cycle-prompt-residual-deflation-and-bloat-guard | P2 | Standing anti-bloat guard: war-story lint + per-section ceiling (D2a) | Part 1 (Phase 1) Complete | Solo |

### Phase: cycle-prompt-residual-deflation-and-bloat-guard P2 — Standing anti-bloat guard: war-story lint + per-section ceiling (D2a)

**Goal:** Extend `skill-size-ratchet.py` with a HARD-gate war-story pattern detector and a per-section byte ceiling over the dispatched-prompt template family, so a future harden round re-accreting narrative is refused at authoring time. Composes with, does not replace, the existing whole-assembled-profile ceiling.

**Entry criteria:** Phase 1 complete (part 1 of this series) — per-section ceilings seed from the deflated sizes it produces; both phases share the `skill-size-baseline.json` writer (single-writer discipline).

**SPEC.md references:** "Proposed Fix Shape (for /plan-bug)" → Deliverable 2, "2a. Mechanical lint"; "Resolved Decisions (operator disposition 2026-07-20)" 1–4 (Locked Decisions, do not re-open); PHASES.md Phase 2.

**Scope note (Locked Decisions — binding, do not re-open):**
1. Lint severity → HARD GATE from day one (no advisory-first soak).
2. Detector breadth → CONFIRMED SHAPES ONLY: ISO-date tokens `\b20\d\d-\d\d-\d\d\b`, `ISSUE \d` / `Round \d+` / `d8-effect-chains`, the literal `Live incident:`, bare `docs/{bugs,features}/<slug>` incident literals. Loose narrative phrasing (`the former … ban`, `used to`, `previously … now`) is a DELIBERATE accepted miss — caught by the per-section byte ceiling + the part-2 `CLAUDE.md` contract instead, not by this detector.
3. Detector home → fold into `skill-size-ratchet.py` (no new script), reachable via `lint-skills.py --check-skill-size`.
4. Allowlist → REASON-REQUIRED INLINE ENTRIES, mirroring `cli-surface-lint.py`'s `<!-- marker -->` style and `lint-skill-config.py`'s `SUPPRESSIONS`.

`TDD: yes` for this whole phase.

#### WU-1 (tests, TDD test WU) — Write failing tests first

**Scope:** Write failing tests in `user/scripts/test_skill_size_ratchet.py` [VERIFY: `grep -rl "skill_size_ratchet\|skill-size-ratchet" user/scripts/ | grep -i test` → confirmed `user/scripts/test_skill_size_ratchet.py`] asserting:
- (a) A fixture dispatched-prompt template carrying each confirmed war-story shape — ISO-date `\b20\d\d-\d\d-\d\d\b`, `ISSUE \d` / `Round \d+` / `d8-effect-chains`, `Live incident:`, bare `docs/{bugs,features}/<slug>` — FAILS the check, naming the file AND the matched shape.
- (b) A fixture section over its per-section byte ceiling FAILS, naming the section.
- (c) A reason-required inline allowlist entry RESCUES a genuine load-bearing literal that would otherwise match a shape.
- (d) An ordinary `SKILL.md` / docs file carrying a date is NOT flagged — scope is dispatched-prompt templates ONLY (`user/skills/_components/lazy-batch-prompts/*.md` + per-repo `<repo>/.claude/skill-config/cycle-prompt-addenda.md`), never orchestrator/docs prose.

**TDD:** yes (this is the test-first half).

**Test files:**
- `user/scripts/test_skill_size_ratchet.py`

**Test expectations:** Four new test functions (or parametrized cases) covering (a)–(d) above, each asserting on the function names/CLI exit code the implementation WU below settles — write against `check()` (existing anchor, [VERIFY: `grep -n "def check" user/scripts/skill-size-ratchet.py` → line 350]) extended with the new detector/ceiling, calling it with fixture content (tmp files or in-memory strings, matching the existing test file's fixture style) rather than real repo files, so the tests are hermetic and don't depend on Phase 1's actual output. Confirm RED (tests fail because the detector/ceiling don't exist yet) before proceeding to WU-2.

**Implementation goal:** N/A (test-only WU).

**Spec requirements:** PHASES.md Phase 2 Deliverables, first bullet (verbatim test-list source for (a)–(d)); SPEC "Resolved Decisions" 1–4.

**Batch:** 1.

#### WU-2 (impl, TDD) — Implement detector + per-section ceiling + allowlist

**Scope:** Make WU-1's tests pass:
1. **War-story pattern detector** — confirmed-shapes-only, structural (shape-keyed, not incident-literal — so it passes `harness-gate.py`'s own overfit detector [VERIFY: `grep -n "def main" user/scripts/harness-gate.py`]): the four shape classes from Locked Decision 2 above. Scope strictly to `user/skills/_components/lazy-batch-prompts/*.md` + per-repo `<repo>/.claude/skill-config/cycle-prompt-addenda.md` — never `SKILL.md` or general docs.
2. **Per-section byte ceiling** — extend `user/scripts/skill-size-baseline.json` with a new per-`@section` block (keyed by section name [VERIFY: section names enumerated via `grep -n "@section" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`], seeded at the sizes produced by part 1 of this series) and a corresponding check function in `user/scripts/skill-size-ratchet.py` alongside the existing per-file (`check`, L350 [VERIFY: `grep -n "def check(" user/scripts/skill-size-ratchet.py` → line 350]) and per-profile (`check_profiles`, L260 [VERIFY: `grep -n "def check_profiles" user/scripts/skill-size-ratchet.py` → line 260]) checks.
3. **Reason-required inline allowlist** for genuine load-bearing literals that would otherwise trip the detector — mirror `cli-surface-lint.py`'s `<!-- marker -->` [VERIFY: `grep -n "<!-- marker\|marker -->" user/scripts/cli-surface-lint.py`] / `lint-skill-config.py`'s `SUPPRESSIONS` [VERIFY: `grep -n "SUPPRESSIONS" user/scripts/lint-skill-config.py`] pattern — every exemption carries a reason at its point of use.
4. Fold both new checks into the default `--check` path (`main()`, L425 [VERIFY: `grep -n "def main" user/scripts/skill-size-ratchet.py` → line 425]) and confirm they run reachable via `python3 user/scripts/lint-skills.py --check-skill-size` [VERIFY: `grep -n "check-skill-size\|check_skill_size" user/scripts/lint-skills.py` → lines 399, 527, 538] — verify the existing wiring already shells the full ratchet `--check` (no new battery entry needed if so; if `lint-skills.py` calls a narrower subset, widen the call site instead of adding a second entry).

**TDD:** yes (this is the implementation half — WU-1's tests must go GREEN, no new test authorship in this WU beyond what's needed to make WU-1's cases pass).

**Files to create/modify:**
- `user/scripts/skill-size-ratchet.py`
- `user/scripts/skill-size-baseline.json` (new per-section block; do not touch the profiles block part 1 already re-locked)
- `user/scripts/lint-skills.py` (only if WU-1's case (a)/(b) is not already reachable via the existing `--check-skill-size` wiring)

**Implementation goal:** Reuse `lazy_core._atomic_write` for the baseline-JSON writer [VERIFY: `grep -n "_atomic_write" user/scripts/lazy_core/*.py`] — never a bespoke write. `check()` / `check_profiles()` / `main()` are the anchors to extend, not re-implement.

**Spec requirements:** SPEC "Proposed Fix Shape" Deliverable 2a bullets 1–2; PHASES.md Phase 2 Deliverables bullets 2–5; "Resolved Decisions" 1–4.

**Batch:** 2 (after WU-1 — implementation follows the failing tests).

**Minimum Verifiable Behavior:** adding any confirmed war-story shape to a dispatched-prompt template makes `python3 user/scripts/lint-skills.py --check-skill-size` exit 1, naming the file and the shape; removing it makes the same command exit 0.

#### WU-3 (register + gate) — Register control surface + run the anti-overfit gate

**Scope:**
1. Register `user/scripts/skill-size-ratchet.py` and the dispatched-prompt template family glob (`user/skills/_components/lazy-batch-prompts/**`) in `docs/gate/control-surfaces.json` `control_surfaces[]` [VERIFY: `grep -n "control_surfaces" docs/gate/control-surfaces.json` → confirmed array present; note `user/scripts/skill-size-ratchet.py` is NOT currently in the list — this WU adds it].
2. Run `python3 user/scripts/harness-gate.py --repo-root . --staged` [VERIFY: `grep -n -- "--staged" user/scripts/harness-gate.py` → line 496] on the WU-2 diff.
3. If it flags the matcher-set edit (or any other detector), author `docs/bugs/cycle-prompt-residual-deflation-and-bloat-guard/GATE_VERDICT.md` per `~/.claude/skills/_components/harness-change-gate.md` [VERIFY: `test -f user/skills/_components/harness-change-gate.md`], `kind: gate-verdict` [VERIFY: `grep -n "kind: gate-verdict" user/skills/_components/sentinel-frontmatter.md` → line 453] — the structural, shape-keyed design (Locked Decision 2) is the intended overfit defense; the verdict records the judgment call, not a rubber stamp.

**TDD:** no (registration + gate-run, not code).

**Files to create/modify:**
- `docs/gate/control-surfaces.json`
- `docs/bugs/cycle-prompt-residual-deflation-and-bloat-guard/GATE_VERDICT.md` (conditional — only if `harness-gate.py` flags the diff)

**Implementation goal:** The new detector's own control surface is registered so `harness-gate.py` covers its own future diffs (the same self-inclusion discipline `control-surfaces.json`'s `_doc` field already documents for its `gate_own` block). This WU runs AFTER WU-2 so `--staged`/`--range` inspects the actual implementation diff, not a speculative one.

**Spec requirements:** SPEC "Proposed Fix Shape" Deliverable 2a bullet 2 (register), "Affected Area" table row "Control-surface manifest"; "Anti-overfit self-check" closing note; PHASES.md Cross-feature Integration Notes → `anti-overfit-design-gate` paragraph; PHASES.md Phase 2 Deliverables bullets 6–7.

**Batch:** 3 (after WU-2 — `harness-gate.py` runs on the implementation diff, not a stub).

**Minimum Verifiable Behavior:**
```bash
python3 user/scripts/harness-gate.py --repo-root . --staged
```
Exit 0 (out-of-scope or pass) with no unresolved verdict requirement, OR exit 1 with `docs/bugs/cycle-prompt-residual-deflation-and-bloat-guard/GATE_VERDICT.md` authored resolving it.

**Testing Strategy:** Strict TDD for WU-1/WU-2 — write the four failing fixtures first, confirm red, then implement the detector + ceiling + allowlist until green:
```bash
python3 -m pytest user/scripts/test_skill_size_ratchet.py -q
```
Then re-run the full Phase 1 (part 1 of this series) regression net to confirm the new checks don't false-positive against the deflated Phase 1 output:
```bash
python3 -m pytest user/scripts/test_dispatch.py user/scripts/test_project_skills.py -q
python3 user/scripts/skill-size-ratchet.py --check
```

**Runtime Verification:** none — deterministic gate exit codes are the proof.

### Batch overview — Phase 2

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1 | Solo | N/A |
| 2     | WU-2 | Solo | N/A (depends on WU-1's failing tests) |
| 3     | WU-3 | Solo | N/A (depends on WU-2's implementation diff) |

## Work Units

- [x] WU-1 — Write failing tests (war-story shapes, per-section ceiling, allowlist rescue, scope exemption)
- [x] WU-2 — Implement detector + per-section ceiling + allowlist
- [x] WU-3 — Register control surface + run harness-gate.py / author GATE_VERDICT.md if flagged

## Plan-specific execution notes

- **Do NOT begin this part until part 1 (Phase 1) of this series shows `status: Complete`.** The per-section ceiling seeded in WU-2 reads sizes that only exist after part 1's deflation + `--lock-in-profile` re-lock lands. Starting early seeds ceilings against pre-deflation (bloated) sizes, defeating the phase's purpose.
- **Single-writer discipline on `skill-size-baseline.json` (same file part 1 wrote):** confirm part 1's commit has landed (git log / plan status) before WU-2 edits the baseline JSON — do not edit it concurrently with a live part-1 cycle.
- **This is the phase most likely to trip `harness-gate.py`** — the detector edits a matcher/pattern set inside a file about to be registered as its own control surface. WU-3's `GATE_VERDICT.md` branch is not optional ceremony; budget for it. Locked Decision 2's structural (shape-keyed) design is the defense argument to write into the verdict if flagged, not a fresh design discussion.
