---
kind: implementation-plan
feature_id: cycle-prompt-residual-deflation-and-bloat-guard
status: Ready
created: 2026-07-20
phases: [1]
complexity: complex
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-20.
> To execute: `/execute-plan {path}`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 3. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-prompt-residual-deflation-and-bloat-guard\plans\all-phases-cycle-prompt-residual-deflation-part-1.md` (this file)
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-prompt-residual-deflation-and-bloat-guard\plans\all-phases-cycle-prompt-residual-deflation-part-2.md`
> - part 3: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\cycle-prompt-residual-deflation-and-bloat-guard\plans\all-phases-cycle-prompt-residual-deflation-part-3.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings
> during execution. Part 3 (Phase 2) depends on this part (Phase 1) — its per-section byte ceiling
> is seeded from the sizes this part produces. Part 2 (Phase 3) is independent of both.

# Implementation Plan — Cycle-Prompt Residual Deflation + Anti-Bloat Guard (part 1 of 3)  (v2)

**PHASES.md files:**
- `docs/bugs/cycle-prompt-residual-deflation-and-bloat-guard/PHASES.md` (cycle-prompt-residual-deflation-and-bloat-guard, 3 phases)

**SPEC.md files:**
- `docs/bugs/cycle-prompt-residual-deflation-and-bloat-guard/SPEC.md`

**Total phases:** 3 (this part covers Phase 1 only)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (e.g. a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and any local `## Component Reference Card` override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

**claude-config-specific override:** this repo's gates are Python/projection — `pytest`, `python3 user/scripts/lazy-state.py --test`, `python3 user/scripts/lint-skills.py`, `python3 user/scripts/generate-coupled-skills.py --check`, `python3 user/scripts/skill-size-ratchet.py --check`, `python3 user/scripts/validate-plan.py --structural <path>` — NOT `msbuild`/`mstest`/`nx`. Step B.4 (Quality Gates) and this part's per-phase Minimum Verifiable Behavior below govern which of these run per batch.

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against. The executing session SHOULD `Read` these before starting any work unit whose Implementation goal references the corresponding upstream contract. Listed in dependency order.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| cycle-prompt-deflation | composes | `docs/features/cycle-prompt-deflation/PHASES.md` | Parent feature this bug follows up — Phase 1 here continues its exact playbook (trim-in-place, `SEMANTIC_DIFF`, `--lock-in-profile`, no reference-by-path) on the sections it deliberately left out of scope. |
| cycle-prompt-deflation | composes | `docs/features/cycle-prompt-deflation/IMPLEMENTATION_NOTES.md` | The deflation playbook + ratchet mechanics this Phase extends; also carries the parent's `⚖ policy: scope-tightening selector narrowing → trim-only` decision this bug is bound by (do NOT re-open selector-narrowing). |
| cycle-prompt-deflation | composes | `docs/features/cycle-prompt-deflation/SEMANTIC_DIFF_PHASE2.md` | Table format precedent for this part's own `SEMANTIC_DIFF.md` deliverable (removed clause → surviving rule). |
| cycle-prompt-deflation | composes | `docs/features/cycle-prompt-deflation/SEMANTIC_DIFF_PHASE3.md` | Same precedent, second half — read both before drafting this part's `SEMANTIC_DIFF.md`. |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | cycle-prompt-residual-deflation-and-bloat-guard | P1 | Residual prose-density deflation (D1) | — | Solo |

### Phase: cycle-prompt-residual-deflation-and-bloat-guard P1 — Residual prose-density deflation (D1)

**Goal:** Trim war-story / historical-justification / redundant-rationale prose IN PLACE from the dispatched cycle-subagent prompt template family (the never-deflated skill-specific `@section`s + residual rationale tails in the already-deflated `skills=all` sections + the AlgoBooth addendum), without dropping any enforceable rule, then re-lock the assembled-profile byte ratchet to the new lower floor.

**Entry criteria:** None — first phase; no prerequisite work.

**SPEC.md references:** "Proposed Fix Shape (for /plan-bug)" → Deliverable 1 (One-time residual deflation); Evidence Collected → "Per-`@section` byte census" table; Symptoms 1–4; Theory 1 (Confirmed).

**Scope note (binding, do not re-derive):** prose-density trim ONLY. No `@section` selector-narrowing, no path-shorthand/reference-by-path externalization — both explicitly operator-rejected (SPEC "Resolved Decisions" / "Out of scope"). `TDD: no` for this whole phase (prose-density trim; the existing pytest guards are the regression net, not new test authorship).

#### WU-1 — Deflate `cycle-base-prompt.md` skill-specific sections + Class-B rationale tails

**Scope:** Strip war-story/incident/dated/removed-history/redundant-rationale prose from:
- The never-deflated `skills=<specific>` `@section` families in `user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`:
  - `skill-execute-plan` (L284) [VERIFY: `grep -n "@section skill-execute-plan " user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` → line 284] and `skill-execute-plan-cloud` (L318) [VERIFY: `grep -n "@section skill-execute-plan-cloud" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` → line 318]
  - `skill-mcp-test-common` (L377) [VERIFY: `grep -n "@section skill-mcp-test-common" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` → line 377] + `mcp-test-runtime` ×2 (L452, L480) [VERIFY: `grep -n "@section mcp-test-runtime" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` → lines 452, 480]
  - `skill-retro` (L361) [VERIFY: `grep -n "@section skill-retro " user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` → line 361] + `skill-retro-feature` (L369) [VERIFY: `grep -n "@section skill-retro-feature" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` → line 369]
  - `provenance-lookup` (L349) [VERIFY: `grep -n "@section provenance-lookup" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` → line 349]
  - `resume-safety` ×2 (L513, L521) [VERIFY: `grep -n "@section resume-safety" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` → lines 513, 521]
  - `park-spec-sentinel-mediation` (L205) [VERIFY: `grep -n "@section park-spec-sentinel-mediation" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` → line 205]
- The Class-B rationale tails already flagged in SPEC Symptom 3, inside the already-deflated `skills=all` sections:
  - `env-dialect-core` (L129) [VERIFY: `grep -n "@section env-dialect-core" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` → line 129] — drop `(an absent file raises on empty stdin)` and `a mature PHASES.md exceeds the Read cap; the slicer returns the index + only the phase(s) named` explanatory tails; keep the surviving rule.
  - `env-dialect-windows` (L144) [VERIFY: `grep -n "@section env-dialect-windows" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` → line 144] — drop the `No /mnt/c/... (WSL dialect)` rationale clause; keep the rule.
  - `workstation-dispatch` (L230) [VERIFY: `grep -n "@section workstation-dispatch" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` → line 230] — drop the `2026-07-09 — the former inline-only ban is lifted on workstation; cloud keeps it` removed-history clause; keep the rule.

**TDD:** no.

**Files to create/modify:**
- `user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`

**Implementation goal:** Every dated/incident/issue-round/removed-history clause named in SPEC Symptoms 1 and 3 is gone from the file, while every enforceable rule it was attached to survives as terse imperative prose. **PRESERVE VERBATIM (load-bearing, never touch):**
- Every `@section` selector line (the `<!-- @section ... -->` marker lines themselves — content trimmed, marker line untouched).
- `WORKSTATION DISPATCH — LOAD-BEARING` [VERIFY: `grep -n "WORKSTATION DISPATCH" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`].
- Tokens `{cwd}` / `{work_branch}` / `{receipt_name}` / `{item_label}` [VERIFY: `grep -n "{cwd}\|{work_branch}\|{receipt_name}\|{item_label}" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`].
- The R5 chained-command form, `git_safe_push`, the `git add -A` ban [VERIFY: `grep -n "git_safe_push\|git add -A" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`].
- `classify_conflict` + `conflict_kind: semantic` [VERIFY: `grep -n "classify_conflict\|conflict_kind" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`].
- The `--verify-ledger` + `ok:true` certification [VERIFY: `grep -n "verify-ledger\|ok:true" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`].
- `cycle-subagent-bg-gate-guard.sh` [VERIFY: `grep -n "cycle-subagent-bg-gate-guard" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`].
- The `series_index` prerequisite-ordering **algorithm** inside `skill-execute-plan[-cloud]` — this is load-bearing LOGIC, not a war-story: compress the surrounding framing prose but keep the algorithm's steps/conditions intact and unambiguous.

**Spec requirements:** SPEC "Proposed Fix Shape" Deliverable 1, Class A + Class B; SPEC "Evidence Collected" per-`@section` byte census (this WU targets every row marked "No" in the "Deflated by parent?" column plus the three "Yes (residual at ...)" rows).

**Batch:** 1.

#### WU-2 — Condense AlgoBooth addendum

**Scope:** Condense the `over-cap-gate-decomposition` and `audio-invariants` blocks in `repos/algobooth/.claude/skill-config/cycle-prompt-addenda.md` to imperative rules.

**TDD:** no.

**Files to create/modify:**
- `repos/algobooth/.claude/skill-config/cycle-prompt-addenda.md`

**Implementation goal:** Strip narrative framing from both blocks while keeping every enforceable rule: the 4 named ts sub-gates under `over-cap-gate-decomposition` [VERIFY: `grep -n "over-cap-gate-decomposition" repos/algobooth/.claude/skill-config/cycle-prompt-addenda.md`], and — under `audio-invariants` — the mandate to read `INVARIANTS.md` plus the §10.1 / `HOT_PATH_FILES` rule [VERIFY: `grep -n "audio-invariants\|INVARIANTS.md\|HOT_PATH_FILES" repos/algobooth/.claude/skill-config/cycle-prompt-addenda.md`].

**Spec requirements:** SPEC "Proposed Fix Shape" Deliverable 1, "AlgoBooth addendum" bullet; SPEC Symptom 4.

**Batch:** 1 (disjoint file from WU-1 — dispatch alongside WU-1 in the same message).

#### WU-3 — `SEMANTIC_DIFF.md` + re-lock the assembled-profile ratchet

**Scope:**
1. Author `docs/bugs/cycle-prompt-residual-deflation-and-bloat-guard/SEMANTIC_DIFF.md` (new) mapping EVERY clause removed in WU-1/WU-2 → its surviving terse rule, following the `SEMANTIC_DIFF_PHASE2.md` / `SEMANTIC_DIFF_PHASE3.md` table format [VERIFY: `test -f docs/features/cycle-prompt-deflation/SEMANTIC_DIFF_PHASE2.md && test -f docs/features/cycle-prompt-deflation/SEMANTIC_DIFF_PHASE3.md`], and list the load-bearing literals preserved verbatim (the same list enumerated in WU-1's Implementation goal above, plus `--park-provisional` [VERIFY: `grep -rn "park-provisional" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`]).
2. Run `python3 user/scripts/generate-coupled-skills.py --check --repo-root .` [VERIFY: `grep -n "add_argument(\"--check\"" user/scripts/generate-coupled-skills.py` → line 422] and confirm exit 0.
3. Re-lock all 20 seeded assembled-profile ceilings to the new lower floor via `python3 user/scripts/skill-size-ratchet.py --lock-in-profile <profile-id>` [VERIFY: `grep -n -- "--lock-in-profile" user/scripts/skill-size-ratchet.py` → argparse def at line 431] per profile — NEVER hand-raise; `--lock-in-profile` only lowers a ceiling on measured improvement.

**TDD:** no.

**Files to create/modify:**
- `docs/bugs/cycle-prompt-residual-deflation-and-bloat-guard/SEMANTIC_DIFF.md` (new)
- `user/scripts/skill-size-baseline.json` (profiles block only — no per-section block; that is Phase 2)

**Implementation goal:** `SEMANTIC_DIFF.md` gives a human reviewer proof no policy was dropped; the 20 profile ceilings in `skill-size-baseline.json` reflect the deflated floor so Phase 2's per-section ceiling (part 3 of this series) has a stable, already-lowered seed to measure against (PHASES.md "Integration Notes for Next Phase" — Phase 2 CANNOT start until this re-lock lands).

**Spec requirements:** SPEC "Proposed Fix Shape" Deliverable 1, "Discipline (inherited from the parent, binding)" bullet — `SEMANTIC_DIFF` + re-lock via `--lock-in-profile`, never hand-raise; PHASES.md Phase 1 "Integration Notes for Next Phase".

**Batch:** 2 (depends on WU-1 + WU-2 landing — measures the deflated sizes they produce; do not dispatch in the same message as WU-1/WU-2).

**Minimum Verifiable Behavior (run after Batch 2, before closing the phase):**
```bash
python3 user/scripts/skill-size-ratchet.py --check
# exit 0 at the new lower floor
grep -REn "Live incident|ISSUE 2|hydra-overlay|d8-effect-chains" \
  user/skills/_components/lazy-batch-prompts/ \
  repos/algobooth/.claude/skill-config/cycle-prompt-addenda.md
# empty output
python3 user/scripts/generate-coupled-skills.py --check --repo-root .
# exit 0
```

**Testing Strategy:** No new tests are authored this phase (`TDD: no`, prose-density trim). The existing pytest guards are the regression net and MUST stay green: `test_dispatch.py`'s binding-matrix + residue guards (assert the emitter still assembles every profile with no unbound `{token}` and that preserved literals survive) and `test_project_skills.py`'s terminal-stop/variant tests. Run:
```bash
python3 -m pytest user/scripts/test_dispatch.py user/scripts/test_project_skills.py -q
```
`SEMANTIC_DIFF.md` is the human-reviewable no-policy-lost artifact that complements (not replaces) these automated guards.

**Runtime Verification:** none — static template artifact; the deterministic gate exit codes above are the proof. Do not author gate-owned rows in this phase.

### Batch overview — Phase 1

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2 | Yes | None (disjoint files: `cycle-base-prompt.md` vs. `cycle-prompt-addenda.md`) |
| 2     | WU-3 | Solo | N/A (depends on Batch 1's deflated output) |

## Work Units

- [ ] WU-1 — Deflate cycle-base-prompt.md skill-specific sections + Class-B rationale tails
- [ ] WU-2 — Condense AlgoBooth addendum
- [ ] WU-3 — SEMANTIC_DIFF + re-lock assembled-profile ratchet

## Plan-specific execution notes

- **Coupled-pair gate (hard dependency, per PHASES.md Cross-feature Integration Notes):** every edit to `cycle-base-prompt.md` in WU-1 MUST be followed by `python3 user/scripts/generate-coupled-skills.py --check --repo-root .` before the WU is considered done — not deferred to WU-3. WU-3's own `--check` run is the final confirmation, not the first one.
- **Single-writer discipline on `skill-size-baseline.json`:** this phase (profiles block) and Phase 2 of this series (part 3, new per-section block) both write this file. Do not run them concurrently — part 3 is sequenced strictly after this part per the series preamble.
- **Anti-overfit design constraint (informational only — this phase does not touch `skill-size-ratchet.py`'s matcher/detector code):** the `harness-gate.py` / `GATE_VERDICT.md` obligation applies to Phase 2 (part 3 of this series), which edits the detector's matcher set. This phase only edits template prose + re-locks existing ceilings via the pre-existing `--lock-in-profile` flag — no new control-surface diff to gate here.
