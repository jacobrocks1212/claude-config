---
kind: implementation-plan
feature_id: adhoc-ensure-runtime-test-injects-signal-under-test
status: Ready
complexity: complex
created: 2026-06-23
phases: [1, 2, 3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-23.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\adhoc-ensure-runtime-test-injects-signal-under-test\plans\all-phases-production-binding-guard.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — ensure_runtime production-binding test-discipline guard  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\adhoc-ensure-runtime-test-injects-signal-under-test\PHASES.md` (adhoc-ensure-runtime-test-injects-signal-under-test, 3 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\adhoc-ensure-runtime-test-injects-signal-under-test\SPEC.md`

**Total phases:** 3
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **INLINE-DISPATCH OVERRIDE (when this plan is executed by a cycle subagent with no `Agent` tool).** If the executing session is itself a dispatched subagent lacking the `Agent` tool (e.g. a `/lazy-bug-batch` cycle), perform ALL work units INLINE with `Read`/`Edit`/`Write` — the dispatch-level prohibition on spawning sub-subagents takes precedence over the orchestrator+subagent model above. Still write the failing test FIRST for each TDD work unit (test-first is preserved regardless of who writes it), and still run every gate. This override does NOT relax the test-first or review discipline — only the "who edits" rule.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines). (See the INLINE-DISPATCH OVERRIDE above when executed by an Agent-less cycle subagent.)
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents
3. Every TDD work unit goes through the test-first pipeline — failing tests written FIRST, then implementation makes them pass
4. PHASES.md is updated AFTER EACH batch completes (not deferred)
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing
6. Mistakes are fixed immediately before launching the next batch
7. After all batches in a phase finish, integration verification confirms all changes work together
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it
9. Each completed phase is committed and pushed before the next phase begins
10. Cross-feature phases may run in parallel when dependencies are satisfied and no file conflicts exist (N/A — single feature here)
11. This plan is self-contained — follow it exactly as written without relying on external context
12. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory

## Work Units

- [ ] WU-1 — Production-binding signal-injection smell collector + guard tests (Phase 1)
- [ ] WU-2 — Spawn-invocation faithful-double smell detection + guard tests (Phase 2)
- [ ] WU-3 — Document the production-binding test convention + manual live cold-boot smoke step (Phase 3)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | adhoc-ensure-runtime-test-injects-signal-under-test | P1 | Signal-injection smell collector + guard | — | Solo |
| 2    | adhoc-ensure-runtime-test-injects-signal-under-test | P2 | Spawn-double faithful-double assertion | P1 (extends same collector) | Solo |
| 3    | adhoc-ensure-runtime-test-injects-signal-under-test | P3 | Documentation + manual smoke step | P1, P2 (references guard test names) | Solo |

All three WUs target overlapping files (WU-1 + WU-2 both modify `test_lazy_core.py`; WU-3 is docs-only) and have strict dependency edges, so each runs in its own sequential batch — one WU per batch.

---

### Phase: adhoc-ensure-runtime-test-injects-signal-under-test P1 — Signal-injection smell collector + guard

**Goal:** Add a pure AST collector + a self-checking positive meta-test + a negative-fixture test that mechanically flag a `test_ensure_runtime_production_*` test which injects the OS signal under test (`boot_alive=`/`restart=`) into `ensure_runtime(...)`.
**Entry criteria:** none (first phase).
**SPEC.md references:** `## Fix Scope` item 1; `## Affected Area` row 1; `## Theories` Theory 1; Open Questions (guard home, convention encoding — both resolved in PHASES Implementation Notes).

#### WU-1 — Production-binding signal-injection smell collector + guard tests

- **Scope (PHASES Phase 1 deliverables):**
  - `_collect_production_binding_smells(module_source: str) -> list` pure AST collector
  - `test_ensure_runtime_production_tests_derive_not_inject_signal()` positive self-checking meta-test
  - `test_production_binding_guard_detects_signal_injection()` negative-fixture test
  - register all three in `_TESTS`
- **TDD:** yes (the two meta-tests ARE the behavior; the negative fixture is the RED-for-the-right-reason proof).
- **Files to create/modify:**
  - `C:\Users\Jacob\source\repos\claude-config\user\scripts\test_lazy_core.py` — add the collector + two meta-tests; append a `_TESTS = _TESTS + [...]` block registering both tests. MIRROR the existing trio `_collect_orphaned_test_names` [VERIFY: grep -n "def _collect_orphaned_test_names" user/scripts/test_lazy_core.py] / `test_no_orphaned_test_functions` / `test_dead_coverage_guard_detects_orphan_by_name` (same file, ~line 22214–22295) — same AST-parse-from-`Path(__file__).read_text()` shape, same positive+negative pairing, same `_TESTS` append idiom.
- **Test files:** same file (the meta-tests live alongside the collector in `test_lazy_core.py`, exactly as the orphan-guard trio does).
- **Test expectations:**
  - Positive meta-test: reads `Path(__file__).read_text(encoding="utf-8")`, calls `_collect_production_binding_smells(source)`, asserts `== []` on the LIVE suite — the cited production tests (`test_ensure_runtime_production_boot_alive_live_handle_patient_waits` [VERIFY: grep -n "def test_ensure_runtime_production_boot_alive_live_handle_patient_waits" user/scripts/test_lazy_core.py], `..._dead_handle_recovers`, `..._wrapper_exits_early_patient_waits_one_spawn`, `..._m4_wrapper_exits_early_patient_waits_one_spawn`, `..._restart_spawns_via_shell_on_windows_cold_boot`) all pass `neither` `boot_alive=` `nor` `restart=`, so this is GREEN today. It would FAIL (naming the offender + injected kwarg) if a future production test injected the signal.
  - Negative fixture: feed synthetic module source with a `def test_ensure_runtime_production_injects():` body calling `lazy_core.ensure_runtime(Path(td), boot_alive=lambda: True)`, assert the collector reports `test_ensure_runtime_production_injects` (by name) with `boot_alive`. Feed a sibling `def test_ensure_runtime_production_clean():` whose `ensure_runtime(...)` call passes only `probe=`/`stale_check=` and assert it is NOT reported (allow-list proof — this is the "would-it-fail" guard against a tautological collector).
- **Implementation goal:** `_collect_production_binding_smells` uses `ast.parse(module_source)`, walks top-level `def test_ensure_runtime_production_*` functions [VERIFY: grep -n "^def test_ensure_runtime_production_" user/scripts/test_lazy_core.py], and for each scans the function body for an `ast.Call` whose callee resolves to the name `ensure_runtime` (match both `ensure_runtime(...)` and `lazy_core.ensure_runtime(...)` — check `ast.Name.id == "ensure_runtime"` OR `ast.Attribute.attr == "ensure_runtime"`) carrying an `ast.keyword` with `arg in {"boot_alive", "restart"}`. Return a sorted, stable list of `(test_name, injected_kwarg)` tuples (or dicts) for each offending injection. The ALLOW-LIST of legitimate external-collaborator kwargs (`probe`, `stale_check`, `sidecar_check`, `frontend_probe`, `read_lock`, `live_session_id`, `kernel_start_time_fn`, `sleep`, `write_lock`, `recover_identity`, `config`) is NEVER flagged — only `boot_alive`/`restart` (the production derivations) are smells. AST over regex (ignores docstring/comment occurrences), matching the `_collect_orphaned_test_names` rationale [VERIFY: grep -n "ast.parse" user/scripts/test_lazy_core.py]. The collector takes `module_source` as a parameter (pure, no I/O) so the negative fixture can feed synthetic source — same signature shape as `_collect_orphaned_test_names`.
- **Spec requirements:** `## Fix Scope` item 1 ("a structural production-binding test-discipline guard … assert it does NOT pass the SIGNAL-UNDER-TEST as an `ensure_runtime` keyword injection — specifically NOT `boot_alive=` and NOT `restart=` … Allow-list the legitimate external-collaborator injections"). `## Proven Findings` 1 + 2.
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | N/A             |

---

### Phase: adhoc-ensure-runtime-test-injects-signal-under-test P2 — Spawn-double faithful-double assertion

**Goal:** Extend the guard so a production-binding `restart`-spawn test that swaps `lazy_core.subprocess` is flagged when it uses an always-succeeds double (`_FakeSubprocess`) instead of a faithful double with real spawn-resolution semantics (`_WindowsSpawnSemanticsSubprocess`).
**Entry criteria:** P1 complete (extends the same collector / AST walk + reuses the `test_ensure_runtime_production_*` enumeration and allow-list class boundary).
**SPEC.md references:** `## Fix Scope` item 2; `## Affected Area` rows 1–2; `## Theories` Theory 2; `## Evidence Collected` (`_FakeSubprocess` vs `_WindowsSpawnSemanticsSubprocess`).

#### WU-2 — Spawn-invocation faithful-double smell detection + guard tests

- **Scope (PHASES Phase 2 deliverables):**
  - extend `_collect_production_binding_smells` (or add `_collect_spawn_double_smells(module_source)` sharing the AST walk) to flag a spawn-binding production test using an always-succeeds `_FakeSubprocess`
  - encode the spawn-binding discriminator so a liveness/timing production test that legitimately uses `_FakeSubprocess` is NOT a false positive
  - `test_spawn_binding_production_tests_use_faithful_double()` positive meta-test
  - `test_spawn_double_guard_detects_always_succeeds_double()` negative fixture
  - register the new test(s) in `_TESTS`
- **TDD:** yes (positive + negative meta-tests; the negative fixture's allow-listed sibling is the false-positive guard).
- **Files to create/modify:**
  - `C:\Users\Jacob\source\repos\claude-config\user\scripts\test_lazy_core.py` — extend the WU-1 collector (or add a sibling sharing the AST walk) + add the two meta-tests; append to `_TESTS`. Reference the existing doubles `_FakeSubprocess` [VERIFY: grep -n "class _FakeSubprocess" user/scripts/test_lazy_core.py] and `_WindowsSpawnSemanticsSubprocess` [VERIFY: grep -n "class _WindowsSpawnSemanticsSubprocess" user/scripts/test_lazy_core.py].
- **Test files:** same file.
- **Test expectations:**
  - Positive meta-test: reads `Path(__file__).read_text()`, asserts the spawn-double collector returns `== []` on the LIVE suite — `test_ensure_runtime_production_restart_spawns_via_shell_on_windows_cold_boot` [VERIFY: grep -n "def test_ensure_runtime_production_restart_spawns_via_shell_on_windows_cold_boot" user/scripts/test_lazy_core.py] already uses `_WindowsSpawnSemanticsSubprocess` (the faithful double), so GREEN today.
  - Negative fixture: synthetic spawn-binding production test that assigns `lazy_core.subprocess = _FakeSubprocess(...)` (always-succeeds) AND asserts on `shell_spawns` / spawn resolution → reported by name. A sibling using `_WindowsSpawnSemanticsSubprocess` → NOT reported (faithful-double allow). ALSO a sibling that is a liveness/timing production test using `_FakeSubprocess` but NOT spawn-binding (no `shell_spawns` assertion / name not `*_spawn_*`) → NOT reported (this is the false-positive guard for `test_ensure_runtime_production_boot_alive_live_handle_patient_waits`, which legitimately uses `_FakeSubprocess` for the `.poll()` liveness sub-case, not spawn resolution).
- **Implementation goal:** Detect, within a `test_ensure_runtime_production_*` function that (a) assigns `lazy_core.subprocess = <double>` AND (b) does NOT inject `restart=`, whether `<double>` is constructed from `_FakeSubprocess` (always-succeeds → smell) vs `_WindowsSpawnSemanticsSubprocess` (faithful → OK). Discriminate SPAWN-binding tests from LIVENESS/TIMING tests conservatively (⚖ scope-class — resolved in PHASES Implementation Notes): prefer a name/marker convention (a `*_spawn_*` substring in the test name OR a `shell_spawns` assertion present in the body) over over-broad AST inference, so `_FakeSubprocess` used purely for `.poll()` liveness/timing is NOT flagged. Record the chosen discriminator inline as a comment. The detection shares the AST module-walk from WU-1 (do NOT re-implement a second parse pass — pass the parsed tree or factor a shared walk).
- **Spec requirements:** `## Fix Scope` item 2 ("a production-binding `restart`-spawn test must drive a double with real spawn-resolution semantics … not a `_FakeSubprocess` that succeeds for any argv. The guard flags a production-binding spawn test that uses an always-succeeds subprocess double"). `## Proven Findings` 1 (`_FakeSubprocess.Popen` always-succeeds is the spawn-invocation smell) + 2 (`_WindowsSpawnSemanticsSubprocess` is the correct pattern). `## Open Questions` (convention encoding — name-prefix, resolved at planning).
- **Batch:** 2 (overlaps WU-1's file; depends on the WU-1 collector).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 2     | WU-2      | Solo      | shares test_lazy_core.py with WU-1 → sequential after Batch 1 |

---

### Phase: adhoc-ensure-runtime-test-injects-signal-under-test P3 — Documentation + manual smoke step

**Goal:** Document the production-binding test convention the guard enforces + the manual operator live cold-boot smoke step in `user/scripts/CLAUDE.md`.
**Entry criteria:** P1 + P2 complete (the doc references the guard test names landed there).
**SPEC.md references:** `## Fix Scope` item 3; `## Affected Area` row 3; `## Theories` Theory 2 (live-smoke half is a documented manual step, not in-repo automation).

#### WU-3 — Document the production-binding test convention + manual live cold-boot smoke step

- **Scope (PHASES Phase 3 deliverables):**
  - add a "production-binding ensure_runtime test convention" note to `user/scripts/CLAUDE.md` near the `--ensure-runtime` section
  - add a "manual live cold-boot smoke" operator step
- **TDD:** no (documentation only — no testable behavior).
- **Files to create/modify:**
  - `C:\Users\Jacob\source\repos\claude-config\user\scripts\CLAUDE.md` — append to the `--ensure-runtime` CLI-surface documentation [VERIFY: grep -n "\-\-ensure-runtime" user/scripts/CLAUDE.md]. This file IS orchestrator-editable (it is a CLAUDE.md, not source) — but under the standard model, still dispatch a Sonnet subagent for it; under the INLINE-DISPATCH OVERRIDE, edit it directly.
- **Implementation goal:**
  - Convention note: a `test_ensure_runtime_production_*` test MUST reach the OS signal under test by swapping `lazy_core.subprocess`/`lazy_core.time` and letting the default closure DERIVE the signal — it MUST NOT pass `boot_alive=`/`restart=` (the derivations). Name the legitimate external-collaborator allow-list (`probe`/`stale_check`/`sidecar_check`/`frontend_probe`/`read_lock`/`live_session_id`/`kernel_start_time_fn`/`sleep`/`write_lock`/`recover_identity`/`config`). Point at the Phase 1–2 guard tests by their final landed names (`test_ensure_runtime_production_tests_derive_not_inject_signal`, `test_spawn_binding_production_tests_use_faithful_double`) as the enforcer, and cross-reference `docs/bugs/adhoc-ensure-runtime-test-injects-signal-under-test/`.
  - Manual smoke step: the operator runs `python3 user/scripts/lazy-state.py --ensure-runtime --repo-root <real-AlgoBooth-checkout>` against a genuinely cold runtime (both ports down, no warm build) and confirms it reaches READY (not a false `mcp-runtime-unready` BLOCKED) — the live verification Round 34 used. Mark it EXPLICITLY as operator/manual, NOT a claude-config CI assertion (environment-dependent: needs a real checkout + cold runtime).
- **Spec requirements:** `## Fix Scope` item 3 ("A documented live cold-boot smoke step in the `--ensure-runtime` contract (manual/operator, NOT in-repo CI) — the only thing that has ever caught the spawn-invocation defect — referenced from `user/scripts/CLAUDE.md`"). `## Theories` Theory 2 contradicting-evidence (live smoke is environment-dependent → documented manual step).
- **Batch:** 3 (docs-only; references P1+P2 test names).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 3     | WU-3      | Solo      | None (CLAUDE.md, distinct from test file) |

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** Identify phase(s) whose entry criteria are satisfied (prerequisite phases complete — all deliverables checked off in their PHASES.md). Phases here are strictly sequential (P1 → P2 → P3). If no phases are ready, jump to Blocking Issue Protocol.
2. **Announce:** Print "Implementing adhoc-ensure-runtime-test-injects-signal-under-test Phase N: [title]"
3. **Review prior context:** Re-read all previously completed phases' Implementation Notes in PHASES.md. They take priority over the plan where they diverge.
4. **Execute the batch** per the Per-Batch Steps below (one WU per batch here).
5. **Run Post-Phase Steps** below.
6. **Report:** Print "Phase N: [title] — committed as [hash]"
7. **Loop:** Return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly. It defines: task tool loading via ToolSearch, task creation for all work units (WU-1, WU-2, WU-3), and the update protocol.

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow its instructions. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and this plan file. For WU-1/WU-2 ALSO re-read the live `test_lazy_core.py` region around the cited line anchors so the collector matches the ACTUAL function names/doubles on disk (they may have shifted line numbers).

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — ZERO INLINE IMPLEMENTATION)

**PRE-FLIGHT CHECK:** Confirm (1) you will use the `Agent` tool with `model: "sonnet"` for ALL code changes, (2) you will NOT call `Edit`/`Write` on source/test files. If either is false, re-read the EXECUTION MODEL section (or, if you are an Agent-less cycle subagent, apply the INLINE-DISPATCH OVERRIDE and write the failing test FIRST yourself).

Read ALL of these before proceeding:
1. `~/.claude/skills/_components/tdd-protocol.md` — TDD decision gate (WU-1 + WU-2 are TDD=yes; WU-3 is TDD=no, docs).
2. `~/.claude/skills/_components/subagent-launch.md` — launch orchestration + failed-agent recovery.
3. `~/.claude/skills/_components/tdd-test-agent.md` — test-agent prompt template (verbatim in every test agent prompt).
4. `~/.claude/skills/_components/implementation-agent.md` — impl-agent prompt template (verbatim in every impl agent prompt).

For WU-1 / WU-2, the failing test MUST be RED for the right reason: the negative fixture must report the synthetic violator (proving non-vacuity), and the positive meta-test asserts the LIVE suite is clean. Confirm RED before implementing the collector. The gate is `python user/scripts/test_lazy_core.py` (manual `_TESTS` runner) AND `pytest user/scripts/test_lazy_core.py -q`.

**POST-DISPATCH GATE:** After subagents complete, verify you composed `Agent` calls (or, under the INLINE override, that you wrote the failing test first) and did NOT bypass test-first.

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` and follow its complete protocol. Also read `~/.claude/skills/_components/mount-site-verification.md` (new-symbol checks: confirm `_collect_production_binding_smells` and the new test functions are registered in `_TESTS` — an orphaned `def test_*` would be caught by the existing `test_no_orphaned_test_functions` guard, but verify it explicitly here). PAY SPECIAL ATTENTION to the assertion-vs-intent + would-it-fail checks: the negative fixture MUST genuinely report the synthetic violator (not vacuously pass), and the allow-listed sibling MUST genuinely NOT be reported (the false-positive guard).

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md` and follow its instructions. Check off completed deliverables for the phase, add an Implementation Notes block with date, work completed, and the final landed names of the collector + guard tests (WU-3 references these). Tick the corresponding `- [ ] WU-N` row in this plan's `## Work Units` checklist.

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/quality-gates.md`. For this repo the gate is the in-file test harness — run BOTH:
- `python user/scripts/test_lazy_core.py` (the manual `_TESTS` runner — collects the new meta-tests)
- `pytest user/scripts/test_lazy_core.py -q` (pytest collection — both runners must be green)

100% pass required before proceeding. No build/lint step applies (pure Python test-discipline change). The `lazy-state.py --test` / `bug-state.py --test` smoke suites are unaffected (no state-machine change), but a quick `python user/scripts/lazy-state.py --test` confirms no import-time breakage was introduced into the shared test module's imports.

#### Step B.4.5: MCP Integration Test (BLOCKING — if applicable)

Read `~/.claude/skills/_components/mcp/mcp-integration-test.md`. **Not applicable** — this is a hermetic Python test-discipline guard with NO runtime-observable / MCP-reachable surface (PHASES `**MCP runtime:** not-required`). Skip with this note.

#### Step B.5: Commit Batch

Read the commit policy: first try `.claude/skill-config/commit-policy.md` in the project root. If it doesn't exist, read `~/.claude/skills/_components/commit-and-push.md` instead. Follow whichever policy applies. Commit to `main` (this repo's work branch). Stage ONLY this bug's files (the PHASES.md, the test file, this plan, and the bug's queue.json entry) — do NOT `git add -A` over unrelated uncommitted working-tree state.

#### Step B.6: Proceed to Next Batch

**Checklist before proceeding (all must be true):**
- [ ] Review report produced with PASS/PASS-WITH-FIXES/NEEDS-REWORK verdict
- [ ] PHASES.md updated with completed deliverables and implementation notes
- [ ] Both test runners pass
- [ ] Step B.5 completed (commit per policy)

### Post-Phase Steps (after the batch in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md` and follow its protocol. Here: confirm the collector and BOTH guard tests are registered in `_TESTS` and run under both the manual runner and pytest, and that the live suite is clean (zero smells reported on the real `test_ensure_runtime_production_*` set).

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md`. Phase 3 (WU-3) IS the `user/scripts/CLAUDE.md` update — once it lands, confirm no OTHER CLAUDE.md (root, `user/skills/`) needs a cross-reference. The root CLAUDE.md hooks/scripts tables do not need a row (the guard is a test, not a hook/script).

#### Commit and Push Post-Phase Changes

Read the commit policy as in Step B.5 and follow it. Push to `main`.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point during execution:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed phases** that haven't been committed yet.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** adhoc-ensure-runtime-test-injects-signal-under-test Phase N: [title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining phases (not attempted):**
   - [list]

4. **Do not attempt to work around the blocker.** Write `BLOCKED.md` per the canonical sentinel schema (`~/.claude/skills/_components/sentinel-frontmatter.md`) — the EXACT name `BLOCKED.md`, never a variant.

Blocking issues include:
- A genuine design fork in the Phase 2 spawn-binding discriminator that the conservative name/marker convention cannot resolve without false-positiving a liveness test (escalate rather than ship an over-broad guard).
- A subagent failure that can't be fixed after 2 retry attempts.
- A test-runner failure that can't be fixed after 2 retry attempts.
- A git push conflict that can't be resolved by rebase.

---

## Completion

When all three phases are complete:

1. **Run the full test suite one final time:** `python user/scripts/test_lazy_core.py` AND `pytest user/scripts/test_lazy_core.py -q` — both green. Plus `python user/scripts/lazy-state.py --test` to confirm no shared-import breakage.
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Feature implemented:** adhoc-ensure-runtime-test-injects-signal-under-test (3 phases)
   **Total phases completed:** 3
   **Total commits:** [M]
   **Final test status:** all green (manual `_TESTS` runner + pytest)

   **Commit log:**
   | Commit | Phase | Title |
   |--------|-------|-------|
   | ... | P1 | Signal-injection smell collector + guard |
   | ... | P2 | Spawn-double faithful-double assertion |
   | ... | P3 | Documentation + manual smoke step |

   **Implementation Notes summary:**
   - The guard is a `--test` meta-test pair mirroring the `test_no_orphaned_test_functions` precedent (no standalone lint script).
   - Convention: `test_ensure_runtime_production_*` name prefix + the `boot_alive=`/`restart=` injection ban + the spawn-double faithful-double assertion.
   - Live cold-boot smoke remains an operator/manual step (environment-dependent), documented in `user/scripts/CLAUDE.md`.

3. When the LAST phase's work lands, set the top-level PHASES.md `**Status:**` to `In-progress` (implementation done, validation pending) — NOT `Fixed`/`Won't-fix`. The `__mark_fixed__` gate owns the terminal flip + `FIXED.md` after the validation tail. Let the state machine route forward.
