---
kind: implementation-plan
feature_id: mcp-tooling-not-predetermined-at-planning
status: In-progress
created: 2026-06-22
complexity: complex
phases: [1, 2, 3, 4]
---

> **Mobile plan** — generated by `/write-plan` (via `/plan-bug`) on 2026-06-22.
> To execute: `/execute-plan docs/bugs/mcp-tooling-not-predetermined-at-planning/plans/all-phases-mcp-tooling-predetermination.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — mcp-tooling-not-predetermined-at-planning  (v2)

**PHASES.md files:**
- `docs/bugs/mcp-tooling-not-predetermined-at-planning/PHASES.md` (mcp-tooling-not-predetermined-at-planning, 4 phases)

**SPEC.md files:**
- `docs/bugs/mcp-tooling-not-predetermined-at-planning/SPEC.md`

**Total phases:** 4
**Plan version:** v2 (reference-based — components loaded from disk per step)

> **Note on this plan's domain.** Every work unit edits **harness markdown** — skill files
> (`user/skills/**/SKILL.md`), shared components (`user/skills/_components/*.md`), per-repo
> skill-config docs (`repos/algobooth/.claude/skill-config/*.md`), and root `CLAUDE.md`. There is
> NO `.ts`/`.rs`/`.cs`/`.vue`/`.py` source change and NO MCP-reachable app surface (see PHASES.md
> `**MCP runtime:** not-required`). The EXECUTION MODEL's "Sonnet subagent writes all source/test
> code" rule still applies in principle, but because the executor here is dispatched WITHOUT the
> Agent tool (bug-pipeline cycle subagent), it performs the markdown edits INLINE with Read/Edit/Write
> per its dispatch override. The verification gate for every WU is the harness lint/projection
> scripts (`lint-skills.py`, `project-skills.py`), not a test runner.

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.ts`, `.js`, `.cs`, `.vue`, `.py`, `.rs`, `.tsx`, `.jsx`, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **DISPATCH OVERRIDE for this plan (harness-markdown work).** This plan modifies NO source/test
> code — only markdown (`*.md` skills, components, skill-config, root `CLAUDE.md`). The HARD
> CONSTRAINT above prohibits editing `.ts`/`.rs`/etc. directly; it does NOT prohibit editing
> markdown. When a bug-pipeline cycle subagent (no Agent tool) executes this plan, perform ALL
> markdown edits INLINE with `Read`/`Edit`/`Write`. Do NOT attempt to dispatch sub-subagents; do
> NOT write BLOCKED.md over the absent Agent tool.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })` (used only when an Agent tool is available; otherwise inline per the override above).

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | (harness lint — see Quality Gates note below) |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

> **Quality Gates for this plan** = the harness lint + projection scripts (this repo has no
> `npm`/`cargo`/`xUnit` suite for markdown edits):
> ```bash
> python ~/.claude/scripts/project-skills.py
> python ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities
> ```
> Both must exit clean before each commit. `project-skills.py` confirms the edited components
> expand into every consumer projection (`_default/` + AlgoBooth) with no broken `!cat` injections
> or circular includes; `lint-skills.py` validates skill structure + capabilities.

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this bug has no completed hard upstream dependencies) | | | |

> Cross-references for context (read-only, not hard deps):
> - `docs/features/unified-pipeline-orchestrator/` — shipped `lazy-state.py --gate-coverage` (the
>   completion-time gate this bug's Phase 4 wires the new tooling decisions into).
> - `user/skills/_components/phases-runtime-validation.md` — the existing capability-audit seam
>   Phase 2 extends.
> - `user/skills/_components/mcp-coverage-audit.md` — the completion gate (Phase 4 assertion target).

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **Code/test changes go through Sonnet subagents** — N/A here (markdown-only; edit inline per the dispatch override). The ONLY files modified: `*.md` skills, components, skill-config, root `CLAUDE.md`.
2. All edits happen in the current worktree — NEVER create worktrees.
3. TDD decision gate: N/A — these are documentation/skill-prose edits with no testable runtime behavior. Verification is the harness lint/projection scripts.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every batch's output is reviewed for spec alignment + correctness before continuing.
6. Mistakes are fixed immediately before the next batch.
7. After all batches in a phase finish, re-run the projection + lint as integration verification.
8. Root `CLAUDE.md` is updated in Phase 4 (its component-description block describes these seams).
9. Each completed phase is committed and pushed (per commit policy) before the next phase begins.
10. Phases here are sequential (Phase 2 depends on Phase 1; Phase 3 depends on Phase 2; Phase 4 depends on 2+3) — no cross-feature parallelism.
11. This plan is self-contained — follow it exactly without external context.
12. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | mcp-tooling | P1 | Per-repo MCP tool catalog declaration | — | Solo |
| 2 | mcp-tooling | P2 | Extend /spec-phases capability audit (enumerate + verify + auto-author) | P1 | Solo |
| 3 | mcp-tooling | P3 | Capture required MCP tooling as a Locked Decision in /spec | P2 | Solo |
| 4 | mcp-tooling | P4 | Wire decisions as completion coverage + docs | P2, P3 | Solo |

## Work Units

- [x] WU-1 — Author `mcp-tool-catalog.md` per-repo skill-config (Phase 1)
- [x] WU-2 — Extend `phases-runtime-validation.md` with MCP tool-existence audit + auto-author rule (Phase 2)
- [x] WU-3 — Point `/spec-phases` Step 2.7 at the new MCP-tool audit behavior (Phase 2)
- [x] WU-4 — Add required-MCP-tooling Locked-Decision capture to `/spec` + AlgoBooth `spec-testing-guidance.md` (Phase 3)
- [ ] WU-5 — Wire tooling decisions into the completion gate (`mcp-coverage-audit.md`) + update root `CLAUDE.md` (Phase 4)

---

### Phase: mcp-tooling P1 — Per-repo MCP tool catalog declaration

**Goal:** Declare WHERE AlgoBooth's live MCP tool surface is enumerated so the shared `/spec-phases` audit can grep the right registry paths.
**Entry criteria:** None (first phase).
**SPEC.md references:** `## Affected Area` row "Repo tool catalog"; Proven Finding 4 ("The mechanism is shared-harness; the tool catalog is repo-specific"); Resolved Open Question 1 (grep live registrations, no generated manifest).

#### WU-1 — Author `mcp-tool-catalog.md` per-repo skill-config

- **Scope:** PHASES.md Phase 1 deliverables (new catalog file + catalog-absent no-op note).
- **TDD:** no (declarative config doc).
- **Files to create/modify:**
  - `repos/algobooth/.claude/skill-config/mcp-tool-catalog.md` (new)
- **Implementation goal:** Write a per-repo catalog doc that:
  1. Names the two live-registry source paths the audit greps: `scripts/mcp-test/tool-methods.ts` and the Rust `inventory::submit!` registration sites. Before writing, CONFIRM these paths exist in the AlgoBooth repo — `[VERIFY: test -f <algobooth-repo>/scripts/mcp-test/tool-methods.ts]` and `[VERIFY: grep -rl "inventory::submit!" <algobooth-repo>/src-tauri/src/]`. If a path differs in the live AlgoBooth tree, record the ACTUAL path (do not invent one); if `inventory::submit!` is not the live registration macro, grep for the real registration site (`register_tool`, a tool-registry module) and name that instead.
  2. Documents the grep contract: how the audit derives the registered-tool-name set from each path (e.g. the tool-name string literals each registration carries) and the one-row-per-tool ledger format the audit consumes (`tool-name | registered? | source file:line`).
  3. Header note: "catalog absent → MCP tool-existence audit is a no-op" — repos without this file get no MCP-existence audit (the audit degrades to skip, never errors), mirroring `phases-runtime-validation.md`'s gate skipping when its source is unconfigured.
- **Spec requirements:** SPEC `## Affected Area` "Repo tool catalog" row; Resolved Open Question 1 in PHASES.md.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | None |

---

### Phase: mcp-tooling P2 — Extend the /spec-phases capability audit (enumerate + verify + auto-author)

**Goal:** Teach the `/spec-phases` Step 2.7 capability audit to enumerate the SPEC's required MCP tools, verify each exists in the Phase-1 catalog, and auto-author a "build MCP tool X" phase up front on a miss.
**Entry criteria:** Phase 1 complete (catalog exists for the audit to grep).
**SPEC.md references:** Theory 1 (root cause — planning has no MCP tool-existence guard); Proven Finding 3 (a correct planning-time seam already exists); Locked Decision 2 (auto-author the build phase by default); Resolved Open Question 2 (auto-author fidelity = name + decision + stub).

#### WU-2 — Extend `phases-runtime-validation.md` with the MCP tool-existence audit + auto-author rule

- **Scope:** PHASES.md Phase 2 deliverables 1–4 + worked example (the audit subsection, auto-author, NEEDS_INPUT fallback, catalog-absent degradation, seeded example).
- **TDD:** no (component-prose edit; verified by projection + traced example).
- **Files to create/modify:**
  - `user/skills/_components/phases-runtime-validation.md` [VERIFY: test -f user/skills/_components/phases-runtime-validation.md]
- **Implementation goal:** Add an "MCP tool-existence audit" subsection to the component that:
  1. Enumerates the MCP tool names the SPEC names (in `## Validation Criteria`, `## Locked Decisions`, and — where present — AlgoBooth's `## Audio Quality Contracts` table).
  2. For each, greps the Phase-1 catalog's registry paths for the tool-name registration; records one ledger row per tool (`how-confirmed: grep`, citing the registration's file:line or absence of hits) — the SAME enumerate→grep→record-ledger-row shape the existing SPEC-example capability audit uses.
  3. On a MISS (required tool not registered): AUTO-AUTHOR a "build MCP tool X" phase/deliverable up front in PHASES.md — naming the tool, citing the driving SPEC decision, emitting a stub `- [ ]` deliverable per Resolved Open Question 2. This is the default (Locked Decision 2), NOT a NEEDS_INPUT halt and NOT a late validation discovery.
  4. NEEDS_INPUT fallback: reserve `NEEDS_INPUT.md` for the genuinely-ambiguous case (the tool's surface/shape cannot be inferred from the SPEC).
  5. Catalog-absent degradation: when no `mcp-tool-catalog.md` is configured, the MCP-tool audit is a no-op (record skip reason) — exactly as the existing capability audit skips when its source is unconfigured.
  6. Add a seeded worked example inline (a SPEC naming a tool absent from the catalog → audit produces an auto-authored build-phase row), mirroring the d8-live-looping motivating-incident example already in the component.
- **Spec requirements:** SPEC `## Affected Area` "Phase decomposition" row; Locked Decision 2; Resolved Open Question 2.
- **Note:** the auto-authored row is an ORDINARY build deliverable — it MUST NOT be a gate-owned row (status flip / receipt write). Keep it a plain `- [ ]` build deliverable.

#### WU-3 — Point `/spec-phases` Step 2.7 at the new MCP-tool audit behavior

- **Scope:** PHASES.md Phase 2 "Files likely modified" `spec-phases/SKILL.md` line.
- **TDD:** no.
- **Files to create/modify:**
  - `user/skills/spec-phases/SKILL.md` [VERIFY: grep -n "phases-runtime-validation.md" user/skills/spec-phases/SKILL.md] (Step 2.7 already injects the component at line ~186)
- **Implementation goal:** If the Step 2.7 injection prose needs to name the new MCP-tool catalog so a reader knows the audit now covers MCP tools, add a one-line mention. The component is ALREADY injected at Step 2.7 (`!cat …/phases-runtime-validation.md`), so the behavior flows automatically once WU-2 lands — this WU is a light prose touch, NOT a re-wiring. If no prose change is needed (the injection already carries the extension), record that and tick the WU with a note.
- **Spec requirements:** SPEC `## Affected Area` "Phase decomposition" row.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-2 | Solo | None |
| 2 | WU-3 | Solo | None (different file from WU-2) |

---

### Phase: mcp-tooling P3 — Capture required MCP tooling as a Locked Decision in /spec

**Goal:** Capture the MCP tools a feature's validation requires AS a Locked Decision during `/spec`, so the Phase-2 audit has a named requirement and the completion gate can assert on it (defense-in-depth).
**Entry criteria:** Phase 2 complete (defines what the captured decision feeds).
**SPEC.md references:** Locked Decision 3 ("Seam = both"); SPEC `## Affected Area` "Spec authoring" row; Evidence note that `/spec` today names tools only as a fixed menu of existing tools.

#### WU-4 — Add required-MCP-tooling Locked-Decision capture to `/spec` + AlgoBooth override

- **Scope:** PHASES.md Phase 3 deliverables (the `/spec` capture step + the AlgoBooth `spec-testing-guidance.md` extension).
- **TDD:** no (skill-prose edit; verified by projection + shape-match against the gate's parser).
- **Files to create/modify:**
  - `user/skills/spec/SKILL.md` [VERIFY: grep -n "Validation Criteria" user/skills/spec/SKILL.md] (capture step near `## Validation Criteria`, line ~537)
  - `repos/algobooth/.claude/skill-config/spec-testing-guidance.md` [VERIFY: test -f repos/algobooth/.claude/skill-config/spec-testing-guidance.md]
- **Implementation goal:**
  1. In `/spec`: add a "required MCP tooling" capture step (in/near `## Validation Criteria`) — when a feature's validation calls MCP tools, enumerate them as a Locked Decision so they land in the `## Locked Decisions` surface the coverage gate already parses. CRITICAL: the captured shape MUST match the canonical Locked-Decision surface `mcp-coverage-audit.md` Step 1 enumerates (a `## Locked Decisions` H2 table or numbered block) — do NOT invent a new section the gate can't see.
  2. In AlgoBooth's `spec-testing-guidance.md`: extend the `## Audio Quality Contracts` override to record required-but-possibly-missing tools, not only the existing-tool menu.
- **Spec requirements:** Locked Decision 3; SPEC `## Affected Area` "Spec authoring" row.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-4 | Solo | None |

---

### Phase: mcp-tooling P4 — Wire decisions as completion coverage + docs

**Goal:** Confirm the Phase-3 tooling Locked Decisions are picked up by the existing completion-time coverage gate, document the now-two-seam contract, and update root `CLAUDE.md`.
**Entry criteria:** Phase 2 AND Phase 3 complete.
**SPEC.md references:** Locked Decision 3 ("Seam = both" — defense-in-depth); SPEC `## Affected Area` "Completion gate" row; cross-link to `docs/features/unified-pipeline-orchestrator/`.

#### WU-5 — Wire tooling decisions into the completion gate + update root CLAUDE.md

- **Scope:** PHASES.md Phase 4 deliverables (gate cross-ref note + root CLAUDE.md update + reverse-reference confirmation).
- **TDD:** no (docs + cross-ref; verified by lint).
- **Files to create/modify:**
  - `user/skills/_components/mcp-coverage-audit.md` [VERIFY: test -f user/skills/_components/mcp-coverage-audit.md]
  - `CLAUDE.md` (repo root) [VERIFY: test -f CLAUDE.md]
- **Implementation goal:**
  1. Confirm a required-MCP-tooling Locked Decision (Phase-3 shape) is enumerated by the `--gate-coverage` algorithm (`mcp-coverage-audit.md` Step 1). If the shape is already covered (it parses `## Locked Decisions` tables/numbered blocks), NO algorithm change is needed — add a one-line cross-reference note to `mcp-coverage-audit.md` naming THIS bug as the upstream planning-time predetermination seam that makes such tooling decisions exist. If the shape needs a parser note, add it.
  2. Update root `CLAUDE.md`'s component-description block where it describes `phases-runtime-validation.md` / the MCP seams, so the layout doc reflects the new planning-time MCP tool-existence audit.
  3. Add a reverse-reference line to PHASES.md Implementation Notes (already present) confirming no spin-off legs were created (all work is in-scope harness edits).
- **Spec requirements:** Locked Decision 3; SPEC `## Affected Area` "Completion gate" row.
- **Completion (gate-owned):** when WU-5 lands, set the top-level PHASES.md `**Status:**` to `In-progress` (implementation done, validation pending). Do NOT set Fixed / write FIXED.md — orchestrator-owned `__mark_fixed__` gate only.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-5 | Solo | None |

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases are complete or a blocking issue triggers early exit:

1. **Select ready phase:** Phases here are strictly sequential (P1 → P2 → P3 → P4; P4 needs both P2 and P3). Select the lowest-numbered phase whose entry criteria are satisfied.
2. **Announce:** Print "Implementing mcp-tooling Phase N: [title]".
3. **Review prior context:** Re-read prior phases' Implementation Notes in PHASES.md — actual file paths landed in earlier phases (especially the real AlgoBooth registry path from Phase 1) take priority over this plan where they diverge.
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "mcp-tooling Phase N: [title] — committed as [hash]".
7. **Loop:** Return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)

Read `~/.claude/skills/_components/task-tracking.md` and follow it exactly (load task tools via ToolSearch, create tasks for all WUs, update protocol). If the executor lacks task tools in its environment, track progress via the `- [ ] WU-N` checkboxes in this plan instead.

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY)

Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and this plan file. Do NOT rely on remembered content.

#### Step B.1: Make the Edits (markdown — INLINE per the dispatch override)

Per the EXECUTION MODEL dispatch override: this plan edits ONLY markdown, so perform the edits INLINE with `Read`/`Edit`/`Write` (no Agent tool needed/available). Run each WU's `[VERIFY: …]` grep/path-check BEFORE editing — a zero-hit `[VERIFY]` means the cited file/path does not exist as written; find the real path (do NOT invent) or convert the dependency to a build deliverable.

#### Step B.2: Review Batch Output (MANDATORY GATE)

Read `~/.claude/skills/_components/subagent-review.md` and apply its protocol to the edits: confirm spec alignment (each edit traces to a SPEC Affected-Area row / Locked Decision), correctness (the new audit prose is internally consistent and references real catalog paths), and no gate-owned rows were authored.

#### Step B.3: Update PHASES.md (MANDATORY)

Read `~/.claude/skills/_components/phases-update.md`. Check off completed deliverables for the phase, add an Implementation Notes block (date, work completed, actual file paths — especially the real AlgoBooth registry path discovered in Phase 1 — and any pitfalls). Tick the corresponding `- [ ] WU-N` checkbox in this plan.

#### Step B.4: Run Quality Gates (MANDATORY)

Run the harness lint + projection (this plan's quality gate — there is no test runner for markdown):
```bash
python ~/.claude/scripts/project-skills.py
python ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities
```
Both must exit clean (no broken `!cat` injections, no circular includes, capabilities valid) before committing. If a gate fails, fix the edit and re-run before proceeding.

#### Step B.5: Commit Batch

Read `.claude/skill-config/commit-policy.md` and follow it: Conventional Commits (`fix(scope): subject` or `docs(scope): subject`), no AI attribution, push to `main`. Re-confirm `git rev-parse --abbrev-ref HEAD` == `main` before committing. Use the ATOMIC GATE+COMMIT pattern where possible:
```bash
python ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities && git add -A && git commit -m "fix(mcp-tooling-predetermination): <phase summary>" && git push origin main
```

#### Step B.6: Proceed to Next Batch

Checklist (all must be true): review verdict produced; PHASES.md updated + WU-N ticked; lint + projection green; committed + pushed per policy. If any unchecked, complete it before the next batch.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY)

Re-run `project-skills.py` + `lint-skills.py --check-projected --check-capabilities` as the integration check — confirm the edited components/skills expand cleanly into EVERY consumer projection (`_default/` and AlgoBooth), since `phases-runtime-validation.md` (Phase 2) and `spec-testing-guidance.md` (Phase 3) are injected into multiple consumers.

#### Update CLAUDE.md Files (MANDATORY)

Read `~/.claude/skills/_components/claude-md-review.md`. Phase 4 explicitly updates root `CLAUDE.md`; for earlier phases, review whether the change warrants a CLAUDE.md note (usually no until Phase 4).

#### Commit and Push Post-Phase Changes

Read `.claude/skill-config/commit-policy.md` and follow it (push to `main`).

## Blocking Issue Protocol

If a blocking issue is encountered:

1. **Stop all in-progress work.**
2. **Commit and push any completed phases** not yet committed.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** mcp-tooling Phase N: [title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining phases (not attempted):**
   - [list]

4. **Do not work around the blocker.**

Blocking issues include:
- The AlgoBooth tool-registry path cannot be located at all (Phase 1 `[VERIFY]` greps all zero-hit) — the catalog has no real target. (Recovery: confirm the AlgoBooth MCP registration mechanism with the operator.)
- A lint/projection failure that can't be fixed after 2 attempts.
- A git push conflict unresolvable by rebase.
- Any change requiring architectural decisions beyond the SPEC's Locked Decisions.

## Completion

When all 4 phases are complete:

1. **Run the harness lint + projection one final time** across all edited skills/components.
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Feature implemented:** mcp-tooling-not-predetermined-at-planning
   **Total phases completed:** 4
   **Total commits:** [M]
   **Final quality-gate status:** lint + projection green

   **Commit log:**
   | Commit | Phase | Title |
   |--------|-------|-------|
   | … | P1 | Per-repo MCP tool catalog |
   | … | P2 | /spec-phases MCP tool-existence audit |
   | … | P3 | /spec required-tooling capture |
   | … | P4 | Completion-gate wiring + docs |

   **Implementation Notes summary:**
   [the real AlgoBooth registry path used by the catalog; confirmation that the planning-time seam (P2) and completion-time seam (P4) cover the same requirement; note that the top-level PHASES.md `**Status:**` was set to `In-progress` — validation tail (/mcp-test → __mark_fixed__) is orchestrator-owned and pending.]

   > NOTE: Do NOT set SPEC/PHASES `**Status:**` to `Fixed` and do NOT write `FIXED.md` — those are
   > owned exclusively by the orchestrator's `__mark_fixed__` gate after the validation tail.
