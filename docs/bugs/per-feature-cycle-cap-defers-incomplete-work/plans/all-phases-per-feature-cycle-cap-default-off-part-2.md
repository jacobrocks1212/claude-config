---
kind: implementation-plan
feature_id: per-feature-cycle-cap-defers-incomplete-work
status: Ready
created: 2026-06-22
complexity: mechanical
phases: [2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-22.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\per-feature-cycle-cap-defers-incomplete-work\plans\all-phases-per-feature-cycle-cap-default-off-part-2.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 2. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\per-feature-cycle-cap-defers-incomplete-work\plans\all-phases-per-feature-cycle-cap-default-off-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\per-feature-cycle-cap-defers-incomplete-work\plans\all-phases-per-feature-cycle-cap-default-off-part-2.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — per-feature-cycle-cap-defers-incomplete-work (Phase 2: docs + baseline regen)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\per-feature-cycle-cap-defers-incomplete-work\PHASES.md` (per-feature-cycle-cap-defers-incomplete-work, 2 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\per-feature-cycle-cap-defers-incomplete-work\SPEC.md`

**Total phases:** 2 (this part covers Phase 2)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture (same table as part 1):

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and doc edits | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit`/`Write` on source files yourself (the ONLY files you may edit directly: `PHASES.md`, `CLAUDE.md`). NOTE: this part is doc-and-baseline work — the doc edits ARE the deliverable; dispatch them to a Sonnet agent (or implement INLINE under the no-Agent dispatch override). The `user/scripts/CLAUDE.md` reword is a CLAUDE.md edit you MAY do directly; the SKILL.md rewords and baseline regen go through an agent (or inline under the override).

> **DISPATCH OVERRIDE NOTE:** In a context without the `Agent` tool, perform ALL edits INLINE with Read/Edit/Write. This is mechanical doc work — no test-first requirement applies to prose edits, but the baseline-regen verification (below) MUST be run.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

Before each step, `Read` the listed component files from disk. After compaction, re-read this plan first.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this plan has no completed hard upstream dependencies) | | | |

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. All non-CLAUDE.md edits go through a Sonnet subagent (or INLINE under the no-Agent override).
2. No worktrees for subagents.
3. PHASES.md updated after the batch completes.
4. Output reviewed before continuing.
5. Quality gates 100% green before commit (this is where the regenerated baseline must pass byte-for-byte).
6. CLAUDE.md updated as warranted.
7. The phase is committed and pushed before claiming done.
8. Self-contained — follow exactly.
9. Before each step, `Read` the listed components from disk.

## Work Units

- [ ] WU-1 — Reword default-on → default-off across the three doc surfaces (`user/scripts/CLAUDE.md`, `user/skills/lazy-batch/SKILL.md`, `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md`)
- [ ] WU-2 — Regenerate `lazy-state-test-baseline.txt` via `_normalize_smoke_output`; confirm `bug-state-test-baseline.txt` unchanged + `lazy_parity_audit.py` green

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | per-feature-cycle-cap-defers-incomplete-work | P2 | Docs + baseline regen | part 1 (Phase 1) complete | Batch 1 = WU-1; Batch 2 = WU-2 |

WU-1 (doc reword) and WU-2 (baseline regen) touch disjoint files; sequenced for review clarity (WU-2's baseline depends on part-1's fixtures already on disk, which they are by the time this part runs).

## Per-Phase Plans

### Phase: per-feature-cycle-cap-defers-incomplete-work P2 — Docs + baseline regen

**Goal:** Reword every "default-on" doc surface to "OFF by default; `--per-feature-cycle-cap <N>` opt-in arms a fixed ceiling; `max_cycles` is the sole default budget." Regenerate the byte-pinned `lazy-state.py --test` baseline. Confirm the bug baseline is unchanged and the parity audit stays green.
**Entry criteria:** Part 1 (Phase 1) complete — `compute_per_feature_ceiling` returns `None` by default and the three new `lazy-state.py --test` fixtures are on disk.
**SPEC.md references:** §Affected Area "Docs" + "Parity" rows; §Evidence/Related Documentation (the `--per-feature-cycle-cap` CLI block).

#### Touchpoint Audit (verified at planning time — ground truth)

| Planned file | Exists? | Real symbols / anchors (verified) | Action | Reuse / refactor directive |
|--------------|---------|-----------------------------------|--------|----------------------------|
| `user/scripts/CLAUDE.md` | yes | the `--per-feature-cycle-cap N` CLI block (long line describing `Default (flag absent): L_task = max(6, …)` + the three composite softenings) | edit (CLAUDE.md — orchestrator may edit directly) | Reword the OPENING from "override the dynamically-computed ceiling / Default = formula" to "OFF by default — guard never arms; `--per-feature-cycle-cap N` opt-in arms a fixed ceiling N; `max_cycles` is the sole default budget." Re-frame the softening description as "under the opt-in flag, the armed guard applies …". |
| `user/skills/lazy-batch/SKILL.md` | yes | argument-hint (line 4), `--per-feature-cycle-cap <N>` flag description (line 66), usage line (72), budget-trip notification §1c.6 (479) | edit (via agent / inline) | Reword line 66 default-on prose → default-off; keep §1c.6 notification (it only fires under the now-opt-in guard). Leave the trip-notification wiring intact. |
| `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` | yes | argument-hint (line 4), `--per-feature-cycle-cap <N>` description (line 84), usage line (89) | edit (via agent / inline) | Mirror the lazy-batch reword EXACTLY (coupled pair); preserve the "NO cloud divergence — identical semantics to /lazy-batch" note. |
| `tests/baselines/lazy-state-test-baseline.txt` | yes | byte-pinned `lazy-state.py --test` output | regenerate | Regenerate ONLY by piping live `lazy-state.py --test` through `_normalize_smoke_output` — never hand-edit. [VERIFY: ls tests/baselines/lazy-state-test-baseline.txt] |
| `tests/baselines/bug-state-test-baseline.txt` | yes | byte-pinned `bug-state.py --test` output | assert-unchanged | Must NOT change (bug pipeline untouched). If it differs, STOP and investigate — that is a regression, not a regen. [VERIFY: ls tests/baselines/bug-state-test-baseline.txt] |
| `user/scripts/lazy_parity_audit.py` | yes | `audit_state_script_parity` | run-only | Confirm exit 0 — no `bug-state.py` budget mirror owed (the budget machinery is feature-only; grep-confirmed zero matches at planning time). [VERIFY: grep -rn "compute_per_feature_ceiling\|budget" user/scripts/lazy_parity_audit.py] |

No contradictions; no drift correction needed.

#### Work Units

**WU-1 — Reword default-on → default-off across the three doc surfaces**
- **Scope:** PHASES Phase 2 deliverables 1–3 (the three doc surfaces).
- **TDD:** no (prose edits).
- **Files to create/modify:**
  - `user/scripts/CLAUDE.md` — `--per-feature-cycle-cap` CLI block. [VERIFY: grep -n "per-feature-cycle-cap" user/scripts/CLAUDE.md]
  - `user/skills/lazy-batch/SKILL.md` — argument-hint + flag description + usage. [VERIFY: grep -n "per-feature-cycle-cap" user/skills/lazy-batch/SKILL.md]
  - `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` — coupled-pair mirror. [VERIFY: grep -n "per-feature-cycle-cap" repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md]
- **Implementation goal:** Reword each surface so the guard is described as OFF by default — it never arms without `--per-feature-cycle-cap <N>`, which arms a fixed ceiling `N`; the whole-run `max_cycles`/`max-cycles` is the sole default budget. In `user/scripts/CLAUDE.md`, keep the three composite-softening descriptions but re-frame them as behaviors of the ARMED (opt-in) guard. In both SKILL.md files, the reword must be byte-identical in intent (the lazy-batch ↔ lazy-batch-cloud pair is coupled — preserve the "NO cloud divergence" note). Do NOT change the budget-trip PushNotification text (§1c.6 / line 479) — it fires only under the now-opt-in guard and stays correct.
- **Spec requirements:** SPEC §Affected Area "Docs" row ("Reword from 'default-on dynamic ceiling, override forces fixed' → 'OFF by default; `--per-feature-cycle-cap <N>` opt-in arms a fixed ceiling'. The whole-run `max_cycles` is the only default budget.").
- **Batch:** 1.

**WU-2 — Regenerate the `lazy-state.py --test` baseline; confirm bug baseline unchanged + parity green**
- **Scope:** PHASES Phase 2 deliverables 4 (regenerate `lazy-state-test-baseline.txt`), 5 (assert `bug-state-test-baseline.txt` unchanged), 6 (parity audit green).
- **TDD:** no (regeneration + verification).
- **Files to create/modify:**
  - `tests/baselines/lazy-state-test-baseline.txt` — regenerated. [VERIFY: ls tests/baselines/lazy-state-test-baseline.txt]
- **Implementation goal:** Regenerate `lazy-state-test-baseline.txt` by piping live `python3 user/scripts/lazy-state.py --test` output through the `_normalize_smoke_output` helper in `test_lazy_core.py` (the canonical, platform-neutral regen path documented in `user/scripts/CLAUDE.md` → Testing — NEVER hand-edit the baseline). The regen captures part-1's three new fixtures. Then run `python3 user/scripts/bug-state.py --test` and confirm `bug-state-test-baseline.txt` is UNCHANGED (the bug pipeline is untouched; a diff here means an unexpected coupling — STOP and investigate, do not regenerate it). Run `python3 user/scripts/lazy_parity_audit.py` and confirm exit 0.
- **Spec requirements:** SPEC §Affected Area "Tests" row ("Regenerate the byte-pinned baseline via `_normalize_smoke_output`") + "Parity" row ("Confirm the feature-only divergence still holds — no `bug-state.py` mirror owed"); §Open Question 3 (default-output byte-identity).
- **Batch:** 2.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | N/A |
| 2 | WU-2 | Solo | None (disjoint from WU-1) |

## Execution Protocol

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)
Read `~/.claude/skills/_components/task-tracking.md`. Create tasks for WU-1/WU-2; update as you progress.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read PHASES.md, SPEC.md, this plan, and the three doc surfaces from disk before editing.

#### Step B.1: Execute the edit (agent or INLINE under the dispatch override)
WU-1: reword the three surfaces. WU-2: regenerate the baseline + run the verifications.

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md`. For WU-1, confirm the reword is accurate (no surviving "default-on"/"Default (flag absent): computes L_task" framing that implies the guard arms by default) and the coupled SKILL.md pair stays in lockstep. For WU-2, confirm the baseline was regenerated via `_normalize_smoke_output` (not hand-edited) and the bug baseline is byte-identical. Produce a PASS/PASS-WITH-FIXES/NEEDS-REWORK verdict.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `~/.claude/skills/_components/phases-update.md`. Tick the Phase 2 deliverable checkboxes; add an Implementation Notes block (date, work, files modified, review verdict). When the LAST Phase 2 deliverable lands, set the top-level PHASES `**Status:**` to `In-progress` (implementation done, validation pending — NOT Fixed/Won't-fix).

#### Step B.4: Run Quality Gates (MANDATORY — the baseline must now pass byte-for-byte)
Read `~/.claude/skills/_components/quality-gates.md`. Run:
```
python3 user/scripts/test_lazy_core.py
python3 user/scripts/lazy-state.py --test
python3 user/scripts/bug-state.py --test
python3 user/scripts/lazy_parity_audit.py
python3 ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities
python3 ~/.claude/scripts/project-skills.py
```
ALL must be green, including the `lazy-state.py --test` byte-pinned baseline comparison (now regenerated). The `lint-skills.py` + `project-skills.py` run validates the reworded SKILL.md files (no broken injections; the lazy-batch ↔ lazy-batch-cloud projection still resolves).

#### Step B.4.5: MCP Integration Test
Skip — `**MCP runtime:** not-required`. Note the skip.

#### Step B.5: Commit Batch
Read `.claude/skill-config/commit-policy.md`. Conventional Commits, no AI attribution, scope `lazy-state`/`skills`/`docs`. Run `python user/scripts/lazy-queue-doc.py --repo-root C:\Users\Jacob\source\repos\claude-config` before the commit so `LAZY_QUEUE.md` rides it. Re-assert `git rev-parse --abbrev-ref HEAD == main` immediately before commit/push. Push to `main`.

#### Step B.6: Proceed to Next Batch
Checklist: review report / PHASES.md updated / all gates green (baseline now matches) / committed. Then Batch 2 (or finish).

### Post-Phase Steps (after WU-1 + WU-2)
- **Update CLAUDE.md:** Read `~/.claude/skills/_components/claude-md-review.md`. `user/scripts/CLAUDE.md` is already reworded in WU-1. Confirm the repo root `CLAUDE.md` Hooks/Scripts tables need no change (they do not reference the budget default).
- **Commit and Push Post-Phase Changes:** per commit policy.

## Blocking Issue Protocol

If a blocking issue is encountered:
1. Stop all in-progress work.
2. Commit and push completed work.
3. Write a canonical `BLOCKED.md` (per `~/.claude/skills/_components/sentinel-frontmatter.md`) naming the blocked WU + reason + recovery.
4. Do NOT work around the blocker.

Blocking issues: `lazy-state.py --test` baseline cannot be made to pass after regen (a logic regression in part 1, not a doc issue — investigate part 1); `bug-state.py --test` baseline differs (unexpected bug-pipeline coupling); `lazy_parity_audit.py` RED; `lint-skills.py` flagging a broken SKILL.md injection.

## Completion

When WU-1 + WU-2 are complete:
1. Run the full quality-gate suite (the six commands above) — all green, baseline byte-matched.
2. Set this part's frontmatter `status:` to `Complete` (owned by `/execute-plan`).
3. Set the top-level PHASES `**Status:**` to `In-progress` (last phase's implementation done; validation pending). Do NOT flip SPEC/PHASES to `Fixed`/`Won't-fix` and do NOT write `FIXED.md` — those are the `__mark_fixed__` gate's job after the validation tail (`bug-state.py` routes there next).
4. Print a completion report (WUs done, files modified, commit hashes, gate status).
