---
kind: implementation-plan
feature_id: per-feature-cycle-cap-defers-incomplete-work
status: Ready
created: 2026-06-22
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-22.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\per-feature-cycle-cap-defers-incomplete-work\plans\all-phases-per-feature-cycle-cap-default-off-part-1.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 2. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\per-feature-cycle-cap-defers-incomplete-work\plans\all-phases-per-feature-cycle-cap-default-off-part-1.md` (this file)
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\per-feature-cycle-cap-defers-incomplete-work\plans\all-phases-per-feature-cycle-cap-default-off-part-2.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — per-feature-cycle-cap-defers-incomplete-work (Phase 1: invert the default)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\per-feature-cycle-cap-defers-incomplete-work\PHASES.md` (per-feature-cycle-cap-defers-incomplete-work, 2 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\per-feature-cycle-cap-defers-incomplete-work\SPEC.md`

**Total phases:** 2 (this part covers Phase 1)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **DISPATCH OVERRIDE NOTE:** If executed in a context WITHOUT the `Agent` tool (e.g. a lazy-bug-batch cycle subagent), perform ALL work INLINE with Read/Edit/Write — the no-Agent dispatch override supersedes the orchestrator/subagent split above. Still write failing tests BEFORE implementation for each TDD work unit (test-first), and still run the review + quality-gate steps inline.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this plan has no completed hard upstream dependencies) | | | |

SPEC `**Depends on:**` block is absent (this is an investigation SPEC); the `**Related:**` block names `feature-budget-guard-and-skip-ahead` (the introducing feature, Complete — soft relation) and the superseded prior softening bug. Neither is a hard dep requiring upstream-plan look-back.

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — unless executed under the no-Agent dispatch override (see EXECUTION MODEL note), in which case perform it INLINE, test-first.
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline — failing tests written before implementation.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in a phase finish, integration verification confirms all changes work together.
8. Relevant CLAUDE.md files are created/updated after the phase if changes warrant it.
9. The completed phase is committed and pushed before part 2 begins.
10. This plan is self-contained — follow it exactly as written without relying on external context.
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.

## Work Units

- [ ] WU-1 — Invert `compute_per_feature_ceiling` default to `None` + re-pin formula characterization tests
- [ ] WU-2 — Add `lazy-state.py --test` fixtures: default-off (no trip), opt-in-arms (trip), baseline-regression byte-identity

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | per-feature-cycle-cap-defers-incomplete-work | P1 | Invert the default | — | Batch 1 = WU-1; Batch 2 = WU-2 |

WU-1 and WU-2 both touch `test_lazy_core.py` / the `--test` harness surface AND WU-2's fixtures assert the behavior WU-1 implements → sequential batches (WU-1 then WU-2). WU-2's new `lazy-state.py` fixtures depend on WU-1's `lazy_core` source change being on disk.

## Per-Phase Plans

### Phase: per-feature-cycle-cap-defers-incomplete-work P1 — Invert the default

**Goal:** Flip the per-feature budget guard OFF by default — `compute_per_feature_ceiling`'s no-override branch returns `None`, disabling the entire marker+ceiling-gated budget block unless `--per-feature-cycle-cap <N>` is supplied.
**Entry criteria:** None (first phase).
**SPEC.md references:** §Proven Findings 1–2 (root cause + fix shape), §Affected Area rows "Ceiling default" + "Trip / defer / evict" + "Tests", §Open Questions 1 & 3.

#### Touchpoint Audit (verified at planning time — ground truth)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `user/scripts/lazy_core.py` | yes | `compute_per_feature_ceiling(max_cycles, ready_queue_depth, override=None)` at line 10273; default branch `q<=0: return 6` (10317-10320) + `return max(6, min(forty_percent_arm, fair_share_arm))` (10321-10323); override branch `if override is not None: return int(override)` (10307-10308) | refactor | Reuse the function; make ALL no-override paths `return None`. Keep the `override is not None: return int(override)` branch (incl. `override=0`) byte-identical. Widen return annotation `int` → `int | None`. Rewrite docstring for default-off. Do NOT touch `_bump_per_feature_forward` / `read_per_feature_forward_cycles` siblings. |
| `user/scripts/lazy-state.py` | yes | call site `_bg_ceiling = compute_per_feature_ceiling(_bg_max_cycles, _bg_ready_depth, override=per_feature_cycle_cap) if _bg_marker is not None else None` (1650-1656); trip gate `if _bg_marker is not None and _bg_ceiling is not None:` (2086) | no source change (WU-1) / add fixtures (WU-2) | The gate at 2086 ALREADY short-circuits on `_bg_ceiling is None`. No logic edit needed. WU-2 adds `--test` fixtures only. |
| `user/scripts/test_lazy_core.py` | yes | `test_compute_per_feature_ceiling_*` block (21315-21376): `_override_short_circuits`, `_six_floor_small_run`, `_deep_queue_six`, `_forty_percent_cap_arm`, `_zero_queue_no_div_by_zero`, `_pure_no_side_effects`, registered in `_TESTS` | refactor | Re-pin the five FORMULA tests to assert the no-override default is now `None`; keep `_override_short_circuits` asserting override returns verbatim. |

Blast radius (`get_callers`-equivalent grep): `compute_per_feature_ceiling` is referenced ONLY at `lazy-state.py:1651` (the one call site, already `None`-tolerant) + its definition + `test_lazy_core.py`. `lazy_parity_audit.py` does NOT reference it (grep: zero hits) → feature-pipeline-only, no `bug-state.py` mirror owed. No contradictions found; no drift correction needed.

#### Work Units

**WU-1 — Invert `compute_per_feature_ceiling` default to `None` + re-pin formula characterization tests**
- **Scope:** PHASES Phase 1 deliverable 1 (source flip + return-type widen + docstring) and deliverable 2 (re-pin `test_compute_per_feature_ceiling_*`).
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/lazy_core.py` — `compute_per_feature_ceiling` (~10273-10323). [VERIFY: grep -n "def compute_per_feature_ceiling" user/scripts/lazy_core.py]
  - `user/scripts/test_lazy_core.py` — the `test_compute_per_feature_ceiling_*` block (~21315-21376). [VERIFY: grep -n "def test_compute_per_feature_ceiling_override_short_circuits" user/scripts/test_lazy_core.py]
- **Test files:** `user/scripts/test_lazy_core.py` (in-file `_TESTS` harness — these ARE the tests).
- **Test expectations (write/repin tests FIRST, confirm RED against the un-flipped source, THEN implement):**
  - `test_compute_per_feature_ceiling_override_short_circuits` — UNCHANGED: `override=3 → 3`, `override=99 → 99`, `override=0 → 0` (the deliberate falsy cap honored verbatim).
  - `test_compute_per_feature_ceiling_six_floor_small_run` → re-pin: `compute_per_feature_ceiling(12, 2) is None` (was `== 6`).
  - `test_compute_per_feature_ceiling_deep_queue_six` → re-pin: `compute_per_feature_ceiling(32, 10) is None` (was `== 6`).
  - `test_compute_per_feature_ceiling_forty_percent_cap_arm` → re-pin: `compute_per_feature_ceiling(50, 2) is None` by default; ADD `compute_per_feature_ceiling(50, 2, override=20) == 20` to keep the opt-in fixed-ceiling characterized.
  - `test_compute_per_feature_ceiling_zero_queue_no_div_by_zero` → re-pin: `compute_per_feature_ceiling(20, 0) is None` and `compute_per_feature_ceiling(20, -1) is None` (default-off makes the div-by-zero path unreachable; the override path still never divides). Keep the docstring note that the guard never arms by default.
  - `test_compute_per_feature_ceiling_pure_no_side_effects` → re-pin: two identical no-override calls both `None` and equal; assert `compute_per_feature_ceiling(25, 4) is None`.
  - Update each test's RED docstring to describe the default-off contract.
- **Implementation goal:** In `compute_per_feature_ceiling`, after the `if override is not None: return int(override)` branch (keep byte-identical), make EVERY remaining (no-override) code path return `None` — replace the `q <= 0: return 6` and the final `return max(6, min(forty_percent_arm, fair_share_arm))` with `return None`. (The `forty_percent_arm`/`fair_share_arm` computations may be removed as dead, or kept harmlessly — prefer removing them so the default branch is simply `return None`.) Widen the signature/annotation return type `int` → `int | None`. Rewrite the docstring: the guard is OFF by default (no override → `None` → `_bg_ceiling is None` short-circuits the entire budget block in `lazy-state.py`); `--per-feature-cycle-cap <N>` is the OFF-by-default opt-in that re-arms a fixed ceiling `N` (including `0`); the whole-run `max_cycles` is the sole default budget.
- **Spec requirements:** SPEC §Proven Findings 2 ("the no-override branch of `compute_per_feature_ceiling` returns **`None`** … the `override` branch is unchanged … Return type widens `int → int | None`; the one call site already tolerates `None`. No deletion of the trip/defer/evict/grace/flush machinery"); §Open Question 1 (default path computes **nothing**, resolved scope-class).
- **Batch:** 1.

**WU-2 — Add `lazy-state.py --test` fixtures: default-off (no trip), opt-in-arms (trip), baseline-regression byte-identity**
- **Scope:** PHASES Phase 1 deliverables 3 (default-off fixture), 4 (re-pin/opt-in-arms fixture), 5 (baseline-regression byte-identity fixture).
- **TDD:** yes (the fixtures ARE the characterization; they pass against WU-1's flipped source and would FAIL against the old default-on source).
- **Files to create/modify:**
  - `user/scripts/lazy-state.py` — the in-file `--test` smoke harness ONLY (add fixtures; NO production-code edit — the trip gate at line 2086 already short-circuits on a `None` ceiling). [VERIFY: grep -n "_bg_ceiling is not None" user/scripts/lazy-state.py]
- **Test files:** `user/scripts/lazy-state.py` (in-file `--test` fixtures).
- **Test expectations (write fixtures, confirm they characterize the WU-1 behavior):**
  - **default-off fixture:** a live run marker + a feature whose `per_feature_forward_cycles[feature_id]` exceeds the old floor-6 ceiling, NO `--per-feature-cycle-cap` flag → `compute_state` produces NO budget trip (the feature is NOT appended to `_DEFERRED_BUDGET`, no `defer`/`evict` action, the `budget_guard` probe field is absent/null). Model it on an existing budget-trip fixture if one exists; otherwise on the closest marker+per-feature-count fixture.
  - **opt-in-arms fixture:** the SAME marker + feature WITH `--per-feature-cycle-cap <N>` where the count exceeds `N` → the trip/defer fires (guard re-arms; `budget_guard` non-null). This proves the opt-in path still works. (If an existing fixture already asserted a default-on trip, RE-PIN it to pass `--per-feature-cycle-cap <N>` rather than relying on the now-disabled default; if none exists, this new fixture is the sole opt-in characterization — record which case applied.)
  - **baseline-regression fixture:** a no-marker / no-flag probe stays byte-identical to today (budget block is marker-gated AND now ceiling-gated). Assert the default probe output is unchanged from the existing baseline-default fixture shape.
- **Implementation goal:** Add the three fixtures to `lazy-state.py`'s `--test` harness and register them in its fixture list, following the existing fixture conventions (temp-dir setup, marker write, assertion shape). Do NOT regenerate the byte-pinned baseline file here — that is part 2 (Phase 2), after both source + fixtures are on disk. Just make `python3 user/scripts/lazy-state.py --test` run green with the new fixtures (the run will FAIL the baseline byte-comparison until part 2 regenerates it — that is EXPECTED and handled in part 2; the fixture LOGIC must pass).
- **Spec requirements:** SPEC §Affected Area "Tests" row; §Open Question 3 (default-output byte-identity — add a baseline-regression fixture to prove it).
- **Batch:** 2.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | N/A |
| 2 | WU-2 | Solo | WU-2 depends on WU-1's `lazy_core` source being on disk |

## Execution Protocol

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)
Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (load task tools via ToolSearch, create tasks for WU-1/WU-2, update as you progress). Under the no-Agent dispatch override, still create + update tasks at the orchestrator level.

### Per-Batch Steps
For each batch (WU-1, then WU-2):

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read PHASES.md, SPEC.md (relevant sections), this plan file. Re-read the actual `compute_per_feature_ceiling` body + the `lazy-state.py:2086` gate from disk before editing.

#### Step B.1: Launch Subagents (or implement INLINE under the dispatch override)
Read `~/.claude/skills/_components/tdd-protocol.md`, `subagent-launch.md`, `tdd-test-agent.md`, `implementation-agent.md`. Dispatch one Sonnet test agent + one impl agent per WU (test-first). Under the no-Agent override: write the failing test edit FIRST, run `python3 user/scripts/test_lazy_core.py` (WU-1) / `python3 user/scripts/lazy-state.py --test` (WU-2) to confirm RED for the right reason, THEN implement, THEN confirm GREEN — all inline.

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md` + `mount-site-verification.md`. Produce a structured review report with a PASS/PASS-WITH-FIXES/NEEDS-REWORK verdict. Verify the assertion-vs-intent: each re-pinned formula test now asserts `is None` and would FAIL against the old `== 6` source.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `~/.claude/skills/_components/phases-update.md`. Tick the landed Phase 1 deliverable checkboxes; add an Implementation Notes block (date, work, files modified, review verdict).

#### Step B.4: Run Quality Gates (MANDATORY)
Read `~/.claude/skills/_components/quality-gates.md`. For claude-config the gates are the hermetic Python suites:
```
python3 user/scripts/test_lazy_core.py
python3 user/scripts/lazy-state.py --test
python3 user/scripts/bug-state.py --test
python3 user/scripts/lazy_parity_audit.py
python3 ~/.claude/scripts/lint-skills.py
```
WU-1 must keep `test_lazy_core.py` green. WU-2's `lazy-state.py --test` fixture LOGIC must pass; the byte-pinned baseline comparison is EXPECTED to differ until part 2 regenerates it — note that explicitly, do NOT treat the baseline mismatch as a WU-2 logic failure. `bug-state.py --test` and `lazy_parity_audit.py` MUST stay green (bug pipeline untouched).

#### Step B.4.5: MCP Integration Test
Skip — `**MCP runtime:** not-required` (claude-config has no MCP/Tauri surface). Note the skip.

#### Step B.5: Commit Batch
Read `.claude/skill-config/commit-policy.md`. Commit per Conventional Commits, no AI attribution, scope `lazy-core` / `lazy-state`. Before the commit, run `python user/scripts/lazy-queue-doc.py --repo-root C:\Users\Jacob\source\repos\claude-config` so `LAZY_QUEUE.md` rides the commit. Push to `main` (allowed — personal repo). Re-assert `git rev-parse --abbrev-ref HEAD == main` immediately before each commit/push.

#### Step B.6: Proceed to Next Batch
Checklist: review report with verdict / PHASES.md updated / quality gates green (modulo the expected WU-2 baseline-mismatch note) / committed. Then launch Batch 2.

### Post-Phase Steps (after WU-1 + WU-2)
- **Integration Verification:** Read `~/.claude/skills/_components/integration-verification.md`. Confirm: default probe = no trip; `--per-feature-cycle-cap N` = trip; bug pipeline + parity audit green.
- **Update CLAUDE.md:** Read `~/.claude/skills/_components/claude-md-review.md`. The `--per-feature-cycle-cap` CLI block in `user/scripts/CLAUDE.md` describes the guard as default-on — that reword is owned by **part 2 (Phase 2)**. Do NOT reword it here; note it as part-2 work.
- **Commit and Push Post-Phase Changes:** per commit policy.

## Blocking Issue Protocol

If a blocking issue is encountered:
1. Stop all in-progress work.
2. Commit and push completed work.
3. Write a canonical `BLOCKED.md` (per `~/.claude/skills/_components/sentinel-frontmatter.md`) in the bug dir naming the blocked WU + reason + recovery suggestion.
4. Do NOT work around the blocker.

Blocking issues: a quality-gate failure unfixable after 2 retries; `bug-state.py --test` or `lazy_parity_audit.py` going RED (would mean an unexpected bug-pipeline coupling — investigate, do not force); any decision requiring architecture beyond the spec.

## Completion

When WU-1 + WU-2 are complete:
1. Run the full quality-gate suite (the five commands above). `test_lazy_core.py` green, `lazy-state.py --test` fixture logic green (baseline regen deferred to part 2), `bug-state.py --test` + `lazy_parity_audit.py` green.
2. Set this part's frontmatter `status:` to `Complete` (owned by `/execute-plan`).
3. Print a completion report (WUs done, files modified, commit hashes, gate status). Do NOT flip SPEC/PHASES top-level `**Status:**` — that is the `__mark_fixed__` gate's job after the validation tail.
4. Proceed to part 2 (Phase 2 — docs reword + baseline regen + parity confirm).
