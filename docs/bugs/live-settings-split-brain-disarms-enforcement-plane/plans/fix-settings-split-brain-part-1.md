---
kind: implementation-plan
feature_id: live-settings-split-brain-disarms-enforcement-plane
status: Complete
created: 2026-07-12
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-12.
> To execute: `/execute-plan <this-path>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 3. Sibling parts:
> - part 1: `docs/bugs/live-settings-split-brain-disarms-enforcement-plane/plans/fix-settings-split-brain-part-1.md` (this file)
> - part 2: `docs/bugs/live-settings-split-brain-disarms-enforcement-plane/plans/fix-settings-split-brain-part-2.md`
> - part 3: `docs/bugs/live-settings-split-brain-disarms-enforcement-plane/plans/fix-settings-split-brain-part-3.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — Live settings split-brain (Phases 1–2)  (v2)

**PHASES.md files:**
- `docs/bugs/live-settings-split-brain-disarms-enforcement-plane/PHASES.md` (5 phases; this part covers 1–2)

**SPEC.md files:**
- `docs/bugs/live-settings-split-brain-disarms-enforcement-plane/SPEC.md`

**Total phases:** 5 (this part: 2)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is the **claude-config** harness repo — gates are Python/PowerShell tests + linters, NOT `/msbuild`/`/mstest`/MCP), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config harness repo)

- **No MCP runtime, no Tauri build.** There is no live-runtime validation step for this repo. "Quality gates" = the commands in the Quality Gates block below.
- **Quality Gates (run from repo root after each phase's batches):**
  - `python3 -m pytest user/scripts/test_hooks.py user/scripts/test_doc_drift_lint.py -q` (all green)
  - `python3 user/scripts/doc-drift-lint.py --repo-root .` (exit 0 — clean)
  - `python3 user/scripts/lint-skills.py` (exit 0 — no skills touched here, but confirms no regression)
- **Commit after each phase** (docs + code + tests green). This is a solo config repo — commit directly to `main`, do NOT branch or open a PR (repo CLAUDE.md Branching Policy). Push is allowed (`git config user.email` is the personal account, not `jacob@cognitoforms.com`). Use the repo's commit-message trailer convention from the environment's git instructions.
- **One-writer-per-file:** the two batches below are file-disjoint within each phase; dispatch disjoint WUs in a single message.

## References — Upstream Artifacts

(none — this bug has no completed hard upstream dependencies. The `**Related:**` turn-routing-enforcement docs are EDITED by part 3, not consumed as a build contract.)

## Touchpoint Audit (verified — Explore pass, 2026-07-12; zero contradictions)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `user/settings.json` | yes | `hooks` object: 10 hooks across SessionStart (:25-51), PreToolUse Read/Bash/Skill/Write\|Edit (:52-118), `PostToolUse: []` (:119) | refactor | Add turn-routing pair INTO the existing `hooks` object; reuse the EXACT command strings from the live file; preserve the 10 existing hooks byte-for-byte |
| `CLAUDE.md` (repo root) | yes | `## Hooks` table | refactor | Add exactly two rows (route-inject, dispatch-guard) |
| `user/scripts/test_hooks.py` | yes | `test_straybranch_registered_in_settings` (:4677), `test_longbuild_guard_registered_in_settings` (:4832); `_REPO_ROOT` (:37) | refactor | Add two registration tests following the SAME ad-hoc `settings_path = _REPO_ROOT / "user" / "settings.json"` pattern (no shared fixture exists) |
| `user/scripts/doc-drift-lint.py` | yes | `check_hooks` (:202, reads `repo_root/"user"/"settings.json"` :205), `check_scripts` (:313), `check_coupled_pairs` (:341), `check_manifest` (:451), `run_checks` (:505-506), `main` (:511, argparse `--repo-root` only) | refactor | Add `--live` flag + `check_live_settings(repo_root)` + importable `live_settings_status(repo_root)` helper; **gate behind `--live` — do NOT add to the default `run_checks` tuple** (it inspects machine-local state) |
| `user/scripts/test_doc_drift_lint.py` | yes | `ddl` module fixture (:21-30), `run_lint(repo_root)` subprocess helper (:33-38), `test_this_repo_is_clean` (:604) | refactor | Add `test_live_*` cases using a monkeypatched/parameterized live path — NEVER read the real `~/.claude` |

Live-file ground truth (source for WU-1's exact strings): `~/.claude/settings.json` `hooks` = `lazy-route-inject.sh` on UserPromptSubmit (timeout 90), SessionStart matcher `compact` (timeout 90), PostCompact (timeout 90); `lazy-dispatch-guard.sh` on PreToolUse matcher `Agent|Task` (timeout 30). Command form: `bash ~/.claude/hooks/<name>.sh`.

## Execution Schedule

| Step | Phase | Title | Blocked by | Parallel? |
|------|-------|-------|------------|-----------|
| 1 | P1 Batch 1 | WU-1 settings merge + WU-2 CLAUDE.md rows | — | Yes (file-disjoint) |
| 2 | P1 Batch 2 | WU-3 test_hooks registration tests | WU-1 | Solo |
| 3 | P2 Batch 1 | WU-4 doc-drift-lint `--live` + WU-5 tests | P1 complete | Solo (TDD single WU spanning impl+test) |

## Work Units

- [x] WU-1 — Merge turn-routing pair into tracked `user/settings.json`
- [x] WU-2 — Document the two hooks in root `CLAUDE.md` Hooks table
- [x] WU-3 — Add registration tests to `test_hooks.py`
- [x] WU-4 — Add `--live` check mode + reusable helper to `doc-drift-lint.py`
- [x] WU-5 — Add `--live` mode tests to `test_doc_drift_lint.py`

---

### Phase: live-settings-split-brain P1 — Reconcile hook registration into the tracked SSOT

**Goal:** Make tracked `user/settings.json` the single SSOT carrying all 12 hooks (the 10 it has + the turn-routing pair), documented and asserted — the load-bearing prerequisite for the part-2 symlink restore.
**Entry criteria:** None (first phase).
**SPEC.md references:** Fix Scope 1; Verified Symptom 2 & 3; Root Cause (config-split-brain).

#### WU-1 — Merge turn-routing pair into tracked `user/settings.json`
- **Scope:** PHASES P1 deliverable 1. Add `UserPromptSubmit` → `lazy-route-inject.sh` (timeout 90) and `PostCompact` → `lazy-route-inject.sh` (timeout 90) top-level keys; add `lazy-route-inject.sh` (timeout 90) to the existing SessionStart `compact` matcher block (as an additional hook alongside `execute-plan-compact-reorient.sh`, OR as a second `compact` matcher entry — either is valid Claude Code config); add a new `PreToolUse` matcher `Agent|Task` block → `lazy-dispatch-guard.sh` (timeout 30). Preserve the 10 existing hooks byte-for-byte.
- **TDD:** no (JSON config; correctness verified by WU-3's tests + a JSON-parse smoke).
- **Files to create/modify:** `user/settings.json` [VERIFY: grep -n '"hooks"' user/settings.json].
- **Implementation goal:** the merged `hooks` object parses as valid JSON and contains all 12 hook registrations; the exact command strings match the live file (`bash ~/.claude/hooks/lazy-route-inject.sh`, `bash ~/.claude/hooks/lazy-dispatch-guard.sh`) [VERIFY: cat ~/.claude/settings.json]. Behavior is unchanged on every machine because both lazy hooks are marker-gated per-repo (fail-open, marker-absent fast path).
- **Spec requirements:** SPEC Fix Scope 1 — "Reconcile both hook sets into tracked `user/settings.json` as the single SSOT … This is safe on all machines: the lazy hooks are marker-gated per-repo."
- **Batch:** 1.

#### WU-2 — Document the two hooks in root `CLAUDE.md` Hooks table
- **Scope:** PHASES P1 deliverable 2. Add one `## Hooks` table row for `lazy-route-inject.sh` (SessionStart-compact / UserPromptSubmit / PostCompact — turn-routing banner injector) and one for `lazy-dispatch-guard.sh` (PreToolUse Agent|Task — the marker-gated verbatim-dispatch guard). Content should be consistent with the existing per-repo-scoping note that already describes both hooks.
- **TDD:** no (docs).
- **Files to create/modify:** `CLAUDE.md` (repo root) [VERIFY: grep -n '## Hooks' CLAUDE.md].
- **Implementation goal:** `doc-drift-lint.py check_hooks` sees both newly-registered hooks as DOCUMENTED (no registered-but-undocumented drift). Rows follow the existing table's column shape (Hook | Trigger | Purpose).
- **Spec requirements:** implied by SPEC Fix Scope 1 + SPEC Detection point 6a (`check_hooks` compares CLAUDE.md ↔ settings.json).
- **Batch:** 1 (file-disjoint from WU-1).

#### WU-3 — Add registration tests to `test_hooks.py`
- **Scope:** PHASES P1 deliverable 3. Add `test_routeinject_registered_in_settings` and `test_dispatchguard_registered_in_settings`.
- **TDD:** yes (test-first: the tests assert the WU-1 registration; run RED against a pre-WU-1 tree if authored first, GREEN after).
- **Files to create/modify:** `user/scripts/test_hooks.py` [VERIFY: grep -n 'def test_longbuild_guard_registered_in_settings' user/scripts/test_hooks.py].
- **Test files:** same file (append tests).
- **Test expectations:** each test resolves `settings_path = _REPO_ROOT / "user" / "settings.json"` [VERIFY: grep -n '_REPO_ROOT' user/scripts/test_hooks.py], loads JSON, and asserts the expected hook script is registered on the expected event/matcher — `lazy-route-inject.sh` under UserPromptSubmit AND SessionStart-`compact` AND PostCompact; `lazy-dispatch-guard.sh` under PreToolUse matcher `Agent|Task`. Mirror the assertion style of `test_straybranch_registered_in_settings` (:4677).
- **Implementation goal:** 4 registration tests total (2 existing + 2 new) pass against the WU-1 tree.
- **Spec requirements:** SPEC Detection point 6c (mount-site registration tests currently read the repo file and stay green while the live machine is disarmed — these new tests extend that coverage to the turn-routing pair).
- **Batch:** 2 (depends on WU-1's registration existing to pass GREEN).

**Runtime Verification** *(none — this repo has no runtime; correctness is proven by pytest + doc-drift-lint at the Quality Gates step):*
- (n/a)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1, WU-2 | Yes | None (settings.json vs CLAUDE.md) |
| 2 | WU-3 | Solo | N/A |

**Completion (gate-owned):** do NOT author a SPEC status flip or FIXED.md write here — those are `__mark_fixed__`-gate-owned (part 3 / the orchestrator's validation tail).

---

### Phase: live-settings-split-brain P2 — `--live` drift-detection mode in `doc-drift-lint.py`

**Goal:** Add the detection primitive that makes the live-vs-tracked split self-announce, plus the reusable helper part 3's surfacing consumes.
**Entry criteria:** P1 complete (the tracked SSOT the `--live` check compares against is now the complete one).
**SPEC.md references:** Fix Scope 4; Decision D2; Detection point 6a.

#### WU-4 — Add `--live` check mode + reusable helper to `doc-drift-lint.py`
- **Scope:** PHASES P2 deliverables 1–3. Add a `--live` argparse flag; `check_live_settings(repo_root)` returning structured findings; and a thin importable `live_settings_status(repo_root) -> (ok: bool, detail: str)` the part-3 banner/probe reuse. `--live` must be OPT-IN and NOT part of the default `run_checks` tuple (it inspects `~/.claude`, so `test_this_repo_is_clean` must not depend on it).
- **TDD:** yes (WU-5 is the test half; author tests first).
- **Files to create/modify:** `user/scripts/doc-drift-lint.py` [VERIFY: grep -n 'def check_hooks' user/scripts/doc-drift-lint.py] [VERIFY: grep -n 'def run_checks' user/scripts/doc-drift-lint.py].
- **Implementation goal:** verdict logic — symlink resolving to the repo `user/settings.json` → clean; real-file-not-symlink → drift finding naming the corrective `setup repair`; symlink resolving elsewhere → drift; content-identical non-symlink → PASS with advisory note (copy-based/cloud hosts); live file missing → finding. Exit codes consistent with existing convention (2 malformed / 1 drift / 0 clean). Provide a test seam (e.g. an internal `_live_settings_path(repo_root)` or a `--live-path` override) so tests never touch the real `~`.
- **Spec requirements:** SPEC Fix Scope 4 — "Add a `--live` check mode to `doc-drift-lint.py`: PASS iff the live `~/.claude/settings.json` is a symlink AND resolves to the repo file (content-identical as fallback for copy-based hosts)."
- **Batch:** 1.

#### WU-5 — Add `--live` mode tests to `test_doc_drift_lint.py`
- **Scope:** PHASES P2 deliverable 4. Tests for: symlink-intact PASS, real-file FAIL (names `setup repair`), symlink-to-wrong-target FAIL, content-identical-copy PASS, missing-live-file finding; and a guard that `--live` did NOT leak into the default check set (`test_this_repo_is_clean` still green).
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/test_doc_drift_lint.py` [VERIFY: grep -n 'def test_this_repo_is_clean' user/scripts/test_doc_drift_lint.py].
- **Test files:** same file.
- **Test expectations:** use the existing `ddl` fixture (:21-30) for direct-call cases and/or `run_lint` (:33-38) for exit-code cases; fabricate a fake live path via `tmp_path` + monkeypatch (or the `--live-path` seam from WU-4). Assert each verdict + exit code.
- **Implementation goal:** full green including the unchanged `test_this_repo_is_clean`.
- **Spec requirements:** SPEC Fix Scope 4.
- **Batch:** 1 (TDD pairing with WU-4 — authored together as the test+impl of one `--live` capability; a single logical WU implemented test-first. If the executor prefers strict test-then-impl ordering, WU-5's test file is written and RED before WU-4's impl lands).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-4 (+ WU-5 tests) | Solo | None (doc-drift-lint.py vs its test file) |

**Completion (gate-owned):** none authored here.

---

## Blocking Issue Protocol & Completion

Single-sourced in `~/.claude/skills/_components/execution-contract.md` (read it before executing). On a genuine design fork (only candidate in this whole plan: none in parts 1–2 — the D1 merge-precedence fork lives in part 2/Phase 3), write `NEEDS_INPUT.md` in the bug dir and halt. Otherwise, on completion of both phases with Quality Gates green and per-phase commits landed, this part is done — proceed to part 2.
