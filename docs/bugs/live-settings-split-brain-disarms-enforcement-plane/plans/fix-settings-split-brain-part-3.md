---
kind: implementation-plan
feature_id: live-settings-split-brain-disarms-enforcement-plane
status: Complete
created: 2026-07-12
complexity: complex
phases: [4, 5]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-12.
> To execute: `/execute-plan <this-path>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 3 of 3. Sibling parts:
> - part 1: `docs/bugs/live-settings-split-brain-disarms-enforcement-plane/plans/fix-settings-split-brain-part-1.md`
> - part 2: `docs/bugs/live-settings-split-brain-disarms-enforcement-plane/plans/fix-settings-split-brain-part-2.md`
> - part 3: `docs/bugs/live-settings-split-brain-disarms-enforcement-plane/plans/fix-settings-split-brain-part-3.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — Live settings split-brain (Phases 4–5)  (v2)

**PHASES.md files:**
- `docs/bugs/live-settings-split-brain-disarms-enforcement-plane/PHASES.md` (5 phases; this part covers 4–5)

**SPEC.md files:**
- `docs/bugs/live-settings-split-brain-disarms-enforcement-plane/SPEC.md`

**Total phases:** 5 (this part: 2)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch. Where this **claude-config** harness repo uses non-default gates (Python/PowerShell tests + linters, NOT `/msbuild`/`/mstest`/MCP), this plan's `## Plan-specific execution notes` override the contract's defaults; everything else in the contract still applies.

## Plan-specific execution notes (claude-config harness repo)

- **Phase 4 is pure documentation** (retire REGISTRATION.md, amend SPEC, blind-window annotation). **Phase 5 is cross-boundary wiring** (banner hook + probe field) — this is why the whole part is tagged `complex`, not `mechanical`.
- **Quality Gates (from repo root):**
  - `python3 user/scripts/doc-drift-lint.py --repo-root .` (exit 0 — no new script/hook rows implied; if lazy-state.py gains a probe field it needs no doc row, but re-confirm clean)
  - `python3 -m pytest user/scripts/test_doc_drift_lint.py user/scripts/test_hooks.py -q` (green; plus any lazy-state probe test)
  - `python3 user/scripts/lint-skills.py` (exit 0)
  - Phase-4 grep assertions (below).
- **Commit after each phase** on `main` (no branch/PR).

## References — Upstream Artifacts

(none.) NOTE: Phase 4 EDITS `docs/specs/turn-routing-enforcement/{REGISTRATION.md,SPEC.md}` — these are the design being retired, not a build dependency.

## Touchpoint Audit (verified — Explore pass, 2026-07-12; zero contradictions)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `docs/specs/turn-routing-enforcement/REGISTRATION.md` | yes | paste-fragment doc: "Paste this fragment as the `hooks` key in each machine's live `~/.claude/settings.json`" (:7); merge example showing `pr-review-cache-guard.sh` (:107-124); 2026-06-11 pipe-test records (:126-141) | refactor | Retire the per-machine paste design; KEEP the pipe-test records; add a pointer that registration now ships tracked |
| `docs/specs/turn-routing-enforcement/SPEC.md` | yes | §"Settings placement (honest constraint, surfaced)" (:114) declaring per-machine registration + deferred unification | refactor | Amend to state registration is now tracked; close/update the deferred-unification note |
| `setup.ps1` | yes | REGISTRATION.md citation in the warn message (:253) | refactor | Update the citation now that the doc is retired (coordinate with part-2 WU-7, which touched the same warn pass — this is the doc-pointer follow-through) |
| hook-event consumers | yes | `user/scripts/incident-scan.py`, efficacy-eval read paths | annotate | Add a machine-scoped `blind_window` note (Jun 11 → fix date) where hook-derived signals are consumed (annotate-only, D3) |
| `user/hooks/lazy-route-inject.sh` | yes | banner injector (fires UserPromptSubmit / SessionStart-compact / PostCompact) | refactor | Emit ONE advisory line on detected drift; fail-open (never break the banner) |
| `user/scripts/lazy-state.py` | yes | `--probe` action | refactor | Add a `live_settings_ok` (or equiv) probe field sourced from part-1's `live_settings_status` helper |

## Execution Schedule

| Step | Phase | Title | Blocked by | Parallel? |
|------|-------|-------|------------|-----------|
| 1 | P4 Batch 1 | WU-9 REGISTRATION/SPEC retire + WU-10 blind-window annotation | parts 1 & 2 complete | Yes (file-disjoint) |
| 2 | P5 Batch 1 | WU-11 lazy-route-inject banner + WU-12 lazy-state probe field | part 1 (`live_settings_status` helper) | Yes (file-disjoint) |

## Work Units

- [x] WU-9 — Retire REGISTRATION.md per-machine design + amend SPEC §Settings placement
- [x] WU-10 — Add the blind-window annotation for hook-event consumers
- [x] WU-11 — Emit a drift advisory line from `lazy-route-inject.sh`
- [x] WU-12 — Add a `live_settings_ok` probe field to `lazy-state.py`

---

### Phase: live-settings-split-brain P4 — Retire the per-machine registration design + annotate the blind window

**Goal:** Make the docs tell the truth now that registration ships tracked, and mark the Jun 11 → fix-date hook-signal blind window so it isn't misread as zero-friction.
**Entry criteria:** Part 1 (registration genuinely in the tracked file — the doc claims must be TRUE) and part 2 (setup.ps1 warn message rewritten, so its REGISTRATION.md citation is updated in step).
**SPEC.md references:** Fix Scope 3 & 6; Decision D3.

#### WU-9 — Retire REGISTRATION.md per-machine design + amend SPEC §Settings placement
- **Scope:** PHASES P4 deliverables 1, 2, 4. Convert `REGISTRATION.md` to a retired/historical doc (remove or mark-superseded the paste-this-fragment instruction; KEEP the 2026-06-11 pipe-test records; add a pointer that registration now ships in tracked `user/settings.json`). Amend `docs/specs/turn-routing-enforcement/SPEC.md` §"Settings placement" (:114) to reflect tracked-file registration and close/update the deferred-unification note. Update any remaining live pointer to the retired design (`setup.ps1:253` citation — the part-2 WU-7 warn-pass rewrite left the pointer for this WU to finalize).
- **TDD:** no (docs).
- **Files to create/modify:** `docs/specs/turn-routing-enforcement/REGISTRATION.md` [VERIFY: grep -n "Paste this fragment" docs/specs/turn-routing-enforcement/REGISTRATION.md]; `docs/specs/turn-routing-enforcement/SPEC.md` [VERIFY: grep -n "Settings placement" docs/specs/turn-routing-enforcement/SPEC.md]; `setup.ps1` [VERIFY: grep -n "REGISTRATION" setup.ps1].
- **Implementation goal:** `grep -rn "paste this fragment\|each machine's live settings" docs/specs/turn-routing-enforcement/` returns no LIVE-instruction hits (only historical/superseded framing); SPEC §Settings-placement no longer claims per-machine registration is required.
- **Spec requirements:** SPEC Fix Scope 3 — "Rewrite/retire `REGISTRATION.md`'s per-machine design and amend `SPEC.md` §Settings placement … Keep the pipe-test run records."
- **Batch:** 1.

#### WU-10 — Add the blind-window annotation for hook-event consumers
- **Scope:** PHASES P4 deliverable 3. Add a machine-scoped `blind_window` annotation for the Jun 11 → fix-date range where hook-derived signals are consumed (near `incident-scan.py` / efficacy-eval's hook-event reads), so those consumers treat the window as partially-blind (not zero-friction). Annotate-only per D3 — do NOT synthesize missing events, do NOT alter signal computation.
- **TDD:** no (annotation/doc).
- **Files to create/modify:** the hook-event-consumer surface — a documented note near `user/scripts/incident-scan.py` [VERIFY: grep -rln "hook-events" user/scripts/incident-scan.py] and/or efficacy-eval's read path (choose the lightest-touch home: a comment/const the consumers can reference, or a short note in the relevant CLAUDE.md — do NOT add a code path that changes verdicts).
- **Implementation goal:** the blind window is recorded where a future incident-scan/efficacy read would encounter it; no behavior change to signal computation.
- **Spec requirements:** SPEC Fix Scope 6 + Decision D3 ("annotate only; backfilling is impossible, silent-ignore re-poisons baselines").
- **Batch:** 1 (file-disjoint from WU-9).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-9, WU-10 | Yes | None (turn-routing docs + setup.ps1 pointer vs incident-scan annotation) |

**Note on WU-9/setup.ps1 overlap:** part-2 WU-7 and part-3 WU-9 both touch `setup.ps1` — but in DIFFERENT parts (sequential), never the same batch. WU-7 rewrites the warn-pass LOGIC and leaves the REGISTRATION.md pointer; WU-9 finalizes the pointer text after the doc is retired. No concurrent-writer conflict.

**Completion (gate-owned):** none authored here.

---

### Phase: live-settings-split-brain P5 — Surface the `--live` check through a runtime vehicle (D2)

**Goal:** Make drift self-announce at runtime via the banner + probe, both reusing part-1's helper.
**Entry criteria:** Part 1's `live_settings_status(repo_root)` helper exists and is importable.
**SPEC.md references:** Fix Scope 4 (surfacing); Decision D2.

#### WU-11 — Emit a drift advisory line from `lazy-route-inject.sh`
- **Scope:** PHASES P5 deliverable 1. When `live_settings_status` reports drift, emit ONE advisory banner line naming the split + corrective (`setup repair`). Silent/no-op when clean OR when the helper is unavailable/errors (fail-open — MUST never break the banner or the prompt-submit path).
- **TDD:** where testable (the fail-open path is the load-bearing assertion).
- **Files to create/modify:** `user/hooks/lazy-route-inject.sh` [VERIFY: test -f user/hooks/lazy-route-inject.sh].
- **Implementation goal:** drift → one extra advisory line; clean → no extra output; helper missing/raising → banner still emits its normal routing content, no crash, no drift line. The check is a cheap symlink+resolve stat (the banner fires every prompt-submit in marked runs — keep it O(1)).
- **Spec requirements:** SPEC Decision D2 — "lazy-route-inject banner line … calling the same `doc-drift-lint.py --live` helper."
- **Batch:** 1.

#### WU-12 — Add a `live_settings_ok` probe field to `lazy-state.py`
- **Scope:** PHASES P5 deliverable 2. Add a `live_settings_ok` (or equivalent) field to `lazy-state.py --probe`, sourced from the SAME part-1 `live_settings_status` helper. Absent-helper / non-workstation → benign default (never a hard error / never a state-compute dependency).
- **TDD:** yes (probe field is pure enough to unit-test both ways).
- **Files to create/modify:** `user/scripts/lazy-state.py` [VERIFY: grep -n "probe" user/scripts/lazy-state.py].
- **Test files:** the existing lazy-state probe test module if present [VERIFY: grep -rln "probe" user/scripts/test_*.py], else add a focused test.
- **Test expectations:** drift → field false + detail; clean → true; helper missing → benign default, no exception. Assert the probe never fails the state computation.
- **Implementation goal:** `lazy-state.py --probe` exposes the live-settings health without ever hard-erroring or entering the state-compute critical path.
- **Spec requirements:** SPEC Decision D2 — "a `lazy-state --probe` field, both calling the same `doc-drift-lint.py --live` helper."
- **Batch:** 1 (file-disjoint from WU-11).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-11, WU-12 | Yes | None (lazy-route-inject.sh vs lazy-state.py) |

**Completion (gate-owned):** the `__mark_fixed__` gate (orchestrator validation tail) flips SPEC `**Status:**` and writes `FIXED.md` once the symptom-reproduction evidence holds — the live `~/.claude/settings.json` resolves to the reconciled SSOT carrying all 12 hooks and `doc-drift-lint.py --live` returns clean (serving-path regression evidence, per SPEC Verified Symptom). Do NOT author a checkbox for the status flip or receipt write.

---

## Blocking Issue Protocol & Completion

Single-sourced in `~/.claude/skills/_components/execution-contract.md`. No design forks are expected in parts 4–5 (all decisions resolved in the SPEC / part 2). On completion of both phases with Quality Gates green and per-phase commits on `main`, all five phases are done — the bug's fix scope is fully implemented and the orchestrator's validation tail can run `__mark_fixed__`.
