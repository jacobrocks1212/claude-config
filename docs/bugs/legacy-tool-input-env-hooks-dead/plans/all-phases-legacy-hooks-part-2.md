---
kind: implementation-plan
feature_id: legacy-tool-input-env-hooks-dead
status: Ready
created: 2026-07-12
complexity: mechanical
phases: [2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-12.
> To execute: `/execute-plan docs/bugs/legacy-tool-input-env-hooks-dead/plans/all-phases-legacy-hooks-part-2.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 2. Sibling parts:
> - part 1: `C:/Users/Jacob/source/repos/claude-config/docs/bugs/legacy-tool-input-env-hooks-dead/plans/all-phases-legacy-hooks-part-1.md`
> - part 2: `C:/Users/Jacob/source/repos/claude-config/docs/bugs/legacy-tool-input-env-hooks-dead/plans/all-phases-legacy-hooks-part-2.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution. Part 2 assumes part 1 has landed (the two hooks are rewritten and their `settings.json` matcher is widened) — its doc edits describe that end state.

# Implementation Plan — legacy-tool-input-env-hooks-dead (Phase 2: retire writes-variant + docs)  (v2)

**PHASES.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/bugs/legacy-tool-input-env-hooks-dead/PHASES.md` (legacy-tool-input-env-hooks-dead, 2 phases)

**SPEC.md files:**
- `C:/Users/Jacob/source/repos/claude-config/docs/bugs/legacy-tool-input-env-hooks-dead/SPEC.md`

**Total phases:** 2 (this part covers Phase 2)
**Plan version:** v2 (reference-based — components loaded from disk per step)

**complexity: mechanical** — every WU in this part is a mechanical file move / pure documentation edit whose correctness is enforced by the deterministic `doc-drift-lint.py` gate; no novel design decision, algorithm, or cross-boundary wiring. Qualifies for Sonnet dispatch per the Step 2.5 mechanical-vs-complex test.

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — EXECUTION MODEL, COMPONENT LOADING PROTOCOL, Component Reference Card, MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates (this is **claude-config**; the Phase-2 gate is `doc-drift-lint.py`, NOT `/msbuild`/`/mstest`), this plan's own steps and the local notes below override the contract's defaults for those rows.

## Plan-specific execution notes

- **Quality gate (Phase 2):**
  ```bash
  python user/scripts/doc-drift-lint.py --repo-root .   # exit 0 = the Hooks table matches settings.json + disk
  python user/scripts/test_hooks.py                     # re-run to confirm the retirement broke no Phase-1 test
  ```
  `doc-drift-lint.py` self-checks that THIS repo is drift-clean; exit 0 is the gate. Re-run `test_hooks.py` as a regression check (the retired file is unregistered, so no test should reference it — confirm none does after the move).
- **Preserve git history on the retirement:** use `git mv`, not delete-then-create, so the file's history follows it into `archived/`.
- **Commit after the phase completes** to `main` (this repo lands all work on `main`; no branch, no PR).
- **Inline, mechanical:** claude-config `/execute-plan` runs inline. These WUs are file moves + doc edits — no test-first cycle (the gate is the linter).

## Component Reference Card (repo override — Phase 2)

| Need | Component / command |
|------|---------------------|
| Quality gate | `python user/scripts/doc-drift-lint.py --repo-root .` + `python user/scripts/test_hooks.py` |
| Deprecation trail | `archived/CLAUDE.md` (add a retirement row) |
| Hooks table (root) | `CLAUDE.md` "## Hooks" table |
| Hooks patterns doc | `user/hooks/CLAUDE.md` |

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none) | — | — | No `**Depends on:**` block. |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | legacy-tool-input-env-hooks-dead | P2 (Batch 1) | Retire the writes-variant to archived/ | part-1 landed | Solo |
| 2    | legacy-tool-input-env-hooks-dead | P2 (Batch 2) | Reconcile the root + hooks CLAUDE.md docs | Batch 1 | Solo |

## Work Units

- [ ] WU-5 — Retire block-work-repo-git-writes.sh to archived/ (git mv + deprecation row)
- [ ] WU-6 — Reconcile the root CLAUDE.md Hooks table + user/hooks/CLAUDE.md reference

---

### Phase: legacy-tool-input-env-hooks-dead P2 — Retire `block-work-repo-git-writes.sh` and reconcile the docs

**Goal:** The unregistered, same-defect writes-variant is retired to `archived/` with a trail, and the root `CLAUDE.md` Hooks table matches reality (`doc-drift-lint.py` exit 0).
**Entry criteria:** Part 1 landed — the two hooks are rewritten and their `settings.json` matcher is `Bash|PowerShell`; the Hooks-table rows WU-6 writes must describe that end state.
**SPEC.md references:** `## Fix Scope` item 4 (retire the writes-variant with an archived trail, not a third rewrite), item 6 (update the root `CLAUDE.md` Hooks-table rows); `## Decisions` D1 (rewrite kill/push, retire the writes-variant to `archived/` with a pointer to the push hook).

#### Batch 1 — WU-5

**WU-5 — Retire block-work-repo-git-writes.sh to archived/**
- **Scope:** SPEC Fix Scope item 4 + D1. PHASES Phase 2 deliverable: `git mv` the file to `archived/` and record the retirement in `archived/CLAUDE.md`.
- **TDD:** no (file move + deprecation-row edit).
- **Files to create/modify:**
  - `git mv user/hooks/block-work-repo-git-writes.sh archived/block-work-repo-git-writes.sh` [VERIFY: `test -f user/hooks/block-work-repo-git-writes.sh`]
  - `archived/CLAUDE.md` — add a retirement row [VERIFY: `test -f archived/CLAUDE.md`]
- **Implementation goal:** Move the file with `git mv` (preserve history). Add a row/entry to `archived/CLAUDE.md` recording: the retired filename, the date (2026-07-12), the reason (shared `$TOOL_INPUT_command` dead-code defect + unregistered-by-documented-decision, SPEC D1), and a pointer to `user/hooks/block-work-repo-git-push.sh` as the live successor (the push hook now covers the work-repo push case; the commit-blocking half was never registered). Match the existing `archived/CLAUDE.md` row format (read the file first).
- **Spec requirements:** Fix Scope item 4 ("Retire `block-work-repo-git-writes.sh` … with an archived/ trail rather than a third rewrite — see D1"); D1 ("retire `block-work-repo-git-writes.sh` to `archived/` with a pointer to the push hook").
- **Batch:** 1

#### Batch 2 — WU-6

**WU-6 — Reconcile the root CLAUDE.md Hooks table + user/hooks/CLAUDE.md reference**
- **Scope:** SPEC Fix Scope item 6 + item 2. PHASES Phase 2 deliverables: update the three Hooks-table rows and any `user/hooks/CLAUDE.md` reference to the retired file, so `doc-drift-lint.py` passes.
- **TDD:** no (documentation edits; the gate is `doc-drift-lint.py`).
- **Files to create/modify:**
  - `CLAUDE.md` (root) — the "## Hooks" table rows for the three hooks [VERIFY: `grep -n "block-work-repo-git-writes.sh" CLAUDE.md`]
  - `user/hooks/CLAUDE.md` — any reference to the retired file [VERIFY: `grep -n "block-work-repo-git-writes" user/hooks/CLAUDE.md`]
- **Implementation goal:**
  - Root `CLAUDE.md` Hooks table: (a) update the `block-work-repo-git-push.sh` row to reflect the stdin-JSON rewrite, deny-via-JSON fail-OPEN contract, and the widened `PreToolUse (Bash, PowerShell)` trigger; (b) update the `block-terminal-kill.sh` row likewise (stdin-JSON, deny-via-JSON, PowerShell-matched, Stop-Process retained); (c) change the `block-work-repo-git-writes.sh` row from "NOT registered (script exists in `user/hooks/`…)" to "**retired to `archived/`**" — the "script exists in `user/hooks/`" claim would itself be drift once WU-5 moves the file. `doc-drift-lint.py` cross-checks the Hooks table ↔ `settings.json` (incl. asserting the NOT-registered rows stay unregistered), so keep the two revived rows registered-and-widened and the retired row out of the `user/hooks/` on-disk set.
  - `user/hooks/CLAUDE.md`: if it names `block-work-repo-git-writes.sh` as a live sibling (the "Write-time complements" / deny-signatures prose), reconcile the reference to point at the archived location or drop the stale in-directory mention. Grep-verify first; edit only if a live reference exists.
- **Spec requirements:** Fix Scope item 6 ("Update the root `CLAUDE.md` Hooks table rows (:275-277) to reflect the rewrite and the writes-variant retirement"); item 2 (the rows should describe the deny-via-JSON fail-OPEN contract).
- **Batch:** 2

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-5      | Solo      | archived/CLAUDE.md + the moved file |
| 2     | WU-6      | Solo      | CLAUDE.md (root) + user/hooks/CLAUDE.md |

> WU-5 and WU-6 are placed in separate sequential batches because WU-6's Hooks-table row describes the retirement WU-5 performs (logical dependency) and both must be coherent before the `doc-drift-lint.py` gate runs. They touch disjoint files, but sequencing keeps the linter's cross-check meaningful.

**Phase-2 completion gate:** `python user/scripts/doc-drift-lint.py --repo-root .` exit 0 AND `python user/scripts/test_hooks.py` still GREEN, then set this part's frontmatter `status: Complete`, tick both `- [ ] WU-N` rows, and — because this is the LAST phase — set the top-level PHASES `**Status:**` to `In-progress` (implementation done, validation pending; the `__mark_fixed__` gate owns the flip to `Fixed` + the `FIXED.md` receipt). Commit to `main`.

**Runtime Verification** *(from PHASES.md — ticked by the doc-drift linter; no app runtime):*
- [ ] <!-- verification-only --> `doc-drift-lint.py --repo-root .` exit 0 after the table edits and the file move (the Hooks-table ↔ settings.json cross-check passes).

---

## Red-flag / gate-owned-row audit (from Step 1c.5)

- No plan WU re-runs a live MCP-validation gate (no MCP runtime in this repo). Compliant.
- No gate-owned rows planned as WUs. The PHASES `**Completion (gate-owned):**` note correctly reserves the SPEC/PHASES `**Status:** Fixed` flip + `FIXED.md` write + archive for the orchestrator's `__mark_fixed__` gate; this plan flips only the plan-part `status:` and (last-phase) the top-level PHASES `**Status:**` to `In-progress`. Compliant.
