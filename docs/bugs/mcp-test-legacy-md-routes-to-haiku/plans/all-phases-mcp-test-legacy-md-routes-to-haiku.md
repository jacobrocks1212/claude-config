---
kind: implementation-plan
feature_id: mcp-test-legacy-md-routes-to-haiku
status: Complete
created: 2026-06-19
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` (via `/plan-bug`) on 2026-06-19.
> To execute: `/execute-plan docs/bugs/mcp-test-legacy-md-routes-to-haiku/plans/all-phases-mcp-test-legacy-md-routes-to-haiku.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — mcp-test-legacy-md-routes-to-haiku  (v2)

**PHASES.md files:**
- `docs/bugs/mcp-test-legacy-md-routes-to-haiku/PHASES.md` (mcp-test-legacy-md-routes-to-haiku, 1 phase)

**SPEC.md files:**
- `docs/bugs/mcp-test-legacy-md-routes-to-haiku/SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **DISPATCH-LIMITED EXECUTION NOTE.** When this plan is executed by a `/lazy-bug-batch` cycle subagent that lacks the `Agent` tool, perform ALL work units INLINE with Read/Edit/Write (test-first: write the failing test, confirm it fails, then implement) — the inline-override in the cycle dispatch supersedes the orchestrator+subagent model above. Outside that context (direct `/execute-plan` with the Agent tool), follow the orchestrator+subagent model as written.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. EXCEPTION: a dispatch-limited cycle subagent (no Agent tool) performs the work INLINE per the dispatch note above.
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents
3. Every TDD work unit goes through the test-first pipeline — write the FAILING test, confirm it fails against current code, then implement until green.
4. PHASES.md is updated AFTER EACH batch completes (not deferred)
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing
6. Mistakes are fixed immediately before launching the next batch
7. After all batches in a phase finish, integration verification confirms all changes work together
8. Relevant CLAUDE.md files are created/updated after the phase if changes warrant it (here: `user/scripts/CLAUDE.md` → mcp-test model-tier routing section — see WU-3)
9. The completed phase is committed and pushed before stopping
10. This plan is self-contained — follow it exactly as written without relying on external context
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | mcp-test-legacy-md-routes-to-haiku | P1 | Wire `route_mcp_test_tier` into `emit_cycle_prompt` | — | Solo |

## Work Units

- [x] WU-1 — Test-first: legacy-`.md`→sonnet + ready-YAML→haiku emit fixtures
- [x] WU-2 — Wire `route_mcp_test_tier` into `emit_cycle_prompt` (conservative escalation)
- [x] WU-3 — Doc: update `user/scripts/CLAUDE.md` mcp-test model-tier routing section

---

### Phase: mcp-test-legacy-md-routes-to-haiku P1 — Wire `route_mcp_test_tier` into `emit_cycle_prompt`

**Goal:** The autonomous mcp-test cycle-model emit consults the script-derived tier signal so an unconverted legacy `.md` scenario routes to Sonnet at dispatch (not the hardcoded haiku that BLOCKs).
**Entry criteria:** None (first and only phase).
**SPEC.md references:** `## Evidence Collected` → "Hardcoded tier (root cause)" (`lazy_core.py:4761`) and "The routing signal exists but is unwired into emit" (`surface_resolver.py:379`); `## Affected Area` table; `## Open Questions` → resolved toward conservative escalation (option b) in PHASES.md `## Scenario-resolution-at-emit-time decision`.

#### WU-1 — Test-first: legacy-`.md`→sonnet + ready-YAML→haiku emit fixtures

- **Scope:** Authors the failing regression first. Covers PHASES.md Phase-1 deliverable "Tests (test-first)".
- **TDD:** yes.
- **Test files:** `user/scripts/test_lazy_core.py`
- **Test expectations:**
  1. New fixture `test_emit_cycle_prompt_mcp_test_legacy_md_escalates_sonnet`: build a temp spec/bug dir containing a legacy mcp-test scenario `.md` (e.g. under `mcp-tests/scenario-x.md`) with NO sibling/converted `corpus/live/*.yaml`; call `lazy_core.emit_cycle_prompt(repo, state, pipeline="feature", cloud=False, repeat_count=1, template_dir=_REAL_TEMPLATE_DIR)` with `sub_skill="/mcp-test"` and `spec_path` pointing at that dir; assert `r["ok"] is True` and `r["model"] == "sonnet"`. This MUST fail against current code (`model` hardcoded to haiku at `lazy_core.py:4761`) — confirm the failure before WU-2.
  2. New fixture `test_emit_cycle_prompt_mcp_test_ready_yaml_stays_haiku`: build a temp dir whose only mcp-test scenario(s) are ready converted YAML (e.g. `corpus/live/scenario-x.yaml` present); assert `r["model"] == "haiku"`. Represents the genuine happy path.
  3. Reshape existing `test_emit_cycle_prompt_mcp_test_cycle_model_haiku` (`test_lazy_core.py:7563`): its current fixture is a bare empty `spec_dir` (no scenarios) — under conservative escalation that is now correctly Sonnet (no ready YAML). Give it a ready converted-YAML scenario so the haiku assertion stays valid and represents the happy path (or fold it into fixture #2 if cleaner — keep coverage of `rc=1` and `rc=None`).
  4. Register the two new fixtures in the `test_lazy_core.py` runner list (the list near lines ~14375–14377 that names `test_emit_cycle_prompt_mcp_test_cycle_model_haiku` and `..._loop_cycle_model_sonnet`). `test_emit_cycle_prompt_mcp_test_loop_cycle_model_sonnet` stays green (loop-block escalation is orthogonal — `repeat_count>=2` still forces sonnet).
- **Implementation goal (test-only WU):** the two new tests are present and the reshaped one compiles; running the suite shows the legacy-`.md`→sonnet test RED before WU-2.
- **Spec requirements:** SPEC `## Affected Area` row "Tests": "New fixture: unconverted-`.md` mcp-test cycle → sonnet."
- **Anchors:** existing `emit_cycle_prompt` [VERIFY: grep -n "def emit_cycle_prompt" user/scripts/lazy_core.py]; existing haiku test [VERIFY: grep -n "def test_emit_cycle_prompt_mcp_test_cycle_model_haiku" user/scripts/test_lazy_core.py]; existing loop-sonnet test [VERIFY: grep -n "def test_emit_cycle_prompt_mcp_test_loop_cycle_model_sonnet" user/scripts/test_lazy_core.py].
- **Batch:** 1.

#### WU-2 — Wire `route_mcp_test_tier` into `emit_cycle_prompt` (conservative escalation)

- **Scope:** The core fix. Covers PHASES.md Phase-1 deliverables "In `lazy_core.py` `emit_cycle_prompt`, import/call `route_mcp_test_tier`…" and the comment-rewrite deliverable.
- **TDD:** yes (makes WU-1's RED test GREEN).
- **Files to create/modify:** `user/scripts/lazy_core.py` (`emit_cycle_prompt`, ~4748–4761).
- **Implementation goal:**
  - Replace the literal `model = "haiku" if norm_sub_skill == "mcp-test" else "opus"` with: `opus` base for non-mcp-test; for `norm_sub_skill == "mcp-test"`, derive the tier via `surface_resolver.route_mcp_test_tier` using **option-b conservative escalation** (per PHASES.md decision):
    - Resolve the item's dir from `state.get("spec_path")` (the spec/bug dir).
    - Enumerate candidate scenarios: legacy `mcp-tests/*.md` + converted `corpus/live/*.yaml` under that dir (use the same scenario locations the mcp-test SKILL.md / `surface_resolver` already recognize — grep the repo to confirm the canonical dirs rather than hardcoding a guess).
    - `model = "haiku"` ONLY IF at least one candidate resolves AND **every** candidate resolves to `"haiku"` via `route_mcp_test_tier(scenario_path)` (ready converted YAML, no adverse prior verdict); otherwise `model = "sonnet"`.
    - **Fail-safe:** if zero candidate scenarios resolve, or enumeration raises, set `model = "sonnet"` (matches the router's "unknown → Sonnet" bias) — NEVER silently fall back to haiku.
  - Import `route_mcp_test_tier` from `surface_resolver` (match the existing import style in `lazy_core.py` — surface_resolver is a sibling module in `user/scripts/`; confirm whether `lazy_core` already imports from it and follow that pattern; if a circular-import risk exists, import lazily inside the branch).
  - Rewrite the explanatory comment block (~4749–4761) to document the tier-routed selection + conservative-escalation rationale, citing bug slug `mcp-test-legacy-md-routes-to-haiku` and the SPEC. Remove the now-false "haiku suffices / single 'sonnet' literal" claim.
  - Preserve the downstream loop-block downgrade (`repeat_count>=2` → sonnet) and `/execute-plan` complexity tiering EXACTLY — both only ever downgrade toward sonnet, so they still compose (sonnet ∧ sonnet = sonnet; haiku-base + loop → sonnet).
- **Spec requirements:** SPEC `## Proven Findings` (fix must give `emit_cycle_prompt` enough signal to escalate BEFORE dispatch); `## Affected Area` rows "Cycle-model emit" + "Tier router".
- **Anchors:** `route_mcp_test_tier` [VERIFY: grep -n "def route_mcp_test_tier" user/scripts/surface_resolver.py]; hardcoded haiku line [VERIFY: grep -n 'model = "haiku" if norm_sub_skill' user/scripts/lazy_core.py].
- **Batch:** 1 (sequential after WU-1 in the same context: WU-1 RED → WU-2 GREEN).

#### WU-3 — Doc: update `user/scripts/CLAUDE.md` mcp-test model-tier routing section

- **Scope:** PHASES.md Phase-1 deliverable "Update `user/scripts/CLAUDE.md` → `## mcp-test model-tier routing`". Pure documentation edit (orchestrator may edit CLAUDE.md directly per the EXECUTION MODEL exception).
- **TDD:** no (docs).
- **Files to modify:** `user/scripts/CLAUDE.md` → `## mcp-test model-tier routing (harness-hardening-retro-fixes Phase 4)` section.
- **Implementation goal:** Amend the sentence stating the helper is consulted ONLY by the interactive `mcp-test` SKILL.md prose: record that `emit_cycle_prompt` now ALSO consults `route_mcp_test_tier` on the autonomous cycle-model path (option-b conservative escalation: haiku only when every candidate scenario is ready converted YAML, else sonnet), closing the SPEC's "wired into zero autonomous paths" gap. Keep the existing test-reference line; add `test_lazy_core.py` emit fixtures to the cited tests.
- **Spec requirements:** SPEC `## Related Documentation` (Phase-4 intent: tier routing "by default — not by a per-run orchestrator override"); the fix realizes that intent on the batch path.
- **Anchors:** section header [VERIFY: grep -n "mcp-test model-tier routing" user/scripts/CLAUDE.md].
- **Batch:** 2 (after WU-1/WU-2 land and gates are green).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2 | No (test-first sequential) | None (test file then source file) |
| 2     | WU-3      | Solo      | N/A             |

---

## Execution Protocol

This protocol governs the autonomous execution of the phase. Follow it exactly.

### Phase Selection Loop

Only one phase exists. Execute it, then run Post-Phase Steps and Completion.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (task tool loading via ToolSearch, task creation for WU-1..WU-3, update protocol).

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)

Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: this plan, the SPEC.md, the PHASES.md, and the three target source/test/doc files (`lazy_core.py` around `emit_cycle_prompt`, `surface_resolver.py` around `route_mcp_test_tier`, `test_lazy_core.py` around the emit fixtures).

#### Step B.1: Implement (test-first)

Read: `~/.claude/skills/_components/tdd-protocol.md`, `~/.claude/skills/_components/subagent-launch.md`, `~/.claude/skills/_components/tdd-test-agent.md`, `~/.claude/skills/_components/implementation-agent.md`.

- Batch 1: WU-1 (write failing tests, RUN them, confirm the legacy-`.md`→sonnet fixture FAILS against current code), then WU-2 (implement until WU-1 tests pass).
- Dispatch via Agent (model: sonnet) when the Agent tool is available; otherwise perform INLINE per the dispatch note at the top of this plan.

#### Step B.2: Review Batch Output (MANDATORY GATE)

Read `~/.claude/skills/_components/subagent-review.md` and follow its protocol. Confirm: the escalation is conservative (haiku only when ALL candidates are ready YAML), the fail-safe escalates to sonnet on zero-candidates/error, and the loop-block + execute-plan tiering are untouched.

#### Step B.3: Update PHASES.md (MANDATORY)

Read `~/.claude/skills/_components/phases-update.md`. Check off the Phase-1 deliverables and add an Implementation Notes block (date, what landed, files, the import seam chosen, the canonical scenario dirs enumerated). Tick the `- [ ] WU-N` rows in this plan's `## Work Units` list as each WU lands. Do NOT tick the PHASES.md Runtime-Verification row (`<!-- verification-only -->`) — that is the Step-9 gate's to tick. Do NOT flip any top-level `**Status:**` (gate-owned).

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Run the full state-machine smoke + unit suite (per `user/scripts/CLAUDE.md` Coupling Rule — a `lazy_core.py` change touches BOTH state machines):

```
python user/scripts/lazy-state.py --test
python user/scripts/bug-state.py --test
python user/scripts/test_lazy_core.py
python user/scripts/test_surface_resolver.py
```

All must pass 100%. If `--test` baselines (`tests/baselines/lazy-state-test-baseline.txt`, `bug-state-test-baseline.txt`) drift, confirm the drift is legitimate and regenerate ONLY via the `_normalize_smoke_output` helper — never by hand. Also run `python ~/.claude/scripts/lint-skills.py` if any skill/component was touched (none expected here).

#### Step B.5: Commit Batch

Read `.claude/skill-config/commit-policy.md` and follow it: Conventional Commits (`fix(...)` / `test(...)` / `docs(...)` scope `lazy-core` or `mcp-test-tier`), **NO AI attribution footer**, push allowed. Use the ATOMIC GATE+COMMIT pattern — chain the gate with the commit+push so an interrupted turn still leaves committed, pushed state:

```
python user/scripts/lazy-state.py --test && python user/scripts/bug-state.py --test && python user/scripts/test_lazy_core.py && git add -A && git commit -m "fix(lazy-core): route legacy .md mcp-test cycles to sonnet at emit time" && git push
```

### Post-Phase Steps

#### Update CLAUDE.md Files (MANDATORY)

WU-3 already updates `user/scripts/CLAUDE.md`. Read `~/.claude/skills/_components/claude-md-review.md` and confirm no other CLAUDE.md (repo root, `user/scripts/`) needs a follow-up note.

#### Commit and Push Post-Phase Changes

Per commit-policy.md. Ensure `git status --short` is EMPTY and the branch (`main`) is pushed before stopping.

---

## Blocking Issue Protocol

If a blocking issue is encountered:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed work** not yet committed.
3. **Write `BLOCKED.md`** in the bug dir per `~/.claude/skills/_components/sentinel-frontmatter.md` and print a blocking-issue report (completed WUs, blocked WU, reason, recovery suggestion, remaining WUs).
4. **Do not work around the blocker.**

Blocking issues include: a circular-import wall preventing `route_mcp_test_tier` from being called in `emit_cycle_prompt` that cannot be resolved with a lazy/in-function import; a `--test` baseline drift that cannot be explained as a legitimate behavior change; any decision requiring a product-class choice not resolved in PHASES.md.

---

## Completion

When the phase is complete:

1. **Run the full quality-gate suite one final time** (the four `--test`/unit commands above, all green).
2. **Confirm:** every `- [ ] WU-N` in `## Work Units` is checked; PHASES.md Phase-1 deliverables checked; `git status --short` empty; `main` pushed.
3. **Print a completion report:** features/bug implemented, WUs completed, commit log (commit | WU | title), final QG status all green, Implementation Notes summary (import seam chosen, candidate-scenario dirs enumerated, conservative-escalation behavior confirmed).
4. Leave the top-level PHASES.md `**Status:**` for the orchestrator/state-machine to advance toward the Step-9 `/mcp-test` validation tail — do NOT flip it to Fixed (gate-owned).
