---
kind: implementation-plan
feature_id: build-queue-outcome-opacity-and-inspect-deny
status: Ready
created: 2026-07-03
complexity: complex
phases: [1, 2, 3, 4]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-03.
> To execute: `/execute-plan {this-file}`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — build-queue-outcome-opacity-and-inspect-deny (v2)

**PHASES.md files:**
- `docs/bugs/build-queue-outcome-opacity-and-inspect-deny/PHASES.md` (4 phases)

**SPEC.md files:**
- `docs/bugs/build-queue-outcome-opacity-and-inspect-deny/SPEC.md`

**Total phases:** 4
**Plan version:** v2 (reference-based — components loaded from disk per step)
**Repo:** `claude-config` (harness repo — gates are Python/Pester/skill-lint, NOT `/msbuild`/`/mstest`)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths, **this plan's own per-phase steps and the "Plan-specific execution notes" below override the contract's defaults for those rows** (this is the harness-config repo — its gates are `python user/scripts/test_hooks.py`, `Invoke-Pester`, and `python user/scripts/lint-skills.py` / `project-skills.py`, not the Cognito `/msbuild`/`/mstest`/`/nxbuild`/`/nxtest` build skills). Everything the contract specifies that this plan does not override still applies.

## References — Upstream Artifacts

This bug has no `**Depends on:**` hard-dep block (bug SPEC uses `**Related:**` only). No completed hard upstream dependencies with plans to inherit.

**(none — this plan has no completed hard upstream dependencies)**

For context, the SPEC's `**Related:**` cross-links (read only if a WU's behavior is unclear): `docs/bugs/build-queue-enforce-cd-prefix-bypass/SPEC.md` (line 90 predicted this false-positive), `user/hooks/CLAUDE.md` (fail-OPEN + deny-via-JSON contract — any hook fix MUST preserve both).

---

## Plan-specific execution notes (override the contract defaults)

**Quality gates for this repo (there is no `/msbuild` etc. here):**
- **Phase 1 (bash hook + Python tests):** `python user/scripts/test_hooks.py` must exit 0 with all `test_bqe_*` and `test_longbuild_guard_*` cases passing. Run from repo root.
- **Phase 2 (PowerShell + Pester):** `Invoke-Pester repos/cognito-forms/.claude/scripts/test-filtered.Tests.ps1` must pass. Use the PowerShell tool: `powershell.exe -Command "Invoke-Pester '<abs-path>' -Output Detailed"`.
- **Phase 3 (PowerShell + Pester):** `Invoke-Pester user/scripts/build-queue-hygiene.Tests.ps1` must pass (via `powershell.exe -Command`).
- **Phase 4 (skill docs):** `python user/scripts/lint-skills.py` exits clean, then `python user/scripts/project-skills.py` regenerates projections without error. Docs-consistency read-through of the four SKILL.md diffs.

**Platform:** Windows. Bash tool = real bash (for `test_hooks.py`, git). PowerShell tool = for Pester (`powershell.exe -Command "..."`). Never mix. Never `/dev/null` — use `$null`/`NUL`.

**No push:** claude-config commits are allowed locally; do NOT push (the executing session commits per-phase per the contract; a human runs `/push`). If `git config user.email` returns `jacob@cognitoforms.com` the push hook blocks anyway.

**Symlink write-through caveat:** the files edited here are the *real* targets inside `claude-config/` (e.g. `user/hooks/build-queue-enforce.sh`). Edit those paths directly — they ARE the source. Do not attempt to edit via the `~/.claude/` symlink (the Edit tool refuses symlink write-through).

**Do NOT expand scope:** nx `client-build-filtered.ps1`/`client-test-filtered.ps1` exit-code propagation is an **accepted out-of-scope limitation** (PHASES Phase 3 note). Do not touch it. Do NOT modify `repos/cognito-forms/.claude/skills/local-ui-tests/SKILL.md` — its line ~68 deliberately tells agents NOT to trust the queue exit code / results JSON for the Selenium `Cognito.Forms.UnitTests` project; it is a separate skill outside this plan's 4-skill scope and its guidance is intentional. Phase 4 touches ONLY msbuild/mstest/nxbuild/nxtest.

---

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | bug | P1 | Hook — invoke-vs-reference discrimination + tests | — | Solo (independent; may run concurrently with P2 if desired) |
| 2 | bug | P2 | Zero-match fidelity — WU-2 (test-filtered signal + Pester) | — | Solo |
| 3 | bug | P2 | Zero-match fidelity — WU-3 (runner fidelity mapping) | WU-2 | Solo (shares `build-queue-runner.ps1` with WU-5) |
| 4 | bug | P3 | Banner helper (WU-4) + counts capture (WU-5) | WU-3 (WU-5 only) | **Yes** (WU-4 = hygiene.ps1, WU-5 = runner.ps1 — disjoint files) |
| 5 | bug | P3 | Wrapper: print banner + fix Step-5 clobber (WU-6) | WU-4, WU-5 | Solo |
| 6 | bug | P4 | Skills — trust the banner (WU-7) | WU-6 | Solo |

**Phase order:** P1 is independent of P2–P4. P2 → P3 → P4 is a hard linear chain (P3's banner surfaces P2's `no-tests-matched` fidelity; P4's docs describe P3's banner). The state machine drives phases in order; the executor MAY interleave P1 with P2 since they touch disjoint files (bash/Python vs PowerShell).

---

## Work Units (progress checklist)

- [x] WU-1 — Anchor the bqe deny surface to invoke-vs-reference + regression tests
- [x] WU-2 — test-filtered.ps1 zero-match detection (pure decision helper) + Pester
- [x] WU-3 — Runner `result_fidelity` mapping for `no-tests-matched` + schema doc
- [x] WU-4 — Pure banner-formatting helper in build-queue-hygiene.ps1 + Pester
- [x] WU-5 — Runner: capture test-op Passed/Failed/Total counts into hygiene JSON
- [x] WU-6 — Wrapper: compose+print banner; fix Step-5 hygiene clobber
- [x] WU-7 — Four SKILL.md files: trust-the-banner guidance

---

## Phase: bug P1 — Hook: invoke-vs-reference discrimination + regression tests

**Goal:** Stop `build-queue-enforce.sh` denying read-only inspection commands that merely *reference* a build token; deny only when a heavy build is the *invoked* command.
**Entry criteria:** None. Independent of P2–P4.
**SPEC.md references:** Theory 1 (PRIMARY — hook thread); Fix Direction §1; Affected Area rows "Build-queue enforcement hook" + "Long-build ownership hook" + "Hook tests".

### WU-1 — Anchor the bqe deny surface to invoke-vs-reference + regression tests

**Scope (copied from PHASES deliverables):**
- Rework the deny surface in `user/hooks/build-queue-enforce.sh` so each build matcher only denies when the build token *begins a command segment* — string start or immediately after a shell separator (`&& || | ; ( {` or newline), with an optional `NAME=value` env-assignment prefix — mirroring `long-build-ownership-guard.sh`'s `_CMD_START`/`_ENV_PREFIX` — OR appears as the `-File <...-filtered.ps1>` argument to a `powershell(.exe)` invocation.
- Ensure a build token appearing only as an argument to a read verb (`cat`, `less`, `head`, `tail`, `grep`, `rg`, `find`, `ls`, `Get-Content`, `Select-String`, `type`), inside a quoted `results/`/`logs/` path, or inside `git diff`/`echo` text does NOT trigger a deny.
- Preserve existing behavior: `cd "..." && dotnet build` still DENIES; `BUILD_QUEUE_BYPASS=1` and `build-queue.ps1` wrapper exemptions intact; Cognito-remote scope gate intact; fail-OPEN via JSON `permissionDecision: deny` (never a non-zero exit).
- Audit `long-build-ownership-guard.sh`'s `_LONG_BUILD_RE`; change ONLY if a reference-only deny is found (expected: no change).
- Tests appended to the `test_bqe_*` and `test_longbuild_guard_*` blocks in `user/scripts/test_hooks.py`, registered in the test list.

**TDD:** yes.

**Files to create/modify:**
- `user/hooks/build-queue-enforce.sh` — anchor `_DOTNET_BUILD_RE` [VERIFY: grep -n "_DOTNET_BUILD_RE" user/hooks/build-queue-enforce.sh] (line 96), `_DOTNET_TEST_RE` (line 97), `_NX_BUILD_TEST_RE` [VERIFY: grep -n "_NX_BUILD_TEST_RE" user/hooks/build-queue-enforce.sh] (lines 101-108), and `_FILTERED_SCRIPT_RE` [VERIFY: grep -n "_FILTERED_SCRIPT_RE" user/hooks/build-queue-enforce.sh] (lines 110-113) to command-segment-start, plus the `-File <...-filtered.ps1>` powershell-argument deny case.
- `user/hooks/long-build-ownership-guard.sh` — audited only; `_CMD_START` [VERIFY: grep -n "_CMD_START" user/hooks/long-build-ownership-guard.sh] (line 94) + `_ENV_PREFIX` [VERIFY: grep -n "_ENV_PREFIX" user/hooks/long-build-ownership-guard.sh] (line 91) already anchor `_LONG_BUILD_RE` (lines 95-101). Change only if a reference-only deny is actually found.
- `user/scripts/test_hooks.py` — new ALLOW/DENY cases in `test_bqe_*` and `test_longbuild_guard_*`, registered in `_ALL_TESTS`.

**Test files:** `user/scripts/test_hooks.py` (append; do not create).

**Test expectations (write FIRST — they must FAIL against the current unanchored regexes):**
Reuse the existing helpers — verified signatures:
- `_init_cognito_worktree(parent) -> repo_path` [VERIFY: grep -n "_init_cognito_worktree" user/scripts/test_hooks.py] (creates a temp git repo whose `origin` is `cognitoforms/cognito` so the scope gate treats it as Cognito).
- `_bqe_payload(command, cwd) -> json_str` [VERIFY: grep -n "_bqe_payload" user/scripts/test_hooks.py].
- `_run_bash(script, stdin_text, env) -> CompletedProcess` [VERIFY: grep -n "def _run_bash" user/scripts/test_hooks.py]; use `_BQE_HOOK_SH` [VERIFY: grep -n "_BQE_HOOK_SH" user/scripts/test_hooks.py] and `_base_env(state_dir)` [VERIFY: grep -n "def _base_env" user/scripts/test_hooks.py].
- `_containment_decision(result) -> "deny" | None` [VERIFY: grep -n "def _containment_decision" user/scripts/test_hooks.py]. **ALLOW = `_containment_decision(result) != "deny"`; DENY = `== "deny"`.** Every test also asserts `result.returncode == 0` (deny is JSON, never a non-zero exit).
- For the long-build regression cases: `_run_longbuild_guard(stdin_text, state_dir) -> CompletedProcess` [VERIFY: grep -n "def _run_longbuild_guard" user/scripts/test_hooks.py] against `_LONGBUILD_GUARD_SH` [VERIFY: grep -n "_LONGBUILD_GUARD_SH" user/scripts/test_hooks.py].

Model the new functions on the two existing verbatim examples in the file — `test_bqe_denies_cd_prefixed_dotnet_build` (DENY) and `test_bqe_allows_bare_restore` (ALLOW). New cases:

ALLOW (against `build-queue-enforce.sh`, in a Cognito worktree):
- `cat "$HOME/.claude/state/build-queue/results/614.json"`
- `grep -n "stale|exit 4" test-filtered.ps1`
- `tail logs/500.build.err.log`
- `find . -name build-filtered.ps1`
- `cat build-filtered.ps1 | head -100`
- `git diff user/settings.json`

DENY (against `build-queue-enforce.sh`):
- bare `dotnet build`
- `cd "<worktree>" && dotnet test`
- `dotnet restore && dotnet build` (restore is suppressed; the trailing `dotnet build` still leads a segment → deny)
- bare `powershell -File <...>/test-filtered.ps1` invocation
- direct `./build-filtered.ps1`

Regression (against `long-build-ownership-guard.sh` via `_run_longbuild_guard`) — lock in that it already discriminates: the same reference-only ALLOW cases (e.g. `cat` / `grep` / `find` naming a `tauri build` / `cargo build --release` / `npm run build` token) must ALLOW.

**Implementation goal:**
1. Introduce a command-segment-start anchor mirroring `long-build-ownership-guard.sh` — copy the `_ENV_PREFIX` (line 91) and `_CMD_START` (line 94) idiom: `_CMD_START = r"(?:^|[\n;&|({])\s*" + _ENV_PREFIX`. Apply it as a prefix to `_DOTNET_BUILD_RE`, `_DOTNET_TEST_RE`, the alternatives inside `_NX_BUILD_TEST_RE`, and the direct-invocation form of `_FILTERED_SCRIPT_RE`. For direct `*-filtered.ps1` invocation also allow an optional path prefix (`\.?[\\/]` / `\.\\` / a drive-absolute path) between the segment start and the script name so `./build-filtered.ps1` and `.\build-filtered.ps1` still DENY.
2. `_FILTERED_SCRIPT_RE` needs **two** deny conditions: (a) segment-leading direct invocation (per above), OR (b) the script appears as a `-File <...-filtered.ps1>` argument to a `powershell` / `powershell.exe` / `pwsh` invocation (match `powershell(?:\.exe)?\b[^\n;&|]*-File\s+\S*(?:build|test|client-build|client-test)-filtered\.ps1`). A bare `cat build-filtered.ps1` matches neither → ALLOW.
3. **CRITICAL — scan target:** the deny regexes run against `scan = _suppress_safe(command)` [VERIFY: grep -n "_suppress_safe" user/hooks/build-queue-enforce.sh] (invoked at line ~290), NOT the raw command — `_suppress_safe` blanks `dotnet restore/--version/-v/ef/msbuild` and `nx lint/typecheck/format` before the scan. Apply the anchoring to the matchers that consume `scan`; verify the `dotnet restore && dotnet build` DENY case still works (restore is blanked → the remaining `&& dotnet build` still leads a segment). Do NOT change `_suppress_safe` itself.
4. Do NOT touch: `_BYPASS_RE` (line 76, already `^`-anchored), `_WRAPPER_RE` (line 81), the `_deny()` JSON emitter (lines 148-156), the scope-gate helper `_is_cognito_worktree` (lines 159-172) or its call site (lines 277-279), or the fail-OPEN `try` wrapper (line 308).
5. Audit `long-build-ownership-guard.sh`: its `_LONG_BUILD_RE` is already `_CMD_START`-anchored and its header comment already documents that `echo tauri build` does not match — expected **no code change**. If the regression tests pass unchanged, record "audited, no change needed" in the phase notes.
6. Register every new test function in `_ALL_TESTS` [VERIFY: grep -n "_ALL_TESTS" user/scripts/test_hooks.py] (the registration list near lines 5247-5266 — the identifier is `_ALL_TESTS`, NOT `_TESTS`).

**Spec requirements:** SPEC Fix Direction §1 (invoke-vs-reference over read-verb whitelist), §3 (regression coverage). Preserve fail-OPEN + deny-via-JSON (`user/hooks/CLAUDE.md` contract). PHASES Phase 1 all deliverables.

**Minimum Verifiable Behavior:** `python user/scripts/test_hooks.py` exits 0 with all `test_bqe_*` and `test_longbuild_guard_*` cases passing, including every new ALLOW/DENY case above.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | None |

---

## Phase: bug P2 — Zero-match fidelity detection

**Goal:** Make a zero-match test filter (a filter that matches no tests) exit as a distinct, non-`verified` fidelity instead of silently reporting `exit 0` indistinguishable from a genuine all-pass run.
**Entry criteria:** None. Independent of P1.
**SPEC.md references:** Theory 2 (fidelity thread); Fix Direction §2 "close the data gap"; Affected Area row "Queue runner".

### WU-2 — test-filtered.ps1 zero-match detection (pure decision helper) + Pester

**Scope (copied from PHASES deliverables):**
- `test-filtered.ps1`: when the modern SDK summary is seen (`$summarySeen = $true`) AND `$summary.total -eq 0`, signal this distinctly instead of falling through to `exit $dotnetExit` (currently 0). Keep exit 1 (not-in-git-repo), exit 3 (genuine zero-output, no summary seen), and exit 4 (stale/missing DLL) semantics fully intact and non-overlapping.
- Pester cases: a filter matching zero tests → the new distinct signal; a real all-pass run → unaffected (`verified` downstream); a genuine zero-output run (no summary line) → still exits 3.

**TDD:** yes.

**Files to create/modify:**
- `repos/cognito-forms/.claude/scripts/test-filtered.ps1` — add the zero-match branch. Anchors verified: `$summarySeen` [VERIFY: grep -n "summarySeen" repos/cognito-forms/.claude/scripts/test-filtered.ps1] (init `$false` line 129, set `$true` line 170), `Test-SummaryLine` [VERIFY: grep -n "function Test-SummaryLine" repos/cognito-forms/.claude/scripts/test-filtered.ps1] returns `@{ isSummary; failed; passed; total }`, `$summary.total` guarded at line 176, the `Results: Passed=.. Failed=.. Total=..` line at line 177, the zero-output check + `exit 3` at lines 185-188, fall-through `exit $dotnetExit` at line 191. Existing `exit 1` (line 114), `exit 4` (line 121).
- `repos/cognito-forms/.claude/scripts/test-filtered.Tests.ps1` — new Pester cases.

**Test files:** `repos/cognito-forms/.claude/scripts/test-filtered.Tests.ps1` (append; do not create).

**Test expectations (write FIRST):**
> **Drift correction (from touchpoint audit — MECHANICAL, baked in):** the existing `test-filtered.Tests.ps1` has **no exit-code / `Invoke-Main` harness** — it only tests the pure functions (`Test-SummaryLine`, `Test-StaleTestDll`, `Resolve-TestDllPath`) by dot-sourcing the script (the `if ($MyInvocation.InvocationName -ne '.')` guard at line ~194 prevents `Invoke-Main` from running on dot-source). Testing the full exit path via a spawned process would require a real `dotnet test` project — heavy and brittle. **Therefore: extract the exit-code DECISION into a pure function** (e.g. `Get-TestOutcomeExitCode -SummarySeen <bool> -Total <int?> -ResultLineCount <int> -DotnetExit <int>`) that returns the exit code, and have `Invoke-Main` call it before `exit`. Then unit-test that pure function directly, matching the file's existing `Test-SummaryLine` test style (dot-source + call + `Should -Be`). This keeps the new cases unit-testable without spawning dotnet and is a mechanical seam extraction, not a behavior change.

Pure-helper cases (mirror the verbatim `Test-SummaryLine` example — dot-source in `BeforeAll`, call, `Should -Be`):
- summary seen + `Total = 0` → returns the new distinct code (recommend **exit 5**; document the choice at the check site).
- summary seen + `Total > 0` (all-pass, `Failed = 0`) → returns `$DotnetExit` (0) — unaffected.
- no summary seen + `ResultLineCount = 0` → returns **3** (genuine zero-output, unchanged).
- (optional) summary seen + `Total > 0` + non-zero dotnet exit → returns `$DotnetExit` (real failure passthrough).

**Implementation goal:**
1. Add a pure `Get-TestOutcomeExitCode` function near the other pure functions (top of the script, alongside `Test-SummaryLine`). Encode: `if (-not $SummarySeen -and $ResultLineCount -eq 0) { 3 } elseif ($SummarySeen -and $Total -eq 0) { 5 } else { $DotnetExit }`. (Exit 1 and exit 4 are earlier guard-clause exits that remain in `Invoke-Main` — the helper covers only the tail decision at lines 185-191.)
2. Replace the inline zero-output check (lines 185-191) with a call to the helper: compute the code, and on `5` print a distinct WARN (e.g. `WARN: Filter matched zero tests (summary reported Total=0)`), else keep the existing WARN for `3`, then `exit <code>`. Add a comment at the check site documenting that exit 5 = "zero-match filter (summary seen, Total=0)", distinct from exit 3 = "no summary captured at all".
3. Recommend a **new distinct exit code (5)** over reusing exit 3 — the PHASES deliverable permits either; a new code keeps the two zero-tests-observed states non-overlapping and is trivially mapped by the runner in WU-3. Document the choice in the comment.

**Spec requirements:** SPEC Fix Direction §2 (flag zero-match-exit-0 as distinct non-`verified`); PHASES Phase 2 deliverables 1 + 3. Non-overlapping exit-code semantics.

**Minimum Verifiable Behavior:** `Invoke-Pester repos/cognito-forms/.claude/scripts/test-filtered.Tests.ps1` asserts the zero-match case's returned code differs from both the all-pass case and the genuine-zero-output case, and all assertions pass.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-2 | Solo | None |

### WU-3 — Runner `result_fidelity` mapping for `no-tests-matched` + schema doc

**Scope (copied from PHASES deliverables):**
- `build-queue-runner.ps1`: extend the `result_fidelity` computation to map the new zero-match signal to a distinct value `no-tests-matched`, alongside `n/a` / `no-output` / `verified`. Update the schema doc comment to document the new value.

**TDD:** no (small mapping + doc change; verified via the runtime-verification row + WU-2's Pester establishing the exit code; the runner's fidelity map has no existing unit-test harness).

**Files to create/modify:**
- `user/scripts/build-queue-runner.ps1` — extend `result_fidelity` computation. Anchor verified: [VERIFY: grep -n "resultFidelity" user/scripts/build-queue-runner.ps1] — current block at lines 161-166 maps `n/a` (non-test op), `no-output` (`$exitCode -eq 3`), else `verified`; schema doc comment at lines 24-25.

**Implementation goal:**
1. Add `elseif ($exitCode -eq 5) { 'no-tests-matched' }` (using the exit code chosen in WU-2) to the `Get-SafeValue { ... }` block computing `$resultFidelity`, positioned before the `else { 'verified' }`. Keep `n/a` for non-test ops and `no-output` for exit 3.
2. Update the schema doc comment (lines 24-25) to list `no-tests-matched` in the `result_fidelity` enum with a one-line meaning (`"no-tests-matched" = test op whose filter matched zero tests (summary reported Total=0)`).
3. Do NOT alter the exit-code capture (`$exitCode = $proc.ExitCode`, line ~128) or the build-op log-failure path.

**Spec requirements:** SPEC Fix Direction §2; PHASES Phase 2 deliverable 2. `no-tests-matched` must be distinct from `no-output` and `verified`.

**Minimum Verifiable Behavior:** static review of the diff shows exit 5 → `no-tests-matched` mapping added and the schema comment updated; confirmed end-to-end by the Phase 2 runtime-verification row.

**Depends on:** WU-2 (the exit code it maps must exist first). **Shares `build-queue-runner.ps1` with WU-5** — must be in an earlier batch than WU-5.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 2 | WU-3 | Solo | Shares `build-queue-runner.ps1` with WU-5 (WU-5 is a later batch) |

### Runtime Verification (Phase 2) *(checked by manual testing on a Cognito worktree — NOT by /execute-plan)*
- [ ] <!-- verification-only --> Running `/mstest -Filter "ClassName~DoesNotExist"` on a Cognito worktree produces a `results/<seq>.json` with `hygiene.result_fidelity == "no-tests-matched"`, not `"verified"`.

---

## Phase: bug P3 — Inline outcome banner (seq + RESULT + counts + next action)

**Goal:** Print one unambiguous outcome line to the invocation's own stdout so agents never inspect the runner/results JSON to disambiguate `exit_code=0`; carry counts through to the results JSON; fix the wrapper's clobber of the runner's hygiene block.
**Entry criteria:** WU-3 complete (the `no-tests-matched` fidelity the banner surfaces must exist).
**SPEC.md references:** Theory 2 + Theory 3 (echo own seq); Fix Direction §2 (inline banner, next action, own-seq echo); Affected Area rows "Queue runner" + "Queue wrapper".

### WU-4 — Pure banner-formatting helper in build-queue-hygiene.ps1 + Pester

**Scope (copied from PHASES deliverables):**
- Add a pure banner-formatting helper to `build-queue-hygiene.ps1` taking `{seq, op, exit_code, result_fidelity, build_fidelity, counts}` and returning a single-line string, e.g. `build-queue: seq=614 op=mstest RESULT=PASS tests=312 failed=0 (result_fidelity=verified)`. Branches: `RESULT=NO-TESTS-MATCHED` (non-green) when `result_fidelity == no-tests-matched`; `RESULT=FAIL` on non-zero exit or `build_fidelity == log-failure-override`; `RESULT=PASS` otherwise. Any non-clean RESULT appends an explicit next-action suffix.
- Pester unit tests: one case per RESULT branch, asserting the exact next-action suffix for each non-clean case.

**TDD:** yes (pure function, unit-testable — write the Pester cases first).

**Files to create/modify:**
- `user/scripts/build-queue-hygiene.ps1` — new pure `Format-BuildQueueBanner` function alongside the existing 13 functions [VERIFY: grep -n "^function " user/scripts/build-queue-hygiene.ps1]. Dot-sourced by runner (line 62) and wrapper (line 48) via `Get-SafeValue { . (Join-Path $PSScriptRoot 'build-queue-hygiene.ps1') }`.
- `user/scripts/build-queue-hygiene.Tests.ps1` — new `Describe "Format-BuildQueueBanner"` block.

**Test files:** `user/scripts/build-queue-hygiene.Tests.ps1` [VERIFY: ls user/scripts/build-queue-hygiene.Tests.ps1] (append; the file exists — `BeforeAll` dot-sources the module, `Describe` per function).

**Test expectations (write FIRST):**
- PASS: `{seq=614, op=mstest, exit_code=0, result_fidelity=verified, build_fidelity=verified, counts=@{passed=312;failed=0;total=312}}` → `build-queue: seq=614 op=mstest RESULT=PASS tests=312 failed=0 (result_fidelity=verified)`, no next-action suffix.
- FAIL: non-zero exit_code (e.g. 1) → `RESULT=FAIL` + suffix naming `read logs/<seq>.build.err.log`; AND separately `build_fidelity=log-failure-override` with exit 0 → `RESULT=FAIL` + the same read-logs next-action.
- NO-TESTS-MATCHED: `result_fidelity=no-tests-matched` → `RESULT=NO-TESTS-MATCHED` + a "widen filter" next-action suffix.
- (staleness) exit_code 4 → non-clean RESULT (FAIL) + a "rebuild (stale DLL)" next-action suffix — assert the exact suffix text.

**Implementation goal:**
1. Write `Format-BuildQueueBanner` as a pure string function (no filesystem/process side effects) taking a parameter set covering `seq`, `op`, `exit_code`, `result_fidelity`, `build_fidelity`, and a `counts` hashtable (`passed`/`failed`/`total`, any may be `$null`). Compute RESULT with this precedence: `NO-TESTS-MATCHED` when `result_fidelity -eq 'no-tests-matched'`; else `FAIL` when `exit_code -ne 0` OR `build_fidelity -eq 'log-failure-override'`; else `PASS`.
2. Base line: `build-queue: seq=<seq> op=<op> RESULT=<result>`; when counts are present append ` tests=<total> failed=<failed>`; always append ` (result_fidelity=<result_fidelity>)`.
3. Next-action suffix (only when RESULT is non-clean): staleness (exit 4) → rebuild guidance; `NO-TESTS-MATCHED` → "widen filter"; `log-failure-override` → "read logs/<seq>.build.err.log". Use stable, exact strings the Pester cases assert.
4. Keep it fail-open-friendly (never throw on `$null` counts).

**Spec requirements:** SPEC Fix Direction §2 (banner shape, non-green NO-TESTS-MATCHED, explicit next action, own-seq echo); PHASES Phase 3 deliverable 1 + Pester deliverable.

**Minimum Verifiable Behavior:** `Invoke-Pester user/scripts/build-queue-hygiene.Tests.ps1` passes for all three RESULT branches, each producing the exact expected one-line string and (for non-clean) the exact next-action suffix.

### WU-5 — Runner: capture test-op Passed/Failed/Total counts into hygiene JSON

**Scope (copied from PHASES deliverables):**
- `build-queue-runner.ps1`: capture test-filtered's Passed/Failed/Total counts into the hygiene JSON so the background-poll path (results JSON read directly, no wrapper stdout) is equally legible.

**TDD:** no (I/O-bound capture/parse against the detached grandchild; verified via the Phase 3 runtime-verification rows — the runner's capture path has no isolated unit-test harness).

**Files to create/modify:**
- `user/scripts/build-queue-runner.ps1` — add count capture + persist into the results JSON body. Anchors verified: the runner redirects stdout/stderr to `logs/<seq>.build.log`/`.err.log` **only for build ops** ([VERIFY: grep -n "isBuildOp" user/scripts/build-queue-runner.ps1]); test-op stdout is currently NOT captured. Results JSON write at lines ~177-197.

**Implementation goal:**
> **Drift correction (from touchpoint audit — baked in):** for **test ops** the runner does not currently capture the grandchild's stdout (only build ops redirect to a log). The `Results: Passed=P Failed=F Total=T` line `test-filtered.ps1` prints therefore isn't visible to the runner today. The PHASES deliverable grants author discretion between two mechanisms — pick and document ONE:
> - **(Recommended) Redirect test-op stdout to `logs/<seq>.test.log`** (mirror the existing build-op `RedirectStandardOutput` block, extending the condition to test ops), then parse the `Results: Passed=(\d+) Failed=(\d+) Total=(\d+)` line from that captured stream and store `passed`/`failed`/`total` into the hygiene JSON. This reuses the existing `Results:` line (no `test-filtered.ps1` change) and the existing log-redirect infrastructure. **Preserve the wrapper's live-tail behavior** — if redirecting test-op stdout changes what the wrapper tails, ensure the wrapper still surfaces live output (the runtime-verification row below confirms live output + banner both work).
> - **(Alternative)** have `test-filtered.ps1` emit a canonical machine-readable line (e.g. `##BQ-COUNTS Passed=.. Failed=.. Total=..`) that the runner captures — only if the redirect approach disturbs the live tail.

1. Capture the counts and add a `counts` object (`passed`/`failed`/`total`, `$null` when not a test op or not parseable) to the results JSON body the runner writes (near lines 177-197). Use `Get-SafeValue` so a parse miss never throws — a missing `Results:` line yields `$null` counts, and the banner degrades to no `tests=` segment (WU-4 already handles `$null` counts).
2. Do NOT regress the existing `result_fidelity`/`build_fidelity`/hygiene fields written in the same body (WU-3's mapping must survive).

**Spec requirements:** SPEC Fix Direction §2 (counts legible on the background-poll path); PHASES Phase 3 deliverable 2.

**Depends on:** WU-3 (shares `build-queue-runner.ps1`; WU-3's `result_fidelity` edit lands first). Runs in the same batch as WU-4 (disjoint file: `build-queue-hygiene.ps1` vs `build-queue-runner.ps1`).

### WU-6 — Wrapper: compose+print banner; fix Step-5 hygiene clobber

**Scope (copied from PHASES deliverables):**
- `build-queue.ps1`: after reading `$proc.ExitCode`, read the runner's rich `results/<seq>.json`, compose the banner via the WU-4 helper, and print it as the LAST line of the wrapper's own stdout.
- Fix the Step-5 clobber (lines 425-438): the wrapper currently overwrites the runner's hygiene-rich `results/<seq>.json` with a minimal `{seq, exit_code, ended_at}` form. The hygiene fields MUST survive intact after Step 5.

**TDD:** no (process/filesystem orchestration in the wrapper; verified via the Phase 3 runtime-verification rows).

**Files to create/modify:**
- `user/scripts/build-queue.ps1` — banner compose+print + Step-5 fix. Anchors verified: `$exitCode = $proc.ExitCode` at line 420 [VERIFY: grep -n "proc.ExitCode" user/scripts/build-queue.ps1]; Step-5 minimal-write block at lines 425-438 [VERIFY: grep -n "resultBody" user/scripts/build-queue.ps1] (writes `{seq, exit_code, ended_at}` to the SAME `results/<seq>.json` the runner wrote, via `File.Replace`); wrapper stdout via `Write-Output`/`Write-Host`. Helper dot-sourced at line 48.

**Implementation goal:**
1. **Fix the clobber (required correctness fix, not polish):** replace the minimal-body write with a **read-merge-write**: read the runner's existing `results/<seq>.json` (which already carries the full `hygiene` block + `counts` from WU-5), set/refresh `exit_code` and `ended_at` on the parsed object, and write that back — preserving `result_fidelity`/`build_fidelity`/`vbcscompiler_recycled`/`counts`/etc. Keep the atomic temp-file + `File.Replace` pattern already in the block. (Alternative permitted by PHASES: drop the wrapper's write entirely if the runner already records `exit_code`+`ended_at` — verify the runner body includes both before choosing this; the read-merge-write is safer and recommended.) Use `Get-SafeValue` around the read/parse so a missing/partial runner file falls back to the current minimal body rather than throwing (fail-open).
2. **Compose + print the banner:** after `$exitCode = $proc.ExitCode` (line 420) and after the results JSON is finalized, read the (now hygiene-preserving) `results/<seq>.json`, extract `seq`/`op`/`exit_code`/`result_fidelity`/`build_fidelity`/`counts`, call `Format-BuildQueueBanner` (dot-sourced from `build-queue-hygiene.ps1`), and `Write-Output` the result as the **last** line of the wrapper's stdout. Guard with `Get-SafeValue` — a helper/parse failure must not change the wrapper's exit behavior.
3. The banner echoes the invocation's **own** `seq` (Symptom 5 mitigation) — agents read the seq inline and never glob `results/`.

**Spec requirements:** SPEC Fix Direction §2 (banner on the wrapper's own stdout, own-seq echo); PHASES Phase 3 deliverables 3 + 4 (the clobber fix is a **required** correctness fix — without it the banner/any hygiene consumer reads the minimal post-clobber JSON and loses `result_fidelity`/`build_fidelity`).

**Depends on:** WU-4 (banner helper) + WU-5 (counts in JSON).

**Minimum Verifiable Behavior (Phase 3):** `Invoke-Pester user/scripts/build-queue-hygiene.Tests.ps1` passes for all three banner RESULT branches (WU-4). WU-5/WU-6 are confirmed by the runtime-verification rows below.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-4, WU-5 | Yes | None (hygiene.ps1 vs runner.ps1) — but WU-5 requires WU-3 already landed |
| 2 | WU-6 | Solo | `build-queue.ps1` (unique) |

### Runtime Verification (Phase 3) *(checked by manual testing on a Cognito worktree — NOT by /execute-plan)*
- [ ] <!-- verification-only --> Running `/mstest` (real filter, all-pass) on a Cognito worktree prints a `build-queue: seq=<N> op=mstest RESULT=PASS tests=<T> failed=0 (result_fidelity=verified)` line as the last line of stdout.
- [ ] <!-- verification-only --> Running `/mstest -Filter "ClassName~DoesNotExist"` prints a banner with `RESULT=NO-TESTS-MATCHED` and a "widen filter" next-action suffix.
- [ ] <!-- verification-only --> After either run, `results/<seq>.json` still contains the full `hygiene` block (`result_fidelity`, `build_fidelity`, `vbcscompiler_recycled`, `counts`, etc.) — confirming the Step-5 clobber fix holds.

---

## Phase: bug P4 — Skills: trust the banner (docs-only)

**Goal:** Point agents at the now-authoritative banner line so they stop `cat`-ing the runner script or results JSON to disambiguate `exit_code=0`.
**Entry criteria:** WU-6 complete (the banner must exist before skills are told to trust it).
**SPEC.md references:** Affected Area row "Build/test skills"; Fix Direction §2 (removes the incentive to inspect).

### WU-7 — Four SKILL.md files: trust-the-banner guidance

**Scope (copied from PHASES deliverable):**
- Add guidance to `msbuild/SKILL.md`, `mstest/SKILL.md`, `nxbuild/SKILL.md`, `nxtest/SKILL.md` that the invocation prints an authoritative one-line `build-queue: ... RESULT=...` banner as its last stdout line — trust it; do NOT `cat` the runner script or `results/<seq>.json` to disambiguate `exit_code=0`. Keep each skill's existing step-4 background-read-by-exact-seq path unchanged. Reinforce the next-action the banner names inline (rebuild on staleness exit 4, widen filter on `NO-TESTS-MATCHED`, read `logs/<seq>.build.err.log` on `log-failure-override`). Keep each skill's existing "Do not interpret or reformat the output" instruction intact.

**TDD:** no (docs-only).

**Files to create/modify (all exist; consistent insertion anchor confirmed):**
- `repos/cognito-forms/.claude/skills/msbuild/SKILL.md` [VERIFY: ls repos/cognito-forms/.claude/skills/msbuild/SKILL.md] — step 3 ends with "Do not interpret or reformat the output." (natural attach point); step 4 background-read-by-seq at line ~36.
- `repos/cognito-forms/.claude/skills/mstest/SKILL.md` [VERIFY: ls repos/cognito-forms/.claude/skills/mstest/SKILL.md] — has an existing exit-4 "Stale-DLL trap" section (lines ~41-45); reinforce alongside the new `NO-TESTS-MATCHED` next-action.
- `repos/cognito-forms/.claude/skills/nxbuild/SKILL.md` [VERIFY: ls repos/cognito-forms/.claude/skills/nxbuild/SKILL.md] — ends at step 4; no post-Instructions section.
- `repos/cognito-forms/.claude/skills/nxtest/SKILL.md` [VERIFY: ls repos/cognito-forms/.claude/skills/nxtest/SKILL.md] — ends at step 4; no post-Instructions section.

**Implementation goal:**
1. In each file, after step 3's "Do not interpret or reformat the output." sentence, add a short banner-trust instruction: the invocation prints an authoritative one-line `build-queue: seq=<N> op=<op> RESULT=<...> ...` banner as its LAST stdout line — trust that line for the outcome; do NOT `cat`/`grep` the runner script (`build-queue-runner.ps1`) or `results/<seq>.json` to disambiguate an `exit_code=0`.
2. Reinforce the banner's inline next-actions: rebuild on staleness (exit 4), widen the filter on `RESULT=NO-TESTS-MATCHED`, read `logs/<seq>.build.err.log` on `log-failure-override`. For `mstest`, fold the `NO-TESTS-MATCHED` note into the existing "Stale-DLL trap" / exit-code section so the guidance sits with the related exit-4 language.
3. Do NOT remove or alter the existing step-4 exact-seq background-read path (no glob fallback exists in any file — nothing to remove; confirmed by the audit). Keep the "Do not interpret or reformat the output" line.
4. Keep edits consistent across the four files (same wording, adapted per file's existing structure).
5. Do NOT touch `local-ui-tests/SKILL.md` — out of scope; its "don't trust the queue exit code / results json" guidance is intentional for the Selenium project.

**Spec requirements:** SPEC Affected Area "Build/test skills"; PHASES Phase 4 deliverable (banner-trust + "do not inspect runner/results to disambiguate"; preserve step-4 exact-seq path; the SPEC's "drop the glob results/ fallback" over-scoping is NOT carried — no such fallback exists).

**Minimum Verifiable Behavior:** each of the four SKILL.md files contains the new banner-trust guidance and the "do not inspect runner/results to disambiguate" instruction (reviewable by diff); `python user/scripts/lint-skills.py` exits clean and `python user/scripts/project-skills.py` regenerates projections without error.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-7 | Solo | None (4 distinct SKILL.md files, one WU) |

**Completion (gate-owned — do NOT author as a WU):** the `__mark_fixed__` gate writes `FIXED.md` and flips SPEC.md **Status** once this phase's runtime verification passes. `/execute-plan` does not perform this step.

---

## Notes carried from the touchpoint audit (drift corrections already baked into the WUs above)

1. **bqe deny scan target** — regexes match against `scan = _suppress_safe(command)`, not the raw command (WU-1 §3).
2. **Test registration constant is `_ALL_TESTS`**, not `_TESTS` (WU-1 §6).
3. **`long-build-ownership-guard.sh` is already correctly anchored** — Phase 1 audit expects no code change (WU-1 §5).
4. **`test-filtered.Tests.ps1` has no exit-code harness** — WU-2 extracts a pure `Get-TestOutcomeExitCode` helper to keep the new cases unit-testable in the file's existing pure-function style.
5. **Runner doesn't capture test-op stdout today** — WU-5 must add capture (redirect to `logs/<seq>.test.log` recommended) to get the `Results:` counts.
6. **Two writers to `results/<seq>.json`** (runner + wrapper Step-5) — WU-6's clobber fix uses read-merge-write to preserve the hygiene block.
7. **No `results/*.json` glob fallback** exists in any of the four skills — Phase 4 is purely additive; nothing to remove.
8. **`local-ui-tests/SKILL.md`** intentionally distrusts the queue exit code/results JSON for the Selenium project — out of scope; do not modify.
