---
kind: implementation-plan
feature_id: interventions-telemetry-repo-scope-split-brain
status: Ready
created: 2026-07-12
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-12.
> To execute: `/execute-plan <this-file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 2. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\interventions-telemetry-repo-scope-split-brain\plans\all-phases-interventions-telemetry-split-brain-part-1.md` (this file)
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\interventions-telemetry-repo-scope-split-brain\plans\all-phases-interventions-telemetry-split-brain-part-2.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — interventions-telemetry-repo-scope-split-brain (Part 1: Phases 1–2)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\interventions-telemetry-repo-scope-split-brain\PHASES.md` (2 phases in this part)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\interventions-telemetry-repo-scope-split-brain\SPEC.md`

**Total phases:** 2 (this part) of 4
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config harness repo — NON-default gates)

This is the **claude-config** repo. There is NO `/msbuild` / `/mstest` / `/nxbuild`. The quality gates for every batch/phase in this part are:

```bash
python3 user/scripts/test_lazy_core.py                       # pytest — shared lazy_core helpers
python3 user/scripts/lazy-state.py --test                    # in-file smoke harness (feature state machine)
python3 user/scripts/bug-state.py --test                     # in-file smoke harness (bug state machine)
python3 user/scripts/lazy_parity_audit.py --repo-root .      # coupled-pair parity (REQUIRED — WU-3 touches both state scripts)
python3 user/scripts/efficacy-eval.py --repo-root . --dry-run # byte-inert smoke of the evaluator entrypoint
```

- The `--test` smoke outputs are **byte-pinned** to `tests/baselines/lazy-state-test-baseline.txt` and `tests/baselines/bug-state-test-baseline.txt`. If WU-3 adds/changes an in-file fixture, regenerate the baseline ONLY by piping live `--test` output through `test_lazy_core.py`'s `_normalize_smoke_output` helper — NEVER hand-edit a baseline.
- **Coupling rule (HARD):** any change to the `--run-end` efficacy gate must land in BOTH `lazy-state.py` and `bug-state.py` (the marker/breadcrumb is shared). `lazy_parity_audit.py --repo-root .` MUST stay exit 0.
- State-machine logic lives in the scripts (`lazy_core.py` shared), NOT in skill prose. WU work here is all Python + tests.
- Commit + push after each phase completes with gates green (claude-config is a personal repo; push allowed; NO AI-attribution footer; Conventional Commits `fix(...)`/`test(...)`).

## References — Upstream Artifacts

(none — this bug has no `**Depends on:**` block and no completed hard upstream dependencies.)

## Verified touchpoint audit (inline — well-traced harness bug; symbols confirmed via grep 2026-07-12)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `user/scripts/lazy_core.py` | yes | `read_intervention_telemetry` (16316), `read_telemetry_events` (16071), `drop_efficacy_breadcrumb` (15832), `efficacy_breadcrumb_present` (15864), `repo_key` (7924), `write_run_marker` (10697), `_atomic_write`, `claude_state_dir` | refactor | Extend `read_intervention_telemetry` to merge originating target-repo keyed ledgers; add coverage field to `drop_efficacy_breadcrumb`/`efficacy_breadcrumb_present`. REUSE `read_telemetry_events(paths=...)` for the sibling-ledger read and `repo_key` for the keyed-dir path — do NOT re-derive either. |
| `user/scripts/efficacy-eval.py` | yes | calls `read_intervention_telemetry` (1002, 1145), `set_active_repo_root` (1101), `drop_efficacy_breadcrumb` (1114) | refactor | Pass the evaluated scope (its `--repo-root`→`repo_key`) into `drop_efficacy_breadcrumb` at line 1114 — do NOT add a second breadcrumb writer. |
| `user/scripts/lazy-state.py` | yes | `--run-end` efficacy gate (11898+), `efficacy_breadcrumb_present` consumer | refactor | Add the coverage check to the existing gate's refusal branch; mirror the `pending_hardening` refusal shape. |
| `user/scripts/bug-state.py` | yes | coupled `--run-end` gate (7262+), `--efficacy-skip-authorized` (6959) | refactor | Mirror the lazy-state.py gate change verbatim (coupled pair). |
| `user/scripts/test_lazy_core.py` | yes | pytest suite (`_TESTS`) | modify | Add merged-read + coverage-breadcrumb tests. |
| `tests/baselines/lazy-state-test-baseline.txt`, `tests/baselines/bug-state-test-baseline.txt` | yes | byte-pinned smoke baselines | modify (only if WU-3 changes a fixture) | Regenerate via `_normalize_smoke_output`, never by hand. |

## Execution Schedule

| Step | Phase(s) | Title | Blocked by | Parallel? |
|------|----------|-------|------------|-----------|
| 1 | P1 | Merged cross-repo intervention-telemetry read (WU-1) | — | Solo |
| 2 | P2 | Coverage-bearing breadcrumb + evaluator caller (WU-2) | WU-1 (shared `lazy_core.py`/`test_lazy_core.py`) | Solo |
| 3 | P2 | Scope-checking `--run-end` gate, coupled pair (WU-3) | WU-2 (breadcrumb coverage semantics; shared `test_lazy_core.py`) | Solo |

All three WUs share `lazy_core.py` and/or `test_lazy_core.py`, so they run in **separate sequential batches** (file-overlap rule). `/execute-plan` runs them inline in one Opus context in order.

## Work Units

- [ ] WU-1 — Merged cross-repo intervention-telemetry read
- [ ] WU-2 — Coverage-bearing efficacy breadcrumb + evaluator caller
- [ ] WU-3 — Scope-checking `--run-end` gate (coupled pair)

---

### Phase: interventions-telemetry-repo-scope-split-brain P1 — Merged cross-repo intervention-telemetry read

**Goal:** A claude-config-rooted `read_intervention_telemetry` also merges the originating target repo's keyed telemetry ledger, so the interventions-bearing repo can be evaluated against the runs' telemetry.
**Entry criteria:** none (first phase).
**SPEC.md references:** `## Fix Scope` §2; `## Decisions` D1 (v1 merge set = current run's target repo + committed cloud segments).

#### WU-1 — Merged cross-repo intervention-telemetry read
- **Scope:** PHASES P1 all deliverables — extend `read_intervention_telemetry` to resolve + merge the originating target-repo keyed ledger(s), fail-open, deduped, sorted; byte-identical no-op when no originating marker exists.
- **TDD:** yes
- **Files to create/modify:**
  - `user/scripts/lazy_core.py` — extend `read_intervention_telemetry` [VERIFY: grep -n "def read_intervention_telemetry" user/scripts/lazy_core.py]; REUSE `read_telemetry_events(paths=...)` [VERIFY: grep -n "def read_telemetry_events" user/scripts/lazy_core.py] and `repo_key` [VERIFY: grep -n "def repo_key" user/scripts/lazy_core.py] for the sibling-ledger path; a small keyed-ledger-path resolver helper if the marker-read/`repo_key` composition warrants it.
  - `user/scripts/test_lazy_core.py` — merged-read tests.
- **Test files:** `user/scripts/test_lazy_core.py`
- **Test expectations:**
  - Two-keyed-dir fixture: a records-bearing repo_root (claude-config-like) + a SEPARATE telemetry-bearing keyed dir (target-repo-like). Assert the merged read under the claude-config `repo_root` SURFACES the target-repo events (RED before the change — target events absent).
  - Dedup across an overlapping cloud-segment event + a keyed-ledger event on the existing `(run_id, ts, event, item_id)` key.
  - Fail-open: an unreadable/absent sibling keyed ledger contributes nothing, never raises.
  - Byte-identical no-op when NO originating target-repo run marker exists (empty extra set == today's behavior).
- **Implementation goal:** Resolve the originating target repo(s) per D1 v1 (the live/most-recent run marker's `repo_root` → `repo_key` → `~/.claude/state/<key>/lazy-telemetry.jsonl` + rotated segments) via a RAW marker read (NOT `read_run_marker`, whose age gate deletes a stale marker — use the `fleet.py`/`write_run_checkpoint` raw-read precedent [VERIFY: grep -n "raw" user/scripts/lazy_core.py]); read the sibling ledger with `read_telemetry_events(paths=...)`; union with the current-repo state-dir ledger + committed cloud segments the function reads today; dedup on the existing key; sort by `(run_id, ts)`. No state-dir creation on the read path (`create=False` semantics).
- **Spec requirements:** SPEC `## Fix Scope` §2 ("`read_intervention_telemetry` merges originating target-repo ledgers"); D1.
- **Batch:** 1

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | N/A |

**Runtime Verification** *(none — no app/MCP surface; validation is pytest + `--test`):*
- N/A — pure Python, hermetic tests.

---

### Phase: interventions-telemetry-repo-scope-split-brain P2 — Coverage-bearing breadcrumb + scope-checking `--run-end` gate

**Goal:** The efficacy breadcrumb records WHICH repo-scopes the flush evaluated, and the `--run-end` gate refuses when the interventions-bearing scope (claude-config) was not covered — closing the "gate verifies invocation, not coverage" hole.
**Entry criteria:** WU-1 landed (shared `lazy_core.py`/`test_lazy_core.py`; coverage semantics reflect the merged read).
**SPEC.md references:** `## Fix Scope` §3; `## Decisions` D2 (second flush is an additional `--repo-root <claude-config>` invocation; breadcrumb must attest coverage).

#### WU-2 — Coverage-bearing efficacy breadcrumb + evaluator caller
- **Scope:** PHASES P2 deliverables 1–2 — `drop_efficacy_breadcrumb` records covered repo-scope(s); `efficacy-eval.py` passes the scope it evaluated.
- **TDD:** yes
- **Files to create/modify:**
  - `user/scripts/lazy_core.py` — `drop_efficacy_breadcrumb` [VERIFY: grep -n "def drop_efficacy_breadcrumb" user/scripts/lazy_core.py] gains a covered-scope param, extending the payload from `{run_started_at, ts}` to add `covered_scopes: [<repo_key|root>]`; still marker-gated + fail-open, written via `_atomic_write` [VERIFY: grep -n "_atomic_write" user/scripts/lazy_core.py]. A multi-scope run accumulates BOTH scopes (read-merge-write the breadcrumb, OR the gate reads the union of same-run breadcrumbs — pick the simpler; document the choice).
  - `user/scripts/efficacy-eval.py` — pass the evaluated scope (its `--repo-root`→`repo_key`) into `drop_efficacy_breadcrumb` [VERIFY: grep -n "drop_efficacy_breadcrumb" user/scripts/efficacy-eval.py] at line ~1114.
  - `user/scripts/test_lazy_core.py` — breadcrumb coverage tests.
- **Test files:** `user/scripts/test_lazy_core.py`
- **Test expectations:**
  - `drop_efficacy_breadcrumb` records the passed scope in `covered_scopes`; a two-scope sequence accumulates both keys for the same `run_started_at`.
  - Marker-gated (no live marker → no write, returns False) and fail-open (write error → False) preserved.
  - The payload remains readable by the existing `efficacy_breadcrumb_present` for the `run_started_at` match.
- **Implementation goal:** Thread the covered scope from the evaluator's bound `--repo-root` (via `repo_key`) into the breadcrumb; accumulate multi-scope coverage for one run without losing the `run_started_at` gating.
- **Spec requirements:** SPEC `## Fix Scope` §3 (first clause); D2.
- **Batch:** 1

#### WU-3 — Scope-checking `--run-end` gate (coupled pair)
- **Scope:** PHASES P2 deliverables 3–6 — `efficacy_breadcrumb_present` (or a coverage-aware sibling) requires the interventions-bearing scope; the `--run-end` gate refuses on incomplete coverage; coupled-pair mirror + parity.
- **TDD:** yes
- **Files to create/modify:**
  - `user/scripts/lazy_core.py` — `efficacy_breadcrumb_present` [VERIFY: grep -n "def efficacy_breadcrumb_present" user/scripts/lazy_core.py] gains coverage-awareness: satisfied only when `repo_key(<claude-config>)` is among `covered_scopes` for the current run; `run_started_at`-moot semantics (no marker → True) preserved.
  - `user/scripts/lazy-state.py` — `--run-end` efficacy gate [VERIFY: grep -n "efficacy_breadcrumb_present\|efficacy" user/scripts/lazy-state.py] refuses (exit 1, marker + registry LEFT IN PLACE) with a corrective message naming the uncovered interventions scope; `--efficacy-skip-authorized` still overrides.
  - `user/scripts/bug-state.py` — coupled mirror of the same gate change [VERIFY: grep -n "run_end\|efficacy-skip-authorized" user/scripts/bug-state.py] (gate at 7262+).
  - `user/scripts/test_lazy_core.py` + each script's in-file `--test` harness + `tests/baselines/*.txt` (regenerate via `_normalize_smoke_output` only if a fixture changed).
- **Test files:** `user/scripts/test_lazy_core.py` + in-file `--test` fixtures in both state scripts.
- **Test expectations:**
  - A `--run-end` after a flush covering ONLY the target scope returns the refusal JSON (`run_marker_deleted: false`) naming the uncovered interventions scope.
  - A `--run-end` after a two-scope flush (target + claude-config) passes.
  - `--efficacy-skip-authorized` overrides (records the skip for retro grading).
  - No-live-marker → gate moot (idempotent no-op run-end).
  - `lazy_parity_audit.py --repo-root .` exit 0 (coupled pair).
- **Implementation goal:** Promote the gate from "was the trio invoked?" to "was the interventions-bearing scope covered?", mirroring the `pending_hardening` refusal shape [VERIFY: grep -n "pending_hardening" user/scripts/lazy-state.py]; keep it byte-identical for the moot/override paths; mirror verbatim into `bug-state.py`.
- **Spec requirements:** SPEC `## Fix Scope` §3 (refusal clause); §5 (coupled-pair mirroring + tests).
- **Batch:** 2 (depends on WU-2's breadcrumb coverage field)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-2 | Solo | shares `lazy_core.py`/`test_lazy_core.py` with WU-1 (prior phase) — sequential |
| 2 | WU-3 | Solo | depends on WU-2; shares `lazy_core.py`/`test_lazy_core.py` |

**Runtime Verification** *(none — no app/MCP surface):*
- N/A — hermetic pytest + in-file `--test`; parity audit is the coupled-pair gate.

---

## Completion (this part)

When WU-1 through WU-3 are landed with all gates green (`test_lazy_core.py`, both `--test` suites, `lazy_parity_audit.py` exit 0), tick the `## Work Units` boxes, flip this part's frontmatter `status:` to `Complete`, commit + push (`fix(interventions-telemetry-split-brain): merged read + coverage-bearing efficacy gate`), and let the state machine route to part 2. Do NOT flip the top-level PHASES `**Status:**` or SPEC `**Status:**` — those are gate-owned. Then proceed per the single-sourced execution contract.
