---
kind: implementation-plan
feature_id: interventions-telemetry-repo-scope-split-brain
status: Complete
created: 2026-07-12
complexity: complex
phases: [3, 4]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-12.
> To execute: `/execute-plan <this-file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 2. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\interventions-telemetry-repo-scope-split-brain\plans\all-phases-interventions-telemetry-split-brain-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\interventions-telemetry-repo-scope-split-brain\plans\all-phases-interventions-telemetry-split-brain-part-2.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution. Part 1 (Phases 1–2) MUST be Complete before this part runs.

# Implementation Plan — interventions-telemetry-repo-scope-split-brain (Part 2: Phases 3–4)  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\interventions-telemetry-repo-scope-split-brain\PHASES.md` (2 phases in this part)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\interventions-telemetry-repo-scope-split-brain\SPEC.md`

**Total phases:** 2 (this part) of 4
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL, the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths, this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config harness repo — NON-default gates)

This is the **claude-config** repo. There is NO `/msbuild` / `/mstest` / `/nxbuild`. The quality gates for this part are:

```bash
python3 user/scripts/test_efficacy_eval.py                   # pytest — the evaluator (WU-5)
python3 user/scripts/test_lazy_core.py                       # pytest — shared helpers (regression)
python3 ~/.claude/scripts/project-skills.py                  # expand !cat refs → projected (WU-4 prose)
python3 ~/.claude/scripts/lint-skills.py                     # broken injections / embedded patterns (WU-4)
python3 user/scripts/doc-drift-lint.py --repo-root .         # doc-drift (WU-4 — only if Hooks/Coupled-pairs tables touched)
python3 user/scripts/efficacy-eval.py --repo-root . --dry-run # byte-inert evaluator smoke
```

- **Coupled trio (HARD):** WU-4 edits `lazy-batch` / `lazy-bug-batch` / `lazy-batch-cloud` §1c.6 flush prose. The three must state the SAME two-scope flush (honoring each sibling's tabulated divergences). After editing any skill prose, ALWAYS re-run `project-skills.py` then `lint-skills.py` — an unresolved `!cat` only shows in projected output.
- **Data repair (WU-6):** the 6 intervention record edits are doc writes; they MUST go through the WU-5 re-baseline act (never a hand-edit of the frozen `baseline` block), which serializes via `_render_intervention_record` (diff-stable field order). r5/r7 are EXCLUDED.
- Commit + push after each phase with gates green (Conventional Commits; NO AI-attribution footer).

## References — Upstream Artifacts

(none — no `**Depends on:**` block. This part depends on part 1's Phases 1–2, already Complete before this part runs.)

## Verified touchpoint audit (inline — symbols confirmed via grep 2026-07-12)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `user/skills/lazy-batch/SKILL.md` | yes | §1c.6 flush prose (lines 476, 478 — efficacy-eval + canary invocations) | modify | Add the two-scope flush prose; do NOT move doc-write/commit ownership. |
| `user/skills/lazy-bug-batch/SKILL.md` | yes | §1c.6 analog | modify | Mirror the two-scope flush prose (bug orchestrator divergences). |
| `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` | yes | §1c.6 cloud flush prose | modify | Mirror; honor the cloud committed-segment path divergence. |
| `user/scripts/efficacy-eval.py` | yes | `read_intervention_telemetry` (merged, from part 1), `_render_intervention_record` (lazy_core.py:16359), `--record-intervention` capture path | refactor | Add an explicit re-baseline act; REUSE `_render_intervention_record` [VERIFY: grep -n "_render_intervention_record" user/scripts/lazy_core.py] for the write (diff-stable field order); do NOT bypass the serializer. |
| `docs/interventions/harden-2026-07-r{14,15,16,18,20,21}.md` | yes | 6 poisoned `event:gate-refusal` records (frozen `runs:1/events:15/value:15.0`) | modify (via WU-5 act) | Re-baseline from the merged ledger + `provenance: backfilled-from-merged-ledger`. |
| `user/scripts/test_efficacy_eval.py` | yes | pytest suite | modify | Re-baseline-act coverage. |

## Execution Schedule

| Step | Phase(s) | Title | Blocked by | Parallel? |
|------|----------|-------|------------|-----------|
| 1 | P3 + P4 | Two-scope flush prose (WU-4) + Re-baseline act (WU-5) | part 1 Complete | Yes (disjoint files) |
| 2 | P4 | Apply re-baseline to 6 records (WU-6) | WU-5 | Solo |

## Work Units

- [x] WU-4 — Two-scope end-of-run flush prose (coupled trio)
- [x] WU-5 — Re-baseline act in the efficacy evaluator
- [x] WU-6 — Apply re-baseline to the six poisoned records

---

### Phase: interventions-telemetry-repo-scope-split-brain P3 — Two-scope end-of-run flush in the coupled orchestrator trio

**Goal:** The §1c.6 end-of-run flush additionally runs the trio with `--repo-root <claude-config>`, so records and the telemetry that grades them meet in one evaluation and the part-1 coverage gate is satisfiable.
**Entry criteria:** part 1 (Phases 1–2) Complete — the merged read (P1) and the coverage-bearing breadcrumb+gate (P2) must exist, or the tightened gate would refuse every run-end with no second flush.
**SPEC.md references:** `## Fix Scope` §1, §5; `## Decisions` D2.

#### WU-4 — Two-scope end-of-run flush prose (coupled trio)
- **Scope:** PHASES P3 all deliverables — the two-scope flush prose in all three orchestrators + re-project/lint.
- **TDD:** no (skill-prose edit; validated by project/lint/doc-drift, not unit tests).
- **Files to create/modify:**
  - `user/skills/lazy-batch/SKILL.md` — §1c.6: the flush additionally runs the trio (`efficacy-eval.py` review + `--canary`) with `--repo-root <claude-config>` (path from the skill's config table), NOT only `--repo-root .`. State the two-scope contract (the claude-config-rooted evaluation sees originating telemetry via the P1 merged read; both scopes attested in the P2 breadcrumb). Doc-write/commit ownership unchanged; NON-BLOCKING `⚠ efficacy-eval failed` posture preserved.
  - `user/skills/lazy-bug-batch/SKILL.md` — mirror into its §1c.6 analog.
  - `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` — mirror, honoring the cloud committed-segment path divergence.
  - Update each skill's coupled-pair "Differences" / State Machine notes if the flush shape changed the dispatch surface.
- **Implementation goal:** All three §1c.6 blocks state the same two-scope flush (manual three-way diff confirms the mirror); `project-skills.py` + `lint-skills.py` clean; `doc-drift-lint.py --repo-root .` exit 0 if any Hooks/Coupled-pairs table row was touched.
- **Spec requirements:** SPEC `## Fix Scope` §1 ("flush covers the interventions-bearing repo"); §5 (coupled-trio mirroring).
- **Batch:** 1

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-4, WU-5 | Yes | None (WU-4 = skill prose; WU-5 = `efficacy-eval.py`/`test_efficacy_eval.py` — disjoint) |

**Runtime Verification** *(none — prose validated by project/lint/doc-drift):*
- N/A.

---

### Phase: interventions-telemetry-repo-scope-split-brain P4 — Re-baseline the six poisoned gate-refusal records

**Goal:** The six `event:gate-refusal` records frozen at the stale single-cloud-run baseline are re-frozen from the real merged ledger, carrying an explicit `provenance` note.
**Entry criteria:** part 1 P1 Complete (the merged read supplies real windows). WU-6 depends on WU-5.
**SPEC.md references:** `## Fix Scope` §4; `## Decisions` D3 (re-baseline provenance honesty).

#### WU-5 — Re-baseline act in the efficacy evaluator
- **Scope:** PHASES P4 deliverable 1 + 4 — an explicit re-baseline act that recomputes an EXISTING record's frozen baseline from the merged ledger and overwrites it with a provenance note; idempotent-safe; honest no-data fallback.
- **TDD:** yes
- **Files to create/modify:**
  - `user/scripts/efficacy-eval.py` — a re-baseline act (a new CLI action, or an explicit re-baseline path adjacent to `--record-intervention`; mechanism the executor chooses) that reads the merged window via `read_intervention_telemetry` [VERIFY: grep -n "read_intervention_telemetry" user/scripts/efficacy-eval.py], recomputes the frozen `baseline` (runs/events/value + `window_start_run`/`window_end_run`/`last_run_id`), and overwrites it through `_render_intervention_record` [VERIFY: grep -n "_render_intervention_record" user/scripts/lazy_core.py] (diff-stable field order), stamping `provenance: backfilled-from-merged-ledger` + the date.
  - `user/scripts/test_efficacy_eval.py` — re-baseline coverage.
- **Test files:** `user/scripts/test_efficacy_eval.py`
- **Test expectations:**
  - Reads a fixture merged ledger, overwrites a poisoned baseline, asserts the new value + `provenance: backfilled-from-merged-ledger` note + date.
  - `record_intervention` never-clobber-by-existence is preserved (re-baseline is the ONLY path that overwrites; a plain re-capture still no-ops).
  - Idempotent: re-running the act does not double-stamp or corrupt.
  - No-data honesty: a record whose merged window is still empty gets an honest no-data/pending baseline, NEVER a fabricated value (evaluator's existing honesty ladder).
- **Implementation goal:** Provide the deliberate evaluator/CLI re-baseline act the D9 manual-backfill path needs, since `record_intervention` is never-clobbering by existence.
- **Spec requirements:** SPEC `## Fix Scope` §4; D3.
- **Batch:** 1

#### WU-6 — Apply re-baseline to the six poisoned records
- **Scope:** PHASES P4 deliverables 2–3 — run the WU-5 act on r14/r15/r16/r18/r20/r21; leave r5/r7 untouched with a pointer note.
- **TDD:** no (data repair via the WU-5 act; correctness asserted by WU-5's tests + inspection).
- **Files to create/modify:**
  - `docs/interventions/harden-2026-07-r14.md`, `...-r15.md`, `...-r16.md`, `...-r18.md`, `...-r20.md`, `...-r21.md` — re-frozen baselines + provenance (written by the WU-5 act, NOT hand-edited).
  - Add a one-line note (in the plan report or the sibling-bug cross-ref) that r5/r7 are excluded — their target signals are vocabulary-invalid, owned by `docs/bugs/hardening-intervention-records-unmeasurable-or-missing`.
- **Implementation goal:** Each of the 6 records' baseline no longer reads `runs:1/events:15/value:15.0`; each carries `provenance: backfilled-from-merged-ledger` + date and re-derived window fields. Verify by inspecting the record frontmatter after the act runs.
- **Spec requirements:** SPEC `## Fix Scope` §4 (r5/r7 exclusion explicit).
- **Batch:** 2 (depends on WU-5)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 2 | WU-6 | Solo | depends on WU-5's act |

**Runtime Verification** *(SEAM B symptom-reproduction — completion-gate evidence):*
- N/A as a plan WU (gate-owned). NOTE for the validation tail: the fix is proven when a claude-config-rooted efficacy flush over the real merged ledger produces ≥1 DUE verdict (a `## Review <date>` section) where zero were ever produced before — the SPEC `## Verified Symptom`.

---

## Completion (this part / feature)

When WU-4 through WU-6 are landed with all gates green (`test_efficacy_eval.py`, `test_lazy_core.py`, `project-skills.py` + `lint-skills.py` clean, `doc-drift-lint.py` exit 0, the 6 records re-baselined), tick the `## Work Units` boxes, flip this part's frontmatter `status:` to `Complete`, and — as this is the LAST part — set the top-level PHASES.md `**Status:**` to `In-progress` (implementation done, validation pending). Commit + push (`fix(interventions-telemetry-split-brain): two-scope flush prose + re-baseline poisoned records`).

Do NOT flip SPEC `**Status:**` to `Fixed` and do NOT write `FIXED.md` — those are `__mark_fixed__`-gate-owned. Let the state machine route to the validation tail. Proceed per the single-sourced execution contract.
