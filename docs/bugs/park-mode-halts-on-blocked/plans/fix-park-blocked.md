---
kind: fix-plan
feature_id: park-mode-halts-on-blocked
status: In-progress
created: 2026-06-16
complexity: complex
phases: [1, 2, 3, 4, 5, 6]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-16.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\park-mode-halts-on-blocked\plans\fix-park-blocked.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — park-mode-halts-on-blocked  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\park-mode-halts-on-blocked\PHASES.md` (park-mode-halts-on-blocked, 6 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\park-mode-halts-on-blocked\SPEC.md`

**Total phases:** 6
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Work Units

- [x] WU-1 — lazy-state.py: --park-blocked flag, BLOCKED park branch, all-parked terminal (+ TDD fixtures)
- [x] WU-2 — bug-state.py: mirror --park-blocked, BLOCKED park branch, all-parked terminal (+ TDD fixtures)
- [x] WU-3 — lazy_core.build_parked_entry: add sentinel_kind field (+ test_lazy_core.py characterization)
- [x] WU-4 — Orchestrator wiring: lazy-batch + lazy-bug-batch + lazy-batch-cloud SKILL.md
- [x] WU-5 — parked-flush.md: surface blocked-parked items (blocked-specific affordance)
- [ ] WU-6 — user/scripts/CLAUDE.md: document --park-blocked + queue-exhausted-all-parked

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py`, `.md` (skill/component), or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: this feature's `PHASES.md`, and project `CLAUDE.md` review edits.

> **EXECUTION-CONTEXT OVERRIDE (autonomous pipeline).** When this plan is executed by a `/lazy-bug-batch` (or `/lazy-batch`) cycle subagent that does NOT have the `Agent` tool, the orchestrator+subagent split is impossible. In that case, perform ALL work INLINE with Read / Edit / Write (the dispatched-skill inline override). Still obey: test-first per TDD WU (write the failing fixture/assertion before the implementation), batch review inline, quality gates, and commit. Never invoke a nested `/lazy*`. This override applies ONLY to the dispatch mechanism; every other rule below stands.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies. It is a self-contained harness-internal bug fix. Authoritative context is the sibling SPEC.md + PHASES.md and the canonical pipeline doc `C:\Users\Jacob\source\repos\claude-config\user\scripts\CLAUDE.md`.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — UNLESS the execution-context override (no Agent tool) applies, in which case work INLINE per that override. The orchestrating session never silently descopes.
2. All edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline — write the failing test/fixture FIRST (RED for the right reason), then implement.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's (or inline) output is reviewed for correctness, spec alignment, and TDD discipline before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. Relevant CLAUDE.md files are created/updated after the relevant phase.
8. Each completed phase is committed (and pushed to `main`) before the next phase begins, per the atomic gate+commit rule.
9. This plan is self-contained — follow it exactly as written without relying on external context.
10. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
11. **Coupled-pair discipline (HARD).** `lazy-state.py` ↔ `bug-state.py` (Phases 1↔2) and the three orchestrators (Phase 4) must keep their mirrored changes symmetric. Never commit one side of a pair without the other in the same phase.
12. **Baseline regeneration discipline.** The `--test` baselines (`tests/baselines/lazy-state-test-baseline.txt`, `bug-state-test-baseline.txt`) are regenerated ONLY by piping live `--test` output through `test_lazy_core.py::_normalize_smoke_output` — NEVER hand-edited. The no-flag (default) output MUST stay byte-identical to its pre-change baseline; only the new park-blocked fixtures add lines.

## Execution Schedule

| Step | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|----------|----------|------------|-----------|
| 1 | P1 | lazy-state.py core park-blocked support | — | Solo |
| 2 | P2 | Mirror into bug-state.py | P1 (contract strings) | Solo |
| 3 | P3 | sentinel_kind on parked entries (lazy_core) | P1, P2 | Solo |
| 4 | P4 | Orchestrator wiring (3 skills) | P1–P3 | Solo |
| 5 | P5 | Flush handles blocked-parked (parked-flush.md) | P3, P4 | Solo |
| 6 | P6 | Documentation (user/scripts/CLAUDE.md) | P1–P2 | Solo |

> **Sequencing note:** all phases are Solo (single-WU each). They are ordered by the contract-dependency chain: P1 defines the `--park-blocked` flag + `queue-exhausted-all-parked` terminal that everything downstream references; P2 mirrors it; P3 adds the shared field both scripts emit; P4 wires the orchestrators to the flag/terminal/field; P5 teaches the flush; P6 documents. Do NOT reorder.

---

### Phase: P1 — lazy-state.py core park-blocked support

**Goal:** Park a feature-local `BLOCKED.md` under a new `--park-blocked` flag (symmetric with `--park-needs-input`) and emit an honest `queue-exhausted-all-parked` terminal when every remaining feature is parked.
**Entry criteria:** none (first phase).
**SPEC.md references:** Verified Symptoms 1–2; Theory 1; Proven Findings 1–2; Locked Decisions D1 (park feature-local BLOCKED), D2 (global terminals still halt), D3 (honest all-parked terminal), D5 (park all blocker kinds).

#### WU-1 (TDD: yes) — lazy-state.py park-blocked support

**Scope (PHASES.md P1 deliverables):**
- `compute_state(...)` gains `park_blocked: bool = False`, appended after `park_needs_input`.
- Step-2 selection loop: new BLOCKED park branch (parks a feature carrying `BLOCKED.md` when `park_blocked`), symmetric with the NEEDS_INPUT branch.
- `_PARK_MODE` true when EITHER `park_needs_input` OR `park_blocked` is active.
- New `terminal_reason="queue-exhausted-all-parked"` when `current is None` AND `_PARKED` non-empty, as the fallback BEFORE `all-features-complete` and AFTER the existing specific terminals.
- `--park-blocked` CLI arg (`action="store_true"`) wired into the `compute_state(...)` call in main.
- Tests: new `--test` fixtures.

**TDD:** yes.

**Files to create/modify:**
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\lazy-state.py` — modify. Verified symbols (read during planning):
  - `compute_state(repo_root, cloud, skip_needs_research=False, real_device=True, scope_feature_id=None, park_needs_input=False)` at line ~1057 [VERIFY: grep -n "def compute_state" user/scripts/lazy-state.py] — append `park_blocked: bool = False`.
  - `global _PARK_MODE, _PARKED` / `_PARK_MODE = park_needs_input` at ~1085 [VERIFY: grep -n "_PARK_MODE = park_needs_input" user/scripts/lazy-state.py] — change to `_PARK_MODE = park_needs_input or park_blocked`.
  - Step-2 NEEDS_INPUT park branch at ~1253–1263 (the `not (spec_path / "BLOCKED.md").exists()` guard at ~1256) [VERIFY: grep -n "BLOCKED.md).exists()" user/scripts/lazy-state.py] — add a sibling BLOCKED park branch ABOVE/adjacent to it: `if park_blocked and (spec_path / "BLOCKED.md").exists(): _PARKED.append(lazy_core.build_parked_entry(feature_id, spec_path / "BLOCKED.md")); _diag(...); continue`. Place it so a feature carrying BOTH sentinels parks exactly once (BLOCKED branch evaluated first, `continue`s).
  - `current is None` terminal block at ~1273–1323 [VERIFY: grep -n "all-features-complete" user/scripts/lazy-state.py] — insert the `queue-exhausted-all-parked` return immediately before the final `all-features-complete` return, guarded by `if _PARKED:`.
  - CLI arg parser `parser.add_argument("--park-needs-input", ...)` at ~4994 [VERIFY: grep -n "park-needs-input" user/scripts/lazy-state.py] — add a sibling `--park-blocked` arg.
  - main `compute_state(...)` call passing `park_needs_input=args.park_needs_input` at ~5692 [VERIFY: grep -n "park_needs_input=args.park_needs_input" user/scripts/lazy-state.py] — add `park_blocked=args.park_blocked`.
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\tests\baselines\lazy-state-test-baseline.txt` — regenerated via `_normalize_smoke_output` after the fixtures land [VERIFY: grep -n "_normalize_smoke_output" user/scripts/test_lazy_core.py].

**Test files (TDD):**
- In-file `--test` harness inside `lazy-state.py` (the `run_smoke_tests()` body), modeled on the existing `WU-1-park` fixture at ~4718 [VERIFY: grep -n "WU-1-park" user/scripts/lazy-state.py].

**Test expectations (write FIRST, must fail RED for the right reason — the new param/flag/terminal does not yet exist):**
1. **`park-blocked-mode-skip`** — temp queue `[blocked-feat (carries BLOCKED.md), workable-feat]`, call `compute_state(root, cloud=False, real_device=True, park_blocked=True)` → `feature_id == "workable-feat"`, `parked` is a list with one entry whose `id == "blocked-feat"` and whose `sentinel` ends in `BLOCKED.md`.
2. **`park-blocked-default-halt`** — same queue, NO flag → `terminal_reason == "blocked"`, AND `"parked"` key ABSENT (byte-identical to today's behavior).
3. **`park-blocked-all-parked-terminal`** — queue where every remaining feature is parked (one BLOCKED.md, optionally one NEEDS_INPUT.md), call with `park_needs_input=True, park_blocked=True` → `terminal_reason == "queue-exhausted-all-parked"` (NOT `all-features-complete`), `parked` non-empty.
4. **`park-blocked-and-needs-input-single-park`** — a feature carrying BOTH BLOCKED.md and NEEDS_INPUT.md, plus a workable feature after it, call with both flags True → the dual-sentinel feature appears exactly ONCE in `parked` (no double-append), `workable-feat` dispatched.

**Implementation goal:** Add `park_blocked` end-to-end so a feature-local `BLOCKED.md` is parked (not selected as `current`, never returns `terminal_reason="blocked"`) under the flag, the queue advances, and an all-parked queue returns the honest distinct terminal. The default (no flag) path stays byte-identical — the `parked` key only appears when `_PARK_MODE` is true (either flag). D2: the global `current is None` terminals (`cloud-queue-exhausted`, `device-queue-exhausted`, `queue-blocked-on-research`, `scoped-id-not-found`) keep their existing precedence; the new terminal is the fallback before `all-features-complete`.

**Spec requirements:** SPEC D1/D2/D3/D5 + Affected Area row "Step 2 park skip" and "Park flag".

**Batch:** 1 (solo).

#### Batch overview (P1)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | N/A |

---

### Phase: P2 — Mirror park-blocked into the bug pipeline (bug-state.py)

**Goal:** Apply the Phase-1 changes to `bug-state.py` so `/lazy-bug-batch --park` parks blocked bugs and advances.
**Entry criteria:** P1 complete (the contract strings `--park-blocked` / `queue-exhausted-all-parked` and the branch shape are defined there; P2 reuses them verbatim).
**SPEC.md references:** Open Question 1 (bug pipeline parity — resolved yes by D7); Affected Area "Cloud mirror (lockstep)" generalizes to bug-state parity.

#### WU-2 (TDD: yes) — bug-state.py park-blocked mirror

**Scope (PHASES.md P2 deliverables):**
- `bug-state.py compute_state(...)` gains `park_blocked: bool = False` after `park_needs_input`.
- Step-2 loop: BLOCKED park branch symmetric with the NEEDS_INPUT branch.
- `_PARK_MODE` true when EITHER flag active.
- `queue-exhausted-all-parked` terminal in the `current is None` block, after the existing specific terminals (`cloud-queue-exhausted`, `device-queue-exhausted`, `all-deferred`) and before `all-bugs-fixed`. The existing `TR_ALL_DEFERRED` (DEFERRED.md) stays distinct.
- `--park-blocked` CLI arg + main wiring.
- Tests: bug-side `--test` fixtures mirroring Phase 1.

**TDD:** yes.

**Files to create/modify:**
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\bug-state.py` — modify. Verified symbols:
  - `compute_state(..., park_needs_input=False)` at ~507 [VERIFY: grep -n "def compute_state" user/scripts/bug-state.py] — append `park_blocked: bool = False`.
  - `_PARK_MODE = park_needs_input` at ~539–540 [VERIFY: grep -n "_PARK_MODE = park_needs_input" user/scripts/bug-state.py] — change to `or park_blocked`.
  - Step-2 NEEDS_INPUT park branch at ~683–693 (BLOCKED guard at ~686) [VERIFY: grep -n "BLOCKED.md).exists()" user/scripts/bug-state.py] — add sibling BLOCKED park branch using `build_parked_entry(bug_id, spec_dir / "BLOCKED.md")`; same single-park ordering.
  - `current is None` terminal block at ~706+ [VERIFY: grep -n "all-bugs-fixed\|TR_ALL_BUGS_FIXED" user/scripts/bug-state.py] — insert `queue-exhausted-all-parked` guarded by `if _PARKED:` before `all-bugs-fixed`.
  - CLI arg `--park-needs-input` at ~3516 [VERIFY: grep -n "park-needs-input" user/scripts/bug-state.py] — add sibling `--park-blocked`.
  - main call `park_needs_input=args.park_needs_input` at ~4090 [VERIFY: grep -n "park_needs_input=args.park_needs_input" user/scripts/bug-state.py] — add `park_blocked=args.park_blocked`.
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\tests\baselines\bug-state-test-baseline.txt` — regenerated via `_normalize_smoke_output`.

**Test files (TDD):**
- In-file `--test` harness inside `bug-state.py` (`run_smoke_tests()`), modeled on the existing `WU-1-park (bug)` fixture + sub-fixtures A–D at ~3219 [VERIFY: grep -n "WU-1-park" user/scripts/bug-state.py].

**Test expectations (write FIRST, RED for the right reason):**
- Mirror Phase-1 fixtures 1–4, bug-flavored (`blocked-bug`/`workable-bug`).
- Existing sub-fixture D (BLOCKED precedence under `park_needs_input` ONLY) MUST still pass unchanged — it now means "blocked NOT parked because `park_blocked` was NOT set".
- New sub-fixture E: under `park_blocked=True` the same blocked bug IS parked + the next bug dispatched.

**Implementation goal:** Byte-for-byte symmetric mirror of WU-1 in `bug-state.py`. Default (no flag) output stays byte-identical to the regenerated `bug-state-test-baseline.txt`.

**Spec requirements:** SPEC Open-Q1; D1/D3 applied to bugs.

**Batch:** 1 (solo).

#### Batch overview (P2)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-2 | Solo | N/A |

---

### Phase: P3 — sentinel_kind on parked entries (lazy_core.build_parked_entry)

**Goal:** Add a `sentinel_kind` field to the parked-entry record so the flush (P5) can distinguish blocked-parked from needs-input-parked without filesystem inspection.
**Entry criteria:** P1, P2 complete (the BLOCKED park branches that produce these entries exist).
**SPEC.md references:** Open Question 2 + D4 (sentinel-kind field); Affected Area "Park flag" row (`build_parked_entry` already accepts any sentinel path).

#### WU-3 (TDD: yes) — build_parked_entry sentinel_kind

**Scope (PHASES.md P3 deliverables):**
- `build_parked_entry(item_id, sentinel_path)` returns an additional `"sentinel_kind"` key: `"blocked"` if `sentinel_path.name == "BLOCKED.md"`, `"needs-input"` if `"NEEDS_INPUT.md"`, else `"unknown"`. Existing four keys unchanged + same positions; purely additive.
- Confirm (do not special-case) that a BLOCKED.md sentinel yields `decision_count == 0` via the existing absent/empty/not-a-list path.
- Update the docstring contract block.
- Tests: `test_lazy_core.py` characterization.

**TDD:** yes.

**Files to create/modify:**
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\lazy_core.py` — modify. Verified symbol: `def build_parked_entry(item_id: str, sentinel_path: Path) -> dict[str, Any]` at ~463, returning `{id, sentinel, decision_count, parked_since}` at ~497–502, docstring contract at ~471–485 [VERIFY: grep -n "def build_parked_entry" user/scripts/lazy_core.py]. Add `"sentinel_kind"` derived from `sentinel_path.name`; document it in the contract block.
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\test_lazy_core.py` — add characterization assertions [VERIFY: grep -n "build_parked_entry" user/scripts/test_lazy_core.py].
- `tests/baselines/lazy-state-test-baseline.txt`, `tests/baselines/bug-state-test-baseline.txt` — regenerated (parked fixture entries now carry `sentinel_kind`).

**Test files (TDD):**
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\test_lazy_core.py`.

**Test expectations (write FIRST, RED — KeyError/missing because the key is absent):**
1. `build_parked_entry("x", Path(".../BLOCKED.md"))["sentinel_kind"] == "blocked"`.
2. `build_parked_entry("x", Path(".../NEEDS_INPUT.md"))["sentinel_kind"] == "needs-input"`.
3. An unrecognized sentinel name → `"unknown"`.
4. The existing keys (`id`, `sentinel`, `decision_count`, `parked_since`) are still present and correct; a BLOCKED.md sentinel gives `decision_count == 0`.

**Implementation goal:** Purely additive field on the shared helper, benefiting both pipelines at once. No raise on any input.

**Spec requirements:** SPEC Open-Q2 / D4.

**Batch:** 1 (solo).

#### Batch overview (P3)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-3 | Solo | N/A |

---

### Phase: P4 — Orchestrator wiring (lazy-batch + lazy-bug-batch + lazy-batch-cloud)

**Goal:** Make the three batch orchestrators pass `--park-blocked` in `--park` mode, stop firing Step-1h blocked-resolution for park-mode blocked features, handle `queue-exhausted-all-parked`, and fire the §1c.6 park notification for blocked-parked items.
**Entry criteria:** P1–P3 complete (flag, terminal, and `sentinel_kind` field exist).
**SPEC.md references:** Affected Area "Orchestrator" + "Cloud mirror (lockstep)" rows; D1 (no inline interruption); D5 (escalation blocks parked too). Coupled-pair rule (repo CLAUDE.md).

#### WU-4 (TDD: no — skill prose) — Orchestrator wiring

**Scope (PHASES.md P4 deliverables):**
- `lazy-batch/SKILL.md` Step-1a park-mode probe flag: in `--park` mode append BOTH `--park-needs-input` AND `--park-blocked` to every probe; neither when `park_mode == false`.
- `lazy-batch/SKILL.md` Step-1b: add `queue-exhausted-all-parked` terminal — `--run-end`, fire parked-flush (trigger (b)/(c)), PushNotification + final report + STOP. NOT `all-features-complete`.
- `lazy-batch/SKILL.md` Step-1b `blocked` + Step-1h: clarify that in `--park` mode a feature-local block is parked by the script and never surfaces as `terminal_reason: blocked`, so Step-1h does not fire for it (deferred to flush). D5: escalation/mcp-validation per-feature blocks are parked too.
- `lazy-batch/SKILL.md` §1c.6: park notification (point 1) reads a `sentinel_kind == "blocked"` entry as a parked BLOCK, e.g. `⏸ parked {feature_name} — BLOCKED ({phase}) · notified`.
- `lazy-bug-batch/SKILL.md`: mirror all of the above.
- `lazy-batch-cloud/SKILL.md`: mirror; record any cloud divergence in its "Differences from /lazy-batch" block (none expected — park is environment-agnostic).
- Update each skill's State Machine Summary / terminal table to list `queue-exhausted-all-parked`.

**TDD:** no (documentation/prose; no runtime surface — behavioral net is the P1–P3 fixtures).

**Files to create/modify (all `exists: yes`, verified):**
- `C:\Users\Jacob\source\repos\claude-config\user\skills\lazy-batch\SKILL.md` — Step-1a park flag at ~360 [VERIFY: grep -n "Park-mode probe flag" user/skills/lazy-batch/SKILL.md]; Step-1b terminals ~370–382; §1c.6 ~402.
- `C:\Users\Jacob\source\repos\claude-config\user\skills\lazy-bug-batch\SKILL.md` — park flag ~248; flush binding ~695 [VERIFY: grep -n "Park-mode probe flag" user/skills/lazy-bug-batch/SKILL.md].
- `C:\Users\Jacob\source\repos\claude-config\repos\algobooth\.claude\skills\lazy-batch-cloud\SKILL.md` — park flag + flush ~567 + terminals [VERIFY: grep -n "park_mode\|1g-flush" repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md].

**Implementation goal:** All three orchestrators pass `--park-blocked` in park mode, route the new terminal, suppress Step-1h for park-mode blocked features, and notify with blocked-aware wording — kept mirrored per the coupled-pair rule.

**Spec requirements:** D1/D5; Affected Area Orchestrator + Cloud-mirror rows.

**Batch:** 1 (solo — three different files, no overlap, but logically one mirrored change; review as one batch).

#### Batch overview (P4)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-4 | Solo | None (3 distinct files) |

**Post-batch verification (P4):** run `python ~/.claude/scripts/lint-skills.py` and `python ~/.claude/scripts/project-skills.py` — both must be clean (no broken `!cat` injections). Diff the three orchestrators to confirm the park-flag append + terminal handling are mirrored.

---

### Phase: P5 — Flush handles blocked-parked items (parked-flush.md)

**Goal:** Teach the shared `parked-flush.md` component to surface a blocked-parked item (`sentinel_kind: blocked`) via a blocked-specific resolution affordance instead of dropping it as a malformed `## Decision Context`.
**Entry criteria:** P3 (sentinel_kind), P4 (orchestrator §1c.6 wording + terminal) complete.
**SPEC.md references:** Open Question 2 (flush UX); Affected Area "Orchestrator" → parked-flush component; D4 sentinel-kind field.

#### WU-5 (TDD: no — skill prose) — parked-flush blocked affordance

**Scope (PHASES.md P5 deliverables):**
- Step-1 (collect): `pending_flush` now includes items whose sentinel is `BLOCKED.md`, partitioned by `sentinel_kind` into needs-input vs blocked.
- Step-2 (schema check): a blocked-parked item is NOT run through the `## Decision Context` rich-body schema check (needs-input-only); validate it against the BLOCKED.md frontmatter (`blocker_kind`, `phase`) and never drop it as "malformed `## Decision Context`".
- New branch in Steps 3–4 for blocked-parked items: re-print the BLOCKED.md body verbatim, then run the SAME resolution affordance as `lazy-batch` Step-1h (auto-resolve sequencing-only per `completeness-policy.md` §3; product-fork → `AskUserQuestion` add-phase / defer-to-tail / spin-off / halt-for-manual). After resolution, neutralize via `--neutralize-sentinel {spec_path}/BLOCKED.md` (canonical `BLOCKED_RESOLVED_<date>.md` rename). Reuse existing machinery — do NOT invent a new resolution path.
- Run-end digest (Steps 5/6): blocked-parked resolutions logged alongside decision flushes (one meta-cycle each); PushNotification wording covers blocked items.
- Update the coupling note / pipeline-binding table only if a new token is needed (none expected).

**TDD:** no (component prose; structural verification only).

**Files to create/modify (verified):**
- `C:\Users\Jacob\source\repos\claude-config\user\skills\_components\parked-flush.md` — Steps 1, 2, 2.4/2.5, 3, 4, 5, 6 [VERIFY: grep -n "Decision Context\|sentinel" user/skills/_components/parked-flush.md].

**Implementation goal:** A blocked-parked item is collected, recognized by `sentinel_kind`, validated against the BLOCKED schema (not the decision-context schema), surfaced with a blocked-specific affordance reusing Step-1h/decision-resume machinery, neutralized on `BLOCKED.md` (not `NEEDS_INPUT.md`), and logged in the run-end digest. The default (`park_mode == false`) path is unaffected.

**Spec requirements:** SPEC Open-Q2 / D4.

**Batch:** 1 (solo).

#### Batch overview (P5)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-5 | Solo | N/A |

**Post-batch verification (P5):** `lint-skills.py` + `project-skills.py` clean (re-projects the three flush consumers: lazy-batch, lazy-bug-batch, lazy-batch-cloud). Confirm `grep -rl "parked-flush.md" ~/.claude/skills/` shows the unchanged consumer set. Trace each flush trigger (a)/(b)/(c) for a blocked-parked item by reading the revised component end-to-end — confirm it reaches a resolution affordance, never the "missing `## Decision Context`" skip path, and the neutralize step targets `BLOCKED.md`.

---

### Phase: P6 — Documentation (user/scripts/CLAUDE.md)

**Goal:** Document `--park-blocked` and the `queue-exhausted-all-parked` terminal in the canonical pipeline doc so it matches the code.
**Entry criteria:** P1–P2 complete (flag + terminal exist).
**SPEC.md references:** Evidence "Related Documentation" + Affected Area note ("`user/scripts/CLAUDE.md` documents the current intent verbatim").

#### WU-6 (TDD: no — documentation) — CLAUDE.md CLI surface

**Scope (PHASES.md P6 deliverables):**
- Add a `--park-blocked` line next to `--park-needs-input` in the CLI-surface block (parks feature/bug-local `BLOCKED.md` on both scripts; byte-identical without the flag).
- Revise the existing `--park-needs-input` parenthetical "(BLOCKED still halts; …)" — now BLOCKED still halts UNLESS `--park-blocked` is also active.
- Note the new `queue-exhausted-all-parked` terminal in the lifecycle/terminal documentation.

**TDD:** no (documentation).

**Files to create/modify (verified):**
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\CLAUDE.md` — CLI-surface block; the `--park-needs-input` line [VERIFY: grep -n "park-needs-input" user/scripts/CLAUDE.md].

**Implementation goal:** Doc matches the implemented flag/terminal names exactly; the stale "BLOCKED still halts" claim corrected.

**Spec requirements:** SPEC Affected Area (CLAUDE.md revision).

**Batch:** 1 (solo).

#### Batch overview (P6)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-6 | Solo | N/A |

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase:** phases are strictly ordered P1→P6 by the contract-dependency chain (see Execution Schedule sequencing note). Execute the lowest-numbered phase whose entry criteria are satisfied. If none is ready, jump to Blocking Issue Protocol.
2. **Announce:** Print "Implementing Phase N: [title]".
3. **Review prior context:** Re-read prior phases' Implementation Notes in PHASES.md (they take priority over the plan where they diverge — especially the actual line numbers, which shift as edits land).
4. **Execute the batch** per Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "Phase N: [title] — committed".
7. **Loop:** return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (ToolSearch for task tools, create tasks for all WUs, update protocol). If running inline under the no-Agent execution-context override, task tracking is still created at the executing-session level.

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow it. Re-read PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), this plan file, AND the actual target source file (line numbers in this plan are planning-time snapshots and shift as edits land — re-grep the `[VERIFY: …]` anchors before editing).

#### Step B.1: Launch Subagents (or work INLINE under the override)

**PRE-FLIGHT CHECK:** Confirm you will use the `Agent` tool with `model: "sonnet"` for ALL code changes — UNLESS the execution-context override (no Agent tool) applies, in which case work inline with Read/Edit/Write per that override. Either way: write the failing test/fixture FIRST for every TDD WU.

Read before proceeding: `~/.claude/skills/_components/tdd-protocol.md`, `~/.claude/skills/_components/subagent-launch.md`, `~/.claude/skills/_components/tdd-test-agent.md`, `~/.claude/skills/_components/implementation-agent.md`.

**TDD discipline for the script WUs (P1–P3):** the in-file `--test` harnesses and `test_lazy_core.py` are the test surface. Add the new fixtures/assertions FIRST and run `python user/scripts/lazy-state.py --test` (resp. `bug-state.py --test`, `test_lazy_core.py`) to confirm they fail RED for the right reason (the new param/flag/terminal/field does not yet exist — NOT a syntax/import error). Then implement until green.

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` and follow its complete protocol (scope measurement, ground-truth verification gate, review, propagation check, verdict). Also read `~/.claude/skills/_components/mount-site-verification.md` for any new file. For inline execution, apply the same review protocol to your own edits (re-run the `--test` suites fresh, diff against the baseline).

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md` and follow it. Check off completed deliverables, add an Implementation Notes block with date, work completed, integration notes (especially actual final line numbers + the regenerated baseline hashes), pitfalls, and files modified. Also tick this plan's `- [ ] WU-N` row for the completed WU.

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/quality-gates.md`. For this repo the gates are the Python test harnesses + skill lint:
- After P1: `python user/scripts/lazy-state.py --test`.
- After P2: `python user/scripts/bug-state.py --test` AND re-run `lazy-state.py --test` (shared `lazy_core`).
- After P3: `python user/scripts/test_lazy_core.py` AND BOTH `--test` suites (shared helper) AND `python user/scripts/lazy_coord.py --test` (full set per `user/scripts/CLAUDE.md`).
- After P4 and P5: `python ~/.claude/scripts/lint-skills.py` (clean) AND `python ~/.claude/scripts/project-skills.py` (re-projects without broken injections).
- After P6: no executable gate — confirm the doc matches the implemented names by grep.
100% pass required before committing.

#### Step B.5: Commit Batch

Read the commit policy: try `.claude/skill-config/commit-policy.md` first; if absent, `~/.claude/skills/_components/commit-and-push.md`. This repo's work branch is `main` — commit and push to `main`. Use the **atomic gate+commit** form so an interrupted turn still leaves pushed state:

```
<gate> && git add -A && git commit -m "fix(park-mode-halts-on-blocked): <phase summary>" && git push
```

Commit message body must end with the Co-Authored-By trailer per repo convention.

#### Step B.6: Proceed to Next Batch

Checklist (all must be true): review verdict produced; PHASES.md updated + WU-N row ticked; quality gates pass; committed + pushed. If any unchecked, go back.

### Post-Phase Steps (after the phase's batch)

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md`. Note: P6 IS the `user/scripts/CLAUDE.md` update — for the other phases, review whether any CLAUDE.md needs a note, but do NOT pre-empt P6's dedicated doc work.

#### Commit and Push Post-Phase Changes

Per Step B.5 policy. Skip if the batch commit already captured everything.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point during execution:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed phases** not yet committed.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list]
   **Blocked phase:** Phase N: [title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining phases (not attempted):**
   - [list]

4. **Do not attempt to work around the blocker.**

Blocking issues include:
- A `--test` baseline regression in the DEFAULT (no-flag) output that cannot be reconciled as an intended additive change (a default-path behavior change is OUT OF SCOPE — the byte-identical-default invariant is load-bearing).
- A subagent failure that can't be fixed after 2 retry attempts.
- A quality-gate failure that can't be fixed after 2 retry attempts.
- A git push conflict that can't be resolved by rebase.
- Any error requiring architectural decisions beyond the specs.

---

## Completion

When all phases in the Execution Schedule are complete:

1. **Run the full quality-gate suite one final time:** `python user/scripts/lazy-state.py --test`, `python user/scripts/bug-state.py --test`, `python user/scripts/test_lazy_core.py`, `python user/scripts/lazy_coord.py --test`, `python ~/.claude/scripts/lint-skills.py`, `python ~/.claude/scripts/project-skills.py`.
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Bug fixed:** park-mode-halts-on-blocked
   **Total phases completed:** 6
   **Final quality-gate status:** all green
   **Commit log:** [table]
   **Implementation Notes summary:** [key integration notes — final line numbers, baseline changes, coupled-pair symmetry confirmation]

3. Leave the top-level PHASES.md `**Status:**` at `In-progress` (implementation done, validation pending) — the `__mark_fixed__` gate owns the flip to Fixed + the FIXED.md receipt. Let the state machine route forward.
