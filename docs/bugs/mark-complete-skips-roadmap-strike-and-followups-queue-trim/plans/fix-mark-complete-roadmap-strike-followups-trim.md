---
kind: fix-plan
feature_id: mark-complete-skips-roadmap-strike-and-followups-queue-trim
status: Ready
created: 2026-06-19
complexity: trivial
phases: [1]
---

> **Mobile plan** — generated by `/plan-bug` on 2026-06-19.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\mark-complete-skips-roadmap-strike-and-followups-queue-trim\plans\fix-mark-complete-roadmap-strike-followups-trim.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — mark-complete-skips-roadmap-strike-and-followups-queue-trim

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\mark-complete-skips-roadmap-strike-and-followups-queue-trim\PHASES.md` (mark-complete-skips-roadmap-strike-and-followups-queue-trim, 1 phase)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\mark-complete-skips-roadmap-strike-and-followups-queue-trim\SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This is a **confirm-and-archive / already-fixed** plan. The defect's fix is ALREADY ON DISK (commit `1b81210`, `unified-pipeline-orchestrator` Phase 5 WU-3) with dedicated passing regression tests. There is **NO source code to write** — the sole work unit is a verification re-certification that re-runs the existing tests and records the result as the confirm-and-archive evidence.

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, run the verification gate, update tracking docs | `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |

**HARD CONSTRAINT:** This plan authors **no** `.py`/`.ts`/`.cs`/test source. The ONLY files it may modify directly: `PHASES.md` (Implementation Notes / status), `SPEC.md` (status reconciliation only — NEVER set `Fixed`/`Won't-fix`, NEVER write `FIXED.md`; those are owned exclusively by the orchestrator's `__mark_fixed__` validation-tail gate). If a real source defect surfaces during verification (i.e. a cited test FAILS), STOP and write `BLOCKED.md` — that would mean the already-fixed premise is wrong and a human must reconcile the SPEC.

> **DISPATCH-LIMIT OVERRIDE (when this plan is executed by a /lazy-bug-batch cycle subagent that lacks the `Agent` tool):** perform the verification INLINE with Read / Bash. No subagent dispatch is needed — there is no implementation to delegate.

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path. **Before executing each step**, `Read` the listed component files from disk. Do NOT proceed from memory. After context compaction, re-read this plan file first.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `.claude/skill-config/quality-gates.md` (fallback: `~/.claude/skills/_components/quality-gates.md`) |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies; the fix it certifies is `1b81210`, already merged)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. This is a **no-source-change verification plan** — author no implementation or test code. The fix exists; the work is to re-certify it.
2. Run the verification gate exactly as written; record the literal result in PHASES.md Implementation Notes.
3. If any cited test FAILS, STOP and write `BLOCKED.md` (the already-fixed premise would be invalidated — human reconciliation required). Do NOT attempt a fix.
4. NEVER set the top-level SPEC/PHASES `**Status:**` to `Fixed`/`Won't-fix`, and NEVER write `FIXED.md` — those are orchestrator-owned (`__mark_fixed__` validation-tail gate).
5. When the WU lands, tick its checkbox + the Phase-1 deliverables in PHASES.md and set the top-level PHASES `**Status:**` to `In-progress` (verification done, validation pending). The state machine routes to `/mcp-test` → `__mark_fixed__` next.
6. This plan is self-contained — follow it exactly without relying on external context.

## Work Units

- [ ] WU-1 — Re-certify the WU-3 fix & regression coverage (no source change) (Phase 1)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | mark-complete-skips-roadmap-strike-and-followups-queue-trim | P1 | Re-certify the existing fix & regression coverage | — | Solo |

Single work unit.

---

### Phase: mark-complete-skips-roadmap-strike-and-followups-queue-trim P1 — Re-certify the existing fix & regression coverage

**Goal:** Confirm the WU-3 fix is present in `lazy_core.py` and its dedicated regression tests are green, recording that run as the confirm-and-archive certifying evidence. No source change.
**Entry criteria:** None (sole phase). The fix (`1b81210`) is already on disk.
**SPEC.md references:** `## Evidence Collected → Source Code` (fix present: `_strike_roadmap_row`, `_resolve_under_repo`/`_entry_matches`); `## Test Coverage` (the three cited tests + 583/583 pass); `## Proven Findings` item 4 (confirmed duplicate / already fixed → confirm-and-archive); `## Note for /plan-bug` (no-op verification phase, route to archive).

#### WU-1 — Re-certify the WU-3 fix & regression coverage (no source change)

- **Scope:**
  - [ ] Confirm the WU-3 fix is present in `user/scripts/lazy_core.py`: `apply_pseudo __mark_complete__` calls `_strike_roadmap_row` (returns `roadmap_struck`) and trims the queue via `_resolve_under_repo` / `_entry_matches` (returns `queue_trimmed`). Source inspection only — no edit.
  - [ ] Run `python user/scripts/test_lazy_core.py`; confirm green including the three cited regression tests.
  - [ ] Record the result (pass count + the three named tests) in PHASES.md Phase-1 Implementation Notes as confirm-and-archive evidence.
- **TDD:** no (no source change — the regression tests already exist and ARE the deliverable; this WU re-runs them as the certifying gate).
- **Files to create/modify:**
  - None (source). Tracking only: `PHASES.md` (Implementation Notes + checkbox/status), optionally `SPEC.md` (status reconciliation — never `Fixed`/`Won't-fix`).
- **Verification gate (this replaces the test-first pipeline for this no-source-change WU):**
  - `python user/scripts/test_lazy_core.py` exits 0, all tests pass.
  - Present in the output / suite: `test_apply_pseudo_mark_complete_trims_by_resolved_spec_dir_followups` (the `-followups` resolved-`spec_dir` miss class; asserts `queue_trimmed is True`), `test_apply_pseudo_mark_complete_strikes_roadmap_row` (asserts `roadmap_struck is True`), `test_apply_pseudo_mark_complete_no_roadmap_is_noop_strike` (no ROADMAP → `roadmap_struck False`, completion succeeds).
  - Confirmed present at `test_lazy_core.py:18348` / `:18385` / `:18427` (VERIFY: `grep -n "test_apply_pseudo_mark_complete_" user/scripts/lazy_core_TESTFILE` → `user/scripts/test_lazy_core.py`).
- **Failure handling:** if `test_lazy_core.py` is RED or any cited test is missing/failing, the already-fixed premise is invalidated — STOP, write `BLOCKED.md` naming the failing test, do NOT attempt a fix. A human must reconcile the SPEC's "already fixed" conclusion.
- **Implementation goal:** A fresh green `test_lazy_core.py` run (incl. the three named tests) stands as the certifying evidence that `__mark_complete__` strikes the ROADMAP row and trims the queue by resolved `spec_dir` (catching the `-followups` class). With evidence recorded, the bug routes to confirm-and-archive via the validation tail.
- **Spec requirements:** SPEC `## Proven Findings` items 1-4; `## Affected Area` (all three rows already FIXED/Present); `## Note for /plan-bug`.
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | N/A             |

**Post-phase (terminal):** Tick WU-1 + the Phase-1 deliverable boxes in PHASES.md, write the Implementation Notes with the literal test result, and set the top-level PHASES `**Status:**` to `In-progress` (verification done, validation pending). The state machine routes to `/mcp-test` (resolves via the structural MCP-skip — `MCP runtime: not-required`, no app surface) → coverage audit → the orchestrator's `__mark_fixed__` gate, which owns the `Fixed` flip + `FIXED.md` receipt. Commit + push to `main`.
