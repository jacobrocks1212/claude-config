---
kind: fix-plan
feature_id: coupled-overlay-drift-gate-not-in-mandatory-gates
status: Ready
created: 2026-07-19
complexity: mechanical
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan {path}`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — Wire the coupled-overlay drift gate into the mandatory gate battery (v2)

**PHASES.md files:**
- `docs/bugs/coupled-overlay-drift-gate-not-in-mandatory-gates/PHASES.md` (coupled-overlay-drift-gate-not-in-mandatory-gates, 1 phase)

**SPEC.md files:**
- `docs/bugs/coupled-overlay-drift-gate-not-in-mandatory-gates/SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (e.g. a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and any local `## Component Reference Card` override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

### Plan-specific execution notes (override the contract's defaults)

- **Repo:** `claude-config` — no compiled app, no Tauri, no MCP server. "Quality gates" are this repo's Python test + lint suite (see `.claude/skill-config/quality-gates.md`). Ignore any `/msbuild` / `/mstest` / `/nxbuild` / MCP-test references in the generic contract — they do not apply here.
- **Gate command for this plan:** the changed surfaces are validated by the mandatory battery itself. The authoritative gate to run at batch completion is:
  ```bash
  python3 user/scripts/gate-battery.py
  ```
  (from the repo root) — trust its LAST-stdout-line banner `gate-battery: run=<id> op=battery RESULT=<PASS|FAIL> cmds=<n> ...`. After this fix, `cmds=8`. Additionally run the targeted script tests:
  ```bash
  python3 -m pytest user/scripts/tests/test_gate_battery.py -q
  python3 user/scripts/doc-drift-lint.py --repo-root .
  python3 user/scripts/cli-surface-lint.py --repo-root .
  ```
- **Branch:** `main` (claude-config lands all work on `main`; never branch — see repo CLAUDE.md Branching Policy). Commit + push to `main` after the batch.
- **This is a bug-pipeline plan.** NEVER flip SPEC.md/PHASES.md top-level `**Status:**` to Fixed or write `FIXED.md` — the `__mark_fixed__` gate owns that after the validation tail. You MAY tick per-WU checkboxes, flip this plan-part `status:`, and set the phase's `Status:`/checkboxes.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | coupled-overlay-drift-gate | P1 | Register drift gate in battery + prose + count + guard | — | Batch 1 (WU-1 ∥ WU-2, file-disjoint) |

## Work Units

- [ ] WU-1 — Register the drift gate in the battery manifest + recurrence-guard test
- [ ] WU-2 — Name the drift gate in prose + correct stale battery-count claims

## Touchpoint Audit (verified inline during /spec-phases — dispatch not warranted for config+doc scope)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `.claude/skill-config/gate-battery.json` | yes | `{version:1, gates:[7 rows]}`; `parity-audit` row at index 3 | edit | Append an 8th `{id, cmd}` row invoking `generate-coupled-skills.py --check --repo-root .`; keep it a peer AFTER `parity-audit`. Do NOT restructure existing rows. |
| `user/scripts/tests/test_gate_battery.py` | yes | 17 hermetic tests (tmp state roots) | edit | ADD a new config-invariant test reading the REAL committed `.claude/skill-config/gate-battery.json`; do not disturb the hermetic-tmp tests. Register it however the file registers its tests (match the existing pattern in the file). |
| `.claude/skill-config/quality-gates.md` | yes | "Lazy skill-family changes" section (line ~17), "Mixed / feature completion" line (~24) | edit | Add `generate-coupled-skills.py --check` alongside `lazy_parity_audit.py --report` as the coupled-overlay drift check. |
| `CLAUDE.md` | yes | `gate-battery.py` script-table row (~line 259): "claude-config seeds the 7-command invariant battery AS COMMANDS" | edit | Correct the stale count (7→8) or reword to drop the hardcoded number. |
| `user/scripts/CLAUDE.md` | yes | `gate-battery.py` row (~56): "7-command invariant battery ... SPEC L5"; line ~158: "7-command invariant battery, any hook, or CI" | edit | Correct both stale count claims consistently with the CLAUDE.md edit. |
| `user/scripts/generate-coupled-skills.py` (`--check`) | yes | exit-coded 0/1/2; banner `coupled-pair generation: all pairs byte-identical (fresh)` | none | Consumed by the fix, NOT modified. |

---

### Phase: coupled-overlay-drift-gate P1 — Register drift gate in battery + prose + count + guard

**Goal:** Make `generate-coupled-skills.py --check` a mandatory battery gate so coupled-overlay drift fails fast at authoring/commit time, and guard the row against silent re-removal.
**Entry criteria:** None (single-phase fix).
**SPEC.md references:** `## Proven Findings` (root cause + serving-path trace), `## Affected Area` (fix sites), `## Reproduction Steps`.

#### WU-1 — Register the drift gate in the battery manifest + recurrence-guard test

- **Scope (PHASES.md deliverables):**
  - [ ] Add an 8th gate row to `.claude/skill-config/gate-battery.json` invoking the drift gate.
  - [ ] Add a recurrence-guard test to `user/scripts/tests/test_gate_battery.py` asserting the committed manifest contains a gate whose `cmd` invokes `generate-coupled-skills.py --check`.
- **TDD:** yes.
- **Files to create/modify:**
  - `.claude/skill-config/gate-battery.json` — append `{ "id": "coupled-overlay-drift", "cmd": "python3 user/scripts/generate-coupled-skills.py --check --repo-root ." }` as the last entry of the `gates` array (after the `parity-audit` peer). [VERIFY: grep -n "parity-audit" .claude/skill-config/gate-battery.json]
  - `user/scripts/tests/test_gate_battery.py` — add the config-invariant test. [VERIFY: test -f user/scripts/tests/test_gate_battery.py]
- **Test files:** `user/scripts/tests/test_gate_battery.py`
- **Test expectations:** A new test loads (`json.load`) the repo's real `.claude/skill-config/gate-battery.json` relative to the repo root and asserts that at least one entry in `gates` has a `cmd` containing both `generate-coupled-skills.py` and `--check`. RED before the JSON edit (no such row), GREEN after. The test reads the actual committed manifest (not a tmp fixture) — its purpose is to guard the real gate list against silent removal, which is precisely the failure class this bug fixes. Follow the file's existing test-registration/discovery convention (read the file first to match how its tests are collected and how it locates the repo root).
- **Implementation goal:** The mandatory battery (`gate-battery.py`, which loops `for gate in gates` over exactly the manifest — `user/scripts/gate-battery.py:363`/`:352`/`:154` [VERIFY: grep -n "for gate in gates" user/scripts/gate-battery.py]) now runs `generate-coupled-skills.py --check`, so future coupled-overlay drift fails the battery. The row currently passes (`--check` exits 0 on the clean tree), so the green baseline is preserved.
- **Spec requirements:** SPEC `## Proven Findings` — the fix adds the missing `generate-coupled-skills.py --check` row to the exact artifact `_load_manifest` reads; `## Affected Area` names `gate-battery.json` as the primary fix site.
- **Batch:** 1

#### WU-2 — Name the drift gate in prose + correct stale battery-count claims

- **Scope (PHASES.md deliverables):**
  - [ ] Name the drift gate in the coupled-pair prose gate list in `.claude/skill-config/quality-gates.md`.
  - [ ] Correct the stale "7-command invariant battery" prose in `CLAUDE.md` and `user/scripts/CLAUDE.md`.
- **TDD:** no (pure documentation edits — no runtime-observable behavior).
- **Files to create/modify:**
  - `.claude/skill-config/quality-gates.md` — in the "Lazy skill-family changes" section and the "Mixed / feature completion" FULL-set line, add `generate-coupled-skills.py --check` next to `lazy_parity_audit.py` as the coupled-overlay drift gate. [VERIFY: grep -n "lazy_parity_audit.py --report" .claude/skill-config/quality-gates.md]
  - `CLAUDE.md` — correct the stale count claim in the `gate-battery.py` script-table row (7→8, or reword to drop the hardcoded count). [VERIFY: grep -n "7-command invariant battery" CLAUDE.md]
  - `user/scripts/CLAUDE.md` — correct the two stale count claims consistently. If the CLAUDE.md row cites "SPEC L5" as authority for the count, keep the citation internally consistent (reword to not hardcode, or note the count moved) rather than leaving CLAUDE.md=8 citing a SPEC that says 7. [VERIFY: grep -n "7-command invariant battery" user/scripts/CLAUDE.md]
- **Implementation goal:** A human/agent following the prose gate list in `quality-gates.md` now also runs the coupled-overlay drift gate (closing the secondary prose surface named in the SPEC's serving-path trace, `quality-gates.md:18,24`), and the repo docs no longer make a stale battery-count claim after the 8th gate is added.
- **Spec requirements:** SPEC `## Affected Area` — `quality-gates.md` (prose omission) and the "Doc-drift/CLI-surface consistency" row (root `CLAUDE.md`, `user/scripts/CLAUDE.md`); `## Evidence Collected` names `quality-gates.md:18,24` as the prose surface omitting the gate.
- **Batch:** 1

#### Batch Overview

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2 | Yes (file-disjoint) | None |

**Runtime Verification** *(checked by running the battery / lints — NOT by the implementation agent):*
- [ ] <!-- verification-only --> Full battery reports `cmds=8` and `RESULT=PASS` on the clean tree (`python3 user/scripts/gate-battery.py`).
- [ ] <!-- verification-only --> Drift is caught: induce coupled-overlay drift (mutate a canonical coupled SKILL.md without re-extracting its overlay), run the battery, confirm it FAILS naming the `coupled-overlay-drift` gate, then revert.
- [ ] <!-- verification-only --> `doc-drift-lint.py --repo-root .` and `cli-surface-lint.py --repo-root .` stay clean after the manifest + doc edits.

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md `**Status:**` to Fixed and writes `FIXED.md` once this phase's work and the validation tail pass — this plan never flips top-level status itself.

## Blocking Issue Protocol / Completion

See `~/.claude/skills/_components/execution-contract.md` (single-sourced). On completion of Batch 1: run the plan-specific gates above, tick the `## Work Units` rows, flip this plan-part `status:` to `Complete`, set PHASES.md Phase 1 `Status:`/deliverable checkboxes, commit + push to `main` (pathspec-scoped, never `git add -A`; never `--force`). Leave top-level SPEC/PHASES `**Status:**` for the `__mark_fixed__` gate.
