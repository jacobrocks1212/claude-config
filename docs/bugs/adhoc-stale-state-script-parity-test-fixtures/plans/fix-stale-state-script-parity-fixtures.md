---
kind: fix-plan
feature_id: adhoc-stale-state-script-parity-test-fixtures
status: Complete
created: 2026-06-20
complexity: mechanical
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` (inline, bug pipeline) on 2026-06-20.
> To execute: `/execute-plan docs/bugs/adhoc-stale-state-script-parity-test-fixtures/plans/fix-stale-state-script-parity-fixtures.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — adhoc-stale-state-script-parity-test-fixtures

**PHASES.md files:**
- `docs/bugs/adhoc-stale-state-script-parity-test-fixtures/PHASES.md` (1 phase)

**SPEC.md files:**
- `docs/bugs/adhoc-stale-state-script-parity-test-fixtures/SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Work Units

- [x] WU-1 — Update the three `TestStateScriptParity` fixtures to the full four-surface token set

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.ts`, `.js`, `.cs`, `.vue`, `.py`, `.rs`, `.tsx`, `.jsx`, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **DISPATCH OVERRIDE (this harness repo, autonomous bug pipeline):** when this plan is executed by a cycle subagent that does NOT have the `Agent` tool, perform ALL implementation INLINE with `Read`/`Edit`/`Write` — the no-Agent constraint above yields to the inline override. The TDD/test-first discipline still holds: confirm the failing assertions are RED first, then make them GREEN.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies. The new audit surfaces' origin features — `single-slot-marker-ownership-race-disarms-owning-run` (surface 3, `--reassert-owner`) and `host-capability-declaration-for-gated-features` (surface 4, `requires_host` fail-fast) — are cited for provenance only, not consumed as contracts. The resolved prior-art bug `docs/bugs/_archive/adhoc-parity-merged-view-fixture-stale-archive-fixed` is the structural template — same class, NOT a dependency.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. (Subordinate to the DISPATCH OVERRIDE above: a no-Agent cycle subagent does this work INLINE.)
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents
3. Every TDD work unit goes through the test-first pipeline — confirm the targeted assertions are RED before making them GREEN
4. PHASES.md is updated AFTER EACH batch completes (not deferred)
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing
6. Mistakes are fixed immediately before launching the next batch
7. After all batches in a phase finish, integration verification confirms all changes work together
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it
9. Each completed phase is committed and pushed before the next phase begins
10. This plan is self-contained — follow it exactly as written without relying on external context
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | adhoc-stale-state-script-parity-test-fixtures | P1 | Update the three `TestStateScriptParity` fixtures to the full four-surface token set | — | Solo |

---

### Phase: adhoc-stale-state-script-parity-test-fixtures P1 — Update the three `TestStateScriptParity` fixtures to the full four-surface token set

**Goal:** Rewrite the three failing `TestStateScriptParity` `tmp_path` stub strings (and the fires-when-missing expectations) in `user/scripts/test_lazy_parity.py` so each stub carries the surfaces it should and drops exactly the ONE surface the test asserts on — bringing the fixtures in line with the four-surface contract `audit_state_script_parity` now enforces. The clean-fixture test returns `[]`; each fires-when-missing test stays `len == 1`; the real-repo test stays green.

**Entry criteria:** None (first and only phase).

**SPEC.md references:** `## Proven Findings` (root cause + fix scope CONFIRMED — test-fixture staleness only, no production change), `## Fix Scope (for /plan-bug)` (the per-fixture surface assignments), `## Evidence Collected → Source Code` (the four-surface table). PHASES.md `## Provenance` (the four-surface table + token list).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | None            |

#### WU-1 — Update the three `TestStateScriptParity` fixtures to the full four-surface token set

- **Scope:** Covers all PHASES.md Phase-1 deliverables (the three failing-fixture edits + the class docstring lockstep note). One file, one cohesive concern (bring the stub strings + expectations in line with the four-surface audit contract), so it is a single work unit.
- **TDD:** yes (the edited fixtures ARE the test data; the three tests are RED today and the edit turns them GREEN — confirm RED-before-GREEN by running the targeted reproduction command first).
- **Files to create/modify:** `user/scripts/test_lazy_parity.py` [VERIFY: `grep -n "class TestStateScriptParity" user/scripts/test_lazy_parity.py`]
- **Test files:** same file — `user/scripts/test_lazy_parity.py` (this is a test-fixture edit; no separate test file).
- **Test expectations:** After the edit, `audit_state_script_parity` [VERIFY: `grep -n "def audit_state_script_parity" user/scripts/lazy_parity_audit.py`] returns `[]` for the clean fixture (`test_audit_state_script_parity_clean_when_both_bind` passes), and each `*_fires_when_*` test reports EXACTLY one finding naming its dropped surface (`set_active_repo_root` / `--reorder-queue`). The real-repo `test_live_state_scripts_bind_active_repo` stays green.
- **Implementation goal:** The audit (`user/scripts/lazy_parity_audit.py:334`) now requires FOUR surface tokens per stub script:
  1. **binding** — `set_active_repo_root(args.repo_root)` (bare or `lazy_core.`-prefixed)
  2. **reorder** — the literal `"--reorder-queue"`
  3. **reassert** — the literal `"--reassert-owner"`  *(NEW surface 3)*
  4. **host-capability fail-fast** — BOTH `format_unknown_host_capability_blocker` AND `unknown-host-capability` *(NEW surface 4 — the audit's check is `failfast_re is None OR blocker_kind_re is None`, so BOTH tokens must be present to satisfy it)*

  Edit the three fixtures (`user/scripts/test_lazy_parity.py:638`–`702`) so each stub carries the surfaces it should and drops exactly the one under test:
  1. `test_audit_state_script_parity_clean_when_both_bind` (`~:685-702`) — add surfaces 3+4 tokens to BOTH stub strings (`lazy-state.py` and `bug-state.py`) so each carries all four → `audit_state_script_parity(tmp_path) == []`. PRESERVE the existing bare-vs-`lazy_core.`-prefixed binding-form split across the two stubs (the test's secondary purpose).
  2. `test_audit_state_script_parity_fires_when_binding_missing` (`~:638-661`) — add surfaces 3+4 tokens to BOTH stubs. The `lazy-state.py` stub keeps binding + reorder + 3 + 4 (all four). The `bug-state.py` stub keeps reorder + 3 + 4 but STILL omits the binding (surface 1) → exactly one finding naming the binding gap. The existing `len(findings) == 1` + `"bug-state.py"`/`"STATE"`/`"set_active_repo_root"` substring asserts stay.
  3. `test_audit_state_script_parity_fires_when_reorder_queue_missing` (`~:663-683`) — add surfaces 3+4 tokens to BOTH stubs. The `lazy-state.py` stub keeps binding + reorder + 3 + 4 (all four). The `bug-state.py` stub keeps binding + 3 + 4 but STILL omits `"--reorder-queue"` (surface 2) → exactly one finding naming `--reorder-queue`. The existing `len(findings) == 1` + `"bug-state.py"`/`"--reorder-queue"` substring asserts stay.

  Add a one-line docstring note to the `TestStateScriptParity` class (`~:623-625`) stating the stubs must mirror `audit_state_script_parity`'s `_STATE_SCRIPTS` four-surface list in lockstep — any future coupled-pair surface added to the audit must update these stubs too (SPEC future-proofing note). Suggested token phrasings that satisfy the regexes literally: include `parser.add_argument("--reassert-owner")` (surface 3) and a line carrying both `format_unknown_host_capability_blocker` and `unknown-host-capability` (e.g. `lazy_core.format_unknown_host_capability_blocker(...)  # blocker_kind: unknown-host-capability`). Match the existing implicitly-concatenated string-literal style; keep the trailing `\n` and the `encoding="utf-8"` write call. Do NOT touch `audit_state_script_parity`, the four surface regexes, or any state-script source.
- **Spec requirements:** SPEC `## Fix Scope (for /plan-bug)` — each fixture isolates exactly ONE missing surface (the others fully present) so each fires-when-missing test asserts `len == 1`; the clean fixture carries all four → `[]`. "No production code change. The fix is confined to `user/scripts/test_lazy_parity.py`."
- **Batch:** 1

**Runtime Verification** *(none — this is a pure-Python unit-test fixture edit with no runtime-observable surface):*

N/A — no runtime-observable behavior. Verification is the `pytest` suite turning green (see Step B.4). The bug's `**MCP runtime:** not-required` line in PHASES.md applies — no Tauri/MCP runtime, no device.

**MCP Integration Test Assertions:**
N/A — no runtime-observable behavior in this phase (pure unit-test fixture text edit).

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** Phase 1 has no prerequisites — it is ready immediately.
2. **Announce:** Print "Implementing adhoc-stale-state-script-parity-test-fixtures Phase 1: Update the three `TestStateScriptParity` fixtures to the full four-surface token set"
3. **Review prior context:** Re-read PHASES.md (Phase 1 + any Implementation Notes) and the SPEC's Proven Findings + Fix Scope.
4. **Execute the single batch** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "adhoc-stale-state-script-parity-test-fixtures Phase 1 — committed as [hash]"
7. **Loop:** Only one phase — done after step 6.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (load task tools via ToolSearch, create a task for WU-1, update it through the test/impl phases).

### Per-Batch Steps

For Batch 1:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow it. Re-read from disk: PHASES.md (Phase 1 + Implementation Notes), SPEC.md (`## Proven Findings`, `## Fix Scope`, `## Evidence Collected`), `user/scripts/test_lazy_parity.py` (the `TestStateScriptParity` class, `~:623-702`), and `user/scripts/lazy_parity_audit.py` (`audit_state_script_parity` + the four surface regexes, `~:299-393`). Do NOT rely on cached content.

#### Step B.1: Launch Subagents (or implement INLINE per the DISPATCH OVERRIDE)

**RED-FIRST (TDD):** before editing, run the reproduction command and confirm RED:
```
python -m pytest user/scripts/test_lazy_parity.py::TestStateScriptParity -q
```
Expect `3 failed, 1 passed` — `_clean_when_both_bind` reports "4 more items" (2 scripts × 2 new surfaces), and the two `_fires_when_*` tests over-count. Record this RED evidence.

Then make the edit (WU-1) — the three fixture stubs + expectations + the class docstring note as detailed in the WU above. Read the TDD/launch/agent component files listed in the card before dispatching (or, for a no-Agent cycle subagent, implement inline).

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` (and `mount-site-verification.md` — N/A here, no new files) and follow the protocol. Produce a structured review with a PASS/PASS-WITH-FIXES/NEEDS-REWORK verdict. Specifically verify: (a) ONLY the three `TestStateScriptParity` fixture stubs + the class docstring changed; (b) each fires-when-missing fixture drops EXACTLY one surface (the others fully present); (c) `audit_state_script_parity`, the four surface regexes, and all state-script source are untouched; (d) the `len == 1` + finding-substring asserts in the two fires-when-missing tests are intact (or adjusted only as needed to keep naming the correct dropped surface).

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md`. Check off the Phase-1 deliverables, add an Implementation Notes block (date, the three fixtures edited, the surface tokens added, RED→GREEN evidence). When the last phase's work lands, set the top-level PHASES `**Status:**` to `In-progress` (NOT Fixed — validation pending).

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/quality-gates.md`. For this repo the gate is the Python test suite. Run, in order:
```
python -m pytest user/scripts/test_lazy_parity.py::TestStateScriptParity -q   # must now be 4 passed
python -m pytest user/scripts/test_lazy_parity.py -q                          # whole module green
python -m pytest user/scripts/ -q                                            # whole suite green (0 failed)
python user/scripts/lazy_parity_audit.py --repo-root .                       # real-repo audit stays exit 0 (no production change)
```
100% pass required (`0 failed`) before proceeding. The real-repo audit must STILL exit 0 — a non-zero exit there means a state-script source change leaked in (this fix must not touch production).

#### Step B.5: Commit Batch

Read the commit policy: first try `.claude/skill-config/commit-policy.md`; if absent, `~/.claude/skills/_components/commit-and-push.md`. Commit to `main` and push (this is a personal config repo — push allowed). Use the ATOMIC GATE+COMMIT pattern: chain the suite-green gate with the commit so an interrupted turn leaves pushed state:
```
python -m pytest user/scripts/test_lazy_parity.py -q && git add -A && git commit -m "..." && git push
```

#### Step B.6: Proceed to Next Batch

Only one batch — proceed to Post-Phase Steps.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md`. The integration check here is the full `python -m pytest user/scripts/ -q` run reporting `0 failed` — the symptom (red `TestStateScriptParity`) is resolved and no other test regressed — PLUS the real-repo `lazy_parity_audit.py --repo-root .` exit 0 (confirming no production drift).

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md`. Likely a no-op — this is a test-fixture correction, not a contract change. Confirm `user/scripts/CLAUDE.md` needs no update (the four-surface contract and `audit_state_script_parity` are unchanged; the doc already documents both new surfaces).

#### Commit and Push Post-Phase Changes

Read the commit policy and commit/push any residual doc updates (PHASES.md Implementation Notes / status flip) to `main`.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point during execution:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed work** that hasn't been committed yet.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** adhoc-stale-state-script-parity-test-fixtures Phase 1
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

4. **Do not attempt to work around the blocker.**

Blocking issues include: a quality-gate failure that can't be fixed after 2 retry attempts; a git push conflict that can't be resolved by rebase; any error requiring architectural decisions beyond the spec. (Unlikely here — the fix is a deterministic test-fixture edit mechanically determined by the audit's required-token set.)

---

## Completion

When Phase 1 is complete:

1. **Run the full quality-gate suite one final time:** `python -m pytest user/scripts/ -q` → `0 failed`, and `python user/scripts/lazy_parity_audit.py --repo-root .` → exit 0.
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Features implemented:** adhoc-stale-state-script-parity-test-fixtures (bug fix)
   **Total phases completed:** 1
   **Total commits:** M
   **Final quality-gate status:** all green (`0 failed`); real-repo parity audit exit 0

   **Commit log:**
   | Commit | Feature | Phase | Title |
   |--------|---------|-------|-------|
   | … | adhoc-stale-state-script-parity-test-fixtures | P1 | Update the three `TestStateScriptParity` fixtures to the full four-surface token set |

   **Implementation Notes summary:** the three synthetic `TestStateScriptParity` fixtures in `user/scripts/test_lazy_parity.py` now carry the full four-surface token set (binding + `--reorder-queue` + `--reassert-owner` + the `requires_host` fail-fast pair); the clean fixture returns `[]` and each fires-when-missing test still fires `len == 1` on its own dropped surface. No production `lazy_parity_audit.py` / state-script change (real-repo audit stays exit 0). Status left `In-progress` — the validation tail (`/mcp-test` skip → `__mark_fixed__`) is the orchestrator's, not this plan's.
