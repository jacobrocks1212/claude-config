---
kind: implementation-plan
feature_id: research-prompt-leaks-identity-doc-meta
status: Ready
created: 2026-06-25
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` (via `/plan-bug`) on 2026-06-25.
> To execute: `/execute-plan docs/bugs/research-prompt-leaks-identity-doc-meta/plans/all-phases-strip-identity-preamble.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — research-prompt-leaks-identity-doc-meta  (v2)

**PHASES.md files:**
- `docs/bugs/research-prompt-leaks-identity-doc-meta/PHASES.md` (research-prompt-leaks-identity-doc-meta, 1 phase)

**SPEC.md files:**
- `docs/bugs/research-prompt-leaks-identity-doc-meta/SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.ts`, `.js`, `.cs`, `.vue`, `.py`, `.rs`, `.tsx`, `.jsx`, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **NOTE on this plan's targets:** the only file this plan modifies is `user/skills/spec/SKILL.md` — Markdown skill *prose*, not source/test code. The HARD-CONSTRAINT file-extension list (`.ts/.js/.cs/.vue/.py/.rs/.tsx/.jsx`/test files) does NOT include `.md` skill prose. A skill-prose edit is the orchestrator's own to make directly (it is in the same class as `PHASES.md`/`CLAUDE.md` edits the orchestrator is explicitly allowed to make). If the executing session is running INLINE (no `Agent` tool available), perform the edits inline with `Edit`. The mechanical verification (`project-skills.py` + `lint-skills.py`) is still mandatory regardless.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against. The executing session SHOULD `Read` these before starting any work unit whose Implementation goal references the corresponding upstream contract. Listed in dependency order.

(none — this plan has no completed hard upstream dependencies)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines). **(This plan's only file is `.md` skill prose, which is orchestrator-editable directly — and an INLINE executing session with no `Agent` tool performs all edits inline; see the EXECUTION MODEL note.)**
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents
3. Every TDD work unit goes through the test-first pipeline — dedicated test agent writes failing tests, dedicated implementation agent makes them pass. **(No executable test harness exists for skill prose — see each WU's TDD field; verification is `project-skills.py` + `lint-skills.py` + a documented trace-through.)**
4. PHASES.md is updated AFTER EACH batch completes (not deferred)
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing
6. Mistakes are fixed immediately before launching the next batch
7. After all batches in a phase finish, integration verification confirms all changes work together
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it
9. Each completed phase is committed and pushed before the next phase begins
10. Cross-feature phases may run in parallel when dependencies are satisfied and no file conflicts exist
11. This plan is self-contained — follow it exactly as written without relying on external context
12. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | research-prompt-leaks-identity-doc-meta | P1 | Strip the identity doc's self-describing preamble in `/spec` Phase 2 | — | Solo |

## Work Units

- [ ] WU-1 — Add the preamble-strip sub-step to `/spec` Phase 2 step 2 (convergence line)
- [ ] WU-2 — Amend step 4's line-433 whitelist so it no longer exempts the self-describing preamble

---

### Phase: research-prompt-leaks-identity-doc-meta P1 — Strip the identity doc's self-describing preamble in `/spec` Phase 2

**Goal:** Make `/spec` Phase 2 strip a resolved identity doc's leading self-describing preamble (self-label H1 + provenance blockquote run) before it becomes the `## Project context` prepend, and amend step 4 so its whitelist no longer exempts that meta.

**Entry criteria:** None (first and only phase). SPEC investigation is Concluded; root cause + fix scope proven.

**SPEC.md references:** `## Proven Findings` (Root cause + Decided fix + Where the fix lands), `## Affected Area`, `## Open Questions` (strip-scope settled uniform in PHASES.md `## Notes on Scope`).

#### WU-1 — Add the preamble-strip sub-step to `/spec` Phase 2 step 2

- **Scope:** PHASES.md Phase 1 deliverable: "Add a preamble-strip sub-step to `user/skills/spec/SKILL.md` Phase 2 step 2, applied at the convergence line … so it covers all three resolution branches."
- **TDD:** no — skill-prose edit; no executable test harness exists. Verified by `project-skills.py` re-projection + `lint-skills.py` + the documented strip-contract trace-through (see Testing Strategy).
- **Files to create/modify:**
  - `user/skills/spec/SKILL.md` — Phase 2 step 2. Insert the preamble-strip sub-step at the convergence line `Whichever file is used, treat its contents as the identity prepend below.` [VERIFY: grep -n "treat its contents as the identity prepend below" user/skills/spec/SKILL.md] (currently L394). The strip must be expressed as harness instruction prose (house style — lenient/heuristic, not a brittle exact-string regex), removing from the TOP of the resolved content ONLY: (a) a leading H1 (`# …`) that self-labels the artifact (heuristic phrase match on identity-summary / Gemini-prepend / prepend self-reference); and (b) the immediately-following contiguous run of blockquote (`> …`) lines (provenance / regeneration / maintainer notes). Strip stops at the first non-blockquote, non-blank line — everything from the first substantive section (e.g. `## What AlgoBooth is`) onward is preserved verbatim. State explicitly that the strip is BOUNDED to the leading preamble and must NOT scan deeper (a legitimate later blockquote inside substantive content is never collateral-stripped), and that it applies AT the convergence point (after step-2 resolution) so all three branches — verbatim summary, verbatim full doc, and the case-2 self-heal condensation — are covered uniformly.
- **Test files:** none (no executable test surface for skill prose).
- **Implementation goal:** After this edit, reading Phase 2 step 2 shows an unambiguous preamble-strip sub-step at the convergence line that, applied to the SPEC's documented AlgoBooth example (H1 `# AlgoBooth — Identity Summary (Gemini Prepend)` + three provenance blockquotes + `## What AlgoBooth is …`), yields a prepend that begins at `## What AlgoBooth is` with the H1 and the three blockquotes removed.
- **Spec requirements:** SPEC `## Proven Findings` → "Decided fix" (Seam = harness-side strip in `/spec`; Strip boundary = self-label H1 + provenance blockquotes) and "Where the fix lands" (add a preamble-strip sub-step to step 2). Strip-scope = uniform at the convergence point (PHASES.md `## Notes on Scope`, ⚖ D7).
- **Batch:** 1

#### WU-2 — Amend step 4's line-433 whitelist so it no longer exempts the self-describing preamble

- **Scope:** PHASES.md Phase 1 deliverable: "Amend step 4's line-433 whitelist … so it no longer blanket-exempts the prepend's self-describing meta."
- **TDD:** no — skill-prose edit; verified by re-projection + lint + trace-through.
- **Files to create/modify:**
  - `user/skills/spec/SKILL.md` — Phase 2 step 4. Amend the whitelist sentence `The `## Project context` identity prepend and the structured prompt sections (below) ARE legitimate prompt content and stay.` [VERIFY: grep -n "ARE legitimate prompt content and stay" user/skills/spec/SKILL.md] (currently L433) so it: (1) keeps affirming that the prepend's SUBSTANTIVE identity content and the structured prompt sections are legitimate prompt content; AND (2) clarifies that the identity artifact's self-describing preamble (self-label H1 + provenance blockquotes) is the SAME class of meta-fluff the step-4 "No meta-fluff in the prompt body (HARD)" rule already bans, and is stripped in step 2 (WU-1) — closing the asymmetry the SPEC Evidence calls out (step 4 banned meta in the body but exempted it in the prepend). Keep the existing step-4 body meta-fluff rule (the `> Combined with …` ban + operator/tool-metadata-header ban) intact.
- **Test files:** none.
- **Implementation goal:** After this edit, step 4's whitelist no longer reads as a blanket exemption of the entire prepend; it scopes the exemption to substantive identity content and explicitly defers the self-describing preamble to the step-2 strip. The body-vs-prepend asymmetry from the SPEC Evidence is gone.
- **Spec requirements:** SPEC `## Evidence Collected` → "The asymmetry" (step-4 body rule bans meta-fluff but L433 exempts the prepend); `## Proven Findings` → "Where the fix lands" ("amend step 4's line-433 whitelist so it no longer blanket-exempts the prepend's self-describing meta").
- **Batch:** 1

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2 | No (same file — apply sequentially: WU-1 then WU-2) | Both edit `user/skills/spec/SKILL.md` (distinct, non-overlapping regions: step 2 ~L394 and step 4 ~L433) |

**Testing Strategy (phase-level):**
- Mechanical: after both WUs land, run `python ~/.claude/scripts/project-skills.py` then `python ~/.claude/scripts/lint-skills.py` — confirm no broken `!cat` injections / embedded-pattern regressions and a clean lint over the edited skill. Spot-check the projected `/spec` SKILL.md.
- Semantic trace-through: against the SPEC's documented AlgoBooth preamble (H1 + three provenance blockquotes + `## What AlgoBooth is …`), confirm the WU-1 strip prose removes the H1 + the three blockquotes and keeps everything from `## What AlgoBooth is` onward; confirm WU-2's whitelist no longer exempts that preamble. This is the verification surface — there is no executable test for skill prose.

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** Phase 1 is the only phase and has no prerequisites — it is ready immediately.
2. **Announce:** Print "Implementing research-prompt-leaks-identity-doc-meta Phase 1: Strip the identity doc's self-describing preamble in `/spec` Phase 2"
3. **Review prior context:** Re-read PHASES.md and SPEC.md from disk (no prior phases exist).
4. **Execute the single batch** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "research-prompt-leaks-identity-doc-meta Phase 1 — committed as [hash]"
7. **Loop:** No further phases. Proceed to Completion.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly.
It defines: task tool loading via ToolSearch, task creation for all work units, and the update protocol for tracking progress through test and implementation phases.

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow its instructions.
Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and the plan file itself. Also re-read `user/skills/spec/SKILL.md` Phase 2 (steps 2 & 4) in full before editing — the line numbers in this plan (L394, L433) are approximate anchors; re-locate the exact convergence/whitelist sentences by their VERIFY greps before editing.

#### Step B.1: Launch Subagents (or apply inline)

**PRE-FLIGHT CHECK:** This batch's only file is `user/skills/spec/SKILL.md` — Markdown skill prose, NOT source/test code. Per the EXECUTION MODEL note, skill-prose edits are orchestrator-editable directly (same class as `PHASES.md`/`CLAUDE.md`), and an INLINE executing session with no `Agent` tool performs all edits inline. Apply WU-1 then WU-2 (sequential — same file). If dispatching a subagent, give it the full strip contract from WU-1/WU-2 verbatim.

Read (if dispatching subagents):
1. `~/.claude/skills/_components/tdd-protocol.md` — TDD decision gate (both WUs are TDD: no — skill prose).
2. `~/.claude/skills/_components/subagent-launch.md`
3. `~/.claude/skills/_components/tdd-test-agent.md`
4. `~/.claude/skills/_components/implementation-agent.md`

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` and follow its complete protocol. Also read `~/.claude/skills/_components/mount-site-verification.md`.
Review focus for this batch: (a) the strip is bounded to the LEADING preamble only (no deep scan); (b) substantive identity content from the first `## …` section onward is preserved; (c) step 4's whitelist no longer blanket-exempts the preamble; (d) the existing step-4 body meta-fluff rule is intact; (e) the strip is placed at the step-2 convergence point so all three resolution branches are covered.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md` and follow its instructions.
Check off the two Phase 1 implementation deliverables and the Tests deliverable; add an Implementation Notes block (date, what changed in step 2 + step 4, the strip-boundary decision, files modified). Leave the `**Runtime Verification**` trace-through row for manual confirmation if it cannot be mechanically closed.

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/quality-gates.md` and `.claude/skill-config/quality-gates.md`.
For this repo the gate is the skills lint chain:
```
python ~/.claude/scripts/project-skills.py
python ~/.claude/scripts/lint-skills.py
```
Both must complete clean (no broken injections / embedded-pattern errors over the edited skill) before proceeding.

#### Step B.4.5: MCP Integration Test (BLOCKING — if applicable)

Read `~/.claude/skills/_components/mcp/mcp-integration-test.md` to determine applicability.
**Not applicable** — PHASES.md declares `**MCP runtime:** not-required` (docs/prose harness edit, no app surface). Skip with a note.

#### Step B.5: Commit Batch

Read `.claude/skill-config/commit-policy.md` and follow it. Conventional Commits, no AI attribution, push allowed (claude-config is a personal config repo). Before committing, regenerate `LAZY_QUEUE.md` per the commit policy (`python user/scripts/lazy-queue-doc.py --repo-root <repo>`) so it rides the commit. Re-assert `git rev-parse --abbrev-ref HEAD` == `main` before each commit/push.

#### Step B.6: Proceed to Next Batch

**Checklist before proceeding (all must be true):**
- [ ] Review report produced with PASS/PASS-WITH-FIXES/NEEDS-REWORK verdict
- [ ] PHASES.md updated with completed deliverables and implementation notes
- [ ] All quality gates pass (`project-skills.py` + `lint-skills.py` clean)
- [ ] Step B.5 completed (commit + push per commit policy)

If any item is unchecked, go back and complete it. No further batch — proceed to Post-Phase Steps.

### Propagation Awareness Note

This batch introduces no import indirection or struct-field changes (it is skill-prose only) — no propagation step required.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md`.
Confirm the projected `/spec` SKILL.md (`~/.claude/skills-projected/_default/spec/SKILL.md` or the per-repo projection) reflects both edits coherently, and that the strip sub-step (step 2) and the amended whitelist (step 4) are mutually consistent (step 4 defers to the step-2 strip).

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md`.
Assess whether `user/skills/CLAUDE.md` or the root `CLAUDE.md` needs a note about the new strip behavior. Likely a no-op (the change is internal to one skill's Phase 2 prose, not a new contract surface), but confirm rather than assume.

#### Commit and Push Post-Phase Changes

Read `.claude/skill-config/commit-policy.md` and follow it. Commit any post-phase doc updates (PHASES.md Implementation Notes, CLAUDE.md if touched) and push to `main`.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point during execution:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed work** that hasn't been committed yet.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** research-prompt-leaks-identity-doc-meta Phase 1
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining phases (not attempted):**
   - [list]

4. **Do not attempt to work around the blocker.** The user provides a resolution and triggers autonomous implementation after.

Blocking issues include:
- The convergence-line or whitelist anchor can no longer be located (skill was restructured) — re-grep; if truly absent, BLOCK rather than guess a new insertion point.
- A `lint-skills.py` / `project-skills.py` failure that can't be fixed after 2 retry attempts.
- A git push conflict that can't be resolved by rebase.
- Any error that would require design decisions beyond the SPEC's proven fix scope.

---

## Completion

When all phases in the Execution Schedule are complete:

1. **Run the skills lint chain one final time** (`project-skills.py` + `lint-skills.py`) — confirm clean.
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Features implemented:** research-prompt-leaks-identity-doc-meta (bug fix)
   **Total phases completed:** 1
   **Total commits:** M
   **Final quality-gate status:** all green (skills lint clean)

   **Commit log:**
   | Commit | Feature | Phase | Title |
   |--------|---------|-------|-------|
   | abc1234 | research-prompt-leaks-identity-doc-meta | P1 | Strip the identity doc's self-describing preamble in `/spec` Phase 2 |

   **Implementation Notes summary:**
   The strip lands at the `/spec` Phase 2 step-2 convergence line (covers all three identity-resolution branches incl. the self-heal condensation); step 4's whitelist now scopes its exemption to substantive identity content and defers the self-describing preamble to the step-2 strip. Bounded to the leading preamble only. Top-level PHASES `**Status:**` left for the orchestrator's `__mark_fixed__` gate — DO NOT flip it here; validation tail (/mcp-test → coverage audit) is owned by the state machine.
