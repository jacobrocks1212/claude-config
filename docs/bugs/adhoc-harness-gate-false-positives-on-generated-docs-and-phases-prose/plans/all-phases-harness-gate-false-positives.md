---
kind: fix-plan
feature_id: adhoc-harness-gate-false-positives-on-generated-docs-and-phases-prose
status: Complete
created: 2026-07-19
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan docs/bugs/adhoc-harness-gate-false-positives-on-generated-docs-and-phases-prose/plans/all-phases-harness-gate-false-positives.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — harness-gate.py false-positives on generated docs + PHASES.md prose (v2)

**PHASES.md files:**
- `docs/bugs/adhoc-harness-gate-false-positives-on-generated-docs-and-phases-prose/PHASES.md` (2 phases)

**SPEC.md files:**
- `docs/bugs/adhoc-harness-gate-false-positives-on-generated-docs-and-phases-prose/SPEC.md`

**Total phases:** 2
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is `claude-config`, a harness-config repo whose gates are Python, not `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes

- **Repo:** `claude-config` (`C:/Users/Jacob/source/repos/claude-config`). Work branch: `main`. Commit + push to `main` after each phase completes (personal config repo — push after every commit, per the repo's HARD push policy).
- **Quality gate (this repo, non-default):** the authoritative gate for this fix is the harness-gate unit suite:
  ```bash
  python3 user/scripts/test_harness_gate.py
  ```
  It runs in well under the Bash cap — run it FOREGROUND, never backgrounded. Additionally run the repo lint before commit:
  ```bash
  python3 user/scripts/lint-skills.py
  python3 user/scripts/doc-drift-lint.py --repo-root .
  ```
  There is no MSBuild/nx/Tauri gate in this repo. `MCP runtime: not-required` (per PHASES.md) — do NOT boot any runtime; there is no MCP surface to validate.
- **TDD is real here:** each WU writes a FAILING fixture first (red — the FP currently fires / the scope filter is absent), then implements until green. The in-file harness at `user/scripts/test_harness_gate.py` is the test home — mirror the existing `Hk`-double / hunk-fixture shape the SPEC Reproduction Steps use.
- **Inline execution:** this is a small 2-WU mechanical-adjacent fix in one file + its test file. `/execute-plan` on the workstation runs inline (Opus) — writing the failing fixture before implementing each WU is still mandatory.

## References — Upstream Artifacts

(none — this bug has no completed hard upstream dependencies.)

## Touchpoint Audit (verified inline — dispatch unavailable in cycle subagent)

`verified: inline (dispatch unavailable)` — grounded during `/spec-phases` and re-carried here.

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `user/scripts/harness-gate.py` | yes | `run_checker` (:373; `hits = scope_hits(...)` :375 used ONLY for `in_scope`; `hunks = parse_diff(diff_text)` :383 unfiltered; detectors at :384-385), `scope_hits` (:166, POSIX-normalized `changed ∩ globs`), `parse_diff` (:188), `_Hunk` (:178; `.file`/`.added`/`.removed`/`.added_ctx`), `detect_overfit` (:230; case (a) `if "\|" in body and _ALTERNATION_ADD_RE.search(body) and _QUOTED_RE.search(body)` :236), `detect_gate_weakening` (:255), `_ALTERNATION_ADD_RE` (:84), `_QUOTED_RE` (:82) | refactor | WU-1: filter `hunks` to `h.file ∈ set(hits)` inside `run_checker` right after :383, reusing `scope_hits` output (manifest SSOT); normalize `h.file` with `.replace("\\","/")`. WU-2: tighten `detect_overfit` case (a) so a shell `\|\|`/pipe breadcrumb is not read as a regex-alternation append. |
| `user/scripts/test_harness_gate.py` | yes (39 `def test_` fns) | in-file fixture harness (`Hk`-shaped hunk doubles) | modify | Add S1/S2/S3 off-manifest + positive-control fixtures (WU-1) and the S4 shell-breadcrumb + genuine-alternation positive-control fixtures (WU-2). |

**Contradiction check:** none. Both fix sites are ON the traced serving path (SPEC Theory 1 & 2, both Confirmed/`traced`). No SPEC premise falsified — no halt.

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | harness-gate-fp | P1 | Scope detector input to manifest control-surface hunks (S1/S2/S3) | — | Solo (Batch 1) |
| 2    | harness-gate-fp | P2 | Precision-tighten overfit alternation heuristic (S4) | P1 (same files — sequential) | Solo (Batch 2) |

> Both phases modify `user/scripts/harness-gate.py` + `user/scripts/test_harness_gate.py`, so they run in **sequential batches** (file-overlap rule), one WU each. Commit + push after each phase.

## Work Units

- [x] WU-1 — Scope `run_checker` detector input to manifest control-surface hunks (S1/S2/S3)
- [x] WU-2 — Tighten `detect_overfit` alternation heuristic against shell breadcrumb pipes (S4)

---

### Phase: harness-gate-fp P1 — Scope detector input to manifest control-surface hunks (S1/S2/S3)

**Goal:** Filter the parsed diff hunks to only manifest control-surface files before the structural detectors run, so off-manifest generated docs / PHASES.md prose / unrelated SPEC.md files swept into a range are never inspected.
**Entry criteria:** none (first phase).
**SPEC.md references:** `## Evidence Collected` → "Root cause is a scope mismatch in `run_checker`"; Theory 1 (PRIMARY, Confirmed/`traced`); `## Affected Area` row "Detector scope gate"; Open Question "Scoping mechanism" (recommend manifest membership).

#### WU-1 — Scope run_checker detector input to manifest control-surface hunks

- **Scope:** PHASES.md Phase 1 deliverables — filter `hunks` to `h.file ∈ scope_hits` in `run_checker` before `detect_overfit`/`detect_gate_weakening`; preserve the `in_scope` verdict shape; add S1/S2/S3 off-manifest regression fixtures + a positive control.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/harness-gate.py` — `run_checker` (:373-407): after `hunks = parse_diff(diff_text)` (:383), narrow to in-scope hunks. `[VERIFY: grep -n "def run_checker" user/scripts/harness-gate.py]` `[VERIFY: grep -n "def scope_hits" user/scripts/harness-gate.py]`
  - `user/scripts/test_harness_gate.py` — new S1/S2/S3 off-manifest fixtures + positive control. `[VERIFY: grep -c "def test_" user/scripts/test_harness_gate.py]`
- **Test files:** `user/scripts/test_harness_gate.py` (in-file harness).
- **Test expectations (write these RED first):**
  1. **S1 off-manifest** — a diff where an ON-manifest file (e.g. a `user/hooks/*.sh` or `user/scripts/lazy*`) is changed (so `in_scope: True`) AND `LAZY_QUEUE.md` carries `## Bugs (17)`→`## Bugs (16)`: `run_checker(...)`'s `gate_weakening` result is `pass` (the off-manifest renumber is NOT inspected).
  2. **S2 off-manifest** — same in-scope setup + `docs/bugs/x/PHASES.md` prose line mentioning `BUILD_QUEUE_BYPASS=1`: `gate_weakening` result `pass`.
  3. **S3 off-manifest** — same in-scope setup + `docs/bugs/unrelated/SPEC.md` line citing another slug's `docs/bugs/<slug>` path: `overfit` result `pass`.
  4. **Positive control** — an ON-manifest file (e.g. `user/hooks/foo.sh`) carrying a `*_BYPASS` line STILL produces `gate_weakening: hit` through `run_checker` (the scoping filter narrows input, it does not disarm detectors).
  Each fixture drives the FULL `run_checker` pipeline (manifest-scope → filter → detectors) against a temp manifest/diff. Reuse the SPEC Reproduction Steps' hunk shapes. RED reason: without the filter, off-manifest hunks reach the detectors and fire.
- **Implementation goal:** In `run_checker`, immediately after `hunks = parse_diff(diff_text)`, compute `hits_set = {h.replace("\\", "/") for h in hits}` and reassign `hunks = [h for h in hunks if h.file.replace("\\", "/") in hits_set]`, then pass the filtered list to `detect_overfit`/`detect_gate_weakening`. `hits` already excludes off-manifest paths (`scope_hits` — the manifest-membership SSOT, no new is-code heuristic). Leave `detect_tautology` (reads the SPEC dir, not hunks) and the `in_scope`/`scope_hit` verdict fields untouched.
- **Spec requirements:** SPEC `## Evidence Collected` (the `:375` scope-only / `:383-385` unfiltered trace) and `## Proven Findings` ("filtering detector input to hunks whose `h.file ∈ hits` resolves S1, S2, S3"). Scoping mechanism = manifest membership (Open Question).
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | None            |

**Post-phase:** run `python3 user/scripts/test_harness_gate.py` (green) + `python3 user/scripts/lint-skills.py` + `python3 user/scripts/doc-drift-lint.py --repo-root .`; tick WU-1 + Phase 1 deliverables; commit + push to `main`.

---

### Phase: harness-gate-fp P2 — Precision-tighten overfit alternation heuristic (S4)

**Goal:** Stop `detect_overfit` case (a) from misreading a shell `||`/pipe fail-open breadcrumb line in an ON-manifest hook as a regex-alternation matcher append, without over-narrowing the detector's genuine coverage.
**Entry criteria:** Phase 1 complete (same files — sequential batch; no logic dependency, but single-writer file discipline).
**SPEC.md references:** Theory 2 (SECONDARY, Confirmed/`traced`); `## Affected Area` row "overfit alternation heuristic"; Open Question FP3/S4 (⚖ recommend B — complete; guard against over-narrowing).

#### WU-2 — Tighten detect_overfit alternation heuristic against shell breadcrumb pipes

- **Scope:** PHASES.md Phase 2 deliverables — tighten `detect_overfit` case (a) so shell `||`/pipe breadcrumb lines don't flag; add S4 shell-breadcrumb regression fixture + a genuine-alternation positive control.
- **TDD:** yes.
- **Files to create/modify:**
  - `user/scripts/harness-gate.py` — `detect_overfit` case (a) at :236 / `_ALTERNATION_ADD_RE` at :84. `[VERIFY: grep -n "_ALTERNATION_ADD_RE" user/scripts/harness-gate.py]` `[VERIFY: grep -n "def detect_overfit" user/scripts/harness-gate.py]`
  - `user/scripts/test_harness_gate.py` — S4 shell-breadcrumb fixture + genuine-alternation positive control.
- **Test files:** `user/scripts/test_harness_gate.py`.
- **Test expectations (write these RED first):**
  1. **S4 shell breadcrumb** — `detect_overfit([Hk('user/hooks/foo.sh', ['  _HOOK_NOPY_TS="$(date +%s 2>/dev/null || echo 0)"'])])['result'] == 'pass'` (currently `flag`; this is the RED). An on-manifest hook fail-open breadcrumb is not a matcher-alternation append.
  2. **Genuine-alternation positive control** — a real regex-alternation matcher append (a quoted literal added into an actual `|`-alternation matcher line, e.g. an added `+    r"...|'new_slug'..."` regex-literal fragment) STILL returns `detect_overfit(...)['result'] == 'flag'`. This guards against over-narrowing (SPEC FP3/S4 Open Question). This fixture must stay GREEN both before and after the change (it asserts the detector's real coverage is preserved).
- **Implementation goal:** Refine case (a) (`:236`) so it does not fire on shell logical-OR/pipe shapes. Prefer keying the match on the genuine regex-alternation tell — the `|`-fragment sits inside a regex/matcher-literal context (an alternation between quoted alternatives) — and exclude the shell-operator shapes: `||`, a ` | ` pipe between commands, `2>/dev/null`, and `$( … )` command substitution. Keep the change minimal and structural (a tightened `_ALTERNATION_ADD_RE` and/or an added shell-shape guard clause). Do NOT weaken detection of a literal genuinely appended to a `re.compile`/alternation string — the positive-control fixture is the mechanical guard.
- **Spec requirements:** SPEC Theory 2 (`detect_overfit:236`; fix site = `_ALTERNATION_ADD_RE` heuristic, on-path) and `## Proven Findings` ("S4 needs a detector-precision change to the overfit alternation heuristic"). Open Question FP3/S4 ⚖ recommends B (complete) + over-narrowing guard.
- **Batch:** 2 (sequential — file overlap with WU-1).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 2     | WU-2      | Solo      | Overlaps WU-1 files → separate batch |

**Post-phase:** run `python3 user/scripts/test_harness_gate.py` (green, all four SPEC symptoms pinned) + `python3 user/scripts/lint-skills.py` + `python3 user/scripts/doc-drift-lint.py --repo-root .`; tick WU-2 + Phase 2 deliverables; set the PHASES.md top-level `**Status:**` to `In-progress` (implementation done, validation pending — never `Fixed`); commit + push to `main`.

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md **Status:** to Fixed, writes `FIXED.md`, and archives the bug dir after the validation tail — never `/execute-plan`.
