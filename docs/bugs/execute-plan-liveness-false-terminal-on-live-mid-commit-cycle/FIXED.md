---
kind: fixed
feature_id: execute-plan-liveness-false-terminal-on-live-mid-commit-cycle
date: 2026-07-20
provenance: backfilled-unverified
validated_via: pytest tests/test_lazy_core/ 1341/1341; test_execute_plan_liveness.py 12/12 (incl. Complete=>commit-pending fresh+stale regression tests); test_hooks.py 286/286; lazy-state.py --test + bug-state.py --test green; generate-coupled-skills.py --check byte-identical; bug-state.py --fsck ok; harness-gate.py overfit+gate_weakening pass; NOT pipeline-gated (fixed OUT-OF-PIPELINE via a harden commit)
auto_ticked_rows: 0
---

# Completion Receipt

`execute-plan-liveness-false-terminal-on-live-mid-commit-cycle` fixed OUT-OF-PIPELINE
by `/harden-harness` Round 130 (2026-07-20). Fix commit: `c297663f`
(`harden(script): route plan-Complete /execute-plan cycle through liveness probe
(commit-pending verdict)`). Bug spec commit: `4576633e`. This receipt was written by
the dispatched harden subagent, not the bug pipeline's `__mark_fixed__` gate —
provenance is `backfilled-unverified`.

## Notes

Direct OPPOSITE-DIRECTION companion to `execute-plan-liveness-blind-to-dead-lineage-stall`
(Round 129, `3f2294a5`). The Step 1e.4a discriminator (`lazy_core.execute_plan_liveness`)
returned `verdict: terminal` whenever `plan_status == Complete`, with no
lineage-liveness gate. But `/execute-plan` flips `status: Complete` at its FINALIZE
step — BEFORE the gate + the atomic commit + the marker removal — so `{marker present
+ Complete}` is the finalize→commit window, during which the cycle may be ALIVE
mid-commit. A direct `terminal` routed the orchestrator straight to a recovery
dispatch with no liveness confirmation, colliding one-writer with the still-live
cycle (LIVE INCIDENT 2026-07-20, `inspector-track-dashboard` part-2, cycle
`ac4df7fdc75a91be6`: 15 files written concurrently; converged only because the
git-safety/FIFO layer serialized both writers to the identical commit `dba37eafa`).

Fix: the `status == Complete` branch now returns the new confirm-then-recover
candidate verdict `commit-pending` (mirrors Round 129's `wedge-candidate` for the
opposite direction). SKILL Step 1e.4a (canonical `lazy-batch` + coupled
`lazy-bug-batch`, regenerated byte-identical) routes `commit-pending` to the SAME
`TaskList` lineage probe as `wedge-candidate`: lineage DEAD ⇒ route recovery; still
LIVE ⇒ treat as `paused` (wait — mid-commit). Marker-ABSENT and read-error stay
DIRECT `terminal` (safe / fail-safe), so marker mtime alone never drives recovery.
`commit-pending` never tears down the marker (decision #12 stays parked), so a
false-positive on a legit long mid-commit cycle is SAFE.

The deeper structural root (gap 2 — the write-once marker mtime is not a true
liveness signal; each live/dead corner is patched branch-by-branch) is spun off to
`adhoc-execute-plan-liveness-no-true-liveness-signal` (over-fit signal 2: 2nd
consecutive round on this discriminator).

## Verification

`pytest tests/test_lazy_core/` green (1341/1341). `test_execute_plan_liveness.py`
12/12 — the two prior `Complete ⇒ terminal` tests updated to assert
`commit-pending`, covering fresh AND stale Complete markers (completion routes to
the probe regardless of marker age); the `paused`/`wedge-candidate`/fresh-marker
regression tests unchanged. `test_hooks.py` 286/286; `lazy-state.py --test` +
`bug-state.py --test` green; `generate-coupled-skills.py --check` byte-identical;
`bug-state.py --fsck` ok; `harness-gate.py` `overfit: pass`, `gate_weakening: pass`.
