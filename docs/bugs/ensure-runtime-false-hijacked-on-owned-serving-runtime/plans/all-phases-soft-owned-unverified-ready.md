---
kind: implementation-plan
feature_id: ensure-runtime-false-hijacked-on-owned-serving-runtime
status: Ready
created: 2026-06-24
phases: [1, 2]
complexity: complex
---

> **Mobile plan** — generated by `/write-plan` (via `/plan-bug`) on 2026-06-24.
> To execute: `/execute-plan docs/bugs/ensure-runtime-false-hijacked-on-owned-serving-runtime/plans/all-phases-soft-owned-unverified-ready.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — ensure-runtime false HIJACKED on owned, serving runtime  (v2)

**PHASES.md files:**
- `docs/bugs/ensure-runtime-false-hijacked-on-owned-serving-runtime/PHASES.md` (1 bug, 2 phases)

**SPEC.md files:**
- `docs/bugs/ensure-runtime-false-hijacked-on-owned-serving-runtime/SPEC.md`

**Total phases:** 2
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py`, `.ts`, `.js`, `.cs`, `.vue`, `.rs`, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **Inline-execution override (claude-config bug pipeline):** when this plan is executed by a cycle subagent that has NO `Agent` tool (the `/lazy-bug-batch` dispatch case), perform ALL implementation INLINE with `Read`/`Edit`/`Write` — but STILL test-first (write the failing `test_lazy_core.py` fixture BEFORE the `lazy_core.py` guard) and still run every gate below. The "delegate to Sonnet" model applies only when the `Agent` tool is actually available.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `.claude/skill-config/quality-gates.md` (fallback: `~/.claude/skills/_components/quality-gates.md`) |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this bug has no completed hard upstream dependencies. Prior-art ensure-runtime bugs are archived under `docs/bugs/_archive/` and cross-linked in the SPEC's `**Related:**` line for context, not as plan dependencies.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — UNLESS executing under the inline-execution override above (no `Agent` tool), in which case perform it inline but still test-first. The orchestrating session otherwise MUST NOT call `Edit`/`Write` on source/test files.
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline — failing tests written BEFORE implementation.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in a phase finish, integration verification confirms all changes work together.
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it (here: `user/scripts/CLAUDE.md` `--ensure-runtime` doc — folded into Phase 2 WU-5).
9. Each completed phase is committed and pushed before the next phase begins.
10. Cross-feature phases may run in parallel when dependencies are satisfied and no file conflicts exist (N/A here — Phase 2 depends on Phase 1).
11. This plan is self-contained — follow it exactly as written without relying on external context.
12. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.

## Execution Schedule

| Step | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|----------|----------|------------|-----------|
| 1    | P1 | Soft owned-unverified READY classifier + hermetic fixtures | — | Solo |
| 2    | P2 | Consumer-routing prose — proceed on soft-READY (retire workaround) | P1 | Solo |

## Work Units

- [ ] WU-1 — Failing fixtures: soft-READY + foreign/dead/non-200/no-MCP/stale twins (test_lazy_core.py)
- [ ] WU-2 — Soft owned-unverified READY guard in `_ensure_runtime_m4` (lazy_core.py)
- [ ] WU-3 — `/lazy-batch` Step 1d.0 routing prose: proceed on soft-READY, retire workaround
- [ ] WU-4 — cycle-base-prompt mirror (if it restates the gate) — else record no-op
- [ ] WU-5 — `user/scripts/CLAUDE.md` `--ensure-runtime` doc sentence + re-project skills

---

### Phase: P1 — Soft owned-unverified READY classifier + hermetic fixtures

**Goal:** Relax `_ensure_runtime_m4`'s `not owned` strict-HIJACKED fail-safe with a narrow soft owned-unverified READY guard so a self-booted, serving runtime that diverges ONLY on the session component classifies READY (proceed), while the genuine-foreign case stays terminal HIJACKED (never SIGKILL — LD3).
**Entry criteria:** None (first phase).
**SPEC.md references:** `## Affected Area` (M4 ownership classifier row), `## Proven Findings`, Theory 1, Open Questions ("which side diverges", "Boot-stamp interaction").

#### WU-1 — Failing fixtures (TEST-FIRST)

- **Scope:** PHASES.md P1 deliverables — the soft-READY fixture + the foreign / dead-PID / non-200 / no-MCP-tools / stale twins. Authored and confirmed RED before WU-2.
- **TDD:** yes.
- **Test files:** `user/scripts/test_lazy_core.py` (append fixtures alongside the existing `ensure_runtime` / `_ensure_runtime_m4` tests).
- **Test expectations:**
  - `test_ensure_runtime_owned_unverified_serving_is_soft_ready` — lock dict with `pid`, `start_time=S`, `controller_session_id="sess-A"`; `live_session_id="sess-B"` (session diverges); injected `kernel_start_time_fn(pid) -> S` (process start_time MATCHES recorded → only session diverges, `verify_runtime_ownership` False); `probe -> (200, payload-with-asserted-MCP-tool)`; `stale_check -> False`. Assert the verdict dict: `state == "READY"`, `ownership_verified is False`, `terminal_blocker is None`, `mcp_tools_present is True`. Drive through `ensure_runtime(...)` / `_ensure_runtime_m4(...)` using the existing injection seams — NOT a hand-call of a private guard. This fixture is RED pre-WU-2 (pre-change returns `state == "HIJACKED"`).
  - `test_ensure_runtime_foreign_live_pid_stays_hijacked` — same lock/session divergence, but `kernel_start_time_fn(pid) -> S2 != S` (live process is a DIFFERENT process via PID reuse). Assert `state == "HIJACKED"`, `terminal_blocker` == `_hijacked_blocker(lock)` text (unchanged fail-safe; never SIGKILL). [GREEN both before and after — a regression guard.]
  - `test_ensure_runtime_dead_pid_stays_dead` — `kernel_start_time_fn(pid) -> None`. Assert the verdict routes to DEAD recovery (unchanged). [GREEN both before/after.]
  - `test_ensure_runtime_owned_unverified_non_200_not_soft_ready` — session-only-divergent + matching PID but `probe -> (503, ...)`. Assert the soft-READY does NOT fire (routes through the non-serving path; NOT a spurious READY). [GREEN both before/after — proves the guard is 200-gated.]
  - `test_ensure_runtime_owned_unverified_no_mcp_tools_not_soft_ready` — session-only-divergent + matching PID + `probe -> (200, payload-WITHOUT-the-asserted-MCP-tool)`. Assert NOT soft READY (falls through to the existing terminal HIJACKED — a serving-but-not-our-MCP process is not provably ours). RED-aware: pre-change this is HIJACKED; post-change it must STILL be HIJACKED (the guard requires `mcp_tools_present`). Assert `state == "HIJACKED"`.
  - `test_ensure_runtime_owned_unverified_stale_not_masked` — session-only-divergent + matching PID + 200 + MCP present but `stale_check -> True`. Assert the verdict routes through STALE/rebuild (the stale binary is NOT masked by soft-READY) — i.e. `state != "READY"` purely from the soft-READY shortcut; the stale path runs first. (Open Question "Boot-stamp interaction".)
- **Implementation goal:** Tests only — establish the contract. Reuse the existing `ensure_runtime` test scaffolding in `test_lazy_core.py` [VERIFY: grep -n "def ensure_runtime" user/scripts/lazy_core.py] and `verify_runtime_ownership` [VERIFY: grep -n "def verify_runtime_ownership" user/scripts/lazy_core.py] and `_hijacked_blocker` [VERIFY: grep -n "def _hijacked_blocker" user/scripts/lazy_core.py].
- **Spec requirements:** SPEC `## Affected Area` Tests row ("lock present, verify_runtime_ownership false on the session component only, live matching PID, probe 200 + MCP present ⇒ soft READY (proceed); a divergent-PID/foreign case ⇒ HIJACKED unchanged").
- **Batch:** 1.

#### WU-2 — Soft owned-unverified READY guard (IMPLEMENTATION)

- **Scope:** PHASES.md P1 implementation deliverables — the classifier guard in `_ensure_runtime_m4`'s `not owned` branch. Makes WU-1's RED fixture GREEN without breaking the foreign/dead/non-200/no-MCP/stale GREEN twins.
- **TDD:** yes (satisfies WU-1's contract).
- **Files to create/modify:** `user/scripts/lazy_core.py` — inside `_ensure_runtime_m4` [VERIFY: grep -n "def _ensure_runtime_m4" user/scripts/lazy_core.py], in the `if not owned:` block [VERIFY: grep -n "if not owned:" user/scripts/lazy_core.py], in the `live_start is not None` sub-branch (currently the unconditional terminal HIJACKED return at ≈7533–7539).
- **Implementation goal:** After `live_start` is resolved and confirmed non-None, BEFORE the terminal HIJACKED return, add a guard that returns `_runtime_verdict("READY", ownership_verified=False, health_code=code, payload=payload, tool_name=tool_name, terminal_blocker=None)` IFF ALL: (a) `code == 200`; (b) `_mcp_tools_present_honest(payload, tool_name, code)` is True [VERIFY: grep -n "_mcp_tools_present_honest" user/scripts/lazy_core.py] — derive it the SAME way the verdict builder does so guard and verdict agree; AND (c) `live_start == lock.get("start_time")` (the live PID is the SAME serving process — only the session component diverged). On any condition False, fall through to the existing terminal HIJACKED return UNCHANGED. Name the predicate clearly (e.g. an inline `is_owned_unverified_serving` boolean or a small `_is_owned_unverified_serving(...)` helper) with a comment documenting the safety argument: the process is provably alive AND is the run's own booted process (start_time match defeats PID reuse), the divergence is bookkeeping-only (controller_session_id source asymmetry), and it is provably serving THIS app's MCP tools — so proceeding is correct and SIGKILL-free. Do NOT modify `verify_runtime_ownership` (the strict predicate stays strict; the relaxation is classifier-level). Do NOT add a new `state` value.
- **Spec requirements:** SPEC `## Affected Area` "M4 ownership classifier" row (the verbatim fix direction) + LD3 (genuine-foreign HIJACKED stays strict never-SIGKILL) + LD4 (foreign path unaffected).
- **Batch:** 2 (after WU-1 is RED-confirmed).

> **Batch overview (Phase P1):**
>
> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-1      | Solo      | test_lazy_core.py |
> | 2     | WU-2      | Solo      | lazy_core.py      |

**Runtime Verification (Phase P1)** *(checked by the test suite / operator smoke, NOT by `/execute-plan`):*
- [ ] <!-- verification-only --> `python3 user/scripts/test_lazy_core.py` GREEN with the soft-READY fixture asserting `state: READY, ownership_verified: false, terminal_blocker: null` (false-positive cured) and the foreign/dead/no-MCP twins asserting HIJACKED/DEAD unchanged (LD3/LD4 preserved).

---

### Phase: P2 — Consumer-routing prose — proceed on soft-READY (retire workaround)

**Goal:** Make the cured verdict change pipeline behavior — update the sole consumer's routing prose (`/lazy-batch` Step 1d.0, + cycle base prompt if it restates the gate) so a soft owned-unverified READY is an explicit "proceed to dispatch" outcome, and retire the per-cycle hand-reasoning workaround. Document the soft-READY in the `--ensure-runtime` CLI doc.
**Entry criteria:** Phase P1 complete (the soft-READY verdict exists in `_ensure_runtime_m4`).
**SPEC.md references:** `## Affected Area` "Consumer routing" row + "ensure-runtime handler" row; Verified Symptom 3 (the hand-reasoning workaround to retire).

#### WU-3 — `/lazy-batch` Step 1d.0 routing prose

- **Scope:** PHASES.md P2 deliverable — Step 1d.0 prose explicitly lists `state: READY` (incl. `ownership_verified: false`) among "proceed to dispatch"; terminal set stays `state ∈ {HIJACKED, BLOCKED}` ⇒ `BLOCKED.md` `blocker_kind: mcp-runtime-unready`; remove the hand-reasoning workaround narration.
- **TDD:** no (skill prose).
- **Files to create/modify:** `user/skills/lazy-batch/SKILL.md` — Step 1d.0 runtime-gate routing section (the `--ensure-runtime` verdict bullets at ≈598–629; the `state: HIJACKED` bullet at ≈614; the State Machine Summary "Runtime-readiness routing" at ≈1399) [VERIFY: grep -n "state: HIJACKED" user/skills/lazy-batch/SKILL.md]. NOTE: `/lazy-batch` is **user-level** (AlgoBooth-flavored), NOT under `repos/algobooth/` — confirmed by `user/scripts/CLAUDE.md`. The `state: READY` bullet at ≈611 is where the soft-READY clause attaches.
- **Implementation goal:** At the `state: READY` bullet (≈611), add a clause that a `state: READY` verdict carrying `ownership_verified: false` is ALSO a "proceed to dispatch" outcome — the soft owned-unverified READY for a self-booted, serving runtime whose ownership diverges only on the session component (the current `state: READY` proceed-conjunction `state == READY AND health_code == 200 AND mcp_tools_present` already admits it, since it reads `state`/`health_code`/`mcp_tools_present`, NOT `ownership_verified` — but say so explicitly so the operator knows an `ownership_verified: false` READY is expected and proceeds). Cross-reference this bug slug. The `state: HIJACKED` bullet (≈614) is UNCHANGED — it now describes only the genuine-foreign case (which is all the producer emits HIJACKED for post-fix). If any narration elsewhere instructs the orchestrator to hand-verify a HIJACKED-but-serving runtime and dispatch against it directly, retire it (grep for it; the SPEC's Verified Symptom 3 workaround). Keep the terminal set (`state ∈ {HIJACKED, BLOCKED}` ⇒ BLOCKED.md) unchanged.
- **Spec requirements:** SPEC `## Affected Area` "Consumer routing" row.
- **Batch:** 1.

#### WU-4 — cycle-base-prompt mirror (conditional)

- **Scope:** PHASES.md P2 deliverable — mirror the Step 1d.0 wording in the cycle base prompt IF it restates the runtime-gate routing; else record "no edit owed — confirmed by grep".
- **TDD:** no.
- **Files to create/modify:** `user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` — ONLY if it restates the runtime-gate verdict routing [VERIFY: grep -n "mcp-runtime-unready" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md]. NOTE the corrected path (under `user/skills/_components/lazy-batch-prompts/`, NOT `repos/algobooth/`). Authoring recon already shows this file's only reference (≈237) is the generic runtime-up `NEEDS_RUNTIME` escape (`mcp-runtime-unready`, escalation-immune) — it does NOT restate the HIJACKED-vs-READY verdict routing or carry the hand-reasoning workaround, so the EXPECTED disposition is no-op.
- **Implementation goal:** Grep the file. If (as recon indicates) it only references `mcp-runtime-unready` generically for the mid-cycle NEEDS_RUNTIME escape and does NOT restate the HIJACKED/READY verdict routing, make NO edit and record "no edit owed — cycle-base-prompt routes on the generic NEEDS_RUNTIME escape, not the HIJACKED/READY verdict; soft-READY needs no mirror here" in the PHASES Implementation Notes. Edit ONLY if a restatement of the verdict routing is found.
- **Spec requirements:** SPEC `## Affected Area` "Consumer routing" row (`/lazy-batch` Step 1d.0 prose / `cycle-base-prompt`).
- **Batch:** 1.

#### WU-5 — `--ensure-runtime` CLI doc + re-project skills

- **Scope:** PHASES.md P2 deliverable — append the soft owned-unverified READY sentence to the `--ensure-runtime` doc; re-run `project-skills.py` so the per-repo `lazy-batch` projection picks up the WU-3 prose change.
- **TDD:** no (docs + projection regen).
- **Files to create/modify:** `user/scripts/CLAUDE.md` — the `--ensure-runtime` CLI doc block [VERIFY: grep -n "ensure-runtime" user/scripts/CLAUDE.md].
- **Implementation goal:** Append one sentence: a serving (200 + MCP-present), live-PID-start_time-matching, session-only-divergent runtime classifies READY (`ownership_verified: false`, proceed) instead of terminal HIJACKED; the genuine-foreign HIJACKED (no matching PID, or divergent live PID/start_time) stays the strict never-SIGKILL fail-safe (LD3). Reference this bug slug. Then run `python3 ~/.claude/scripts/project-skills.py` and spot-check the projected `lazy-batch` routing section; run `python3 ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities` if any injection changed.
- **Spec requirements:** SPEC `## Affected Area` "ensure-runtime handler" + "Consumer routing" rows; `user/scripts/CLAUDE.md` projection note.
- **Batch:** 1.

> **Batch overview (Phase P2):**
>
> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-3, WU-4, WU-5 | Yes (distinct files) | None |

**Runtime Verification (Phase P2)** *(operator-observable on a live AlgoBooth run, NOT claude-config CI):*
- [ ] <!-- verification-only --> A `/lazy-batch` mcp-test cycle on a self-booted serving AlgoBooth runtime proceeds to dispatch on the soft owned-unverified READY without writing `BLOCKED.md` and without the hand-reasoning workaround or a wasted `dev:kill` + cold boot (the SPEC's Expected behavior).

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md/PHASES.md top-level `**Status:**` to `Fixed` and writes the `FIXED.md` receipt once the validation tail certifies this fix. This plan and `/execute-plan` NEVER write `FIXED.md` or flip the top-level status to Fixed; when the last phase's work lands, set the top-level PHASES `**Status:**` to `In-progress` only.

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until both phases are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** P1 first (no entry criteria); P2 once P1's deliverables are checked off in PHASES.md. If no phase is ready, jump to the Blocking Issue Protocol.
2. **Announce:** Print "Implementing Phase N: [title]".
3. **Review prior context:** Re-read previously completed phases' Implementation Notes in PHASES.md (imports, patterns, gotchas, actual file paths). They take priority over the plan where they diverge.
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "Phase N: [title] — committed as [hash]".
7. **Loop:** Re-evaluate readiness; return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (ToolSearch task tools, create tasks for all WUs, update protocol).

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and this plan file. Do NOT rely on cached content.

#### Step B.1: Launch Subagents (or execute inline per the override) — TEST-FIRST

Read before proceeding: `~/.claude/skills/_components/tdd-protocol.md`, `~/.claude/skills/_components/subagent-launch.md`, `~/.claude/skills/_components/tdd-test-agent.md`, `~/.claude/skills/_components/implementation-agent.md`. For Phase P1: write the failing `test_lazy_core.py` fixtures (WU-1) and confirm RED for the RIGHT reason (the soft-READY fixture fails asserting `state == "READY"` because the pre-change classifier returns HIJACKED — NOT a compile/import error) BEFORE the WU-2 guard. For Phase P2 (docs/prose, TDD=no): apply edits directly (no test agent).

> **Inline override active when no `Agent` tool:** perform WU-1/WU-2 inline with Write/Edit, still test-first (fixture RED before the guard).

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` and `~/.claude/skills/_components/mount-site-verification.md`. Produce a structured review report with a PASS / PASS-WITH-FIXES / NEEDS-REWORK verdict. For inline execution, self-review against the same protocol: confirm WU-2's guard matches the spec fix direction, the foreign/dead/no-MCP twins stay GREEN, and `verify_runtime_ownership` was NOT relaxed.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md`. Check off completed deliverables; add an Implementation Notes block (date, work completed, integration notes, pitfalls, files modified). For WU-4, record the no-op disposition if the cycle prompt did not need editing.

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `.claude/skill-config/quality-gates.md` (fallback `~/.claude/skills/_components/quality-gates.md`). For this repo the load-bearing gates are:
- `python3 user/scripts/test_lazy_core.py` (the pytest suite — the new fixtures live here).
- `python3 user/scripts/lazy-state.py --test` and `python3 user/scripts/bug-state.py --test` (the shared-`lazy_core` in-file smoke harnesses — both must stay GREEN; baselines byte-pinned).
- `python3 user/scripts/lazy_parity_audit.py --repo-root .` (confirm NO coupled-pair mirror is owed — `--ensure-runtime` is feature-only; the audit must stay green).
- For Phase P2: `python3 ~/.claude/scripts/project-skills.py` (no circular include / broken injection) + `python3 ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities` if an injection changed.
100% pass required before proceeding.

#### Step B.4.5: MCP Integration Test (BLOCKING — if applicable)

Read `~/.claude/skills/_components/mcp/mcp-integration-test.md`. **N/A here** — this bug has no AlgoBooth runtime-observable app surface (PHASES `**MCP runtime:** not-required`); the `--ensure-runtime` classifier is validated by hermetic injected-callable fixtures. Skip with this note. (The operator-manual live cold-boot smoke in `user/scripts/CLAUDE.md` is the optional out-of-CI confirmation.)

#### Step B.5: Commit Batch

Read the commit policy: first `.claude/skill-config/commit-policy.md`; if absent, `~/.claude/skills/_components/commit-and-push.md`. Commit + push to `main` per the policy on disk. ATOMIC GATE+COMMIT: run the gate and commit as one chained command so an interrupted turn leaves committed, pushed state.

#### Step B.6: Proceed to Next Batch

Checklist (all true): review verdict produced; PHASES.md updated; all quality gates pass; Step B.5 committed. If any unchecked, complete it before the next batch.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md`. Confirm the soft-READY guard + the consumer-routing prose agree on the verdict shape (`state: READY`, `ownership_verified: false`).

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md`. The `user/scripts/CLAUDE.md` `--ensure-runtime` doc update is folded into WU-5; confirm no other CLAUDE.md needs a note.

#### Commit and Push Post-Phase Changes

Read the commit policy (as in B.5) and commit + push post-phase changes to `main`.

## Blocking Issue Protocol

If a blocking issue is encountered at any point during execution:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed phases** that haven't been committed yet.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** Phase N: [title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining phases (not attempted):**
   - [list]

4. **Do not attempt to work around the blocker.** Write `BLOCKED.md` per the canonical sentinel schema and halt.

Blocking issues include: a quality-gate failure that can't be fixed after 2 retry attempts; the parity audit going red (a genuine coupled-pair mirror surfaced — re-evaluate the feature-only assumption); `verify_runtime_ownership` requiring a relaxation to make a fixture pass (a sign the guard is mis-placed — it must be classifier-level, never a predicate relaxation); any error requiring architectural decisions beyond the SPEC's locked fix scope.

## Completion

When both phases are complete:

1. **Run the full quality-gate suite one final time:** `python3 user/scripts/test_lazy_core.py`, `python3 user/scripts/lazy-state.py --test`, `python3 user/scripts/bug-state.py --test`, `python3 user/scripts/lazy_parity_audit.py --repo-root .`, `python3 ~/.claude/scripts/project-skills.py`.
2. **Set the top-level PHASES `**Status:**` to `In-progress`** (implementation done, validation pending) — do NOT flip to Fixed and do NOT write `FIXED.md` (the `__mark_fixed__` gate owns both after the validation tail).
3. **Print a completion report:**

   ## Implementation Batch — Complete

   **Bug fixed (pending validation):** ensure-runtime-false-hijacked-on-owned-serving-runtime
   **Total phases completed:** 2
   **Final quality-gate status:** all green

   **Commit log:**
   | Commit | Phase | Title |
   |--------|-------|-------|
   | … | P1 | Soft owned-unverified READY classifier + fixtures |
   | … | P2 | Consumer-routing prose + CLI doc |

   **Implementation Notes summary:**
   The soft owned-unverified READY guard fires ONLY on `code==200` + `mcp_tools_present` + `live_start == lock['start_time']` (process matches, only session diverges). Genuine-foreign HIJACKED (no matching PID / divergent live start_time) and dead-PID DEAD are unchanged (LD3/LD4). Feature-pipeline-only — no `bug-state.py` mirror (parity audit green).
