---
kind: implementation-plan
feature_id: adhoc-plan-bug-no-guard-for-fixed-annotated-specs
status: In-progress
created: 2026-07-19
complexity: complex
phases: [1, 2, 3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan {path}`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — plan-bug guard for Fixed-annotated / already-implemented SPECs  (v2)

**PHASES.md files:**
- `docs/bugs/adhoc-plan-bug-no-guard-for-fixed-annotated-specs/PHASES.md` (adhoc-plan-bug-no-guard-for-fixed-annotated-specs, 3 phases)

**SPEC.md files:**
- `docs/bugs/adhoc-plan-bug-no-guard-for-fixed-annotated-specs/SPEC.md`

**Total phases:** 3
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (e.g. a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and any local `## Component Reference Card` override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

### Plan-specific execution notes (claude-config — OVERRIDES the contract's AlgoBooth defaults)

This is **claude-config**, the harness repo. There is NO Tauri app, NO MCP runtime, NO `/msbuild`/`/mstest`/`nx`. The quality gates for THIS plan are the Python/skill toolchain — run these (from the repo root) as the per-batch and post-phase gates in place of the contract's build/test defaults:

```bash
# State-script + shared-helper regression (the hermetic acceptance net — MUST stay green):
python3 user/scripts/bug-state.py --test
python3 user/scripts/lazy-state.py --test
python3 -m pytest user/scripts/tests/test_lazy_core/ -q
# Coupled-pair / routing-parity audit (REQUIRED after any state-script edit):
python3 user/scripts/lazy_parity_audit.py --repo-root .
# Skill projection + lint (REQUIRED after any SKILL.md / _components edit):
python3 user/scripts/project-skills.py
python3 user/scripts/lint-skills.py
```

- **Single-writer / inline execution.** Per the workstation cycle-subagent contract, `/execute-plan` runs INLINE (zero `Agent()` calls) for this small mechanical batch; write failing tests before implementing each batch (test-first). The three WUs are dependency-ordered into three sequential batches — never parallel (each later WU consumes the prior WU's symbols).
- **Branch:** all commits land on `main` (claude-config HARD branching policy). Push after every commit (claude-config HARD push policy).
- **No MCP / runtime validation tail.** MCP runtime is `not-required` (PHASES.md header). The verification is the `--test` harness + pytest + parity audit + lint above. Do NOT author a `/mcp-test` WU.

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against. Listed in dependency order.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this plan has no completed hard upstream dependencies) | — | — | The SPEC carries no `**Depends on:**` block. |

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | adhoc-plan-bug-no-guard | P1 | Shared `**Fixed:**`-annotation reader + `fixed-unreconciled` detector/formatter | — | Solo |
| 2 | adhoc-plan-bug-no-guard | P2 | `bug-state.py` Step 4 route diversion | P1 | Solo |
| 3 | adhoc-plan-bug-no-guard | P3 | `/plan-bug` Step 0.4 belt-and-suspenders gate | P1, P2 | Solo |

## Work Units

- [x] WU-1 — Phase 1: shared `**Fixed:**` reader + `is_fixed_unreconciled` predicate + `format_fixed_unreconciled_blocker` (+ unit tests)
- [ ] WU-2 — Phase 2: wire the pre-gate into `bug-state.py` Step 4 (divert to `BLOCKED.md`) + parity-divergence row + `--test` fixture
- [ ] WU-3 — Phase 3: `/plan-bug` Step 0.4 belt-and-suspenders prose + re-project/lint

---

### Phase: adhoc-plan-bug-no-guard P1 — Shared `**Fixed:**`-annotation reader + `fixed-unreconciled` detector/formatter

**Goal:** Add the domain-agnostic `lazy_core` building blocks both fix sites consume — no routing change yet, pure helpers unit-tested in isolation.
**Entry criteria:** None (first phase).
**SPEC.md references:** `## Proven Findings` (the traced serving path); `## Affected Area` rows "shared status/receipt vocabulary" (`docmodel.py`) and the reuse-existing-vocabulary constraint.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | None |

#### WU-1 — shared reader + predicate + blocker formatter (Batch 1, TDD: yes)

**Scope (PHASES Phase 1 deliverables):**
- `spec_fixed_annotation(spec_path)` in `lazy_core/docmodel.py` — sibling of `spec_status`, returns the first `**Fixed:**` line's value or `None`.
- `is_fixed_unreconciled(spec_dir, repo_root)` in `lazy_core/gates.py` — the already-implemented-but-unreconciled predicate.
- `format_fixed_unreconciled_blocker(bug_id, fixed_annotation)` in `lazy_core/gates.py` — canonical `BLOCKED.md` body (`blocker_kind: fixed-unreconciled`).
- Unit tests for all three.

**Files to create/modify:**
- `user/scripts/lazy_core/docmodel.py` — add `spec_fixed_annotation`.
- `user/scripts/lazy_core/gates.py` — add `is_fixed_unreconciled` + `format_fixed_unreconciled_blocker`.
- `user/scripts/tests/test_lazy_core/test_docmodel.py` — tests for the reader.
- `user/scripts/tests/test_lazy_core/test_gates.py` — tests for the predicate + formatter.

**Test files:** `test_docmodel.py`, `test_gates.py` (extend the existing modules; register new tests in each seam's `_TESTS` manual runner per the module convention).

**Test expectations (write these RED first):**
- `test_docmodel.py`: `spec_fixed_annotation` returns the value for a SPEC whose first `**Fixed:**` line is present; returns `None` when absent; the FIRST occurrence wins when two are present (mirrors `spec_status`'s first-wins rule).
- `test_gates.py`: `is_fixed_unreconciled` returns `True` for a tmp bug dir with `**Status:** Concluded` + a `**Fixed:**` line + NO `FIXED.md`; returns `False` for each negative — status `Fixed`, no `**Fixed:**` line, a valid `FIXED.md` present, and a dir under `docs/bugs/_archive/`. `format_fixed_unreconciled_blocker` returns a string carrying valid sentinel frontmatter with `blocker_kind: fixed-unreconciled` and a body naming the reconciliation remedy.

**Implementation goal:** `spec_fixed_annotation` reuses `spec_status`'s exact read+`re.match` shape [VERIFY: grep -n "def spec_status" user/scripts/lazy_core/docmodel.py] with pattern `^\*\*Fixed:\*\*\s*(.+?)\s*$`. `is_fixed_unreconciled` composes `docmodel.spec_status` + the new `spec_fixed_annotation` + `has_completion_receipt(spec_dir, filename="FIXED.md")` [VERIFY: grep -n "def has_completion_receipt" user/scripts/lazy_core/gates.py] + a `docs/bugs/_archive/` path check; predicate = status ∈ pre-fix set (not `Fixed`/`Won't-fix`) AND `**Fixed:**` present AND no `FIXED.md` receipt AND not under `_archive/`. `format_fixed_unreconciled_blocker` is modeled on `depdag.format_unknown_dependency_blocker` [VERIFY: grep -n "def format_unknown_dependency_blocker" user/scripts/lazy_core/depdag.py] — same frontmatter+body shape, new `blocker_kind: fixed-unreconciled`, remedy text = "reconcile via the docs/bugs/CLAUDE.md receipt + `--archive-fixed` contract, or clear the stray `**Fixed:**` annotation to re-plan."

**Spec requirements:** SPEC `## Proven Findings` — "reuse the existing receipt/archive vocabulary (`--fsck` semantics, `dep_completion_status` archive-awareness) rather than inventing a new is-fixed heuristic." The `**Fixed:**` annotation is the documented load-bearing signal (`docs/bugs/CLAUDE.md` → "Fixing a bug OUT-OF-PIPELINE"; `_components/mark-fixed-archive.md` step 1).

**Batch:** 1.

**Runtime Verification** *(none — pure helpers, verified by the unit tests above):*
- N/A — no runtime-observable behavior in this phase.

---

### Phase: adhoc-plan-bug-no-guard P2 — `bug-state.py` Step 4 route diversion (primary catch)

**Goal:** Wire the Phase-1 predicate into `compute_state` Step 4 so a Concluded + `**Fixed:**`-annotated + no-receipt SPEC is diverted to a `fixed-unreconciled` `BLOCKED.md` BEFORE it can route to `plan-bug`.
**Entry criteria:** Phase 1 complete (the `is_fixed_unreconciled` + `format_fixed_unreconciled_blocker` helpers exist and are unit-green).
**SPEC.md references:** `## Evidence Collected` → `bug-state.py:1701-1714` (the earlier/more-complete fix site); `## Affected Area` "bug-state routing (earlier catch)".

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 2 | WU-2 | Solo | None |

#### WU-2 — Step-4 pre-gate + parity-divergence row + `--test` fixture (Batch 2, TDD: yes)

**Scope (PHASES Phase 2 deliverables):**
- Insert the `is_fixed_unreconciled` pre-check into `bug-state.py` Step 4 inside the `if _status == "Concluded":` branch, before the `plan-bug` return; on a hit write canonical `BLOCKED.md` + `_diag` + return the blocked outcome.
- Add a `tabulated-divergence` row to `compute-state-routing-parity.json`.
- Add a `bug-state.py --test` fixture (divert-on-hit + no-regression-on-plain-Concluded).

**Files to create/modify:**
- `user/scripts/bug-state.py` — the Step-4 pre-check (calls `lazy_core.is_fixed_unreconciled` / `lazy_core.format_fixed_unreconciled_blocker`; writes `BLOCKED.md` via `lazy_core._atomic_write`; adds a `_diag`) + the new in-file `--test` fixture.
- `user/scripts/compute-state-routing-parity.json` — the `tabulated-divergence` row (owner + reason).
- `user/scripts/tests/baselines/bug-state-test-baseline.txt` — regenerate via `_normalize_smoke_output` ONLY if the new fixture shifts the byte-pinned output (never hand-edit).

**Test files:** the `bug-state.py` in-file `--test` harness (add `def test_fixed_unreconciled_diverts_from_plan_bug():` + a plain-Concluded companion; register in the script's test list per `user/scripts/CLAUDE.md` → "Adding a test to the in-file `--test` harness").

**Test expectations (write RED first):**
- `test_fixed_unreconciled_diverts_from_plan_bug`: a tmp bug dir with `**Status:** Concluded` + a `**Fixed:**` line + no `PHASES.md` + no `FIXED.md` yields a state whose `sub_skill` is NOT `plan-bug`, writes `BLOCKED.md`, and reports the blocked terminal.
- companion: a tmp bug dir with `**Status:** Concluded` + no `**Fixed:**` line + no `PHASES.md` still routes to `sub_skill: plan-bug` (no regression to the existing Step-4 behavior).

**Implementation goal:** In `compute_state` Step 4 [VERIFY: grep -n "Step 4:" user/scripts/bug-state.py], immediately inside the existing `if _status == "Concluded":` branch and BEFORE `return ... sub_skill=SKILL_PLAN_BUG`, call `lazy_core.is_fixed_unreconciled(spec_dir, repo_root)`; on `True`, write the canonical `BLOCKED.md` into `spec_dir` via `lazy_core._atomic_write` using `lazy_core.format_fixed_unreconciled_blocker(bug_id, annotation)`, append `lazy_core._diag(...)` ("Concluded SPEC carries a `**Fixed:**` annotation with no receipt — diverted to reconciliation, not plan-bug"), and `return _bug_state(**common, ...)` with the blocked terminal so the next probe halts at the Step-3 `BLOCKED.md` check. The `plan-bug` route is otherwise byte-unchanged for the non-hit path. Add the divergence row to `compute-state-routing-parity.json` [VERIFY: test -f user/scripts/compute-state-routing-parity.json] classifying the pre-gate `tabulated-divergence` (reason: the feature pipeline has no out-of-pipeline `**Fixed:**`-annotation / `Concluded`-intermediate analog, so no `lazy-state.py` mirror is owed).

**Spec requirements:** SPEC `## Proven Findings` fix-site 2 ("a probe-time diagnostic and/or route diversion for the same signature, so the item never routes to plan-bug at all — the cheapest possible catch"). Design note (PHASES): the pre-gate HALTS for reconciliation (writes `BLOCKED.md`), it does NOT auto-write `FIXED.md` — the Status-honesty PIPELINE-GATE owns `FIXED.md` exclusively.

**Batch:** 2 (depends on WU-1; the `bug-state.py` edit imports the Phase-1 `lazy_core` helpers).

**Runtime Verification** *(none — routing verified by the hermetic `--test` fixture):*
- N/A — no MCP/app surface; the `bug-state.py --test` fixture is the structural verification.

---

### Phase: adhoc-plan-bug-no-guard P3 — `/plan-bug` Step 0.4 belt-and-suspenders gate

**Goal:** Make `/plan-bug` safe when invoked directly on a Fixed-annotated Concluded SPEC (bypassing the Phase-2 divert) — refuse to plan, name the reconciliation remedy.
**Entry criteria:** Phase 1 complete (the predicate exists, named as the prose contract); Phase 2 complete (establishes the `fixed-unreconciled` vocabulary + remedy wording to mirror).
**SPEC.md references:** `## Evidence Collected` → `plan-bug/SKILL.md:59-73` (the primary fix site); `## Affected Area` "plan-bug planning gate (primary)".

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 3 | WU-3 | Solo | None |

#### WU-3 — Step 0.4 prose sub-check + re-project/lint (Batch 3, TDD: no)

**Scope (PHASES Phase 3 deliverables):**
- Extend `user/skills/plan-bug/SKILL.md` Step 0.4 with a `**Fixed:**`-annotation refuse sub-check.
- Re-project + lint.

**Files to create/modify:**
- `user/skills/plan-bug/SKILL.md` — the Step 0.4 belt-and-suspenders sub-check.
- `skills-projected/_default/plan-bug/SKILL.md` — regenerated output (via `project-skills.py`; not hand-edited).

**Implementation goal:** In `plan-bug/SKILL.md` Step 0.4 [VERIFY: grep -n "Step 0.4" user/skills/plan-bug/SKILL.md], after the status gate confirms a pre-fix status, add a sub-check: if the SPEC carries a `**Fixed:**` annotation (status not yet `Fixed`) AND no valid `FIXED.md` receipt (name `lazy_core.is_fixed_unreconciled` as the mechanical contract), REFUSE the planning round with a distinct `fixed-unreconciled` note ("this bug appears already-implemented out-of-pipeline — `**Fixed:**` annotation present, no receipt; reconcile via the `docs/bugs/CLAUDE.md` receipt + `--archive-fixed` contract, or clear the stray annotation to re-plan; do NOT author `PHASES.md` / a plan"), return success with the note, and STOP before Step 1. Mirror Phase 2's remedy wording so both surfaces speak with one voice. No new `!cat` component is needed — the prose references the shared predicate by name.

**Spec requirements:** SPEC `## Proven Findings` fix-site 1 (primary — the Step 0.4 pre-gate refusing with a distinct `fixed-unreconciled` outcome). No coupled-pair mirror is owed: `/plan-bug` is a bug-axis skill; `/plan-feature` (feature pipeline) has no `**Fixed:**`-annotation analog.

**Batch:** 3 (depends on WU-1 for the predicate name and WU-2 for the vocabulary).

**Runtime Verification** *(prose — verified by projection + lint + read-through):*
- [ ] <!-- verification-only --> The updated Step 0.4, read end-to-end in the projected `skills-projected/_default/plan-bug/SKILL.md`, refuses a Fixed-annotated no-receipt Concluded SPEC (naming the reconciliation remedy) and still proceeds normally for a plain Concluded SPEC.

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md/PHASES.md `**Status:**` to `Fixed` and writes `FIXED.md` once this bug's validation tail passes — never authored as a checkbox row here.
