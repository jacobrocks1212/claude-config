---
kind: implementation-plan
feature_id: adhoc-orchestrator-redundant-recovery-on-background-suite-reinvoke
status: Ready
created: 2026-07-19
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan <this-file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 2. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\adhoc-orchestrator-redundant-recovery-on-background-suite-reinvoke\plans\all-phases-recovery-discriminator-part-1.md` (this file)
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\adhoc-orchestrator-redundant-recovery-on-background-suite-reinvoke\plans\all-phases-recovery-discriminator-part-2.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — Orchestrator Redundant-Recovery on Background-Suite Re-invoke (part 1: Gap 2 + Gap 1 mechanics) (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\adhoc-orchestrator-redundant-recovery-on-background-suite-reinvoke\PHASES.md` (Phases 1–2 of 3)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\adhoc-orchestrator-redundant-recovery-on-background-suite-reinvoke\SPEC.md`

**Total phases:** 2 (of 3; Phase 3 is part 2)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config gate battery — OVERRIDES the contract's default build/test gates)

This repo has NO `/msbuild`/`/mstest`/Nx gates. The quality gate after every batch and at phase completion is the claude-config Python/lint battery, run from the repo root (`C:\Users\Jacob\source\repos\claude-config`). Prefer the individual under-cap sub-commands (each drives to a real pass/fail well within the Bash cap — never background them from inside an execute-plan cycle):

```bash
python3 -m pytest user/scripts/test_execute_plan_liveness.py user/scripts/test_hooks.py -q   # the tests this part authors
python3 user/scripts/lazy_parity_audit.py --repo-root .                                       # coupled-pair parity (must exit 0)
python3 user/scripts/cli_surface_gen.py --check                                               # CLI-surface registry freshness (new flag)
python  ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities               # skills lint
python  ~/.claude/scripts/project-skills.py                                                    # re-project component edits
python3 user/scripts/doc-drift-lint.py --repo-root .                                           # doc-vs-reality (Hooks table etc.)
python3 ~/.claude/scripts/validate-plan.py --structural <this-plan-file>                        # structural gate (Step 4.5)
```

A gate is GREEN only when its command exits 0. After editing any `SKILL.md`/`_components/` file, run `project-skills.py` then `lint-skills.py`. After editing a hook + `settings.json`, `doc-drift-lint.py` cross-checks the CLAUDE.md Hooks table against the registration (the table row lands in part 2 — so `doc-drift-lint` may WARN on a not-yet-documented hook until part 2; that is expected and does not block part 1's non-doc gates).

## References — Upstream Artifacts

> | Upstream feature | Kind | Path | Why this plan references it |
> |------------------|------|------|------------------------------|
> | (none — this plan has no completed hard upstream dependencies) | | | |

The plan REUSES (does not modify behaviorally) two existing surfaces: `user/skills/_components/dispatched-agent-liveness.md` (the authoritative pause-signal recipe Gap 2 encodes) and `user/hooks/hook-prelude.sh` + `user/scripts/hook_lib.py` (the shared guard scaffold Gap 1's hook sources).

## Touchpoint Audit (verified: inline — dispatch available; small, well-traced harness set; verified during /spec-phases + /write-plan)

| Planned file | Exists? | Real symbols / anchors (verified) | Action | Reuse / refactor directive |
|--------------|---------|-----------------------------------|--------|----------------------------|
| `user/scripts/lazy_core.py` | yes | shared state helpers imported by both state scripts | create (net-new fn) | Add `execute_plan_liveness`; reuse the `~/.claude/state/execute-plan/<md5(repo_root)[:12]>.json` recipe `[VERIFY: grep -n "state/execute-plan" user/skills/execute-plan/SKILL.md]`; NON-destructive raw read (never `read_run_marker`, which is delete-on-read). |
| `user/scripts/lazy-state.py` | yes | argparse roster; `--verify-ledger` handler `[VERIFY: grep -n "verify-ledger" user/scripts/lazy-state.py]` | refactor | Add `--execute-plan-liveness --plan <p> --repo-root <cwd>`; always exit 0; register on `--dump-cli-surface`. |
| `user/scripts/bug-state.py` | yes | mirror roster (parity-audited) | refactor | Mirror the identical flag (marker is pipeline-agnostic). |
| `user/scripts/lazy-parity-manifest.json` | yes | `pairs` / `mechanic_sets` `[VERIFY: grep -n "mechanic_sets" user/scripts/lazy-parity-manifest.json]` | refactor | Register the shared flag so `lazy_parity_audit.py` stays exit 0. |
| `user/skills/lazy-batch/SKILL.md` | yes | Step 1e §4a recovery region; `--emit-dispatch recovery` `[VERIFY: grep -n "emit-dispatch recovery" user/skills/lazy-batch/SKILL.md]` | refactor | Gate the existing sanctioned recovery emit; do NOT hand-compose a new dispatch. |
| `user/skills/lazy-bug-batch/SKILL.md` | yes | guardrail-D reference block `[VERIFY: grep -n "verify-ledger" user/skills/lazy-bug-batch/SKILL.md]` | refactor | Mirror the gate with `bug-state.py`. Coupled pair — parity-audited. |
| `user/hooks/cycle-subagent-bg-gate-guard.sh` | **NO (net-new)** | — | create | New PreToolUse guard; source `hook-prelude.sh` + import `hook_lib` `[VERIFY: ls user/hooks/hook-prelude.sh user/scripts/hook_lib.py]`; model on `long-build-ownership-guard.sh` `[VERIFY: ls user/hooks/long-build-ownership-guard.sh]`. |
| `user/settings.json` | yes | `hooks.PreToolUse` `Bash\|PowerShell` chain `[VERIFY: grep -n "long-build-ownership-guard" user/settings.json]` | refactor | Register the new guard on the `Bash\|PowerShell` chain. |
| `user/scripts/test_execute_plan_liveness.py` | **NO (net-new)** | — | create | New pytest module for the discriminator helper. |
| `user/scripts/test_hooks.py` | yes | `test_wedge_*` + synthetic PreToolUse harness `[VERIFY: grep -n "def test_" user/scripts/test_hooks.py]` | refactor | Add Gap-1 guard tests. |

No premise-grade contradictions; all anchors verified present. The two judgment calls (script-owned discriminator; conservative hook command set) are scope-class and recorded in PHASES.md — no `NEEDS_INPUT.md` fork.

## Work Units

- [ ] WU-1 — `lazy_core.execute_plan_liveness` discriminator helper + tests
- [ ] WU-2 — `--execute-plan-liveness` CLI flag on both state scripts + parity-manifest registration
- [ ] WU-3 — orchestrator recovery-suppression wiring in lazy-batch + lazy-bug-batch Step 1e/4a
- [ ] WU-4 — Gap-1 mechanical foreground-enforcement PreToolUse guard + registration + tests

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | Phase 1 + Phase 2 | WU-1, WU-4 | discriminator helper + Gap-1 hook | — | Yes (file-disjoint, independent) |
| 2 | Phase 1 | WU-2 | CLI flags + parity | WU-1 | Solo |
| 3 | Phase 1 | WU-3 | orchestrator wiring | WU-2 | Solo |

---

### Phase: Phase 1 — Gap 2 pause-vs-terminal discriminator (LOAD-BEARING)

**Goal:** Suppress the redundant recovery dispatch while a just-returned `/execute-plan` cycle is paused (a backgrounded suite about to be re-invoked), via a deterministic script-owned liveness discriminator wired into the orchestrator's post-cycle guardrail-D.
**Entry criteria:** None (first phase).
**SPEC.md references:** Proven Findings (serving-path trace for Symptom 2/3, fix-site-on-path Gap 2); Locked Decision 1 ("Both"); `## Validated Assumptions` (robust to both re-invocation truths).

#### WU-1 — `lazy_core.execute_plan_liveness` discriminator helper + tests

- **Scope:** PHASES Phase 1 deliverable 1 + 7 (helper + its unit tests).
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/lazy_core.py` (add `execute_plan_liveness`).
- **Test files:** `user/scripts/test_execute_plan_liveness.py` (net-new).
- **Test expectations:** drive the REAL helper against REAL marker files in a `tmp_path` state dir (monkeypatch the claude-state root the helper resolves, mirroring how existing tests point at a temp state dir): (a) marker present for `repo_root` + plan `status: Ready` ⇒ `verdict == "paused"`; (b) marker present + plan `status: In-progress` ⇒ `paused`; (c) marker present + plan `status: Complete` ⇒ `terminal`; (d) marker absent ⇒ `terminal`; (e) unreadable/missing plan file ⇒ `terminal` (fail-safe). Assert the returned dict carries `marker_present`, `plan_status`, `verdict`.
- **Implementation goal:** `execute_plan_liveness(repo_root, plan_path) -> {"marker_present": bool, "plan_status": str|None, "verdict": "paused"|"terminal"|"wedge-candidate"}`. Resolve the marker path via the existing `~/.claude/state/execute-plan/<md5(repo_root)[:12]>.json` recipe `[VERIFY: grep -n "state/execute-plan" user/skills/execute-plan/SKILL.md]` — read it NON-destructively (plain file read + `json.load`, never `read_run_marker` which deletes on read). Parse the plan frontmatter `status:`. Decision rule (encodes `dispatched-agent-liveness.md` §43–62 `[VERIFY: grep -n "AUTHORITATIVE completion signal" user/skills/_components/dispatched-agent-liveness.md]`): marker absent OR `plan_status == "Complete"` ⇒ `terminal`; marker present AND status not `Complete` ⇒ `paused`. Any exception (unreadable marker/plan, bad JSON) ⇒ `terminal` (fail-SAFE — never suppress a legitimate recovery on an unreadable signal). The `wedge-candidate` value is reserved for the orchestrator's bounded-wait escalation (the helper cannot observe live descendants) and is NOT returned by the pure function in v1 — document this in the docstring.
- **Spec requirements:** SPEC Proven Findings ("a pause-vs-terminal check … consulting the execute-plan run marker + plan `status:` per `dispatched-agent-liveness.md`"); `## Validated Assumptions` (fail-safe direction = terminal; reuse the full decision procedure).
- **Batch:** 1.

#### WU-2 — `--execute-plan-liveness` CLI flag on both state scripts + parity-manifest registration

- **Scope:** PHASES Phase 1 deliverables 2, 3, 4.
- **TDD:** yes (covered by the parity audit + a CLI-surface check; add a focused CLI test in `test_execute_plan_liveness.py` invoking the flag via subprocess and asserting the JSON verdict + exit 0).
- **Files to create/modify:** `user/scripts/lazy-state.py`, `user/scripts/bug-state.py`, `user/scripts/lazy-parity-manifest.json`, `user/scripts/test_execute_plan_liveness.py` (extend).
- **Implementation goal:** add a `--execute-plan-liveness` action to each state script's argparse roster, taking `--plan <p>` and the existing `--repo-root`, shelling `lazy_core.execute_plan_liveness` `[VERIFY: grep -n "def execute_plan_liveness" user/scripts/lazy_core.py]` (exists after WU-1) and printing the JSON verdict to stdout; ALWAYS exit 0 (a probe never gates). Register the flag on the shared `--dump-cli-surface` roster so `cli_surface_gen.py --check` stays green. Add the flag as a parity-audited mechanic in `lazy-parity-manifest.json` `[VERIFY: grep -n "mechanic_sets" user/scripts/lazy-parity-manifest.json]` so `lazy_parity_audit.py --repo-root .` exits 0 (the two scripts must expose the identical flag — the execute-plan marker is pipeline-agnostic).
- **Spec requirements:** SPEC Affected Area (coupled `lazy-batch`/`lazy-bug-batch`); the harness mission's deterministic-script-owned-state preference.
- **Batch:** 2 (depends on WU-1's helper).

#### WU-3 — orchestrator recovery-suppression wiring in lazy-batch + lazy-bug-batch Step 1e/4a

- **Scope:** PHASES Phase 1 deliverables 5, 6.
- **TDD:** no (orchestrator SKILL.md prose — no code path; verified by projection + parity + doc-drift lint).
- **Files to create/modify:** `user/skills/lazy-batch/SKILL.md` (Step 1e §4a), `user/skills/lazy-bug-batch/SKILL.md` (guardrail-D reference block).
- **Implementation goal:** in `lazy-batch/SKILL.md` Step 1e §4a, BEFORE the `--emit-dispatch recovery` on a `clean_tree`/`head_matches_origin` failure `[VERIFY: grep -n "emit-dispatch recovery" user/skills/lazy-batch/SKILL.md]` AND only when the just-returned cycle was `/execute-plan`, add a step: run `python3 ~/.claude/scripts/lazy-state.py --repo-root <repo_root> --execute-plan-liveness --plan {plan_file}`; if `verdict == "paused"` → do NOT dispatch recovery, emit a T6 line `⚠ execute-plan cycle paused (backgrounded suite) — recovery suppressed, awaiting harness re-invocation`, and fall through to the next state probe (Step 1a) instead. `verdict == "terminal"` → proceed to the existing recovery emit unchanged. Note the genuine-wedge fallback: if the orchestrator observes the marker persisting with NO live descendant after a bounded wait, that is a genuine wedge per `dispatched-agent-liveness.md` §57–62 and recovery IS appropriate (do not loop forever on a dead lineage). Mirror the identical gate into `lazy-bug-batch/SKILL.md`'s guardrail-D block using `bug-state.py --execute-plan-liveness` `[VERIFY: grep -n "verify-ledger" user/skills/lazy-bug-batch/SKILL.md]`. Keep the `/mcp-test` cycle path (no `--plan`) unaffected — the gate is scoped to `/execute-plan` returns.
- **Spec requirements:** SPEC Proven Findings fix-site-on-path (Gap 2: the check is inserted BEFORE the recovery emit, on the traced path); Locked Decision 1.
- **Coupling:** `/lazy-batch` ↔ `/lazy-bug-batch` is a coupled pair — run `lazy_parity_audit.py --repo-root .` (exit 0) after editing both. Re-project + lint the components.
- **Batch:** 3 (depends on WU-2's CLI flag existing).

**Runtime Verification** *(checked by integration test or manual testing — NOT by the implementation agent):*

N/A — the discriminator is a pure function of on-disk marker + plan status, fully covered by WU-1/WU-2 unit tests; there is no live app runtime in this repo. No MCP assertions.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 (+ WU-4 from Phase 2) | Yes | None |
| 2 | WU-2 | Solo | None |
| 3 | WU-3 | Solo | None |

---

### Phase: Phase 2 — Gap 1 mechanical foreground-enforcement guard

**Goal:** Prevent the ambiguous "holding, will re-invoke" return at its source — a PreToolUse guard denies a `run_in_background` long-gate/test-suite launch from inside an armed cycle subagent.
**Entry criteria:** None (independent of Phase 1 — file-disjoint; WU-4 runs in Batch 1 alongside WU-1).
**SPEC.md references:** Proven Findings (serving-path trace for Symptom 1, fix-site-on-path Gap 1); Evidence Collected → Source Code (the prose-only mandate at `cycle-base-prompt.md` turn-end §1 / `turn-end-gate.md`); Locked Decision 1.

#### WU-4 — Gap-1 mechanical foreground-enforcement PreToolUse guard + registration + tests

- **Scope:** PHASES Phase 2 deliverables 1, 2, 3.
- **TDD:** yes.
- **Files to create/modify:** `user/hooks/cycle-subagent-bg-gate-guard.sh` (net-new), `user/settings.json`, `user/scripts/test_hooks.py` (extend).
- **Test files:** `user/scripts/test_hooks.py` (the established synthetic-PreToolUse-payload harness `[VERIFY: grep -n "def test_" user/scripts/test_hooks.py]`).
- **Test expectations (write FIRST, RED before the hook exists):** feed synthetic PreToolUse payloads to the guard and assert the emitted decision: (a) `agent_id` present + cycle marker staged + `tool_input.run_in_background == true` + `command:"npm run qg"` ⇒ DENY (`permissionDecision: deny`); (b) same but NO `agent_id` (main thread) ⇒ ALLOW; (c) `agent_id` + marker + `run_in_background:false` ⇒ ALLOW; (d) `agent_id` + marker + `run_in_background:true` + a non-gate command (e.g. `sleep 2` / a log tail) ⇒ ALLOW; (e) no cycle marker ⇒ ALLOW; (f) malformed JSON / no-python ⇒ ALLOW (fail-open) and a `hook-error.json` breadcrumb is written. Mirror the existing `test_wedge_*` payload-construction + marker-staging helpers.
- **Implementation goal:** author `cycle-subagent-bg-gate-guard.sh` as a PreToolUse (Bash|PowerShell) guard that SOURCES `hook-prelude.sh` and imports `hook_lib` `[VERIFY: ls user/hooks/hook-prelude.sh user/scripts/hook_lib.py]` (reuse python resolution, allow/deny emitters, the `_CMD_START`/`ENV_PREFIX` segment anchor, `COMMAND_TOOL_NAMES`, and the no-python + catch-all breadcrumb — do NOT re-implement any). Deny (JSON `permissionDecision: deny`, NEVER `exit 2`) ONLY when ALL hold: payload carries `agent_id` (dispatched subagent) AND the cycle marker `~/.claude/state/lazy-cycle-active.json` is present AND `tool_input.run_in_background == true` AND the command's first real command-segment token (after an optional env prefix, using the shared anchor) matches a conservative gate/test-suite token set — `npm run qg`, `npm run test`, `vitest`, `pytest`, `cargo test`, `dotnet test`, `gate-battery`, and the aggregate gate-battery invocation. The deny message names the foreground-await mandate ("run the individual under-cap sub-components synchronously; never background a long gate inside a cycle subagent"). Everything else ALLOWS. Fail-OPEN on every error path (malformed JSON / missing python / unresolvable state ⇒ allow + breadcrumb), modeled on `long-build-ownership-guard.sh` `[VERIFY: ls user/hooks/long-build-ownership-guard.sh]`. Register the guard in `user/settings.json` `hooks.PreToolUse` on the `Bash|PowerShell` chain, positioned AFTER `long-build-ownership-guard.sh` `[VERIFY: grep -n "long-build-ownership-guard" user/settings.json]` (so a raw long BUILD still surfaces the ownership-takeover signature first — this guard targets the gate/test-SUITE background class the ownership guard does not cover).
- **Spec requirements:** SPEC Proven Findings ("the enforcement seam for 'no backgrounded long gate in a cycle subagent' … sits on the path that produces the 'holding' return"); Locked Decision 1 ("a `user/hooks/` PreToolUse guard denying a `run_in_background` long-gate launch inside an armed cycle subagent").
- **Delineation (record in the WU's implementation note, carried to the CLAUDE.md row in part 2):** this guard covers the backgrounded gate/test-SUITE class inside cycle subagents; `long-build-ownership-guard.sh` covers exact long-BUILD invocations request-time; `lazy-cycle-containment.sh` covers routing/lifecycle ops. Three distinct concerns — not redundant.
- **Batch:** 1 (independent of WU-1/2/3; file-disjoint — runs parallel with WU-1).

**Runtime Verification** *(checked by integration test or manual testing — NOT by the implementation agent):*

N/A — the guard is exercised deterministically by synthetic PreToolUse JSON payloads in `test_hooks.py`; no live subagent or app runtime is needed, and this repo has no MCP surface. No MCP assertions.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-4 (+ WU-1 from Phase 1) | Yes | None |

---

## Completion (part 1)

When WU-1..WU-4 are landed, their `- [ ] WU-N` boxes ticked, all part-1 gates green (pytest for the two test modules, `lazy_parity_audit.py` exit 0, `cli_surface_gen.py --check` exit 0, projection + lint clean), and committed+pushed: flip THIS plan part's frontmatter `status:` to `Complete`, tick Phase 1 + Phase 2 deliverables in `PHASES.md`, and let the state machine route to part 2. Do NOT flip the top-level PHASES `**Status:**` yet (part 2's docs remain) and NEVER write `FIXED.md` / flip SPEC `**Status:**` — those are `__mark_fixed__`-gate-owned.
