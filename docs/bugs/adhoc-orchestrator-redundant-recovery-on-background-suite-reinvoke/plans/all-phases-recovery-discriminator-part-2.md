---
kind: implementation-plan
feature_id: adhoc-orchestrator-redundant-recovery-on-background-suite-reinvoke
status: Ready
created: 2026-07-19
complexity: mechanical
phases: [3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan <this-file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 2. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\adhoc-orchestrator-redundant-recovery-on-background-suite-reinvoke\plans\all-phases-recovery-discriminator-part-1.md`
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\adhoc-orchestrator-redundant-recovery-on-background-suite-reinvoke\plans\all-phases-recovery-discriminator-part-2.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — Orchestrator Redundant-Recovery on Background-Suite Re-invoke (part 2: contract hardening + docs) (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\adhoc-orchestrator-redundant-recovery-on-background-suite-reinvoke\PHASES.md` (Phase 3 of 3)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\adhoc-orchestrator-redundant-recovery-on-background-suite-reinvoke\SPEC.md`

**Total phases:** 1 (Phase 3 of 3)
**Plan version:** v2 (reference-based — components loaded from disk per step)

> **Entry criteria:** part 1 (Phases 1–2) complete — the Gap-2 discriminator (`lazy_core.execute_plan_liveness` + the `--execute-plan-liveness` flag) and the Gap-1 guard (`cycle-subagent-bg-gate-guard.sh` registered in `settings.json`) MUST already exist, because this part's prose and Hooks-table row reference them. This part is a mechanical documentation pass with NO new logic — every WU documents surfaces part 1 already built.

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract.

Where this plan's repo uses non-default gates or component paths (this is a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults. Everything the contract specifies that this plan does not override still applies.

## Plan-specific execution notes (claude-config gate battery — OVERRIDES the contract's default build/test gates)

This is a docs/prose part; the gate is the projection + lint battery run from the repo root:

```bash
python  ~/.claude/scripts/project-skills.py                                     # re-expand !cat component edits
python  ~/.claude/scripts/lint-skills.py --check-projected --check-capabilities # skills lint (broken injections)
python3 user/scripts/doc-drift-lint.py --repo-root .                            # Hooks table ↔ settings.json consistency (MUST exit 0 after WU-2)
python3 user/scripts/lazy_parity_audit.py --repo-root .                         # coupled-pair prose parity (must stay exit 0)
python3 ~/.claude/scripts/validate-plan.py --structural <this-plan-file>        # structural gate (Step 4.5)
```

`doc-drift-lint.py --repo-root .` is the load-bearing gate here: the new CLAUDE.md Hooks-table row (WU-2) must match the `settings.json` registration part 1 wrote — the linter cross-checks the table against the live registrations and FAILS on drift. Both directions must agree before this part is Complete.

## References — Upstream Artifacts

> | Upstream feature | Kind | Path | Why this plan references it |
> |------------------|------|------|------------------------------|
> | (none — this plan has no completed hard upstream dependencies) | | | |

## Touchpoint Audit (verified: inline — all docs surfaces confirmed present during /spec-phases + /write-plan)

| Planned file | Exists? | Anchors (verified) | Action | Directive |
|--------------|---------|--------------------|--------|-----------|
| `user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md` | yes | turn-end §1 `[VERIFY: grep -n "torn down when your turn ends" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md]` | refactor | Point the mandate at the new mechanical guard; keep the foreground-await instruction. |
| `user/skills/_components/turn-end-gate.md` | yes | L13–18 / L23–30 `[VERIFY: grep -n "background-completion re-invocation" user/skills/_components/turn-end-gate.md]` | refactor | Add the mechanical-enforcement reference + annotate the re-invocation contradiction. |
| `user/skills/_components/dispatched-agent-liveness.md` | yes | authoritative-signal section `[VERIFY: grep -n "AUTHORITATIVE completion signal" user/skills/_components/dispatched-agent-liveness.md]` | refactor | One-line cross-ref that the Gap-2 discriminator now wires the recipe into the awaited-Agent-result path. |
| `CLAUDE.md` (root) | yes | Hooks table `[VERIFY: grep -n "block-noncanonical-blocker-write.sh" CLAUDE.md]` | refactor | Add the new hook's row + the three-guard delineation. |

## Work Units

- [ ] WU-1 — prose hardening: point the foreground-await mandate at the mechanical guard + annotate the re-invocation contradiction (component files)
- [ ] WU-2 — CLAUDE.md Hooks-table row for the new guard + doc-drift consistency

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | Phase 3 | WU-1, WU-2 | prose hardening + Hooks-table row | part 1 complete | Yes (file-disjoint) |

---

### Phase: Phase 3 — Contract hardening + docs

**Goal:** Point the prose foreground-await mandate at the now-existing mechanical guard, annotate the `turn-end-gate.md` re-invocation contradiction, add the new hook's CLAUDE.md Hooks-table row + `dispatched-agent-liveness.md` cross-ref, and keep all doc-drift/parity/lint gates green.
**Entry criteria:** part 1 (Phases 1–2) Complete — the guard + flag exist to reference.
**SPEC.md references:** `## Validated Assumptions` (the turn-end-gate.md L13–18 contradiction is documented, not silently resolved); Affected Area (cycle turn-end foreground contract; receiver-side liveness discriminator).

> **Why `mechanical`:** every WU is a pure documentation edit against surfaces part 1 already built (no novel design decision, no algorithm, no cross-boundary wiring). Per the write-plan mechanical-vs-complex test this part qualifies for the Sonnet tier.

#### WU-1 — prose hardening (component files)

- **Scope:** PHASES Phase 3 deliverables 1, 2, 4.
- **TDD:** no (component prose; verified by projection + lint + parity).
- **Files to create/modify:** `user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md`, `user/skills/_components/turn-end-gate.md`, `user/skills/_components/dispatched-agent-liveness.md`.
- **Implementation goal:**
  - `cycle-base-prompt.md` turn-end §1 (both the feature+bug workstation variant and the cloud variant `[VERIFY: grep -n "torn down when your turn ends" user/skills/_components/lazy-batch-prompts/cycle-base-prompt.md]`): add a sentence that the ban is now mechanically enforced (a PreToolUse guard, `cycle-subagent-bg-gate-guard.sh`, denies a `run_in_background` long gate inside a cycle subagent), keeping the positive foreground-await instruction intact.
  - `turn-end-gate.md`: at the over-cap-prevention rule (L23–30) reference the mechanical guard; annotate the L13–18 deterrent `[VERIFY: grep -n "background-completion re-invocation" user/skills/_components/turn-end-gate.md]` — note that dispatched cycle subagents MAY in practice receive background-completion re-invocation (undocumented/inconsistent, per `ADHOC_BRIEF.md`), so the mandate no longer rests on the "process tree is torn down" premise alone; it is enforced by the guard AND backed by the Gap-2 discriminator. Do NOT delete the existing contract text — augment it.
  - `dispatched-agent-liveness.md`: add a one-line cross-ref that the Gap-2 discriminator (`execute_plan_liveness` / `--execute-plan-liveness`) now wires this component's authoritative-signal recipe into the orchestrator's awaited-`Agent`-result path (previously notification-path only).
- **Spec requirements:** SPEC `## Validated Assumptions` (contradiction documented, not resolved); Theory 1 (the deterrent premise conflicts with observed behavior).
- **Post-edit:** run `project-skills.py` then `lint-skills.py --check-projected`; run `lazy_parity_audit.py --repo-root .` (the cloud/bug variants of cycle-base-prompt are parity-relevant — keep exit 0).
- **Batch:** 1 (file-disjoint from WU-2).

#### WU-2 — CLAUDE.md Hooks-table row + doc-drift consistency

- **Scope:** PHASES Phase 3 deliverable 3.
- **TDD:** no (doc edit; the deterministic check is `doc-drift-lint.py`).
- **Files to create/modify:** `CLAUDE.md` (root, Hooks table).
- **Implementation goal:** add a Hooks-table row for `cycle-subagent-bg-gate-guard.sh` `[VERIFY: grep -n "block-noncanonical-blocker-write.sh" CLAUDE.md]` — Trigger: `PreToolUse (Bash, PowerShell)`; Purpose: denies a `run_in_background` long-gate/test-suite launch inside an armed cycle subagent (`agent_id` present + cycle marker present), redirecting to the foreground-await mandate; fail-OPEN; deny-via-JSON. State the three-guard delineation from part 1 WU-4 (this guard = backgrounded gate/test-SUITE class inside cycle subagents; `long-build-ownership-guard.sh` = exact long-BUILD invocations request-time; `lazy-cycle-containment.sh` = routing/lifecycle ops — distinct concerns). The row's registration claim MUST match the `settings.json` registration part 1 wrote, so `doc-drift-lint.py --repo-root .` exits 0 (it cross-checks the Hooks table against live registrations `[VERIFY: grep -n "Hooks table" user/scripts/doc-drift-lint.py]`).
- **Spec requirements:** SPEC Affected Area (enforcement hook surface).
- **Post-edit:** run `doc-drift-lint.py --repo-root .` and confirm exit 0 (table row ↔ settings.json agree).
- **Batch:** 1 (file-disjoint from WU-1).

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md `**Status:**` to `Fixed`, writes `FIXED.md`, and archives the bug dir once the validation tail passes. When part 2's WUs land and gates are green, flip THIS plan part's frontmatter `status:` to `Complete`, tick Phase 3's deliverables in `PHASES.md`, and set the top-level PHASES `**Status:**` to `In-progress` (implementation done across all 3 phases, validation pending) — never `Fixed`/`Won't-fix`, and never write `FIXED.md` here.

**Runtime Verification** *(checked by integration test or manual testing — NOT by the implementation agent):*

N/A — documentation-only; the deterministic doc-drift/lint/parity gates ARE the verification and run in-session. No app runtime, no MCP assertions.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1, WU-2 | Yes | None |

---

## Completion (part 2 — final part)

When WU-1..WU-2 land, their `- [ ] WU-N` boxes are ticked, and all gates green (`project-skills.py`, `lint-skills.py --check-projected`, `doc-drift-lint.py --repo-root .` exit 0, `lazy_parity_audit.py --repo-root .` exit 0) and committed+pushed: flip THIS plan part's frontmatter `status:` to `Complete`, tick Phase 3's deliverables in `PHASES.md`, and set the top-level `PHASES.md` **Status:** to `In-progress`. Let the state machine route to the validation tail (`__mark_fixed__` gate owns the terminal `Fixed` flip + `FIXED.md` + archive).
