---
kind: implementation-plan
feature_id: worktree-claude-doc-drift
status: Ready
created: 2026-07-02
phases: [2, 3]
complexity: complex
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-02.
> To execute: `/execute-plan docs/bugs/worktree-claude-doc-drift/plans/all-phases-worktree-doc-drift-part-2.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 2. Sibling parts:
> - part 1: `C:\Users\JacobMadsen\source\repos\claude-config\docs\bugs\worktree-claude-doc-drift\plans\all-phases-worktree-doc-drift-part-1.md`
> - part 2: `C:\Users\JacobMadsen\source\repos\claude-config\docs\bugs\worktree-claude-doc-drift\plans\all-phases-worktree-doc-drift-part-2.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution. **Do NOT run this part until Part 1's tracer completed green** (its `setup.ps1 check` showed `OK` for `Cognito.Core\CLAUDE.local.md` across all worktrees). Part 1 falsifying → this part is void until `setup.ps1` is fixed.

# Implementation Plan — worktree-claude-doc-drift (Part 2 of 2)  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\bugs\worktree-claude-doc-drift\PHASES.md` (worktree-claude-doc-drift, 3 phases — this part covers Phases 2 and 3)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\bugs\worktree-claude-doc-drift\SPEC.md`

**Total phases:** 2 (this part: Phases 2, 3) of 3 across the feature
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is the **claude-config** harness repo — its gates are PowerShell/symlink state, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| — | — | — | (none — no completed hard upstream dependencies; SPEC `**Depends on:**` absent → `(none)`) |

**Intra-feature reference:** this part depends on **Part 1** (Phase 1) having validated the nested-symlink mechanism. Part 1's proven facts this part relies on: (a) nested `RootFiles` subpaths symlink correctly with zero `setup.ps1` change; (b) relocated sources are git-trackable in claude-config **without `-f`**; (c) deleting -B/-C stopgaps before bootstrap avoids the `setup.ps1:149-151` WARN-skip.

## Plan-specific execution notes (OVERRIDES the contract defaults — read before Step B)

This is the **claude-config harness repo** — **no C#/TS build, no test runner, no `/msbuild`/`/mstest`/`/nxbuild`, no MCP/app runtime** (PHASES.md header: `MCP runtime: not-required`). The contract's default quality gates DO NOT APPLY. Substitute:

- **The quality gate for Phase 2 is `.\setup.ps1 check` console output**: `OK` for all 11 subdir mappings across every present worktree, zero `WARN`/`REAL`/`MISSING`/`WRONG` rows for subdir docs, and the `broken` count not increased vs. the pre-change baseline.
- **The quality gate for Phase 3 is a before/after `.\setup.ps1 check` comparison**: the two new advisory passes emit their expected lines (`BEHIND` for -D's absent team docs, zero `UNREGISTERED`, no spurious `DRIFT`) AND the numeric `Check: N OK, M broken, K absent` `broken` count is **identical** to a pre-Phase-3 run (warn-only — must not affect exit code). Also run `powershell.exe -Command "... setup.ps1 check; $LASTEXITCODE"`-equivalent to confirm exit behavior unchanged.
- **Phase 2 is a runtime-observation spike** (like Part 1): the closing evidence is the REAL bootstrap+check console output, not a static file read.
- **Phase 3 IS code** — new PowerShell logic in `Invoke-Check`. There is no unit-test harness; verify by running `setup.ps1 check` and inspecting the emitted advisory lines + the unchanged summary counts. Author defensively (Test-Path guards, try/catch mirroring the existing hook advisory block at `setup.ps1:215-259`).
- **PowerShell dialect (Windows).** Run via `powershell.exe -Command "cd 'C:\Users\JacobMadsen\source\repos\claude-config'; .\setup.ps1 <cmd> ..."`. Use `$null`, never `/dev/null`. Absolute paths, quote spaces.
- **Editing.** `manifest.psd1` and `setup.ps1` are real files in the claude-config repo root — edit directly.
- **Commit per phase, do NOT push** (personal repo; pushing left to human).
- **One writer per file.** WU-1 (Phase 2) owns `manifest.psd1` + worktree filesystem; WU-2 (Phase 3) owns `setup.ps1`. Different files, but WU-2 is in a later batch because its UNREGISTERED-guard test depends on Phase 2 completing.

## References — Ground-Truth Touchpoint Audit (verified 2026-07-02, carry into execution)

The **10 remaining subdir docs** (Phase 1 already handled `Cognito.Core`). All verified present as real files in main, mirrored as real stopgap copies in -B and -C, absent in -D, and **trackable in claude-config without `-f`** (deepest path `libs/model.js` check-ignore-verified clean):

| # | Subpath (append to `RootFiles`, single-quoted) | main size | in -B/-C? | in -D? |
|---|---|---|---|---|
| 1 | `Cognito\CLAUDE.local.md` | 3533 | stopgap | no |
| 2 | `Cognito.Services\CLAUDE.local.md` | 7744 | stopgap | no |
| 3 | `Cognito.QueueJob\CLAUDE.local.md` | 3702 | stopgap | no |
| 4 | `Cognito.UnitTests\CLAUDE.local.md` | 4866 | stopgap | no |
| 5 | `Cognito.Web.Client\CLAUDE.local.md` | 3338 | stopgap | no |
| 6 | `Cognito.Web.Client\apps\spa\CLAUDE.local.md` | (verify) | stopgap | no |
| 7 | `Cognito.Web.Client\apps\client\CLAUDE.local.md` | 2243 | stopgap | no |
| 8 | `Cognito.Web.Client\libs\model.js\CLAUDE.local.md` | (verify) | stopgap | no |
| 9 | `Cognito.Web.Client\libs\types\CLAUDE.local.md` | (verify) | stopgap | no |
| 10 | `Cognito.Web.Client\libs\vuemodel\CLAUDE.local.md` | (verify) | stopgap | no |

> **NOTE — do NOT register `node_modules` copies.** The main worktree also contains many `**/node_modules/**/CLAUDE.local.md` files (dependency artifacts, gitignored). These are NOT personal docs and NOT part of this fix. Register ONLY the 10 source-tree subpaths above (+ the tracer from Part 1 = 11 total).

| Planned file | Exists? | Verified reality | Action | Directive |
|---|---|---|---|---|
| `manifest.psd1` | yes | After Part 1, `RootFiles` = `('CLAUDE.local.md', 'worktree-wizard.ps1', 'Cognito.Core\CLAUDE.local.md')` | modify | Append the 10 remaining subpaths to the same `RootFiles` array. |
| `setup.ps1` | yes | `Invoke-Check` @ 175-262; `Check:` summary `Write-Host` @ **line 213**; warn-only hook advisory block @ **215-259** (the template); `return ($broken -eq 0)` @ **261** | modify | Insert two warn-only passes AFTER line 213's summary and BEFORE line 261's `return`, styled on 215-259. Never mutate `$broken`. |
| `repos\cognito-forms\**\CLAUDE.local.md` ×10 | **NO — net-new** | claude-config holds only root doc + `Cognito.Core` (from Part 1) | create via bootstrap MOVE + `git add` | Bootstrap MOVEs from main; `git add` without `-f` (Part 1 proved trackable). |
| Worktree paths (main / -B / -C / -D / -A) | main present; -B/-C present w/ 10 stopgaps each; -D present, 0 subdir docs; **-A absent** | from manifest `Repos` entries (`.Path`; B/C/D `Alias='cognito-forms'`) | delete -B/-C stopgaps; -A no-op | Delete the 10 -B and 10 -C stopgaps pre-bootstrap. Phase-3 passes must `Test-Path`-guard every worktree so absent `-A` (and any future gap) no-ops, never throws. |

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | worktree-claude-doc-drift | P2 | Bulk relocate & register the remaining 10 subdir docs | Part 1 (P1 mechanism validated) | Solo |
| 2 | worktree-claude-doc-drift | P3 | Warn-only Invoke-Check additions — drift detector + backfill guard | P2 (soft — zero UNREGISTERED false positives) | Solo |

---

### Phase: worktree-claude-doc-drift P2 — Bulk relocate & register the remaining 10 subdir docs

**Goal:** Apply the Part-1-validated mechanism to the other 10 subdir `CLAUDE.local.md` docs so all 11 are claude-config-managed symlinks in every present worktree.

**Entry criteria:** Part 1 complete (tracer green; staging approach known — `git add` without `-f`). Working tree otherwise clean.

**SPEC.md references:** "Proven Findings #1"; "Decided Approach → Personal subdir docs" + "Stopgap copies → replace with symlinks"; "Reuse Ledger" rows 1-3.

#### WU-1 — Bulk-register, relocate, and validate the 10 remaining subdir docs

- **Scope** (covers all four Phase-2 `### Deliverables` — one atomic bulk sequence; the `setup.ps1 check` observable cannot go green until every subpath is registered, every stopgap deleted, and bootstrap run, so splitting is impossible):
  - Append the 10 remaining nested subpaths (audit table above) to `RootFiles` in `manifest.psd1`.
  - Delete all remaining `-B`/`-C` stopgap copies of those 10 docs (10 in -B, 10 in -C).
  - Run `.\setup.ps1 bootstrap -Target Repos`; `git add` the 10 relocated sources in claude-config (no `-f`, per Part 1).
  - Run `.\setup.ps1 check -Target Repos` → `OK` for all 11 subdir mappings across all present worktrees.
- **TDD:** no (executable verification is `setup.ps1 check`).
- **Files to create/modify:**
  - `manifest.psd1` — append 10 single-quoted subpaths to the `RootFiles` array. [VERIFY after edit: `powershell.exe -Command "(Import-PowerShellDataFile 'C:\Users\JacobMadsen\source\repos\claude-config\manifest.psd1').Repos['cognito-forms'].RootFiles.Count"` → expect 13 (2 original + Cognito.Core + 10)]
  - `repos\cognito-forms\**\CLAUDE.local.md` ×10 — net-new via bootstrap MOVE.
  - Delete: the 10 stopgap copies in `Cognito Forms-B\` and the 10 in `Cognito Forms-C\` (paths = the 10 subpaths under each worktree root).
- **Implementation goal / step sequence (capture real console output at each observe-step):**
  1. **Baseline:** `.\setup.ps1 check -Target Repos`; record the `broken` count.
  2. **Edit manifest:** append the 10 subpaths (audit table) to `RootFiles`. Reuses the nested mapping loop [VERIFY: `grep -n "Expand-RepoPath \"repos" "C:\Users\JacobMadsen\source\repos\claude-config\setup.ps1"`].
  3. **Delete stopgaps:** for each of the 10 subpaths, delete the -B and -C copies (Test-Path-guard each before Remove). This avoids the `setup.ps1:149-151` WARN-skip [VERIFY: `grep -n "both live and repo exist" "C:\Users\JacobMadsen\source\repos\claude-config\setup.ps1"`].
  4. **Bootstrap:** `.\setup.ps1 bootstrap -Target Repos`. **Observe:** 10 `MOVE` lines (main) + `LINK (recovery)` lines for -B/-C/-D. Capture actual output.
  5. **Stage:** `git add` the 10 relocated sources under `repos/cognito-forms/`. Confirm no `-f` needed (`git status` shows them staged, not ignored).
  6. **Verify (closing evidence):** `.\setup.ps1 check -Target Repos`; confirm `OK` for all 11 subdir mappings in main+-B+-C+-D, **zero** `WARN`/`REAL`/`MISSING`/`WRONG` rows for subdir docs, `broken` count == baseline. Spot-check content integrity on 2-3 relocated docs (byte size matches the audit table; e.g. `Cognito.Services` = 7744, `Cognito` = 3533).
- **Spec requirements:** Phase 2 "Minimum Verifiable Behavior": `OK` for all 11 subdir mappings in every present worktree; no `WARN`/`REAL`/`MISSING` rows for subdir docs; -D now carries all 11 as symlinks.
- **HALT:** any `WARN`/`REAL`/`MISSING`/`WRONG` row for a subdir doc after bootstrap, or `broken` count increase → write `BLOCKED.md` with the failing console line (the Part-1 mechanism did not generalize — investigate the specific path, likely a deep `libs/*` or `apps/*` nesting edge case).
- **Batch:** 1 (solo).

**PHASES.md Runtime Verification rows (PHASES-owned):** `setup.ps1 check` OK for all 11 mappings × all present worktrees, zero WARN/REAL/MISSING for subdir docs; -D gains working symlinks it previously lacked. Satisfied by WU-1 step 6.

---

### Phase: worktree-claude-doc-drift P3 — Warn-only Invoke-Check additions: team-doc drift detector + unregistered-subdir backfill guard

**Goal:** Add two warn-only passes to `setup.ps1`'s `Invoke-Check`, modeled on the existing warn-only hook-registration advisory block (`setup.ps1:215-259`). Neither pass mutates anything or changes the exit code (`$broken` unchanged).

**Entry criteria:** Phase 2 complete (soft — so the UNREGISTERED backfill guard finds zero false positives; all 11 subdir docs registered). File-independent of Phases 1-2 (touches `setup.ps1` only).

**SPEC.md references:** "Proven Findings #2" (team-owned docs stay git-tracked real files; add a warn-only drift detector); "Decided Approach → Team-owned files → drift detection (warn only)"; "Reuse Ledger" row 4 (build-new drift detector); "Open Questions" resolutions (canonical ref = main worktree working copy; stale-branch absence → `BEHIND`, not `DRIFT`; `-A`/absent → `Test-Path` no-op; backfill guard = `UNREGISTERED`).

#### WU-2 — Add two warn-only advisory passes to `Invoke-Check` (drift detector + backfill guard)

- **Scope** (covers all four Phase-3 `### Deliverables` — merged into ONE WU: both passes append to the same `Invoke-Check` region, share the absent-worktree `Test-Path` guard, and are jointly verified by a single before/after `setup.ps1 check` comparison; splitting into same-file WUs would violate the file-overlap rule for no benefit):
  - Team-owned-doc drift pass: per worktree, compare tracked `AGENTS.md`, `CLAUDE.md`, `Cognito.Web.Client\AGENTS.md` against the MAIN worktree working copy. Emit `DRIFT` on content difference; emit `BEHIND` (separate advisory) when the file is absent because the branch is stale. Warn-only — never copy/symlink/mutate git-tracked content.
  - Unregistered-subdir backfill guard: scan the main worktree for `*/CLAUDE.local.md` files (excluding `node_modules` and `.claude`) not present in the manifest `RootFiles`; emit `UNREGISTERED` for each.
  - Absent-worktree guard: both passes `Test-Path`-guard every worktree path (so absent `-A` / future gaps no-op, never throw).
  - Verify: `.\setup.ps1 check` exercises both passes; `broken` count / exit code unchanged from before.
- **TDD:** no (no unit harness; verified by `setup.ps1 check` before/after console + count comparison).
- **Files to create/modify:**
  - `setup.ps1` — inside `Invoke-Check`, insert the two passes AFTER the `Check: $ok OK, $broken broken, $absent absent` summary line (**~line 213**) and BEFORE `return ($broken -eq 0)` (**~line 261**), i.e. after the existing hook advisory block. [VERIFY the insertion anchors: `grep -n "Check: \$ok OK" "C:\Users\JacobMadsen\source\repos\claude-config\setup.ps1"` and `grep -n "return (\$broken -eq 0)" "C:\Users\JacobMadsen\source\repos\claude-config\setup.ps1"`]
- **Implementation goal (mirror the existing advisory-block idioms — `Import-PowerShellDataFile`, `Test-Path` guards, `Write-Host ... -ForegroundColor Yellow`, try/catch; do NOT touch `$broken`):**
  - **Enumerate worktrees from the manifest** (same source as `Get-AllMappings` [VERIFY: `grep -n "Import-PowerShellDataFile" "C:\Users\JacobMadsen\source\repos\claude-config\setup.ps1"`]): re-import `manifest.psd1`; main worktree = `Repos['cognito-forms'].Path`; other worktrees = every `Repos` entry whose `.Alias -eq 'cognito-forms'` (i.e. -B/-C/-D), taking each `.Path`.
  - **Pass 1 — team-doc drift:** for `AGENTS.md`, `CLAUDE.md`, `Cognito.Web.Client\AGENTS.md`:
    - Read the MAIN copy (reference). For each non-main worktree: `Test-Path` the worktree root first (no-op if absent — covers `-A`). Then `Test-Path` the doc: if absent → `Write-Host "  BEHIND   <worktree> | <doc> (branch predates this file)" -ForegroundColor Yellow`. If present → compare content to main (e.g. `(Get-Content -Raw)` equality, or hash); on difference → `Write-Host "  DRIFT    <worktree> | <doc>" -ForegroundColor Yellow`. Absence is `BEHIND`, never `DRIFT`.
  - **Pass 2 — unregistered-subdir backfill guard:** in the MAIN worktree, `Get-ChildItem -Recurse -Filter CLAUDE.local.md`, filtering OUT any path containing `\node_modules\` or `\.claude\`. For each, compute its worktree-relative subpath; if that subpath is NOT in the manifest `RootFiles` (and is not the root `CLAUDE.local.md`) → `Write-Host "  UNREGISTERED <subpath> (author it in manifest RootFiles)" -ForegroundColor Yellow`. After Phase 2, expect ZERO.
  - **Never** increment `$broken`, change the `return` value, copy, symlink, or mutate any git-tracked content.
- **Spec requirements:** Phase 3 "Minimum Verifiable Behavior": `check` prints `BEHIND` for -D's three missing team docs (stale branch `inno/documents-and-signing`), ZERO `UNREGISTERED` rows (all 11 registered in Phase 2), no `DRIFT` for worktrees whose team docs match main, and the final `Check: N OK, M broken, K absent` `broken` count is identical to a pre-Phase-3 run.
- **Verification (closing evidence):** run `.\setup.ps1 check` (capture full output). Confirm: (a) `BEHIND` lines for -D's `AGENTS.md` / `CLAUDE.md` / `Cognito.Web.Client\AGENTS.md`; (b) zero `UNREGISTERED`; (c) no spurious `DRIFT`; (d) the `broken` count equals the pre-Phase-3 baseline. Also confirm no exception is thrown for absent `-A`.
- **HALT:** if adding the passes changes the `broken` count / exit code, or throws on an absent worktree → `BLOCKED.md` (the pass is not warn-only / not guarded; fix before completion).
- **Batch:** 2 (later than WU-1: the UNREGISTERED guard's zero-false-positive assertion depends on Phase 2 having registered all 11 docs).

**PHASES.md Runtime Verification rows (PHASES-owned):** `BEHIND` (not `DRIFT`) for -D's absent team docs; zero `UNREGISTERED` post-Phase-2; unchanged `broken` count; clean no-op on absent worktree. Satisfied by WU-2's verification run.

## Work Units

- [ ] WU-1 — Bulk-register, relocate, and validate the 10 remaining subdir docs (Phase 2)
- [ ] WU-2 — Add two warn-only advisory passes to `Invoke-Check` (Phase 3)

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | None (manifest.psd1 + worktree filesystem) |
| 2     | WU-2      | Solo      | None (setup.ps1) — later batch: depends on Phase 2 registration |

---

## Post-phase (per the single-sourced contract)

After each phase's gate is green: update `PHASES.md` (tick that phase's `### Deliverables`; the `<!-- verification-only -->` Runtime Verification rows are ticked by the observed `setup.ps1 check` output), record the phase's Integration Notes, then commit (do NOT push). Phase 3 is terminal — after WU-2 commits, this part (and the feature's implementation) is done; the pipeline advances to its completion gate (which owns the SPEC `**Status:**` flip / `FIXED.md` receipt — do NOT author those as work units here).
