---
kind: implementation-plan
feature_id: worktree-claude-doc-drift
status: Complete
created: 2026-07-02
phases: [1]
complexity: complex
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-02.
> To execute: `/execute-plan docs/bugs/worktree-claude-doc-drift/plans/all-phases-worktree-doc-drift-part-1.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 2. Sibling parts:
> - part 1: `C:\Users\JacobMadsen\source\repos\claude-config\docs\bugs\worktree-claude-doc-drift\plans\all-phases-worktree-doc-drift-part-1.md` (this file)
> - part 2: `C:\Users\JacobMadsen\source\repos\claude-config\docs\bugs\worktree-claude-doc-drift\plans\all-phases-worktree-doc-drift-part-2.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution. **Part 1 is a de-risk gate:** if its tracer falsifies the nested-symlink assumption, this part halts via `BLOCKED.md` and Part 2 MUST NOT run.

# Implementation Plan — worktree-claude-doc-drift (Part 1 of 2)  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\bugs\worktree-claude-doc-drift\PHASES.md` (worktree-claude-doc-drift, 3 phases — this part covers Phase 1)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\docs\bugs\worktree-claude-doc-drift\SPEC.md`

**Total phases:** 1 (this part) of 3 across the feature
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is the **claude-config** harness repo — its gates are PowerShell/symlink state, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| — | — | — | (none — this plan has no completed hard upstream dependencies; SPEC `**Depends on:**` is absent → treated as `(none)`) |

## Plan-specific execution notes (OVERRIDES the contract defaults — read before Step B)

This is the **claude-config harness repo**. It has **no C#/TS build, no test runner, no `/msbuild`/`/mstest`/`/nxbuild` gates, and no MCP/app runtime** (PHASES.md header: `MCP runtime: not-required`). The contract's default quality gates DO NOT APPLY. Substitute:

- **The quality gate for this part is the observed console output of `.\setup.ps1 check`** run from the claude-config repo root. "Green" = the tracer's mapping shows `OK` across all present worktrees and the overall `Check:` `broken` count did not increase versus the pre-change baseline.
- **This is a runtime-observation spike, not a code-writing WU.** The closing evidence is the REAL `setup.ps1 bootstrap` + `setup.ps1 check` console output. A static read of `setup.ps1`/`manifest.psd1` does **NOT** satisfy it. Capture the actual `MOVE`/`LINK`/`OK` lines.
- **No TDD.** There is no test framework here; the executable verification IS `setup.ps1`. Skip the contract's test-agent / red-green steps.
- **PowerShell dialect (Windows).** Run `setup.ps1` via `powershell.exe -Command "cd 'C:\Users\JacobMadsen\source\repos\claude-config'; .\setup.ps1 <cmd> -Target Repos"`. Never `/dev/null` — use `$null`. Absolute paths, quote spaces.
- **Editing through symlinks.** `manifest.psd1` and `setup.ps1` live in the claude-config repo root and are real files there — edit them directly. (The docs they manage are symlinks; you are editing the manifest, not the symlinks.)
- **Commit, do NOT push.** claude-config is a **personal** repo (default git identity), so `git push` is *not* hook-blocked — but this pipeline commits per phase and leaves pushing to the human. Commit after the phase completes and the gate is green; do not push.
- **One writer per file.** This part runs solo (single WU). Do not dispatch a background agent against `manifest.psd1` or any worktree.

## References — Ground-Truth Touchpoint Audit (verified 2026-07-02, carry into execution)

| Planned file | Exists? | Verified reality | Action | Reuse / refactor directive |
|---|---|---|---|---|
| `manifest.psd1` | yes | `Repos.'cognito-forms'.RootFiles = @('CLAUDE.local.md', 'worktree-wizard.ps1')` at **line 27**; `worktree-wizard.ps1` already resolves as a working nested-free RootFile (verified symlink present in main) | modify | Append the tracer subpath to the **existing** `RootFiles` array — do NOT introduce a `RootFilesNested` key (SPEC "Decided Approach": overload `RootFiles`). |
| `repos\cognito-forms\Cognito.Core\CLAUDE.local.md` | **NO — net-new** | claude-config `repos/cognito-forms/` currently holds only root `CLAUDE.local.md` + `.claude/{settings.json,settings.local.json}` | create (via bootstrap MOVE) | Bootstrap MOVEs the real file from the main worktree; then `git add`. Verified **NOT gitignored** in claude-config (`git check-ignore` clean) → `git add` works WITHOUT `-f`. |
| `Cognito Forms\Cognito.Core\CLAUDE.local.md` (main) | yes — real file, 5572 bytes, `islink: False` | source of truth; gitignored in the Cognito repo (`.git/info/exclude:5`), untracked there | convert → symlink | Bootstrap MOVE converts main's real file into a symlink → claude-config. Content must remain byte-identical (5572 bytes). |
| `Cognito Forms-B\Cognito.Core\CLAUDE.local.md` | yes — real stopgap copy | one of 11 stopgaps added to -B this session | delete pre-bootstrap | `setup.ps1:149-151` WARN-skips when both live+repo are real. Delete the -B stopgap BEFORE bootstrap so it re-links instead of WARNing. |
| `Cognito Forms-C\Cognito.Core\CLAUDE.local.md` | yes — real stopgap copy | one of 11 stopgaps added to -C this session | delete pre-bootstrap | Same as -B. |
| `Cognito Forms-D` (worktree) | present; 0 subdir docs | branch `inno/documents-and-signing`; lacked subdir docs entirely | gains symlink | After bootstrap, -D's `Cognito.Core\CLAUDE.local.md` becomes a recovery-`LINK` (setup.ps1:158-165). |
| `Cognito Forms-A` | **absent** (no directory) | confirmed by directory scan | no-op | Bootstrap/check must no-op on the absent path (no manifest entry for `-A`; nothing to do). |

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | worktree-claude-doc-drift | P1 | Tracer — relocate & register ONE subdir doc; validate nested-symlink mechanism end-to-end | — | Solo |

---

### Phase: worktree-claude-doc-drift P1 — Tracer: validate the nested-symlink mechanism on ONE doc

**Goal:** Prove the load-bearing "nested `RootFiles` path → working symlink across all worktrees, zero `setup.ps1` code change" assumption on exactly ONE doc (`Cognito.Core\CLAUDE.local.md`) before Phase 2 bulk-applies to the other 10. This is an explicit **runtime-validation spike** — satisfied only by observing the real `setup.ps1` run.

**Entry criteria:** None (first phase). Working tree clean (dirty-tree check per contract).

**SPEC.md references:** "Proven Findings #1" (relocate subdir docs into claude-config + manifest entries; no setup.ps1 change — `setup.ps1:70,122-126,158-163`); "Decided Approach → Personal subdir docs" (overload `RootFiles`); "Reuse Ledger" rows 1-3 (extend RootFiles / reuse nested Join-Path / reuse bootstrap MOVE, delete stopgaps first).

#### WU-1 — Tracer: register, relocate, and validate `Cognito.Core\CLAUDE.local.md`

- **Scope** (covers ALL four Phase-1 `### Deliverables` — this is one atomic validation sequence; the `setup.ps1 check` observable cannot go green until every step is done, so splitting is impossible):
  - Append `Cognito.Core\CLAUDE.local.md` to `RootFiles` in `manifest.psd1` (cognito-forms entry).
  - Delete the `-B` and `-C` stopgap real-file copies of `Cognito.Core\CLAUDE.local.md`.
  - Run `.\setup.ps1 bootstrap -Target Repos`; confirm main **MOVE**s the real file into `claude-config\repos\cognito-forms\Cognito.Core\CLAUDE.local.md` and produces recovery-`LINK`s on -B/-C/-D.
  - Verify the relocated source is git-trackable in claude-config (`git check-ignore`; `git add -f` only if ignored — verified NOT needed) and `git add` it. Run `.\setup.ps1 check` → OK for this mapping across all present worktrees.
- **TDD:** no (no test framework in this repo; `setup.ps1 check` is the executable verification).
- **Files to create/modify:**
  - `manifest.psd1` — append one string to the `cognito-forms` `RootFiles` array (line 27). In a single-quoted PowerShell string, backslashes are literal: `'Cognito.Core\CLAUDE.local.md'`. [VERIFY: `powershell.exe -Command "(Import-PowerShellDataFile 'C:\Users\JacobMadsen\source\repos\claude-config\manifest.psd1').Repos['cognito-forms'].RootFiles"`]
  - `repos\cognito-forms\Cognito.Core\CLAUDE.local.md` — net-new, created by bootstrap MOVE (do NOT hand-author).
  - Delete: `Cognito Forms-B\Cognito.Core\CLAUDE.local.md`, `Cognito Forms-C\Cognito.Core\CLAUDE.local.md` (real stopgap copies).
- **Implementation goal / step sequence (run in order, capture real console output at each observe-step):**
  1. **Baseline:** run `.\setup.ps1 check -Target Repos` and record the `Check: N OK, M broken, K absent` line — this `M` (broken) is the baseline the post-change run must not exceed. Reuses the existing `Invoke-Check` [VERIFY: `grep -n "function Invoke-Check" "C:\Users\JacobMadsen\source\repos\claude-config\setup.ps1"`].
  2. **Edit manifest:** append `'Cognito.Core\CLAUDE.local.md'` to the `RootFiles` array at `manifest.psd1:27`, keeping the existing two entries. Reuses the RootFiles mapping loop [VERIFY: `grep -n "RootFiles" "C:\Users\JacobMadsen\source\repos\claude-config\setup.ps1"`] which already builds `Live = Join-Path $livePath $f` / `Repo = Expand-RepoPath "repos\$configName\$f"` for nested `$f`.
  3. **Delete stopgaps:** remove the -B and -C real copies (so the `setup.ps1:149-151` WARN-gate does not skip them). [VERIFY path exists first: `Test-Path "C:\Users\JacobMadsen\source\repos\Cognito Forms-B\Cognito.Core\CLAUDE.local.md"`]
  4. **Bootstrap:** run `.\setup.ps1 bootstrap -Target Repos`. **Observe** the console: expect a `MOVE` for main's `Cognito.Core\CLAUDE.local.md` (real file → repo, then symlink) and `LINK (recovery)` for -B/-C/-D. Capture the actual lines.
  5. **Trackability:** `git check-ignore -v repos/cognito-forms/Cognito.Core/CLAUDE.local.md` (expect non-zero = trackable); `git add repos/cognito-forms/Cognito.Core/CLAUDE.local.md`. Record whether `-f` was needed (expected: not needed) — this informs Part 2's staging.
  6. **Verify (the closing evidence):** run `.\setup.ps1 check -Target Repos`; confirm `OK` for the `Cognito.Core\CLAUDE.local.md` mapping in main + -B + -C + -D, and the `broken` count == baseline `M` (did not increase). Confirm main's `Cognito.Core\CLAUDE.local.md` is now a ReparsePoint (`powershell.exe -Command "(Get-Item 'C:\Users\JacobMadsen\source\repos\Cognito Forms\Cognito.Core\CLAUDE.local.md' -Force).Attributes -band [IO.FileAttributes]::ReparsePoint"`) and content is byte-identical to the 5572-byte original.
- **Spec requirements:** SPEC "Minimum Verifiable Behavior" (Phase 1): `setup.ps1 check` prints `OK` for the mapping in main/-B/-C/-D; main's file is a ReparsePoint resolving to the claude-config source; content byte-identical.
- **FALSIFICATION HALT (hard):** if bootstrap does NOT produce a working nested symlink (e.g. Windows path-with-spaces + nested-dir edge case leaves a `WARN`/`REAL`/`MISSING`/`WRONG` row for the tracer, or the `broken` count increased), **STOP and write `BLOCKED.md`** per the Blocking Issue Protocol. The "no setup.ps1 change needed" assumption is falsified; Part 2 (bulk apply) MUST NOT proceed until `setup.ps1` is fixed. Record the exact failing console line in `BLOCKED.md`.
- **Batch:** 1 (solo).

**PHASES.md Runtime Verification rows (PHASES-owned — ticked by the verification observation, NOT authored as separate WUs):** `setup.ps1 check` OK for `Cognito.Core\CLAUDE.local.md` across main+-B+-C+-D; main's copy is a ReparsePoint with unchanged content. These are satisfied by WU-1 step 6's observed output.

## Work Units

- [x] WU-1 — Tracer: register, relocate, and validate `Cognito.Core\CLAUDE.local.md`

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | N/A             |

---

## Post-phase (per the single-sourced contract)

After WU-1's gate is green: update `PHASES.md` (tick Phase 1 `### Deliverables`; the `<!-- verification-only -->` Runtime Verification rows are ticked by the observed `setup.ps1 check` output), record the Phase-1 "Integration Notes" answer (was `git add -f` required? — expected no; did the nested `Join-Path` mapping resolve correctly? — the tracer proves it), then commit (do NOT push — see Plan-specific execution notes). This closes Part 1; the pipeline then advances to Part 2.
