---
kind: fix-plan
feature_id: research-gate-ignores-existing-phases
status: In-progress
created: 2026-06-16
complexity: complex
phases: [1, 2, 3]
source_branch: main
source_commit: 982dc12
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-16.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\research-gate-ignores-existing-phases\plans\fix-research-gate.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — research-gate-ignores-existing-phases

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\research-gate-ignores-existing-phases\PHASES.md` (research-gate-ignores-existing-phases, 3 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\research-gate-ignores-existing-phases\SPEC.md`

**Total phases:** 3
**Plan version:** v1 (reference-based — components loaded from disk per step)

## Work Units

- [x] WU-1 — lazy_core.phases_show_implementation predicate (+ test_lazy_core.py characterization, TDD)
- [ ] WU-2 — lazy-state.py: pre-Step-5 guard skips research when PHASES.md shows implementation (+ --test fixtures, TDD)
- [ ] WU-3 — Lockstep wrapper prose: lazy/SKILL.md + lazy-cloud/SKILL.md Step-5 note

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or `.md` (skill/component) file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: this bug's `PHASES.md`, and project `CLAUDE.md` review edits.

> **EXECUTION-CONTEXT OVERRIDE (autonomous pipeline).** When this plan is executed by a `/lazy-bug-batch` (or `/lazy-batch`) cycle subagent that does NOT have the `Agent` tool, the orchestrator+subagent split is impossible. In that case, perform ALL work INLINE with Read / Edit / Write (the dispatched-skill inline override). Still obey: test-first per TDD WU (write the failing fixture/assertion before the implementation), batch review inline, quality gates, and commit. Never invoke a nested `/lazy*`. This override applies ONLY to the dispatch mechanism; every other rule below stands.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies. It is a self-contained harness-internal bug fix. Authoritative context is the sibling SPEC.md + PHASES.md and the canonical pipeline doc `C:\Users\Jacob\source\repos\claude-config\user\scripts\CLAUDE.md`.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — UNLESS the execution-context override (no Agent tool) applies, in which case work INLINE per that override. The orchestrating session never silently descopes.
2. All edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline — write the failing test/fixture FIRST (RED for the right reason), then implement.
4. PHASES.md is updated AFTER EACH phase completes (not deferred): tick the phase's deliverable boxes, set the phase `**Status:**`, append an Implementation Notes block.
5. Every subagent's (or inline) output is reviewed for correctness, spec alignment, and TDD discipline before continuing.
6. Mistakes are fixed immediately before launching the next phase.
7. Relevant CLAUDE.md files are reviewed/updated after the relevant phase (here: no `user/scripts/CLAUDE.md` change is required — the fix changes no CLI surface or terminal; the research gate's internal behavior is documented by the SPEC/PHASES. Confirm during the post-phase review and note "no CLAUDE.md change needed" if so.)
8. Each completed phase is committed (and pushed to `main`) before the next phase begins, per the atomic gate+commit rule.
9. This plan is self-contained — follow it exactly as written without relying on external context.
10. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
11. **Coupled-pair discipline (HARD).** `/lazy` ↔ `/lazy-cloud` (Phase 3) must keep their Step-5 prose symmetric (only intended divergence: the `--cloud` flag). Never commit one wrapper's prose change without the other in the same phase. Diff the two SKILL.md files against each other immediately after editing.
12. **Baseline regeneration discipline.** The `--test` baseline (`tests/baselines/lazy-state-test-baseline.txt`) is regenerated ONLY by piping live `--test` output through `test_lazy_core.py::_normalize_smoke_output` — NEVER hand-edited. The no-PHASES research path output MUST stay byte-identical to its pre-change baseline; only the new fixtures add lines.
13. **Shared-import gate.** `lazy_core.py` is imported by both `lazy-state.py` and `bug-state.py`. After ANY `lazy_core.py` change (Phase 1) run the FULL set: `lazy-state.py --test`, `bug-state.py --test`, `test_lazy_core.py`, `lazy_coord.py --test`.

## Execution Schedule

| Step | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|----------|----------|------------|-----------|
| 1 | P1 | lazy_core.phases_show_implementation predicate | — | Solo |
| 2 | P2 | lazy-state.py pre-Step-5 guard | P1 (predicate) | Solo |
| 3 | P3 | Lockstep wrapper prose (lazy + lazy-cloud) | P2 (gate behavior) | Solo |

> **Sequencing note:** all phases are Solo (single-WU each), ordered by the dependency chain. P1 adds the reusable predicate the guard consults; P2 inserts the guard that calls it (the behavioral fix); P3 updates the wrapper prose to describe the new gate behavior. Do NOT reorder — the guard cannot be written before the predicate exists, and the prose must not describe behavior that is not yet implemented.

---

### Phase: P1 — lazy_core.phases_show_implementation predicate

**Goal:** Add a deterministic, fence-aware predicate that reports whether a PHASES.md shows implementation evidence, composing the existing `parse_phases` / `count_deliverables` parsers — the reusable primitive the Step-5 guard consults.
**Entry criteria:** none (first phase).
**SPEC.md references:** Theory 1 (Confirmed); Proven Findings 1–2; Locked Decision D2 (implementation-evidence predicate, reuse existing parsers); Open-Q1 (stub PHASES.md must NOT suppress research).

#### WU-1 (TDD: yes) — phases_show_implementation predicate

**Scope (PHASES.md P1 deliverables):**
- `lazy_core.phases_show_implementation(phases_text: str) -> bool` added near `parse_phases` / `count_deliverables`.
- Returns `False` when `parse_phases(phases_text)` yields zero phases (stub guard). Otherwise `True` when ANY of: a parsed phase `status` is `Complete`/`In-progress` (case-insensitive), OR `count_deliverables(phases_text)[1] >= 1`, OR an `## Implementation Notes` heading is present; else `False`.
- Docstring documents the three OR'd signals, the zero-phase stub guard, and that the function is side-effect-free (no `_diag` — the caller emits the diagnostic).
- Tests: RED-first characterization in `test_lazy_core.py`.

**TDD:** yes.

**Files to create/modify:**
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\lazy_core.py` — modify. Verified symbols (read during planning):
  - `def parse_phases(phases_text: str) -> list[dict]:` at ~1384 [VERIFY: grep -n "def parse_phases" user/scripts/lazy_core.py] — returns records with `status` (the first `**Status:**` value inside each phase section, or `None`) and per-phase `unchecked`/`checked`. Reuse it for the phase-status and zero-phase signals.
  - `def count_deliverables(phases_text: str) -> tuple[int, int]:` at ~1218 [VERIFY: grep -n "def count_deliverables" user/scripts/lazy_core.py] — returns `(unchecked, checked)`, fence-aware. Reuse `[1] >= 1` for the checked-box signal (inherits fence-awareness).
  - `def _diag(msg: str) -> None:` at ~83 [VERIFY: grep -n "def _diag" user/scripts/lazy_core.py] — do NOT call it from the predicate; it is the caller's (P2) responsibility.
  - Add the new function adjacent to `parse_phases` (after it, in the "PHASES.md analysis" section ~`:1216`). Status compare must be case-insensitive on the stripped value (`(p.get("status") or "").strip().lower() in {"complete", "in-progress"}`). The Implementation-Notes signal: a line-start regex `re.match(r"^#{2,3}\s+Implementation Notes\b", line)` over the text's lines (or `re.search` with `re.M`).
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\test_lazy_core.py` — modify. Add RED-first characterization tests and register them in the runner list [VERIFY: grep -n "def test_" user/scripts/test_lazy_core.py to find the existing test-registration pattern].

**Test-first plan (write these BEFORE the implementation; confirm RED for the right reason — `AttributeError`/`NameError` because the function does not yet exist):**
- (a) zero parsed phases (a string with no `## Phase` heading) → `False`.
- (b) one phase `**Status:** Complete` → `True`.
- (c) one phase `**Status:** In-progress`, zero checked boxes → `True`.
- (d) phases all `**Status:** Planned` but ≥1 `- [x]` deliverable → `True`.
- (e) phases all `Planned`, zero checked, but an `## Implementation Notes` block present → `True`.
- (f) phases parsed, all `Planned`, zero checked, no Implementation Notes → `False`.
- (g) the only `- [x]` is inside a ```fenced``` block → `False` (fence-awareness inherited from `count_deliverables`).

**Quality gates (run, capture output, paste into Implementation Notes):**
- `python C:\Users\Jacob\source\repos\claude-config\user\scripts\test_lazy_core.py` (authoritative, hermetic — must pass including the new cases).
- `python C:\Users\Jacob\source\repos\claude-config\user\scripts\lazy_coord.py --test` (shared-import sanity).

**Commit (atomic gate+commit, R5):** after gates pass, tick WU-1, set PHASES.md Phase 1 `**Status:** Complete`, append the Implementation Notes block, then
`git add -A && git commit -m "fix(research-gate): add phases_show_implementation predicate (P1)" && git push`.

**Minimum Verifiable Behavior:** `test_lazy_core.py` passes including the seven new cases; an `In-progress`-phase string returns `True`, an empty-stub string returns `False`.

---

### Phase: P2 — lazy-state.py pre-Step-5 guard

**Goal:** Insert the root-cause fix — a guard before the Step-5 research gate that, when `PHASES.md` exists with implementation evidence, emits a diagnostic and falls through to Step 6 instead of routing to `needs-research`.
**Entry criteria:** P1 complete (`phases_show_implementation` exists and is import-callable).
**SPEC.md references:** Theory 1 (Confirmed); Reproduction Steps; Locked Decisions D1 (research is a pre-PHASES gate), D3 (no silent skip — emit `_diag`); Affected Area row "Research gate".

#### WU-2 (TDD: yes) — pre-Step-5 guard in compute_state

**Scope (PHASES.md P2 deliverables):**
- A guard inserted immediately BEFORE the Step-5 research gate: if `PHASES.md` exists AND `lazy_core.phases_show_implementation(phases_text)` is `True`, emit a `_diag(...)` and fall through to Step 6 (do NOT enter/return the research gate).
- The guard fires only when no `RESEARCH.md`/`RESEARCH_SUMMARY.md` is present; when research already exists, behavior is byte-identical (the guard is a no-op there).
- No double-read of `PHASES.md` in the same `compute_state` call — the text read by the guard is reused at Step 6.
- Tests: new `--test` fixtures.

**TDD:** yes.

**Files to create/modify:**
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\lazy-state.py` — modify. Verified symbols (read during planning):
  - Step-5 research gate: `research = spec_path / "RESEARCH.md"` … `if not research.exists() and not research_summary.exists():` at ~1493–1499 [VERIFY: grep -n "Step 5: Research validation gate" user/scripts/lazy-state.py]. Insert the guard ABOVE this block (after the Step-4.6 realign return, before the `research = ...` assignments).
  - Step-6 PHASES read: `phases_file = spec_path / "PHASES.md"` at ~1547–1548 and `phases_text = phases_file.read_text(encoding="utf-8")` at ~1574 [VERIFY: grep -n "phases_file = spec_path" user/scripts/lazy-state.py]. To avoid a double read, hoist the `phases_file` definition + a single `read_text` to the guard (read once into a local such as `phases_text_cached` when the file exists), and have Step 6 reuse that local rather than re-reading. If hoisting is awkward, the guard MAY read independently but the implementer MUST confirm the file is opened at most once per `compute_state` invocation on the gate-skippable path.
  - Guard shape: `phases_file = spec_path / "PHASES.md"`; `if phases_file.exists():` read its text; `if lazy_core.phases_show_implementation(phases_text): _diag("Step 5 research gate skipped: PHASES.md present with implementation evidence — feature is past pre-planning research")` and fall through (do NOT return a research action). The `_diag` STRING is a contract (D3 / probe-greppable) — keep it stable.
  - `_diag` is already imported/used in this module [VERIFY: grep -n "_diag(" user/scripts/lazy-state.py — confirm the import path, e.g. `from lazy_core import ... _diag` or `lazy_core._diag`].
- `C:\Users\Jacob\source\repos\claude-config\user\scripts\tests\baselines\lazy-state-test-baseline.txt` — regenerate via `test_lazy_core.py::_normalize_smoke_output` (NEVER hand-edit). New fixtures add lines; the no-PHASES research path stays byte-identical.

**Test-first plan (write fixtures BEFORE the implementation; confirm RED for the right reason — fixture 1 currently returns `needs-research`). Model on the existing research-gate fixtures [VERIFY: grep -n "needs-research" user/scripts/lazy-state.py to find the fixture style]:**
- (1) `research-gate-skipped-when-phases-implemented` — SPEC (Ready) + PHASES.md with one `In-progress` phase (or ≥1 checked box) + no RESEARCH.md → dispatched action is Step 6/7 (plan/execute), NOT `terminal_reason="needs-research"` and NOT `sub_skill="spec"` research-prompt generation.
- (2) `research-gate-fires-when-no-phases` — SPEC (Ready) + no PHASES.md + no RESEARCH.md → still routes to research (unchanged default).
- (3) `research-gate-fires-when-phases-stub` — SPEC (Ready) + an empty-stub PHASES.md (no phase headings) + no RESEARCH.md → still routes to research (Open-Q1 / D2).
- (4) `research-path-byte-identical-when-research-present` — SPEC + implemented PHASES.md + `RESEARCH_SUMMARY.md` present → routes to Step 6 exactly as today (guard is a no-op).

**Quality gates (run, capture output, paste into Implementation Notes):**
- `python C:\Users\Jacob\source\repos\claude-config\user\scripts\lazy-state.py --test` (hermetic — set an isolated `LAZY_STATE_DIR` per the test-harness gotcha; do NOT delete the orchestrator's live cycle marker).
- `python C:\Users\Jacob\source\repos\claude-config\user\scripts\bug-state.py --test` (shared-import gate).
- `python C:\Users\Jacob\source\repos\claude-config\user\scripts\test_lazy_core.py` (authoritative).
- Regenerate `lazy-state-test-baseline.txt` ONLY via `_normalize_smoke_output`; confirm the default (no-PHASES) path is byte-identical and only the new fixtures added lines.

**Commit (atomic gate+commit, R5):** after gates pass, tick WU-2, set PHASES.md Phase 2 `**Status:** Complete`, append Implementation Notes, then
`git add -A && git commit -m "fix(research-gate): skip Step-5 research gate when PHASES.md shows implementation (P2)" && git push`.

**Minimum Verifiable Behavior:** `lazy-state.py --test` passes including the four new fixtures; a feature with SPEC + an `In-progress` PHASES.md + no RESEARCH.md routes to Step 6/7 (NOT `needs-research`), while the same feature with no PHASES.md still routes to research. The no-PHASES baseline is byte-identical.

---

### Phase: P3 — Lockstep wrapper prose (lazy + lazy-cloud)

**Goal:** Update the two paired wrappers' Step-5 state-machine prose to accurately describe the new gate behavior (research skipped when PHASES.md shows implementation). No logic — prose only, per the coupled-pair rule.
**Entry criteria:** P2 complete (the gate behavior exists).
**SPEC.md references:** Affected Area row "Wrappers (lockstep)"; repo `CLAUDE.md` Coupled Skill Pairs (`/lazy` ↔ `/lazy-cloud`).

#### WU-3 (TDD: no — prose) — wrapper Step-5 note

**Scope (PHASES.md P3 deliverables):**
- `user/skills/lazy/SKILL.md`: in the state-machine prose summary and/or the Step-4.5-vs-5 explainer, add a sentence that the Step-5 research gate is skipped when `PHASES.md` already shows implementation evidence (any phase Complete/In-progress, a checked deliverable, or an Implementation Notes block) — falls through to Step 6 — and that the predicate + diagnostic live in `lazy-state.py`/`lazy_core.py`.
- `repos/algobooth/.claude/skills/lazy-cloud/SKILL.md`: mirror the same note at its Step-5 mention. Only intended divergence: the `--cloud` flag.
- Diff the two SKILL.md files against each other after editing; confirm Step-5 prose matches modulo `--cloud`.

**TDD:** no (documentation/prose — verified structurally).

**Files to create/modify:**
- `C:\Users\Jacob\source\repos\claude-config\user\skills\lazy\SKILL.md` — modify. Anchors: state-machine prose summary at ~270, Step-4.5-vs-5 explainer at ~272–275 [VERIFY: grep -n "research gate" user/skills/lazy/SKILL.md].
- `C:\Users\Jacob\source\repos\claude-config\repos\algobooth\.claude\skills\lazy-cloud\SKILL.md` — modify. Anchors: Step-5 mentions at ~133, ~90 [VERIFY: grep -n "needs-research" repos/algobooth/.claude/skills/lazy-cloud/SKILL.md].

**Quality gates (run, capture output, paste into Implementation Notes):**
- `python C:\Users\Jacob\source\repos\claude-config\user\scripts\lint-skills.py` (no broken `!cat` injections).
- `python C:\Users\Jacob\source\repos\claude-config\user\scripts\project-skills.py` (re-projects both wrappers without errors).
- `grep` both SKILL.md files to confirm the new "research gate skipped when PHASES.md shows implementation" prose is present in EACH; diff the two to confirm the Step-5 prose is mirrored modulo `--cloud`.

**Commit (atomic gate+commit, R5):** after gates pass, tick WU-3, set PHASES.md Phase 3 `**Status:** Complete`, set the top-level PHASES `**Status:**` to `In-progress` (implementation done, validation pending — NEVER `Fixed`), append Implementation Notes, then
`git add -A && git commit -m "fix(research-gate): lockstep wrapper Step-5 prose for lazy + lazy-cloud (P3)" && git push`.

**Minimum Verifiable Behavior:** `lint-skills.py` clean; `project-skills.py` re-projects both wrappers without errors; both SKILL.md files contain the new Step-5 note and the prose is mirrored (modulo `--cloud`). The behavioral proof lives in the P1–P2 `--test` fixtures.

---

## After all phases complete

- All three phases committed and pushed to `main`.
- Top-level `PHASES.md` `**Status:**` is `In-progress` (implementation done; validation pending). Do NOT flip SPEC.md/PHASES.md to `Fixed` and do NOT write `FIXED.md` — those are the `__mark_fixed__` gate's, after the validation tail.
- Let the state machine route to the next step (validation / mark-fixed). This plan's job ends when all WU boxes are ticked and the work is committed.
