---
kind: implementation-plan
feature_id: ensure-runtime-starves-pre-vite-sidecar-build
status: In-progress
created: 2026-06-22
complexity: complex
phases: [1, 2, 3]
---

> **Mobile plan** — generated by `/write-plan` (via `/plan-bug`) on 2026-06-22.
> To execute: `/execute-plan docs/bugs/ensure-runtime-starves-pre-vite-sidecar-build/plans/fix-pre-vite-boot-liveness.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — `ensure_runtime` starves the pre-Vite `sidecar:build` window of a cold boot  (v2)

**PHASES.md files:**
- `docs/bugs/ensure-runtime-starves-pre-vite-sidecar-build/PHASES.md` (ensure-runtime-starves-pre-vite-sidecar-build, 3 phases)

**SPEC.md files:**
- `docs/bugs/ensure-runtime-starves-pre-vite-sidecar-build/SPEC.md`

**Total phases:** 3 (single part — all phases fit under the 8-WU cap)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.ts`, `.js`, `.cs`, `.vue`, `.py`, `.rs`, `.tsx`, `.jsx`, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **DISPATCH-ENVIRONMENT OVERRIDE (read at execution time):** if the executing session is itself a dispatched cycle subagent WITHOUT the `Agent` tool, perform ALL work INLINE with Read/Edit/Write (the override in the dispatch prompt is authoritative over the orchestrator+subagent model above). The test-first discipline still holds: write the failing test, then the implementation, in that order, for each TDD work unit.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against. The executing session SHOULD `Read` these before starting any work unit whose Implementation goal references the corresponding upstream contract. Listed in dependency order.

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| ensure-runtime-recovery-starves-cold-compile | sibling (Fixed) | `docs/bugs/_archive/ensure-runtime-recovery-starves-cold-compile/PHASES.md` | The PRIOR FIX this one extends — `_classify_compile_state`, `_default_frontend_probe`, `_await_compile_serving`, the `frontend_probe` injection seam, the `_cold_compile_timeout_blocker`, and the cold-compile-sized ceiling are all REUSED here for the pre-Vite window. Every invariant it established must stay green. |

The SPEC's `**Related:**` line is a bug-pipeline reference list, not a machine-parseable `**Depends on:**` block, so no automated upstream look-back fires. The sibling above is the load-bearing reference.

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines). (Under the dispatch-environment override above, perform inline instead — test-first preserved.)
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents
3. Every TDD work unit goes through the test-first pipeline — failing tests written first, then implementation makes them pass
4. PHASES.md is updated AFTER EACH batch completes (not deferred)
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing
6. Mistakes are fixed immediately before launching the next batch
7. After all batches in a phase finish, integration verification confirms all changes work together
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it (here: `user/scripts/CLAUDE.md`'s `--ensure-runtime` doc in Phase 3)
9. Each completed phase is committed and pushed before the next phase begins
10. Cross-feature phases may run in parallel when dependencies are satisfied and no file conflicts exist (N/A — this plan is one feature, strictly sequential phases)
11. This plan is self-contained — follow it exactly as written without relying on external context
12. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory

## Work Units

- [x] WU-1 — Boot-liveness config seam + injected `boot_alive` callable + extended `_classify_compile_state`
- [x] WU-2 — Phase 1 hermetic tests (extended truth table + default-off back-compat)
- [x] WU-3 — Route pre-Vite live-boot off recovery in BOTH routers (`_route_legacy_non_serving` + M4 `_route_non_serving`) + `_await_compile_serving` went-dead handling
- [x] WU-4 — Phase 2 hermetic fixtures (legacy + M4 patient-wait, went-dead fall-through, ceiling-timeout, genuine-crash preserved, default-off)
- [ ] WU-5 — Phase 3 CLI-seam confirmation + consumer/doc alignment + bidirectional reverse-reference
- [ ] WU-6 — Phase 3 parity audit + baseline confirmation + skill lint/projection

## Execution Schedule

| Step | Feature | Phase(s) | Title | Blocked by | Parallel? |
|------|---------|----------|-------|------------|-----------|
| 1 | ensure-runtime-starves-pre-vite-sidecar-build | P1 | Boot-liveness signal + pre-Vite-aware discriminator | — | Solo |
| 2 | ensure-runtime-starves-pre-vite-sidecar-build | P2 | Route pre-Vite live-boot off recovery (both routers) | P1 | Solo |
| 3 | ensure-runtime-starves-pre-vite-sidecar-build | P3 | CLI/doc alignment + parity + baselines | P2 | Solo |

---

### Phase: ensure-runtime-starves-pre-vite-sidecar-build P1 — Boot-liveness signal + pre-Vite-aware discriminator

**Goal:** Add a boot-process-liveness signal to the ensure-runtime config/injection surface and extend the cold-compile discriminator so a both-ports-down observation with a LIVE boot process classifies as patient-wait, not `dead`. Default-off byte-identical preserved.
**Entry criteria:** None (first phase). The prior-fix symbols (`_classify_compile_state`, `_default_frontend_probe`, `_await_compile_serving`, the `frontend_probe`/`sidecar_check` seams) already exist on `main`.
**SPEC.md references:** Proven Findings (classification gap); Open Question 1 (which "still booting" signal — candidate (a) boot-process liveness, decided in PHASES.md); Open Question 3 (default-off repo-agnostic byte-identity).

#### WU-1 — Boot-liveness config seam + injected `boot_alive` callable + extended `_classify_compile_state`

- **Scope:** PHASES.md Phase 1 deliverables 1–4 — add the boot-liveness config key, thread the injected `boot_alive` callable through `ensure_runtime`, extend `_classify_compile_state` with a back-compat `boot_alive` parameter, and add the production `_default_boot_alive` source (or document the `Popen`-handle threading from `restart()`).
- **TDD:** yes (the extended classifier + the back-compat default + the injection seam are all testable in WU-2; author the implementation here, the tests in WU-2 per the test-first protocol — write WU-2's failing tests BEFORE this implementation under the inline override).
- **Files to create/modify:** `user/scripts/lazy_core.py`.
- **Implementation goal:**
  - Add a boot-liveness config key to `_ENSURE_RUNTIME_DEFAULT_CONFIG` [VERIFY: grep -n "_ENSURE_RUNTIME_DEFAULT_CONFIG: dict" user/scripts/lazy_core.py] documented as the pre-Vite "still booting" signal and parameterized repo-agnostic exactly like `frontend_health_url`/`assert_sidecar_connected`. The DEFAULT MUST yield "no boot-liveness signal" (so a repo without it degrades to today's `dead`). Read via `.get()` everywhere so a legacy override lacking the key never raises (mirror the `assert_sidecar_connected` back-compat pattern).
  - Extend `_classify_compile_state(backend_code, frontend_up, boot_alive=False)` [VERIFY: grep -n "def _classify_compile_state" user/scripts/lazy_core.py] with a back-compat `boot_alive` parameter DEFAULTING to `False` so every existing positional caller is byte-identical. New branch: `backend_code != 200 and not frontend_up and boot_alive` ⇒ return `"compiling"` (reuse the existing patient-wait path — ⚖ decided in PHASES.md: reuse the label, do not add a new `"booting"` state). All prior branches unchanged: `200 ⇒ serving`; `non-200 and frontend_up ⇒ compiling`; `non-200 and not frontend_up and not boot_alive ⇒ dead`. Pure function, no I/O.
  - Thread an optional injected `boot_alive` callable through `ensure_runtime(...)` [VERIFY: grep -n "def ensure_runtime" user/scripts/lazy_core.py], default-bound from the config like `frontend_probe`/`sidecar_check` [VERIFY: grep -n "if frontend_probe is None" user/scripts/lazy_core.py]: when the config carries the boot-liveness key bind the real source, else bind `lambda: False`. In production the source reads the liveness of the `restart()`-spawned boot process — REUSE the `Popen` handle the `restart()` closure already owns [VERIFY: grep -n "subprocess.Popen" user/scripts/lazy_core.py] / the `spawn_detached` primitive [VERIFY: grep -n "def spawn_detached" user/scripts/lazy_core.py]; do NOT invent a parallel spawn/poll scheme.
  - Add `_default_boot_alive(...)` modeled on `_default_frontend_probe` [VERIFY: grep -n "def _default_frontend_probe" user/scripts/lazy_core.py] / `_default_sidecar_probe` [VERIFY: grep -n "def _default_sidecar_probe" user/scripts/lazy_core.py]; never raises (any error → False = fail-safe toward NOT-booting/dead). If liveness is read from the in-process `Popen` handle rather than a URL, document inline why no URL probe is needed and how the handle reaches the classifier (then no `_default_*` URL helper is required).
- **Spec requirements:** SPEC "The missing signal" / Open Question 1 — process-liveness of the orchestrator-spawned boot process is the chosen signal; SPEC Open Question 3 — default-off byte-identity is mandatory.
- **Batch:** 1.

#### WU-2 — Phase 1 hermetic tests (extended truth table + default-off back-compat)

- **Scope:** PHASES.md Phase 1 deliverable 5 — the extended `_classify_compile_state` truth table + config-default + injection-seam tests.
- **TDD:** yes (these tests are WRITTEN FIRST — they fail RED against the un-extended classifier/config, then WU-1's implementation makes them pass).
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:**
  - `_classify_compile_state(0, False, True)` ⇒ `"compiling"` (both ports down + live boot ⇒ patient-wait).
  - `_classify_compile_state(0, False, False)` ⇒ `"dead"` (both ports down + dead boot ⇒ UNCHANGED).
  - `_classify_compile_state(0, False)` (no `boot_alive` arg) ⇒ `"dead"` (back-compat default — byte-identical to the prior fix's three-branch truth table).
  - `_classify_compile_state(200, anything, anything) ⇒ "serving"` and `_classify_compile_state(0, True, anything) ⇒ "compiling"` (prior branches unchanged regardless of `boot_alive`).
  - The new config key defaults correctly; a legacy config dict WITHOUT the key does not crash `ensure_runtime` (mirror `test_ensure_runtime_legacy_config_without_sidecar_key_does_not_crash` [VERIFY: grep -n "legacy_config_without" user/scripts/test_lazy_core.py]).
  - The `boot_alive` injection seam threads through `ensure_runtime` without crashing (mirror the prior fix's `test_ensure_runtime_threads_injected_frontend_probe_to_m4` [VERIFY: grep -n "threads_injected_frontend_probe" user/scripts/test_lazy_core.py]).
- **Implementation goal:** All new tests pass; the prior-fix `_classify_compile_state` tests stay green (byte-identical for `boot_alive` absent).
- **Spec requirements:** as WU-1.
- **Batch:** 1 (test-first: author this WU's tests RED, then WU-1's implementation green; group as one batch since they touch a disjoint file pair and one is the gate for the other).

**Batch overview (Phase 1):**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-2 (tests, RED first) → WU-1 (impl, GREEN) | No (test-first ordering) | None (disjoint files) |

**Minimum Verifiable Behavior:** `python3 user/scripts/test_lazy_core.py` passes with the extended classifier returning `compiling` for `(0, False, True)` and `dead` for `(0, False, False)`; the prior three-branch table byte-identical for `boot_alive` absent; `python3 user/scripts/lazy-state.py --test` green.

---

### Phase: ensure-runtime-starves-pre-vite-sidecar-build P2 — Route pre-Vite live-boot off the crash-recovery loop (both routers)

**Goal:** Rework BOTH non-serving routers (legacy `_route_legacy_non_serving` + M4 `_route_non_serving`) so a both-ports-down runtime with a live boot process is patiently waited on via `_await_compile_serving` instead of kill-restarted. Reserve the bounded recovery loop strictly for genuine `dead`.
**Entry criteria:** Phase 1 complete (the `boot_alive` signal, extended classifier, config key, injection seam exist).
**SPEC.md references:** Affected Area (both routers + bounded recovery); Open Question 2 (preserve genuine-crash recovery); Theory 1 (the kill-restart loop is the starvation mechanism).

#### WU-3 — Route pre-Vite live-boot off recovery in both routers + `_await_compile_serving` went-dead handling

- **Scope:** PHASES.md Phase 2 deliverables 1–4 — thread `boot_alive` into both routers' `_classify_compile_state` calls, mirror in M4, fix `_await_compile_serving`'s went-dead check for the pre-Vite case, preserve all invariants.
- **TDD:** yes (fixtures in WU-4 are written first, RED against the current kill-restart behavior).
- **Files to create/modify:** `user/scripts/lazy_core.py`.
- **Implementation goal:**
  - In `_route_legacy_non_serving` [VERIFY: grep -n "def _route_legacy_non_serving" user/scripts/lazy_core.py] pass the new `boot_alive` signal into the discriminator (`_classify_compile_state(code, frontend_up, boot_alive)`) so both-ports-down-but-live-boot routes to `_await_compile_serving` [VERIFY: grep -n "def _await_compile_serving" user/scripts/lazy_core.py] (the existing patient, NON-killing wait) rather than `_recover_runtime` [VERIFY: grep -n "def _recover_runtime" user/scripts/lazy_core.py]. Thread `boot_alive` through the function signature alongside `frontend_probe`. Genuine `dead` (both down, boot NOT alive) keeps today's `_recover_runtime` behavior unchanged.
  - Mirror the identical change in the M4 `_route_non_serving` [VERIFY: grep -n "def _route_non_serving" user/scripts/lazy_core.py] so an ownership-mode caller gets the same pre-Vite patient-wait; thread `boot_alive` through `_ensure_runtime_m4`'s signature [VERIFY: grep -n "def _ensure_runtime_m4" user/scripts/lazy_core.py] alongside `frontend_probe` (mirror the prior fix's threading exactly).
  - Fix `_await_compile_serving`'s mid-wait went-dead check [VERIFY: grep -n "if not frontend_probe" user/scripts/lazy_core.py] so "boot process died AND both ports still down" returns the `_COMPILE_WENT_DEAD` sentinel [VERIFY: grep -n "_COMPILE_WENT_DEAD" user/scripts/lazy_core.py] → fall through to bounded `_recover_runtime` (the live-boot signal going false is the pre-Vite analog of `frontend_probe()` going false). REUSE the existing cold-compile-sized ceiling [VERIFY: grep -n "_COLD_COMPILE_WAIT_MAX_POLLS" user/scripts/lazy_core.py] — do NOT add a parallel ceiling.
  - Preserve every invariant: the prior Vite-up `compiling` patient-wait unchanged; HIJACKED never restart/kill; the ≤5×backoff loop still applies to genuine `dead`; `recover_identity`/`write_lock` lock-rewrite on recovery unchanged (legacy keeps `_noop_write_lock` [VERIFY: grep -n "_noop_write_lock" user/scripts/lazy_core.py]); sidecar assertion still composes; ceiling-exhaustion-while-booting BLOCKED with the existing `_cold_compile_timeout_blocker` [VERIFY: grep -n "_cold_compile_timeout_blocker" user/scripts/lazy_core.py] text (no new blocker_kind).
- **Spec requirements:** SPEC Proven Findings (route the cold boot off the kill-restart loop); Open Question 2 (genuine-crash recovery preserved).
- **Batch:** 1.

#### WU-4 — Phase 2 hermetic fixtures

- **Scope:** PHASES.md Phase 2 deliverable 5 — all six fixtures.
- **TDD:** yes (WRITTEN FIRST — the legacy-path patient-wait fixture fails RED against the current kill-restart behavior; WU-3 makes it green).
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations (extend the existing `_M4_CONFIG`/`_owned_lock`/`_SESSION` + legacy-path fixtures [VERIFY: grep -n "_M4_CONFIG" user/scripts/test_lazy_core.py]):**
  - **Legacy path (the SPEC's exact failure — THE regression test):** both ports down + `boot_alive=True` → `_route_legacy_non_serving` patient-waits, `restart` NEVER called (call-count == 0), READY once `:3333` answers 200 within the ceiling.
  - Both ports down + live boot that crosses to dead (`boot_alive` goes False, ports still down mid-wait) → falls through to bounded `_recover_runtime`.
  - Both ports down + live boot that never serves within the ceiling → BLOCKED with the distinct cold-compile-timeout text; `restart` STILL never called during the booting wait.
  - Genuine `dead` (both down, `boot_alive=False`) → UNCHANGED bounded `_recover_runtime` — assert by keeping the existing `_recovers_within_five` / `_exhausts_to_blocked` fixtures green [VERIFY: grep -n "recovers_within_five\|exhausts_to_blocked" user/scripts/test_lazy_core.py].
  - M4 mirror: the same both-ports-down + live-boot fixture against the ownership path reaches READY with `restart == 0`.
  - Default-off (no boot-liveness signal, `boot_alive → False`) → byte-identical to today's both-ports-down → recovery path.
- **Implementation goal:** All new fixtures pass; all preserved-path fixtures (genuine-crash, HIJACKED, Vite-up compiling) stay green.
- **Spec requirements:** as WU-3.
- **Batch:** 1 (test-first: WU-4 RED → WU-3 GREEN).

**Batch overview (Phase 2):**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-4 (fixtures, RED first) → WU-3 (impl, GREEN) | No (test-first ordering) | None (disjoint files) |

**Minimum Verifiable Behavior:** `python3 user/scripts/lazy-state.py --test` and `python3 user/scripts/test_lazy_core.py` pass with a new LEGACY-path fixture proving a both-ports-down-but-live-boot runtime reaches READY with `restart` call-count == 0 (starvation root cause structurally gone), AND the genuine-crash fixtures still cap restart at 5; `python3 user/scripts/bug-state.py --test` green.

---

### Phase: ensure-runtime-starves-pre-vite-sidecar-build P3 — CLI/doc alignment + parity + baselines

**Goal:** Confirm the production boot-liveness binding flows through the `--ensure-runtime` handler, align the consumer prose, add the bidirectional reverse-reference, and confirm parity + baselines + lints.
**Entry criteria:** Phase 2 complete (the pre-Vite patient-wait branch exists on both routers).
**SPEC.md references:** Affected Area (consumer — `/lazy-batch` Step 1d.0); Open Question 3 (default-off byte-identity confirmed via baselines).

#### WU-5 — CLI-seam confirmation + consumer/doc alignment + bidirectional reverse-reference

- **Scope:** PHASES.md Phase 3 deliverables 1–3.
- **TDD:** partial (the handler-wiring test is extended; the doc/prose edits are non-TDD).
- **Files to create/modify:** `user/scripts/lazy-state.py` (thin pass-through confirmation), `user/skills/lazy-batch/SKILL.md` (additive prose), `user/scripts/lazy_core.py` (reverse-reference comment), `user/scripts/test_lazy_core.py` (handler-wiring test extension).
- **Implementation goal:**
  - Confirm the `lazy-state.py --ensure-runtime` handler [VERIFY: grep -n "ensure-runtime\|ensure_runtime" user/scripts/lazy-state.py] gets the production boot-liveness binding via Phase 1's config-driven default binding so the handler stays a thin pass-through (document the choice inline; the existing `live_session_id` threading from the run marker stays UNCHANGED). If the boot-liveness source is the in-process `Popen` handle, document how `ensure_runtime` obtains it so no real handle is needed in `--test`. Extend `test_ensure_runtime_handler_wiring_*` [VERIFY: grep -n "handler_wiring" user/scripts/test_lazy_core.py] to assert a both-ports-down live-boot runtime reaches READY through the handler binding.
  - ADD to `user/skills/lazy-batch/SKILL.md` Step 1d.0 routing prose [VERIFY: grep -n "1d.0" user/skills/lazy-batch/SKILL.md] a note: a cold/first boot is now patiently waited through its PRE-VITE `BeforeDevCommand`/`sidecar:build` window (both ports down + live boot ⇒ booting, not killed) and reaches READY without starvation — extending the existing Vite-up patient-wait note. Keep the READY/STALE/HIJACKED/BLOCKED table intact; ADD the note, do not rewrite.
  - Add the BIDIRECTIONAL reverse-reference: in `lazy_core.py`'s ensure-runtime docstring (or an inline comment at the reworked `_route_legacy_non_serving`/`_classify_compile_state` site) note the pre-Vite window is covered by `docs/bugs/ensure-runtime-starves-pre-vite-sidecar-build` (the pre-Vite sibling of `ensure-runtime-recovery-starves-cold-compile`). The PHASES.md already names the prior-fix origin (origin direction satisfied).
- **Spec requirements:** SPEC Affected Area (consumer false-blocks the MCP tail on any cold runtime); spin-off contract (bidirectional cross-reference).
- **Batch:** 1.

#### WU-6 — Parity audit + baseline confirmation + skill lint/projection

- **Scope:** PHASES.md Phase 3 deliverables 4–5.
- **TDD:** no (verification/confirmation work — runs gates, confirms no regression).
- **Files to create/modify:** `user/scripts/CLAUDE.md` (additive `--ensure-runtime` doc dimension); `tests/baselines/*.txt` (confirm-only; regenerate ONLY on a legitimate `--test` delta via `_normalize_smoke_output`, never by hand).
- **Implementation goal:**
  - Update `user/scripts/CLAUDE.md`'s `--ensure-runtime` doc [VERIFY: grep -n "ensure-runtime" user/scripts/CLAUDE.md] with the boot-liveness pre-Vite patient-wait dimension (additive; document the feature-pipeline-only coupling already noted for the prior fix).
  - Confirm `python3 user/scripts/lazy_parity_audit.py --repo-root .` [VERIFY: grep -n "def audit_state_script_parity" user/scripts/lazy_parity_audit.py] exits 0 — the `--ensure-runtime` CLI is feature-pipeline-only, so NO `bug-state.py` mirror is owed; confirm the audit does not regress and document the justified divergence.
  - Confirm both byte-pinned `--test` baselines [VERIFY: ls tests/baselines/lazy-state-test-baseline.txt tests/baselines/bug-state-test-baseline.txt] are UNTOUCHED unless a default-path fixture legitimately changed (default-off byte-identity means they should NOT change).
  - Run `python3 ~/.claude/scripts/lint-skills.py` against the edited `lazy-batch/SKILL.md` and `python3 ~/.claude/scripts/project-skills.py` — both clean.
- **Spec requirements:** SPEC Open Question 3 (default-off byte-identity); the coupling note in `user/scripts/CLAUDE.md`.
- **Batch:** 2 (depends on WU-5's edits landing first).

**Batch overview (Phase 3):**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-5 | Solo | None |
| 2 | WU-6 | Solo | None (depends on WU-5) |

**Minimum Verifiable Behavior:** `python3 user/scripts/lazy-state.py --test`, `python3 user/scripts/bug-state.py --test`, `python3 user/scripts/test_lazy_core.py`, and `python3 user/scripts/lazy_parity_audit.py` all pass; `python3 ~/.claude/scripts/lint-skills.py` passes against `lazy-batch/SKILL.md`; `python3 ~/.claude/scripts/project-skills.py` re-projects cleanly. Both byte-pinned baselines untouched.

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** Identify the lowest-numbered phase whose entry criteria are satisfied (this plan is strictly sequential: P1 → P2 → P3). If no phase is ready, jump to Blocking Issue Protocol.
2. **Announce:** Print "Implementing ensure-runtime-starves-pre-vite-sidecar-build Phase N: [title]"
3. **Review prior context:** Re-read previously completed phases' Implementation Notes in this PHASES.md. They take priority over the plan where they diverge.
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "Phase N: [title] — committed as [hash]"
7. **Loop:** Re-evaluate readiness; return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (task tool loading via ToolSearch, task creation for all work units, the update protocol).

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow its instructions. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and this plan file. Do NOT rely on cached content.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — ZERO INLINE IMPLEMENTATION)

**PRE-FLIGHT CHECK:** Confirm (1) you will use the `Agent` tool with `model: "sonnet"` for ALL code changes, (2) you will NOT call `Edit`/`Write` on source/test files. If either is false, re-read the EXECUTION MODEL section. **(Under the dispatch-environment override, perform inline with Read/Edit/Write, test-first.)**

Read ALL of these before proceeding:
1. `~/.claude/skills/_components/tdd-protocol.md`
2. `~/.claude/skills/_components/subagent-launch.md`
3. `~/.claude/skills/_components/tdd-test-agent.md`
4. `~/.claude/skills/_components/implementation-agent.md`

**POST-DISPATCH GATE:** After subagents complete, verify you composed `Agent` calls and did NOT edit source/test files directly (N/A under the inline override).

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` and `~/.claude/skills/_components/mount-site-verification.md` and follow the complete protocol. Produce a structured review report with a PASS / PASS-WITH-FIXES / NEEDS-REWORK verdict before proceeding.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md`. Check off completed deliverables; add an Implementation Notes block (date, work completed, integration notes, pitfalls, files modified). Tick the matching `- [ ] WU-N` rows in this plan's Work Units checklist.

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/quality-gates.md`. Run the project gates. For this plan the gate set is:
```
python3 user/scripts/test_lazy_core.py
python3 user/scripts/lazy-state.py --test
python3 user/scripts/bug-state.py --test
python3 user/scripts/lazy_parity_audit.py --repo-root .
```
(Phase 3 adds `python3 ~/.claude/scripts/lint-skills.py` + `python3 ~/.claude/scripts/project-skills.py`.) 100% pass required before proceeding.

#### Step B.4.5: MCP Integration Test (BLOCKING — if applicable)

Read `~/.claude/skills/_components/mcp/mcp-integration-test.md`. **N/A here** — this is a pure harness-script change; the dev runtime is the subject under test, not an MCP target. claude-config has no `src-tauri/`/`package.json`, so the runtime-verification rows are STRUCTURALLY SKIPPED (the hermetic injected-probe tests are the validation). Skip with this note.

#### Step B.5: Commit Batch

Read `.claude/skill-config/commit-policy.md` (conventional commits, no AI attribution, push allowed). Before each commit run `python user/scripts/lazy-queue-doc.py --repo-root .` so the regenerated `LAZY_QUEUE.md` rides the commit. Re-assert `git rev-parse --abbrev-ref HEAD == main` before each commit/push.

#### Step B.6: Proceed to Next Batch

**Checklist (all must be true):** review report with verdict; PHASES.md + plan WU rows updated; all quality gates pass; commit per policy. If any unchecked, complete it before the next batch.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md`. Confirm the cross-phase changes work together (the discriminator + both routers + the patient wait compose; default-off byte-identity holds).

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md`. For this plan the only warranted update is `user/scripts/CLAUDE.md`'s `--ensure-runtime` doc (WU-6).

#### Commit and Push Post-Phase Changes

Per `.claude/skill-config/commit-policy.md`.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed phases** not yet committed.
3. **Write `BLOCKED.md`** per `~/.claude/skills/_components/sentinel-frontmatter.md` (canonical name `BLOCKED.md`, exact schema) in `docs/bugs/ensure-runtime-starves-pre-vite-sidecar-build/`, and print a blocking-issue report:

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** Phase N: [title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what to do]

   **Remaining phases (not attempted):** [list]

4. **Do not attempt to work around the blocker.**

Blocking issues include:
- A quality-gate failure that can't be fixed after 2 retry attempts (especially: a genuine-crash recovery fixture regressing — that means the discriminator change leaked into the `dead` path; STOP and reconsider).
- The boot-liveness signal cannot be read from a `Popen` handle/spawn primitive without inventing a parallel spawn scheme (architectural fork — write `NEEDS_INPUT.md` instead and surface the seam options).
- `lazy_parity_audit.py` regressing (would mean the change leaked into a coupled-pair surface — investigate before forcing).
- Any error that would require architectural decisions beyond the SPEC + PHASES scope.

---

## Completion

When all phases are complete:

1. **Run the full quality-gate suite one final time:**
   ```
   python3 user/scripts/test_lazy_core.py
   python3 user/scripts/lazy-state.py --test
   python3 user/scripts/bug-state.py --test
   python3 user/scripts/lazy_parity_audit.py --repo-root .
   python3 ~/.claude/scripts/lint-skills.py
   ```
2. **Set the top-level PHASES.md `**Status:**` to `In-progress`** (implementation done, validation pending). Do NOT set `Fixed` — that flip + the `FIXED.md` receipt are owned EXCLUSIVELY by the `__mark_fixed__` gate after the validation tail. Let the state machine route forward.
3. **Print a completion report:**

   ## Implementation Batch — Complete

   **Bug:** ensure-runtime-starves-pre-vite-sidecar-build
   **Total phases completed:** 3
   **Final quality-gate status:** all green
   **Commit log:** [Commit | Phase | Title table]
   **Implementation Notes summary:** [the boot-liveness signal source actually used (Popen handle vs URL probe), the both-router threading, and confirmation the genuine-crash + default-off paths stayed byte-identical]

---

```
/execute-plan docs/bugs/ensure-runtime-starves-pre-vite-sidecar-build/plans/fix-pre-vite-boot-liveness.md
```
