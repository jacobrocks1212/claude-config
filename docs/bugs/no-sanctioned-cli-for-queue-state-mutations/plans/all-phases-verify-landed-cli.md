---
kind: implementation-plan
feature_id: no-sanctioned-cli-for-queue-state-mutations
status: Ready
created: 2026-07-18
complexity: mechanical
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan docs/bugs/no-sanctioned-cli-for-queue-state-mutations/plans/all-phases-verify-landed-cli.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — no-sanctioned-cli-for-queue-state-mutations (verify landed fix)  (v2)

**PHASES.md files:**
- `docs/bugs/no-sanctioned-cli-for-queue-state-mutations/PHASES.md` (1 phase)

**SPEC.md files:**
- `docs/bugs/no-sanctioned-cli-for-queue-state-mutations/SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Grounding — the fix already landed; this plan is the verification tail

The entire SPEC fix scope (sanctioned CLI mutators for in-place queue priority/dep changes:
`--set-tier`/`--set-severity`/`--add-deps`/`--remove-deps`/`--unpin`/`--list-ops`/`--search-ops`,
their `lazy_core/depdag.py` + `bug-state.py` implementations, `--operator-authorized` +
`refuse_if_cycle_active` gating, the 28-test `test_depdag.py` suite, the `lazy-batch/SKILL.md`
prose, and the `cli-surface.json` flag entries) **already landed out-of-pipeline** in commit
`8a7bc738 harden(script): add sanctioned CLI for in-place queue priority/dep mutations`. The SPEC
was left `Concluded` (never reconciled), so the pipeline routed it to planning. Every
implementation deliverable in PHASES.md is `[x]` and was verified present on disk at planning time.

**Therefore this plan authors NO implementation work.** Its single work unit is the SPEC's
verification tail: regenerate `docs/cli/cli-surface.json` (to clear an UNRELATED `--set-independent`
drift — see below), run the gate battery green, and commit. The executor VERIFIES; it does NOT
re-implement any mutator — the symbols already exist (`set_queue_priority`, `reposition_by_priority`,
`mutate_queue_deps` in `depdag.py`; `unpin_bug_severity` in `bug-state.py`).

> **Unrelated pre-existing drift (NOT this bug's defect):** `cli_surface_gen.py --check` currently
> reports `lazy-state.py: added flag(s) --set-independent` — a SEPARATE feature's flag whose owner
> never regenerated the registry. Because `cli_surface_gen.py` regenerates the whole roster surface
> atomically, WU-1's regen sweeps it in as a side effect (the correct, current registry state). This
> is disclosed, expected, and required to make the SPEC's `--check`-clean gate pass.

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (claude-config is a stdlib-Python harness repo — gates are `pytest` / the state-script `--test` smokes / `lazy_parity_audit.py` / `cli_surface_gen.py --check`, NOT `/msbuild`/`/mstest`, and there is NO MCP/`/mcp-test` runtime gate), this plan's own per-phase steps and the Plan-specific execution notes below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

**Plan-specific execution notes:**
- **No subagent fan-out required.** WU-1 is a mechanical verify-and-regenerate task (run scripts, confirm exit codes, commit one regenerated file). Execute it inline; there is no source/test code to author, so the test-first split does not apply (TDD: no — the 28-test suite already exists and is the gate).
- **No MCP / runtime validation.** claude-config has no Tauri/MCP surface. The gate battery in WU-1 IS the complete verification; do not look for a Step-9 `/mcp-test` step or a `VALIDATED.md` sentinel.
- **Do NOT re-implement anything.** Every PHASES.md `[x]` deliverable is landed and verified. If a gate fails, investigate the specific failure — do not assume a mutator is missing.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies; the SPEC has no `**Depends on:**` block.)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | no-sanctioned-cli-for-queue-state-mutations | P1 | Verify landed CLI fix & regenerate surface | — | Solo |

## Work Units

- [ ] WU-1 — Regenerate CLI surface + run verification gate battery green

### Phase: no-sanctioned-cli-for-queue-state-mutations P1 — Verify landed CLI fix & regenerate surface

**Goal:** Certify the already-landed sanctioned-mutation CLI against the SPEC's Verification section and make the committed CLI-surface registry fresh, so the pipeline can advance to `__mark_fixed__`.

**Entry criteria:** None — implementation landed in `8a7bc738`; PHASES.md implementation deliverables are all `[x]`.

**SPEC.md references:** `## Verification` (regression tests green; `--test` smoke suites green; `lazy_parity_audit.py` exit 0; `cli_surface_gen.py --check` clean after regeneration).

#### WU-1 — Regenerate CLI surface + run verification gate battery green

- **Scope:** Covers PHASES.md Phase 1's three remaining `[ ]` deliverables (regenerate CLI surface + confirm `--check` clean; confirm `test_depdag.py` green; confirm both state-script `--test` smokes + `lazy_parity_audit.py` exit 0). These are one cohesive verification-tail concern — they must all pass together to certify the landed fix — so they are one work unit.
- **TDD:** no (no new logic; the 28-test `test_depdag.py` suite already exists and is itself the gate; the other steps run existing scripts).
- **Files to create/modify:**
  - `docs/cli/cli-surface.json` — regenerate via `cli_surface_gen.py` (the ONLY file this WU rewrites).
- **Test files:** none (no new tests authored).
- **Implementation goal — run each gate from the repo root and confirm the stated outcome; then commit the regenerated registry:**
  1. `python3 user/scripts/cli_surface_gen.py --repo-root .` [VERIFY: `test -f user/scripts/cli_surface_gen.py`] — regenerates `docs/cli/cli-surface.json` (sweeps in the unrelated `--set-independent` flag; expected).
  2. `python3 user/scripts/cli_surface_gen.py --check` — MUST exit 0 (registry fresh). If it still reports drift, regenerate again and re-check.
  3. `python -m pytest user/scripts/tests/test_lazy_core/test_depdag.py -q` [VERIFY: `test -f user/scripts/tests/test_lazy_core/test_depdag.py`] — MUST be green (28 passed): asserts priority mutation actually re-orders listed position (the load-bearing atomic reorder), deps add/remove + cycle refusal, `--unpin` restore+reposition, `--operator-authorized` gate + `refuse_if_cycle_active` refusal, `--search-ops` NL-query resolution.
  4. `python user/scripts/lazy-state.py --test` and `python user/scripts/bug-state.py --test` — both smoke suites MUST be green.
  5. `python3 user/scripts/lazy_parity_audit.py --repo-root .` [VERIFY: `test -f user/scripts/lazy_parity_audit.py`] — MUST exit 0 (no new SKILL heading was added; bug-batch + cloud inherit the operator-authorized-mutator prose by reference).
  6. Tick the three PHASES.md Phase 1 `[ ]` deliverables and the `- [ ] WU-1` row, flip this plan-part frontmatter `status:` to `Complete`, and commit + push to `main` (the regenerated `cli-surface.json` + the PHASES/plan ticks).
- **Spec requirements:** SPEC `## Verification` — all five gates green; the priority mutators actually change listed order (the load-bearing atomic-reorder assertion).
- **Batch:** 1.

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-1      | Solo      | None            |

**Completion (gate-owned):** once WU-1's gate battery is green and committed, the `__mark_fixed__` gate (state-machine-owned, NOT this plan) flips `SPEC.md` **Status:** to `Fixed`, writes `FIXED.md`, records the intervention with a measurable `target_signal`, and archives via `--archive-fixed`. Do NOT author any of those as work — they are not `/execute-plan`-closeable.
