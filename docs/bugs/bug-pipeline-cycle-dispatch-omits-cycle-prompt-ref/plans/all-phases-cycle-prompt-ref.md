---
kind: implementation-plan
feature_id: bug-pipeline-cycle-dispatch-omits-cycle-prompt-ref
status: In-progress
created: 2026-06-23
complexity: mechanical
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-23.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-pipeline-cycle-dispatch-omits-cycle-prompt-ref\plans\all-phases-cycle-prompt-ref.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — bug-pipeline-cycle-dispatch-omits-cycle-prompt-ref  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-pipeline-cycle-dispatch-omits-cycle-prompt-ref\PHASES.md` (2 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-pipeline-cycle-dispatch-omits-cycle-prompt-ref\SPEC.md`

**Total phases:** 2
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **EXECUTION-ENVIRONMENT NOTE (bug pipeline / batch dispatch):** when this plan is executed by a `/lazy-bug-batch` cycle subagent that lacks the `Agent` tool, perform ALL implementation INLINE with `Read`/`Edit`/`Write` per that dispatch's inline-override — still test-first (write the failing fixture assertion's expectation, confirm the unfixed code fails it, then fix). The orchestrator+subagent split above is the default for an Agent-capable session.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies. Prior art for the `@@lazy-ref` cycle-token machinery being mirrored: `docs/bugs/_archive/byref-dispatch-undercounts-forward-cycles` — reference context only, not a dependency.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines). (Inline-override exception: a `/lazy-bug-batch` cycle subagent without the Agent tool implements INLINE per its dispatch override — still test-first.)
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents
3. Every TDD work unit goes through the test-first pipeline — failing test written first, then implementation makes it pass
4. PHASES.md is updated AFTER EACH batch completes (not deferred)
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing
6. Mistakes are fixed immediately before launching the next batch
7. After all batches in a phase finish, integration verification confirms all changes work together
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it
9. Each completed phase is committed and pushed before the next phase begins
10. This plan is self-contained — follow it exactly as written without relying on external context
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory

## Verified Touchpoint Audit (grounded against the real tree)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `user/scripts/bug-state.py` (~5807-5812) | yes | `if cycle_prompt:` block calling `lazy_core.register_emission_if_marked(cycle_prompt, "cycle", item_id=state.get("feature_id"))` — return value DISCARDED; no `state["cycle_prompt_ref"]` assignment | refactor | Capture `_ref_entry = lazy_core.register_emission_if_marked(...)`; set `state["cycle_prompt_ref"]` mirroring `lazy-state.py:10036-10050` exactly. Do NOT alter the registration call args. |
| `user/scripts/lazy-state.py` (~10036-10050) | yes | the correct mirror: captures `_ref_entry`, sets `state["cycle_prompt_ref"] = f"@@lazy-ref nonce={_ref_entry['nonce']}"` / `None` / outer-`else` `None` | no change | Reference source ONLY — copy its shape into `bug-state.py`. [VERIFY: grep -n "cycle_prompt_ref" user/scripts/lazy-state.py] |
| `user/scripts/bug-state.py` `--test` harness | yes | in-file `def test_*` fixtures + a test-list registration block | refactor | Add a fixture asserting `cycle_prompt_ref` surfaced under a live marker / `None` without one; mirror marker+queue setup from a nearby `--emit-prompt` fixture. [VERIFY: grep -n "emit.prompt\|emit_prompt\|cycle_prompt" user/scripts/bug-state.py] |
| `user/scripts/lazy_parity_audit.py` | yes | state-script parity audit (`audit_state_script_parity`) | refactor | Add a targeted assertion that BOTH scripts assign `state["cycle_prompt_ref"]` in their emit-prompt block. `cycle_prompt_ref` is NOT currently referenced. [VERIFY: grep -n "cycle_prompt_ref" user/scripts/lazy_parity_audit.py] |
| `user/scripts/lazy_core.py` `register_emission_if_marked` | yes | shared helper returning the entry dict (or `None`) | no change | Already returns the entry; NO change owed — only the bug-side caller discards it. |
| `user/scripts/tests/baselines/bug-state-test-baseline.txt` | yes | byte-pinned smoke baseline | conditional | Regenerate via `_normalize_smoke_output` in `test_lazy_core.py` ONLY if the new fixture changes pinned stdout — NEVER by hand. |

All paths verified `exists: yes`. No net-new files. No phantom anchors. No genuine design fork — this is a deterministic coupled-pair mirror.

## Work Units

- [x] WU-1 — Surface `cycle_prompt_ref` in `bug-state.py` + `--test` fixture (Phase 1)
- [ ] WU-2 — Targeted `cycle_prompt_ref` surfacing-parity assertion in `lazy_parity_audit.py` (Phase 2)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | bug-pipeline-cycle-dispatch-omits-cycle-prompt-ref | P1 | Surface `cycle_prompt_ref` in bug-state.py | — | Solo |
| 2 | bug-pipeline-cycle-dispatch-omits-cycle-prompt-ref | P2 | Parity-audit assertion | P1 | Solo |

WU-1 and WU-2 both touch `user/scripts/` but DIFFERENT files (`bug-state.py` vs `lazy_parity_audit.py`); they are nonetheless ordered sequentially because WU-2's assertion can only PASS once WU-1 has landed (a dependency edge, not a file conflict). One WU per batch.

---

### Phase: bug-pipeline-cycle-dispatch-omits-cycle-prompt-ref P1 — Surface `cycle_prompt_ref` in `bug-state.py`

**Goal:** Mirror the `cycle_prompt_ref` capture-and-surface logic from `lazy-state.py:10036-10050` into `bug-state.py:5807-5812` so the bug pipeline surfaces the `@@lazy-ref` dispatch token, and add a `--test` fixture proving it.
**Entry criteria:** None (first phase).
**SPEC.md references:** `## Evidence Collected → Source Code (root cause)`; `## Fix Scope` items 1, 2, 4.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | None |

#### WU-1 — Surface `cycle_prompt_ref` in `bug-state.py` + `--test` fixture

- **Scope:** PHASES.md Phase 1 deliverables — the capture+assign block; the `--test` fixture; both `--test` suites green; conditional baseline regen.
- **TDD:** yes (the new `--test` fixture is the failing test — assert it fails against the unfixed `bug-state.py` block FIRST, then make the edit so it passes).
- **Files to create/modify:**
  - `user/scripts/bug-state.py` — at ~5807-5812, change the `if cycle_prompt:` block from the bare `lazy_core.register_emission_if_marked(cycle_prompt, "cycle", item_id=state.get("feature_id"))` to capture `_ref_entry = lazy_core.register_emission_if_marked(...)` (same args), then `if _ref_entry is not None: state["cycle_prompt_ref"] = f"@@lazy-ref nonce={_ref_entry['nonce']}"` `else: state["cycle_prompt_ref"] = None`, and add an outer `else: state["cycle_prompt_ref"] = None` (no `cycle_prompt`). Mirror `lazy-state.py:10036-10050` exactly. Keep the bug-pipeline comment noting `feature_id` holds the bug id. Add the new `def test_<name>():` fixture + register it in the script's `--test` list block.
  - `user/scripts/tests/baselines/bug-state-test-baseline.txt` — regenerate ONLY via `_normalize_smoke_output` IF the new fixture changes pinned stdout.
- **Test files:** `user/scripts/bug-state.py` (the fixture lives in the in-file `--test` harness — NOT pytest).
- **Test expectations:** New fixture asserts: (a) live run marker + a real-skill `--emit-prompt` cycle ⇒ `state["cycle_prompt_ref"] == "@@lazy-ref nonce=<hex>"` (starts with `@@lazy-ref nonce=`); (b) no marker ⇒ `state["cycle_prompt_ref"] is None`. Mirror marker+queue scaffolding from a nearby existing `--emit-prompt`/marker fixture rather than inventing it. [VERIFY: grep -n "register_emission_if_marked" user/scripts/bug-state.py]
- **Implementation goal:** `bug-state.py` hands the orchestrator a `@@lazy-ref` token whenever a marker is live, so `/lazy-bug` + `/lazy-bug-batch` dispatch real-skill cycles by reference (49-char token) instead of re-inlining 9.5–12K-char prompts. Reuse the shared `lazy_core.register_emission_if_marked` return value [VERIFY: grep -n "def register_emission_if_marked" user/scripts/lazy_core.py] — do NOT change that helper.
- **Spec requirements:** Fix Scope items 1 (capture+assign), 2 (fixture), 4 (baseline via `_normalize_smoke_output` only). Proven Findings: root cause is the discarded return value.
- **Batch:** 1
- **Gate (atomic gate+commit per CLAUDE.md R5):** `python3 user/scripts/bug-state.py --test && python3 user/scripts/lazy-state.py --test && python3 user/scripts/lazy_parity_audit.py --repo-root . && git add -A && git commit -m "..." && git push` — both smoke suites green AND the parity audit green (the audit must already pass at this point; WU-2 only ADDS a new assertion). Run `python3 -m pytest user/scripts/test_lazy_core.py` too if the baseline changed.

---

### Phase: bug-pipeline-cycle-dispatch-omits-cycle-prompt-ref P2 — Parity-audit assertion

**Goal:** Add a targeted assertion to `lazy_parity_audit.py` so the audit fails if exactly one of the two `--emit-prompt` paths surfaces `cycle_prompt_ref` — closing the SPEC's "Secondary gap" (the audit did not catch this divergence).
**Entry criteria:** Phase 1 complete (`bug-state.py` now surfaces the field; otherwise the new assertion would correctly fail).
**SPEC.md references:** `## Proven Findings → Secondary gap`; `## Affected Area → Parity audit row`; `## Open Questions` (targeted chosen over generic — ⚖ scope-class, same product behavior).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-2 | Solo | None |

#### WU-2 — Targeted `cycle_prompt_ref` surfacing-parity assertion

- **Scope:** PHASES.md Phase 2 deliverables — add the targeted parity assertion; both `--pair lazy-bug-batch` and all-pairs audits green.
- **TDD:** yes (confirm the assertion is load-bearing: it should reference both scripts' `cycle_prompt_ref` assignment such that a missing one in either is a reported divergence — optionally demonstrate by transiently reverting WU-1's assignment to see the audit fail, then restore).
- **Files to create/modify:**
  - `user/scripts/lazy_parity_audit.py` — add, within the existing state-script parity audit path (`audit_state_script_parity` or the equivalent assertion site), a check that BOTH `lazy-state.py` and `bug-state.py` assign `state["cycle_prompt_ref"]` in their emit-prompt block. Use the existing audit's reporting/diff convention; do NOT invent a new failure channel. [VERIFY: grep -n "def audit_state_script_parity\|audit_state_script_parity" user/scripts/lazy_parity_audit.py]
- **Test files:** none net-new — the audit IS the test; `lazy_parity_audit.py --repo-root .` is the gate.
- **Test expectations:** `python3 user/scripts/lazy_parity_audit.py --repo-root . --pair lazy-bug-batch` exits 0 with the new assertion present and passing (post-WU-1); `python3 user/scripts/lazy_parity_audit.py --repo-root .` (all pairs) exits 0.
- **Implementation goal:** The mirror can no longer silently drift — a future edit dropping `cycle_prompt_ref` surfacing from either script trips the parity gate. Targeted (one field), NOT the generic "every surfaced field" sweep (SPEC Open Question: targeted sufficient; generic is a larger hardening, deferred). ⚖ policy: targeted-vs-generic parity assertion → targeted (sizing/completeness only; same product behavior).
- **Spec requirements:** Secondary gap; Open Questions (targeted is sufficient to close this bug).
- **Batch:** 1
- **Gate (atomic gate+commit per CLAUDE.md R5):** `python3 user/scripts/lazy_parity_audit.py --repo-root . && python3 user/scripts/lazy_parity_audit.py --repo-root . --pair lazy-bug-batch && python3 user/scripts/bug-state.py --test && python3 user/scripts/lazy-state.py --test && git add -A && git commit -m "..." && git push`

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** Phase 1 first (no entry criteria); Phase 2 once Phase 1's deliverables are checked off. No parallelism (P2 depends on P1).
2. **Announce:** Print "Implementing bug-pipeline-cycle-dispatch-omits-cycle-prompt-ref Phase N: [title]"
3. **Review prior context:** Re-read prior phase Implementation Notes in PHASES.md — they take priority over the plan where they diverge.
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "Phase N: [title] — committed as [hash]"
7. **Loop:** Re-evaluate readiness; return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly. (Inline-dispatch exception: a cycle subagent without task tools tracks progress via the `- [ ] WU-N` plan checkboxes and the PHASES.md deliverable boxes instead.)

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow its instructions. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and the plan file itself. Also re-read the reference block `lazy-state.py:10036-10050` before editing `bug-state.py`.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — ZERO INLINE IMPLEMENTATION)

**PRE-FLIGHT CHECK:** Confirm you will use `Agent` with `model: "sonnet"` for all code changes and will NOT call `Edit`/`Write` on source/test files. (Inline-override exception applies for an Agent-less cycle subagent — implement inline, test-first.)

Read ALL of these before proceeding:
1. `~/.claude/skills/_components/tdd-protocol.md`
2. `~/.claude/skills/_components/subagent-launch.md`
3. `~/.claude/skills/_components/tdd-test-agent.md`
4. `~/.claude/skills/_components/implementation-agent.md`

Note: `subagent-launch.md` references the other components above via internal directives — since you've already read them, ignore those directives in the file.

**POST-DISPATCH GATE:** After all subagents complete, verify you composed `Agent` calls (or, under the inline override, performed test-first inline edits) and did not silently skip the failing-test-first step.

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` and follow its complete protocol. Also read `~/.claude/skills/_components/mount-site-verification.md` (no net-new files here, but the protocol still runs). Re-run the WU's gate commands fresh (ground-truth diff). Verdict: PASS / PASS-WITH-FIXES / NEEDS-REWORK.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md`. Check off completed deliverables, add an Implementation Notes block (date, work completed, integration notes, pitfalls, files modified). Tick the matching `- [ ] WU-N` row in this plan's Work Units list.

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/quality-gates.md`. For this repo the gates are: `python3 user/scripts/bug-state.py --test`, `python3 user/scripts/lazy-state.py --test`, `python3 user/scripts/lazy_parity_audit.py --repo-root .`, and (if the baseline changed) `python3 -m pytest user/scripts/test_lazy_core.py`. 100% pass required before proceeding.

#### Step B.4.5: MCP Integration Test (BLOCKING — if applicable)

N/A — this fix has NO MCP-reachable surface (PHASES.md `MCP runtime: not-required`). Skip with this note.

#### Step B.5: Commit Batch

Read the commit policy: first try `.claude/skill-config/commit-policy.md` in the project root. If it doesn't exist, read `~/.claude/skills/_components/commit-and-push.md`. Follow whichever applies. Default for claude-config (no `feat`/work-repo restriction): commit + push to `main`. Re-assert `git rev-parse --abbrev-ref HEAD == main` immediately before committing.

#### Step B.6: Proceed to Next Batch

**Checklist (all must be true):**
- [ ] Review report produced with a verdict
- [ ] PHASES.md updated + `- [ ] WU-N` row ticked
- [ ] All quality gates pass (both `--test` suites + parity audit)
- [ ] Step B.5 completed (commit + push to main)

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md`. Confirm the two `--test` suites + the parity audit all pass together after the phase's edits.

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md`. Consider whether `user/scripts/CLAUDE.md` warrants a one-line note that `bug-state.py` now surfaces `cycle_prompt_ref` (coupled-pair parity with `lazy-state.py`) and that the parity audit asserts it — update if warranted.

#### Commit and Push Post-Phase Changes

Read the commit policy as in Step B.5. Commit + push to `main`.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point during execution:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed phases** that haven't been committed yet.
3. **Write `BLOCKED.md`** per `~/.claude/skills/_components/sentinel-frontmatter.md` (canonical name EXACTLY `BLOCKED.md`) and print a blocking-issue report:

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** Phase N: [title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining phases (not attempted):**
   - [list]

4. **Do not attempt to work around the blocker.**

Blocking issues include:
- A quality-gate (`--test` / parity audit) failure that can't be fixed after 2 retry attempts
- A git push conflict that can't be resolved by rebase
- Any error requiring architectural decisions beyond the scope of the specs

---

## Completion

When all phases in the Execution Schedule are complete:

1. **Run the full quality-gate suite one final time:** `python3 user/scripts/bug-state.py --test`, `python3 user/scripts/lazy-state.py --test`, `python3 user/scripts/lazy_parity_audit.py --repo-root .`, `python3 -m pytest user/scripts/test_lazy_core.py`.
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Bug fixed:** bug-pipeline-cycle-dispatch-omits-cycle-prompt-ref
   **Total phases completed:** 2
   **Total commits:** M
   **Final quality-gate status:** all green

   **Commit log:**
   | Commit | Phase | Title |
   |--------|-------|-------|
   | abc1234 | P1 | Surface cycle_prompt_ref in bug-state.py |
   | def5678 | P2 | Parity-audit assertion |

3. **Do NOT flip SPEC/PHASES `**Status:**` to Fixed, and do NOT write `FIXED.md`.** Those are owned exclusively by the orchestrator's `__mark_fixed__` gate (fires after the validation tail). Set the last phase's top-level PHASES `**Status:**` to `In-progress` (implementation done, validation pending) and let the state machine route forward.

```
/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\bug-pipeline-cycle-dispatch-omits-cycle-prompt-ref\plans\all-phases-cycle-prompt-ref.md
```
