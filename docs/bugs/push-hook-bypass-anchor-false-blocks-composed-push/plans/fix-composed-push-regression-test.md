---
kind: fix-plan
feature_id: push-hook-bypass-anchor-false-blocks-composed-push
status: Complete
created: 2026-07-18
complexity: mechanical
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan docs/bugs/push-hook-bypass-anchor-false-blocks-composed-push/plans/fix-composed-push-regression-test.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — push-hook-bypass-anchor-false-blocks-composed-push  (v2)

**PHASES.md files:**
- `docs/bugs/push-hook-bypass-anchor-false-blocks-composed-push/PHASES.md` (push-hook-bypass-anchor-false-blocks-composed-push, 1 phase)

**SPEC.md files:**
- `docs/bugs/push-hook-bypass-anchor-false-blocks-composed-push/SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (claude-config is a harness-config repo whose gates are Python/pytest and skill-lint, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the Plan-specific execution notes below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this bug has no completed hard upstream dependencies) | | | |

## Plan-specific execution notes

- **Repo gates (override the contract's C#/Nx defaults):** claude-config's quality gates are the Python hook-test suite and the skill linter. The relevant gate for this plan is:
  - `python user/scripts/test_hooks.py` — runs the full hook test suite (the new test registers into its `_TESTS` list).
  - Optionally `python ~/.claude/scripts/lint-skills.py` (no skill files touched here, so this is a no-op sanity check).
  There is NO MSBuild / Nx / MCP runtime in this repo. `**MCP runtime:** not-required` (see PHASES.md).
- **The load-bearing hook fix already landed** at commit `365df0b9` ("fix(hooks): recognize push bypass token anywhere in command", 2026-07-14). `user/hooks/block-work-repo-git-push.sh` already uses the unanchored `_BYPASS_RE.search(command)` (lines 50–52, 82). **Do NOT edit the hook** — this plan adds only the missing regression test (SPEC scope item 2).
- Single-WU, single-batch plan. No file overlaps.

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | push-hook-bypass-anchor-false-blocks-composed-push | P1 | Regression coverage for composed approved pushes | — | Solo |

## Work Units

- [x] WU-1 — Composed approved-push allow-case regression test

## Phase: push-hook-bypass-anchor-false-blocks-composed-push P1 — Regression coverage for composed approved pushes

**Goal:** Add a regression test asserting a composed approved push (`cd "…" && CLAUDE_PUSH_APPROVED=1 git push origin main`) is ALLOWED by `block-work-repo-git-push.sh` in a work-email repo, locking in the already-landed unanchored-bypass fix.

**Entry criteria:** None. (The hook fix is already on `main` at commit `365df0b9`; this phase only supplies its missing regression test.)

**SPEC.md references:** `## Proven Findings` → "Confirmed scope" item 2 (regression coverage); `## Reproduction Steps` (the composed command that was false-denied); `## Evidence Collected` → "Test Harness" (names the reusable harness helpers).

### WU-1 — Composed approved-push allow-case regression test

- **Scope:** PHASES.md Phase 1 deliverables — add `test_push_allows_with_bypass_token_after_cd_prefix` to `user/scripts/test_hooks.py` and register it in the `_TESTS` list.
- **TDD:** yes (the deliverable IS a test; it is genuinely RED against the pre-`365df0b9` `^`-anchored `re.match` and GREEN against the current unanchored `_BYPASS_RE.search`).
- **Files to create/modify:**
  - `user/scripts/test_hooks.py` — add one test function next to the existing push-hook block (near `test_push_allows_with_bypass_token` and `test_push_allows_with_powershell_style_bypass_token`), and append its `("test_push_allows_with_bypass_token_after_cd_prefix", …)` entry to the `_TESTS` registration list so the runner executes it. [VERIFY: grep -n "def test_push_allows_with_bypass_token\b" user/scripts/test_hooks.py] [VERIFY: grep -n "_PUSH_HOOK_SH" user/scripts/test_hooks.py]
- **Test files:** same file (`user/scripts/test_hooks.py`) — this repo's hook tests are self-contained functions registered in `_TESTS`, not a separate test tree.
- **Test expectations:** The new test builds a work-email repo via `_init_email_repo(td, "jacob@cognitoforms.com")` [VERIFY: grep -n "def _init_email_repo" user/scripts/test_hooks.py], fires the hook with a COMPOSED command through `_run_bash(_PUSH_HOOK_SH, _hook_payload('cd "<repo>" && CLAUDE_PUSH_APPROVED=1 git push origin main', cwd=str(repo)), _base_env(state_dir))` [VERIFY: grep -n "def _hook_payload" user/scripts/test_hooks.py] [VERIFY: grep -n "def _hook_decision" user/scripts/test_hooks.py], and asserts `_hook_decision(result) is None` (allow — no deny emitted). Mirror the docstring/style of `test_push_allows_with_bypass_token`. The assertion must match intent: the test name claims "allows … after cd prefix", and the assertion asserts allow — it would FAIL (deny) against the old `re.match(r"^CLAUDE_PUSH_APPROVED=1\b", …)` anchor, which is the regression this locks.
- **Implementation goal:** One new self-contained test function reusing the existing push-hook harness helpers verbatim; the ONLY substantive difference from `test_push_allows_with_bypass_token` is the composed (cd-prefixed) command string. Register it in `_TESTS`. Do NOT modify any other test, the hook, or `push/SKILL.md`.
- **Spec requirements:** SPEC `## Proven Findings` "Confirmed scope" item 2 prescribes exactly this test (`test_push_allows_with_bypass_token_after_cd_prefix` asserting `cd "…" && CLAUDE_PUSH_APPROVED=1 git push origin main` → allow).
- **Batch:** 1

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | None            |

**Runtime Verification** *(checked by integration test or manual testing — NOT by the implementation agent):*
- [x] <!-- verification-only --> `python user/scripts/test_hooks.py` reports the full hook suite green, including the new `test_push_allows_with_bypass_token_after_cd_prefix` case (composed approved push → allow). **Verified 2026-07-18:** `test_push_allows_with_bypass_token_after_cd_prefix` — PASS. Full suite: 226/248 passed, 0 skipped, 22 failed (the 22 failures are pre-existing `test_containment_*`/`test_events_containment_*` cases in the unrelated `lazy-cycle-containment.sh` coverage — confirmed identical, same 22 names, via an A/B `git stash`/`stash pop` compare against this change; no regression introduced by this WU).

**MCP Integration Test Assertions:** N/A — no runtime-observable app behavior; verified by the `test_hooks.py` subprocess harness (`**MCP runtime:** not-required`).

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md **Status:** to Fixed and writes FIXED.md once this phase's regression test is green and the validation tail passes. Do NOT author a Status-flip or receipt-write work unit.
