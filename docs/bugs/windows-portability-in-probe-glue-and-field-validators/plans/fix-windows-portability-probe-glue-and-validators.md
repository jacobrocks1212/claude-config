---
kind: fix-plan
feature_id: windows-portability-in-probe-glue-and-field-validators
status: Ready
created: 2026-06-19
complexity: mechanical
phases: [1, 2, 3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-19.
> To execute: `/execute-plan docs/bugs/windows-portability-in-probe-glue-and-field-validators/plans/fix-windows-portability-probe-glue-and-validators.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — Windows path & line-ending portability defects (probe glue / validators)  (v2)

**PHASES.md files:**
- `docs/bugs/windows-portability-in-probe-glue-and-field-validators/PHASES.md` (windows-portability-in-probe-glue-and-field-validators, 3 phases)

**SPEC.md files:**
- `docs/bugs/windows-portability-in-probe-glue-and-field-validators/SPEC.md`

**Total phases:** 3
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Work Units

- [ ] WU-1 — Harden probe-glue prose in the coupled `/lazy-batch` ↔ `/lazy-batch-cloud` pair (Phase 1)
- [ ] WU-2 — Reconcile the `fix-line-endings.ps1` hook-wiring gap in settings.json + CLAUDE.md (Phase 2)
- [ ] WU-3 — Author the AlgoBooth-side validator follow-up record + reverse-reference (Phase 3)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source files when an `Agent` tool is available. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **DISPATCH-LIMIT OVERRIDE (this harness, autonomous pipeline).** If the executing session is itself a `/lazy-bug-batch` cycle subagent WITHOUT the `Agent` tool, it CANNOT dispatch sub-subagents — perform ALL work units INLINE with `Read`/`Edit`/`Write`, test-first. The no-Edit/Write constraint above yields to this override; the rest of the protocol (review gate, PHASES update, commit policy, per-WU checkboxes) still applies. This plan's WUs are prose/config edits to `.md` and `.json` files (and, in Phase 2, possibly `settings.json`) — they are NOT the `.ts`/`.rs`/`.vue` source the no-inline rule targets.

**Dispatch pattern (only when an Agent tool is present):** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |

## References — Upstream Artifacts

(none — this bug has no completed hard upstream dependencies; it is a harness-repo defect with no `**Depends on:**` block.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. ALL implementation work is delegated to Sonnet subagents via the Agent tool — UNLESS the DISPATCH-LIMIT OVERRIDE applies (no Agent tool → perform inline, test-first).
2. All edits happen in the current worktree — NEVER create worktrees.
3. Each WU with verifiable behavior is checked by a deterministic assertion (lint / grep / JSON-parse) — this bug has no runtime/MCP surface (PHASES `MCP runtime: not-required`).
4. PHASES.md is updated AFTER EACH batch completes (check off deliverables, add Implementation Notes).
5. Every WU's output is reviewed for SPEC alignment before continuing.
6. Mistakes are fixed immediately before the next batch.
7. The coupled-pair lockstep (WU-1) is verified by diffing the two edited files after the edit.
8. Relevant CLAUDE.md hook table is reconciled in WU-2 (it currently mis-documents `fix-line-endings.ps1`).
9. The completed work is committed and pushed (work branch: `main`).
10. This plan is self-contained — follow it exactly without external context.
11. **Before each step, `Read` the component files listed for that step from disk.**
12. NEVER flip the PHASES.md top-level `**Status:**` to `Fixed`/`Won't-fix` and NEVER write `FIXED.md` — those are owned by the orchestrator's `__mark_fixed__` gate (after the validation tail). You MAY flip the plan frontmatter `status:` and per-phase checkboxes/`Status:` lines, and set the top-level PHASES `**Status:**` to `In-progress` when the last phase's work lands.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | windows-portability | P1 | Harden probe-glue prose (coupled pair) | — | Solo |
| 2 | windows-portability | P2 | Reconcile hook-wiring gap | — | Solo |
| 3 | windows-portability | P3 | AlgoBooth follow-up cross-reference | — | Solo |

Phases are independent (no shared files, no dep edges) but are executed in order as one batch each for clean per-phase review + commit.

---

### Phase: windows-portability P1 — Harden probe-glue prose (coupled pair)

**Goal:** Make `/lazy-batch` and `/lazy-batch-cloud` probe-glue prose forbid the POSIX `/tmp/` round-trip that crashes on Windows-native Python, mandating in-band stdout capture.
**Entry criteria:** None (first phase).
**SPEC.md references:** Proven Findings → Symptom A; Affected Area row 1; Reproduction Steps (Symptom A); Open Question 2 (resolved: sweep is the coupled pair only).

#### WU-1 — Harden probe-glue prose in the coupled `/lazy-batch` ↔ `/lazy-batch-cloud` pair

- **Scope:** PHASES Phase 1 deliverables — edit both `lazy-batch/SKILL.md` (~L400) and `lazy-batch-cloud/SKILL.md` (L323) probe-glue prose; diff the two edited sentences for lockstep.
- **TDD:** no (prose edit to LLM-improvised glue; no runnable probe path to red/green — verified by lint + grep assertions instead).
- **Files to create/modify:**
  - `user/skills/lazy-batch/SKILL.md` [VERIFY: grep -n "redirect probe or diagnostic output into the repo tree" user/skills/lazy-batch/SKILL.md]
  - `repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md` [VERIFY: grep -n "redirect probe or diagnostic output into the repo tree" repos/algobooth/.claude/skills/lazy-batch-cloud/SKILL.md]
- **Implementation goal:** In `lazy-batch/SKILL.md`, replace the sentence `Never redirect probe or diagnostic output into the repo tree — write to the OS temp dir (`$TMPDIR` / `%TEMP%`) if you must capture it.` with guidance that: (a) keeps "never redirect probe/diagnostic output into the repo tree"; (b) mandates **in-band stdout capture** as the default — the probe already prints JSON to stdout, so consume it directly (`$(python3 … )` or pipe to the consumer), NO temp file; (c) if a temp file is genuinely unavoidable, FORBID a POSIX `/tmp/...` path (it crashes on Windows-native Python read-back) and require a path produced AND read by the SAME interpreter (`mktemp` consumed by the same shell, or `%TEMP%` resolved by the same Windows Python). In `lazy-batch-cloud/SKILL.md`, mirror the SAME fix at L323, preserving its cloud-specific "doubly important under cloud, where stray repo files get committed + pushed" framing. Then DIFF the two edited sentences and confirm the only divergence is the cloud framing (coupled-pair lockstep, per `CLAUDE.md` → "Coupled Skill Pairs").
- **Spec requirements:** SPEC Proven Findings Symptom A — "the cleanest fix is to instruct in-band capture … and NEVER round-trip through a temp file; failing that, mandate a portable temp path (`mktemp` / `%TEMP%` resolved by the SAME interpreter that reads it)."
- **Batch:** 1
- **Verification (deterministic, no runtime):**
  - `python3 ~/.claude/scripts/lint-skills.py` exits 0 (no broken injections / embedded-pattern regressions over the edited skills).
  - `python3 ~/.claude/scripts/project-skills.py` re-projects cleanly (no circular-include / expansion error).
  - `grep -c "redirect probe or diagnostic output into the repo tree"` returns ≥1 in BOTH files (intent preserved).
  - grep confirms the new in-band / no-`/tmp` guidance text present in BOTH files; the bare permissive `OS temp dir (`$TMPDIR` / `%TEMP%`) if you must capture it` phrasing is no longer the operative instruction in `lazy-batch/SKILL.md`.

---

### Phase: windows-portability P2 — Reconcile hook-wiring gap

**Goal:** Make `user/settings.json` and the CLAUDE.md hook tables agree on the actual wiring state of `fix-line-endings.ps1`, without blind-wiring a CRLF-adding hook that would worsen Symptom B.
**Entry criteria:** None (independent of P1).
**SPEC.md references:** Proven Findings → Symptom B (secondary, claude-config mitigation gap); Affected Area row 3; Evidence → `fix-line-endings.ps1` not registered in settings.json.

#### WU-2 — Reconcile the `fix-line-endings.ps1` hook-wiring gap

- **Scope:** PHASES Phase 2 deliverables — reconcile `settings.json` `PostToolUse` and the CLAUDE.md hook table; record why a naive global CRLF-adding registration is NOT the fix.
- **TDD:** no (config/doc reconcile; verified by JSON-parse + grep cross-check).
- **⚖ Scope note (D7, carried from PHASES):** the naive "register the hook" path is the trap SPEC line 50 flags — `fix-line-endings.ps1` ADDS `\r`, which is exactly what AlgoBooth's `\n`-only TS validator trips on. Implement the **reconcile** path (settings.json + docs agree, `\r`-source story points at Phase 3), NOT a blind global registration.
  - ⚖ policy: wire CRLF hook vs document gap → reconcile the doc to reality
- **Files to create/modify:**
  - `user/settings.json` [VERIFY: grep -n "\"PostToolUse\"" user/settings.json] — only if the chosen reconcile path registers the hook.
  - `CLAUDE.md` (project root) [VERIFY: grep -n "fix-line-endings" CLAUDE.md] and/or `user/CLAUDE.md` — the Hooks table that documents `fix-line-endings.ps1` as a PostToolUse hook.
- **Implementation goal:** Make `user/settings.json` `PostToolUse` (currently `[]`) and the CLAUDE.md / `user/CLAUDE.md` hook table AGREE on the real state of `fix-line-endings.ps1`. Choose the reconcile that leaves both internally consistent: EITHER (2a) register the hook in a way that does NOT feed CRLF into the docs-consistency path, OR (2b) correct the CLAUDE.md hook table so it no longer claims an unwired/counterproductive PostToolUse registration, recording the rationale (the CRLF-add behavior is why a naive global registration is wrong; the primary `\r`-tolerance fix is AlgoBooth-side — Phase 3). End state: no doc claims a hook that is both ABSENT and counterproductive, and the `\r`-source-reduction story points at Phase 3.
- **Spec requirements:** SPEC Proven Findings Symptom B — "`fix-line-endings.ps1` is documented as a PostToolUse hook but is NOT registered in `user/settings.json` — wiring it (or confirming the registration belongs in a per-repo settings file) is the claude-config-side hardening … Even when it runs, it normalizes to CRLF (adds `\r`), which is exactly what a `\n`-only TS parser then trips on."
- **Batch:** 1 (separate phase from WU-1; no file overlap with WU-1).
- **Verification (deterministic, no runtime):**
  - `python3 -c "import json; json.load(open('user/settings.json'))"` exits 0 (settings.json stays valid JSON after any edit).
  - grep cross-check: the CLAUDE.md hook table and `settings.json` `PostToolUse` agree on `fix-line-endings.ps1` (both claim it wired, or both reflect it unwired with the recorded rationale — no doc claims a registration settings.json lacks).

---

### Phase: windows-portability P3 — AlgoBooth follow-up cross-reference

**Goal:** Record the out-of-repo AlgoBooth-side validator fix (Symptom B primary) so it is not lost, with both-directions cross-reference per the spin-off contract.
**Entry criteria:** None (independent; documents work that cannot land in claude-config).
**SPEC.md references:** Open Questions (the AlgoBooth `check-docs-consistency.ts` `.trim()` fix is out-of-repo); Proven Findings → Symptom B (primary, AlgoBooth locus); Related Documentation → CRLF seam.

#### WU-3 — Author the AlgoBooth-side validator follow-up record + reverse-reference

- **Scope:** PHASES Phase 3 deliverables — author the out-of-repo follow-up record; add the reverse-reference in this bug's PHASES Implementation Notes.
- **TDD:** no (bookkeeping deliverable; verified by grep over this bug dir).
- **⚖ Scope note (D7, carried from PHASES):** SPEC offers (a) spin off an AlgoBooth work item or (b) doc-only follow-up. No reachable AlgoBooth bug queue from this repo's pipeline → implement doc-only (b) as the baseline.
  - ⚖ policy: spin-off vs doc-only for AlgoBooth fix → document the out-of-repo follow-up here
- **Files to create/modify:**
  - `docs/bugs/windows-portability-in-probe-glue-and-field-validators/PHASES.md` (Implementation Notes — the follow-up record + reverse-reference), or a sibling `docs/bugs/windows-portability-in-probe-glue-and-field-validators/FOLLOWUP.md`.
- **Implementation goal:** Author an out-of-repo follow-up record naming: target file `scripts/check-docs-consistency.ts` (AlgoBooth repo root), the fix (`.trim()` / strip trailing `\r` from each frontmatter value BEFORE date/enum/integer field-type validation), the evidence (the three SPEC field-validator error formats: `"2026-05-18\r"` date, `"lazy\r"` enum, `"11\r"`/`"13\r"` integer), and the convergence target (claude-config's CRLF-safe `lazy_core.py::parse_sentinel` `splitlines()` behavior). Add a REVERSE-REFERENCE in this bug's PHASES Implementation Notes naming the follow-up record (both-directions cross-reference contract). If a reachable AlgoBooth bug queue exists at execution time, the executor MAY ALSO `--enqueue-adhoc --type bug` into AlgoBooth `docs/bugs/queue.json` and cross-reference it — but the doc-only record is the baseline that always lands.
- **Spec requirements:** SPEC Open Questions — "either (a) spin off / cross-reference an AlgoBooth-branch work item for the TS validator `.trim()`, or (b) document it as an out-of-repo follow-up in the PHASES Implementation Notes. The AlgoBooth validator change cannot land in claude-config."
- **Batch:** 1 (separate phase; touches only this bug's docs).
- **Verification (deterministic, no runtime):**
  - grep over `docs/bugs/windows-portability-in-probe-glue-and-field-validators/` finds the AlgoBooth follow-up record (file path `check-docs-consistency.ts` + the `.trim()`/`\r`-strip fix description) AND the reverse-reference.

---

## Execution Protocol

Repeat until all three phases are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** All three phases are independent and ready immediately. Execute in order P1 → P2 → P3 (one batch each).
2. **Announce:** Print "Implementing windows-portability Phase N: [title]".
3. **Review prior context:** Re-read this plan + the bug's PHASES.md/SPEC.md relevant sections from disk before each phase.
4. **Execute the phase's single WU** per Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "windows-portability Phase N: [title] — committed as [hash]".
7. **Loop** to the next phase.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)

Read `~/.claude/skills/_components/task-tracking.md` and follow it: load task tools via ToolSearch, create one task per WU, update as work progresses. (If running as a no-Agent cycle subagent, task tracking is still useful for compaction recovery.)

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)

Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: this bug's PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and this plan file. Do NOT rely on remembered content.

#### Step B.1: Perform the WU (dispatch OR inline)

- If an `Agent` tool is available: dispatch one Sonnet subagent per WU with FULL self-contained context (the WU scope + implementation goal + spec requirements above, verbatim). The subagent does NOT commit/push/stage.
- If NO `Agent` tool (DISPATCH-LIMIT OVERRIDE): perform the WU INLINE with `Read`/`Edit`/`Write`. These are `.md`/`.json` prose-config edits — inline is correct.

For WU-1: edit BOTH coupled files, then run the lockstep diff. For WU-2: reconcile settings.json + CLAUDE.md. For WU-3: author the follow-up record + reverse-reference.

#### Step B.2: Review Batch Output (MANDATORY GATE)

Read `~/.claude/skills/_components/subagent-review.md` and follow its protocol (inline review is fine for a ≤2-file, prose-only batch). Verify each WU's deterministic checks ran green (lint exit 0 / JSON parses / grep assertions). For WU-1 specifically, confirm the coupled-pair diff shows only the intended cloud-framing divergence.

#### Step B.3: Update PHASES.md (MANDATORY)

Read `~/.claude/skills/_components/phases-update.md`. Check off the phase's completed `- [ ]` deliverables, tick the matching `- [ ] WU-N` row in this plan's `## Work Units` checklist, add an Implementation Notes block (date, work completed, files modified, the `⚖ policy:` line for P2/P3). Flip this plan's frontmatter `status:` toward `Complete` only when ALL WUs land (that flip is `/execute-plan`'s, not yours mid-batch).

#### Step B.4: Quality Gates (MANDATORY)

Run the repo's skill-lint suite as the quality gate (this is a harness-config repo, not an app):
```
python3 ~/.claude/scripts/lint-skills.py
python3 ~/.claude/scripts/project-skills.py
```
Both must exit 0. If `settings.json` was edited, also run `python3 -c "import json; json.load(open('user/settings.json'))"`. There is NO `/mcp-test` for this bug (PHASES `MCP runtime: not-required`).

#### Step B.5: Commit Batch

Read the commit policy: first try `.claude/skill-config/commit-policy.md`; if absent, `~/.claude/skills/_components/commit-and-push.md`. Commit to `main` (work branch) and push. Never branch, never `--force`.

#### Step B.6: Proceed to Next Batch

Checklist (all must be true): review report produced; PHASES.md updated + WU-N row ticked; quality gates green; committed per policy. If any unchecked, go back.

### Post-Phase Steps (after the final phase)

#### CLAUDE.md Review (MANDATORY)

Read `~/.claude/skills/_components/claude-md-review.md`. WU-2 already touches the CLAUDE.md hook table; confirm no OTHER CLAUDE.md (e.g. the Hooks section in the project root `CLAUDE.md`) still mis-documents `fix-line-endings.ps1` after the reconcile.

#### Commit and Push Post-Phase Changes

Commit any residual doc updates to `main` and push.

---

## Blocking Issue Protocol

If a blocking issue is encountered:

1. Stop all in-progress work. Do not launch new subagents.
2. Commit and push any completed phases.
3. Write `BLOCKED.md` (canonical name — see `~/.claude/skills/_components/sentinel-frontmatter.md`) in the bug dir and print a blocking-issue report (completed phases, blocked phase, reason, recovery suggestion, remaining phases).
4. Do NOT work around the blocker.

Blocking issues include: a quality-gate (lint) failure unfixable after 2 attempts; a git push conflict unresolvable by rebase; a coupled-pair lockstep that cannot be reconciled; any decision requiring architectural judgment beyond the SPEC. NOTE: the no-Agent dispatch limit is NOT a blocker — perform the WU inline (DISPATCH-LIMIT OVERRIDE).

## Completion

When all three phases are complete:

1. Run the lint suite one final time (`lint-skills.py` + `project-skills.py`, both exit 0).
2. Set the bug's PHASES.md top-level `**Status:**` to `In-progress` (implementation done, validation pending — NEVER `Fixed`/`Won't-fix`; that is the `__mark_fixed__` gate's).
3. Print a completion report (phases completed, commits, final lint status, the `⚖ policy:` lines).
4. Let the state machine route to the validation tail. Do NOT write `FIXED.md` yourself.

```
/execute-plan docs/bugs/windows-portability-in-probe-glue-and-field-validators/plans/fix-windows-portability-probe-glue-and-validators.md
```
