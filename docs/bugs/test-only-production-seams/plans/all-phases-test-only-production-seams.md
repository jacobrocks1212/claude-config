---
kind: implementation-plan
feature_id: test-only-production-seams
status: In-progress
created: 2026-07-18
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan docs/bugs/test-only-production-seams/plans/all-phases-test-only-production-seams.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — test-only-production-seams (v2)

**PHASES.md files:**
- `docs/bugs/test-only-production-seams/PHASES.md` (test-only-production-seams, 2 phases)

**SPEC.md files:**
- `docs/bugs/test-only-production-seams/SPEC.md`

**Total phases:** 2
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (e.g. a harness-config repo whose gates are Python/projection rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and any local `## Component Reference Card` override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

### Plan-specific execution notes (override the contract's build/test defaults)

**This is the `claude-config` harness repo — a docs/skills/config repo with NO compiled backend, NO Tauri app, and NO MCP runtime.** There is nothing to `/msbuild`, `/mstest`, or MCP-validate. The contract's build/test gate rows are replaced, for THIS plan, by the following repo-native gates:

- **Quality gate (run after any batch that edits a `skills/` or `_components/` file — WU-1, WU-2, WU-3):**
  ```bash
  python ~/.claude/scripts/project-skills.py && python ~/.claude/scripts/lint-skills.py
  ```
  Both must exit 0 (the `_components/subagent-review.md` and TDD-skill edits must re-project cleanly and lint clean — no broken `!cat` injections, no embedded-pattern regressions).
- **Quality gate (run after WU-4 / WU-5 — the cognito-pr-review edits):** the regeneration IS the gate — `/cognito-pr-review:rebuild-agents` must complete and the rendered shard must carry the new rule (WU-5's verification). No compile step.
- **TDD note:** these are documentation / YAML-registry / constitution edits with **no executable behavior** to unit-test. Every WU is `TDD: no` — the "test" is a deterministic grep/lint assertion (each WU states its exact verification command). Do NOT fabricate xUnit/vitest tests for prose or YAML edits; that would itself be a test-only artifact. Verification is the grep/lint gate, run by the orchestrator.
- **MCP runtime:** not-required (see PHASES.md header). No `/mcp-test`, no `VALIDATED.md`. Completion routes straight to the `__mark_fixed__` gate after the plan's WUs land.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | test-only-production-seams | P1 | Authoring + review + constitution guardrails | — | Batch 1: WU-1, WU-2, WU-3 parallel (file-disjoint) |
| 2 | test-only-production-seams | P2 | cognito-pr-review detector (rule + weight) | — | Batch 2: WU-4 solo |
| 3 | test-only-production-seams | P2 | cognito-pr-review detector (regenerate shard) | WU-4 | Batch 3: WU-5 solo |

Phase 1 (WU-1..3) and Phase 2's WU-4 are file-disjoint and dependency-free relative to each other; they MAY run concurrently. WU-5 depends on WU-4 (the shard can only render a rule that exists). The schedule above sequences conservatively by batch; an executor may overlap Batch 1 and Batch 2 since their file sets are disjoint.

## Work Units

- [ ] WU-1 — Author the "production seams that exist only for test observability" anti-pattern + its SKILL.md index bullet
- [ ] WU-2 — Add the test-seam-hygiene flag line to subagent-review.md's TDD-DISCIPLINE block
- [ ] WU-3 — Add the always-on test-seam principle to user/CLAUDE.md `<testing>`
- [ ] WU-4 — Add the `no-test-only-production-seam` rule + weight entry to cognito-pr-review
- [ ] WU-5 — Regenerate the rendered shard via `/cognito-pr-review:rebuild-agents` and verify the rule is present

---

### Phase: test-only-production-seams P1 — Authoring + review + constitution guardrails

**Goal:** Stop the workflow *emitting* test-only production seams by closing the authoring, pre-commit-review, and constitution gaps, each keyed on "sole consumer is a test" with a redirect to a legitimate injectable seam.
**Entry criteria:** None (first phase).
**SPEC.md references:** `## Fix Scope` items 1–3; `## Affected Area` rows "TDD authoring guidance", "Execute/review gate", "Constitution"; `## Proven Findings` (the "forbid-and-redirect" requirement).

**Batch overview:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1, WU-2, WU-3 | Yes | None (three disjoint files + one shared-nothing pair) |

#### WU-1 — Author the anti-pattern + SKILL.md index bullet

- **Scope:** PHASES.md P1 deliverables 1 & 2 — the new anti-pattern in `testing-anti-patterns.md` plus its index bullet in `SKILL.md`. Merged as one WU: the index bullet must name the anti-pattern authored here (tightly coupled title reference), and the two edits are trivially co-authored.
- **TDD:** no (documentation authoring; verified by grep, not unit tests).
- **Files to create/modify:**
  - `user/skills/test-driven-development/testing-anti-patterns.md` — add a new anti-pattern section after Anti-Pattern 3 (`Mocking Without Understanding`) [VERIFY: `grep -n "Anti-Pattern 2: Test-Only Methods" user/skills/test-driven-development/testing-anti-patterns.md`].
  - `user/skills/test-driven-development/SKILL.md` — add a 4th bullet under the `## Testing Anti-Patterns` index [VERIFY: `grep -n "## Testing Anti-Patterns" user/skills/test-driven-development/SKILL.md`].
- **Implementation goal:** Author an anti-pattern titled for **production seams that exist only for test observability**, following the file's existing anti-pattern shape (violation → why-wrong → fix → `### Gate Function`). It MUST cover BOTH uncovered shapes with C# `❌ BAD` / `✅ GOOD` examples:
  1. **Test-only hook invoked on a production path** — model the SPEC's `ArchiveMidpointHookForTests?.Invoke()` case (an `internal Action`/`Func` invoked inside a production method purely to let a test simulate a mid-operation event). `✅ GOOD` restructures the test to drive real behavior or injects a real dependency.
  2. **Settable test-override property/field whose sole consumer is a test** — model the SPEC's `internal Func<Type> PaymentAccountTypeResolverOverride` set only by a test via reflection. `✅ GOOD` uses a constructor-injected dependency / mockable interface instead.
  The `### Gate Function` asks: "Does this production member have any consumer OTHER than a test?" → if no, STOP; introduce a real injectable seam. Include the **redirect** explicitly: the remedy is a constructor-injected dependency / mockable interface / `protected virtual` extension point — NOT deleting the test. Add cross-references consolidating the cluster: to **Anti-Pattern 2** (test-only *methods*) [VERIFY: `grep -n "Anti-Pattern 2" user/skills/test-driven-development/testing-anti-patterns.md`], and to the PR-review seam-hygiene rules `no-test-only-service-params` / `no-public-for-tests` / `no-internals-visible-to-for-tests`. The rule keys on **"sole consumer is a test," NOT "used by tests"** — genuine injectable dependencies and `protected virtual` members are explicitly NOT flagged. Then add the matching 4th `SKILL.md` index bullet naming the new anti-pattern (mirroring the three existing bullets' one-line form).
- **Spec requirements:** SPEC `## Fix Scope` item 1; `## Proven Findings` bullet "Correct output when no seam exists" (must redirect, not merely forbid); `## Open Questions` item 3 (legitimate-seam boundary — key on sole-consumer).
- **Verification (orchestrator):**
  ```bash
  grep -n "test observability\|sole consumer is a test" user/skills/test-driven-development/testing-anti-patterns.md   # anti-pattern present
  grep -n "ArchiveMidpointHook\|Invoke()\|Override" user/skills/test-driven-development/testing-anti-patterns.md        # both shapes illustrated
  grep -n "protected virtual\|injectable\|mockable" user/skills/test-driven-development/testing-anti-patterns.md        # redirect present
  grep -n "test observability\|sole consumer is a test\|production seam" user/skills/test-driven-development/SKILL.md    # index bullet present
  ```
- **Batch:** 1.

#### WU-2 — subagent-review.md TDD-DISCIPLINE flag line

- **Scope:** PHASES.md P1 deliverable 3 — one flag line in the pre-commit review gate.
- **TDD:** no.
- **Files to create/modify:** `user/skills/_components/subagent-review.md` — append one bullet to the `TDD DISCIPLINE (for TDD work units):` block [VERIFY: `grep -n "TDD DISCIPLINE (for TDD work units)" user/skills/_components/subagent-review.md`].
- **Implementation goal:** Add one review-flag line to the TDD-DISCIPLINE block: net-new production code whose ONLY consumer is a test — a hook `?.Invoke()`'d on a production path, a settable test-override field, or visibility widening (`[InternalsVisibleTo]`) — is **defective → `NEEDS-REWORK`**, redirecting to a real injectable dependency / mockable interface / `protected virtual`. Match the existing bullets' bold-lead style (e.g. `- **Test-only production seams:** ...`). Keys on "sole consumer is a test," not "used by tests."
- **Spec requirements:** SPEC `## Fix Scope` item 2; `## Affected Area` row "Execute/review gate".
- **Verification (orchestrator):**
  ```bash
  grep -n "only consumer is a test\|sole consumer is a test\|test-only production seam" user/skills/_components/subagent-review.md
  ```
- **Batch:** 1.

#### WU-3 — user/CLAUDE.md `<testing>` principle

- **Scope:** PHASES.md P1 deliverable 4 — one terse always-on constitution principle.
- **TDD:** no.
- **Files to create/modify:** `user/CLAUDE.md` — add one bullet inside the `<testing>` block [VERIFY: `grep -n "<testing>" user/CLAUDE.md`].
- **Implementation goal:** Add a single terse bullet to the `<testing>` constitution block, matching the existing bullets' clipped style (e.g. `- Production code carries no seams (hooks, settable overrides, visibility widening) whose sole consumer is a test — introduce a real injectable dependency instead`). This is the always-on principle that applies OUTSIDE the skill pipeline. Keep it to one line.
- **Spec requirements:** SPEC `## Fix Scope` item 3; `## Affected Area` row "Constitution".
- **Verification (orchestrator):**
  ```bash
  grep -n "sole consumer is a test\|seams" user/CLAUDE.md
  ```
- **Batch:** 1.

**Post-Phase-1 quality gate (orchestrator runs after Batch 1):**
```bash
python ~/.claude/scripts/project-skills.py && python ~/.claude/scripts/lint-skills.py
```
Both exit 0 — the `_components/` and TDD-skill edits re-project and lint clean.

---

### Phase: test-only-production-seams P2 — cognito-pr-review detector

**Goal:** Add a correctly-scoped PR-review detector so a test-only production seam that reaches a PR is caught — in a category whose `file_patterns` match production code (NOT `testing.yaml`).
**Entry criteria:** None (independent of Phase 1; disjoint files).
**SPEC.md references:** `## Fix Scope` item 4; `## Affected Area` row "PR-review detector"; `## Evidence Collected` → "Fix-site gaps" → the scoping-trap bullet; `## Open Questions` item 1 (category placement — resolved to `code-consistency.yaml`, see PHASES.md ⚖ policy).

**Batch overview:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 2 | WU-4 | Solo | rule + weight sources |
| 3 | WU-5 | Solo | rendered shard (depends on WU-4) |

#### WU-4 — Add the `no-test-only-production-seam` rule + weight

- **Scope:** PHASES.md P2 deliverables 1 & 2 — the rule in `code-consistency.yaml` plus its `weights.yaml` entry (tightly coupled: a rule without a weight is not scored).
- **TDD:** no (YAML-catalog edit; verified by grep + the shard regeneration).
- **Files to create/modify:**
  - `user/plugins/local-tools/plugins/cognito-pr-review/knowledge/rules/code-consistency.yaml` — add a new rule to the `rules:` list [VERIFY: `grep -n "^category: code-consistency" user/plugins/local-tools/plugins/cognito-pr-review/knowledge/rules/code-consistency.yaml`]. This category's `file_patterns` are `*.cs` / `*.ts` / `*.tsx` / `*.vue` — matching production code (the whole point; `testing.yaml` matches only `*Tests.cs` and would never fire on the offending production files).
  - `user/plugins/local-tools/plugins/cognito-pr-review/knowledge/weights.yaml` — add the weight entry [VERIFY: `grep -n "no-internals-visible-to-for-tests:" user/plugins/local-tools/plugins/cognito-pr-review/knowledge/weights.yaml`].
- **Implementation goal:** Add a rule `id: no-test-only-production-seam` mirroring the `no-internals-visible-to-for-tests` schema exactly [VERIFY: `grep -n "id: no-internals-visible-to-for-tests" user/plugins/local-tools/plugins/cognito-pr-review/knowledge/rules/testing.yaml`] — `severity: important`, `description`, `rationale`, `anti_pattern`, `correct_pattern`. `description` flags net-new production code whose **sole consumer is a test**, enumerating the shapes: a hook (`internal Action`/`Func`) `?.Invoke()`'d on a production path, a settable `internal` test-override property/field, and visibility widening. `anti_pattern` shows a C# `?.Invoke()`-on-production-path or settable-override example; `correct_pattern` shows the DI / mockable-interface / `protected virtual` remedy. State explicitly that genuine injectable dependencies and `protected virtual` extension points are NOT flagged ("used by tests" ≠ "sole consumer is a test"). Then add `no-test-only-production-seam:` with `weight: 0.7` and `data_points: 0` to `weights.yaml`, matching the existing seam-cluster entries' nested `{weight, data_points}` shape.
- **Spec requirements:** SPEC `## Fix Scope` item 4; the scoping-trap evidence (rule must live in a production-file-matching category); Open Question 1 resolution (`code-consistency.yaml`, all-files — most complete coverage per PHASES.md ⚖ policy).
- **Verification (orchestrator):**
  ```bash
  grep -n "no-test-only-production-seam" user/plugins/local-tools/plugins/cognito-pr-review/knowledge/rules/code-consistency.yaml   # rule present
  grep -n "no-test-only-production-seam" user/plugins/local-tools/plugins/cognito-pr-review/knowledge/weights.yaml                  # weight present
  ```
- **Batch:** 2.

#### WU-5 — Regenerate the rendered shard + verify

- **Scope:** PHASES.md P2 deliverables 3 & 4 — regenerate the rendered catalog shard and confirm the rule is present.
- **TDD:** no.
- **Files to create/modify:** `user/plugins/local-tools/plugins/cognito-pr-review/knowledge/rendered/code-consistency.md` — regenerated (NOT hand-edited) by running `/cognito-pr-review:rebuild-agents` [VERIFY: `ls user/plugins/local-tools/plugins/cognito-pr-review/commands/rebuild-agents.md`].
- **Implementation goal:** Run the plugin's `/cognito-pr-review:rebuild-agents` command to regenerate the rendered shard from the updated rule catalog. Do NOT hand-edit `rendered/code-consistency.md` — it is a generated artifact. Then confirm the new rule appears in the regenerated shard. (Entry dependency: WU-4 must have landed the rule — the shard can only render a rule that exists.)
- **Spec requirements:** SPEC `## Fix Scope` item 4 (the `/cognito-pr-review:rebuild-agents` step).
- **Verification (orchestrator):**
  ```bash
  grep -n "no-test-only-production-seam" user/plugins/local-tools/plugins/cognito-pr-review/knowledge/rendered/code-consistency.md   # shard regenerated with the rule
  ```
- **Batch:** 3.

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md `**Status:**` to Fixed and writes `FIXED.md` after the validation tail — this plan never flips status or writes the receipt.

---

## Notes

- **Two disjoint layers, one defect.** Phase 1 prevents *emitting* the seam (authoring + review + constitution); Phase 2 *catches* one that slips through (PR review). File sets are disjoint — execute in any order; the only real edge is WU-5 → WU-4.
- **⚖ policy: PR-review category placement → `code-consistency.yaml`** (all-files) — most-complete coverage (C# witnessed cases + TS/Vue analogs); the rule keys narrowly on "sole consumer is a test," so breadth adds no false-positive risk. (SPEC Open Question 1; scope-class D7.)
- **⚖ policy: anti-pattern granularity → one consolidated anti-pattern with cross-references** (to AP-2 and the seam-hygiene rules) rather than a fragmented sibling — matches the SPEC's stated lean, leaves existing Anti-Patterns 1–3 renumbering-free. (SPEC Open Question 2; scope-class D7.)
- **Legitimate-seam boundary (SPEC Open Question 3):** settled by the SPEC, not a decision — every guardrail keys on "sole consumer is a test," never "used by tests." Baked into WU-1, WU-2, and WU-4.
