---
kind: fix-plan
feature_id: stale-marker-arms-validate-deny-on-unrelated-dispatches
status: Ready
created: 2026-06-19
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-19.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\stale-marker-arms-validate-deny-on-unrelated-dispatches\plans\all-phases-over-fire-session-scope.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — Stale/same-repo marker arms validate-deny (over-fire) (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\stale-marker-arms-validate-deny-on-unrelated-dispatches\PHASES.md` (stale-marker-arms-validate-deny-on-unrelated-dispatches, 2 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\stale-marker-arms-validate-deny-on-unrelated-dispatches\SPEC.md`

**Total phases:** 2
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.sh`, `.py`, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **DISPATCH OVERRIDE NOTE (for an autonomous bug-pipeline cycle subagent):** if the executing session is itself a dispatched cycle subagent WITHOUT the `Agent` tool, perform ALL work INLINE with Read/Edit/Write per its dispatch override, and ignore the subagent-launch indirection below. The plan's review/TDD discipline still applies inline (write the failing test first, then implement).

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies. The `multi-repo-concurrent-runs` feature is prior art referenced in the SPEC's `**Related:**`, not a `**Depends on:**` hard dep; its per-repo `--marker-present` gate is the surface this fix extends with `--session-id`.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines). (Inline override: a cycle subagent without the Agent tool performs the work inline, test-first.)
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents
3. Every TDD work unit goes through the test-first pipeline — failing tests are written BEFORE implementation
4. PHASES.md is updated AFTER EACH batch completes (not deferred)
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing
6. Mistakes are fixed immediately before launching the next batch
7. After all batches in a phase finish, integration verification confirms all changes work together
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it (notably `user/scripts/CLAUDE.md` → "Per-repo keyed state dir" / "Hooks gate via --marker-present" should note the `--session-id` owner-scoping)
9. Each completed phase is committed and pushed before the next phase begins
10. Cross-feature phases may run in parallel when dependencies are satisfied and no file conflicts exist (N/A — single feature, sequential phases)
11. This plan is self-contained — follow it exactly as written without relying on external context
12. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory

## Touchpoint Audit (verified against the tree 2026-06-19 — every anchor grep-confirmed)

| Planned file | Exists? | Real symbols (verified) | Action | Reuse / refactor directive |
|--------------|---------|-------------------------|--------|----------------------------|
| `user/hooks/lazy-dispatch-guard.sh` | yes | CWD-extract python (lines 69-74); `--marker-present --repo-root` call (lines 81-90) | refactor | Extend the SINGLE existing python payload-parse to also yield `session_id`; pass `--session-id "$SID"` (when non-empty) into the existing `--marker-present` call. Do NOT add a second python process. |
| `user/scripts/lazy-state.py` | yes | `--session-id` arg (line 5953); `if args.marker_present: read_run_marker(session_id=args.session_id)` (lines 6210-6212) | reuse (verify-only) | Already honors `--session-id`. No edit expected; verify during impl. |
| `user/scripts/lazy_guard.py` | yes | `_deny_no_ledger` (252), `_deny_and_ledger` (486), `guard()` (517), `read_run_marker(session_id=session_id)` (563), default-deny tail (~780), consumed-by-other/stale denies (~725/745) | reuse + minimal branch | Branch the default-deny path(s) on `marker.get("session_id") is None` to `_deny_no_ledger`. REUSE `_deny_no_ledger`; author no new helper. |
| `user/scripts/lazy_core.py` | yes | `read_run_marker` path B (6195-6206), `append_deny_ledger_entry`, `pending_hardening` | reuse (no change) | No schema change under D2-A. Verify path-B semantics unchanged. |
| `user/scripts/test_hooks.py` | yes | `_e1_preToolUse_json` (193), `_run_bash` (259), `_base_env` (286), `_write_marker_in_dir` (293) | extend | Add over-fire + no-debt fixtures reusing these helpers. |
| `user/scripts/test_lazy_core.py` | yes | path-B / `pending_hardening` characterization | extend (if needed) | Add a direct `pending_hardening()==0` unit assertion for the no-debt path if the guard-level fixture does not cover it cleanly. |

No contradictions found; no design forks. All `[VERIFY:]` anchors below were grep-confirmed at authoring time.

## Work Units

- [ ] WU-1 — Session-scope the dispatch-guard gate (pass `--session-id` from the hook)
- [ ] WU-2 — Over-fire regression fixtures (owner runs guard; non-owner fast-path-allows, no ledger)
- [ ] WU-3 — No-debt deny for the unbound-marker pre-bind window (`_deny_no_ledger` branch)
- [ ] WU-4 — No-debt vs debt contrast fixtures (unbound→no debt; bound→debt)

## Execution Schedule

| Step | Feature | Phase(s) | Title | Blocked by | Parallel? |
|------|---------|----------|-------|------------|-----------|
| 1 | stale-marker-arms-validate-deny | P1 | Session-scope the gate (WU-1, WU-2) | — | Solo (WU-1 then WU-2; same files) |
| 2 | stale-marker-arms-validate-deny | P2 | No-debt pre-bind deny (WU-3, WU-4) | P1 | Solo (WU-3 then WU-4) |

---

### Phase: stale-marker-arms-validate-deny P1 — Session-scope the dispatch-guard gate (D1)

**Goal:** Make `lazy-dispatch-guard.sh` pass the hook-input `session_id` into `lazy-state.py --marker-present` so the gate treats the marker as present ONLY for its bound owning session — a non-owner dispatch fast-path-allows at the gate, never reaching the guard.
**Entry criteria:** None (first phase).
**SPEC.md references:** Resolved Decision D1 (Owning-session scoping at the gate); Proven Findings #1 (over-fire root cause); Affected Area rows "Dispatch-guard gate (session-blindness)".

**Batch overview:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | hook + (verify) state-script |
| 2 | WU-2 | Solo | test_hooks.py (depends on WU-1) |

#### WU-1 — Session-scope the dispatch-guard gate
- **Scope:** PHASES P1 deliverables — extend the hook's payload-parse to extract `session_id`; pass `--session-id "$SID"` into the existing `--marker-present --repo-root "$CWD"` call only when `$SID` is non-empty; preserve fail-OPEN; leave `--repo-root` / per-repo keying untouched.
- **TDD:** yes (the failing fixture in WU-2 defines the contract; if running inline, write WU-2's non-owner fixture FIRST and watch it fail — pre-fix it produces a deny+ledger for the non-owner — then implement WU-1).
- **Files to create/modify:** `user/hooks/lazy-dispatch-guard.sh` (extend the single python parse at lines 69-74 to also print `session_id`; pass `--session-id` into the call at lines 81-90 `[VERIFY: grep -n "marker-present --repo-root" user/hooks/lazy-dispatch-guard.sh]`). VERIFY-ONLY (no edit expected): `user/scripts/lazy-state.py` — confirm `--marker-present` handler already calls `read_run_marker(session_id=args.session_id)` `[VERIFY: grep -n "read_run_marker(session_id=args.session_id)" user/scripts/lazy-state.py]` and `--session-id` arg exists `[VERIFY: grep -n '"--session-id"' user/scripts/lazy-state.py]`.
- **Implementation goal:** The gate read (`--marker-present`) and the guard's own read (`read_run_marker(session_id=session_id)` `[VERIFY: grep -n "read_run_marker(session_id=session_id)" user/scripts/lazy_guard.py]`) AGREE — both keyed on the caller session. An owning-session dispatch still runs the guard exactly as today; a non-owner dispatch exits 0 at the gate with no guard invocation, no deny, no ledger entry. Fail-OPEN: an empty/failed `session_id` parse omits the flag and degrades to today's session-blind gate (never silently disables enforcement).
- **Spec requirements:** SPEC Resolved Decision D1 (mechanism A); sacred invariants (fail-OPEN; never destroy the owner's marker; the two reads must agree).
- **Batch:** 1.

#### WU-2 — Over-fire regression fixtures
- **Scope:** PHASES P1 Runtime-Verification + test deliverable — end-to-end bash-hook fixtures proving owner-vs-non-owner gate behavior.
- **TDD:** yes (these ARE the failing-first tests for WU-1).
- **Files to create/modify:** `user/scripts/test_hooks.py` (add fixtures; reuse `_run_bash` `[VERIFY: grep -n "def _run_bash" user/scripts/test_hooks.py]`, `_base_env` `[VERIFY: grep -n "def _base_env" user/scripts/test_hooks.py]`, `_write_marker_in_dir` `[VERIFY: grep -n "def _write_marker_in_dir" user/scripts/test_hooks.py]` with a `session_id`, `_e1_preToolUse_json` `[VERIFY: grep -n "def _e1_preToolUse_json" user/scripts/test_hooks.py]` with a `session_id`).
- **Test expectations:** With a marker bound to `session_A`: (a) an unregistered-prompt dispatch carrying `session_B` → bash hook exits 0, EMPTY stdout (gate fast-path allow), and NO `lazy-deny-ledger.jsonl` row is written; (b) the same unregistered-prompt dispatch carrying `session_A` → deny JSON produced and a ledger row written (guard ran — baseline behavior preserved). Assertion (a) must FAIL against the pre-WU-1 hook (which denies+ledgers the non-owner) and PASS after.
- **Implementation goal:** Lock the over-fire regression: a non-owning same-repo dispatch is never policed by the gate.
- **Spec requirements:** SPEC Reproduction Steps "Over-fire (Symptom 1)"; Expected vs Actual.
- **Batch:** 2 (depends on WU-1; both touch the guard path / test file).

**Runtime Verification (ticked by the test harness, NOT the implementer):**
- [ ] <!-- verification-only --> `python user/scripts/test_hooks.py` passes including the new owner/non-owner gate fixtures.
- [ ] <!-- verification-only --> `python user/scripts/lazy-state.py --test` and `python user/scripts/bug-state.py --test` stay green (byte-pinned baselines unchanged).

---

### Phase: stale-marker-arms-validate-deny P2 — No-debt deny for the unbound-marker pre-bind window (D2)

**Goal:** When a live marker is UNBOUND (`session_id: None`, bind-pending), route an unrelated deny through `_deny_no_ledger` so it carries no hardening debt — the deny verdict is preserved, only the ledger append is suppressed. A deny under a BOUND marker still accrues debt unchanged.
**Entry criteria:** P1 complete (gate session-scoped; the unbound-marker window is the only remaining same-repo deny surface).
**SPEC.md references:** Resolved Decision D2 (Allow-through + pre-bind no-debt deny); Proven Finding #3 (deny ledger is the choke point); the `_deny_no_ledger` transcription-slip precedent.

**Batch overview:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-3 | Solo | lazy_guard.py |
| 2 | WU-4 | Solo | test_hooks.py / test_lazy_core.py (depends on WU-3) |

#### WU-3 — No-debt deny for the unbound-marker pre-bind window
- **Scope:** PHASES P2 deliverables — in `guard()`, when the live marker is unbound (`marker.get("session_id") is None`), route the GENERIC default-deny path(s) through `_deny_no_ledger(_default_deny_reason())` instead of `_deny_and_ledger(...)`; scope precisely to the unbound case; preserve the hardening-cap and ref-token deny semantics; fail-OPEN to `_deny_and_ledger` on any error.
- **TDD:** yes (WU-4 defines the contract; inline → write WU-4's unbound no-debt fixture first and watch it FAIL — pre-fix the unbound deny still ledgers — then implement).
- **Files to create/modify:** `user/scripts/lazy_guard.py` (branch the default-deny tail at ~line 780 `[VERIFY: grep -n "_default_deny_reason()" user/scripts/lazy_guard.py]` and the consumed-by-other/stale default-deny branches on `marker.get("session_id") is None`; reuse `_deny_no_ledger` `[VERIFY: grep -n "def _deny_no_ledger" user/scripts/lazy_guard.py]`). The `marker` dict is already in scope at the deny sites (`read_run_marker` result, line 563 `[VERIFY: grep -n "marker = lazy_core.read_run_marker" user/scripts/lazy_guard.py]`).
- **Implementation goal:** A pre-bind (unbound-marker) unrelated deny returns the same deny JSON but writes NO `lazy-deny-ledger.jsonl` entry, so `pending_hardening()` `[VERIFY: grep -n "def pending_hardening" user/scripts/lazy_core.py]` does not rise. A BOUND-marker unregistered-prompt deny (a genuine validate-deny) still `_deny_and_ledger`s and accrues debt unchanged. Do NOT broaden the no-debt path to bound-marker denies; do NOT touch the depth-1 hardening cap or the bare-ref deny.
- **Spec requirements:** SPEC Resolved Decision D2 (mechanism A + pre-bind no-debt pairing); the `_deny_no_ledger` in-codebase precedent (lines 252-267).
- **Batch:** 1.

#### WU-4 — No-debt vs debt contrast fixtures
- **Scope:** PHASES P2 Runtime-Verification + test deliverable — contrasting fixtures pinning the no-debt scope.
- **TDD:** yes (the failing-first tests for WU-3).
- **Files to create/modify:** `user/scripts/test_hooks.py` (UNBOUND-marker no-debt fixture: `_write_marker_in_dir` WITHOUT a `session_id` to write a bind-pending marker; assert ledger emptiness; BOUND-marker debt-accrual contrast fixture). Optionally `user/scripts/test_lazy_core.py` for a direct `pending_hardening()` unit assertion if the guard-level fixture does not cover it cleanly.
- **Test expectations:** (1) UNBOUND marker (`session_id: None`) + unregistered prompt → deny JSON returned, `lazy-deny-ledger.jsonl` empty/absent, `pending_hardening()==0`. (2) BOUND marker (owner session) + unregistered prompt → deny JSON returned, exactly one ledger row, `pending_hardening()==1`. Fixture (1) must FAIL against pre-WU-3 guard (it ledgers) and PASS after.
- **Implementation goal:** Pin that no-debt routing is scoped to the unbound window and does NOT erode the genuine validate-deny debt path.
- **Spec requirements:** SPEC Resolved Decision D2; Proven Finding #3.
- **Batch:** 2 (depends on WU-3).

**Runtime Verification (ticked by the test harness, NOT the implementer):**
- [ ] <!-- verification-only --> `python user/scripts/test_hooks.py` passes including the unbound no-debt + bound debt-accrual contrast fixtures.
- [ ] <!-- verification-only --> `python user/scripts/test_lazy_core.py` stays green (no `append_deny_ledger_entry` / `pending_hardening` / path-B regression).
- [ ] <!-- verification-only --> `python user/scripts/lazy-state.py --test` and `python user/scripts/bug-state.py --test` stay green.

**Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md `**Status:**` to `Fixed` and writes `FIXED.md` once both phases land and the validation tail passes. This plan never authors that flip or receipt. When WU-4 lands, set PHASES top-level `**Status:**` to `In-progress` (implementation done, validation pending) and let the state machine route forward.

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** Identify phase(s) whose entry criteria are satisfied. Phases here are sequential (P2 depends on P1).
2. **Announce:** Print "Implementing stale-marker-arms-validate-deny Phase N: [title]"
3. **Review prior context:** Re-read previously completed phases' Implementation Notes in PHASES.md. They take priority over the plan where they diverge.
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "stale-marker-arms-validate-deny Phase N: [title] — committed as [hash]"
7. **Loop:** Re-evaluate readiness. Return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (ToolSearch task tools, create tasks for all WUs, update protocol).

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow it. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and this plan file.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — ZERO INLINE IMPLEMENTATION)

**PRE-FLIGHT CHECK:** Confirm you will use the `Agent` tool with `model: "sonnet"` for ALL code changes and will NOT call `Edit`/`Write` on source/test files. (INLINE OVERRIDE: a dispatched cycle subagent without the Agent tool does this work inline with Read/Edit/Write, test-first — write the failing fixture, watch it fail, then implement.)

Read before proceeding: `~/.claude/skills/_components/tdd-protocol.md`, `~/.claude/skills/_components/subagent-launch.md`, `~/.claude/skills/_components/tdd-test-agent.md`, `~/.claude/skills/_components/implementation-agent.md`.

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` and `~/.claude/skills/_components/mount-site-verification.md`; follow the complete protocol (scope measurement, ground-truth gate, review, propagation check, verdict). For inline execution, self-review against the same checklist and record a verdict.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md`. Check off completed deliverables, add an Implementation Notes block (date, work, integration notes, pitfalls, files). Tick the `- [ ] WU-N` row in this plan's Work Units list. Do NOT tick verification-only rows (Step-9 gate owns them).

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/quality-gates.md`. For THIS repo the gates are the hermetic Python suites — run ALL of: `python user/scripts/test_hooks.py`, `python user/scripts/test_lazy_core.py`, `python user/scripts/lazy-state.py --test`, `python user/scripts/bug-state.py --test`. 100% pass required before proceeding. (No npm/build gate — this repo is harness scripts.)

#### Step B.4.5: MCP Integration Test (BLOCKING — if applicable)

N/A for both phases — PHASES.md declares MCP `not-required` (harness scripts, no app surface). Skip with a note. Verification is the hermetic suites in B.4.

#### Step B.5: Commit Batch

Read the commit policy: first try `.claude/skill-config/commit-policy.md` in the project root; if absent, `~/.claude/skills/_components/commit-and-push.md`. Commit to `main` and push (this is a personal repo — `git config user.email` is NOT `jacob@cognitoforms.com`). Atomic gate+commit (R5): chain `<gates> && git add -A && git commit -m "..." && git push`.

#### Step B.6: Proceed to Next Batch

Checklist (all true): review verdict produced; PHASES.md + WU row updated; all gates pass; committed/pushed. Else go back.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md`. Confirm the gate (hook) and guard reads agree and the no-debt scope holds together — exercise the full bash-hook path, not just the Python unit.

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md`. After P1, update `user/scripts/CLAUDE.md` → "Hooks gate via `--marker-present`" to note the gate now passes `--session-id` (owner-scoped, not merely repo-scoped); after P2, note the unbound-marker no-debt deny pairing.

#### Commit and Push Post-Phase Changes

Same commit policy as B.5. Push to `main`.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed phases** not yet committed.
3. **Print a blocking-issue report** (Completed phases + hashes; Blocked phase; Reason; Recovery suggestion; Remaining phases).
4. **Do not attempt to work around the blocker.** Write `BLOCKED.md` per the canonical sentinel schema (`~/.claude/skills/_components/sentinel-frontmatter.md`) and halt.

Blocking issues include: a subagent/gate failure unfixable after 2 retries; a git push conflict unresolvable by rebase; an entry-criteria reference outside the input set; any error needing architectural decisions beyond the specs.

---

## Completion

When both phases are complete:

1. **Run the full hermetic suite one final time:** `python user/scripts/test_hooks.py`, `python user/scripts/test_lazy_core.py`, `python user/scripts/lazy-state.py --test`, `python user/scripts/bug-state.py --test` — all green.
2. **Print a completion report** (features implemented; phases completed; commits; final gate status; commit log table; implementation-notes summary).
3. Set PHASES.md top-level `**Status:**` to `In-progress` (implementation done, validation pending). Do NOT write FIXED.md or flip SPEC `**Status:**` — gate-owned by `__mark_fixed__`. Let the state machine route to the validation tail.
