---
kind: implementation-plan
feature_id: no-gemini-sprint-staging-in-claude-config
status: Ready
created: 2026-06-20
complexity: mechanical
phases: [1]
---

> **Mobile plan** — generated by `/plan-bug` (`/write-plan`) on 2026-06-20.
> To execute: `/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\no-gemini-sprint-staging-in-claude-config\plans\all-phases-no-gemini-sprint-staging.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — no-gemini-sprint-staging-in-claude-config  (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\no-gemini-sprint-staging-in-claude-config\PHASES.md` (no-gemini-sprint-staging-in-claude-config, 1 phase)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\no-gemini-sprint-staging-in-claude-config\SPEC.md`

**Total phases:** 1
**Plan version:** v2 (reference-based — components loaded from disk per step)

This is a **documentation-only** fix. The investigation (SPEC `**Status:** Concluded`) proved the bug is a docs/convention gap, not a code defect: both consumers of the gemini-sprint staging path already degrade gracefully when claude-config has no `docs/gemini-sprint/` structure. The only working research-resume route for this repo is dropping `RESEARCH.md` directly into the feature dir; this plan records that as the **blessed** route in two convention files. No code, no skills, no scripts, no hooks. There is no executable behavior, so the plan carries no TDD work units — verification is a content read.

⚖ policy: gemini-sprint staging in claude-config → document direct-RESEARCH.md-drop as blessed path (no unused staging machinery)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **Documentation-only carve-out for THIS plan.** Both deliverables here are pure prose edits to convention/markdown files (`docs/bugs/CLAUDE.md` and the repo-root `CLAUDE.md`). The repo-root `CLAUDE.md` is on the orchestrator's allowed-direct-edit list. `docs/bugs/CLAUDE.md` is a markdown convention doc, not source/test code — editing it directly (or via subagent) is acceptable. No `.ts/.js/.cs/.vue/.py/.rs/.tsx/.jsx`/test file is touched by this plan, so the Sonnet-subagent-for-code rule has no code to apply to. If the executing session lacks the `Agent` tool, perform the edits INLINE with Edit/Write — that is correct for this docs-only plan.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

> Quality gates (Step B.4) and MCP integration test (Step B.4.5) are **N/A** for this docs-only plan — there is no build/test surface and no runtime-observable behavior. Step B.1 TDD pipeline is N/A — no testable code is produced. Run a markdown sanity read in their place (see Per-Batch Steps).

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this plan has no completed hard upstream dependencies) | | | |

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. This plan is documentation-only — no source/test code is produced, so the Sonnet-subagent-for-code rule has no code to apply to. Edits to `docs/bugs/CLAUDE.md` and `CLAUDE.md` may be made directly (or via subagent).
2. All edits happen in the current worktree — NEVER create worktrees.
3. PHASES.md is updated AFTER the batch completes (not deferred).
4. Every edit's output is reviewed against the SPEC's Proven Findings for factual accuracy before continuing.
5. The phase is committed and pushed when its work units are done.
6. This plan is self-contained — follow it exactly as written without relying on external context.
7. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
8. Do NOT flip the top-level SPEC/PHASES `**Status:**` to Fixed/Won't-fix and do NOT write `FIXED.md` — those are owned by the orchestrator's `__mark_fixed__` gate after the validation tail. You MAY tick the per-phase deliverable checkboxes, flip this plan-part frontmatter `status:` to `Complete`, and set the per-phase `**Status:**` line.

## Work Units

- [ ] WU-1 — Add blessed direct-RESEARCH.md-drop note to docs/bugs/CLAUDE.md
- [ ] WU-2 — Add research-resume convention note to repo-root CLAUDE.md

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | no-gemini-sprint-staging-in-claude-config | P1 | Document blessed direct-RESEARCH.md-drop resume path | — | Solo (WU-1 + WU-2 edit different files — may run in one batch) |

## Phase: no-gemini-sprint-staging-in-claude-config P1 — Document the blessed direct-`RESEARCH.md`-drop resume path

**Goal:** Record, in two convention docs, that claude-config has no `docs/gemini-sprint/` staging by design and that research resume here is a direct `RESEARCH.md` drop (routed by `lazy-state.py` Step 5 → `/spec` Phase 3), cross-linking the `/ingest-research` per-repo adoption escape hatch.
**Entry criteria:** None (first and only phase; investigation Concluded).
**SPEC.md references:** `## Proven Findings` (graceful degradation in both consumers; direct drop is the working route) and `## Affected Area` (the two doc touch points).

### Work Unit WU-1 — Add blessed direct-`RESEARCH.md`-drop note to `docs/bugs/CLAUDE.md`

- **Scope:** PHASES.md P1 deliverable — `docs/bugs/CLAUDE.md` research-resume note.
- **TDD:** no (documentation edit, no testable behavior).
- **Files to create/modify:** `C:\Users\Jacob\source\repos\claude-config\docs\bugs\CLAUDE.md` [VERIFY: path exists — `C:\Users\Jacob\source\repos\claude-config\docs\bugs\CLAUDE.md`]
- **Implementation goal:** Append a short, durable subsection (e.g. `## Research resume`) to `docs/bugs/CLAUDE.md` stating:
  - claude-config has **no `docs/gemini-sprint/` staging structure by design** (negligible research volume).
  - The blessed research-resume route is a **direct `RESEARCH.md` drop** into the canonical feature/bug dir, picked up by `lazy-state.py` Step 5 → `/spec` Phase 3 naturally.
  - Both staging consumers already degrade gracefully when the staging dir is absent: `/lazy*` Step 0.5's `find docs/gemini-sprint/results …` returns empty and skips to the main loop; `/ingest-research` (no args, missing dir) exits 0 as an explicit no-op.
  - Cross-link `user/skills/ingest-research/SKILL.md` (the "per-repo adoption" note, ~line 65) as the escape hatch should a future high-research-volume self-edit workflow ever warrant the full structure (parameterize via `.claude/skill-config/gemini-sprint.md`).
- **Spec requirements:** SPEC `## Proven Findings` and `## Affected Area` row "Bug-pipeline docs convention | `docs/bugs/CLAUDE.md` | Add blessed-direct-drop note for self-edit repos". Keep the note factually consistent with the SPEC (do not claim the staging path is broken — it is absent, and the runtime degrades cleanly).
- **Batch:** 1

### Work Unit WU-2 — Add research-resume convention note to repo-root `CLAUDE.md`

- **Scope:** PHASES.md P1 deliverable — repo-root `CLAUDE.md` research-resume paragraph.
- **TDD:** no (documentation edit, no testable behavior).
- **Files to create/modify:** `C:\Users\Jacob\source\repos\claude-config\CLAUDE.md` [VERIFY: path exists — `C:\Users\Jacob\source\repos\claude-config\CLAUDE.md`]
- **Implementation goal:** Add a one-paragraph note to the repo-root `CLAUDE.md` in a Skills/Research-adjacent location (e.g. near the Skills System or Scripts section, wherever research/ingest is discussed) stating:
  - claude-config has no `docs/gemini-sprint/` staging; **research resume in this repo is the direct `RESEARCH.md` drop** into the feature/bug dir (routed by `lazy-state.py` Step 5 → `/spec` Phase 3).
  - Reference `user/skills/ingest-research/SKILL.md` per-repo adoption note (~line 65) as the escape hatch for a future high-volume case (parameterize via `.claude/skill-config/gemini-sprint.md`).
  - Keep it terse (the repo CLAUDE.md is dense); one paragraph, no new top-level section needed unless it reads cleaner as one.
- **Spec requirements:** SPEC `## Affected Area` row "Repo constitution | `CLAUDE.md` (repo root) | Optional: Research/resume note in the Scripts/Skills section". This deliverable was marked optional in the SPEC; per the completeness-first standing policy (D7) it is taken IN-CYCLE since it is scope-class (no divergent product behavior — both docs simply record the same convention).
- **No change to `user/skills/ingest-research/SKILL.md`** — its line ~65 already documents the per-repo adoption escape hatch (read-only reference only). Do NOT edit it.
- **Batch:** 1

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2 | Yes — different files | None |

## Execution Protocol

This protocol governs the autonomous execution of this phase. Follow it exactly.

### Phase Selection Loop

Only one phase exists. Execute its single batch, then run Post-Phase Steps, then Completion.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions. Create one task per work unit (WU-1, WU-2).

### Per-Batch Steps

For Batch 1:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow its instructions. Re-read from disk: this plan file, the sibling `PHASES.md`, and the sibling `SPEC.md` `## Proven Findings` / `## Affected Area` sections. The SPEC is the source of truth for the note's factual claims.

#### Step B.1: Make the Edits

This is a documentation-only batch — no TDD pipeline applies. Edit the two files per WU-1 and WU-2 above (directly, or via a single Sonnet subagent if the `Agent` tool is available). Each edit is additive (append/insert a note); do not restructure or delete existing content.

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` and apply the relevant parts. The review subject here is **factual accuracy against the SPEC**, not code correctness. Confirm:
- Both notes name the direct-`RESEARCH.md`-drop as the blessed route.
- Both correctly state the staging structure is absent **by design** (not broken).
- Both cross-link the `/ingest-research` per-repo adoption escape hatch.
- No existing content was deleted or misrepresented.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md` and follow its instructions. Tick the Phase 1 deliverable checkboxes for both edited files (and the "no change to ingest-research" + "Tests: none" rows), add an Implementation Notes block (date, what landed, files modified), and set the Phase 1 `**Status:**` line. Tick `- [x] WU-1` and `- [x] WU-2` in this plan's `## Work Units` list.

#### Step B.4: Run Quality Gates (N/A — documentation-only)

No build/test surface exists in claude-config for these files. In place of quality gates, run a markdown sanity read of both edited files to confirm the notes render cleanly (no broken fences, intact surrounding sections). Note the N/A explicitly.

#### Step B.4.5: MCP Integration Test (N/A)

No runtime-observable behavior — skip with a note (PHASES.md `**MCP runtime:** not-required`).

#### Step B.5: Commit Batch

Read the commit policy: first try `.claude/skill-config/commit-policy.md` in the project root; if absent, read `~/.claude/skills/_components/commit-and-push.md`. Commit the two doc edits + PHASES.md update + this plan's frontmatter/checkbox updates, and push to `main` (the work branch).

### Post-Phase Steps (after the batch)

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md`. The repo-root `CLAUDE.md` edit IS WU-2, so this is already satisfied; confirm no OTHER CLAUDE.md needs the note. (It does not — `docs/bugs/CLAUDE.md` is WU-1.)

#### Commit and Push Post-Phase Changes

Already handled in Step B.5 (single batch). If any reconciliation writes remain, commit + push them to `main`.

#### Finalize plan-part status

Flip THIS plan-part's frontmatter `status:` from `Ready` to `Complete` once both WUs are landed and committed. Do NOT touch the top-level SPEC/PHASES `**Status:**` (gate-owned).

## Blocking Issue Protocol

If a blocking issue is encountered:

1. Stop all in-progress work.
2. Commit and push any completed work.
3. Print a blocking-issue report (completed WUs, blocked WU, reason, recovery suggestion).
4. Write `BLOCKED.md` (canonical name) per `~/.claude/skills/_components/sentinel-frontmatter.md` if the obstacle is unrecoverable. Do not attempt to work around the blocker.

Blocking issues for this docs-only plan are unlikely; the most plausible is a merge/push conflict on `main` (resolve by rebase) or a SPEC/PHASES inconsistency discovered mid-edit (reconcile against the SPEC's Proven Findings).

## Completion

When the batch is complete:

1. Confirm both notes are on disk and factually consistent with the SPEC.
2. Confirm PHASES.md deliverables + both `- [x] WU-N` rows are ticked, Phase 1 `**Status:**` set, and this plan's frontmatter `status: Complete`.
3. Print a completion report:

   ## Implementation Batch — Complete

   **Features implemented:** no-gemini-sprint-staging-in-claude-config (documentation-only)
   **Total phases completed:** 1
   **Final quality-gate status:** N/A (docs-only — markdown sanity read passed)

   The top-level SPEC/PHASES `**Status:**` flip to Fixed and the `FIXED.md` receipt remain owned by the orchestrator's `__mark_fixed__` gate after the validation tail — NOT performed here.
