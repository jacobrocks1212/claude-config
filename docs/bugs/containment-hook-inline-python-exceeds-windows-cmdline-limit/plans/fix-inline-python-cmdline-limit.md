---
kind: implementation-plan
feature_id: containment-hook-inline-python-exceeds-windows-cmdline-limit
status: Complete
created: 2026-07-18
complexity: complex
phases: [1, 2, 3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-18.
> To execute: `/execute-plan docs/bugs/containment-hook-inline-python-exceeds-windows-cmdline-limit/plans/fix-inline-python-cmdline-limit.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — containment-hook-inline-python-exceeds-windows-cmdline-limit  (v2)

**PHASES.md files:**
- `docs/bugs/containment-hook-inline-python-exceeds-windows-cmdline-limit/PHASES.md` (3 phases)

**SPEC.md files:**
- `docs/bugs/containment-hook-inline-python-exceeds-windows-cmdline-limit/SPEC.md`

**Total phases:** 3
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (this is **claude-config**, a harness-config repo whose gates are Python/pytest + skill-lint, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this plan has no completed hard upstream dependencies) | | | |

## Plan-specific execution notes

- **Repo gates (claude-config).** There is no `/msbuild`/`/mstest` here. The quality gate for every WU is:
  - `python user/scripts/test_hooks.py` — the hook regression suite (drives the hooks via subprocess). The 22 `test_containment_*` are the Phase-1 red→green target; the whole suite must stay green.
  - `python ~/.claude/scripts/lint-skills.py` — advisory skill lint (should stay clean; hooks are not skills but run it for hygiene).
  - Optional: `ruff check user/scripts --config ruff.toml` (advisory, exit code informational) for any `test_hooks.py` edit.
- **No MCP runtime.** Per PHASES.md `**MCP runtime:** not-required` — this is bash-hook + pytest work with no app runtime surface. No `/mcp-test`, no `VALIDATED.md`; the bug pipeline's `__mark_fixed__` gate + `symptom-reproduction-gate` own completion. The runtime-verification rows in PHASES.md are ticked by the pytest gate, not by `/execute-plan`.
- **This IS the repro host.** DESKTOP-GHTC5K6 is exactly where the E2BIG fail-open manifests, so `python user/scripts/test_hooks.py` locally is the authoritative Windows regression — no separate device needed. The temp-file cross-dialect mechanism was already spike-validated here (see PHASES.md `## Validated Assumptions`).
- **Single writer per file / sequential batches.** The three WUs edit three distinct files but carry hard dependency edges (P2 copies P1's invocation pattern; P3's size ceiling must sit below every *remaining* `-c` body, which only P1+P2 establish). Execute them strictly in phase order — one batch per phase.
- **Fail-OPEN discipline is load-bearing.** Every hook edited here MUST preserve fail-OPEN-via-empty-output (never a non-zero PreToolUse exit) and MUST keep the unconditional `exit 0`. The ONLY new behavior is a *traced* fail-open (breadcrumb + `hook-events.jsonl` append) on the new temp-file-write failure path.
- **Do NOT feed the python body on stdin.** stdin carries the PreToolUse payload (`sys.stdin.read()` in each hook's embedded body). The SPEC's `- <<<"$BODY"` option is falsified; temp-file invocation only.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | containment-hook | P1 | Temp-file invocation for `lazy-cycle-containment.sh` + traced fail-open | — | Solo |
| 2 | containment-hook | P2 | Convert near-limit sibling `build-queue-enforce.sh` | P1 | Solo |
| 3 | containment-hook | P3 | Plane-wide embedded-`-c`-body size-guard test | P1, P2 | Solo |

## Work Units

- [x] WU-1 — Temp-file invocation + traced fail-open in `lazy-cycle-containment.sh`
- [x] WU-2 — Convert `build-queue-enforce.sh` to the temp-file invocation shape
- [x] WU-3 — Plane-wide embedded-`-c`-body size-guard test in `test_hooks.py`

---

### Phase: containment-hook P1 — Temp-file invocation for `lazy-cycle-containment.sh` + traced fail-open

**Goal:** Spawn the containment guard's python via a temp `.py` file (payload stays on stdin) so the ~33.7 KB body no longer exceeds the 32,767-char Windows command-line limit, restoring the guard's DENY behavior on Windows; add a traced fail-open for temp-write failure.
**Entry criteria:** None (first phase).
**SPEC.md references:** `## Root cause (Concluded)` (line 809 `-c "$_LCC_PY"`, 33,575-byte body, E2BIG → `exit 0` at 813); `## Fix scope` (temp-file invocation keeping payload on stdin; Windows regression); PHASES.md `## Validated Assumptions` (cygpath cross-dialect spike).

#### WU-1 — Temp-file invocation + traced fail-open in `lazy-cycle-containment.sh`

- **Scope:** PHASES.md Phase 1 deliverables — replace the `-c` invocation with a temp-file invocation; Windows path handling (`cygpath -w` where available, raw fallback); traced fail-open on mktemp/temp-write failure; preserve the fail-OPEN contract + `exit 0`; embedded python body (incl. `sys.stdin.read()`) otherwise untouched.
- **TDD:** yes (test-first — a Windows deny regression must go red-before-green; the 22 existing `test_containment_*` are already red on this host and become the green target).
- **Files to create/modify:** `user/hooks/lazy-cycle-containment.sh` [VERIFY: grep -n '"\$PYTHON" -c "\$_LCC_PY"' user/hooks/lazy-cycle-containment.sh].
- **Test files:** `user/scripts/test_hooks.py` (extend the existing Phase-4 containment section) [VERIFY: grep -n 'Phase 4 (lazy-cycle-containment' user/scripts/test_hooks.py].
- **Test expectations:**
  - Add/confirm a test that, with a cycle marker present + a subagent routing/lifecycle-op PreToolUse payload, driving `lazy-cycle-containment.sh` via `_run_bash` [VERIFY: grep -n 'def _run_bash' user/scripts/test_hooks.py] yields a `permissionDecision: deny` block on stdout (NOT empty/allow, NOT `Argument list too long` on stderr). This is RED before the fix on Windows (E2BIG fail-open) and GREEN after.
  - Add a test that forces the temp-file-write path to fail (e.g. point the temp dir at an unwritable location via the mechanism the hook uses, or assert the breadcrumb write on a simulated failure) and asserts fail-OPEN (exit 0, empty deny output) AND that a `hook-error.json` + a `hook-events.jsonl` line were written (traced, not silent). If forcing the failure is impractical in-suite, at minimum assert the breadcrumb-writing code path exists and is reached under a unit-level shim — but prefer a real driven failure.
  - The full existing containment suite (all 22) must pass.
- **Implementation goal:** In `lazy-cycle-containment.sh`, at the invocation site (currently `LCC_SCRIPTS_DIR="$LCC_SCRIPTS_DIR" "$PYTHON" -c "$_LCC_PY"`): `mktemp` a `.py` file; write `$_LCC_PY` into it; compute the path to pass to python as `cygpath -w "$tmpfile"` when `cygpath` is on PATH, else the raw path; invoke `LCC_SCRIPTS_DIR="$LCC_SCRIPTS_DIR" "$PYTHON" "$tmppath"` with the hook's real stdin (the payload) intact; `trap 'rm -f "$tmpfile"' EXIT` for cleanup. On any `mktemp`/write failure, write the `hook-error.json` breadcrumb + `hook-events.jsonl` append reusing the shape of the existing no-python block [VERIFY: grep -n 'no python interpreter on PATH' user/hooks/lazy-cycle-containment.sh] (distinct `detail`, e.g. `"temp-file write failed: <err>"`), then `exit 0`. Keep the trailing unconditional `exit 0`. Do NOT alter the embedded body (lines 117–797), including its `raw = sys.stdin.read()` [VERIFY: grep -n 'sys.stdin.read()' user/hooks/lazy-cycle-containment.sh]. Update the 111–116 comment block to describe the temp-file mechanism (and WHY not stdin: payload occupies it).
- **Spec requirements:** SPEC `## Fix scope` — "switching the SCRIPT to a temp-file invocation (keeping the payload on stdin) is the cleaner shape"; "Add a Windows-side regression: assert the containment hook DENIES (not fail-opens) for a subagent routing op under a marker on Windows." SPEC `## Root cause` — the fail-open must become traced.
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1 | Solo | None |

**Runtime Verification** *(ticked by the pytest gate, NOT the implementation agent):*
- [ ] <!-- verification-only --> Under a cycle marker on Windows, `lazy-cycle-containment.sh` DENIES a subagent routing/lifecycle op (`permissionDecision: deny`) — the reproduction inverted.
- [ ] <!-- verification-only --> A forced temp-file-write failure fails OPEN and leaves a `hook-error.json` + `hook-events.jsonl` breadcrumb (traced).

---

### Phase: containment-hook P2 — Convert near-limit sibling `build-queue-enforce.sh`

**Goal:** Convert `build-queue-enforce.sh`'s 31,966-byte `-c` invocation (≈800 B under the hard limit, before env-prefix/quoting overhead) to the Phase-1 temp-file shape, proactively removing the next E2BIG candidate.
**Entry criteria:** Phase 1 complete (the temp-file invocation pattern is established and green).
**SPEC.md references:** `## Sibling check` (audit the other `-c "$BODY"` hooks by shape and body size; the E2BIG class is plane-wide where a body approaches 32 K).

#### WU-2 — Convert `build-queue-enforce.sh` to the temp-file invocation shape

- **Scope:** PHASES.md Phase 2 deliverables — convert `build-queue-enforce.sh` to the temp-file invocation (mktemp `.py`, `cygpath -w` where available, payload on stdin, `trap` cleanup, traced fail-open on write failure); preserve `BQE_SCRIPTS_DIR` passthrough + the fail-OPEN-via-JSON contract; record the audit ledger.
- **TDD:** yes (behavior-preserving — the existing `build-queue-enforce` deny/allow/bypass/fail-open tests are the contract; they must stay green through the mechanism change).
- **Files to create/modify:** `user/hooks/build-queue-enforce.sh` [VERIFY: grep -n '"\$PYTHON" -c "\$_BQE_PY"' user/hooks/build-queue-enforce.sh].
- **Test files:** `user/scripts/test_hooks.py` (existing `build-queue-enforce` coverage; add a Windows deny-still-fires assertion if not already present) [VERIFY: grep -n 'build-queue-enforce' user/scripts/test_hooks.py].
- **Test expectations:** After conversion, the existing `build-queue-enforce` tests (deny of a raw manifested build token beginning a command segment; ALLOW of safe variants, the `BUILD_QUEUE_BYPASS=1` override, and the `build-queue.ps1` wrapper; fail-OPEN on malformed) all still pass — byte-identical behavior, only the invocation mechanism changed. Add/confirm one assertion that a manifested raw build token under a `build-queue-ops.json` repo still emits `permissionDecision: deny` post-conversion.
- **Implementation goal:** Apply the SAME transformation as WU-1 to `build-queue-enforce.sh`'s invocation site (`BQE_SCRIPTS_DIR="$BQE_SCRIPTS_DIR" "$PYTHON" -c "$_BQE_PY"`) [VERIFY: grep -n 'BQE_SCRIPTS_DIR=' user/hooks/build-queue-enforce.sh]: mktemp `.py`, write `$_BQE_PY`, `cygpath -w` where available, invoke `"$PYTHON" "$tmppath"` with payload on stdin, `trap` cleanup, traced fail-open on write failure. Keep the fail-OPEN-via-JSON contract and the silently-inert-off-Windows behavior. Reuse WU-1's established shape faithfully.
- **Spec requirements:** SPEC `## Sibling check` — siblings "should be audited for the same `-c "$BODY"` shape and body size during the fix." PHASES.md `## Validated Assumptions` — `build-queue-enforce.sh` body = 31,966 B, one harden round from tripping.
- **Batch:** 1 (of Phase 2).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-2 | Solo | None |

---

### Phase: containment-hook P3 — Plane-wide embedded-`-c`-body size-guard test

**Goal:** Add a `test_hooks.py` test that fails if any hook still invoking `"$PYTHON" -c "$_..._PY"` carries an embedded body over a conservative ceiling (well under 32,767), making the E2BIG class mechanically impossible to reintroduce silently.
**Entry criteria:** Phase 1 and Phase 2 complete (both large `-c` bodies converted to temp-file, so the ceiling sits below every remaining `-c` body without a false failure — max remaining is `long-build-ownership-guard.sh` at 19,805 B).
**SPEC.md references:** `## Fix scope` ("...and/or a size guard that fails if `$_LCC_PY` approaches the 32K limit"); `## Sibling check` (the E2BIG class is plane-wide).

#### WU-3 — Plane-wide embedded-`-c`-body size-guard test in `test_hooks.py`

- **Scope:** PHASES.md Phase 3 deliverables — a new test scanning every hook that invokes `"$PYTHON" -c "$_..._PY"`, measuring the named heredoc body, asserting `len(body) <= CEILING`; documented ceiling constant + rationale; test green because P1+P2 removed the over/near-limit bodies.
- **TDD:** yes (the test IS the deliverable; prove it gates by inflating a body past the ceiling in a scratch copy → FAIL, then confirm shipped hooks → PASS).
- **Files to create/modify:** `user/scripts/test_hooks.py` [VERIFY: test -f user/scripts/test_hooks.py].
- **Test files:** same file (`user/scripts/test_hooks.py`).
- **Test expectations:** A new test that globs `user/hooks/*.sh`, and for each file invoking `"$PYTHON" -c "$_<VAR>"`, extracts the `$_<VAR>` heredoc body (`read -r -d '' _<VAR> <<'PYEOF' … PYEOF`) and asserts its byte length `<= CEILING` (module constant, e.g. `25000`). Hooks with no `-c "$_..._PY"` invocation (converted to temp-file, or no python body) are skipped. The test passes for the shipped tree post-P1/P2. A demonstration (inflate a body past the ceiling in a scratch/monkeypatched copy) shows it FAILS — proving it is not tautologically green.
- **Implementation goal:** Add the test + a `_EMBEDDED_PY_CEILING = 25000` constant with a comment citing the 32,767 Windows `CreateProcess` limit, the env-prefix/quoting overhead margin, and `docs/bugs/containment-hook-inline-python-exceeds-windows-cmdline-limit`. Discover hooks generically so a future `-c`-invoking hook is covered automatically. Reuse the existing subprocess/path helpers where useful [VERIFY: grep -n '_HOOKS_DIR' user/scripts/test_hooks.py].
- **Spec requirements:** SPEC `## Fix scope` (size guard) + `## Sibling check` (plane-wide). This is the recurrence-prevention half of the fix.
- **Batch:** 1 (of Phase 3).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-3 | Solo | None |

**Runtime Verification** *(ticked by the pytest gate):*
- [ ] <!-- verification-only --> The size-guard test FAILS when an embedded `-c` body is inflated past the ceiling and PASSES for the shipped hooks (it actually gates, not tautologically green).

---

**Completion (gate-owned):** the bug pipeline's `__mark_fixed__` gate — after the `symptom-reproduction-gate` confirms the original symptom (containment hook fail-opens on Windows) is gone at its reported surface (`test_hooks.py` containment suite green + a driven DENY) — flips SPEC.md **Status:** to Fixed and writes FIXED.md. This plan never flips status or writes a receipt itself.
