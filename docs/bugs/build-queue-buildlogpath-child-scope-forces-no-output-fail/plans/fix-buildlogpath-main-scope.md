---
kind: fix-plan
feature_id: build-queue-buildlogpath-child-scope-forces-no-output-fail
status: Ready
created: 2026-07-06
complexity: complex
phases: []
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-06.
> To execute: `/execute-plan {this-path}`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — build-queue `$buildLogPath` child-scope discard forces `no-output` FAIL (fix)  (v2)

**Bug directory:** `docs/bugs/build-queue-buildlogpath-child-scope-forces-no-output-fail/`

**SPEC.md:** `docs/bugs/build-queue-buildlogpath-child-scope-forces-no-output-fail/SPEC.md` (Status: Concluded)
**INVESTIGATION.md:** `docs/bugs/build-queue-buildlogpath-child-scope-forces-no-output-fail/INVESTIGATION.md` (root-cause-confirmed at `investigated_commit: c90e2db` == current HEAD; cite its `## Repro Recipe` + `## Recommended Fix Scope` — do NOT re-derive)

**Total phases:** n/a (fix plan from a Concluded investigation SPEC — no PHASES.md)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (claude-config is a harness-config repo — its gates are PowerShell Pester + Python lint, NOT `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Plan-specific execution notes` below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## References — Upstream Artifacts

| Upstream feature | Kind | Path | Why this plan references it |
|------------------|------|------|------------------------------|
| (none — this plan has no completed hard upstream dependencies) | | | |

Adjacent prior-art (same defect CLASS, already Fixed — read for the test pattern, not a dep): `docs/bugs/build-queue-hygiene-dot-source-discarded-in-child-scope/` (commits `00437ca` RED guard / `2d9f8ae` fix). Its RED regression guard is the template for WU-1.

## Plan-specific execution notes

- **Quality gate (claude-config):** the relevant gate for this fix is the PowerShell Pester suite —
  `powershell.exe -ExecutionPolicy Bypass -Command "Invoke-Pester -Path 'user/scripts/build-queue-hygiene.Tests.ps1' -Output Detailed"`.
  `build-queue-runner.ps1` has **no** dedicated `*.Tests.ps1` (it executes top-level, not a dot-sourceable module), which is exactly why the sibling bug used a *structural* regression guard living in `build-queue-hygiene.Tests.ps1`. Also run `python user/scripts/lint-skills.py` (should be a no-op for a `.ps1` change) so the standard claude-config gates are green.
- **Dirty tree:** at plan time the tree carries a pre-existing unrelated `M user/plugins/.../weights.yaml` and this bug's `SPEC.md`/`INVESTIGATION.md`. The `INVESTIGATION.md` is already committed; commit `SPEC.md` with the fix. Do NOT sweep `weights.yaml` into the fix commit — it is unrelated; leave it for Jacob (surface it, don't auto-commit it).
- **Runtime re-verify is an operator/manual step, NOT a blocking WU.** claude-config has no `/mcp-test` Step-9 gate; the live proof (`/msbuild` on a clean Cognito worktree → `RESULT=PASS build_fidelity=verified`, and confirm `log-failure-override` revives on a genuinely-broken build) requires a Windows host + a real Cognito checkout. It is captured as a non-blocking `## Runtime Verification` row below and flagged in the Completion report for Jacob to run — `/execute-plan` cannot close it in-session.
- **One writer per file:** another live session may still run builds through the queue. This fix edits `build-queue-runner.ps1` (not edited by that session) — safe. The runner change only affects builds started AFTER it lands.

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title | Blocked by | Parallel? |
|------|-----------|----------|-------|------------|-----------|
| 1 | fix | — | WU-1 RED regression guard | — | Solo |
| 2 | fix | — | WU-2 main-scope bind | WU-1 | Solo |

## Work Units

- [x] WU-1 — RED regression guard: `$buildLogPath` bound at runner script scope
- [ ] WU-2 — Fix: bind `$buildLogPath` in main scope (turns WU-1 green)

Runtime Verification (operator-owned; `/execute-plan` does NOT tick these):

## Runtime Verification

- [ ] <!-- verification-only --> Live `/msbuild` on a clean Cognito worktree reports `RESULT=PASS (build_fidelity=verified)` on an exit-0 compile (was false `no-output` FAIL). Operator, Windows host.
- [ ] <!-- verification-only --> `log-failure-override` revives: a genuinely-broken build (MSBuild error signature, exit 0) reports `RESULT=FAIL (build_fidelity=log-failure-override)`, proving the classify block now reads the log. Operator, Windows host.

---

### Phase: fix — build-queue `$buildLogPath` main-scope bind

**Goal:** Bind `$buildLogPath` in `build-queue-runner.ps1`'s main scope so the classify block reads the captured log, so an exit-0 build with a populated `<seq>.build.log` reports `verified` instead of a forced `no-output` FAIL.
**Entry criteria:** none (Concluded SPEC; root cause `traced`).
**SPEC.md references:** `## Proven Findings` (1–4), `## Affected Area`, `## Evidence Collected → Source Code` serving-path node 6 (`build-queue-runner.ps1:112` defect node).

#### WU-1 — RED regression guard: `$buildLogPath` bound at runner script scope

- **Scope:** Add a failing structural regression guard that fails against the CURRENT (buggy) runner and passes only once the assignment is at script scope.
- **TDD:** yes (RED first).
- **Files to create/modify:** `user/scripts/build-queue-hygiene.Tests.ps1` (add a new `Describe` block).
- **Test files:** same file.
- **Test expectations:** Mirror the existing `Describe 'scope-in-caller guard …'` regression-guard style [VERIFY: grep -n "scope-in-caller guard" user/scripts/build-queue-hygiene.Tests.ps1] — read `build-queue-runner.ps1` as text/AST and assert that the `$buildLogPath = Join-Path ... "$Seq.build.log"` assignment [VERIFY: grep -n 'buildLogPath = Join-Path' user/scripts/build-queue-runner.ps1] is a **script/main-scope statement**, NOT nested inside a `Get-SafeValue { … }` (or any child scriptblock) within the `if ($isBuildOp)` block [VERIFY: grep -n 'if ($isBuildOp)' user/scripts/build-queue-runner.ps1]. Prefer an AST assertion (parse the file, find the `$buildLogPath` `AssignmentStatementAst`, assert its enclosing scope is the script block and not a `ScriptBlockExpressionAst` argument to `Get-SafeValue`); a robust text-structure check is acceptable if AST is impractical, matching the sibling guard's approach. The test MUST be RED against current HEAD.
- **Implementation goal:** A guard that would have caught this bug class and the sibling's — anchored on the variable that must survive to classify (`$buildLogPath`), not on incidental text.
- **Spec requirements:** SPEC `## Proven Findings` 1; INVESTIGATION serving-path node 6.
- **Batch:** 1.

#### WU-2 — Fix: bind `$buildLogPath` in main scope

- **Scope:** Refactor `build-queue-runner.ps1:106-116` so `$buildLogPath` (and the err-log path) are assigned in the runner's main scope, surviving to the classify block at `:150-183`.
- **TDD:** no (closes WU-1's RED guard; no new behavior beyond the guard + the operator runtime rows).
- **Files to create/modify:** `user/scripts/build-queue-runner.ps1` (the `if ($isBuildOp)` path-setup block, ~106-116) [VERIFY: grep -n 'RedirectStandardOutput' user/scripts/build-queue-runner.ps1].
- **Implementation goal:** Minimal, low-risk main-scope bind (preferred: compute `$buildLogPath = Join-Path $logsDir "$Seq.build.log"` and the err path at main scope; keep ONLY the fail-open `New-Item -ItemType Directory` inside a `Get-SafeValue { }`; still set `$startProcParams['RedirectStandardOutput']`/`['RedirectStandardError']` at main scope — the hashtable mutation already propagated correctly, so preserve it). Acceptable equivalents: `$script:buildLogPath = …`, or return the path from the `Get-SafeValue` block. **Do NOT touch** `Test-BuildProducedNoOutput`, `Read-WithRetry`, or the classify branches (`:163-183`) — they are correct and were merely fed `$null`. Binding the path revives BOTH the `no-output` and `log-failure-override` branches (same short-circuit defeated both). Keep the `$buildLogPath = $null` init at `:86`.
- **Spec requirements:** SPEC `## Proven Findings` 1–3; INVESTIGATION `## Recommended Fix Scope` (claude-config-only; `build-filtered.ps1` is correct — no Cognito-side change).
- **Batch:** 2.

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1     | WU-1      | Solo      | None            |
> | 2     | WU-2      | Solo      | None (different file; ordered after WU-1 so the guard flips RED→GREEN) |

---

## Completion

Follow the Completion report in `~/.claude/skills/_components/execution-contract.md`. In addition, this fix plan's report MUST:
- Confirm the Pester suite is green (`build-queue-hygiene.Tests.ps1`), with WU-1's guard now passing.
- Confirm `build-queue-runner.ps1:86` init retained and the classify branches untouched.
- **Surface the two `## Runtime Verification` rows to Jacob as an operator TODO** — they require a Windows host + a real Cognito worktree and cannot be closed in-session. The bug is not `Fixed` (no `FIXED.md`) until the symptom-reproduction gate is satisfied by that live re-verify (SPEC `## Reproduction Steps`).
- Note `weights.yaml` was left uncommitted (unrelated pre-existing change).
