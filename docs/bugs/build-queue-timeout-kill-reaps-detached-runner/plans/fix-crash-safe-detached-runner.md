---
kind: fix-plan
feature_id: build-queue-timeout-kill-reaps-detached-runner
status: Ready
created: 2026-07-19
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-07-19.
> To execute: `/execute-plan docs/bugs/build-queue-timeout-kill-reaps-detached-runner/plans/fix-crash-safe-detached-runner.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — build-queue-timeout-kill-reaps-detached-runner (v2)

**PHASES.md files:**
- `docs/bugs/build-queue-timeout-kill-reaps-detached-runner/PHASES.md` (build-queue-timeout-kill-reaps-detached-runner, 2 phases)

**SPEC.md files:**
- `docs/bugs/build-queue-timeout-kill-reaps-detached-runner/SPEC.md`

**Total phases:** 2
**Plan version:** v2 (reference-based — components loaded from disk per step)

> **Reconciliation note — fix already landed 2026-07-10.** The crash-safe-release fix is **already
> on disk** (SPEC `## Fix (implemented 2026-07-10)`; `build-queue-runner.Tests.ps1` 8/8 Pester). This
> plan decomposes the landed work so the bug pipeline reconciles it through `/execute-plan` →
> validation → the gated `__mark_fixed__` receipt. **Executor guidance:** for each WU, VERIFY the
> deliverable against current source (cited line ranges) and confirm its Pester coverage is green
> rather than re-authoring code that already exists. If a deliverable is genuinely already satisfied,
> tick its WU after confirming the tests pass; do NOT churn working code. (The `is_fixed_unreconciled`
> gate did not divert this bug because the SPEC recorded the fix under a `## Fix (implemented …)`
> heading, not the `**Fixed:**` evidence annotation the gate keys on — flagged for harness hardening.)

## Execution Policy — single-sourced

This plan's autonomous-execution policy lives in **`~/.claude/skills/_components/execution-contract.md`**. The executing session MUST `Read` that file before executing any batch and follow it as the operating contract — it defines the EXECUTION MODEL (orchestrator + Sonnet subagent roles), the COMPONENT LOADING PROTOCOL, the Component Reference Card, the MANDATORY RULES, the full Execution Protocol (Phase-Selection Loop + per-batch Steps B.0–B.6 + Post-Phase Steps), the Blocking Issue Protocol, and the Completion report.

Where this plan's repo uses non-default gates or component paths (a harness-config repo whose gates are Python/Pester rather than `/msbuild`/`/mstest`), this plan's own per-phase steps and the `## Component Reference Card` override below override the contract's defaults for those rows. Everything the contract specifies that this plan does not override still applies.

## Component Reference Card (repo-specific overrides)

claude-config has **no Tauri/MCP dev-runtime** and **no msbuild/mstest gates**. Substitute:

| Contract default | This repo's gate |
|------------------|------------------|
| `/msbuild`, `/mstest` build/test gates | `Invoke-Pester user/scripts/build-queue-runner.Tests.ps1` (+ the sibling `build-queue-hygiene.Tests.ps1` / `build-queue-await.Tests.ps1` for regression) — run via `powershell.exe -ExecutionPolicy Bypass -Command "Invoke-Pester -Path <file> -Output Detailed"` |
| MCP live-validation (Step 9) | N/A — `MCP runtime: not-required` (Windows/Pester-only build tooling). The PHASES.md `**Runtime Verification**` rows are ticked by the Pester regression suite, not a live MCP pass. |
| Doc/skill lint | `python ~/.claude/scripts/lint-skills.py` (only if a skill/component was touched — not applicable to this PowerShell fix) |

**Windows/Pester note:** these tests launch a REAL detached runner and kill it with `TerminateProcess`; they are workstation-only (Windows PowerShell 5.1+ / Pester 5.x) and are inert / skipped off-Windows. A cloud/non-Windows executor cannot run them — record that as the reason if the runtime rows defer.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies. SPEC `**Related:**` cross-links `build-queue-orphaned-result-on-wrapper-kill`, `build-queue-generalization`, and `build-queue-eta-priority-lanes` for context, but this bug declares no `**Depends on:**` block.)

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | build-queue-timeout-kill | P1 | `Write-BuildQueueResult` early-write helper (WU-1) | — | Solo |
| 2 | build-queue-timeout-kill | P1 | Runner two-phase crash-safe write (WU-2) | WU-1 | Solo |
| 3 | build-queue-timeout-kill | P2 | Adjacent serving-path defects (WU-3) | WU-2 | Solo |

All three WUs are serialized: WU-1 → WU-2 → WU-3 share `build-queue-runner.Tests.ps1`, and WU-2/WU-3 both modify `build-queue-runner.ps1` (file-overlap rule → separate sequential batches, each a review gate).

## Work Units

- [ ] WU-1 — `Write-BuildQueueResult` fail-open atomic early-write helper
- [ ] WU-2 — Runner two-phase crash-safe result write (early pending + final complete)
- [ ] WU-3 — Adjacent serving-path defects: bare-`True` stdout leak + counts sharing violation

---

### Phase: build-queue-timeout-kill P1 — Two-phase crash-safe result write

**Goal:** Shrink the runner's unprotected post-build window so a truthful result survives a kill (exit 143) at any point after the build process exits.
**Entry criteria:** None (first phase).
**SPEC.md references:** `## Fix (implemented 2026-07-10)` (What changed, items 1–2, 6); Trace 1 + Trace 2 (`## Evidence Collected`); Proven Finding 3 (crash-safe release constraint).

#### WU-1 — `Write-BuildQueueResult` fail-open atomic early-write helper

- **Scope:** PHASES.md Phase 1 deliverable "new `Write-BuildQueueResult` — fail-open atomic result writer (temp + `File.Replace`, direct-write fallback) implementing the EARLY write; a Pester-testable reference for the result schema."
- **TDD:** yes
- **Files to create/modify:** `user/scripts/build-queue-hygiene.ps1` (add/verify `function Write-BuildQueueResult` [VERIFY: grep -n "function Write-BuildQueueResult" user/scripts/build-queue-hygiene.ps1] — already present at ~:2159; confirm atomic temp+`File.Replace` with direct-write fallback and the documented result schema).
- **Test files:** `user/scripts/build-queue-runner.Tests.ps1` (the `Describe 'Write-BuildQueueResult (unit — real hygiene module)'` block [VERIFY: grep -n "Write-BuildQueueResult (unit" user/scripts/build-queue-runner.Tests.ps1] — already present at ~:591).
- **Test expectations:** the helper writes the result atomically and returns the recorded `ended_at`; final-over-early overwrite; legacy no-op field omission; fail-open on write failure.
- **Implementation goal:** a reusable fail-open writer callable from the runner's `finally` for the EARLY write, sharing the exact result schema the FINAL inline write emits. Uses `Get-SafeValue` [VERIFY: grep -n "function Get-SafeValue" user/scripts/build-queue-hygiene.ps1] for defensive wrapping where applicable.
- **Spec requirements:** SPEC `## Fix` item "build-queue-hygiene.ps1 — Write-BuildQueueResult. New fail-open atomic result writer (temp + File.Replace, direct-write fallback)…".
- **Batch:** 1

#### WU-2 — Runner two-phase crash-safe result write (early pending + final complete)

- **Scope:** PHASES.md Phase 1 deliverables: crash-safe EARLY write with `hygiene.status: "pending"` once `$exitCode` + fidelity known and guarded on unknown exit; `$resultFidelity`/`$counts`/`$durationSeconds` moved into the `finally`; FINAL inline write kept INLINE merging real hygiene fields with `hygiene.status: "complete"`; `exit 1` on the degraded spawn-exception path; result-schema doc header updated.
- **TDD:** yes
- **Files to create/modify:** `user/scripts/build-queue-runner.ps1` — the `finally` block region [VERIFY: grep -n "^} finally {" user/scripts/build-queue-runner.ps1] (~:260–:437): early `Write-BuildQueueResult` call with `status = 'pending'` [VERIFY: grep -n "status  *= *'pending'" user/scripts/build-queue-runner.ps1] (~:360), final inline write with `status = 'complete'` [VERIFY: grep -n "status  *= *'complete'" user/scripts/build-queue-runner.ps1] (~:425), degraded `exit 1` [VERIFY: grep -n "exit 1" user/scripts/build-queue-runner.ps1] (~:481), and the header schema doc for `hygiene.status`. Calls hygiene's `Stop-BuildJobTree` / `Reset-CompilerServer` / `Remove-PoisonedArtifacts` [VERIFY: grep -n "function Remove-PoisonedArtifacts" user/scripts/build-queue-hygiene.ps1] in unchanged order.
- **Test files:** `user/scripts/build-queue-runner.Tests.ps1` — `Describe 'build-queue-runner.ps1 — crash-safe early write (kill-mid-hygiene serving path)'` [VERIFY: grep -n "crash-safe early write" user/scripts/build-queue-runner.Tests.ps1] (~:435) and `Describe '… — final write merges real hygiene over the early write'` [VERIFY: grep -n "final write merges real hygiene" user/scripts/build-queue-runner.Tests.ps1] (~:526).
- **Test expectations:** REAL runner launched detached against a temp StateRoot with a machine-safe shim hygiene module (real module dot-sourced; `Reset-CompilerServer`/`Stop-DllLockers` overridden; `Remove-PoisonedArtifacts` blocks ~120s to model the Cognito RED sweep) and a fake RED exec (`exit 1`), then killed with TerminateProcess mid-sweep → assert `results/<seq>.json` EXISTS with `exit_code: 1` + `hygiene.status: "pending"`, `active.lock` honestly still held. Fast-path tests: FINAL write shows `status: "complete"`, merged quarantine/recycle fields, lock released, stats ring appended (green build path exit 0, no sweep).
- **Implementation goal:** the two-phase write — truthful result the moment the exit code is known (BEFORE hygiene), then a final merge — with lock-release position, hygiene order/gating, and try/finally exception semantics UNCHANGED. Depends on WU-1's `Write-BuildQueueResult`.
- **Spec requirements:** SPEC `## Fix` item "build-queue-runner.ps1 — two-phase result write"; Proven Finding 3 (crash-safe release, operator-locked).
- **Batch:** 2

**Runtime Verification (Phase 1)** *(checked by the Pester regression suite — NOT by the implementation agent):*
- [ ] <!-- verification-only --> A detached runner killed (TerminateProcess) mid-`Remove-PoisonedArtifacts` leaves `results/<seq>.json` with `exit_code: 1` and `hygiene.status: "pending"`.
- [ ] <!-- verification-only --> A runner completing hygiene normally produces a FINAL result with `hygiene.status: "complete"`, real quarantine/recycle fields merged, `active.lock` released, stats ring appended.
- [ ] <!-- verification-only --> `build-queue-await.ps1 -Seq <n>` after a mid-sweep kill exits 1 re-emitting the `RESULT=FAIL` banner (reads the early result) instead of exiting 124.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | None            |
| 2     | WU-2      | Solo      | Shares test file with WU-1 (sequential) |

---

### Phase: build-queue-timeout-kill P2 — Adjacent same-serving-path defects

**Goal:** Fix two defects on the same runner serving path that corrupt the truthful result: the bare-`True` stdout leak and the deterministic counts sharing violation.
**Entry criteria:** Phase 1 complete (the `finally` restructure and early/final write must exist — these fixes live inside that region and feed the same result body).
**SPEC.md references:** `## Fix` items "Bare-`True` stdout leak fixed" and "Adjacent defect found & fixed on the same serving path — test-op counts sharing violation."

#### WU-3 — Adjacent serving-path defects: bare-`True` stdout leak + counts sharing violation

- **Scope:** PHASES.md Phase 2 deliverables: both unassigned `Get-SafeValue { Stop-BuildJobTree … }` returns `$null =`-assigned (trap + finally) so no stray `True` reaches `logs/<seq>.log`; the test-counts parse opens `logs/<seq>.log` with `FileShare.ReadWrite` instead of `ReadAllText`/`FileShare.Read`.
- **TDD:** yes
- **Files to create/modify:** `user/scripts/build-queue-runner.ps1` — trap + finally `$null =` assignments [VERIFY: grep -n "\$null = Get-SafeValue { Stop-BuildJobTree" user/scripts/build-queue-runner.ps1] (~:143, ~:376) and the FileShare.ReadWrite counts open [VERIFY: grep -n "FileShare\]::ReadWrite" user/scripts/build-queue-runner.ps1] (~:316).
- **Test files:** `user/scripts/build-queue-runner.Tests.ps1` — the RED-test-op kill test [VERIFY: grep -n "RED test op" user/scripts/build-queue-runner.Tests.ps1] (~:493) and the no-bare-`True` stdout pin in the final-write fast-path Describe (~:535).
- **Test expectations:** a RED **test** op killed mid-hygiene produces an early result carrying the parsed `counts`; `build-queue-await.ps1` reports `tests=<n> failed=<k>` in the `RESULT=FAIL` banner; `logs/<seq>.log` carries no stray bare `True` line after normal completion.
- **Implementation goal:** correct the counts read (live-tail `FileShare.ReadWrite` pattern) and silence the leaked `Stop-BuildJobTree` `True` — both latent at the OLD post-hygiene position too; the crash-safe move made a correct counts read newly load-bearing for the early result. No changes to `build-queue-await.ps1` / `build-queue.ps1`.
- **Spec requirements:** SPEC `## Fix` items on the stdout leak and the counts sharing violation (empirically-confirmed deterministic `FileShare.Read` violation on the runner's own open stdout handle).
- **Batch:** 3

**Runtime Verification (Phase 2)** *(checked by the Pester regression suite — NOT by the implementation agent):*
- [ ] <!-- verification-only --> A RED **test** op killed mid-hygiene produces an early result carrying the parsed `counts`, and `build-queue-await.ps1` reports `tests=<n> failed=<k>` in the `RESULT=FAIL` banner.
- [ ] <!-- verification-only --> `logs/<seq>.log` contains no stray bare `True` line after a normal runner completion.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 3     | WU-3      | Solo      | Shares `build-queue-runner.ps1` + test file with WU-2 (sequential) |

---

## Plan-specific execution notes

- **Gates are Pester, not msbuild/mstest.** After each WU, run `Invoke-Pester -Path user/scripts/build-queue-runner.Tests.ps1 -Output Detailed` (+ the sibling `build-queue-hygiene.Tests.ps1` / `build-queue-await.Tests.ps1` on regression). There is no Tauri/MCP runtime — the `**Runtime Verification**` rows are Pester-owned, not Step-9-owned.
- **Fix already on disk — verify, don't churn.** Confirm each deliverable against the cited line ranges and confirm green Pester before ticking a WU. Per the SPEC, `build-queue-hygiene.Tests.ps1` shows 3 pre-existing failures (`Add-ProcessToBuildJob`/`Stop-BuildJobTree` zero-handle bool + `Reset-CompilerServer` bool) that reproduce identically against unmodified HEAD — a pre-existing Pester scriptblock-scope gotcha in functions this fix does not touch; do NOT treat them as regressions introduced by this work.
- **Windows-only.** These tests kill a real detached process (`TerminateProcess`); they are inert off-Windows. A non-Windows executor records that as the deferral reason for the runtime rows and cannot close them.
- **Completion (gate-owned):** the `__mark_fixed__` gate flips SPEC.md **Status:** to `Fixed`, writes `FIXED.md`, and archives the bug dir once the validation tail passes. This plan never authors a Status flip / receipt / archive row.

Plan written to: docs/bugs/build-queue-timeout-kill-reaps-detached-runner/plans/fix-crash-safe-detached-runner.md
