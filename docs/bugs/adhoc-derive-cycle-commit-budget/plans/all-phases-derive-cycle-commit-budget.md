---
kind: implementation-plan
feature_id: adhoc-derive-cycle-commit-budget
status: Complete
created: 2026-06-22
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-22.
> To execute: `/execute-plan docs/bugs/adhoc-derive-cycle-commit-budget/plans/all-phases-derive-cycle-commit-budget.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — adhoc-derive-cycle-commit-budget  (v2)

**PHASES.md files:**
- `docs/bugs/adhoc-derive-cycle-commit-budget/PHASES.md` (adhoc-derive-cycle-commit-budget, 2 phases)

**SPEC.md files:**
- `docs/bugs/adhoc-derive-cycle-commit-budget/SPEC.md`

**Total phases:** 2
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.ts`, `.js`, `.cs`, `.vue`, `.py`, `.rs`, `.tsx`, `.jsx`, or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **DISPATCH-OVERRIDE NOTE (autonomous bug-pipeline execution).** When this plan is executed by a `/lazy-bug-batch` cycle subagent that lacks the `Agent` tool, the orchestrator's inline-override instructs the executor to perform ALL implementation INLINE with `Read`/`Edit`/`Write` (test-first), since `Agent()` would fail. In that mode the "delegate to Sonnet" rule below is satisfied by inline test-first authoring. Outside that mode, dispatch via Agent as written.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

Upstream plans and PHASES.md files this plan was authored against. The executing session SHOULD `Read` these before starting any work unit whose Implementation goal references the corresponding upstream contract. Listed in dependency order.

(none — this bug has no completed hard upstream dependencies)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on source or test files. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines), AND the autonomous bug-pipeline inline-override noted in the EXECUTION MODEL block.
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents
3. Every TDD work unit goes through the test-first pipeline — failing tests written before implementation
4. PHASES.md is updated AFTER EACH batch completes (not deferred)
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing
6. Mistakes are fixed immediately before launching the next batch
7. After all batches in a phase finish, integration verification confirms all changes work together
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it
9. Each completed phase is committed and pushed before the next phase begins
10. Cross-feature phases may run in parallel when dependencies are satisfied and no file conflicts exist
11. This plan is self-contained — follow it exactly as written without relying on external context
12. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1    | adhoc-derive-cycle-commit-budget | P1 | Derive the cycle-commit budget from a single registry SSOT | — | Solo |
| 2    | adhoc-derive-cycle-commit-budget | P2 | Regression-prove the class is closed and all gates green | P1 | Solo |

## Work Units

- [x] WU-1 — Introduce `lazy_core` multi-commit dispatch-skill registry SSOT
- [x] WU-2 — Derive `detect_cycle_bracket_friction` branch-3 budget from the registry; retire the literal table
- [x] WU-3 — Regression test the closed class + run the full gate set

---

### Phase: adhoc-derive-cycle-commit-budget P1 — Derive the cycle-commit budget from a single registry SSOT

**Goal:** Replace the reactive literal `_CYCLE_COMMIT_BUDGET` table with a budget derived from a single `lazy_core`-owned dispatch-skill registry, so an unenumerated multi-commit sub_skill cannot silently default to 1.
**Entry criteria:** none (head phase).
**SPEC.md references:** `## Proven Findings` → "Fix scope (class boundary)" (IN: branch 3 at `lazy_core.py:9191-9192`; OUT: `budget_override` + `kind=="meta"`); `## Open Questions` (both resolved scope-class under D7 — registry SSOT, no per-skill metadata flag needed since the ceiling is uniform).

#### WU-1 — Introduce `lazy_core` multi-commit dispatch-skill registry SSOT

- **Scope:** PHASES.md P1 deliverable 1 — "Introduce a `lazy_core`-owned SSOT for the multi-commit dispatch-skill set".
- **TDD:** yes (the registry's membership is asserted by the WU-3 regression; a focused test on the constant's contents is appropriate).
- **Files to create/modify:** `user/scripts/lazy_core.py` (add a module-level constant near the existing `_CYCLE_COMMIT_BUDGET_DEFAULT` / `_CYCLE_COMMIT_BUDGET` block, ~line 8939 `[VERIFY: grep -n "_CYCLE_COMMIT_BUDGET_DEFAULT" user/scripts/lazy_core.py]`).
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** assert the new SSOT constant (e.g. `_MULTI_COMMIT_DISPATCH_SKILLS`) is a `frozenset` containing exactly the multi-commit dispatch identities: `execute-plan`, `retro-feature`, `mcp-test`, `write-plan`, `plan-feature`, `plan-bug`, `__mark_complete__`, `__mark_fixed__` — and does NOT contain a single-commit-only name (e.g. `spec-bug` / `spec-phases`). This locks the membership so a future regression catches an accidental drop.
- **Implementation goal:** add a single `frozenset[str]` constant naming every dispatch identity whose cycle legitimately commits more than once. Carry ONE block comment documenting that this is the SSOT the budget derives from (consolidating the five reactive per-row provenance comments into one structural note), and that adding a new multi-commit dispatch skill means adding it HERE — co-located with the dispatch-skill identity — never a separate budget row. Note in the comment that the `bug-state.py` `SKILL_*` constants (`:159-166` `[VERIFY: grep -n "SKILL_EXECUTE_PLAN" user/scripts/bug-state.py]`) and the `lazy-state.py` bare-literal dispatch sites pass these SAME strings, so this set is the natural SSOT for both pipelines. Keep `_CYCLE_COMMIT_BUDGET_DEFAULT = 1` and define the uniform multi-commit ceiling (e.g. `_CYCLE_COMMIT_MULTI = 3`) as a named constant.
- **Spec requirements:** SPEC `## Proven Findings` → "The SSOT exists already" + "Coupling" (lands once in `lazy_core`, serves both pipelines, no parity mirror).
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2 | No (WU-2 depends on WU-1's constant; same file) | Same file `lazy_core.py` — sequential |

#### WU-2 — Derive `detect_cycle_bracket_friction` branch-3 budget from the registry; retire the literal table

- **Scope:** PHASES.md P1 deliverables 2 + 3 + 4 — replace branch (3) with the derivation; remove the hand-maintained literal dict (sole consumer confirmed); confirm both pipelines served unchanged.
- **TDD:** yes (behavior asserted by the WU-3 regression; covered by the existing `test_detect_friction_*_within_budget` suite staying green).
- **Files to create/modify:** `user/scripts/lazy_core.py` — `detect_cycle_bracket_friction` branch (3) at `[VERIFY: grep -n "_CYCLE_COMMIT_BUDGET.get" user/scripts/lazy_core.py]` (currently `lazy_core.py:9191-9192`).
- **Test files:** none new in this WU (WU-3 owns the new regression).
- **Implementation goal:**
  1. Replace branch (3)'s `budget = _CYCLE_COMMIT_BUDGET.get(sub_skill or "", _CYCLE_COMMIT_BUDGET_DEFAULT)` with a DERIVATION: `budget = _CYCLE_COMMIT_MULTI if (sub_skill or "") in _MULTI_COMMIT_DISPATCH_SKILLS else _CYCLE_COMMIT_BUDGET_DEFAULT`. Leave branch (1) `if marker.get("kind") == "meta": return None` and branch (2) `if isinstance(budget_override, int) and budget_override > 0: budget = budget_override` BYTE-IDENTICAL and in the same order. Leave the `if commits_since > budget:` runaway-detection block unchanged (genuine runaways still trip).
  2. Remove the hand-maintained `_CYCLE_COMMIT_BUDGET: dict[str, int]` literal and its five reactive per-row provenance comment blocks. Branch (3) is its sole consumer `[VERIFY: grep -rn "_CYCLE_COMMIT_BUDGET" user/scripts/]` (confirmed: only `lazy_core.py` references it). If the grep at execution time finds ANY consumer other than branch 3 or the new derivation, do NOT delete — instead rebuild the dict FROM the registry (`{k: _CYCLE_COMMIT_MULTI for k in _MULTI_COMMIT_DISPATCH_SKILLS}`) so the literal table is no longer hand-appended, and report the extra consumer.
- **Spec requirements:** SPEC `## Proven Findings` → "Fix scope (class boundary)" — IN branch 3 only; OUT `budget_override` (`:9183-9189`) + `kind=="meta"` (`:9180-9181`); "No new behavior beyond making the budget table self-populating/derived. Genuine runaways must still trip."
- **Batch:** 1.

---

### Phase: adhoc-derive-cycle-commit-budget P2 — Regression-prove the class is closed and all gates green

**Goal:** Add a regression proving a registry-known multi-commit skill is budgeted without a literal row (closing the missing-row CLASS, not just re-pinning current rows), and verify the full gate set stays green.
**Entry criteria:** P1 complete (registry SSOT + derivation landed).
**SPEC.md references:** `## Affected Area` → "Friction-detector tests" row ("add a regression asserting a registry-known multi-commit skill is budgeted without a literal row").

#### WU-3 — Regression test the closed class + run the full gate set

- **Scope:** PHASES.md P2 deliverables 1 + 2 + 3 — add the class-closure regression; confirm existing friction/budget tests green; run the full gate set.
- **TDD:** yes.
- **Files to create/modify:** `user/scripts/test_lazy_core.py` (add the test + register it in the module's test-runner list near `[VERIFY: grep -n "test_detect_friction_within_commit_budget_returns_none" user/scripts/test_lazy_core.py]`, ~line 20645).
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** add `test_detect_friction_registry_known_skill_budgeted_without_literal_row` asserting:
  1. A multi-commit dispatch skill IN `_MULTI_COMMIT_DISPATCH_SKILLS` (e.g. `mcp-test`) with `commits_since` = 2 or 3 returns `None` (no friction) via the DERIVATION — and is budgeted at `_CYCLE_COMMIT_MULTI`, not via any literal dict row.
  2. A skill ABSENT from the registry (e.g. a fabricated `sub_skill="brand-new-skill"`) with `commits_since` = 2 returns an `unexpected-commits` descriptor (default budget 1 preserved → genuine-runaway detection intact).
  3. (Class-closure assertion) Membership in the registry — not a literal-table row — is what grants the multi-commit budget: assert that every name in `_MULTI_COMMIT_DISPATCH_SKILLS` is budgeted multi-commit by `detect_cycle_bracket_friction` (loop the set; `commits_since=_CYCLE_COMMIT_MULTI` → `None`, `commits_since=_CYCLE_COMMIT_MULTI+1` → `unexpected-commits`). Use the same fixture shape as the existing `test_detect_friction_*_within_budget` tests `[VERIFY: grep -n "def test_detect_friction_mcp_test_cycle_multi_commit_within_budget" user/scripts/test_lazy_core.py]`.
- **Implementation goal:** prove the defect CLASS is closed (registry membership ⇒ budget, no manual append) and that genuine runaways still trip. Register the new test in the runner list so it executes under `python3 user/scripts/test_lazy_core.py`.
- **Spec requirements:** SPEC `## Reproduction Steps` "Expected" — "the friction detector knows the new sub_skill's commit shape from the same registry the dispatch sites already draw the name from — no separate hand-maintained budget row required."
- **Batch:** 1.

> **GATE SET (HARD — run all four after WU-3; all must be green before flipping the plan to Complete):**
> - `python3 user/scripts/lazy-state.py --test`
> - `python3 user/scripts/bug-state.py --test`
> - `python3 user/scripts/test_lazy_core.py`
> - `python3 user/scripts/lazy_parity_audit.py`
>
> The two `--test` baselines (`tests/baselines/lazy-state-test-baseline.txt`,
> `tests/baselines/bug-state-test-baseline.txt`) MUST be unaffected — this fix changes no
> state-machine route, only the friction-budget source. If either baseline drifts, the change
> leaked beyond branch 3: investigate the leak; do NOT regenerate the baseline by hand.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-3       | Solo      | N/A             |

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** Identify phase(s) whose entry criteria are satisfied. P1 is the head; P2 enters after P1's deliverables are checked. If no phases are ready, jump to Blocking Issue Protocol.
2. **Announce:** Print "Implementing adhoc-derive-cycle-commit-budget Phase N: [title]"
3. **Review prior context:** Re-read all previously completed phases' Implementation Notes in PHASES.md. They take priority over the plan where they diverge.
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "adhoc-derive-cycle-commit-budget Phase N: [title] — committed as [hash]"
7. **Loop:** Re-evaluate which phases are now ready. Return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly. It defines: task tool loading via ToolSearch, task creation for all work units, and the update protocol for tracking progress through test and implementation phases.

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow its instructions. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and the plan file itself. Do NOT rely on cached/remembered content.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — ZERO INLINE IMPLEMENTATION)

**PRE-FLIGHT CHECK:** Confirm (1) you will use the `Agent` tool with `model: "sonnet"` for ALL code changes, (2) you will NOT call `Edit`/`Write` on source/test files — UNLESS the autonomous bug-pipeline inline-override (EXECUTION MODEL block) is active, in which case perform the work INLINE test-first. If neither true nor overridden, re-read the EXECUTION MODEL section.

Read ALL of these before proceeding:
1. `~/.claude/skills/_components/tdd-protocol.md`
2. `~/.claude/skills/_components/subagent-launch.md`
3. `~/.claude/skills/_components/tdd-test-agent.md`
4. `~/.claude/skills/_components/implementation-agent.md`

**TDD ORDER (HARD):** write the failing test FIRST (the WU-3 regression, and the WU-1 membership test), confirm it fails for the right reason, THEN implement WU-1/WU-2 until green.

**POST-DISPATCH GATE:** verify code changes were authored test-first.

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` and follow its complete protocol. Also read `~/.claude/skills/_components/mount-site-verification.md`. Produce a structured review report with a PASS / PASS-WITH-FIXES / NEEDS-REWORK verdict. Confirm the OUT-of-scope branches (`budget_override`, `kind=="meta"`) were left byte-identical.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md`. Check off completed deliverables, add an Implementation Notes block with date, work completed, integration notes, pitfalls, files modified.

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/quality-gates.md`. For this bug the quality gates ARE the four-script gate set in the GATE SET block above. 100% pass required before proceeding.

#### Step B.5: Commit Batch

Read the commit policy: `.claude/skill-config/commit-policy.md` (conventional commits, NO AI attribution, push allowed). Commit + push to `main`.

#### Step B.6: Proceed to Next Batch

**Checklist before proceeding (all must be true):**
- [ ] Review report produced with a verdict
- [ ] PHASES.md updated with completed deliverables and implementation notes
- [ ] All four gate scripts pass
- [ ] Step B.5 completed (commit + push per policy)

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md`. Confirm `detect_cycle_bracket_friction` derives budgets correctly for BOTH pipelines (the shared `lazy_core` helper is exercised by both `lazy-state.py --test` and `bug-state.py --test`).

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md`. The friction-detector machinery is documented in `user/scripts/CLAUDE.md` ("Process-friction detector" / "Cycle-counter advance" context). If the budget-source change warrants a note there, update it (a one-line note that the per-sub_skill budget is now derived from the `lazy_core` multi-commit dispatch-skill registry SSOT, not a hand-maintained literal table).

#### Commit and Push Post-Phase Changes

Read the commit policy and commit + push any post-phase doc changes.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point during execution:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed phases** that haven't been committed yet.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** adhoc-derive-cycle-commit-budget Phase N: [title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining phases (not attempted):**
   - [list]

4. **Do not attempt to work around the blocker.**

Blocking issues include:
- A discovered second consumer of `_CYCLE_COMMIT_BUDGET` outside branch 3 that cannot be cleanly rebuilt from the registry (surface it; do NOT silently delete the table).
- A `--test` baseline drift indicating the change leaked beyond branch 3.
- A quality-gate failure that can't be fixed after 2 retry attempts.
- `lazy_parity_audit.py` going red (the fix is single-landing-site in `lazy_core`; a parity failure means an unintended divergence was introduced).
- Any error that would require architectural decisions beyond the scope of the SPEC.

---

## Completion

When all phases in the Execution Schedule are complete:

1. **Run the full gate set one final time:** `lazy-state.py --test`, `bug-state.py --test`, `test_lazy_core.py`, `lazy_parity_audit.py` — all green.
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Features implemented:** adhoc-derive-cycle-commit-budget
   **Total phases completed:** 2
   **Total commits:** M
   **Final quality-gate status:** all green (four gate scripts)

   **Commit log:**
   | Commit | Feature | Phase | Title |
   |--------|---------|-------|-------|
   | ... | adhoc-derive-cycle-commit-budget | P1 | Derive budget from registry SSOT |
   | ... | adhoc-derive-cycle-commit-budget | P2 | Regression + gates |

   **Implementation Notes summary:**
   [registry SSOT location, the OUT-of-scope branches left intact, and confirmation both pipelines are served by the single `lazy_core` landing site]
