---
kind: implementation-plan
feature_id: budget-guard-defers-near-complete-feature
status: In-progress
created: 2026-06-21
complexity: complex
phases: [1, 2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-21.
> To execute: `/execute-plan <this-file>`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 2. Sibling parts:
> - part 1: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\budget-guard-defers-near-complete-feature\plans\all-phases-budget-guard-defers-near-complete-feature-part-1.md` (this file)
> - part 2: `C:\Users\Jacob\source\repos\claude-config\docs\bugs\budget-guard-defers-near-complete-feature\plans\all-phases-budget-guard-defers-near-complete-feature-part-2.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — budget-guard-defers-near-complete-feature (v2)

**PHASES.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\budget-guard-defers-near-complete-feature\PHASES.md` (budget-guard-defers-near-complete-feature, 4 phases)

**SPEC.md files:**
- `C:\Users\Jacob\source\repos\claude-config\docs\bugs\budget-guard-defers-near-complete-feature\SPEC.md`

**Total phases:** 2 (this part: Phases 1–2 of 4)
**Plan version:** v2 (reference-based — components loaded from disk per step)

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source and test code | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on source or test files. If you are about to modify a `.py` or test file — STOP and compose an `Agent` tool call instead. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

> **INLINE-EXECUTION OVERRIDE (when this plan runs under a `/lazy-bug-batch` cycle subagent):** the dispatched cycle subagent has NO `Agent` tool. In that context perform ALL implementation and test-writing INLINE with `Read`/`Edit`/`Write` yourself, test-first (write the failing test before the implementation for each WU). The orchestrator+subagent table above is the interactive-session contract; the inline override is authoritative when the executing context cannot dispatch sub-subagents. Either way the TDD discipline (failing test first) and the review/QG gates are mandatory.

**Dispatch pattern (interactive only):** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Test Agent Briefing | `~/.claude/skills/_components/tdd-test-agent.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `.claude/skill-config/quality-gates.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

## References — Upstream Artifacts

(none — this bug has no completed hard upstream dependencies; the introducing feature `docs/features/feature-budget-guard-and-skip-ahead/` is the prior-art the SPEC cites, not a hard dep to integrate against.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL implementation and test-writing work goes through Sonnet subagents (interactive) OR inline test-first (under a cycle subagent per the override above)** — the orchestrating interactive session MUST NOT call `Edit`/`Write` on source/test files.
2. All edits happen in the current worktree — NEVER create worktrees for subagents.
3. Every TDD work unit goes through the test-first pipeline — the failing test is written and observed failing BEFORE the implementation.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's output is reviewed for correctness, spec alignment, and TDD discipline before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in a phase finish, integration verification confirms all changes work together.
8. Relevant CLAUDE.md files are created/updated after each phase if changes warrant it (the budget-guard prose in `user/scripts/CLAUDE.md` is owned by part 2, Phase 4 — do not pre-edit it here).
9. Each completed phase is committed and pushed before the next phase begins (work branch: `main`).
10. This plan is self-contained — follow it exactly as written.
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
12. **No `bug-state.py` edits in this part.** The fix is feature-pipeline only (SPEC Proven Finding 2). Touching `bug-state.py` here is out of scope; the parity confirmation lives in part 2.

## Execution Schedule

| Step | Feature(s) | Phase(s) | Title(s) | Blocked by | Parallel? |
|------|-----------|----------|----------|------------|-----------|
| 1 | budget-guard | P1 | Near-completion predicate + corrective-cycle accounting (pure helpers) | — | Solo |
| 2 | budget-guard | P2 | Wire grace gate + corrective discount into the trip site | P1 | Solo |

## Work Units

- [ ] WU-1 — Near-completion predicate (`feature_is_near_complete`)
- [ ] WU-2 — Corrective-cycle counter helpers + marker seed (`count_validation_corrective_cycles`, `record_corrective_cycle`)
- [ ] WU-3 — Composite trip-signal evaluator (`budget_trip_signals`)
- [ ] WU-4 — Wire grace gate + composite signals into the trip site (`lazy-state.py`)
- [ ] WU-5 — Increment the corrective counter at the corrective-dispatch bracket

---

### Phase: budget-guard P1 — Near-completion predicate + corrective-cycle accounting

**Goal:** Land the four pure helpers (and the new marker sub-map seed) that Phase 2 wires into the trip site, each with direct red→green `_TESTS` fixtures.
**Entry criteria:** None (first phase).
**SPEC.md references:** `## Fix Direction` (near-completion grace + composite signal), `## Open Questions` (resolved in PHASES.md scope decisions), `## Affected Area` (Ceiling computation row).

#### WU-1 — Near-completion predicate (`feature_is_near_complete`)
- **Scope:** PHASES.md deliverable "`lazy_core.feature_is_near_complete(feature_dir, repo_root) -> bool`".
- **TDD:** yes.
- **Files to modify:** `user/scripts/lazy_core.py` (add the helper near `remaining_unchecked_are_verification_only` [VERIFY: grep -n "def remaining_unchecked_are_verification_only" user/scripts/lazy_core.py] and the budget helpers at ~10132).
- **Test files:** `user/scripts/test_lazy_core.py` (new fixtures + `_TESTS` registration).
- **Test expectations:** Returns True when PHASES.md has only verification-only unchecked rows (carrying `<!-- verification-only -->` [VERIFY: grep -n "_VERIFICATION_ONLY_MARKER" user/scripts/lazy_core.py]) AND a plan part is `Complete` AND no `BLOCKED.md`; False when an unchecked implementation row exists, when `BLOCKED.md` is present, when no plan part is Complete, and when PHASES.md / plans dir is missing (no raise).
- **Implementation goal:** Reuse `remaining_unchecked_are_verification_only` [VERIFY: grep -n "def remaining_unchecked_are_verification_only" user/scripts/lazy_core.py] for the verification-only check (do NOT re-implement), glob `<feature_dir>/plans/*.md` for a `status: Complete` plan part, and check `(feature_dir / "BLOCKED.md").exists()`. Tolerate every missing input by returning False.
- **Spec requirements:** SPEC `## Open Questions` → "Defining 'near-complete' precisely" — the candidate signal is exactly this predicate.
- **Batch:** 1.

#### WU-2 — Corrective-cycle counter helpers + marker seed
- **Scope:** PHASES.md deliverables "`lazy_core.count_validation_corrective_cycles(...)`" and "`lazy_core.record_corrective_cycle(...)`" + the `write_run_marker` seed.
- **TDD:** yes.
- **Files to modify:** `user/scripts/lazy_core.py` (helpers near `read_per_feature_forward_cycles` [VERIFY: grep -n "def read_per_feature_forward_cycles" user/scripts/lazy_core.py]; seed in `write_run_marker` [VERIFY: grep -n "def write_run_marker" user/scripts/lazy_core.py]).
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** `count_validation_corrective_cycles` returns 0 for a legacy/absent `per_feature_corrective_cycles` map (mirror the `read_per_feature_forward_cycles` legacy-tolerance test [VERIFY: grep -n "per_feature_forward_cycles" user/scripts/test_lazy_core.py]); `record_corrective_cycle` increments `per_feature_corrective_cycles[id]` by 1 per call; `write_run_marker` seeds `per_feature_corrective_cycles: {}` (assert on the in-memory + on-disk marker, parallel to the existing `per_feature_forward_cycles == {}` seed test [VERIFY: grep -n "per_feature_forward_cycles\") == {}" user/scripts/test_lazy_core.py]).
- **Implementation goal:** Add `per_feature_corrective_cycles` as a new run-marker sub-map with the SAME read/legacy-tolerance and seed shape as `per_feature_forward_cycles` [VERIFY: grep -n "per_feature_forward_cycles" user/scripts/lazy_core.py].
- **Spec requirements:** SPEC Theory 2 + `## Fix Direction` scope (don't punish legitimate corrective work) + `## Open Questions` → "Composite-signal shape" option (a).
- **Batch:** 1.

#### WU-3 — Composite trip-signal evaluator (`budget_trip_signals`)
- **Scope:** PHASES.md deliverable "`lazy_core.budget_trip_signals(...) -> dict`".
- **TDD:** yes.
- **Files to modify:** `user/scripts/lazy_core.py` (near `compute_per_feature_ceiling` [VERIFY: grep -n "def compute_per_feature_ceiling" user/scripts/lazy_core.py]).
- **Test files:** `user/scripts/test_lazy_core.py`.
- **Test expectations:** `effective_count = max(0, forward_count - corrective_count)` (clamp at 0 when corrective ≥ forward); `should_defer` True iff `effective_count >= ceiling` AND NOT `near_complete`; `near_complete=True` ⇒ `should_defer=False` even when `effective_count >= ceiling` (grace); a `reason` string distinguishes `near-complete-grace` / `corrective-discount` / `over-ceiling`. Pure: same inputs → identical dict, no marker/clock I/O.
- **Implementation goal:** A pure function returning `{should_defer, effective_count, reason}`. This is the single decision point Phase 2 substitutes for the bare `_bg_count >= _bg_ceiling` comparison.
- **Spec requirements:** SPEC Theory 1 + Theory 2 + `## Fix Direction` (composite signals: forward-cycles + validation-blocks + completion-distance).
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-1, WU-2, WU-3 | Sequential (same files: `lazy_core.py` + `test_lazy_core.py` — one writer at a time) | Yes — serialize |

> **Note:** WU-1/2/3 all edit `lazy_core.py` and `test_lazy_core.py`. Dispatch them as ONE batch but serialize the writes (or hand one subagent all three) to avoid the one-writer-per-file hazard.

---

### Phase: budget-guard P2 — Wire grace gate + corrective discount into the trip site

**Goal:** Substitute the bare ceiling comparison at the budget-guard trip with the composite `budget_trip_signals` decision, grant one-shot grace to a near-complete feature, surface the new signals in probe metadata, and increment the corrective counter at the corrective-dispatch bracket.
**Entry criteria:** Phase 1 complete (all four helpers + the marker seed exist and are green).
**SPEC.md references:** `## Evidence Collected` → "Trip + deferral — `user/scripts/lazy-state.py:1737-1797`"; `## Affected Area` → Trip + deferral row.

#### WU-4 — Wire grace gate + composite signals into the trip site
- **Scope:** PHASES.md Phase 2 deliverables 1–3 (compute `near_complete` + `corrective`, branch on `budget_trip_signals`, one-shot grace, probe metadata).
- **TDD:** yes (via `lazy-state.py --test` fixtures, characterized in `test_lazy_core.py` per repo convention).
- **Files to modify:** `user/scripts/lazy-state.py` (trip site at the bare `_bg_count >= _bg_ceiling` check [VERIFY: grep -n "_bg_count >= _bg_ceiling" user/scripts/lazy-state.py]; `_BUDGET_GUARD` probe dict [VERIFY: grep -n "computed_ceiling" user/scripts/lazy-state.py]).
- **Test files:** `user/scripts/test_lazy_core.py` and/or new `lazy-state.py --test` fixtures; regenerate `tests/baselines/lazy-state-test-baseline.txt` through `_normalize_smoke_output` [VERIFY: grep -n "_normalize_smoke_output" user/scripts/test_lazy_core.py] (never by hand).
- **Test expectations:** d2 reproduction — feature at `forward=ceiling`, verification-only PHASES, plan-Complete, no prior budget defer ⇒ dispatched (grace), NOT appended to `budget_deferred_skipped` [VERIFY: grep -n "budget_deferred_skipped" user/scripts/lazy-state.py]. A near-complete feature that ALREADY has `_bg_deferred_counts[id] >= 1` [VERIFY: grep -n "_bg_deferred_counts" user/scripts/lazy-state.py] IS deferred on the next trip (grace is one-shot). A feature with `corrective=2`, `forward=ceiling+1` dispatches (effective < ceiling). Probe `budget_guard` carries `effective_count` / `corrective_count` / `near_complete_grace_granted`.
- **Implementation goal:** Replace the bare comparison with `lazy_core.budget_trip_signals(_bg_count, corrective, _bg_ceiling, near_complete)["should_defer"]`, where `near_complete = lazy_core.feature_is_near_complete(spec_path, repo_root)` AND grace is gated on `_bg_deferred_counts.get(feature_id, 0) < 1`. Add the three new keys to the `_BUDGET_GUARD` dict [VERIFY: grep -n "_BUDGET_GUARD = {" user/scripts/lazy-state.py] keeping existing keys byte-stable. Add a `_diag` line announcing the grace grant.
- **Spec requirements:** SPEC `## Fix Direction` item 1 (near-completion grace) + the one-grace-cycle bound (PHASES scope decision).
- **Batch:** 1.

#### WU-5 — Increment the corrective counter at the corrective-dispatch bracket
- **Scope:** PHASES.md Phase 2 deliverable 4 (`per_feature_corrective_cycles` increment).
- **TDD:** yes.
- **Files to modify:** `user/scripts/lazy-state.py` (the corrective-dispatch bracket — fold alongside the existing resolution-signal record path [VERIFY: grep -n "record_resolution_signal\|--record-resolution-signal" user/scripts/lazy-state.py]; marker-persist mirror near the existing `_bg_*` persist block [VERIFY: grep -n "_bg_fresh\[.budget_deferred.\]" user/scripts/lazy-state.py]).
- **Test files:** `user/scripts/test_lazy_core.py` (direct fixture on `record_corrective_cycle` wiring is covered by WU-2; this WU adds a `--test` fixture proving the count survives a probe and discounts the trip).
- **Test expectations:** After a validation-failure-driven corrective dispatch is recorded, `per_feature_corrective_cycles[id]` is incremented and persisted; a subsequent probe reads it via `count_validation_corrective_cycles` and discounts it from the trip count. Marker-gated + fail-open (a write error does not break dispatch).
- **Implementation goal:** At the corrective bracket, call `lazy_core.record_corrective_cycle(...)` and persist via the same atomic marker-write pattern as the existing budget-guard persist block [VERIFY: grep -n "_atomic_write" user/scripts/lazy-state.py]. Marker-gated; wrap in the same `(OSError, ValueError)` tolerance as the existing block.
- **Spec requirements:** SPEC Theory 2 (legitimate corrective cycles counted identically to monopolization) — this is the discount's production wiring.
- **Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1 | WU-4, WU-5 | Sequential (both edit `lazy-state.py`) | Yes — serialize |

---

## Execution Protocol

This protocol governs autonomous execution of every phase in this part. Follow it exactly.

### Phase Selection Loop

Repeat until both phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** Identify the lowest-numbered phase whose entry criteria are satisfied. Phase 1 first; Phase 2 only after Phase 1's deliverables are checked off in PHASES.md.
2. **Announce:** Print "Implementing budget-guard Phase N: [title]".
3. **Review prior context:** Re-read previously completed phases' Implementation Notes in PHASES.md.
4. **Execute all batches** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "budget-guard Phase N: [title] — committed".
7. **Loop:** Return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly (ToolSearch for task tools, create tasks for all WUs, update protocol).

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)
Read `~/.claude/skills/_components/source-reread.md`. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), and this plan file. Also re-read `user/scripts/CLAUDE.md` budget-guard sections and the live trip-site code before editing.

#### Step B.1: Implement the Batch (test-first)
Read: `~/.claude/skills/_components/tdd-protocol.md`, `~/.claude/skills/_components/subagent-launch.md`, `~/.claude/skills/_components/tdd-test-agent.md`, `~/.claude/skills/_components/implementation-agent.md`.
For each WU: write the FAILING test first, observe it fail, then implement until green. Under a `/lazy-bug-batch` cycle subagent (no `Agent` tool), do this INLINE per the EXECUTION MODEL override. Register every new `def test_*` in `_TESTS` (the dead-coverage guard `test_no_orphaned_test_functions` [VERIFY: grep -n "test_no_orphaned_test_functions" user/scripts/test_lazy_core.py] fails any unregistered test).

#### Step B.2: Review Batch Output (MANDATORY GATE)
Read `~/.claude/skills/_components/subagent-review.md` and produce a structured review report with a PASS / PASS-WITH-FIXES / NEEDS-REWORK verdict before proceeding.

#### Step B.3: Update PHASES.md (MANDATORY)
Read `~/.claude/skills/_components/phases-update.md`. Check off completed deliverables for the phase, add an Implementation Notes block (date, work completed, files modified, pitfalls). Do NOT tick verification-only rows (the `<!-- verification-only -->` rows) — those are gate-owned.

#### Step B.4: Run Quality Gates (MANDATORY)
Read `.claude/skill-config/quality-gates.md`. For Python-script changes run: `python -m pytest user/scripts/ -q` AND `python user/scripts/lazy-state.py --test` AND `python user/scripts/bug-state.py --test` (shared `lazy_core` — confirm no bug-side regression) AND `python user/scripts/lazy_parity_audit.py --report`. 100% pass required — no "preexisting" triage.

#### Step B.5: Commit Batch
Read `.claude/skill-config/commit-policy.md` and follow it. Work branch is `main`; re-run `git rev-parse --abbrev-ref HEAD` and confirm `main` immediately before each commit/push.

#### Step B.6: Proceed to Next Batch
Checklist (all must be true): review report with verdict; PHASES.md updated; all quality gates pass; commit done. If any unchecked, complete it first.

### Post-Phase Steps (after all batches in a phase)

#### Integration Verification (MANDATORY)
Read `~/.claude/skills/_components/integration-verification.md`. Confirm the helpers integrate (Phase 2's trip site actually calls the Phase 1 helpers; the probe metadata is well-formed JSON).

#### Update CLAUDE.md Files
Read `~/.claude/skills/_components/claude-md-review.md`. NOTE: the budget-guard prose in `user/scripts/CLAUDE.md` is owned by part 2 (Phase 4). Do NOT pre-edit it here — only update a CLAUDE.md if THIS part introduced something Phase 4 will not cover.

#### Commit and Push Post-Phase Changes
Per `.claude/skill-config/commit-policy.md` (branch `main`).

## Blocking Issue Protocol

If a blocking issue is encountered:
1. Stop all in-progress work. Do not launch new subagents.
2. Commit and push any completed phases.
3. Write `BLOCKED.md` (canonical name) per `~/.claude/skills/_components/sentinel-frontmatter.md` and print a blocking-issue report (completed phases, blocked phase, reason, recovery suggestion, remaining phases).
4. Do not work around the blocker.

Blocking issues include: a quality-gate failure unfixable after 2 attempts; a parity-audit drift that demands a `bug-state.py` mirror the SPEC says is not owed (a genuine design fork — halt for human); any error requiring architectural decisions beyond the specs.

## Completion

When both phases in this part are complete:
1. Run the full quality-gate suite one final time (`pytest user/scripts/ -q` + both `--test` harnesses + `lazy_parity_audit.py --report`).
2. Print a completion report (phases completed, commits, final QG status: all green).
3. Proceed to part 2 (`/execute-plan` the part-2 file).

Plan written to: C:\Users\Jacob\source\repos\claude-config\docs\bugs\budget-guard-defers-near-complete-feature\plans\all-phases-budget-guard-defers-near-complete-feature-part-1.md

```
/execute-plan C:\Users\Jacob\source\repos\claude-config\docs\bugs\budget-guard-defers-near-complete-feature\plans\all-phases-budget-guard-defers-near-complete-feature-part-1.md
```
