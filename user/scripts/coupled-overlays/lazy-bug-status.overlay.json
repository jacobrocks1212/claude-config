{
  "schema_version": 1,
  "canonical": "user/skills/lazy-status/SKILL.md",
  "derived": "user/skills/lazy-bug-status/SKILL.md",
  "generator": "generate-coupled-skills.py",
  "note": "GENERATED build input for the coupled-pair generator. Token substitutions live in lazy-parity-manifest.json (single source). 'canonical' directives restate the canonical block via those subs (edit the canonical to change them); 'verbatim' directives are authored divergences/inserts for THIS variant (edit here, then re-run generate-coupled-skills.py --write). Re-extract with --extract after a byte-faithful bootstrap.",
  "directives": [
    {
      "op": "verbatim",
      "heading": "__preamble__",
      "lines": [
        "---\r",
        "name: lazy-bug-status\r",
        "description: Read-only bug-pipeline dashboard — runs bug-state.py and formats queue progress, next action, blockers. NO mutations. Drives docs/bugs/.\r",
        "argument-hint: [optional: \"--cloud\" to use the cloud state machine]\r",
        "model: haiku\r",
        "plan-mode: never\r",
        "allowed-tools: [\"Bash\", \"Read\"]\r",
        "---\r",
        "\r",
        "> **Parity note:** before editing this skill, run `python3 user/scripts/lazy_parity_audit.py --repo-root . --pair lazy-bug-status` to confirm parity with its canonical twin is clean, and run `pytest user/scripts/test_lazy_parity.py` after to confirm your change introduces no drift. Intentional divergences are recorded in `user/scripts/lazy-parity-manifest.json` (the source of truth).\r",
        "\r",
        "# Lazy Bug Status\r",
        "\r",
        "Read-only dashboard for the autonomous bug pipeline. Runs `~/.claude/scripts/bug-state.py`,\r",
        "parses its JSON output, and formats a compact status report. **This skill is pure presentation\r",
        "— all state inference lives in the script.**\r",
        "\r",
        "State-machine logic lives exclusively in `bug-state.py`. If the dashboard is wrong, fix the\r",
        "script — do not duplicate the state machine here.\r",
        "\r",
        "---\r",
        "\r",
        ""
      ]
    },
    {
      "op": "verbatim",
      "heading": "## Step 0: Parse Arguments",
      "lines": [
        "## Step 0: Parse Arguments\r",
        "\r",
        "If `$ARGUMENTS` contains `--cloud`, pass `--cloud` to the state script. Otherwise run without it.\r",
        "\r",
        "The cloud variant changes nothing about this skill's behavior beyond which sub-skill the script\r",
        "picks for Step 9 (workstation: `mcp-test`; cloud: `__write_deferred_non_cloud__`) and how it\r",
        "handles cloud-saturated bugs at completion.\r",
        "\r",
        "---\r",
        "\r",
        ""
      ]
    },
    {
      "op": "verbatim",
      "heading": "## Step 1: Run bug-state.py",
      "lines": [
        "## Step 1: Run bug-state.py\r",
        "\r",
        "```bash\r",
        "python3 ~/.claude/scripts/bug-state.py [--cloud]\r",
        "```\r",
        "\r",
        "Capture stdout (one JSON object). If the script exits non-zero, print its error verbatim and\r",
        "STOP — do not try to format malformed state.\r",
        "\r",
        "Parse the JSON:\r",
        "- `feature_id`, `feature_name`, `spec_path` — current-bug context (null when there is no\r",
        "  current bug)\r",
        "- `current_step` — the state script's human-readable position\r",
        "- `sub_skill`, `sub_skill_args` — what /lazy-bug would dispatch next\r",
        "- `terminal_reason`, `notify_message` — set when the pipeline halts\r",
        "- `device_deferred_features` — bugs the device axis skipped this probe (each has\r",
        "  `DEFERRED_REQUIRES_DEVICE.md` + no `VALIDATED.md` on a no-real-device host). Always present;\r",
        "  non-empty means lingering In-progress deferrals awaiting a real-device host.\r",
        "\r",
        "---\r",
        "\r",
        ""
      ]
    },
    {
      "op": "verbatim",
      "heading": "## Step 2: Gather Light Additional Context",
      "lines": [
        "## Step 2: Gather Light Additional Context\r",
        "\r",
        "These are presentation-only enrichments — they do NOT influence state. Run in parallel:\r",
        "\r",
        "1. `git log --oneline -1` → last commit (hash + message).\r",
        "2. If `feature_name` is set: count `### Phase` headings in `{spec_path}/PHASES.md` (if it exists)\r",
        "   and count how many phases have ALL deliverables checked (`- [x]`) → produces\r",
        "   \"Phase {current}/{total}\".\r",
        "3. If `feature_name` is set: list `{spec_path}/mcp-tests/` entries if the directory exists.\r",
        "4. If `feature_name` is set AND `{spec_path}/BLOCKED.md` exists: parse its YAML frontmatter per\r",
        "   `~/.claude/skills/_components/sentinel-frontmatter.md` and grab the `phase` and\r",
        "   `recovery_suggestion` fields.\r",
        "5. Open / archived bug counts. `docs/bugs/queue.json` is OPTIONAL (on-disk bugs are\r",
        "   auto-discovered), so:\r",
        "   - If `docs/bugs/queue.json` exists: total queue length from it.\r",
        "   - Otherwise (the common case): enumerate on-disk bug directories — count open bugs as\r",
        "     immediate subdirectories of `docs/bugs/` that contain a `SPEC.md` and are NOT under\r",
        "     `_archive/`.\r",
        "   - Archived count either way: count `FIXED.md` files under `docs/bugs/_archive/`.\r",
        "\r",
        "If any of these reads fail (file missing, parse error), continue with the field set to `\"—\"`.\r",
        "Do not error out — this skill is a dashboard.\r",
        "\r",
        "---\r",
        "\r",
        ""
      ]
    },
    {
      "op": "verbatim",
      "heading": "## Step 3: Map sub_skill to a Human-Readable Next Action",
      "lines": [
        "## Step 3: Map sub_skill to a Human-Readable Next Action\r",
        "\r",
        "The state script emits both real sub-skill names and pseudo-skills (prefixed `__`). Translate:\r",
        "\r",
        "| `sub_skill` from script | \"Next /lazy-bug action\" |\r",
        "|-------------------------|------------------------|\r",
        "| `null` + `terminal_reason` set | halt — see Terminal column below |\r",
        "| `spec-bug` | /spec-bug — root-cause investigation |\r",
        "| `spec-phases` | /spec-phases — decompose bug into phases |\r",
        "| `write-plan` | /write-plan — write the implementation plan |\r",
        "| `execute-plan` | /execute-plan — run the next plan |\r",
        "| `mcp-test` | /mcp-test — validate via MCP |\r",
        "| `__write_deferred_non_cloud__` | DEFER MCP test (cloud) → phases complete routes directly to the Step 9 MCP gate on the next cycle (retro is unwired) |\r",
        "| `__write_validated_from_skip__` | promote SKIP_MCP_TEST.md → VALIDATED.md |\r",
        "| `__write_validated_from_results__` | promote MCP_TEST_RESULTS.md → VALIDATED.md |\r",
        "| `__mark_fixed__` | mark bug fixed, write FIXED.md receipt, archive to _archive/ |\r",
        "| `__flip_plan_complete_cloud_saturated__` | mark plan part Complete (cloud-saturated) |\r",
        "\r",
        "Terminal-reason mapping:\r",
        "\r",
        "| `terminal_reason` | Status line |\r",
        "|---|---|\r",
        "| `blocked` | \"BLOCKED — see {spec_path}/BLOCKED.md\" |\r",
        "| `needs-input` | \"Awaiting human decision — see {spec_path}/NEEDS_INPUT.md\" |\r",
        "| `completion-unverified` | \"⚠ {bug} marks Fixed with no FIXED.md receipt — flipped outside the gate. Reconcile: reopen to In-progress, or `bug-state.py --backfill-receipts` to grandfather.\" |\r",
        "| `stale_upstream` | \"STALE UPSTREAM — an upstream item changed since materialize (see {spec_path}/STALE_UPSTREAM.md). Re-materialize / realign, or reject.\" |\r",
        "| `all-bugs-fixed` | \"ALL BUGS FIXED — nothing left in queue\" |\r",
        "| `all-remaining-deferred` | \"ALL REMAINING DEFERRED — every open bug is operator-parked via DEFERRED.md. Delete a bug's DEFERRED.md to re-include it.\" |\r",
        "| `cloud-queue-exhausted` | \"Cloud queue exhausted — run /lazy-bug on workstation for MCP testing\" |\r",
        "| `device-queue-exhausted` | \"Device queue exhausted — remaining bug(s) have real-device-only assertions deferred via DEFERRED_REQUIRES_DEVICE.md. Re-run /lazy-bug on a real-device host (ALGOBOOTH_REAL_AUDIO_DEVICE=1 or native hardware) to certify them.\" |\r",
        "| `queue-missing` | \"docs/bugs/queue.json not found (optional — on-disk bugs auto-discovered; create it for explicit ordering)\" |\r",
        "\r",
        "---\r",
        "\r",
        ""
      ]
    },
    {
      "op": "canonical",
      "heading": "## Step 4: Format and Output"
    },
    {
      "op": "verbatim",
      "heading": "## Bug Pipeline Status{ (cloud)}",
      "lines": [
        "## Bug Pipeline Status{ (cloud)}\r",
        "\r",
        "**Current:** {bug_name | \"—\"} (Phase {current}/{total} — {state})\r",
        "**State step:** {current_step | \"—\"}\r",
        "**Queue:** {archived}/{total queue length} bugs fixed ({remaining} remaining open)\r",
        "**Last commit:** {short hash} \"{commit message}\"\r",
        "**Blockers:** {None | \"<BLOCKED phase>: <recovery_suggestion>\"}\r",
        "**MCP Tests:** {count} scenarios linked | Not yet created | Skipped (see SKIP_MCP_TEST.md) | Deferred (cloud) | Deferred (real-device — see DEFERRED_REQUIRES_DEVICE.md)\r",
        "**Device-deferred:** {None | comma-separated `device_deferred_features`} — bugs awaiting a real-device host\r",
        "**Next /lazy-bug action:** {mapped action from Step 3}\r",
        "```\r",
        "\r",
        "Notes:\r",
        "\r",
        "- If `feature_name` is null (terminal queue state), drop the **Current**, **Blockers**, and\r",
        "  **MCP Tests** lines and surface just **State step**, **Queue**, **Last commit**, and **Next\r",
        "  /lazy-bug action** — the latter will be the terminal status line.\r",
        "- When `terminal_reason` is `blocked`, the **Blockers** line takes the BLOCKED.md `phase` +\r",
        "  `recovery_suggestion` from Step 2.4.\r",
        "- The \"Phase {current}/{total}\" annotation is best-effort; if PHASES.md doesn't exist yet,\r",
        "  write `\"—/— ({state})\"`.\r",
        "- The \"Deferred (cloud)\" / \"Skipped\" labels for MCP Tests apply when `$ARGUMENTS` contains\r",
        "  `--cloud` AND the spec dir has `DEFERRED_NON_CLOUD.md` / `SKIP_MCP_TEST.md` respectively.\r",
        "- The \"Deferred (real-device)\" label applies when the spec dir has\r",
        "  `DEFERRED_REQUIRES_DEVICE.md` — real-device-only assertions deferred on the device axis.\r",
        "- The **Device-deferred** line lists `device_deferred_features` from the state JSON (bugs the\r",
        "  QUEUE skipped past this probe). Drop the line when the list is empty.\r",
        "- The **Queue** counts bugs fixed/archived. If `queue.json` is absent, derive the total from\r",
        "  on-disk open bug directories (the script auto-discovers them).\r",
        "\r",
        "**Do NOT execute any skills or modify any files. Report only.**\r",
        "\r",
        "---\r",
        "\r",
        ""
      ]
    },
    {
      "op": "verbatim",
      "heading": "## Notes",
      "lines": [
        "## Notes\r",
        "\r",
        "- This skill replaces ad-hoc sentinel-parsing with a single `bug-state.py` invocation. Do not\r",
        "  re-introduce sentinel-parsing logic here; that lives in the script.\r",
        "- For human-only \"what's next?\" checks, prefer this over `/lazy-bug` itself — `/lazy-bug` will\r",
        "  dispatch a sub-skill, `/lazy-bug-status` just reports.\r",
        "- The script is project-agnostic for the `docs/bugs/` layout; this skill works in any repo with\r",
        "  a `docs/bugs/` directory containing `SPEC.md` files (queue.json is optional).\r",
        "- For the equivalent feature pipeline dashboard, use `/lazy-status`.\r",
        ""
      ]
    }
  ]
}
