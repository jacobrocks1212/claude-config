# Python bytecode generated when running the lazy-state / bug-state / lazy_core scripts
__pycache__/
*.pyc

# Machine-specific wrapper generated by `ado-sync.py --install-task` (absolute paths)
_ado-mirror-sync.cmd
