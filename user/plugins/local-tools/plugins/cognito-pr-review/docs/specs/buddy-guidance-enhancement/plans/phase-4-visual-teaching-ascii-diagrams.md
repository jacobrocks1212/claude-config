---
kind: implementation-plan
feature_id: buddy-guidance-enhancement
status: Complete
created: 2026-06-15
complexity: complex
phases: [4]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-15.
> To execute: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review\docs\specs\buddy-guidance-enhancement\plans\phase-4-visual-teaching-ascii-diagrams.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — buddy-guidance-enhancement Phase 4: Visual teaching (ASCII diagrams in the Orient step) (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review\docs\specs\buddy-guidance-enhancement\PHASES.md` (buddy-guidance-enhancement, Phase 4)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review\docs\specs\buddy-guidance-enhancement\SPEC.md`

**Total phases:** 1 (Phase 4)
**Plan version:** v2 (reference-based — components loaded from disk per step)

---

## FEATURE-SPECIFIC EXECUTION NOTE — READ BEFORE ANYTHING ELSE

This feature is **prompt-file-only**. PHASES.md declares `**MCP runtime:** not-required` — there is no app integration, build step, compiled code, or MCP-reachable runtime surface. The only artifacts edited are **Claude Code agent/command markdown prompts** in the `cognito-pr-review` plugin. Consequences that override the generic protocol below:

- **No TDD.** There is no automated test harness for prompt files (confirmed across Phases 1–3). Both work units are `TDD: no`. Do NOT attempt to write `.test.ts`/unit tests; do NOT search for a test runner.
- **Quality gates = grep consistency checks + a rendering lint**, not `dotnet`/`nx`/`vitest`. See Step B.4 below for the exact commands. There is no compiler to satisfy.
- **Verification of correctness is a manual buddy walk-through** (the PHASES.md "Runtime Verification" rows). Those rows are NOT closed by this plan — they are Jacob's manual acceptance. Do not check them off.
- **Edit the real files under `claude-config`** at the absolute paths given in each work unit. These are the source-of-truth paths (the live `~/.claude/plugins/...` locations are symlinks into this repo; the Edit tool refuses to write through symlinks).
- **Drift guard (carried from Phases 2–3 — HARD):** do NOT modify `agents/synthesizer-v2.md`. Do NOT introduce any new `### Suggestion` section or any new severity vocabulary. The README edit is prose-only.

The repo for all paths in this plan is:
`C:\Users\JacobMadsen\source\repos\claude-config` (git identity is **personal** for this repo — pushing is allowed; it is NOT a Cognito Forms work repo, so the work-repo push hook does not apply).

The plugin directory (root for `commands/`, `agents/`, `README.md`) is:
`C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review`

---

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run consistency-check gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL prompt-file edits (the markdown prompt content) | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on the prompt files (`commands/review-pr-buddy.md`, `README.md`). Delegate those edits to a Sonnet subagent via the `Agent` tool. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

> Note: although the generic constraint enumerates code extensions (`.ts`, `.cs`, `.vue`, …), the markdown prompt files in this feature are likewise delegated to subagents — they are the deliverable artifacts, not orchestrator scratch docs. PHASES.md/CLAUDE.md remain the only orchestrator-editable files.

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

> TDD components (`tdd-protocol.md`, `tdd-test-agent.md`) and the MCP integration test component are intentionally **omitted** — this is a no-test prompt-file feature (see the feature-specific note above). `quality-gates.md` is replaced by the grep/rendering checks in Step B.4.

## References — Upstream Artifacts

(none — the sole dependency is `cognito-pr-review-v2 — composes`, which is not a `hard` dep, so there is no settled upstream contract to reality-check.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL prompt-file edits MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit`/`Write` on `review-pr-buddy.md` or `README.md`. The ONLY exception: a trivial PASS-WITH-FIXES touch-up (a few lines).
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. **No TDD** — this is a prompt-file feature with no automated harness. Both WUs are `TDD: no`.
4. PHASES.md is updated AFTER the batch completes (not deferred).
5. Every subagent's output is reviewed for correctness, spec alignment, and drift-guard compliance before continuing.
6. Mistakes are fixed immediately.
7. After the batch finishes, integration verification confirms the two files are mutually consistent.
8. Relevant CLAUDE.md files are reviewed/updated after the phase if changes warrant it (none expected).
9. The completed phase is committed (and pushed — personal repo) before reporting done.
10. This plan is self-contained — follow it exactly as written without relying on external context.
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
12. **Drift guard (HARD):** never modify `agents/synthesizer-v2.md`; never add a `### Suggestion` section or new severity vocabulary.

---

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1    | buddy-guidance-enhancement | P4 | Visual teaching — ASCII diagrams in the Orient step | — (Phases 1–3 complete) | Batch 1: WU-1 ∥ WU-2 |

---

## Work Units

- [x] WU-1 — ASCII-diagram capability in the buddy Orient step (`review-pr-buddy.md`)
- [x] WU-2 — README Buddy-Review doc: mention visual teaching

---

### Phase: buddy-guidance-enhancement P4 — Visual teaching (ASCII diagrams in the Orient step)

**Goal:** For `non-trivial` chunks, the buddy's Orient teach additionally renders a compact, grounded ASCII diagram (data-flow / component / sequence) of the chunk's behavioral thread; trivial chunks render none, with diagrams available on demand; the README documents this.

**Entry criteria:** Phases 1–3 complete (the two-pass loop + complexity-gated Orient teach the diagram extends; the README Buddy-Review section Phase 3 rewrote). All satisfied — verified in PHASES.md Implementation Notes.

**SPEC.md references:** §2 (Guiding-loop — Orient step, Decision 2 teach-by-complexity); §5 (AI-role scoping — diagrams are a facilitation/orientation aid, not the arbiter of correctness). The diagram capability is a net-new extension layered onto §2's Orient step; it does not change the disposition model (§3/§4) or the synthesizer mapping.

**Batch overview:**

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2 | Yes       | None (different files) |

---

#### WU-1 — ASCII-diagram capability in the buddy Orient step

**Scope (PHASES.md deliverables 4a, 4b, 4c, 4d):**
- [x] 4a — Diagram in the Orient teach (non-trivial only); reuse the existing complexity gate verbatim; trivial chunks render no diagram.
- [x] 4b — Diagram-type selection guidance: data-flow (default for a cross-layer thread) / component-dependency / sequence-control-flow; one is the norm, more allowed when warranted.
- [x] 4c — On-demand diagrams for trivial chunks / richer diagrams on any chunk, wired into the existing "Ask to dig deeper" carve-out (no new command verb).
- [x] 4d — Grounding + rendering constraints: derived from cached diff + structural context, reflect actual changed components/edges (no invented architecture); ASCII/box-drawing ONLY (no Mermaid, no image links, no HTML); compact (fit a terminal pane); label nodes with the chunk's real `Files` names.

> These four deliverables are merged into one work unit: they are tightly coupled (all add one cohesive "diagram" capability), and 4a/4b/4c/4d all edit the **same file** in adjacent regions — splitting them would force sequential same-file batches with no benefit.

**TDD:** no (prompt-file edit; no automated harness).

**Files to create/modify:**
- `C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review\commands\review-pr-buddy.md` — extend **§1 Orient** [VERIFY: `grep -n "#### 1. Orient" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/commands/review-pr-buddy.md"`] (currently the senior-architect teach paragraph) and the **§Interruption Handling "Ask to dig deeper"** bullet [VERIFY: `grep -n "Ask to dig deeper" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/commands/review-pr-buddy.md"`].

**Test files:** none.

**Implementation goal:**
1. In **§1 Orient**, after the existing complexity-gated teach instruction, add that for a `non-trivial` chunk (or missing/ambiguous `Complexity`, treated as `non-trivial`) the teach renders **at least one compact ASCII diagram** of the chunk's behavioral thread alongside the prose. Reuse the EXISTING complexity gate — do NOT introduce a parallel complexity branch; `trivial` chunks render no diagram (the one-liner remains the whole orientation).
2. Add **diagram-type selection guidance** (4b): pick the diagram fitting the thread's shape — **data-flow** (value/request across layers; the default for a cross-layer behavioral thread), **component/dependency** (which components the thread touches + how they relate), or **sequence/control-flow** (ordered steps for stateful/async logic). One is the norm; more than one only when the thread genuinely warrants it.
3. Add **grounding + rendering constraints** (4d), mirroring the existing "grounded in the cached diff … not a dump of the raw diff" rule: diagrams are derived from the cached diff + structural context and must reflect the *actual* changed components/edges (no invented or generic architecture); **ASCII / box-drawing characters only — no Mermaid fences, no image links, no HTML**; keep them compact enough to fit a terminal pane (favor a focused thread view over a whole-system map); label nodes with the real file/type/layer names drawn from the chunk's `**Files:**` list.
4. In **§Interruption Handling**, extend the existing "Ask to dig deeper" carve-out (4c) so a reviewer may request a diagram on a `trivial` chunk, or a richer/alternative diagram on any chunk, answered from cached context + architectural knowledge — **riding the existing deeper-dig path; do NOT add a new command verb**.
5. Keep the AI-role framing intact: the diagram is a facilitation/orientation aid (§5), not a claim about business-logic correctness.

**Spec requirements:** SPEC §2 Orient (Decision 2 teach-by-complexity — diagrams inherit the same gate); §5 AI-role scoping (facilitation aid, reviewer remains sole arbiter). PHASES.md Phase 4 deliverables 4a–4d and "Context from prior phases" (reuse the gate; grounding mirrors the existing teach rule; drift guard).

**Drift guard (HARD):** do NOT touch `agents/synthesizer-v2.md`; introduce no `### Suggestion` section or new severity vocabulary. This WU touches only `review-pr-buddy.md`.

**Batch:** 1.

---

#### WU-2 — README Buddy-Review doc: mention visual teaching

**Scope (PHASES.md deliverable 4e):**
- [x] 4e — Update the `### Buddy Review` section of `README.md` (the Phase-1 paragraph Phase 3 rewrote) to mention that non-trivial chunks are taught with ASCII data-flow/component diagrams. Prose only; introduce no new synthesizer section or severity vocabulary.

**TDD:** no (documentation edit).

**Files to create/modify:**
- `C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review\README.md` — the `### Buddy Review` section [VERIFY: `grep -n "### Buddy Review" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/README.md"`], specifically the **Phase 1 — Interactive walk** paragraph [VERIFY: `grep -n "Phase 1 — Interactive walk" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/README.md"`].

**Test files:** none.

**Implementation goal:** In the existing "Phase 1 — Interactive walk" paragraph, where it describes the non-trivial-chunk senior-architect teach scaled to complexity, add a brief clause noting that non-trivial chunks are additionally taught with a compact ASCII data-flow / component diagram of the chunk's behavioral thread (and that diagrams are available on demand for any chunk). Keep it to one or two added clauses — prose only, consistent with the existing paragraph's voice. Do NOT add a new heading, a new synthesizer section, or any new severity vocabulary.

**Spec requirements:** PHASES.md deliverable 4e; Phase 3 Implementation Notes (README is prose-only, no synthesizer section).

**Drift guard (HARD):** README mentions the diagram capability in prose only; no `### Suggestion` heading, no new severity terms, no edit to `synthesizer-v2.md`. This WU touches only `README.md`.

**Batch:** 1.

---

## Execution Protocol

This protocol governs the autonomous execution of the phase. Follow it exactly.

### Phase Selection Loop

There is one phase. Execute it, then run Post-Phase Steps, then Completion.

1. **Announce:** Print "Implementing buddy-guidance-enhancement Phase 4: Visual teaching — ASCII diagrams in the Orient step".
2. **Review prior context:** Re-read PHASES.md Phases 1–3 Implementation Notes (field contract, the complexity gate, the drift guards) and SPEC §2/§5. They take priority where they diverge from this plan.
3. **Execute Batch 1** per the Per-Batch Steps below.
4. **Run Post-Phase Steps.**
5. **Report:** Print "buddy-guidance-enhancement Phase 4 — committed as [hash]".

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)

Read `~/.claude/skills/_components/task-tracking.md` and follow it. Create tasks for WU-1 and WU-2 and track them through dispatch → review.

### Per-Batch Steps

For Batch 1 (WU-1 and WU-2 run in parallel — different files, no conflict):

#### Step B.0: Re-read Source Documents (MANDATORY)

Read `~/.claude/skills/_components/source-reread.md` and follow it. Re-read from disk: PHASES.md (Phase 4 + prior Implementation Notes), SPEC.md (§2, §5), and this plan file. Also `Read` the current `commands/review-pr-buddy.md` §1 Orient + §Interruption Handling and the `README.md` `### Buddy Review` section so subagent prompts quote the real current text.

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS — ZERO INLINE EDITS)

**PRE-FLIGHT CHECK:** All prompt-file edits go to Sonnet subagents via the `Agent` tool (`model: "sonnet"`); you will NOT `Edit`/`Write` the prompt files yourself.

Read before proceeding:
1. `~/.claude/skills/_components/subagent-launch.md` — launch orchestration + failed-agent recovery.
2. `~/.claude/skills/_components/implementation-agent.md` — impl-agent prompt template; include its briefing verbatim in each agent's prompt.

> Ignore `subagent-launch.md`'s internal directives to load the TDD/test-agent components — this feature has no TDD. Dispatch WU-1 and WU-2 as two **direct-implementation** subagents in a single message (parallel; different files).

Each subagent prompt MUST be fully self-contained and MUST include:
- The exact absolute file path it edits and the current text of the target region (paste it).
- Its WU scope/implementation goal verbatim from above.
- The **drift guard**: do NOT touch `agents/synthesizer-v2.md`; no `### Suggestion` section; no new severity vocabulary.
- The rendering constraint (WU-1): ASCII/box-drawing only — no Mermaid, no image links, no HTML — and a worked example is encouraged but it must be illustrative ASCII (e.g. a small `Controller → Service → Storage` data-flow box chain) and clearly framed as a template, not a literal output.
- Instruction to make the **minimal** edit that satisfies the goal — do not restructure surrounding sections, do not renumber the six-step loop, do not alter the field-label contract or disposition model.

**POST-DISPATCH GATE:** Confirm you composed `Agent` calls and did NOT edit the prompt files directly. If violated, revert and re-dispatch.

#### Step B.2: Review Batch Output (MANDATORY GATE)

Read `~/.claude/skills/_components/subagent-review.md` and follow its protocol; also read `~/.claude/skills/_components/mount-site-verification.md` for the placement check. Produce a structured review with a PASS / PASS-WITH-FIXES / NEEDS-REWORK verdict. Review MUST confirm:
- WU-1: §1 Orient now renders a diagram for non-trivial chunks only, reusing the existing complexity gate (no parallel branch); diagram-type guidance present; grounding + ASCII-only/compact/real-node-label constraints present; "Ask to dig deeper" extended for on-demand diagrams with no new verb.
- WU-2: README Phase-1 paragraph mentions ASCII diagram teaching in prose; no new heading/section/severity term.
- **Drift guard held:** `git diff --name-only` lists ONLY `commands/review-pr-buddy.md` and `README.md` (NOT `synthesizer-v2.md`); `grep -c "### Suggestion"` across both files = 0; no Mermaid/```mermaid/`<img`/`<svg`/HTML tags introduced.

#### Step B.3: Update PHASES.md (MANDATORY)

Read `~/.claude/skills/_components/phases-update.md` and follow it. Check off deliverables 4a–4e under Phase 4. Add an "Implementation Notes (Phase 4)" block: completed date, work completed, the drift-guard result, files modified, and the grep-consistency results from Step B.4. **Do NOT check the "Runtime Verification" rows** — those are Jacob's manual buddy-walk acceptance. **Do NOT flip SPEC.md `**Status:**`** — that is gate-owned.

#### Step B.4: Run Quality Gates (consistency checks — there is no compiler/test runner)

From the plugin directory (`C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review`), run and require all to pass:

1. **Drift guard — no synthesizer edit:** `git -C "C:/Users/JacobMadsen/source/repos/claude-config" diff --name-only` must NOT list `agents/synthesizer-v2.md`.
2. **No new Suggestion section:** `grep -rc "### Suggestion" commands/review-pr-buddy.md README.md` → both `0`.
3. **No non-ASCII rendering constructs introduced** in the diagram additions: `grep -nEi "mermaid|<img|<svg|<table|\\]\\(.*\\.(png|svg|jpg|gif)\\)" commands/review-pr-buddy.md` → no hits inside the new diagram region (a pre-existing unrelated hit elsewhere is fine; inspect each).
4. **Capability present:** `grep -nic "diagram" commands/review-pr-buddy.md` ≥ 1 and the README Phase-1 paragraph contains a diagram mention (`grep -ni "diagram" README.md`).
5. **Gate-reuse check (no parallel complexity branch):** confirm by inspection that the diagram instruction sits inside / references the existing `non-trivial` Orient gate, not a newly-added complexity conditional.

Record the counts in the PHASES.md Implementation Notes (mirroring how Phases 1–3 recorded their grep-consistency checks).

#### Step B.4.5: MCP Integration Test

**Skip** — `MCP runtime: not-required`; there is no runtime surface. Note the skip.

#### Step B.5: Commit Batch

Read the commit policy: try `.claude/skill-config/commit-policy.md` in the project root first; if absent, read `~/.claude/skills/_components/commit-and-push.md`. This repo (`claude-config`) commits as the **personal** identity and **push is allowed** (not a Cognito Forms work repo). Stage ONLY the four touched files (`commands/review-pr-buddy.md`, `README.md`, `PHASES.md`, and — already modified — `SPEC.md`). Suggested message: `feat(buddy-guidance-enhancement): Phase 4 — ASCII diagrams in the buddy Orient teach`.

#### Step B.6: Proceed

**Checklist (all true before finishing the batch):**
- [ ] Review report produced with a verdict.
- [ ] PHASES.md deliverables 4a–4e checked + Implementation Notes added (Runtime-Verification rows left unchecked).
- [ ] All Step B.4 consistency checks pass.
- [ ] Batch committed (and pushed) per policy.

### Post-Phase Steps

#### Integration Verification (MANDATORY)

Read `~/.claude/skills/_components/integration-verification.md` and follow it. For this feature, integration verification = confirm the two edited files are mutually consistent and consistent with the unchanged pipeline: the README's prose description of diagram teaching matches the behavior `review-pr-buddy.md` §1 now specifies; the diagram capability is gated by the same `Complexity` field Phase 1 emits and Phase 2 consumes; nothing references a synthesizer section or severity term that does not exist.

#### Update CLAUDE.md Files

Read `~/.claude/skills/_components/claude-md-review.md` and follow it. No CLAUDE.md change is expected (no new build/convention) — note the no-op unless something surfaces.

#### Commit and Push Post-Phase Changes

If integration verification produced any fixes, commit + push them per the commit policy (personal repo — push allowed).

---

## Blocking Issue Protocol

If a blocking issue is encountered:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed work** not yet committed.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed work:** [WUs done, with commit hash]
   **Blocked WU:** [WU-N]
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

4. **Do not attempt to work around the blocker.**

Blocking issues include: a subagent failure unfixable after 2 retries; an edit that cannot be made without violating the drift guard (e.g. the only sensible place to express a diagram severity would require a new section — STOP and surface it); a request that would require modifying `synthesizer-v2.md`; any decision requiring architecture beyond this spec.

---

## Completion

When the phase is complete:

1. **Re-run the Step B.4 consistency checks** one final time across both files.
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Feature implemented:** buddy-guidance-enhancement Phase 4 (Visual teaching — ASCII diagrams in the Orient step)
   **Work units completed:** WU-1, WU-2
   **Final consistency-check status:** all green
   **Files modified:** `commands/review-pr-buddy.md`, `README.md`, `PHASES.md` (+ `SPEC.md` status already flipped to In-progress)

   **Commit log:**
   | Commit | Phase | Title |
   |--------|-------|-------|
   | [hash] | P4 | ASCII diagrams in the buddy Orient teach |

   **Implementation Notes summary:** [drift-guard result; grep-consistency counts; reminder that the PHASES.md Runtime-Verification rows remain Jacob's manual buddy-walk acceptance, and flipping SPEC.md Status back to Complete is gate-owned once that manual validation passes].
