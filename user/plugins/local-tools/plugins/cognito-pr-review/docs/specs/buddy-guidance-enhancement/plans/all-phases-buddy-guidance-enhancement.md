---
kind: implementation-plan
feature_id: buddy-guidance-enhancement
status: Ready
created: 2026-06-15
complexity: complex
phases: [1, 2, 3]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-15.
> To execute: `/execute-plan C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review\docs\specs\buddy-guidance-enhancement\plans\all-phases-buddy-guidance-enhancement.md`
> This plan is fully self-contained. The executing session needs no additional context.

# Implementation Plan — Buddy Guidance Enhancement  (v2)

**PHASES.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review\docs\specs\buddy-guidance-enhancement\PHASES.md` (buddy-guidance-enhancement, 3 phases)

**SPEC.md files:**
- `C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review\docs\specs\buddy-guidance-enhancement\SPEC.md`

**Total phases:** 3
**Plan version:** v2 (reference-based — components loaded from disk per step)

---

## NATURE OF THIS FEATURE — READ BEFORE EXECUTING

This feature edits **Claude Code agent/command markdown prompts** in the `cognito-pr-review` plugin. There is **no app integration, build step, compiled code, automated test harness, or MCP-reachable runtime**. The three editable artifacts are:

- `agents/journey-planner.md` (Phase 1)
- `commands/review-pr-buddy.md` (Phase 2)
- `README.md` + a net-new `VALIDATION.md` (Phase 3)

All paths are relative to the plugin root:
`C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review\`

**Consequences that override the generic template below:**

1. **TDD is N/A for every work unit.** PHASES.md states explicitly: "No automated test harness exists for prompt files, and none is in scope." Do NOT dispatch test agents and do NOT write `*.test.*` files. Skip the test-first pipeline entirely; dispatch implementation agents directly.
2. **There are no build/test quality gates.** The Step B.4 "Quality Gates" for this plan are **grep-based consistency checks** (defined per-WU below), not `dotnet build` / `nx test`. Run exactly the greps each WU specifies; 100% of them must return the expected result before proceeding.
3. **Runtime verification is MANUAL and DEFERRED by design.** The PHASES.md "Runtime Verification" rows and Phase 3's "No-regression validation" / "Behavioral-clustering check" deliverables can only be closed by Jacob running the buddy interactively on real PRs. They are **NOT** subagent-executable and are **NOT** work units in this plan. `/execute-plan` must NOT attempt them, must NOT check them off, and must leave them for the manual acceptance walk. The plan's only Phase 3 executable work is the `README.md` rewrite plus scaffolding an empty `VALIDATION.md` template for Jacob to fill.
4. **Repo identity.** These files live in the **`claude-config`** repo (a personal repo, `jacobmadsen12321@gmail.com`) under `user/plugins/...` — this path is the real source, not a symlink. Commits AND pushes are allowed here (the work-repo push hook does not apply). Do NOT confuse this with the Cognito Forms work repo.
5. **The orchestrator-must-not-edit-source rule still applies to these markdown prompt files.** Although `.md` is not in the literal forbidden-extension list, these prompts ARE this feature's source. Delegate every edit to a subagent and review its output — the value here is a disciplined review gate on a load-bearing prompt contract, not raw throughput.

---

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run grep consistency checks, update tracking docs | `Agent`, `Read`, `Bash` (greps/commits only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL prompt-file edits | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on the prompt files (`agents/journey-planner.md`, `commands/review-pr-buddy.md`, `README.md`, `VALIDATION.md`). Delegate every edit via `Agent`. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

---

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.5 | Commit Policy | `.claude/skill-config/commit-policy.md` (fallback: `~/.claude/skills/_components/commit-and-push.md`) |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

> **NOTE:** TDD Protocol, Test Agent, MCP Integration Test, and the build/test Quality-Gates components are intentionally OMITTED from this card — see "Nature of This Feature" §1–§3. Do not load or apply them.

## References — Upstream Artifacts

(none — this plan has no completed hard upstream dependencies. The sole dependency `cognito-pr-review-v2 — composes` is not a `hard` dep, so no upstream PHASES.md reality-check applies; this feature extends the journey-file Manual Review Guide and the buddy walk loop in place, as a peer.)

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL prompt-file edits MUST be delegated to Sonnet subagents via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on the prompt files. The ONLY exception: a trivial PASS-WITH-FIXES item (a few lines).
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents.
3. **TDD is N/A** (no test harness for prompt files). Dispatch implementation agents directly; do NOT run the test-first pipeline.
4. PHASES.md is updated AFTER EACH batch completes (not deferred).
5. Every subagent's output is reviewed for correctness and spec alignment before continuing.
6. Mistakes are fixed immediately before launching the next batch.
7. After all batches in a phase finish, integration verification confirms the change is internally consistent and (for Phase 2) honors the Phase 1 field contract.
8. Relevant CLAUDE.md files are reviewed after each phase (unlikely to need changes here — note and move on).
9. Each completed phase is committed and pushed before the next phase begins (claude-config is a personal repo — push is allowed).
10. Phases are strictly sequential here (P1 → P2 → P3); each depends on the prior. No cross-feature parallelism.
11. This plan is self-contained — follow it exactly as written without relying on external context.
12. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory.
13. **Quality gates = the grep consistency checks defined per-WU**, not build/test. **Runtime verification is manual + deferred** — never attempt or check off a manual-validation row.

---

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | buddy-guidance-enhancement | P1 | Partitioning — `journey-planner.md` | — | Solo |
| 2 | buddy-guidance-enhancement | P2 | Guiding loop — `review-pr-buddy.md` | P1 (field contract) | Solo |
| 3 | buddy-guidance-enhancement | P3 | Validation + docs (`README.md` + `VALIDATION.md` scaffold) | P1, P2 | Solo |

Each phase has exactly one work unit (one file). The three files have no overlap, but each phase depends on the prior (Phase 2 parses the field labels Phase 1 emits; Phase 3 documents what Phases 1–2 shipped) — so they run sequentially as one WU per batch.

## Work Units

- [x] WU-1 — Restructure `journey-planner.md` partitioning + journey template (Phase 1)
- [x] WU-2 — Rewrite `review-pr-buddy.md` two-pass loop + schema + mapping (Phase 2)
- [ ] WU-3 — Update `README.md` Buddy section + scaffold `VALIDATION.md` (Phase 3)

---

### Phase: buddy-guidance-enhancement P1 — Partitioning — `journey-planner.md`

**Goal:** Re-base the journey planner's partitioning guidance and journey-file template on behavioral/dependency clustering; define the journey field contract Phase 2 consumes.
**Entry criteria:** None (first phase).
**SPEC.md references:** §1 (1a–1f), Validation Criteria rows 1–3, 7.

#### WU-1 — Restructure `journey-planner.md` partitioning + journey template

- **Scope:** Phase 1 deliverables 1a–1f plus the template/Behaviour-Notes update. Copied from PHASES.md:
  - 1a — Behavioral-thread review unit: replace `_Group files logically, not by directory._` (L104) and the Manual Review Guide grouping guidance so each `### Step N` chunk is one behavioral thread spanning the layers that achieve a single objective (e.g. migration + data-access + business-logic together). Add anti-pattern guidance: do not group by directory; do not split one behavioral thread across chunks. State this is LLM-judged by the planner from cached diffs + structural-context (no new tooling).
  - 1b — Tests alongside, not last: remove the `| Tests | {files} | {purpose} | After core |` row (L102) and the `_Core changes first, tests last._` instruction (L108). Each behavioral chunk lists its tests **with** the implementation they exercise, framed as the change's executable oracle; chunk guidance pairs each test with the behavior it covers.
  - 1c — Hard 400-LOC split: add a planner rule — if a behavioral chunk's changed LOC > 400, subdivide along data-flow / architectural boundaries into sub-chunks each ≤ 400 LOC. Emit NO session-time / 60-min ceiling. Record a per-chunk `loc_estimate`.
  - 1d — Per-chunk persona + predictive questions: replace `**What to look for:**` / `**Key questions:**` (L113–114) with **`**Perspective:**`** (risk-matched PBR persona — security auditor for API/data-access, DBA for a migration, performance tester for a hot path, concurrency auditor for shared mutable state) and **`**Predictive questions:**`** (boundary-condition / failure-mode questions forcing predictive simulation, not descriptive recall).
  - 1e — Complexity signal: each chunk carries `**Complexity:** trivial | non-trivial`, set from intrinsic difficulty (cross-layer span, unfamiliar subsystem, algorithmic density). Default to `non-trivial` when uncertain. Document that the buddy uses it to scale teach depth.
  - 1f — Risk-first ordering: retain risk-first ordering among behavioral threads (critical threads first); update Re-Review Priority Order (L69–74) wording to refer to threads, not bare files.
  - Update the Journey File Template (L81–132) and Behaviour Notes (L205–212) to reflect 1a–1f, replacing the generic "Key questions" Behaviour Note (L209) with persona/predictive-question guidance.
- **TDD:** No (prompt file; no test harness).
- **Files to create/modify:** `agents/journey-planner.md` [VERIFY: path `C:\Users\JacobMadsen\source\repos\claude-config\user\plugins\local-tools\plugins\cognito-pr-review\agents\journey-planner.md`].
- **Test files:** none.
- **Implementation goal:** A reviewer running review-prep on a multi-layer PR gets a `PR-{id}-journey.md` whose Manual Review Guide chunks are behavioral threads (each spanning files across layers for one objective), with no standalone tests-last chunk, and every chunk emitting `**Perspective:**`, `**Predictive questions:**`, `**Complexity:**`, and a `loc_estimate`. Edit the EXISTING template in place — do NOT rewrite unrelated sections (Cache-Based File Access, Part 2 Triage Validation, Input/Output Spec stay intact). Refactor: the File Change Map table [VERIFY: grep -n "| Tests |" journey-planner.md], the grouping note [VERIFY: grep -n "Group files logically, not by directory" journey-planner.md], the ordering note [VERIFY: grep -n "Core changes first, tests last" journey-planner.md], the Step template fields [VERIFY: grep -n "What to look for" journey-planner.md], the Re-Review Priority Order [VERIFY: grep -n "Re-Review Priority Order" journey-planner.md], and Behaviour Notes [VERIFY: grep -n "Key questions" journey-planner.md].
- **Spec requirements:** SPEC §1 (1a–1f) verbatim; Decisions 4–7 and 2. No new tooling — clustering is LLM-judged by the Opus planner from cached diffs + structural-context.
- **Batch:** 1.

**FIELD-LABEL CONTRACT (load-bearing — Phase 2 parses these verbatim).** WU-1 MUST ship exactly these per-chunk labels and spellings in each `### Step N` chunk; pin them and do not let Phase 2 drift:
- `**Files:**`
- `**Perspective:**`  (NOT `Persona`)
- `**Predictive questions:**`
- `**Complexity:**` with values `trivial` | `non-trivial`
- a per-chunk `loc_estimate` (record it in the journey; the buddy reads it to verify the 400-LOC cap held)

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1 | WU-1 | Solo | None |

---

### Phase: buddy-guidance-enhancement P2 — Guiding loop — `review-pr-buddy.md`

**Goal:** Replace the single-pass six-step loop with the two-pass loop, scale teaching to complexity, replace disposition verbs with the severity taxonomy, add AI-role framing, bump the `buddy-session.json` schema, and update the curated-synthesis severity→section mapping and `REVIEWED.md` counts. Consumes the Phase 1 field contract.
**Entry criteria:** Phase 1 complete — `journey-planner.md` emits `**Perspective:**`, `**Predictive questions:**`, `**Complexity:**`, `loc_estimate`.
**SPEC.md references:** §2 (two-pass loop), §3 (schema), §4 (mapping + drift correction), §5 (AI-role). Validation Criteria rows 4–6.

#### WU-2 — Rewrite `review-pr-buddy.md` two-pass loop + schema + mapping

- **Scope:** Phase 2 deliverables. Copied from PHASES.md:
  - **Two-pass loop (Decision 1).** Rewrite the Per-Chunk Loop (L74–121) to: (1) **Orient** — always a one-line objective; if `Complexity` is `non-trivial`, add a senior-architect teach of what changed and why it matters vs. the journey Objective; for `trivial`, the one-liner is the whole orientation; deep teaching otherwise only on explicit request. (2) **Independent read — Pass 1** — present the chunk (implementation + its bundled tests) and pose the chunk's `**Perspective:**` persona + `**Predictive questions:**`; reviewer reads cold and records own observations; **tool findings are NOT shown in this pass** (anti-anchoring); include AI-role framing (buddy is a facilitator; reviewer is sole arbiter of logic correctness). (3) **Reconcile — Pass 2** — reveal the chunk's pre-computed findings (investigation / sweep / reuse / intrafile) as a reconciliation against the reviewer's Pass-1 take (overlaps, tool-only flags, implicitly what the tool may have missed). (4) **Disposition (Decision 3)** — for every finding (tool-surfaced and reviewer-authored) capture a **severity** via `AskUserQuestion`: **Blocking / Important / Suggestion / Dismiss**, with an optional free-text note on any non-dismissed finding (subsumes old `will-comment`); `add-own` becomes the mechanism by which a Pass-1 observation becomes a severity-tagged finding. (5) **Checkpoint** to `buddy-session.json`. (6) **Advance.**
  - **AI-role scoping (SPEC §5).** Bake facilitator framing into the Pass-1 and Pass-2 copy so the reviewer does not defer to the tool on business-logic correctness.
  - **`buddy-session.json` schema bump (SPEC §3).** Per-chunk record gains `complexity` (`trivial|non-trivial`), `loc_estimate` (int), and `pass1_observations[]` (`{file, line, note}`). Replace the disposition `verdict` enum `keep|dismiss|will-comment|add-own` with a `severity` field (`blocking|important|suggestion|dismiss`) and add a `source` field (`investigation|sweep|reuse|intrafile|reviewer`). Update the schema block (L143–172) and every reference to the old enum (L104–109, L184–191).
  - **Phase 2 severity→section mapping (SPEC §4 + drift correction).** Map onto the **existing** synthesizer-v2 sections: **Blocking** → `## Critical Findings` (investigation-sourced) / `### Important` (rule/reuse/intrafile); **Important** → `### Important` subsections; **Suggestion** → the existing **`### Minor` (nit)** subsections — do **NOT** create a `### Suggestion` section; **Dismiss** → excluded. Update Collect Curated Content (L184–191).
  - **`REVIEWED.md` counts.** Derive `critical` = blocking count, `important` = important count, `minor` = suggestion count from the severity tally (L226, L238–240).
  - Update the Overview Phase-1 bullet (L22) and Compaction Recovery references (L72, L132–139) so resume language matches the two-pass loop (recovery still resumes at the first chunk whose `status` ≠ `done`; mechanism unchanged).
- **TDD:** No (prompt file; no test harness).
- **Files to create/modify:** `commands/review-pr-buddy.md` [VERIFY: path `...\cognito-pr-review\commands\review-pr-buddy.md`].
- **Test files:** none.
- **Implementation goal:** The buddy walks a two-pass loop, withholds tool findings until Pass 2, scales teach to `Complexity`, and records `pass1_observations[]` + `severity`/`source` per disposition in `buddy-session.json`. The Phase-1 labels are parsed verbatim — replace the chunk-extraction copy at L68 (`**What to look for:**`/`**Key questions:**`) with the new labels (`**Perspective:**`/`**Predictive questions:**`/`**Complexity:**`). Refactor in place: the old enum [VERIFY: grep -n "will-comment" review-pr-buddy.md], the schema block [VERIFY: grep -n "buddy-session.json" review-pr-buddy.md], Collect Curated Content [VERIFY: grep -n "Collect Curated Content" review-pr-buddy.md], the REVIEWED.md template [VERIFY: grep -n "findings_total" review-pr-buddy.md].
- **CRITICAL DRIFT GUARD — do NOT modify `agents/synthesizer-v2.md`.** Its severity enum is `blocking | important | nit` and its minor tier is `### Minor` (no `suggestion` severity, no `### Suggestion` section) [VERIFY: grep -n "### Minor" synthesizer-v2.md ; grep -n "### Suggestion" synthesizer-v2.md → must be zero hits]. The reviewer-facing taxonomy stays Blocking/Important/Suggestion/Dismiss, but the Phase 2 writer MUST translate a `suggestion` disposition into the existing `### Minor` (nit) buckets. Introducing a `### Suggestion` section breaks synthesizer-v2 format parity — it is the single highest-risk error in this WU.
- **Spec requirements:** SPEC §2, §3, §4 (incl. the mechanical-drift correction: Suggestion→`### Minor`), §5. PHASES.md "Mechanical drift corrected in-plan (Phase 2)" note is authoritative.
- **Batch:** 1.

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1 | WU-2 | Solo | None |

---

### Phase: buddy-guidance-enhancement P3 — Validation + docs

**Goal:** Update user-facing docs to describe the shipped two-pass loop + severity vocabulary, and scaffold the manual-validation note. Empirical no-regression validation and the behavioral-clustering check are MANUAL acceptance activities Jacob performs — they are NOT executed by this plan.
**Entry criteria:** Phase 1 and Phase 2 complete.
**SPEC.md references:** §2 (user-visible vocabulary), Validation Criteria (all rows — for the manual walk Jacob runs later).

#### WU-3 — Update `README.md` Buddy section + scaffold `VALIDATION.md`

- **Scope:**
  - **README update (executable).** Rewrite `README.md` L96 and the surrounding Buddy Review section (L83–98 as needed) to describe the two-pass loop (independent read → reconcile) and the **Blocking / Important / Suggestion / Dismiss** severity vocabulary, replacing the old `keep / dismiss / will-comment / add-own` + single-pass framing. After the edit, the string `keep / dismiss / will-comment / add-own` must NOT appear anywhere in `README.md`.
  - **`VALIDATION.md` scaffold (net-new, executable).** Create `docs/specs/buddy-guidance-enhancement/VALIDATION.md` as an EMPTY template Jacob fills during his manual walk: a heading, a one-line purpose, and a checklist table mirroring the 8 SPEC Validation Criteria rows (Behavior | Trigger | Expected Evidence | Result [ ] pass / [ ] fail | Notes), plus a "Behavioral-clustering quality" line (reliable / needs Tier-2 follow-up). Do NOT fabricate results — leave Result/Notes blank.
- **MANUAL — NOT executed by this plan (do NOT check off):** PHASES.md Phase 3 deliverables "No-regression validation" and "Behavioral-clustering check" require running the buddy interactively on prior PRs. Leave these unchecked; surface them in the completion report as Jacob's manual acceptance step.
- **TDD:** No.
- **Files to create/modify:** `README.md` (modify) [VERIFY: grep -n "keep / dismiss / will-comment / add-own" README.md] ; `docs/specs/buddy-guidance-enhancement/VALIDATION.md` (**net-new — create**).
- **Test files:** none.
- **Implementation goal:** `README.md`'s Buddy section matches the shipped behavior (two-pass loop + severity vocabulary) and contains none of the old picker vocabulary; `VALIDATION.md` exists as a blank-but-structured acceptance checklist. Edit only the Buddy Review section of README; leave all other sections intact.
- **Spec requirements:** SPEC §2 (user-visible vocabulary); PHASES.md Phase 3 "README update" deliverable + "Minimum Verifiable Behavior".
- **Batch:** 1.

> | Batch | Work Units | Parallel? | File Conflicts? |
> |-------|-----------|-----------|-----------------|
> | 1 | WU-3 | Solo | None |

---

## Execution Protocol

This protocol governs the autonomous execution of every phase. Follow it exactly.

### Phase Selection Loop

Repeat until all phases in the Execution Schedule are complete or a blocking issue triggers early exit:

1. **Select ready phase(s):** Phases are strictly sequential (P1 → P2 → P3). Select the lowest-numbered phase whose prerequisites are complete (all of its deliverables checked off in PHASES.md). If none is ready, jump to the Blocking Issue Protocol.
2. **Announce:** Print "Implementing buddy-guidance-enhancement Phase N: [title]".
3. **Review prior context:** Re-read all previously completed phases' Implementation Notes in PHASES.md. For Phase 2, this includes the exact field-label spellings WU-1 shipped (the contract). They take priority over this plan where they diverge.
4. **Execute the phase's single batch** per the Per-Batch Steps below.
5. **Run Post-Phase Steps** below.
6. **Report:** Print "buddy-guidance-enhancement Phase N: [title] — committed as [hash]".
7. **Loop:** Return to step 1.

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly. Create one task per work unit (WU-1, WU-2, WU-3). Since TDD is N/A, each task moves straight to an implementation phase (no test phase). Update task status as you progress.

### Per-Batch Steps

For each batch within a phase:

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow its instructions. Re-read from disk: PHASES.md (current phase + prior Implementation Notes), SPEC.md (relevant sections), the target prompt file(s) for this WU, and this plan file. Do NOT rely on cached content. For Phase 2, also re-read the **shipped** `agents/journey-planner.md` to confirm the exact field labels you must parse.

#### Step B.1: Launch Subagent (COMPOSE Agent TOOL CALL — ZERO INLINE EDITS)

**PRE-FLIGHT CHECK:** Confirm (1) you will use the `Agent` tool with `model: "sonnet"` for the prompt-file edit, (2) you will NOT call `Edit`/`Write` on the prompt files yourself. If either is false, re-read the EXECUTION MODEL section.

Read before proceeding:
1. `~/.claude/skills/_components/subagent-launch.md` — launch orchestration + failed-agent recovery protocol. **Ignore its TDD / test-agent (Phase A) directives — TDD is N/A here.** Dispatch only the implementation agent (Phase B).
2. `~/.claude/skills/_components/implementation-agent.md` — impl agent prompt template; include its briefing verbatim in the agent's prompt.

The subagent prompt MUST be fully self-contained (the subagent has zero prior context). Include: the plugin root path, the exact target file path, the verbatim deliverable scope from the WU above, the FIELD-LABEL CONTRACT (WU-1) or the requirement to parse those exact labels (WU-2), the CRITICAL DRIFT GUARD for WU-2 (never touch synthesizer-v2.md; never create a `### Suggestion` section), the editorconfig note (these are CRLF markdown files — preserve line endings; do not reflow unrelated content), and the instruction to edit IN PLACE without rewriting unrelated sections.

**POST-DISPATCH GATE:** After the subagent completes, verify you composed an `Agent` call and did NOT edit the prompt files directly. If violated, revert and re-dispatch.

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

This is a blocking gate. Read `~/.claude/skills/_components/subagent-review.md` and follow its protocol; also read `~/.claude/skills/_components/mount-site-verification.md` for the net-new `VALIDATION.md` (WU-3). Produce a structured review report with a PASS / PASS-WITH-FIXES / NEEDS-REWORK verdict. Review specifically for:
- **Spec alignment** against the WU scope and the cited SPEC sections.
- **Field-contract integrity** (WU-1 ships the exact labels; WU-2 parses those exact labels — no `Persona`/`Perspective` drift).
- **Drift guard** (WU-2): `agents/synthesizer-v2.md` was NOT modified, and NO `### Suggestion` section was introduced anywhere.
- **No collateral damage**: unrelated sections of the file are untouched.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md` and follow its instructions. Check off the completed **Deliverables** for this phase and add an Implementation Notes block (date, work completed, the exact field labels shipped [Phase 1], integration notes, pitfalls, files modified). **Do NOT check off the "Runtime Verification" rows** — those are manual. **For Phase 3, do NOT check off "No-regression validation" or "Behavioral-clustering check"** — those are manual acceptance deliverables; check off only "README update". Note in the Implementation block that the manual deliverables remain for Jacob's acceptance walk.

#### Step B.4: Quality Gates = Grep Consistency Checks (MANDATORY — DO NOT SKIP)

There are NO build/test gates. Run the WU-specific grep checks below from the plugin root and confirm each returns the expected result. 100% must pass before proceeding.

**WU-1 (journey-planner.md):**
- `grep -c "| Tests |" agents/journey-planner.md` → **0** (tests-last row removed).
- `grep -c "Core changes first, tests last" agents/journey-planner.md` → **0**.
- `grep -c "Group files logically, not by directory" agents/journey-planner.md` → **0**.
- `grep -c "Perspective:" agents/journey-planner.md` → **≥ 1**.
- `grep -c "Predictive questions:" agents/journey-planner.md` → **≥ 1**.
- `grep -c "Complexity:" agents/journey-planner.md` → **≥ 1**.
- `grep -c "loc_estimate" agents/journey-planner.md` → **≥ 1**.

**WU-2 (review-pr-buddy.md):**
- `grep -c "will-comment" commands/review-pr-buddy.md` → **0** (old enum gone everywhere).
- `grep -c "pass1_observations" commands/review-pr-buddy.md` → **≥ 1**.
- `grep -Ec "\"?severity\"?" commands/review-pr-buddy.md` → **≥ 1** (severity field present).
- `grep -c "### Suggestion" commands/review-pr-buddy.md` → **0** (drift guard — no new section).
- `git diff --name-only` must NOT list `agents/synthesizer-v2.md`.

**WU-3 (README.md + VALIDATION.md):**
- `grep -c "keep / dismiss / will-comment / add-own" README.md` → **0**.
- `grep -Ec "Blocking|Important|Suggestion|Dismiss" README.md` → **≥ 1**.
- `test -f docs/specs/buddy-guidance-enhancement/VALIDATION.md` → exists.

If any check fails, treat it as a NEEDS-REWORK and fix via a follow-up subagent dispatch before proceeding.

#### Step B.4.5: MCP Integration Test — SKIPPED

Not applicable. PHASES.md declares **MCP runtime: not-required** — there is no MCP-reachable surface. Skip with this note.

#### Step B.5: Commit Batch

Read the commit policy: first try `.claude/skill-config/commit-policy.md` in the project root; if absent, read `~/.claude/skills/_components/commit-and-push.md`. **Commit in the `claude-config` repo** (`cd C:\Users\JacobMadsen\source\repos\claude-config`). This is a personal repo — commit AND push are permitted. Stage only the files this WU changed (the target prompt file + PHASES.md). Do NOT use `git add -A` (the broader claude-config tree carries unrelated config).

#### Step B.6: Proceed to Next Batch

**Checklist before proceeding (all must be true):**
- [ ] Review report produced with a verdict
- [ ] PHASES.md updated (deliverables checked, Implementation Notes added; manual rows left unchecked)
- [ ] All WU grep consistency checks pass
- [ ] Step B.5 commit completed

If any item is unchecked, complete it before moving on.

### Propagation Awareness Note

The only cross-file coupling is the **Phase 1 → Phase 2 field-label contract** (the markdown labels `**Perspective:**` / `**Predictive questions:**` / `**Complexity:**` + `loc_estimate`). It is migrated within the same plan, sequentially: WU-1 emits the labels; WU-2 parses them. There are no import wrappers, struct fields, or jest aliases. The propagation guard is the Step B.2 field-contract review + the Step B.4 grep checks confirming both sides use identical spellings.

### Post-Phase Steps (after the phase's batch)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md` and follow it. For this feature, integration verification means: (Phase 1) the journey template is internally consistent and emits the contract labels; (Phase 2) the buddy parses exactly those labels, the schema/loop/mapping/REVIEWED.md are mutually consistent, and synthesizer-v2 parity holds (Suggestion→`### Minor`, no new section); (Phase 3) the README describes what Phases 1–2 actually shipped and references no removed vocabulary.

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md`. Review whether the plugin's `README.md` (already a Phase 3 deliverable) or any CLAUDE.md needs updating. Likely no CLAUDE.md change is warranted — note that and move on.

#### Commit and Push Post-Phase Changes

Read the commit policy (as in Step B.5) and commit/push any post-phase doc changes in the `claude-config` repo.

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit and push any completed phases** not yet committed (claude-config repo).
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** buddy-guidance-enhancement Phase N: [title]
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining phases (not attempted):**
   - [list]

4. **Do not attempt to work around the blocker.**

Blocking issues include:
- A subagent failure that can't be fixed after 2 retry attempts
- A Step B.4 grep check that can't be made to pass after 2 attempts
- A git push conflict that can't be resolved by rebase
- The drift guard tripping irrecoverably (synthesizer-v2.md altered, or a `### Suggestion` section that can't be removed cleanly)
- Any error requiring architectural decisions beyond the specs

> **Not a blocker:** the manual Phase 3 validation deliverables. They are out of `/execute-plan` scope by design — report them as Jacob's manual acceptance step, do not treat their incompleteness as a blocker.

---

## Completion

When Phases 1–3 are complete (all subagent-executable deliverables done):

1. **Re-run all WU grep consistency checks** one final time across the three files; confirm all pass.
2. **Print a completion report:**

   ## Implementation Batch — Complete

   **Feature implemented:** buddy-guidance-enhancement
   **Total phases completed:** 3
   **Total commits:** M
   **Final consistency-check status:** all green

   **Commit log:**
   | Commit | Phase | Title |
   |--------|-------|-------|
   | abc1234 | P1 | Partitioning — journey-planner.md |
   | def5678 | P2 | Guiding loop — review-pr-buddy.md |
   | ghi9012 | P3 | Validation docs — README.md + VALIDATION.md |

   **Manual acceptance remaining (Jacob — NOT executed by this plan):**
   - Run `/cognito-pr-review:review-pr-buddy` on ≥1 prior reviewed PR (multi-layer + one >400-LOC thread). Confirm: chunks are behavioral threads (not directory groups); no tests-last chunk; `loc_estimate` ≤ 400 per chunk; tool findings withheld until Pass 2; teach scales to `Complexity`; severities captured Blocking/Important/Suggestion/Dismiss; a `suggestion` lands under `### Minor` (no `### Suggestion` section); `REVIEWED.md` counts match the tally; no-regression vs the archived `PR-{id}.md`.
   - Record results in `VALIDATION.md` and judge behavioral-clustering quality (reliable / needs Tier-2 follow-up).
   - When validation passes, the completion gate flips SPEC.md `**Status:**` to Complete (gate-owned — not a checkbox).

   **Implementation Notes summary:**
   [field-label contract shipped, any drift corrections, synthesizer-v2 parity confirmation]
