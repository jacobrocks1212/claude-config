---
kind: implementation-plan
feature_id: spot-check
status: Complete
created: 2026-06-29
complexity: complex
phases: [1]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-29.
> To execute: `/execute-plan C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/docs/specs/spot-check/plans/all-phases-spot-check-part-1.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 1 of 2. Sibling parts:
> - part 1: `C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/docs/specs/spot-check/plans/all-phases-spot-check-part-1.md` (this file)
> - part 2: `C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/docs/specs/spot-check/plans/all-phases-spot-check-part-2.md`
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution.

# Implementation Plan — spot-check (Phase 1: Author the command)  (v2)

**PHASES.md files:**
- `C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/docs/specs/spot-check/PHASES.md` (spot-check, 2 phases; this part covers Phase 1)

**SPEC.md files:**
- `C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/docs/specs/spot-check/SPEC.md`

**Total phases:** 2 (this part: Phase 1 only)
**Plan version:** v2 (reference-based — components loaded from disk per step)

---

## REPO & TASK CONTEXT — READ THIS BEFORE ANYTHING ELSE

**This is NOT the Cognito Forms .NET repo.** This plan authors a **Claude Code plugin command** (markdown + JSON) inside the `claude-config` repo:

- **Plugin root:** `C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/`
- **Language:** Markdown command files + YAML frontmatter + a single JSON manifest. **No C#, no Vue, no compiled code.**
- **No build system, no test harness.** There is NO `/msbuild`, NO `/mstest`, NO `/nxbuild`, NO `dotnet`, NO vitest for this work. Do NOT invoke any of those. The PHASES.md explicitly marks this feature **non-TDD** ("there is no automated unit-test harness for slash-command markdown").
- **Quality gates here mean:** valid YAML frontmatter on the command file, and (for Phase 2) well-formed JSON. Runtime behavior (a real `/cognito-pr-review:spot-check` run, plugin reload) is **deferred runtime verification** observed by a human — it is NOT a work unit in this plan (see PHASES.md "Runtime Verification" rows).
- **Git identity:** `claude-config` uses the **personal** identity by default (`git config user.email` → `jacobmadsen12321@gmail.com`). It is **NOT** a Cognito work repo, so the work-repo push hook does **NOT** apply here. Commit + push are both allowed.

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL source files (here: the `.md` command file) | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit` or `Write` on the command file or any other authored file. The file being authored in this part (`commands/spot-check.md`) is the "source" — delegate its authoring to a Sonnet subagent. The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md` (the plugin's own).

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

This plan references reusable component files by path instead of inlining their content. **Before executing each step**, `Read` the component files listed for that step from disk. Do NOT proceed from memory of their contents — always load fresh. After context compaction, re-read this plan file first, then load components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.2 | Mount-Site Verification | `~/.claude/skills/_components/mount-site-verification.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` (see repo-specific override below) |
| Step B.5 | Commit Policy | `~/.claude/skills/_components/commit-and-push.md` |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

> **TDD note:** This feature is non-TDD (no test harness for slash-command markdown). When you read `tdd-protocol.md`, the decision gate resolves to **direct implementation** — there is no test-agent phase. Skip `tdd-test-agent.md`.

## References — Upstream Artifacts

The SPEC's sole dependency (`cognito-pr-review-v2`) is `composes`, not `hard`, so no upstream PHASES look-back is required.

`(none — this plan has no completed hard upstream dependencies)`

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **ALL authoring work MUST be delegated to a Sonnet subagent via the Agent tool** — the orchestrating session MUST NOT call `Edit` or `Write` on the command file. The ONLY exception: trivial PASS-WITH-FIXES items (a few lines).
2. All subagent edits happen in the current worktree — NEVER create worktrees for subagents
3. This feature is **non-TDD**; the work unit goes through direct implementation (no failing-test-first pipeline)
4. PHASES.md is updated AFTER the batch completes (not deferred)
5. The subagent's output is reviewed for correctness and spec alignment before continuing
6. Mistakes are fixed immediately before proceeding
7. After the batch finishes, integration verification confirms the command is internally consistent
8. Relevant CLAUDE.md files are created/updated after the phase if changes warrant it (Phase 1 does NOT touch the plugin CLAUDE.md — that is Phase 2's job)
9. The completed phase is committed before Phase 2 (part 2) begins
10. This plan is self-contained — follow it exactly as written without relying on external context
11. **Before each step, `Read` the component files listed for that step from disk** — do NOT rely on memory
12. **Do NOT run `/msbuild`, `/mstest`, `/nxbuild`, `dotnet`, or any compile/test command** — this is markdown/JSON, see the repo-specific QG override

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | spot-check | P1 | Author `commands/spot-check.md` | — | Solo |

---

## Work Units

- [x] WU-1 — Author `commands/spot-check.md` (full command orchestration)

---

## Phase: spot-check P1 — Author the `spot-check` command

**Goal:** Author the complete `commands/spot-check.md` — the entire lightweight spot-check command in one file.
**Entry criteria:** None (first phase).
**SPEC.md references:** Technical Design → Pipeline + Step detail (Steps 1–6); Reuse Ledger; User Experience (Invocation, Scope syntax, Examples, Output). PHASES.md Phase 1 deliverables.

### WU-1 — Author `commands/spot-check.md`

**Scope (PHASES.md Phase 1 deliverables — all in this one file):**
- [ ] `commands/spot-check.md` with frontmatter: `description`, `argument-hint: "[PR_ID | local] [scope: last-commit|since-review|<sha>..<sha>|<glob>|<free-text>]"`, `allowed-tools: ["Bash", "Glob", "Grep", "Read", "Write", "Agent"]`.
- [ ] Argument parsing: PR mode (first numeric token) vs local mode (`local` / no ID, with `--base`, `--include-untracked`); remaining tokens parsed as scope (structured tokens + free-text).
- [ ] Step 1 — Prep delegation: invoke `prep-pr.ts {id}` (PR) or `--local …` exactly as `review-pr.md` Step 1, referencing it as source of truth (do not duplicate the body). Read `cacheDir` / `cogDocsItemDir` from prep output. Explicitly skip the cache-boundary marker.
- [ ] Step 2 — Scope resolution: whole-PR default; `last-commit`/`latest`; `since-review` via `pr-timeline.json` review timestamps with `last-commit` fallback; `<sha>..<sha>`; path/glob filter; free-text interpreted against the manifest. Record the resolved human-readable scope.
- [ ] Step 3 — Inline review: orchestrator reads the scoped cached diffs and reviews directly with senior-Cognito-reviewer judgment (no sweep/triage/journey/reuse/intra-file agents).
- [ ] Step 4 — Conditional escalation: dispatch at most one `cognito-pr-review:investigation` agent, only for a genuinely risky change; fold its findings in.
- [ ] Step 5 — Inline synthesis in `synthesizer-v2` output format (no synthesizer agent dispatch); header marks it a spot-check and states the reviewed scope.
- [ ] Step 6 — Write artifact: PR mode → `<cogDocsItemDir>/PR-{id}-spot-{YYYY-MM-DD-HHMM}.md`; local → `.claude.local/reviews/LOCAL-{branch}-spot-{YYYY-MM-DD-HHMM}.md`. Print completion summary. No `REVIEWED.md`, no `pending-calibration.json`, no calibration.
- [ ] Explicit non-goal stated in-command: never invoke ADO MCP or `az`.

**Coupling reason (why one WU, not nine):** All nine deliverables are sections of a **single markdown file** (`commands/spot-check.md`). They are mutually dependent — the command is non-functional and non-verifiable with any section missing, and there is no test harness to exercise sections independently. Splitting would put multiple writers on one file (forbidden) and produce no independently testable artifact. This is the textbook "single-file co-creation" merge case.

**TDD:** no (non-TDD feature — no markdown test harness; direct authoring).

**Files to create/modify:**
- `C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/commands/spot-check.md` — **net-new (create)**.

**Test files:** none (non-TDD).

**Implementation goal (verified facts the subagent MUST use):**

Author a self-contained slash-command markdown file modeled on the structure of the existing `commands/review-pr.md` (mirror its prose style and step layout), but implementing ONLY the lightweight pipeline below.

- **Frontmatter** exactly per the deliverable: `description` (one line, e.g. "Fast, lightweight spot-check review of a small or narrowly-scoped PR — inline-first, ≤1 investigation agent, standalone (no ADO/calibration/sentinels)."), the `argument-hint` string **verbatim** as written in the deliverable above (Phase 2 docs will mirror this string, so it is canonical), and `allowed-tools: ["Bash", "Glob", "Grep", "Read", "Write", "Agent"]`.

- **Step 1 — Prep (reuse):** Invoke prep exactly as `commands/review-pr.md` Step 1 does — `npx tsx ~/.claude/plugins/local-tools/plugins/cognito-pr-review/scripts/prep-pr.ts {pr_id}` for PR mode, or `... prep-pr.ts --local [--base <branch>] [--include-untracked]` for local mode. [VERIFY: grep -n "prep-pr.ts" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/commands/review-pr.md"] Reference `review-pr.md` Step 1 as the source of truth for the invocation; do NOT copy its body. Read `cacheDir` from the manifest JSON the script prints to stdout, and `cogDocsItemDir` from the `Cog-docs item dir: <path>` line the script prints to **stderr**. [VERIFY: grep -n "resolveOrCreateCogDocsItemDir\|Cog-docs item dir" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/scripts/prep-pr.ts"] **Explicitly SKIP** `review-pr.md` Step 1.5 (the cache-boundary marker / `pr-review-active.json`) — state in-command WHY: the inline review and the optional investigation agent both need normal codebase read access, and there is no cache-only sweep agent in this pipeline. [VERIFY: grep -n "Step 1.5" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/commands/review-pr.md"]

- **Step 2 — Scope resolution (inline):** Implement the token + free-text grammar from SPEC "Scope syntax": whole-PR default; `last-commit`/`latest` (PR: `gh api repos/cognitoforms/cognito/pulls/{id}/commits` last entry or `gh pr diff`; local: `git show`/`git diff HEAD~1`); `since-review` (read `pr-timeline.json` from the cache, find the reviewer's most recent review timestamp, select commits/files after it; **fall back to `last-commit`** if no prior review is found, and state the fallback); `<sha>..<sha>` range; path/glob filter against the manifest; free-text interpreted against manifest+diffs (state which files were chosen). Record a human-readable resolved scope string for the artifact header + summary.

- **Step 3 — Inline review:** Orchestrator reads the scoped cached diffs and reviews directly with senior-Cognito-reviewer judgment (correctness, DI/storage/async pattern issues, test gaps on changed behavior). **No** sweep / triage / journey / reuse / intra-file agents. Most small PRs yield no findings.

- **Step 4 — Conditional escalation:** Dispatch **at most one** `investigation` agent via `subagent_type: cognito-pr-review:investigation` [VERIFY: grep -rn "cognito-pr-review:investigation" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/commands/review-pr.md"] — ONLY for a change the orchestrator cannot confidently resolve inline (subtle correctness risk, non-obvious blast radius, pattern needing codebase verification). Pass the relevant cached files/diffs + `cacheDir`. Fold its evidence-based findings into synthesis. If nothing warrants it, dispatch nothing.

- **Step 5 — Inline synthesis:** Compose the review inline following the `agents/synthesizer-v2.md` **output format** (do NOT dispatch the agent). [VERIFY: ls "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/agents/synthesizer-v2.md"] Sections, in order, with synthesizer-v2's omission rules: `# Cognito PR Review — …` title; `## Summary` (never omit); `## Requirements Coverage` (never omit); `## Critical Findings` (omit if none); `## Rule-Based Findings` (omit — spot-check runs no sweep); `## Reuse & Duplication` (omit — not run); `## Intra-File Consistency` (omit — not run); `## Re-Review Status` (only when scope implies a re-review, e.g. `since-review`); `## Strengths` (never omit). Severity tiers: `blocking` / `important` / `nit`. Add a short header line marking this a **spot-check** and stating the **reviewed scope**.

- **Step 6 — Write + report:** PR mode → `<cogDocsItemDir>/PR-{id}-spot-{YYYY-MM-DD-HHMM}.md`; local → `.claude.local/reviews/LOCAL-{branch}-spot-{YYYY-MM-DD-HHMM}.md` (date **and** time so repeat runs never clobber each other or the authoritative `PR-{id}.md`). Print a chat summary: artifact path, scope reviewed, file/finding counts, whether an investigation agent was escalated. Do **NOT** write `REVIEWED.md` or `pending-calibration.json`; do **NOT** run calibration or touch `weights.yaml`.

- **Explicit non-goal in-command:** A clear statement that spot-check NEVER invokes Azure DevOps MCP or the `az` CLI at any point.

**Spec requirements:** SPEC.md "Technical Design" (Pipeline diagram + Step detail Steps 1–6), "Reuse Ledger" (reuse prep as-is; dispatch investigation ≤1; wrap synthesizer-v2 format inline; do NOT use sweep/triage/journey/reuse/intra-file/post-process/sentinels/calibration), "User Experience" (Invocation, Scope syntax table, Examples, Output paths). The command must satisfy SPEC Goals (fast/few dispatches, self-contained, still produces an artifact, scope-targetable, standalone) and respect all Non-Goals.

**Batch:** 1 (solo).

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1      | Solo      | None            |

---

## Execution Protocol

This protocol governs autonomous execution. Follow it exactly.

### Phase Selection Loop

This part contains exactly one phase (spot-check P1) with one batch. Execute Step 0, then the Per-Batch Steps for Batch 1, then the Post-Phase Steps, then proceed to Completion. If a blocking issue arises, jump to the Blocking Issue Protocol.

1. **Announce:** Print "Implementing spot-check Phase 1: Author the spot-check command".
2. **Review prior context:** Re-read SPEC.md (Technical Design, Reuse Ledger, User Experience) and PHASES.md Phase 1 from disk. They take priority over this plan where they diverge.
3. **Execute Batch 1** per the Per-Batch Steps below.
4. **Run Post-Phase Steps.**
5. **Report:** Print "spot-check Phase 1: Author the spot-check command — committed as [hash]".

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE — EXECUTE BEFORE ANYTHING ELSE)

Read `~/.claude/skills/_components/task-tracking.md` and follow its instructions exactly. Create a task for WU-1 and update its status through the implementation.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/source-reread.md` and follow it. Re-read from disk: PHASES.md (Phase 1), SPEC.md (relevant sections), and this plan file. Also re-read the three reuse anchors so the subagent prompt carries accurate facts: skim `commands/review-pr.md` Step 1 + Step 1.5 + Step 5, confirm `scripts/prep-pr.ts` output shape, and skim `agents/synthesizer-v2.md` output format.

#### Step B.1: Launch Subagent (COMPOSE Agent TOOL CALL — ZERO INLINE AUTHORING)

**PRE-FLIGHT CHECK:** Confirm (1) you will use the `Agent` tool with `model: "sonnet"` for authoring `commands/spot-check.md`, (2) you will NOT call `Edit`/`Write` on it yourself. If either is false, re-read EXECUTION MODEL.

Read:
1. `~/.claude/skills/_components/tdd-protocol.md` — gate resolves to **direct implementation** (non-TDD); skip the test-agent phase.
2. `~/.claude/skills/_components/subagent-launch.md` — launch orchestration + failed-agent recovery.
3. `~/.claude/skills/_components/implementation-agent.md` — impl-agent prompt template: include verbatim in the agent's prompt.

Dispatch ONE Sonnet implementation agent for WU-1. Its prompt MUST embed: the full WU-1 Implementation goal above (including every `[VERIFY: …]` fact), the verified reuse facts (prep-pr.ts invocation + output reading, SKIP Step 1.5, subagent_type `cognito-pr-review:investigation`, synthesizer-v2 section list + omission rules, artifact path patterns), the canonical `argument-hint` string, and the instruction to model the file's prose/structure on `commands/review-pr.md`. Tell the agent: this is a Claude Code plugin command markdown file (no code compiles; no tests run); follow the project convention of lowercase-hyphen `.md` filename + YAML frontmatter.

**POST-DISPATCH GATE:** Verify you composed an `Agent` call and did NOT edit the file directly. If violated, revert and re-dispatch.

#### Step B.2: Review Batch Output (MANDATORY GATE — DO NOT SKIP OR SHORTCUT)

Read `~/.claude/skills/_components/subagent-review.md` and follow its full protocol; also read `~/.claude/skills/_components/mount-site-verification.md` (new-file check). Produce a structured review report with a verdict (PASS / PASS-WITH-FIXES / NEEDS-REWORK). Verify specifically:
- Frontmatter matches the deliverable exactly (the `argument-hint` string verbatim; `allowed-tools` list).
- All six steps present; Step 1.5 cache-boundary marker is explicitly SKIPPED with a stated reason.
- Investigation escalation is capped at exactly one and uses `cognito-pr-review:investigation`.
- Synthesis follows synthesizer-v2 sections + omission rules and is inline (no synthesizer agent dispatch).
- Artifact paths are the timestamped `PR-{id}-spot-{YYYY-MM-DD-HHMM}.md` / `LOCAL-{branch}-spot-{...}.md` forms.
- Standalone guarantees present: no `REVIEWED.md`, no `pending-calibration.json`, no calibration/weights, no ADO/`az`.

#### Step B.3: Update PHASES.md (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/phases-update.md` and follow it. Check off the Phase 1 **implementation** deliverables (the non-`verification-only` checkboxes). Do NOT check the `<!-- verification-only -->` Runtime Verification rows — those are closed by a real `/spot-check` run, not by this implementation. Add an Implementation Notes block (date, work completed, integration notes, files modified) per the component.

#### Step B.4: Run Quality Gates (MANDATORY — DO NOT SKIP) — REPO-SPECIFIC OVERRIDE

Read `~/.claude/skills/_components/quality-gates.md` for the general protocol, but **apply this repo-specific override** (there is no .NET/nx build here):

1. **Frontmatter validity:** Confirm `commands/spot-check.md` opens with a well-formed YAML frontmatter block (`---` … `---`) containing `description`, `argument-hint`, and `allowed-tools`. A quick parse check:
   ```bash
   python -c "import sys,yaml; t=open(r'C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/commands/spot-check.md',encoding='utf-8').read(); fm=t.split('---')[1]; yaml.safe_load(fm); print('frontmatter OK')"
   ```
2. **No accidental ADO/az/calibration leakage:** grep the new file to confirm it does NOT instruct calls to `az `, ADO MCP, `REVIEWED.md`, `pending-calibration.json`, or `weights.yaml` except where explicitly naming them as non-goals.
   ```bash
   grep -nE "\baz \b|pending-calibration|REVIEWED\.md|weights\.yaml" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/commands/spot-check.md"
   ```
   (Hits are acceptable ONLY inside the explicit non-goal statement.)

**Do NOT** run `/msbuild`, `/mstest`, `/nxbuild`, `dotnet`, or vitest — none apply. If the frontmatter parse fails, treat it as a gate failure and route the fix back through a Sonnet subagent.

#### Step B.4.5: MCP Integration Test — N/A

Read `~/.claude/skills/_components/mcp/mcp-integration-test.md` only if uncertain. PHASES.md Phase 1 declares **"MCP Integration Test Assertions: N/A — no MCP-reachable runtime surface."** Skip with a note.

#### Step B.5: Commit Batch

Read `~/.claude/skills/_components/commit-and-push.md` and follow it. This is the `claude-config` repo (personal identity; the Cognito work-repo push hook does NOT apply). Stage only the files this part touched (`commands/spot-check.md`, the PHASES.md update) and commit with a clear message, e.g. `feat(cognito-pr-review): add lightweight spot-check command`. Push is permitted but optional — follow the component's policy.

#### Step B.6: Proceed

**Checklist (all must be true):**
- [ ] Review report produced with a verdict
- [ ] PHASES.md updated (implementation deliverables checked; verification-only rows left unchecked)
- [ ] Repo-specific quality gates pass (frontmatter valid; no leakage)
- [ ] Batch committed

If any item is unchecked, complete it before continuing.

### Propagation Awareness Note

WU-1 introduces no import indirection, no shared struct/interface field additions, and no re-exports — it is a standalone markdown command file. No propagation step or full-suite run is required.

### Post-Phase Steps (after the batch)

#### Integration Verification (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/integration-verification.md` and follow it. For this part, verification is **static/internal-consistency** (runtime is deferred to a human run): confirm the command's prep invocation, scope grammar, escalation cap, synthesis format, artifact paths, and standalone guarantees are internally consistent and match SPEC Technical Design + Reuse Ledger. Confirm the `argument-hint` string is exactly the canonical string (Phase 2 docs will mirror it).

#### Update CLAUDE.md Files (MANDATORY — DO NOT SKIP)

Read `~/.claude/skills/_components/claude-md-review.md` and follow it. **Note:** the plugin's `CLAUDE.md` Key Commands row for spot-check is owned by **Phase 2 (part 2)** — do NOT add it here (one writer per file). Only update a CLAUDE.md in this part if Phase 1 surfaced something genuinely new and unrelated to Phase 2's planned edits; otherwise record "no CLAUDE.md change needed in Phase 1 (deferred to Phase 2)".

#### Commit and Push Post-Phase Changes

Read `~/.claude/skills/_components/commit-and-push.md` and follow it for any post-phase doc changes (likely none beyond the PHASES.md update already committed).

---

## Blocking Issue Protocol

If a blocking issue is encountered at any point:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit any completed work** that is not yet committed.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** spot-check Phase 1: Author the spot-check command
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining phases (not attempted):**
   - spot-check Phase 2 (part 2)

4. **Do not attempt to work around the blocker.** The user provides a resolution and re-triggers.

Blocking issues include: a subagent failure unfixable after 2 retries; a quality-gate (frontmatter/leakage) failure unfixable after 2 retries; a reuse anchor that no longer exists in the tree (the audit verified all of them — if one is now missing, the codebase drifted: stop and report); any error requiring architectural decisions beyond the spec.

---

## Completion

When Phase 1 is complete:

1. **Re-run the repo-specific quality gates** (frontmatter parse + leakage grep) one final time.
2. **Print a completion report:**

   ## Implementation Batch — Complete (part 1 of 2)

   **Features implemented:** spot-check (Phase 1 — `commands/spot-check.md`)
   **Total phases completed (this part):** 1
   **Total commits:** [N]
   **Final quality-gate status:** frontmatter valid; no leakage; green

   **Commit log:**
   | Commit | Feature | Phase | Title |
   |--------|---------|-------|-------|
   | [hash] | spot-check | P1 | Author the spot-check command |

   **Implementation Notes summary:** [key facts the part-2 executor needs: the exact `argument-hint` string authored, and the timestamped artifact naming `PR-{id}-spot-{YYYY-MM-DD-HHMM}.md` — Phase 2 docs must mirror both.]

3. **Hand off to part 2:** `/execute-plan C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/docs/specs/spot-check/plans/all-phases-spot-check-part-2.md`
