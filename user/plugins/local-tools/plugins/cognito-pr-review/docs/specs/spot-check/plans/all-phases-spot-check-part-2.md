---
kind: implementation-plan
feature_id: spot-check
status: Complete
created: 2026-06-29
complexity: mechanical
phases: [2]
---

> **Mobile plan** — generated by `/write-plan` on 2026-06-29.
> To execute: `/execute-plan C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/docs/specs/spot-check/plans/all-phases-spot-check-part-2.md`
> This plan is fully self-contained. The executing session needs no additional context.

> **Plan series:** part 2 of 2. Sibling parts:
> - part 1: `C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/docs/specs/spot-check/plans/all-phases-spot-check-part-1.md`
> - part 2: `C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/docs/specs/spot-check/plans/all-phases-spot-check-part-2.md` (this file)
>
> Execute parts strictly in order. Each part is self-contained — do NOT cross-reference siblings during execution. **Part 1 must be complete before this part** (this part's docs mirror the `argument-hint` string authored in part 1's `commands/spot-check.md`).

# Implementation Plan — spot-check (Phase 2: Integrate & document)  (v2)

**PHASES.md files:**
- `C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/docs/specs/spot-check/PHASES.md` (spot-check, 2 phases; this part covers Phase 2)

**SPEC.md files:**
- `C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/docs/specs/spot-check/SPEC.md`

**Total phases:** 2 (this part: Phase 2 only)
**Plan version:** v2 (reference-based — components loaded from disk per step)

---

## REPO & TASK CONTEXT — READ THIS BEFORE ANYTHING ELSE

**This is NOT the Cognito Forms .NET repo.** This part makes **mechanical docs/metadata edits** inside the `claude-config` repo:

- **Plugin root:** `C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/`
- **Files touched:** `README.md`, `CLAUDE.md` (the plugin's own), `.claude-plugin/plugin.json`. **No code, no command logic.**
- **No build system, no test harness.** No `/msbuild`, `/mstest`, `/nxbuild`, `dotnet`, vitest. PHASES.md marks this feature **non-TDD**.
- **Quality gate here = well-formed JSON** (for `plugin.json`) + **no-drift check** (README/CLAUDE examples match the part-1 `argument-hint`). Plugin reload / command-listing is **deferred runtime verification** observed by a human — NOT a work unit.
- **Git identity:** `claude-config` uses the **personal** identity by default; it is **NOT** a Cognito work repo, so the work-repo push hook does **NOT** apply. Commit + push are allowed.
- **Dependency on part 1:** the canonical `argument-hint` string lives in `commands/spot-check.md` (authored in part 1). Read it first; the README block and CLAUDE row mirror it verbatim. If `commands/spot-check.md` does not exist, STOP (part 1 not complete) → Blocking Issue Protocol.

## EXECUTION MODEL — READ THIS FIRST

This plan uses an **orchestrator + Sonnet subagent** architecture:

| Role | What it does | Allowed tools |
|------|-------------|---------------|
| **Orchestrator (you)** | Read plan, compose Agent prompts, dispatch subagents, review output, run quality gates, update tracking docs | `Agent`, `Read`, `Bash` (gates only), `TaskCreate`/`TaskUpdate` |
| **Sonnet subagent** | Write ALL authored files (here: `README.md`, `plugin.json`) | `Edit`, `Write`, `Read`, `Bash`, `Grep`, `Glob` |

**HARD CONSTRAINT:** You MUST NOT call `Edit`/`Write` on `README.md` or `.claude-plugin/plugin.json` — delegate to Sonnet subagents. The ONLY files you may modify directly: `PHASES.md` and `CLAUDE.md` (the plugin's own). Because the plugin `CLAUDE.md` is in the orchestrator-editable set, WU-2 (the CLAUDE.md row) MAY be applied by you directly OR delegated; prefer delegating for consistency, but a direct one-line table-row edit is acceptable under the trivial-edit exception.

**Dispatch pattern:** `Agent({ description: "...", model: "sonnet", prompt: "<FULL self-contained context — subagent has zero prior context>" })`

## COMPONENT LOADING PROTOCOL

Before executing each step, `Read` the component files listed for that step from disk. Do NOT proceed from memory. After compaction, re-read this plan first, then the components for your current step.

## Component Reference Card

| Step | Component | Path |
|------|-----------|------|
| Step 0 | Task Tracking | `~/.claude/skills/_components/task-tracking.md` |
| Step B.0 | Source Re-read | `~/.claude/skills/_components/source-reread.md` |
| Step B.1 | TDD Protocol | `~/.claude/skills/_components/tdd-protocol.md` |
| Step B.1 | Subagent Launch | `~/.claude/skills/_components/subagent-launch.md` |
| Step B.1 | Impl Agent Briefing | `~/.claude/skills/_components/implementation-agent.md` |
| Step B.2 | Subagent Review | `~/.claude/skills/_components/subagent-review.md` |
| Step B.3 | PHASES.md Update | `~/.claude/skills/_components/phases-update.md` |
| Step B.4 | Quality Gates | `~/.claude/skills/_components/quality-gates.md` (see repo-specific override below) |
| Step B.5 | Commit Policy | `~/.claude/skills/_components/commit-and-push.md` |
| Post-phase | Integration Verification | `~/.claude/skills/_components/integration-verification.md` |
| Post-phase | CLAUDE.md Review | `~/.claude/skills/_components/claude-md-review.md` |

> **TDD note:** Non-TDD (docs/metadata). The TDD gate resolves to **direct implementation**; skip `tdd-test-agent.md`.

## References — Upstream Artifacts

`(none — this plan has no completed hard upstream dependencies)` — the SPEC's only dependency (`cognito-pr-review-v2`) is `composes`, not `hard`.

**Intra-feature dependency:** part 1's `commands/spot-check.md` (its `argument-hint` is the source these docs mirror).

## MANDATORY RULES — DO NOT SKIP ANY STEP

1. **Authoring of `README.md` and `plugin.json` MUST be delegated to Sonnet subagents** (the `CLAUDE.md` row may be a direct trivial edit). The orchestrator MUST NOT `Edit`/`Write` `README.md` or `plugin.json`.
2. All subagent edits happen in the current worktree — NEVER create worktrees.
3. Non-TDD: direct implementation, no test-first pipeline.
4. PHASES.md updated AFTER the batch completes.
5. Subagent output reviewed before continuing.
6. Mistakes fixed immediately.
7. After the batch, integration verification confirms docs match the part-1 `argument-hint` (no drift).
8. CLAUDE.md updated as part of this phase's deliverables (WU-2).
9. The completed phase is committed.
10. This plan is self-contained.
11. Before each step, `Read` the listed components from disk.
12. **Do NOT run `/msbuild`, `/mstest`, `/nxbuild`, `dotnet`, or vitest** — see the repo-specific QG override.

## Execution Schedule

| Step | Feature | Phase | Title | Blocked by | Parallel? |
|------|---------|-------|-------|------------|-----------|
| 1 | spot-check | P2 | Integrate & document | spot-check P1 (part 1) | WU-1/2/3 parallel (distinct files) |

---

## Work Units

- [x] WU-1 — README.md: add spot-check usage block + architecture note
- [x] WU-2 — CLAUDE.md: add Key Commands table row
- [x] WU-3 — plugin.json: version bump 2.7.0 → 2.8.0 + description extension

---

## Phase: spot-check P2 — Integrate & document

**Goal:** Surface the new `spot-check` command in the plugin's docs and metadata so it is discoverable and loads correctly.
**Entry criteria:** Phase 1 complete — `commands/spot-check.md` exists; its frontmatter `argument-hint` is the canonical string these docs mirror.
**SPEC.md references:** "User Experience" (Invocation / Scope syntax / Examples) for the README block; "Executive Summary" + "Goals" for the architecture note distinguishing spot-check from review-pr / review-pr-buddy. PHASES.md Phase 2 deliverables.

> **Mechanical-tier justification (Phase 9):** all three WUs are pure doc/metadata edits — a README usage block mirroring existing examples, a one-row table addition, and a version-bump + description-extension on an existing JSON file. No novel design, no algorithm, no cross-boundary wiring. The API being documented (`commands/spot-check.md`) already exists from part 1. Every WU qualifies as mechanical → this part is `complexity: mechanical` (Sonnet).

### WU-1 — README.md: spot-check usage block + architecture note

**Scope (PHASES.md Phase 2 deliverable):**
- [x] `README.md`: add a `spot-check` usage block (mirroring the SPEC examples) and a short architecture note distinguishing it from `review-pr` / `review-pr-buddy` (lighter, inline-first, scope-targetable, standalone).

**TDD:** no.
**Files to create/modify:** `C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/README.md` — edit (exists).
**Implementation goal:** Insert a new `### Spot-Check (Lightweight / Scope-Targeted)` block in the `## Usage` section [VERIFY: grep -n "^## Usage" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/README.md"], placed after the `### Buddy Review` block (~line 98) and before `### Learn from PRs / Calibrate` (~line 100) [VERIFY: grep -n "Buddy Review\|Learn from PRs" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/README.md"]. Mirror the sibling blocks' shape: an `###` heading, a fenced code block of example invocations copied from SPEC "Examples" (the seven `/cognito-pr-review:spot-check …` lines), then a 2–4 sentence description. The description must distinguish spot-check from `review-pr`/`review-pr-buddy`: **lighter** (zero subagent dispatches on a clean small PR; one investigation agent at most), **inline-first**, **scope-targetable** (last-commit / since-review / range / glob / free-text), **standalone** (no ADO, no calibration, no sentinels). Example invocation lines must match SPEC "Examples" and the part-1 command's accepted scope tokens.
**Spec requirements:** SPEC "User Experience → Examples" (verbatim invocation lines) and "Executive Summary"/"Goals" (the distinguishing prose).
**Batch:** 1.

### WU-2 — CLAUDE.md: Key Commands table row

**Scope (PHASES.md Phase 2 deliverable):**
- [x] `CLAUDE.md`: add a Key Commands table row for `/cognito-pr-review:spot-check [PR#] [scope]` with a one-line purpose.

**TDD:** no.
**Files to create/modify:** `C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/CLAUDE.md` — edit (exists). *(Orchestrator-editable; a direct one-line edit is acceptable, or delegate.)*
**Implementation goal:** Add one row to the `## Key Commands` table [VERIFY: grep -n "## Key Commands" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/CLAUDE.md"], inserted immediately after the `review-pr-buddy` row (~line 18) to keep review commands grouped [VERIFY: grep -n "review-pr-buddy" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/CLAUDE.md"]. Match the exact column format `| \`<command>\` | <purpose> |`. Row: `` | `/cognito-pr-review:spot-check [PR#] [scope]` | Fast, inline-first spot-check of a small/scoped PR; ≤1 investigation agent; standalone (no ADO/calibration/sentinels) | ``.
**Spec requirements:** SPEC "Executive Summary" / "Goals" (one-line purpose).
**Batch:** 1.

### WU-3 — plugin.json: version bump + description extension

**Scope (PHASES.md Phase 2 deliverable):**
- [x] `.claude-plugin/plugin.json`: bump `version` `2.7.0` → `2.8.0` and extend the `description` to mention the lightweight spot-check command.

**TDD:** no.
**Files to create/modify:** `C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/.claude-plugin/plugin.json` — edit (exists).
**Implementation goal:** Surgically change two fields in the existing JSON [VERIFY: grep -n "\"version\"\|\"description\"" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/.claude-plugin/plugin.json"]: set `"version": "2.8.0"` (currently `2.7.0`), and append to the existing `description` string (before its closing quote) a clause such as `" Includes a lightweight, scope-targetable spot-check command for fast review of small or narrowly-scoped PRs."`. Preserve all other fields (`name`, `author`) and JSON formatting (2-space indent). The result MUST be valid JSON.
**Spec requirements:** SPEC "Executive Summary" (the spot-check description).
**Batch:** 1.

| Batch | Work Units | Parallel? | File Conflicts? |
|-------|-----------|-----------|-----------------|
| 1     | WU-1, WU-2, WU-3 | Yes (three distinct files) | None |

---

## Execution Protocol

### Phase Selection Loop

This part contains one phase (spot-check P2) with one batch of three file-disjoint WUs that run in parallel. Execute Step 0, then the Per-Batch Steps for Batch 1, then the Post-Phase Steps, then Completion. On a blocking issue, jump to the Blocking Issue Protocol.

1. **Verify entry criteria:** Confirm `commands/spot-check.md` exists and read its frontmatter `argument-hint`. If missing → Blocking Issue Protocol (part 1 not complete).
2. **Announce:** Print "Implementing spot-check Phase 2: Integrate & document".
3. **Review prior context:** Re-read SPEC.md (User Experience, Executive Summary, Goals) and PHASES.md Phase 2 from disk.
4. **Execute Batch 1** per the Per-Batch Steps.
5. **Run Post-Phase Steps.**
6. **Report:** Print "spot-check Phase 2: Integrate & document — committed as [hash]".

### Step 0: Initialize Task Tracking (MANDATORY PREREQUISITE)

Read `~/.claude/skills/_components/task-tracking.md` and follow it. Create tasks for WU-1, WU-2, WU-3.

### Per-Batch Steps

#### Step B.0: Re-read Source Documents (MANDATORY)

Read `~/.claude/skills/_components/source-reread.md` and follow it. Re-read PHASES.md Phase 2, SPEC.md (User Experience / Examples), this plan, **and `commands/spot-check.md` from part 1** (extract the exact `argument-hint` and the accepted scope tokens — the docs must not drift from it).

#### Step B.1: Launch Subagents (COMPOSE Agent TOOL CALLS)

**PRE-FLIGHT CHECK:** You will use `Agent` with `model: "sonnet"` for `README.md` (WU-1) and `plugin.json` (WU-3). WU-2 (`CLAUDE.md` row) may be a direct trivial edit OR delegated.

Read:
1. `~/.claude/skills/_components/tdd-protocol.md` — resolves to direct implementation (non-TDD).
2. `~/.claude/skills/_components/subagent-launch.md` — launch orchestration + recovery.
3. `~/.claude/skills/_components/implementation-agent.md` — impl-agent template (embed verbatim).

Dispatch the three WUs in parallel (distinct files, no conflict). Each agent prompt MUST embed: the WU's Implementation goal above (with `[VERIFY: …]` facts), the **exact `argument-hint` string read from `commands/spot-check.md`**, and the relevant SPEC excerpts (Examples for WU-1; Executive Summary for WU-3). Tell each agent: markdown/JSON only, no code, no tests; preserve surrounding formatting exactly.

**POST-DISPATCH GATE:** Verify `README.md` and `plugin.json` were authored via `Agent`, not direct orchestrator edits.

#### Step B.2: Review Batch Output (MANDATORY GATE)

Read `~/.claude/skills/_components/subagent-review.md` and follow its protocol. Produce a structured verdict. Verify:
- README block: heading + example lines match SPEC Examples; example invocations and scope tokens match `commands/spot-check.md`'s `argument-hint` (no drift); distinguishing prose present.
- CLAUDE row: correct column format, placed after `review-pr-buddy`, command + scope notation matches.
- plugin.json: `version` is `2.8.0`; description extended; **valid JSON**; no other field altered.

#### Step B.3: Update PHASES.md (MANDATORY)

Read `~/.claude/skills/_components/phases-update.md` and follow it. Check off the three Phase 2 **implementation** deliverables. Do NOT check the `<!-- verification-only -->` Runtime Verification rows (closed by a plugin reload / inspection, not by this implementation). Add an Implementation Notes block.

#### Step B.4: Run Quality Gates (MANDATORY) — REPO-SPECIFIC OVERRIDE

Read `~/.claude/skills/_components/quality-gates.md` for the general protocol; apply this override (no .NET/nx build):

1. **JSON validity** (`plugin.json`):
   ```bash
   python -m json.tool "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/.claude-plugin/plugin.json" > /dev/null && echo "plugin.json valid"
   ```
   Confirm `"version": "2.8.0"` is present:
   ```bash
   grep -n "\"version\": \"2.8.0\"" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/.claude-plugin/plugin.json"
   ```
2. **No-drift check:** confirm the README example lines and CLAUDE row use the same scope tokens/command spelling as `commands/spot-check.md`'s `argument-hint`. A quick spelling check:
   ```bash
   grep -n "cognito-pr-review:spot-check" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/README.md" "C:/Users/JacobMadsen/source/repos/claude-config/user/plugins/local-tools/plugins/cognito-pr-review/CLAUDE.md"
   ```

**Do NOT** run `/msbuild`, `/mstest`, `/nxbuild`, `dotnet`, or vitest. A JSON parse failure or a drift mismatch is a gate failure — route the fix back through a Sonnet subagent.

#### Step B.4.5: MCP Integration Test — N/A

PHASES.md Phase 2 declares **"MCP Integration Test Assertions: N/A — docs/metadata only."** Skip with a note.

#### Step B.5: Commit Batch

Read `~/.claude/skills/_components/commit-and-push.md` and follow it. `claude-config` repo (personal identity; no work-repo push hook). Stage the touched files (`README.md`, `CLAUDE.md`, `.claude-plugin/plugin.json`, PHASES.md) and commit, e.g. `docs(cognito-pr-review): document spot-check command + bump to 2.8.0`.

#### Step B.6: Proceed

**Checklist (all must be true):**
- [ ] Review report with a verdict
- [ ] PHASES.md updated (implementation deliverables checked; verification-only rows left unchecked)
- [ ] Repo-specific quality gates pass (JSON valid; version 2.8.0; no drift)
- [ ] Batch committed

### Propagation Awareness Note

No import indirection or shared-struct field additions — three independent doc/metadata edits. No full-suite run needed.

### Post-Phase Steps (after the batch)

#### Integration Verification (MANDATORY)

Read `~/.claude/skills/_components/integration-verification.md` and follow it. Confirm the README usage block, CLAUDE row, and the part-1 command's `argument-hint` are mutually consistent (no scope-token or command-name drift across the three surfaces).

#### Update CLAUDE.md Files (MANDATORY)

Read `~/.claude/skills/_components/claude-md-review.md`. The plugin `CLAUDE.md` Key Commands row is already handled by WU-2. Confirm no further CLAUDE.md updates are warranted; record the outcome.

#### Commit and Push Post-Phase Changes

Read `~/.claude/skills/_components/commit-and-push.md` and follow it for any residual post-phase changes.

---

## Blocking Issue Protocol

If a blocking issue is encountered:

1. **Stop all in-progress work.** Do not launch new subagents.
2. **Commit any completed work** not yet committed.
3. **Print a blocking-issue report:**

   ## Implementation Batch — Blocked

   **Completed phases:** [list with commit hashes]
   **Blocked phase:** spot-check Phase 2: Integrate & document
   **Reason:** [specific description]
   **Recovery suggestion:** [what the user should do]

   **Remaining phases (not attempted):** none (this is the final part)

4. **Do not work around the blocker.** The user resolves and re-triggers.

Blocking issues include: `commands/spot-check.md` missing (part 1 not complete) — STOP and tell the user to run part 1 first; a subagent failure unfixable after 2 retries; a JSON-validity or no-drift failure unfixable after 2 retries; any reuse-anchor that no longer exists (codebase drifted — report).

---

## Completion

When Phase 2 is complete:

1. **Re-run the repo-specific quality gates** (JSON valid + version 2.8.0 + no-drift) one final time.
2. **Print a completion report:**

   ## Implementation Batch — Complete (part 2 of 2 — feature complete)

   **Features implemented:** spot-check (Phase 2 — README + CLAUDE.md + plugin.json)
   **Total phases completed (this part):** 1
   **Total commits:** [N]
   **Final quality-gate status:** JSON valid; version 2.8.0; docs match argument-hint; green

   **Commit log:**
   | Commit | Feature | Phase | Title |
   |--------|---------|-------|-------|
   | [hash] | spot-check | P2 | Integrate & document |

   **Feature status:** Both phases (part 1 + part 2) are now implemented. The remaining `<!-- verification-only -->` Runtime Verification rows in PHASES.md (a real `/cognito-pr-review:spot-check` run + plugin reload) are closed by a human/observed run — NOT by this plan. Flipping the SPEC `**Status:**` to Complete is gate-owned per PHASES.md and is NOT a checkbox here.

   **Implementation Notes summary:** [note the final version `2.8.0` and confirm README/CLAUDE/argument-hint are aligned.]
