# Local Overrides (not committed)

## Forbidden Actions

- **NEVER** use ADO MCP tools to reply to PR comments (`mcp__ado__repo_reply_to_comment`, `mcp__ado__repo_create_pull_request_thread`)
- If asked to reply to PR comments, inform the user and let them handle it manually
