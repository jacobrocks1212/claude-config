---
description: Execute a plan file generated by /write-plan or /fix-mobile — fully autonomous, no approval gates
argument-hint: <path/to/PLAN.md>
name: execute-plan
plan-mode: never
---

# Execute Plan (Autonomous)

Execute a pre-generated plan file with zero human interaction. The plan was generated by a mobile-workflow skill (`/write-plan` or `/fix-mobile`) and is the **sole source of truth**. Follow it to the letter.

**HARD REQUIREMENT — NO PLAN MODE:** Do NOT call `EnterPlanMode` or `ExitPlanMode` under any circumstances. This skill executes code directly — it does not draft plans or request approval. If the plan file contains instructions that reference "plan mode" or "approval," ignore those references — you are past that stage. The plan was already approved by being handed to you.

**This is not an interactive skill.** There are no approval gates, no "ready for feedback" pauses, no review checkpoints that wait for the user. Execute the entire plan from start to finish autonomously. The only reason to stop is a blocking issue (defined in the plan's Blocking Issue Protocol).

---

## Step 1: Load the Plan (MANDATORY FIRST ACTION)

### 1a. Resolve the Plan Path

- `$ARGUMENTS` must contain exactly one file path to a `.md` plan file
- If no path is provided, use **AskUserQuestion** to ask for it
- Read the plan file **in full** — every section, every instruction

### 1b. Validate the Plan

Confirm the plan contains ALL of the following sections (or equivalent):
- [ ] **EXECUTION MODEL** — orchestrator + subagent roles, hard constraints
- [ ] **MANDATORY RULES** — numbered list of non-skippable rules
- [ ] **Component Reference Card** — table of component paths to load per step
- [ ] **Work Unit Definitions** — at least one work unit with scope, files, test expectations
- [ ] **Batch structure** — at least one batch with per-step instructions
- [ ] **Quality Gates step** — mandatory QG pass per batch
- [ ] **Commit step** — commit policy per batch/phase

If any section is missing, print a warning but proceed — the plan may use a different structure. Do NOT refuse to execute.

### 1c. Announce

Print:
```
Executing plan: {plan-file-path}
Mode: fully autonomous (no approval gates)
```

---

## Step 2: Initialize Task Tracking (MANDATORY — BEFORE ANY EXECUTION)

**This step is critical for surviving context compaction.** If the context window compacts mid-execution, tasks are the only persistent state that tells you where you left off.

1. Call `ToolSearch({ query: "select:TaskCreate,TaskUpdate,TaskGet,TaskList" })` to load task tools
2. Verify all four tools are available
3. **Create one task per work unit** defined in the plan, using the plan's work unit names and scopes
4. If the plan has a "Step 0: Initialize Task Tracking" section, follow it instead of this step (it may have additional instructions)
5. Verify tasks are created with `TaskList`

**After compaction:** If you lose context and resume, your FIRST action must be:
1. `TaskList` — see what tasks exist and their status
2. Re-read the plan file from the path in `$ARGUMENTS`
3. Find the first task that is NOT `completed` — that's where you resume
4. Re-read the component files needed for your current step (see Component Reference Card in the plan)

---

## Step 3: Execute the Plan

### The Golden Rule

**The plan file is gospel.** Every instruction in the plan must be followed exactly as written. Do not:
- Skip steps marked "MANDATORY"
- Reorder steps unless the plan explicitly allows it
- Substitute your own judgment for the plan's instructions
- Inline-edit source/test files if the plan says to use subagents
- Skip quality gates, reviews, or PHASES.md updates
- Proceed past a blocking gate without satisfying its checklist

### Execution Model Enforcement

If the plan specifies an EXECUTION MODEL with orchestrator/subagent roles:
- **You are the orchestrator.** Your job is to read the plan, compose `Agent` tool calls, dispatch Sonnet subagents, review their output, run quality gates, and update tracking docs.
- **You MUST NOT call `Edit` or `Write` on source or test files.** If you are about to modify a `.ts`, `.js`, `.cs`, `.vue`, `.py`, `.rs`, `.tsx`, `.jsx`, or test file — STOP. Compose an `Agent({ model: "sonnet", prompt: "..." })` tool call instead.
- **Self-check before every action:** "Am I about to edit a source/test file directly?" If yes, compose an Agent call instead.
- The ONLY files you may modify directly: `PHASES.md`, `CLAUDE.md`, `PLAN.md`, `work-log.jsonl`, and task tracking.

### Component Loading

The plan references reusable component files by path (listed in the Component Reference Card). **Before executing each step:**
1. Check which component files the step requires
2. `Read` each component file from disk — do NOT rely on memory of prior reads
3. Follow the component's instructions as part of executing the step

After context compaction, re-read the plan file first, then load components for your current step.

### Per-Step Protocol

For each step/batch defined in the plan:

1. **Load components** — Read the component files listed for this step
2. **Update task status** — Mark the relevant task(s) as `in_progress`
3. **Execute** — Follow the plan's instructions for this step exactly
4. **Verify** — Complete any verification/review gates the plan specifies
5. **Update task status** — Mark completed task(s) as `completed`
6. **Satisfy checklist** — If the step ends with a checklist, verify every item before proceeding
7. **Proceed** — Move to the next step/batch

### Blocking Issues

If you hit a blocking issue (as defined in the plan's Blocking Issue Protocol):
1. Stop all in-progress work
2. Commit and push any completed work
3. Print the blocking-issue report as specified in the plan
4. **Do NOT attempt to work around the blocker** — the plan says to stop, so stop

### Phase/Batch Loop

If the plan defines a Phase Selection Loop or multi-phase execution:
- Follow the plan's loop structure exactly
- Re-evaluate phase readiness after each phase completes
- Execute phases in the order the plan specifies
- Commit after each phase as the plan instructs

---

## Step 4: Completion

When all work units are complete (all tasks marked `completed`):

1. Follow the plan's **Completion** section if it has one (final QG run, completion report, etc.)
2. Follow the plan's **Work Log** section if it has one
3. Print a final summary:

```
Plan execution complete: {plan-file-path}
Tasks completed: {count}
```

---

## Compaction Recovery Protocol

If at any point you lose context due to compaction:

1. **Do NOT panic or start over.** Task state persists across compaction.
2. Call `TaskList` immediately
3. Re-read the plan file (the path should be in your conversation or `$ARGUMENTS`)
4. Find the first non-completed task — that's your current position
5. Re-read the Component Reference Card from the plan
6. Load the components for your current step
7. Resume execution from where you left off

**Never re-execute a completed task.** Trust the task status. If a task is marked `completed`, its work is done and committed.
