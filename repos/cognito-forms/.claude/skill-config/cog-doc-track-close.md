Close work-dashboard tracking for the current item: run `python ~/.claude/scripts/track-work.py close` (removes the item's `WIP.md` liveness marker, then regenerates DASHBOARD.md so it drops off the In Flight panel immediately). Non-fatal — exits 0 and writes nothing if no cog-docs item resolves; never block on its result.

Pass what you know: add `--wi-id <id>` when this work references a work item (bare number, `#id`, `AB#id`, or ADO URL), or `--slug <dir-name>` when you know the item's cog-docs directory name (e.g. `56650-save-crash`). Run bare only as a last resort — it then resolves from the current `p/<id>-` git branch.
