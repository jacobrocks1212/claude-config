### housing-locator Quality Gates

Run the relevant quality gates for changes:
- TypeScript/Node changes → `npm test` and `npm run lint`
- Data quality checks → `npm run validate-data` (if it exists)
